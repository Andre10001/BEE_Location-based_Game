var VM = Object.defineProperty;
var qM = (r, t, i) => t in r ? VM(r, t, { enumerable: !0, configurable: !0, writable: !0, value: i }) : r[t] = i;
var qC = (r, t, i) => qM(r, typeof t != "symbol" ? t + "" : t, i);
import ql, { useState as Fl, useRef as $b, useEffect as r2 } from "react";
import u2 from "react-dom";
function ZM(r) {
  return r && r.__esModule && Object.prototype.hasOwnProperty.call(r, "default") ? r.default : r;
}
var Ab = { exports: {} }, Z0 = {};
/**
 * @license React
 * react-jsx-runtime.production.js
 *
 * Copyright (c) Meta Platforms, Inc. and affiliates.
 *
 * This source code is licensed under the MIT license found in the
 * LICENSE file in the root directory of this source tree.
 */
var ZC;
function FM() {
  if (ZC) return Z0;
  ZC = 1;
  var r = Symbol.for("react.transitional.element"), t = Symbol.for("react.fragment");
  function i(a, u, h) {
    var g = null;
    if (h !== void 0 && (g = "" + h), u.key !== void 0 && (g = "" + u.key), "key" in u) {
      h = {};
      for (var y in u)
        y !== "key" && (h[y] = u[y]);
    } else h = u;
    return u = h.ref, {
      $$typeof: r,
      type: a,
      key: g,
      ref: u !== void 0 ? u : null,
      props: h
    };
  }
  return Z0.Fragment = t, Z0.jsx = i, Z0.jsxs = i, Z0;
}
var F0 = {};
/**
 * @license React
 * react-jsx-runtime.development.js
 *
 * Copyright (c) Meta Platforms, Inc. and affiliates.
 *
 * This source code is licensed under the MIT license found in the
 * LICENSE file in the root directory of this source tree.
 */
var FC;
function KM() {
  return FC || (FC = 1, process.env.NODE_ENV !== "production" && function() {
    function r(F) {
      if (F == null) return null;
      if (typeof F == "function")
        return F.$$typeof === Et ? null : F.displayName || F.name || null;
      if (typeof F == "string") return F;
      switch (F) {
        case H:
          return "Fragment";
        case O:
          return "Profiler";
        case N:
          return "StrictMode";
        case Z:
          return "Suspense";
        case dt:
          return "SuspenseList";
        case it:
          return "Activity";
      }
      if (typeof F == "object")
        switch (typeof F.tag == "number" && console.error(
          "Received an unexpected object in getComponentNameFromType(). This is likely a bug in React. Please file an issue."
        ), F.$$typeof) {
          case L:
            return "Portal";
          case I:
            return (F.displayName || "Context") + ".Provider";
          case U:
            return (F._context.displayName || "Context") + ".Consumer";
          case X:
            var St = F.render;
            return F = F.displayName, F || (F = St.displayName || St.name || "", F = F !== "" ? "ForwardRef(" + F + ")" : "ForwardRef"), F;
          case ft:
            return St = F.displayName || null, St !== null ? St : r(F.type) || "Memo";
          case yt:
            St = F._payload, F = F._init;
            try {
              return r(F(St));
            } catch {
            }
        }
      return null;
    }
    function t(F) {
      return "" + F;
    }
    function i(F) {
      try {
        t(F);
        var St = !1;
      } catch {
        St = !0;
      }
      if (St) {
        St = console;
        var Q = St.error, At = typeof Symbol == "function" && Symbol.toStringTag && F[Symbol.toStringTag] || F.constructor.name || "Object";
        return Q.call(
          St,
          "The provided key is an unsupported type %s. This value must be coerced to a string before using it here.",
          At
        ), t(F);
      }
    }
    function a(F) {
      if (F === H) return "<>";
      if (typeof F == "object" && F !== null && F.$$typeof === yt)
        return "<...>";
      try {
        var St = r(F);
        return St ? "<" + St + ">" : "<...>";
      } catch {
        return "<...>";
      }
    }
    function u() {
      var F = xt.A;
      return F === null ? null : F.getOwner();
    }
    function h() {
      return Error("react-stack-top-frame");
    }
    function g(F) {
      if (pt.call(F, "key")) {
        var St = Object.getOwnPropertyDescriptor(F, "key").get;
        if (St && St.isReactWarning) return !1;
      }
      return F.key !== void 0;
    }
    function y(F, St) {
      function Q() {
        Rt || (Rt = !0, console.error(
          "%s: `key` is not a prop. Trying to access it will result in `undefined` being returned. If you need to access the same value within the child component, you should pass it as a different prop. (https://react.dev/link/special-props)",
          St
        ));
      }
      Q.isReactWarning = !0, Object.defineProperty(F, "key", {
        get: Q,
        configurable: !0
      });
    }
    function _() {
      var F = r(this.type);
      return Y[F] || (Y[F] = !0, console.error(
        "Accessing element.ref was removed in React 19. ref is now a regular prop. It will be removed from the JSX Element type in a future release."
      )), F = this.props.ref, F !== void 0 ? F : null;
    }
    function b(F, St, Q, At, Ut, kt, Xt, Ze) {
      return Q = kt.ref, F = {
        $$typeof: w,
        type: F,
        key: St,
        props: kt,
        _owner: Ut
      }, (Q !== void 0 ? Q : null) !== null ? Object.defineProperty(F, "ref", {
        enumerable: !1,
        get: _
      }) : Object.defineProperty(F, "ref", { enumerable: !1, value: null }), F._store = {}, Object.defineProperty(F._store, "validated", {
        configurable: !1,
        enumerable: !1,
        writable: !0,
        value: 0
      }), Object.defineProperty(F, "_debugInfo", {
        configurable: !1,
        enumerable: !1,
        writable: !0,
        value: null
      }), Object.defineProperty(F, "_debugStack", {
        configurable: !1,
        enumerable: !1,
        writable: !0,
        value: Xt
      }), Object.defineProperty(F, "_debugTask", {
        configurable: !1,
        enumerable: !1,
        writable: !0,
        value: Ze
      }), Object.freeze && (Object.freeze(F.props), Object.freeze(F)), F;
    }
    function E(F, St, Q, At, Ut, kt, Xt, Ze) {
      var _e = St.children;
      if (_e !== void 0)
        if (At)
          if (Tt(_e)) {
            for (At = 0; At < _e.length; At++)
              x(_e[At]);
            Object.freeze && Object.freeze(_e);
          } else
            console.error(
              "React.jsx: Static children should always be an array. You are likely explicitly calling React.jsxs or React.jsxDEV. Use the Babel transform instead."
            );
        else x(_e);
      if (pt.call(St, "key")) {
        _e = r(F);
        var De = Object.keys(St).filter(function(yl) {
          return yl !== "key";
        });
        At = 0 < De.length ? "{key: someKey, " + De.join(": ..., ") + ": ...}" : "{key: someKey}", vt[_e + At] || (De = 0 < De.length ? "{" + De.join(": ..., ") + ": ...}" : "{}", console.error(
          `A props object containing a "key" prop is being spread into JSX:
  let props = %s;
  <%s {...props} />
React keys must be passed directly to JSX without using spread:
  let props = %s;
  <%s key={someKey} {...props} />`,
          At,
          _e,
          De,
          _e
        ), vt[_e + At] = !0);
      }
      if (_e = null, Q !== void 0 && (i(Q), _e = "" + Q), g(St) && (i(St.key), _e = "" + St.key), "key" in St) {
        Q = {};
        for (var ke in St)
          ke !== "key" && (Q[ke] = St[ke]);
      } else Q = St;
      return _e && y(
        Q,
        typeof F == "function" ? F.displayName || F.name || "Unknown" : F
      ), b(
        F,
        _e,
        kt,
        Ut,
        u(),
        Q,
        Xt,
        Ze
      );
    }
    function x(F) {
      typeof F == "object" && F !== null && F.$$typeof === w && F._store && (F._store.validated = 1);
    }
    var R = ql, w = Symbol.for("react.transitional.element"), L = Symbol.for("react.portal"), H = Symbol.for("react.fragment"), N = Symbol.for("react.strict_mode"), O = Symbol.for("react.profiler"), U = Symbol.for("react.consumer"), I = Symbol.for("react.context"), X = Symbol.for("react.forward_ref"), Z = Symbol.for("react.suspense"), dt = Symbol.for("react.suspense_list"), ft = Symbol.for("react.memo"), yt = Symbol.for("react.lazy"), it = Symbol.for("react.activity"), Et = Symbol.for("react.client.reference"), xt = R.__CLIENT_INTERNALS_DO_NOT_USE_OR_WARN_USERS_THEY_CANNOT_UPGRADE, pt = Object.prototype.hasOwnProperty, Tt = Array.isArray, Nt = console.createTask ? console.createTask : function() {
      return null;
    };
    R = {
      react_stack_bottom_frame: function(F) {
        return F();
      }
    };
    var Rt, Y = {}, K = R.react_stack_bottom_frame.bind(
      R,
      h
    )(), nt = Nt(a(h)), vt = {};
    F0.Fragment = H, F0.jsx = function(F, St, Q, At, Ut) {
      var kt = 1e4 > xt.recentlyCreatedOwnerStacks++;
      return E(
        F,
        St,
        Q,
        !1,
        At,
        Ut,
        kt ? Error("react-stack-top-frame") : K,
        kt ? Nt(a(F)) : nt
      );
    }, F0.jsxs = function(F, St, Q, At, Ut) {
      var kt = 1e4 > xt.recentlyCreatedOwnerStacks++;
      return E(
        F,
        St,
        Q,
        !0,
        At,
        Ut,
        kt ? Error("react-stack-top-frame") : K,
        kt ? Nt(a(F)) : nt
      );
    };
  }()), F0;
}
process.env.NODE_ENV === "production" ? Ab.exports = FM() : Ab.exports = KM();
var ot = Ab.exports, Mb = { exports: {} }, K0 = {}, x1 = { exports: {} }, FE = {};
/**
 * @license React
 * scheduler.production.js
 *
 * Copyright (c) Meta Platforms, Inc. and affiliates.
 *
 * This source code is licensed under the MIT license found in the
 * LICENSE file in the root directory of this source tree.
 */
var KC;
function PM() {
  return KC || (KC = 1, function(r) {
    function t(Y, K) {
      var nt = Y.length;
      Y.push(K);
      t: for (; 0 < nt; ) {
        var vt = nt - 1 >>> 1, F = Y[vt];
        if (0 < u(F, K))
          Y[vt] = K, Y[nt] = F, nt = vt;
        else break t;
      }
    }
    function i(Y) {
      return Y.length === 0 ? null : Y[0];
    }
    function a(Y) {
      if (Y.length === 0) return null;
      var K = Y[0], nt = Y.pop();
      if (nt !== K) {
        Y[0] = nt;
        t: for (var vt = 0, F = Y.length, St = F >>> 1; vt < St; ) {
          var Q = 2 * (vt + 1) - 1, At = Y[Q], Ut = Q + 1, kt = Y[Ut];
          if (0 > u(At, nt))
            Ut < F && 0 > u(kt, At) ? (Y[vt] = kt, Y[Ut] = nt, vt = Ut) : (Y[vt] = At, Y[Q] = nt, vt = Q);
          else if (Ut < F && 0 > u(kt, nt))
            Y[vt] = kt, Y[Ut] = nt, vt = Ut;
          else break t;
        }
      }
      return K;
    }
    function u(Y, K) {
      var nt = Y.sortIndex - K.sortIndex;
      return nt !== 0 ? nt : Y.id - K.id;
    }
    if (r.unstable_now = void 0, typeof performance == "object" && typeof performance.now == "function") {
      var h = performance;
      r.unstable_now = function() {
        return h.now();
      };
    } else {
      var g = Date, y = g.now();
      r.unstable_now = function() {
        return g.now() - y;
      };
    }
    var _ = [], b = [], E = 1, x = null, R = 3, w = !1, L = !1, H = !1, N = !1, O = typeof setTimeout == "function" ? setTimeout : null, U = typeof clearTimeout == "function" ? clearTimeout : null, I = typeof setImmediate < "u" ? setImmediate : null;
    function X(Y) {
      for (var K = i(b); K !== null; ) {
        if (K.callback === null) a(b);
        else if (K.startTime <= Y)
          a(b), K.sortIndex = K.expirationTime, t(_, K);
        else break;
        K = i(b);
      }
    }
    function Z(Y) {
      if (H = !1, X(Y), !L)
        if (i(_) !== null)
          L = !0, dt || (dt = !0, pt());
        else {
          var K = i(b);
          K !== null && Rt(Z, K.startTime - Y);
        }
    }
    var dt = !1, ft = -1, yt = 5, it = -1;
    function Et() {
      return N ? !0 : !(r.unstable_now() - it < yt);
    }
    function xt() {
      if (N = !1, dt) {
        var Y = r.unstable_now();
        it = Y;
        var K = !0;
        try {
          t: {
            L = !1, H && (H = !1, U(ft), ft = -1), w = !0;
            var nt = R;
            try {
              e: {
                for (X(Y), x = i(_); x !== null && !(x.expirationTime > Y && Et()); ) {
                  var vt = x.callback;
                  if (typeof vt == "function") {
                    x.callback = null, R = x.priorityLevel;
                    var F = vt(
                      x.expirationTime <= Y
                    );
                    if (Y = r.unstable_now(), typeof F == "function") {
                      x.callback = F, X(Y), K = !0;
                      break e;
                    }
                    x === i(_) && a(_), X(Y);
                  } else a(_);
                  x = i(_);
                }
                if (x !== null) K = !0;
                else {
                  var St = i(b);
                  St !== null && Rt(
                    Z,
                    St.startTime - Y
                  ), K = !1;
                }
              }
              break t;
            } finally {
              x = null, R = nt, w = !1;
            }
            K = void 0;
          }
        } finally {
          K ? pt() : dt = !1;
        }
      }
    }
    var pt;
    if (typeof I == "function")
      pt = function() {
        I(xt);
      };
    else if (typeof MessageChannel < "u") {
      var Tt = new MessageChannel(), Nt = Tt.port2;
      Tt.port1.onmessage = xt, pt = function() {
        Nt.postMessage(null);
      };
    } else
      pt = function() {
        O(xt, 0);
      };
    function Rt(Y, K) {
      ft = O(function() {
        Y(r.unstable_now());
      }, K);
    }
    r.unstable_IdlePriority = 5, r.unstable_ImmediatePriority = 1, r.unstable_LowPriority = 4, r.unstable_NormalPriority = 3, r.unstable_Profiling = null, r.unstable_UserBlockingPriority = 2, r.unstable_cancelCallback = function(Y) {
      Y.callback = null;
    }, r.unstable_forceFrameRate = function(Y) {
      0 > Y || 125 < Y ? console.error(
        "forceFrameRate takes a positive int between 0 and 125, forcing frame rates higher than 125 fps is not supported"
      ) : yt = 0 < Y ? Math.floor(1e3 / Y) : 5;
    }, r.unstable_getCurrentPriorityLevel = function() {
      return R;
    }, r.unstable_next = function(Y) {
      switch (R) {
        case 1:
        case 2:
        case 3:
          var K = 3;
          break;
        default:
          K = R;
      }
      var nt = R;
      R = K;
      try {
        return Y();
      } finally {
        R = nt;
      }
    }, r.unstable_requestPaint = function() {
      N = !0;
    }, r.unstable_runWithPriority = function(Y, K) {
      switch (Y) {
        case 1:
        case 2:
        case 3:
        case 4:
        case 5:
          break;
        default:
          Y = 3;
      }
      var nt = R;
      R = Y;
      try {
        return K();
      } finally {
        R = nt;
      }
    }, r.unstable_scheduleCallback = function(Y, K, nt) {
      var vt = r.unstable_now();
      switch (typeof nt == "object" && nt !== null ? (nt = nt.delay, nt = typeof nt == "number" && 0 < nt ? vt + nt : vt) : nt = vt, Y) {
        case 1:
          var F = -1;
          break;
        case 2:
          F = 250;
          break;
        case 5:
          F = 1073741823;
          break;
        case 4:
          F = 1e4;
          break;
        default:
          F = 5e3;
      }
      return F = nt + F, Y = {
        id: E++,
        callback: K,
        priorityLevel: Y,
        startTime: nt,
        expirationTime: F,
        sortIndex: -1
      }, nt > vt ? (Y.sortIndex = nt, t(b, Y), i(_) === null && Y === i(b) && (H ? (U(ft), ft = -1) : H = !0, Rt(Z, nt - vt))) : (Y.sortIndex = F, t(_, Y), L || w || (L = !0, dt || (dt = !0, pt()))), Y;
    }, r.unstable_shouldYield = Et, r.unstable_wrapCallback = function(Y) {
      var K = R;
      return function() {
        var nt = R;
        R = K;
        try {
          return Y.apply(this, arguments);
        } finally {
          R = nt;
        }
      };
    };
  }(FE)), FE;
}
var KE = {};
/**
 * @license React
 * scheduler.development.js
 *
 * Copyright (c) Meta Platforms, Inc. and affiliates.
 *
 * This source code is licensed under the MIT license found in the
 * LICENSE file in the root directory of this source tree.
 */
var PC;
function QM() {
  return PC || (PC = 1, function(r) {
    process.env.NODE_ENV !== "production" && function() {
      function t() {
        if (Z = !1, it) {
          var Y = r.unstable_now();
          pt = Y;
          var K = !0;
          try {
            t: {
              I = !1, X && (X = !1, ft(Et), Et = -1), U = !0;
              var nt = O;
              try {
                e: {
                  for (g(Y), N = a(w); N !== null && !(N.expirationTime > Y && _()); ) {
                    var vt = N.callback;
                    if (typeof vt == "function") {
                      N.callback = null, O = N.priorityLevel;
                      var F = vt(
                        N.expirationTime <= Y
                      );
                      if (Y = r.unstable_now(), typeof F == "function") {
                        N.callback = F, g(Y), K = !0;
                        break e;
                      }
                      N === a(w) && u(w), g(Y);
                    } else u(w);
                    N = a(w);
                  }
                  if (N !== null) K = !0;
                  else {
                    var St = a(L);
                    St !== null && b(
                      y,
                      St.startTime - Y
                    ), K = !1;
                  }
                }
                break t;
              } finally {
                N = null, O = nt, U = !1;
              }
              K = void 0;
            }
          } finally {
            K ? Tt() : it = !1;
          }
        }
      }
      function i(Y, K) {
        var nt = Y.length;
        Y.push(K);
        t: for (; 0 < nt; ) {
          var vt = nt - 1 >>> 1, F = Y[vt];
          if (0 < h(F, K))
            Y[vt] = K, Y[nt] = F, nt = vt;
          else break t;
        }
      }
      function a(Y) {
        return Y.length === 0 ? null : Y[0];
      }
      function u(Y) {
        if (Y.length === 0) return null;
        var K = Y[0], nt = Y.pop();
        if (nt !== K) {
          Y[0] = nt;
          t: for (var vt = 0, F = Y.length, St = F >>> 1; vt < St; ) {
            var Q = 2 * (vt + 1) - 1, At = Y[Q], Ut = Q + 1, kt = Y[Ut];
            if (0 > h(At, nt))
              Ut < F && 0 > h(kt, At) ? (Y[vt] = kt, Y[Ut] = nt, vt = Ut) : (Y[vt] = At, Y[Q] = nt, vt = Q);
            else if (Ut < F && 0 > h(kt, nt))
              Y[vt] = kt, Y[Ut] = nt, vt = Ut;
            else break t;
          }
        }
        return K;
      }
      function h(Y, K) {
        var nt = Y.sortIndex - K.sortIndex;
        return nt !== 0 ? nt : Y.id - K.id;
      }
      function g(Y) {
        for (var K = a(L); K !== null; ) {
          if (K.callback === null) u(L);
          else if (K.startTime <= Y)
            u(L), K.sortIndex = K.expirationTime, i(w, K);
          else break;
          K = a(L);
        }
      }
      function y(Y) {
        if (X = !1, g(Y), !I)
          if (a(w) !== null)
            I = !0, it || (it = !0, Tt());
          else {
            var K = a(L);
            K !== null && b(
              y,
              K.startTime - Y
            );
          }
      }
      function _() {
        return Z ? !0 : !(r.unstable_now() - pt < xt);
      }
      function b(Y, K) {
        Et = dt(function() {
          Y(r.unstable_now());
        }, K);
      }
      if (typeof __REACT_DEVTOOLS_GLOBAL_HOOK__ < "u" && typeof __REACT_DEVTOOLS_GLOBAL_HOOK__.registerInternalModuleStart == "function" && __REACT_DEVTOOLS_GLOBAL_HOOK__.registerInternalModuleStart(Error()), r.unstable_now = void 0, typeof performance == "object" && typeof performance.now == "function") {
        var E = performance;
        r.unstable_now = function() {
          return E.now();
        };
      } else {
        var x = Date, R = x.now();
        r.unstable_now = function() {
          return x.now() - R;
        };
      }
      var w = [], L = [], H = 1, N = null, O = 3, U = !1, I = !1, X = !1, Z = !1, dt = typeof setTimeout == "function" ? setTimeout : null, ft = typeof clearTimeout == "function" ? clearTimeout : null, yt = typeof setImmediate < "u" ? setImmediate : null, it = !1, Et = -1, xt = 5, pt = -1;
      if (typeof yt == "function")
        var Tt = function() {
          yt(t);
        };
      else if (typeof MessageChannel < "u") {
        var Nt = new MessageChannel(), Rt = Nt.port2;
        Nt.port1.onmessage = t, Tt = function() {
          Rt.postMessage(null);
        };
      } else
        Tt = function() {
          dt(t, 0);
        };
      r.unstable_IdlePriority = 5, r.unstable_ImmediatePriority = 1, r.unstable_LowPriority = 4, r.unstable_NormalPriority = 3, r.unstable_Profiling = null, r.unstable_UserBlockingPriority = 2, r.unstable_cancelCallback = function(Y) {
        Y.callback = null;
      }, r.unstable_forceFrameRate = function(Y) {
        0 > Y || 125 < Y ? console.error(
          "forceFrameRate takes a positive int between 0 and 125, forcing frame rates higher than 125 fps is not supported"
        ) : xt = 0 < Y ? Math.floor(1e3 / Y) : 5;
      }, r.unstable_getCurrentPriorityLevel = function() {
        return O;
      }, r.unstable_next = function(Y) {
        switch (O) {
          case 1:
          case 2:
          case 3:
            var K = 3;
            break;
          default:
            K = O;
        }
        var nt = O;
        O = K;
        try {
          return Y();
        } finally {
          O = nt;
        }
      }, r.unstable_requestPaint = function() {
        Z = !0;
      }, r.unstable_runWithPriority = function(Y, K) {
        switch (Y) {
          case 1:
          case 2:
          case 3:
          case 4:
          case 5:
            break;
          default:
            Y = 3;
        }
        var nt = O;
        O = Y;
        try {
          return K();
        } finally {
          O = nt;
        }
      }, r.unstable_scheduleCallback = function(Y, K, nt) {
        var vt = r.unstable_now();
        switch (typeof nt == "object" && nt !== null ? (nt = nt.delay, nt = typeof nt == "number" && 0 < nt ? vt + nt : vt) : nt = vt, Y) {
          case 1:
            var F = -1;
            break;
          case 2:
            F = 250;
            break;
          case 5:
            F = 1073741823;
            break;
          case 4:
            F = 1e4;
            break;
          default:
            F = 5e3;
        }
        return F = nt + F, Y = {
          id: H++,
          callback: K,
          priorityLevel: Y,
          startTime: nt,
          expirationTime: F,
          sortIndex: -1
        }, nt > vt ? (Y.sortIndex = nt, i(L, Y), a(w) === null && Y === a(L) && (X ? (ft(Et), Et = -1) : X = !0, b(y, nt - vt))) : (Y.sortIndex = F, i(w, Y), I || U || (I = !0, it || (it = !0, Tt()))), Y;
      }, r.unstable_shouldYield = _, r.unstable_wrapCallback = function(Y) {
        var K = O;
        return function() {
          var nt = O;
          O = K;
          try {
            return Y.apply(this, arguments);
          } finally {
            O = nt;
          }
        };
      }, typeof __REACT_DEVTOOLS_GLOBAL_HOOK__ < "u" && typeof __REACT_DEVTOOLS_GLOBAL_HOOK__.registerInternalModuleStop == "function" && __REACT_DEVTOOLS_GLOBAL_HOOK__.registerInternalModuleStop(Error());
    }();
  }(KE)), KE;
}
var QC;
function c2() {
  return QC || (QC = 1, process.env.NODE_ENV === "production" ? x1.exports = PM() : x1.exports = QM()), x1.exports;
}
/**
 * @license React
 * react-dom-client.production.js
 *
 * Copyright (c) Meta Platforms, Inc. and affiliates.
 *
 * This source code is licensed under the MIT license found in the
 * LICENSE file in the root directory of this source tree.
 */
var WC;
function WM() {
  if (WC) return K0;
  WC = 1;
  var r = c2(), t = ql, i = u2;
  function a(l) {
    var o = "https://react.dev/errors/" + l;
    if (1 < arguments.length) {
      o += "?args[]=" + encodeURIComponent(arguments[1]);
      for (var c = 2; c < arguments.length; c++)
        o += "&args[]=" + encodeURIComponent(arguments[c]);
    }
    return "Minified React error #" + l + "; visit " + o + " for the full message or use the non-minified dev environment for full errors and additional helpful warnings.";
  }
  function u(l) {
    return !(!l || l.nodeType !== 1 && l.nodeType !== 9 && l.nodeType !== 11);
  }
  function h(l) {
    var o = l, c = l;
    if (l.alternate) for (; o.return; ) o = o.return;
    else {
      l = o;
      do
        o = l, o.flags & 4098 && (c = o.return), l = o.return;
      while (l);
    }
    return o.tag === 3 ? c : null;
  }
  function g(l) {
    if (l.tag === 13) {
      var o = l.memoizedState;
      if (o === null && (l = l.alternate, l !== null && (o = l.memoizedState)), o !== null) return o.dehydrated;
    }
    return null;
  }
  function y(l) {
    if (h(l) !== l)
      throw Error(a(188));
  }
  function _(l) {
    var o = l.alternate;
    if (!o) {
      if (o = h(l), o === null) throw Error(a(188));
      return o !== l ? null : l;
    }
    for (var c = l, d = o; ; ) {
      var v = c.return;
      if (v === null) break;
      var S = v.alternate;
      if (S === null) {
        if (d = v.return, d !== null) {
          c = d;
          continue;
        }
        break;
      }
      if (v.child === S.child) {
        for (S = v.child; S; ) {
          if (S === c) return y(v), l;
          if (S === d) return y(v), o;
          S = S.sibling;
        }
        throw Error(a(188));
      }
      if (c.return !== d.return) c = v, d = S;
      else {
        for (var A = !1, M = v.child; M; ) {
          if (M === c) {
            A = !0, c = v, d = S;
            break;
          }
          if (M === d) {
            A = !0, d = v, c = S;
            break;
          }
          M = M.sibling;
        }
        if (!A) {
          for (M = S.child; M; ) {
            if (M === c) {
              A = !0, c = S, d = v;
              break;
            }
            if (M === d) {
              A = !0, d = S, c = v;
              break;
            }
            M = M.sibling;
          }
          if (!A) throw Error(a(189));
        }
      }
      if (c.alternate !== d) throw Error(a(190));
    }
    if (c.tag !== 3) throw Error(a(188));
    return c.stateNode.current === c ? l : o;
  }
  function b(l) {
    var o = l.tag;
    if (o === 5 || o === 26 || o === 27 || o === 6) return l;
    for (l = l.child; l !== null; ) {
      if (o = b(l), o !== null) return o;
      l = l.sibling;
    }
    return null;
  }
  var E = Object.assign, x = Symbol.for("react.element"), R = Symbol.for("react.transitional.element"), w = Symbol.for("react.portal"), L = Symbol.for("react.fragment"), H = Symbol.for("react.strict_mode"), N = Symbol.for("react.profiler"), O = Symbol.for("react.provider"), U = Symbol.for("react.consumer"), I = Symbol.for("react.context"), X = Symbol.for("react.forward_ref"), Z = Symbol.for("react.suspense"), dt = Symbol.for("react.suspense_list"), ft = Symbol.for("react.memo"), yt = Symbol.for("react.lazy"), it = Symbol.for("react.activity"), Et = Symbol.for("react.memo_cache_sentinel"), xt = Symbol.iterator;
  function pt(l) {
    return l === null || typeof l != "object" ? null : (l = xt && l[xt] || l["@@iterator"], typeof l == "function" ? l : null);
  }
  var Tt = Symbol.for("react.client.reference");
  function Nt(l) {
    if (l == null) return null;
    if (typeof l == "function")
      return l.$$typeof === Tt ? null : l.displayName || l.name || null;
    if (typeof l == "string") return l;
    switch (l) {
      case L:
        return "Fragment";
      case N:
        return "Profiler";
      case H:
        return "StrictMode";
      case Z:
        return "Suspense";
      case dt:
        return "SuspenseList";
      case it:
        return "Activity";
    }
    if (typeof l == "object")
      switch (l.$$typeof) {
        case w:
          return "Portal";
        case I:
          return (l.displayName || "Context") + ".Provider";
        case U:
          return (l._context.displayName || "Context") + ".Consumer";
        case X:
          var o = l.render;
          return l = l.displayName, l || (l = o.displayName || o.name || "", l = l !== "" ? "ForwardRef(" + l + ")" : "ForwardRef"), l;
        case ft:
          return o = l.displayName || null, o !== null ? o : Nt(l.type) || "Memo";
        case yt:
          o = l._payload, l = l._init;
          try {
            return Nt(l(o));
          } catch {
          }
      }
    return null;
  }
  var Rt = Array.isArray, Y = t.__CLIENT_INTERNALS_DO_NOT_USE_OR_WARN_USERS_THEY_CANNOT_UPGRADE, K = i.__DOM_INTERNALS_DO_NOT_USE_OR_WARN_USERS_THEY_CANNOT_UPGRADE, nt = {
    pending: !1,
    data: null,
    method: null,
    action: null
  }, vt = [], F = -1;
  function St(l) {
    return { current: l };
  }
  function Q(l) {
    0 > F || (l.current = vt[F], vt[F] = null, F--);
  }
  function At(l, o) {
    F++, vt[F] = l.current, l.current = o;
  }
  var Ut = St(null), kt = St(null), Xt = St(null), Ze = St(null);
  function _e(l, o) {
    switch (At(Xt, o), At(kt, l), At(Ut, null), o.nodeType) {
      case 9:
      case 11:
        l = (l = o.documentElement) && (l = l.namespaceURI) ? Ys(l) : 0;
        break;
      default:
        if (l = o.tagName, o = o.namespaceURI)
          o = Ys(o), l = dc(o, l);
        else
          switch (l) {
            case "svg":
              l = 1;
              break;
            case "math":
              l = 2;
              break;
            default:
              l = 0;
          }
    }
    Q(Ut), At(Ut, l);
  }
  function De() {
    Q(Ut), Q(kt), Q(Xt);
  }
  function ke(l) {
    l.memoizedState !== null && At(Ze, l);
    var o = Ut.current, c = dc(o, l.type);
    o !== c && (At(kt, l), At(Ut, c));
  }
  function yl(l) {
    kt.current === l && (Q(Ut), Q(kt)), Ze.current === l && (Q(Ze), Ii._currentValue = nt);
  }
  var no = Object.prototype.hasOwnProperty, Fi = r.unstable_scheduleCallback, Kl = r.unstable_cancelCallback, xi = r.unstable_shouldYield, Dn = r.unstable_requestPaint, yn = r.unstable_now, pl = r.unstable_getCurrentPriorityLevel, io = r.unstable_ImmediatePriority, Pl = r.unstable_UserBlockingPriority, _l = r.unstable_NormalPriority, Sa = r.unstable_LowPriority, Fe = r.unstable_IdlePriority, tf = r.log, Eu = r.unstable_setDisableYieldValue, Ql = null, pn = null;
  function Wl(l) {
    if (typeof tf == "function" && Eu(l), pn && typeof pn.setStrictMode == "function")
      try {
        pn.setStrictMode(Ql, l);
      } catch {
      }
  }
  var Bn = Math.clz32 ? Math.clz32 : iy, bu = Math.log, Tu = Math.LN2;
  function iy(l) {
    return l >>>= 0, l === 0 ? 32 : 31 - (bu(l) / Tu | 0) | 0;
  }
  var lo = 256, vl = 4194304;
  function ni(l) {
    var o = l & 42;
    if (o !== 0) return o;
    switch (l & -l) {
      case 1:
        return 1;
      case 2:
        return 2;
      case 4:
        return 4;
      case 8:
        return 8;
      case 16:
        return 16;
      case 32:
        return 32;
      case 64:
        return 64;
      case 128:
        return 128;
      case 256:
      case 512:
      case 1024:
      case 2048:
      case 4096:
      case 8192:
      case 16384:
      case 32768:
      case 65536:
      case 131072:
      case 262144:
      case 524288:
      case 1048576:
      case 2097152:
        return l & 4194048;
      case 4194304:
      case 8388608:
      case 16777216:
      case 33554432:
        return l & 62914560;
      case 67108864:
        return 67108864;
      case 134217728:
        return 134217728;
      case 268435456:
        return 268435456;
      case 536870912:
        return 536870912;
      case 1073741824:
        return 0;
      default:
        return l;
    }
  }
  function Sl(l, o, c) {
    var d = l.pendingLanes;
    if (d === 0) return 0;
    var v = 0, S = l.suspendedLanes, A = l.pingedLanes;
    l = l.warmLanes;
    var M = d & 134217727;
    return M !== 0 ? (d = M & ~S, d !== 0 ? v = ni(d) : (A &= M, A !== 0 ? v = ni(A) : c || (c = M & ~l, c !== 0 && (v = ni(c))))) : (M = d & ~S, M !== 0 ? v = ni(M) : A !== 0 ? v = ni(A) : c || (c = d & ~l, c !== 0 && (v = ni(c)))), v === 0 ? 0 : o !== 0 && o !== v && !(o & S) && (S = v & -v, c = o & -o, S >= c || S === 32 && (c & 4194048) !== 0) ? o : v;
  }
  function Ki(l, o) {
    return (l.pendingLanes & ~(l.suspendedLanes & ~l.pingedLanes) & o) === 0;
  }
  function _n(l, o) {
    switch (l) {
      case 1:
      case 2:
      case 4:
      case 8:
      case 64:
        return o + 250;
      case 16:
      case 32:
      case 128:
      case 256:
      case 512:
      case 1024:
      case 2048:
      case 4096:
      case 8192:
      case 16384:
      case 32768:
      case 65536:
      case 131072:
      case 262144:
      case 524288:
      case 1048576:
      case 2097152:
        return o + 5e3;
      case 4194304:
      case 8388608:
      case 16777216:
      case 33554432:
        return -1;
      case 67108864:
      case 134217728:
      case 268435456:
      case 536870912:
      case 1073741824:
        return -1;
      default:
        return -1;
    }
  }
  function El() {
    var l = lo;
    return lo <<= 1, !(lo & 4194048) && (lo = 256), l;
  }
  function Pi() {
    var l = vl;
    return vl <<= 1, !(vl & 62914560) && (vl = 4194304), l;
  }
  function Ci(l) {
    for (var o = [], c = 0; 31 > c; c++) o.push(l);
    return o;
  }
  function Qi(l, o) {
    l.pendingLanes |= o, o !== 268435456 && (l.suspendedLanes = 0, l.pingedLanes = 0, l.warmLanes = 0);
  }
  function ef(l, o, c, d, v, S) {
    var A = l.pendingLanes;
    l.pendingLanes = c, l.suspendedLanes = 0, l.pingedLanes = 0, l.warmLanes = 0, l.expiredLanes &= c, l.entangledLanes &= c, l.errorRecoveryDisabledLanes &= c, l.shellSuspendCounter = 0;
    var M = l.entanglements, G = l.expirationTimes, W = l.hiddenUpdates;
    for (c = A & ~c; 0 < c; ) {
      var ct = 31 - Bn(c), gt = 1 << ct;
      M[ct] = 0, G[ct] = -1;
      var $ = W[ct];
      if ($ !== null)
        for (W[ct] = null, ct = 0; ct < $.length; ct++) {
          var et = $[ct];
          et !== null && (et.lane &= -536870913);
        }
      c &= ~gt;
    }
    d !== 0 && ao(l, d, 0), S !== 0 && v === 0 && l.tag !== 0 && (l.suspendedLanes |= S & ~(A & ~o));
  }
  function ao(l, o, c) {
    l.pendingLanes |= o, l.suspendedLanes &= ~o;
    var d = 31 - Bn(o);
    l.entangledLanes |= o, l.entanglements[d] = l.entanglements[d] | 1073741824 | c & 4194090;
  }
  function On(l, o) {
    var c = l.entangledLanes |= o;
    for (l = l.entanglements; c; ) {
      var d = 31 - Bn(c), v = 1 << d;
      v & o | l[d] & o && (l[d] |= o), c &= ~v;
    }
  }
  function bn(l) {
    switch (l) {
      case 2:
        l = 1;
        break;
      case 8:
        l = 4;
        break;
      case 32:
        l = 16;
        break;
      case 256:
      case 512:
      case 1024:
      case 2048:
      case 4096:
      case 8192:
      case 16384:
      case 32768:
      case 65536:
      case 131072:
      case 262144:
      case 524288:
      case 1048576:
      case 2097152:
      case 4194304:
      case 8388608:
      case 16777216:
      case 33554432:
        l = 128;
        break;
      case 268435456:
        l = 134217728;
        break;
      default:
        l = 0;
    }
    return l;
  }
  function In(l) {
    return l &= -l, 2 < l ? 8 < l ? l & 134217727 ? 32 : 268435456 : 8 : 2;
  }
  function Wi() {
    var l = K.p;
    return l !== 0 ? l : (l = window.event, l === void 0 ? 32 : o0(l.type));
  }
  function Ri(l, o) {
    var c = K.p;
    try {
      return K.p = l, o();
    } finally {
      K.p = c;
    }
  }
  var Oe = Math.random().toString(36).slice(2), Ue = "__reactFiber$" + Oe, se = "__reactProps$" + Oe, an = "__reactContainer$" + Oe, Ai = "__reactEvents$" + Oe, ad = "__reactListeners$" + Oe, sd = "__reactHandles$" + Oe, U_ = "__reactResources$" + Oe, wt = "__reactMarker$" + Oe;
  function nf(l) {
    delete l[Ue], delete l[se], delete l[Ai], delete l[ad], delete l[sd];
  }
  function Xn(l) {
    var o = l[Ue];
    if (o) return o;
    for (var c = l.parentNode; c; ) {
      if (o = c[an] || c[Ue]) {
        if (c = o.alternate, o.child !== null || c !== null && c.child !== null)
          for (l = Pn(l); l !== null; ) {
            if (c = l[Ue]) return c;
            l = Pn(l);
          }
        return o;
      }
      l = c, c = l.parentNode;
    }
    return null;
  }
  function er(l) {
    if (l = l[Ue] || l[an]) {
      var o = l.tag;
      if (o === 5 || o === 6 || o === 13 || o === 26 || o === 27 || o === 3)
        return l;
    }
    return null;
  }
  function lf(l) {
    var o = l.tag;
    if (o === 5 || o === 26 || o === 27 || o === 6) return l.stateNode;
    throw Error(a(33));
  }
  function ps(l) {
    var o = l[U_];
    return o || (o = l[U_] = { hoistableStyles: /* @__PURE__ */ new Map(), hoistableScripts: /* @__PURE__ */ new Map() }), o;
  }
  function Tn(l) {
    l[wt] = !0;
  }
  var af = /* @__PURE__ */ new Set(), Ji = {};
  function so(l, o) {
    oo(l, o), oo(l + "Capture", o);
  }
  function oo(l, o) {
    for (Ji[l] = o, l = 0; l < o.length; l++)
      af.add(o[l]);
  }
  var G_ = RegExp(
    "^[:A-Z_a-z\\u00C0-\\u00D6\\u00D8-\\u00F6\\u00F8-\\u02FF\\u0370-\\u037D\\u037F-\\u1FFF\\u200C-\\u200D\\u2070-\\u218F\\u2C00-\\u2FEF\\u3001-\\uD7FF\\uF900-\\uFDCF\\uFDF0-\\uFFFD][:A-Z_a-z\\u00C0-\\u00D6\\u00D8-\\u00F6\\u00F8-\\u02FF\\u0370-\\u037D\\u037F-\\u1FFF\\u200C-\\u200D\\u2070-\\u218F\\u2C00-\\u2FEF\\u3001-\\uD7FF\\uF900-\\uFDCF\\uFDF0-\\uFFFD\\-.0-9\\u00B7\\u0300-\\u036F\\u203F-\\u2040]*$"
  ), od = {}, ly = {};
  function H_(l) {
    return no.call(ly, l) ? !0 : no.call(od, l) ? !1 : G_.test(l) ? ly[l] = !0 : (od[l] = !0, !1);
  }
  function _s(l, o, c) {
    if (H_(o))
      if (c === null) l.removeAttribute(o);
      else {
        switch (typeof c) {
          case "undefined":
          case "function":
          case "symbol":
            l.removeAttribute(o);
            return;
          case "boolean":
            var d = o.toLowerCase().slice(0, 5);
            if (d !== "data-" && d !== "aria-") {
              l.removeAttribute(o);
              return;
            }
        }
        l.setAttribute(o, "" + c);
      }
  }
  function sf(l, o, c) {
    if (c === null) l.removeAttribute(o);
    else {
      switch (typeof c) {
        case "undefined":
        case "function":
        case "symbol":
        case "boolean":
          l.removeAttribute(o);
          return;
      }
      l.setAttribute(o, "" + c);
    }
  }
  function Ea(l, o, c, d) {
    if (d === null) l.removeAttribute(c);
    else {
      switch (typeof d) {
        case "undefined":
        case "function":
        case "symbol":
        case "boolean":
          l.removeAttribute(c);
          return;
      }
      l.setAttributeNS(o, c, "" + d);
    }
  }
  var rd, ay;
  function nr(l) {
    if (rd === void 0)
      try {
        throw Error();
      } catch (c) {
        var o = c.stack.trim().match(/\n( *(at )?)/);
        rd = o && o[1] || "", ay = -1 < c.stack.indexOf(`
    at`) ? " (<anonymous>)" : -1 < c.stack.indexOf("@") ? "@unknown:0:0" : "";
      }
    return `
` + rd + l + ay;
  }
  var gi = !1;
  function ro(l, o) {
    if (!l || gi) return "";
    gi = !0;
    var c = Error.prepareStackTrace;
    Error.prepareStackTrace = void 0;
    try {
      var d = {
        DetermineComponentFrameRoot: function() {
          try {
            if (o) {
              var gt = function() {
                throw Error();
              };
              if (Object.defineProperty(gt.prototype, "props", {
                set: function() {
                  throw Error();
                }
              }), typeof Reflect == "object" && Reflect.construct) {
                try {
                  Reflect.construct(gt, []);
                } catch (et) {
                  var $ = et;
                }
                Reflect.construct(l, [], gt);
              } else {
                try {
                  gt.call();
                } catch (et) {
                  $ = et;
                }
                l.call(gt.prototype);
              }
            } else {
              try {
                throw Error();
              } catch (et) {
                $ = et;
              }
              (gt = l()) && typeof gt.catch == "function" && gt.catch(function() {
              });
            }
          } catch (et) {
            if (et && $ && typeof et.stack == "string")
              return [et.stack, $.stack];
          }
          return [null, null];
        }
      };
      d.DetermineComponentFrameRoot.displayName = "DetermineComponentFrameRoot";
      var v = Object.getOwnPropertyDescriptor(
        d.DetermineComponentFrameRoot,
        "name"
      );
      v && v.configurable && Object.defineProperty(
        d.DetermineComponentFrameRoot,
        "name",
        { value: "DetermineComponentFrameRoot" }
      );
      var S = d.DetermineComponentFrameRoot(), A = S[0], M = S[1];
      if (A && M) {
        var G = A.split(`
`), W = M.split(`
`);
        for (v = d = 0; d < G.length && !G[d].includes("DetermineComponentFrameRoot"); )
          d++;
        for (; v < W.length && !W[v].includes(
          "DetermineComponentFrameRoot"
        ); )
          v++;
        if (d === G.length || v === W.length)
          for (d = G.length - 1, v = W.length - 1; 1 <= d && 0 <= v && G[d] !== W[v]; )
            v--;
        for (; 1 <= d && 0 <= v; d--, v--)
          if (G[d] !== W[v]) {
            if (d !== 1 || v !== 1)
              do
                if (d--, v--, 0 > v || G[d] !== W[v]) {
                  var ct = `
` + G[d].replace(" at new ", " at ");
                  return l.displayName && ct.includes("<anonymous>") && (ct = ct.replace("<anonymous>", l.displayName)), ct;
                }
              while (1 <= d && 0 <= v);
            break;
          }
      }
    } finally {
      gi = !1, Error.prepareStackTrace = c;
    }
    return (c = l ? l.displayName || l.name : "") ? nr(c) : "";
  }
  function ir(l) {
    switch (l.tag) {
      case 26:
      case 27:
      case 5:
        return nr(l.type);
      case 16:
        return nr("Lazy");
      case 13:
        return nr("Suspense");
      case 19:
        return nr("SuspenseList");
      case 0:
      case 15:
        return ro(l.type, !1);
      case 11:
        return ro(l.type.render, !1);
      case 1:
        return ro(l.type, !0);
      case 31:
        return nr("Activity");
      default:
        return "";
    }
  }
  function sy(l) {
    try {
      var o = "";
      do
        o += ir(l), l = l.return;
      while (l);
      return o;
    } catch (c) {
      return `
Error generating stack: ` + c.message + `
` + c.stack;
    }
  }
  function ii(l) {
    switch (typeof l) {
      case "bigint":
      case "boolean":
      case "number":
      case "string":
      case "undefined":
        return l;
      case "object":
        return l;
      default:
        return "";
    }
  }
  function of(l) {
    var o = l.type;
    return (l = l.nodeName) && l.toLowerCase() === "input" && (o === "checkbox" || o === "radio");
  }
  function oy(l) {
    var o = of(l) ? "checked" : "value", c = Object.getOwnPropertyDescriptor(
      l.constructor.prototype,
      o
    ), d = "" + l[o];
    if (!l.hasOwnProperty(o) && typeof c < "u" && typeof c.get == "function" && typeof c.set == "function") {
      var v = c.get, S = c.set;
      return Object.defineProperty(l, o, {
        configurable: !0,
        get: function() {
          return v.call(this);
        },
        set: function(A) {
          d = "" + A, S.call(this, A);
        }
      }), Object.defineProperty(l, o, {
        enumerable: c.enumerable
      }), {
        getValue: function() {
          return d;
        },
        setValue: function(A) {
          d = "" + A;
        },
        stopTracking: function() {
          l._valueTracker = null, delete l[o];
        }
      };
    }
  }
  function uo(l) {
    l._valueTracker || (l._valueTracker = oy(l));
  }
  function lr(l) {
    if (!l) return !1;
    var o = l._valueTracker;
    if (!o) return !0;
    var c = o.getValue(), d = "";
    return l && (d = of(l) ? l.checked ? "true" : "false" : l.value), l = d, l !== c ? (o.setValue(l), !0) : !1;
  }
  function xu(l) {
    if (l = l || (typeof document < "u" ? document : void 0), typeof l > "u") return null;
    try {
      return l.activeElement || l.body;
    } catch {
      return l.body;
    }
  }
  var DS = /[\n"\\]/g;
  function bl(l) {
    return l.replace(
      DS,
      function(o) {
        return "\\" + o.charCodeAt(0).toString(16) + " ";
      }
    );
  }
  function ud(l, o, c, d, v, S, A, M) {
    l.name = "", A != null && typeof A != "function" && typeof A != "symbol" && typeof A != "boolean" ? l.type = A : l.removeAttribute("type"), o != null ? A === "number" ? (o === 0 && l.value === "" || l.value != o) && (l.value = "" + ii(o)) : l.value !== "" + ii(o) && (l.value = "" + ii(o)) : A !== "submit" && A !== "reset" || l.removeAttribute("value"), o != null ? rf(l, A, ii(o)) : c != null ? rf(l, A, ii(c)) : d != null && l.removeAttribute("value"), v == null && S != null && (l.defaultChecked = !!S), v != null && (l.checked = v && typeof v != "function" && typeof v != "symbol"), M != null && typeof M != "function" && typeof M != "symbol" && typeof M != "boolean" ? l.name = "" + ii(M) : l.removeAttribute("name");
  }
  function cd(l, o, c, d, v, S, A, M) {
    if (S != null && typeof S != "function" && typeof S != "symbol" && typeof S != "boolean" && (l.type = S), o != null || c != null) {
      if (!(S !== "submit" && S !== "reset" || o != null))
        return;
      c = c != null ? "" + ii(c) : "", o = o != null ? "" + ii(o) : c, M || o === l.value || (l.value = o), l.defaultValue = o;
    }
    d = d ?? v, d = typeof d != "function" && typeof d != "symbol" && !!d, l.checked = M ? l.checked : !!d, l.defaultChecked = !!d, A != null && typeof A != "function" && typeof A != "symbol" && typeof A != "boolean" && (l.name = A);
  }
  function rf(l, o, c) {
    o === "number" && xu(l.ownerDocument) === l || l.defaultValue === "" + c || (l.defaultValue = "" + c);
  }
  function ar(l, o, c, d) {
    if (l = l.options, o) {
      o = {};
      for (var v = 0; v < c.length; v++)
        o["$" + c[v]] = !0;
      for (c = 0; c < l.length; c++)
        v = o.hasOwnProperty("$" + l[c].value), l[c].selected !== v && (l[c].selected = v), v && d && (l[c].defaultSelected = !0);
    } else {
      for (c = "" + ii(c), o = null, v = 0; v < l.length; v++) {
        if (l[v].value === c) {
          l[v].selected = !0, d && (l[v].defaultSelected = !0);
          return;
        }
        o !== null || l[v].disabled || (o = l[v]);
      }
      o !== null && (o.selected = !0);
    }
  }
  function ry(l, o, c) {
    if (o != null && (o = "" + ii(o), o !== l.value && (l.value = o), c == null)) {
      l.defaultValue !== o && (l.defaultValue = o);
      return;
    }
    l.defaultValue = c != null ? "" + ii(c) : "";
  }
  function uy(l, o, c, d) {
    if (o == null) {
      if (d != null) {
        if (c != null) throw Error(a(92));
        if (Rt(d)) {
          if (1 < d.length) throw Error(a(93));
          d = d[0];
        }
        c = d;
      }
      c == null && (c = ""), o = c;
    }
    c = ii(o), l.defaultValue = c, d = l.textContent, d === c && d !== "" && d !== null && (l.value = d);
  }
  function Cu(l, o) {
    if (o) {
      var c = l.firstChild;
      if (c && c === l.lastChild && c.nodeType === 3) {
        c.nodeValue = o;
        return;
      }
    }
    l.textContent = o;
  }
  var Y_ = new Set(
    "animationIterationCount aspectRatio borderImageOutset borderImageSlice borderImageWidth boxFlex boxFlexGroup boxOrdinalGroup columnCount columns flex flexGrow flexPositive flexShrink flexNegative flexOrder gridArea gridRow gridRowEnd gridRowSpan gridRowStart gridColumn gridColumnEnd gridColumnSpan gridColumnStart fontWeight lineClamp lineHeight opacity order orphans scale tabSize widows zIndex zoom fillOpacity floodOpacity stopOpacity strokeDasharray strokeDashoffset strokeMiterlimit strokeOpacity strokeWidth MozAnimationIterationCount MozBoxFlex MozBoxFlexGroup MozLineClamp msAnimationIterationCount msFlex msZoom msFlexGrow msFlexNegative msFlexOrder msFlexPositive msFlexShrink msGridColumn msGridColumnSpan msGridRow msGridRowSpan WebkitAnimationIterationCount WebkitBoxFlex WebKitBoxFlexGroup WebkitBoxOrdinalGroup WebkitColumnCount WebkitColumns WebkitFlex WebkitFlexGrow WebkitFlexPositive WebkitFlexShrink WebkitLineClamp".split(
      " "
    )
  );
  function fd(l, o, c) {
    var d = o.indexOf("--") === 0;
    c == null || typeof c == "boolean" || c === "" ? d ? l.setProperty(o, "") : o === "float" ? l.cssFloat = "" : l[o] = "" : d ? l.setProperty(o, c) : typeof c != "number" || c === 0 || Y_.has(o) ? o === "float" ? l.cssFloat = c : l[o] = ("" + c).trim() : l[o] = c + "px";
  }
  function uf(l, o, c) {
    if (o != null && typeof o != "object")
      throw Error(a(62));
    if (l = l.style, c != null) {
      for (var d in c)
        !c.hasOwnProperty(d) || o != null && o.hasOwnProperty(d) || (d.indexOf("--") === 0 ? l.setProperty(d, "") : d === "float" ? l.cssFloat = "" : l[d] = "");
      for (var v in o)
        d = o[v], o.hasOwnProperty(v) && c[v] !== d && fd(l, v, d);
    } else
      for (var S in o)
        o.hasOwnProperty(S) && fd(l, S, o[S]);
  }
  function sr(l) {
    if (l.indexOf("-") === -1) return !1;
    switch (l) {
      case "annotation-xml":
      case "color-profile":
      case "font-face":
      case "font-face-src":
      case "font-face-uri":
      case "font-face-format":
      case "font-face-name":
      case "missing-glyph":
        return !1;
      default:
        return !0;
    }
  }
  var OS = /* @__PURE__ */ new Map([
    ["acceptCharset", "accept-charset"],
    ["htmlFor", "for"],
    ["httpEquiv", "http-equiv"],
    ["crossOrigin", "crossorigin"],
    ["accentHeight", "accent-height"],
    ["alignmentBaseline", "alignment-baseline"],
    ["arabicForm", "arabic-form"],
    ["baselineShift", "baseline-shift"],
    ["capHeight", "cap-height"],
    ["clipPath", "clip-path"],
    ["clipRule", "clip-rule"],
    ["colorInterpolation", "color-interpolation"],
    ["colorInterpolationFilters", "color-interpolation-filters"],
    ["colorProfile", "color-profile"],
    ["colorRendering", "color-rendering"],
    ["dominantBaseline", "dominant-baseline"],
    ["enableBackground", "enable-background"],
    ["fillOpacity", "fill-opacity"],
    ["fillRule", "fill-rule"],
    ["floodColor", "flood-color"],
    ["floodOpacity", "flood-opacity"],
    ["fontFamily", "font-family"],
    ["fontSize", "font-size"],
    ["fontSizeAdjust", "font-size-adjust"],
    ["fontStretch", "font-stretch"],
    ["fontStyle", "font-style"],
    ["fontVariant", "font-variant"],
    ["fontWeight", "font-weight"],
    ["glyphName", "glyph-name"],
    ["glyphOrientationHorizontal", "glyph-orientation-horizontal"],
    ["glyphOrientationVertical", "glyph-orientation-vertical"],
    ["horizAdvX", "horiz-adv-x"],
    ["horizOriginX", "horiz-origin-x"],
    ["imageRendering", "image-rendering"],
    ["letterSpacing", "letter-spacing"],
    ["lightingColor", "lighting-color"],
    ["markerEnd", "marker-end"],
    ["markerMid", "marker-mid"],
    ["markerStart", "marker-start"],
    ["overlinePosition", "overline-position"],
    ["overlineThickness", "overline-thickness"],
    ["paintOrder", "paint-order"],
    ["panose-1", "panose-1"],
    ["pointerEvents", "pointer-events"],
    ["renderingIntent", "rendering-intent"],
    ["shapeRendering", "shape-rendering"],
    ["stopColor", "stop-color"],
    ["stopOpacity", "stop-opacity"],
    ["strikethroughPosition", "strikethrough-position"],
    ["strikethroughThickness", "strikethrough-thickness"],
    ["strokeDasharray", "stroke-dasharray"],
    ["strokeDashoffset", "stroke-dashoffset"],
    ["strokeLinecap", "stroke-linecap"],
    ["strokeLinejoin", "stroke-linejoin"],
    ["strokeMiterlimit", "stroke-miterlimit"],
    ["strokeOpacity", "stroke-opacity"],
    ["strokeWidth", "stroke-width"],
    ["textAnchor", "text-anchor"],
    ["textDecoration", "text-decoration"],
    ["textRendering", "text-rendering"],
    ["transformOrigin", "transform-origin"],
    ["underlinePosition", "underline-position"],
    ["underlineThickness", "underline-thickness"],
    ["unicodeBidi", "unicode-bidi"],
    ["unicodeRange", "unicode-range"],
    ["unitsPerEm", "units-per-em"],
    ["vAlphabetic", "v-alphabetic"],
    ["vHanging", "v-hanging"],
    ["vIdeographic", "v-ideographic"],
    ["vMathematical", "v-mathematical"],
    ["vectorEffect", "vector-effect"],
    ["vertAdvY", "vert-adv-y"],
    ["vertOriginX", "vert-origin-x"],
    ["vertOriginY", "vert-origin-y"],
    ["wordSpacing", "word-spacing"],
    ["writingMode", "writing-mode"],
    ["xmlnsXlink", "xmlns:xlink"],
    ["xHeight", "x-height"]
  ]), j_ = /^[\u0000-\u001F ]*j[\r\n\t]*a[\r\n\t]*v[\r\n\t]*a[\r\n\t]*s[\r\n\t]*c[\r\n\t]*r[\r\n\t]*i[\r\n\t]*p[\r\n\t]*t[\r\n\t]*:/i;
  function cf(l) {
    return j_.test("" + l) ? "javascript:throw new Error('React has blocked a javascript: URL as a security precaution.')" : l;
  }
  var or = null;
  function hd(l) {
    return l = l.target || l.srcElement || window, l.correspondingUseElement && (l = l.correspondingUseElement), l.nodeType === 3 ? l.parentNode : l;
  }
  var Ru = null, Au = null;
  function B_(l) {
    var o = er(l);
    if (o && (l = o.stateNode)) {
      var c = l[se] || null;
      t: switch (l = o.stateNode, o.type) {
        case "input":
          if (ud(
            l,
            c.value,
            c.defaultValue,
            c.defaultValue,
            c.checked,
            c.defaultChecked,
            c.type,
            c.name
          ), o = c.name, c.type === "radio" && o != null) {
            for (c = l; c.parentNode; ) c = c.parentNode;
            for (c = c.querySelectorAll(
              'input[name="' + bl(
                "" + o
              ) + '"][type="radio"]'
            ), o = 0; o < c.length; o++) {
              var d = c[o];
              if (d !== l && d.form === l.form) {
                var v = d[se] || null;
                if (!v) throw Error(a(90));
                ud(
                  d,
                  v.value,
                  v.defaultValue,
                  v.defaultValue,
                  v.checked,
                  v.defaultChecked,
                  v.type,
                  v.name
                );
              }
            }
            for (o = 0; o < c.length; o++)
              d = c[o], d.form === l.form && lr(d);
          }
          break t;
        case "textarea":
          ry(l, c.value, c.defaultValue);
          break t;
        case "select":
          o = c.value, o != null && ar(l, !!c.multiple, o, !1);
      }
    }
  }
  var cy = !1;
  function Mu(l, o, c) {
    if (cy) return l(o, c);
    cy = !0;
    try {
      var d = l(o);
      return d;
    } finally {
      if (cy = !1, (Ru !== null || Au !== null) && (jr(), Ru && (o = Ru, l = Au, Au = Ru = null, B_(o), l)))
        for (o = 0; o < l.length; o++) B_(l[o]);
    }
  }
  function rr(l, o) {
    var c = l.stateNode;
    if (c === null) return null;
    var d = c[se] || null;
    if (d === null) return null;
    c = d[o];
    t: switch (o) {
      case "onClick":
      case "onClickCapture":
      case "onDoubleClick":
      case "onDoubleClickCapture":
      case "onMouseDown":
      case "onMouseDownCapture":
      case "onMouseMove":
      case "onMouseMoveCapture":
      case "onMouseUp":
      case "onMouseUpCapture":
      case "onMouseEnter":
        (d = !d.disabled) || (l = l.type, d = !(l === "button" || l === "input" || l === "select" || l === "textarea")), l = !d;
        break t;
      default:
        l = !1;
    }
    if (l) return null;
    if (c && typeof c != "function")
      throw Error(
        a(231, o, typeof c)
      );
    return c;
  }
  var ba = !(typeof window > "u" || typeof window.document > "u" || typeof window.document.createElement > "u"), dd = !1;
  if (ba)
    try {
      var vs = {};
      Object.defineProperty(vs, "passive", {
        get: function() {
          dd = !0;
        }
      }), window.addEventListener("test", vs, vs), window.removeEventListener("test", vs, vs);
    } catch {
      dd = !1;
    }
  var Ss = null, wu = null, ur = null;
  function fy() {
    if (ur) return ur;
    var l, o = wu, c = o.length, d, v = "value" in Ss ? Ss.value : Ss.textContent, S = v.length;
    for (l = 0; l < c && o[l] === v[l]; l++) ;
    var A = c - l;
    for (d = 1; d <= A && o[c - d] === v[S - d]; d++) ;
    return ur = v.slice(l, 1 < d ? 1 - d : void 0);
  }
  function kn(l) {
    var o = l.keyCode;
    return "charCode" in l ? (l = l.charCode, l === 0 && o === 13 && (l = 13)) : l = o, l === 10 && (l = 13), 32 <= l || l === 13 ? l : 0;
  }
  function gd() {
    return !0;
  }
  function md() {
    return !1;
  }
  function mi(l) {
    function o(c, d, v, S, A) {
      this._reactName = c, this._targetInst = v, this.type = d, this.nativeEvent = S, this.target = A, this.currentTarget = null;
      for (var M in l)
        l.hasOwnProperty(M) && (c = l[M], this[M] = c ? c(S) : S[M]);
      return this.isDefaultPrevented = (S.defaultPrevented != null ? S.defaultPrevented : S.returnValue === !1) ? gd : md, this.isPropagationStopped = md, this;
    }
    return E(o.prototype, {
      preventDefault: function() {
        this.defaultPrevented = !0;
        var c = this.nativeEvent;
        c && (c.preventDefault ? c.preventDefault() : typeof c.returnValue != "unknown" && (c.returnValue = !1), this.isDefaultPrevented = gd);
      },
      stopPropagation: function() {
        var c = this.nativeEvent;
        c && (c.stopPropagation ? c.stopPropagation() : typeof c.cancelBubble != "unknown" && (c.cancelBubble = !0), this.isPropagationStopped = gd);
      },
      persist: function() {
      },
      isPersistent: gd
    }), o;
  }
  var co = {
    eventPhase: 0,
    bubbles: 0,
    cancelable: 0,
    timeStamp: function(l) {
      return l.timeStamp || Date.now();
    },
    defaultPrevented: 0,
    isTrusted: 0
  }, yd = mi(co), ff = E({}, co, { view: 0, detail: 0 }), I_ = mi(ff), hy, pd, hf, cr = E({}, ff, {
    screenX: 0,
    screenY: 0,
    clientX: 0,
    clientY: 0,
    pageX: 0,
    pageY: 0,
    ctrlKey: 0,
    shiftKey: 0,
    altKey: 0,
    metaKey: 0,
    getModifierState: Es,
    button: 0,
    buttons: 0,
    relatedTarget: function(l) {
      return l.relatedTarget === void 0 ? l.fromElement === l.srcElement ? l.toElement : l.fromElement : l.relatedTarget;
    },
    movementX: function(l) {
      return "movementX" in l ? l.movementX : (l !== hf && (hf && l.type === "mousemove" ? (hy = l.screenX - hf.screenX, pd = l.screenY - hf.screenY) : pd = hy = 0, hf = l), hy);
    },
    movementY: function(l) {
      return "movementY" in l ? l.movementY : pd;
    }
  }), dy = mi(cr), X_ = E({}, cr, { dataTransfer: 0 }), k_ = mi(X_), LS = E({}, ff, { relatedTarget: 0 }), gy = mi(LS), zS = E({}, co, {
    animationName: 0,
    elapsedTime: 0,
    pseudoElement: 0
  }), NS = mi(zS), US = E({}, co, {
    clipboardData: function(l) {
      return "clipboardData" in l ? l.clipboardData : window.clipboardData;
    }
  }), df = mi(US), V_ = E({}, co, { data: 0 }), my = mi(V_), q_ = {
    Esc: "Escape",
    Spacebar: " ",
    Left: "ArrowLeft",
    Up: "ArrowUp",
    Right: "ArrowRight",
    Down: "ArrowDown",
    Del: "Delete",
    Win: "OS",
    Menu: "ContextMenu",
    Apps: "ContextMenu",
    Scroll: "ScrollLock",
    MozPrintableKey: "Unidentified"
  }, Z_ = {
    8: "Backspace",
    9: "Tab",
    12: "Clear",
    13: "Enter",
    16: "Shift",
    17: "Control",
    18: "Alt",
    19: "Pause",
    20: "CapsLock",
    27: "Escape",
    32: " ",
    33: "PageUp",
    34: "PageDown",
    35: "End",
    36: "Home",
    37: "ArrowLeft",
    38: "ArrowUp",
    39: "ArrowRight",
    40: "ArrowDown",
    45: "Insert",
    46: "Delete",
    112: "F1",
    113: "F2",
    114: "F3",
    115: "F4",
    116: "F5",
    117: "F6",
    118: "F7",
    119: "F8",
    120: "F9",
    121: "F10",
    122: "F11",
    123: "F12",
    144: "NumLock",
    145: "ScrollLock",
    224: "Meta"
  }, yy = {
    Alt: "altKey",
    Control: "ctrlKey",
    Meta: "metaKey",
    Shift: "shiftKey"
  };
  function F_(l) {
    var o = this.nativeEvent;
    return o.getModifierState ? o.getModifierState(l) : (l = yy[l]) ? !!o[l] : !1;
  }
  function Es() {
    return F_;
  }
  var fr = E({}, ff, {
    key: function(l) {
      if (l.key) {
        var o = q_[l.key] || l.key;
        if (o !== "Unidentified") return o;
      }
      return l.type === "keypress" ? (l = kn(l), l === 13 ? "Enter" : String.fromCharCode(l)) : l.type === "keydown" || l.type === "keyup" ? Z_[l.keyCode] || "Unidentified" : "";
    },
    code: 0,
    location: 0,
    ctrlKey: 0,
    shiftKey: 0,
    altKey: 0,
    metaKey: 0,
    repeat: 0,
    locale: 0,
    getModifierState: Es,
    charCode: function(l) {
      return l.type === "keypress" ? kn(l) : 0;
    },
    keyCode: function(l) {
      return l.type === "keydown" || l.type === "keyup" ? l.keyCode : 0;
    },
    which: function(l) {
      return l.type === "keypress" ? kn(l) : l.type === "keydown" || l.type === "keyup" ? l.keyCode : 0;
    }
  }), Jl = mi(fr), $i = E({}, cr, {
    pointerId: 0,
    width: 0,
    height: 0,
    pressure: 0,
    tangentialPressure: 0,
    tiltX: 0,
    tiltY: 0,
    twist: 0,
    pointerType: 0,
    isPrimary: 0
  }), gf = mi($i), _d = E({}, ff, {
    touches: 0,
    targetTouches: 0,
    changedTouches: 0,
    altKey: 0,
    metaKey: 0,
    ctrlKey: 0,
    shiftKey: 0,
    getModifierState: Es
  }), py = mi(_d), Mi = E({}, co, {
    propertyName: 0,
    elapsedTime: 0,
    pseudoElement: 0
  }), K_ = mi(Mi), vd = E({}, cr, {
    deltaX: function(l) {
      return "deltaX" in l ? l.deltaX : "wheelDeltaX" in l ? -l.wheelDeltaX : 0;
    },
    deltaY: function(l) {
      return "deltaY" in l ? l.deltaY : "wheelDeltaY" in l ? -l.wheelDeltaY : "wheelDelta" in l ? -l.wheelDelta : 0;
    },
    deltaZ: 0,
    deltaMode: 0
  }), hr = mi(vd), _y = E({}, co, {
    newState: 0,
    oldState: 0
  }), P_ = mi(_y), Q_ = [9, 13, 27, 32], mf = ba && "CompositionEvent" in window, yf = null;
  ba && "documentMode" in document && (yf = document.documentMode);
  var vy = ba && "TextEvent" in window && !yf, Ta = ba && (!mf || yf && 8 < yf && 11 >= yf), Sy = " ", Sd = !1;
  function pf(l, o) {
    switch (l) {
      case "keyup":
        return Q_.indexOf(o.keyCode) !== -1;
      case "keydown":
        return o.keyCode !== 229;
      case "keypress":
      case "mousedown":
      case "focusout":
        return !0;
      default:
        return !1;
    }
  }
  function fo(l) {
    return l = l.detail, typeof l == "object" && "data" in l ? l.data : null;
  }
  var ho = !1;
  function Ey(l, o) {
    switch (l) {
      case "compositionend":
        return fo(o);
      case "keypress":
        return o.which !== 32 ? null : (Sd = !0, Sy);
      case "textInput":
        return l = o.data, l === Sy && Sd ? null : l;
      default:
        return null;
    }
  }
  function dr(l, o) {
    if (ho)
      return l === "compositionend" || !mf && pf(l, o) ? (l = fy(), ur = wu = Ss = null, ho = !1, l) : null;
    switch (l) {
      case "paste":
        return null;
      case "keypress":
        if (!(o.ctrlKey || o.altKey || o.metaKey) || o.ctrlKey && o.altKey) {
          if (o.char && 1 < o.char.length)
            return o.char;
          if (o.which) return String.fromCharCode(o.which);
        }
        return null;
      case "compositionend":
        return Ta && o.locale !== "ko" ? null : o.data;
      default:
        return null;
    }
  }
  var W_ = {
    color: !0,
    date: !0,
    datetime: !0,
    "datetime-local": !0,
    email: !0,
    month: !0,
    number: !0,
    password: !0,
    range: !0,
    search: !0,
    tel: !0,
    text: !0,
    time: !0,
    url: !0,
    week: !0
  };
  function Ed(l) {
    var o = l && l.nodeName && l.nodeName.toLowerCase();
    return o === "input" ? !!W_[l.type] : o === "textarea";
  }
  function bd(l, o, c, d) {
    Ru ? Au ? Au.push(d) : Au = [d] : Ru = d, o = hc(o, "onChange"), 0 < o.length && (c = new yd(
      "onChange",
      "change",
      null,
      c,
      d
    ), l.push({ event: c, listeners: o }));
  }
  var $l = null, ta = null;
  function by(l) {
    kr(l, 0);
  }
  function xa(l) {
    var o = lf(l);
    if (lr(o)) return l;
  }
  function Ty(l, o) {
    if (l === "change") return o;
  }
  var xy = !1;
  if (ba) {
    var gr;
    if (ba) {
      var mr = "oninput" in document;
      if (!mr) {
        var Cy = document.createElement("div");
        Cy.setAttribute("oninput", "return;"), mr = typeof Cy.oninput == "function";
      }
      gr = mr;
    } else gr = !1;
    xy = gr && (!document.documentMode || 9 < document.documentMode);
  }
  function Du() {
    $l && ($l.detachEvent("onpropertychange", Ry), ta = $l = null);
  }
  function Ry(l) {
    if (l.propertyName === "value" && xa(ta)) {
      var o = [];
      bd(
        o,
        ta,
        l,
        hd(l)
      ), Mu(by, o);
    }
  }
  function Td(l, o, c) {
    l === "focusin" ? (Du(), $l = o, ta = c, $l.attachEvent("onpropertychange", Ry)) : l === "focusout" && Du();
  }
  function go(l) {
    if (l === "selectionchange" || l === "keyup" || l === "keydown")
      return xa(ta);
  }
  function bs(l, o) {
    if (l === "click") return xa(o);
  }
  function Ay(l, o) {
    if (l === "input" || l === "change")
      return xa(o);
  }
  function My(l, o) {
    return l === o && (l !== 0 || 1 / l === 1 / o) || l !== l && o !== o;
  }
  var Vn = typeof Object.is == "function" ? Object.is : My;
  function mo(l, o) {
    if (Vn(l, o)) return !0;
    if (typeof l != "object" || l === null || typeof o != "object" || o === null)
      return !1;
    var c = Object.keys(l), d = Object.keys(o);
    if (c.length !== d.length) return !1;
    for (d = 0; d < c.length; d++) {
      var v = c[d];
      if (!no.call(o, v) || !Vn(l[v], o[v]))
        return !1;
    }
    return !0;
  }
  function yo(l) {
    for (; l && l.firstChild; ) l = l.firstChild;
    return l;
  }
  function Ke(l, o) {
    var c = yo(l);
    l = 0;
    for (var d; c; ) {
      if (c.nodeType === 3) {
        if (d = l + c.textContent.length, l <= o && d >= o)
          return { node: c, offset: o - l };
        l = d;
      }
      t: {
        for (; c; ) {
          if (c.nextSibling) {
            c = c.nextSibling;
            break t;
          }
          c = c.parentNode;
        }
        c = void 0;
      }
      c = yo(c);
    }
  }
  function _f(l, o) {
    return l && o ? l === o ? !0 : l && l.nodeType === 3 ? !1 : o && o.nodeType === 3 ? _f(l, o.parentNode) : "contains" in l ? l.contains(o) : l.compareDocumentPosition ? !!(l.compareDocumentPosition(o) & 16) : !1 : !1;
  }
  function wy(l) {
    l = l != null && l.ownerDocument != null && l.ownerDocument.defaultView != null ? l.ownerDocument.defaultView : window;
    for (var o = xu(l.document); o instanceof l.HTMLIFrameElement; ) {
      try {
        var c = typeof o.contentWindow.location.href == "string";
      } catch {
        c = !1;
      }
      if (c) l = o.contentWindow;
      else break;
      o = xu(l.document);
    }
    return o;
  }
  function vf(l) {
    var o = l && l.nodeName && l.nodeName.toLowerCase();
    return o && (o === "input" && (l.type === "text" || l.type === "search" || l.type === "tel" || l.type === "url" || l.type === "password") || o === "textarea" || l.contentEditable === "true");
  }
  var yr = ba && "documentMode" in document && 11 >= document.documentMode, Ca = null, ea = null, po = null, pr = !1;
  function xd(l, o, c) {
    var d = c.window === c ? c.document : c.nodeType === 9 ? c : c.ownerDocument;
    pr || Ca == null || Ca !== xu(d) || (d = Ca, "selectionStart" in d && vf(d) ? d = { start: d.selectionStart, end: d.selectionEnd } : (d = (d.ownerDocument && d.ownerDocument.defaultView || window).getSelection(), d = {
      anchorNode: d.anchorNode,
      anchorOffset: d.anchorOffset,
      focusNode: d.focusNode,
      focusOffset: d.focusOffset
    }), po && mo(po, d) || (po = d, d = hc(ea, "onSelect"), 0 < d.length && (o = new yd(
      "onSelect",
      "select",
      null,
      o,
      c
    ), l.push({ event: o, listeners: d }), o.target = Ca)));
  }
  function Ts(l, o) {
    var c = {};
    return c[l.toLowerCase()] = o.toLowerCase(), c["Webkit" + l] = "webkit" + o, c["Moz" + l] = "moz" + o, c;
  }
  var _r = {
    animationend: Ts("Animation", "AnimationEnd"),
    animationiteration: Ts("Animation", "AnimationIteration"),
    animationstart: Ts("Animation", "AnimationStart"),
    transitionrun: Ts("Transition", "TransitionRun"),
    transitionstart: Ts("Transition", "TransitionStart"),
    transitioncancel: Ts("Transition", "TransitionCancel"),
    transitionend: Ts("Transition", "TransitionEnd")
  }, Tl = {}, na = {};
  ba && (na = document.createElement("div").style, "AnimationEvent" in window || (delete _r.animationend.animation, delete _r.animationiteration.animation, delete _r.animationstart.animation), "TransitionEvent" in window || delete _r.transitionend.transition);
  function Ra(l) {
    if (Tl[l]) return Tl[l];
    if (!_r[l]) return l;
    var o = _r[l], c;
    for (c in o)
      if (o.hasOwnProperty(c) && c in na)
        return Tl[l] = o[c];
    return l;
  }
  var J_ = Ra("animationend"), Dy = Ra("animationiteration"), $_ = Ra("animationstart"), Oy = Ra("transitionrun"), Cd = Ra("transitionstart"), tv = Ra("transitioncancel"), Ly = Ra("transitionend"), zy = /* @__PURE__ */ new Map(), Ou = "abort auxClick beforeToggle cancel canPlay canPlayThrough click close contextMenu copy cut drag dragEnd dragEnter dragExit dragLeave dragOver dragStart drop durationChange emptied encrypted ended error gotPointerCapture input invalid keyDown keyPress keyUp load loadedData loadedMetadata loadStart lostPointerCapture mouseDown mouseMove mouseOut mouseOver mouseUp paste pause play playing pointerCancel pointerDown pointerMove pointerOut pointerOver pointerUp progress rateChange reset resize seeked seeking stalled submit suspend timeUpdate touchCancel touchEnd touchStart volumeChange scroll toggle touchMove waiting wheel".split(
    " "
  );
  Ou.push("scrollEnd");
  function xl(l, o) {
    zy.set(l, o), so(o, [l]);
  }
  var Ny = /* @__PURE__ */ new WeakMap();
  function tl(l, o) {
    if (typeof l == "object" && l !== null) {
      var c = Ny.get(l);
      return c !== void 0 ? c : (o = {
        value: l,
        source: o,
        stack: sy(o)
      }, Ny.set(l, o), o);
    }
    return {
      value: l,
      source: o,
      stack: sy(o)
    };
  }
  var wi = [], _o = 0, Aa = 0;
  function ia() {
    for (var l = _o, o = Aa = _o = 0; o < l; ) {
      var c = wi[o];
      wi[o++] = null;
      var d = wi[o];
      wi[o++] = null;
      var v = wi[o];
      wi[o++] = null;
      var S = wi[o];
      if (wi[o++] = null, d !== null && v !== null) {
        var A = d.pending;
        A === null ? v.next = v : (v.next = A.next, A.next = v), d.pending = v;
      }
      S !== 0 && zu(c, v, S);
    }
  }
  function vo(l, o, c, d) {
    wi[_o++] = l, wi[_o++] = o, wi[_o++] = c, wi[_o++] = d, Aa |= d, l.lanes |= d, l = l.alternate, l !== null && (l.lanes |= d);
  }
  function Lu(l, o, c, d) {
    return vo(l, o, c, d), Sf(l);
  }
  function Ma(l, o) {
    return vo(l, null, null, o), Sf(l);
  }
  function zu(l, o, c) {
    l.lanes |= c;
    var d = l.alternate;
    d !== null && (d.lanes |= c);
    for (var v = !1, S = l.return; S !== null; )
      S.childLanes |= c, d = S.alternate, d !== null && (d.childLanes |= c), S.tag === 22 && (l = S.stateNode, l === null || l._visibility & 1 || (v = !0)), l = S, S = S.return;
    return l.tag === 3 ? (S = l.stateNode, v && o !== null && (v = 31 - Bn(c), l = S.hiddenUpdates, d = l[v], d === null ? l[v] = [o] : d.push(o), o.lane = c | 536870912), S) : null;
  }
  function Sf(l) {
    if (50 < sc)
      throw sc = 0, Np = null, Error(a(185));
    for (var o = l.return; o !== null; )
      l = o, o = l.return;
    return l.tag === 3 ? l.stateNode : null;
  }
  var Nu = {};
  function ev(l, o, c, d) {
    this.tag = l, this.key = c, this.sibling = this.child = this.return = this.stateNode = this.type = this.elementType = null, this.index = 0, this.refCleanup = this.ref = null, this.pendingProps = o, this.dependencies = this.memoizedState = this.updateQueue = this.memoizedProps = null, this.mode = d, this.subtreeFlags = this.flags = 0, this.deletions = null, this.childLanes = this.lanes = 0, this.alternate = null;
  }
  function Di(l, o, c, d) {
    return new ev(l, o, c, d);
  }
  function Ef(l) {
    return l = l.prototype, !(!l || !l.isReactComponent);
  }
  function la(l, o) {
    var c = l.alternate;
    return c === null ? (c = Di(
      l.tag,
      o,
      l.key,
      l.mode
    ), c.elementType = l.elementType, c.type = l.type, c.stateNode = l.stateNode, c.alternate = l, l.alternate = c) : (c.pendingProps = o, c.type = l.type, c.flags = 0, c.subtreeFlags = 0, c.deletions = null), c.flags = l.flags & 65011712, c.childLanes = l.childLanes, c.lanes = l.lanes, c.child = l.child, c.memoizedProps = l.memoizedProps, c.memoizedState = l.memoizedState, c.updateQueue = l.updateQueue, o = l.dependencies, c.dependencies = o === null ? null : { lanes: o.lanes, firstContext: o.firstContext }, c.sibling = l.sibling, c.index = l.index, c.ref = l.ref, c.refCleanup = l.refCleanup, c;
  }
  function ne(l, o) {
    l.flags &= 65011714;
    var c = l.alternate;
    return c === null ? (l.childLanes = 0, l.lanes = o, l.child = null, l.subtreeFlags = 0, l.memoizedProps = null, l.memoizedState = null, l.updateQueue = null, l.dependencies = null, l.stateNode = null) : (l.childLanes = c.childLanes, l.lanes = c.lanes, l.child = c.child, l.subtreeFlags = 0, l.deletions = null, l.memoizedProps = c.memoizedProps, l.memoizedState = c.memoizedState, l.updateQueue = c.updateQueue, l.type = c.type, o = c.dependencies, l.dependencies = o === null ? null : {
      lanes: o.lanes,
      firstContext: o.firstContext
    }), l;
  }
  function _t(l, o, c, d, v, S) {
    var A = 0;
    if (d = l, typeof l == "function") Ef(l) && (A = 1);
    else if (typeof l == "string")
      A = Uv(
        l,
        c,
        Ut.current
      ) ? 26 : l === "html" || l === "head" || l === "body" ? 27 : 5;
    else
      t: switch (l) {
        case it:
          return l = Di(31, c, o, v), l.elementType = it, l.lanes = S, l;
        case L:
          return Cl(c.children, v, S, o);
        case H:
          A = 8, v |= 24;
          break;
        case N:
          return l = Di(12, c, o, v | 2), l.elementType = N, l.lanes = S, l;
        case Z:
          return l = Di(13, c, o, v), l.elementType = Z, l.lanes = S, l;
        case dt:
          return l = Di(19, c, o, v), l.elementType = dt, l.lanes = S, l;
        default:
          if (typeof l == "object" && l !== null)
            switch (l.$$typeof) {
              case O:
              case I:
                A = 10;
                break t;
              case U:
                A = 9;
                break t;
              case X:
                A = 11;
                break t;
              case ft:
                A = 14;
                break t;
              case yt:
                A = 16, d = null;
                break t;
            }
          A = 29, c = Error(
            a(130, l === null ? "null" : typeof l, "")
          ), d = null;
      }
    return o = Di(A, c, o, v), o.elementType = l, o.type = d, o.lanes = S, o;
  }
  function Cl(l, o, c, d) {
    return l = Di(7, l, d, o), l.lanes = c, l;
  }
  function Uu(l, o, c) {
    return l = Di(6, l, null, o), l.lanes = c, l;
  }
  function en(l, o, c) {
    return o = Di(
      4,
      l.children !== null ? l.children : [],
      l.key,
      o
    ), o.lanes = c, o.stateNode = {
      containerInfo: l.containerInfo,
      pendingChildren: null,
      implementation: l.implementation
    }, o;
  }
  var So = [], Eo = 0, bf = null, Gu = 0, Rl = [], Oi = 0, xs = null, aa = 1, sn = "";
  function me(l, o) {
    So[Eo++] = Gu, So[Eo++] = bf, bf = l, Gu = o;
  }
  function Rd(l, o, c) {
    Rl[Oi++] = aa, Rl[Oi++] = sn, Rl[Oi++] = xs, xs = l;
    var d = aa;
    l = sn;
    var v = 32 - Bn(d) - 1;
    d &= ~(1 << v), c += 1;
    var S = 32 - Bn(o) + v;
    if (30 < S) {
      var A = v - v % 5;
      S = (d & (1 << A) - 1).toString(32), d >>= A, v -= A, aa = 1 << 32 - Bn(o) + v | c << v | d, sn = S + l;
    } else
      aa = 1 << S | c << v | d, sn = l;
  }
  function vr(l) {
    l.return !== null && (me(l, 1), Rd(l, 1, 0));
  }
  function wa(l) {
    for (; l === bf; )
      bf = So[--Eo], So[Eo] = null, Gu = So[--Eo], So[Eo] = null;
    for (; l === xs; )
      xs = Rl[--Oi], Rl[Oi] = null, sn = Rl[--Oi], Rl[Oi] = null, aa = Rl[--Oi], Rl[Oi] = null;
  }
  var mn = null, Ee = null, ve = !1, Al = null, Ml = !1, Sr = Error(a(519));
  function Cs(l) {
    var o = Error(a(418, ""));
    throw ju(tl(o, l)), Sr;
  }
  function Tf(l) {
    var o = l.stateNode, c = l.type, d = l.memoizedProps;
    switch (o[Ue] = l, o[se] = d, c) {
      case "dialog":
        Qt("cancel", o), Qt("close", o);
        break;
      case "iframe":
      case "object":
      case "embed":
        Qt("load", o);
        break;
      case "video":
      case "audio":
        for (c = 0; c < rh.length; c++)
          Qt(rh[c], o);
        break;
      case "source":
        Qt("error", o);
        break;
      case "img":
      case "image":
      case "link":
        Qt("error", o), Qt("load", o);
        break;
      case "details":
        Qt("toggle", o);
        break;
      case "input":
        Qt("invalid", o), cd(
          o,
          d.value,
          d.defaultValue,
          d.checked,
          d.defaultChecked,
          d.type,
          d.name,
          !0
        ), uo(o);
        break;
      case "select":
        Qt("invalid", o);
        break;
      case "textarea":
        Qt("invalid", o), uy(o, d.value, d.defaultValue, d.children), uo(o);
    }
    c = d.children, typeof c != "string" && typeof c != "number" && typeof c != "bigint" || o.textContent === "" + c || d.suppressHydrationWarning === !0 || Kp(o.textContent, c) ? (d.popover != null && (Qt("beforetoggle", o), Qt("toggle", o)), d.onScroll != null && Qt("scroll", o), d.onScrollEnd != null && Qt("scrollend", o), d.onClick != null && (o.onclick = Gg), o = !0) : o = !1, o || Cs(l);
  }
  function Uy(l) {
    for (mn = l.return; mn; )
      switch (mn.tag) {
        case 5:
        case 13:
          Ml = !1;
          return;
        case 27:
        case 3:
          Ml = !0;
          return;
        default:
          mn = mn.return;
      }
  }
  function Hu(l) {
    if (l !== mn) return !1;
    if (!ve) return Uy(l), ve = !0, !1;
    var o = l.tag, c;
    if ((c = o !== 3 && o !== 27) && ((c = o === 5) && (c = l.type, c = !(c !== "form" && c !== "button") || Pa(l.type, l.memoizedProps)), c = !c), c && Ee && Cs(l), Uy(l), o === 13) {
      if (l = l.memoizedState, l = l !== null ? l.dehydrated : null, !l) throw Error(a(317));
      t: {
        for (l = l.nextSibling, o = 0; l; ) {
          if (l.nodeType === 8)
            if (c = l.data, c === "/$") {
              if (o === 0) {
                Ee = fa(l.nextSibling);
                break t;
              }
              o--;
            } else
              c !== "$" && c !== "$!" && c !== "$?" || o++;
          l = l.nextSibling;
        }
        Ee = null;
      }
    } else
      o === 27 ? (o = Ee, jo(l.type) ? (l = Bo, Bo = null, Ee = l) : Ee = o) : Ee = mn ? fa(l.stateNode.nextSibling) : null;
    return !0;
  }
  function Yu() {
    Ee = mn = null, ve = !1;
  }
  function Gy() {
    var l = Al;
    return l !== null && (Hi === null ? Hi = l : Hi.push.apply(
      Hi,
      l
    ), Al = null), l;
  }
  function ju(l) {
    Al === null ? Al = [l] : Al.push(l);
  }
  var xf = St(null), Rs = null, sa = null;
  function As(l, o, c) {
    At(xf, o._currentValue), o._currentValue = c;
  }
  function Da(l) {
    l._currentValue = xf.current, Q(xf);
  }
  function Ad(l, o, c) {
    for (; l !== null; ) {
      var d = l.alternate;
      if ((l.childLanes & o) !== o ? (l.childLanes |= o, d !== null && (d.childLanes |= o)) : d !== null && (d.childLanes & o) !== o && (d.childLanes |= o), l === c) break;
      l = l.return;
    }
  }
  function Hy(l, o, c, d) {
    var v = l.child;
    for (v !== null && (v.return = l); v !== null; ) {
      var S = v.dependencies;
      if (S !== null) {
        var A = v.child;
        S = S.firstContext;
        t: for (; S !== null; ) {
          var M = S;
          S = v;
          for (var G = 0; G < o.length; G++)
            if (M.context === o[G]) {
              S.lanes |= c, M = S.alternate, M !== null && (M.lanes |= c), Ad(
                S.return,
                c,
                l
              ), d || (A = null);
              break t;
            }
          S = M.next;
        }
      } else if (v.tag === 18) {
        if (A = v.return, A === null) throw Error(a(341));
        A.lanes |= c, S = A.alternate, S !== null && (S.lanes |= c), Ad(A, c, l), A = null;
      } else A = v.child;
      if (A !== null) A.return = v;
      else
        for (A = v; A !== null; ) {
          if (A === l) {
            A = null;
            break;
          }
          if (v = A.sibling, v !== null) {
            v.return = A.return, A = v;
            break;
          }
          A = A.return;
        }
      v = A;
    }
  }
  function Bu(l, o, c, d) {
    l = null;
    for (var v = o, S = !1; v !== null; ) {
      if (!S) {
        if (v.flags & 524288) S = !0;
        else if (v.flags & 262144) break;
      }
      if (v.tag === 10) {
        var A = v.alternate;
        if (A === null) throw Error(a(387));
        if (A = A.memoizedProps, A !== null) {
          var M = v.type;
          Vn(v.pendingProps.value, A.value) || (l !== null ? l.push(M) : l = [M]);
        }
      } else if (v === Ze.current) {
        if (A = v.alternate, A === null) throw Error(a(387));
        A.memoizedState.memoizedState !== v.memoizedState.memoizedState && (l !== null ? l.push(Ii) : l = [Ii]);
      }
      v = v.return;
    }
    l !== null && Hy(
      o,
      l,
      c,
      d
    ), o.flags |= 262144;
  }
  function Cf(l) {
    for (l = l.firstContext; l !== null; ) {
      if (!Vn(
        l.context._currentValue,
        l.memoizedValue
      ))
        return !0;
      l = l.next;
    }
    return !1;
  }
  function bo(l) {
    Rs = l, sa = null, l = l.dependencies, l !== null && (l.firstContext = null);
  }
  function Ln(l) {
    return Yy(Rs, l);
  }
  function Rf(l, o) {
    return Rs === null && bo(l), Yy(l, o);
  }
  function Yy(l, o) {
    var c = o._currentValue;
    if (o = { context: o, memoizedValue: c, next: null }, sa === null) {
      if (l === null) throw Error(a(308));
      sa = o, l.dependencies = { lanes: 0, firstContext: o }, l.flags |= 524288;
    } else sa = sa.next = o;
    return c;
  }
  var Iu = typeof AbortController < "u" ? AbortController : function() {
    var l = [], o = this.signal = {
      aborted: !1,
      addEventListener: function(c, d) {
        l.push(d);
      }
    };
    this.abort = function() {
      o.aborted = !0, l.forEach(function(c) {
        return c();
      });
    };
  }, Md = r.unstable_scheduleCallback, nv = r.unstable_NormalPriority, xn = {
    $$typeof: I,
    Consumer: null,
    Provider: null,
    _currentValue: null,
    _currentValue2: null,
    _threadCount: 0
  };
  function Xu() {
    return {
      controller: new Iu(),
      data: /* @__PURE__ */ new Map(),
      refCount: 0
    };
  }
  function Oa(l) {
    l.refCount--, l.refCount === 0 && Md(nv, function() {
      l.controller.abort();
    });
  }
  var To = null, Af = 0, wl = 0, Cn = null;
  function wd(l, o) {
    if (To === null) {
      var c = To = [];
      Af = 0, wl = Xr(), Cn = {
        status: "pending",
        value: void 0,
        then: function(d) {
          c.push(d);
        }
      };
    }
    return Af++, o.then(Dd, Dd), o;
  }
  function Dd() {
    if (--Af === 0 && To !== null) {
      Cn !== null && (Cn.status = "fulfilled");
      var l = To;
      To = null, wl = 0, Cn = null;
      for (var o = 0; o < l.length; o++) (0, l[o])();
    }
  }
  function iv(l, o) {
    var c = [], d = {
      status: "pending",
      value: null,
      reason: null,
      then: function(v) {
        c.push(v);
      }
    };
    return l.then(
      function() {
        d.status = "fulfilled", d.value = o;
        for (var v = 0; v < c.length; v++) (0, c[v])(o);
      },
      function(v) {
        for (d.status = "rejected", d.reason = v, v = 0; v < c.length; v++)
          (0, c[v])(void 0);
      }
    ), d;
  }
  var Od = Y.S;
  Y.S = function(l, o) {
    typeof o == "object" && o !== null && typeof o.then == "function" && wd(l, o), Od !== null && Od(l, o);
  };
  var La = St(null);
  function Mf() {
    var l = La.current;
    return l !== null ? l : Be.pooledCache;
  }
  function Er(l, o) {
    o === null ? At(La, La.current) : At(La, o.pool);
  }
  function Ld() {
    var l = Mf();
    return l === null ? null : { parent: xn._currentValue, pool: l };
  }
  var xo = Error(a(460)), zd = Error(a(474)), wf = Error(a(542)), Nd = { then: function() {
  } };
  function Ud(l) {
    return l = l.status, l === "fulfilled" || l === "rejected";
  }
  function Df() {
  }
  function jy(l, o, c) {
    switch (c = l[c], c === void 0 ? l.push(o) : c !== o && (o.then(Df, Df), o = c), o.status) {
      case "fulfilled":
        return o.value;
      case "rejected":
        throw l = o.reason, Iy(l), l;
      default:
        if (typeof o.status == "string") o.then(Df, Df);
        else {
          if (l = Be, l !== null && 100 < l.shellSuspendCounter)
            throw Error(a(482));
          l = o, l.status = "pending", l.then(
            function(d) {
              if (o.status === "pending") {
                var v = o;
                v.status = "fulfilled", v.value = d;
              }
            },
            function(d) {
              if (o.status === "pending") {
                var v = o;
                v.status = "rejected", v.reason = d;
              }
            }
          );
        }
        switch (o.status) {
          case "fulfilled":
            return o.value;
          case "rejected":
            throw l = o.reason, Iy(l), l;
        }
        throw br = o, xo;
    }
  }
  var br = null;
  function By() {
    if (br === null) throw Error(a(459));
    var l = br;
    return br = null, l;
  }
  function Iy(l) {
    if (l === xo || l === wf)
      throw Error(a(483));
  }
  var za = !1;
  function Gd(l) {
    l.updateQueue = {
      baseState: l.memoizedState,
      firstBaseUpdate: null,
      lastBaseUpdate: null,
      shared: { pending: null, lanes: 0, hiddenCallbacks: null },
      callbacks: null
    };
  }
  function Hd(l, o) {
    l = l.updateQueue, o.updateQueue === l && (o.updateQueue = {
      baseState: l.baseState,
      firstBaseUpdate: l.firstBaseUpdate,
      lastBaseUpdate: l.lastBaseUpdate,
      shared: l.shared,
      callbacks: null
    });
  }
  function Li(l) {
    return { lane: l, tag: 0, payload: null, callback: null, next: null };
  }
  function Na(l, o, c) {
    var d = l.updateQueue;
    if (d === null) return null;
    if (d = d.shared, Re & 2) {
      var v = d.pending;
      return v === null ? o.next = o : (o.next = v.next, v.next = o), d.pending = o, o = Sf(l), zu(l, null, c), o;
    }
    return vo(l, d, o, c), Sf(l);
  }
  function Tr(l, o, c) {
    if (o = o.updateQueue, o !== null && (o = o.shared, (c & 4194048) !== 0)) {
      var d = o.lanes;
      d &= l.pendingLanes, c |= d, o.lanes = c, On(l, c);
    }
  }
  function Xy(l, o) {
    var c = l.updateQueue, d = l.alternate;
    if (d !== null && (d = d.updateQueue, c === d)) {
      var v = null, S = null;
      if (c = c.firstBaseUpdate, c !== null) {
        do {
          var A = {
            lane: c.lane,
            tag: c.tag,
            payload: c.payload,
            callback: null,
            next: null
          };
          S === null ? v = S = A : S = S.next = A, c = c.next;
        } while (c !== null);
        S === null ? v = S = o : S = S.next = o;
      } else v = S = o;
      c = {
        baseState: d.baseState,
        firstBaseUpdate: v,
        lastBaseUpdate: S,
        shared: d.shared,
        callbacks: d.callbacks
      }, l.updateQueue = c;
      return;
    }
    l = c.lastBaseUpdate, l === null ? c.firstBaseUpdate = o : l.next = o, c.lastBaseUpdate = o;
  }
  var ky = !1;
  function ku() {
    if (ky) {
      var l = Cn;
      if (l !== null) throw l;
    }
  }
  function Ms(l, o, c, d) {
    ky = !1;
    var v = l.updateQueue;
    za = !1;
    var S = v.firstBaseUpdate, A = v.lastBaseUpdate, M = v.shared.pending;
    if (M !== null) {
      v.shared.pending = null;
      var G = M, W = G.next;
      G.next = null, A === null ? S = W : A.next = W, A = G;
      var ct = l.alternate;
      ct !== null && (ct = ct.updateQueue, M = ct.lastBaseUpdate, M !== A && (M === null ? ct.firstBaseUpdate = W : M.next = W, ct.lastBaseUpdate = G));
    }
    if (S !== null) {
      var gt = v.baseState;
      A = 0, ct = W = G = null, M = S;
      do {
        var $ = M.lane & -536870913, et = $ !== M.lane;
        if (et ? (fe & $) === $ : (d & $) === $) {
          $ !== 0 && $ === wl && (ky = !0), ct !== null && (ct = ct.next = {
            lane: 0,
            tag: M.tag,
            payload: M.payload,
            callback: null,
            next: null
          });
          t: {
            var Gt = l, Ht = M;
            $ = o;
            var Te = c;
            switch (Ht.tag) {
              case 1:
                if (Gt = Ht.payload, typeof Gt == "function") {
                  gt = Gt.call(Te, gt, $);
                  break t;
                }
                gt = Gt;
                break t;
              case 3:
                Gt.flags = Gt.flags & -65537 | 128;
              case 0:
                if (Gt = Ht.payload, $ = typeof Gt == "function" ? Gt.call(Te, gt, $) : Gt, $ == null) break t;
                gt = E({}, gt, $);
                break t;
              case 2:
                za = !0;
            }
          }
          $ = M.callback, $ !== null && (l.flags |= 64, et && (l.flags |= 8192), et = v.callbacks, et === null ? v.callbacks = [$] : et.push($));
        } else
          et = {
            lane: $,
            tag: M.tag,
            payload: M.payload,
            callback: M.callback,
            next: null
          }, ct === null ? (W = ct = et, G = gt) : ct = ct.next = et, A |= $;
        if (M = M.next, M === null) {
          if (M = v.shared.pending, M === null)
            break;
          et = M, M = et.next, et.next = null, v.lastBaseUpdate = et, v.shared.pending = null;
        }
      } while (!0);
      ct === null && (G = gt), v.baseState = G, v.firstBaseUpdate = W, v.lastBaseUpdate = ct, S === null && (v.shared.lanes = 0), Us |= A, l.lanes = A, l.memoizedState = gt;
    }
  }
  function Yd(l, o) {
    if (typeof l != "function")
      throw Error(a(191, l));
    l.call(o);
  }
  function Of(l, o) {
    var c = l.callbacks;
    if (c !== null)
      for (l.callbacks = null, l = 0; l < c.length; l++)
        Yd(c[l], o);
  }
  var xr = St(null), Lf = St(0);
  function zn(l, o) {
    l = Ns, At(Lf, l), At(xr, o), Ns = l | o.baseLanes;
  }
  function Vu() {
    At(Lf, Ns), At(xr, xr.current);
  }
  function qu() {
    Ns = Lf.current, Q(xr), Q(Lf);
  }
  var Dl = 0, Pt = null, Ce = null, nn = null, zf = !1, el = !1, Co = !1, oa = 0, nl = 0, ws = null, Vy = 0;
  function ln() {
    throw Error(a(321));
  }
  function jd(l, o) {
    if (o === null) return !1;
    for (var c = 0; c < o.length && c < l.length; c++)
      if (!Vn(l[c], o[c])) return !1;
    return !0;
  }
  function Bd(l, o, c, d, v, S) {
    return Dl = S, Pt = o, o.memoizedState = null, o.updateQueue = null, o.lanes = 0, Y.H = l === null || l.memoizedState === null ? ap : sp, Co = !1, S = c(d, v), Co = !1, el && (S = qy(
      o,
      c,
      d,
      v
    )), Ro(l), S;
  }
  function Ro(l) {
    Y.H = ig;
    var o = Ce !== null && Ce.next !== null;
    if (Dl = 0, nn = Ce = Pt = null, zf = !1, nl = 0, ws = null, o) throw Error(a(300));
    l === null || Rn || (l = l.dependencies, l !== null && Cf(l) && (Rn = !0));
  }
  function qy(l, o, c, d) {
    Pt = l;
    var v = 0;
    do {
      if (el && (ws = null), nl = 0, el = !1, 25 <= v) throw Error(a(301));
      if (v += 1, nn = Ce = null, l.updateQueue != null) {
        var S = l.updateQueue;
        S.lastEffect = null, S.events = null, S.stores = null, S.memoCache != null && (S.memoCache.index = 0);
      }
      Y.H = Ds, S = o(c, d);
    } while (el);
    return S;
  }
  function lv() {
    var l = Y.H, o = l.useState()[0];
    return o = typeof o.then == "function" ? Uf(o) : o, l = l.useState()[0], (Ce !== null ? Ce.memoizedState : null) !== l && (Pt.flags |= 1024), o;
  }
  function Id() {
    var l = oa !== 0;
    return oa = 0, l;
  }
  function Zu(l, o, c) {
    o.updateQueue = l.updateQueue, o.flags &= -2053, l.lanes &= ~c;
  }
  function Xd(l) {
    if (zf) {
      for (l = l.memoizedState; l !== null; ) {
        var o = l.queue;
        o !== null && (o.pending = null), l = l.next;
      }
      zf = !1;
    }
    Dl = 0, nn = Ce = Pt = null, el = !1, nl = oa = 0, ws = null;
  }
  function li() {
    var l = {
      memoizedState: null,
      baseState: null,
      baseQueue: null,
      queue: null,
      next: null
    };
    return nn === null ? Pt.memoizedState = nn = l : nn = nn.next = l, nn;
  }
  function on() {
    if (Ce === null) {
      var l = Pt.alternate;
      l = l !== null ? l.memoizedState : null;
    } else l = Ce.next;
    var o = nn === null ? Pt.memoizedState : nn.next;
    if (o !== null)
      nn = o, Ce = l;
    else {
      if (l === null)
        throw Pt.alternate === null ? Error(a(467)) : Error(a(310));
      Ce = l, l = {
        memoizedState: Ce.memoizedState,
        baseState: Ce.baseState,
        baseQueue: Ce.baseQueue,
        queue: Ce.queue,
        next: null
      }, nn === null ? Pt.memoizedState = nn = l : nn = nn.next = l;
    }
    return nn;
  }
  function Nf() {
    return { lastEffect: null, events: null, stores: null, memoCache: null };
  }
  function Uf(l) {
    var o = nl;
    return nl += 1, ws === null && (ws = []), l = jy(ws, l, o), o = Pt, (nn === null ? o.memoizedState : nn.next) === null && (o = o.alternate, Y.H = o === null || o.memoizedState === null ? ap : sp), l;
  }
  function vn(l) {
    if (l !== null && typeof l == "object") {
      if (typeof l.then == "function") return Uf(l);
      if (l.$$typeof === I) return Ln(l);
    }
    throw Error(a(438, String(l)));
  }
  function kd(l) {
    var o = null, c = Pt.updateQueue;
    if (c !== null && (o = c.memoCache), o == null) {
      var d = Pt.alternate;
      d !== null && (d = d.updateQueue, d !== null && (d = d.memoCache, d != null && (o = {
        data: d.data.map(function(v) {
          return v.slice();
        }),
        index: 0
      })));
    }
    if (o == null && (o = { data: [], index: 0 }), c === null && (c = Nf(), Pt.updateQueue = c), c.memoCache = o, c = o.data[o.index], c === void 0)
      for (c = o.data[o.index] = Array(l), d = 0; d < l; d++)
        c[d] = Et;
    return o.index++, c;
  }
  function Ua(l, o) {
    return typeof o == "function" ? o(l) : o;
  }
  function Gf(l) {
    var o = on();
    return Vd(o, Ce, l);
  }
  function Vd(l, o, c) {
    var d = l.queue;
    if (d === null) throw Error(a(311));
    d.lastRenderedReducer = c;
    var v = l.baseQueue, S = d.pending;
    if (S !== null) {
      if (v !== null) {
        var A = v.next;
        v.next = S.next, S.next = A;
      }
      o.baseQueue = v = S, d.pending = null;
    }
    if (S = l.baseState, v === null) l.memoizedState = S;
    else {
      o = v.next;
      var M = A = null, G = null, W = o, ct = !1;
      do {
        var gt = W.lane & -536870913;
        if (gt !== W.lane ? (fe & gt) === gt : (Dl & gt) === gt) {
          var $ = W.revertLane;
          if ($ === 0)
            G !== null && (G = G.next = {
              lane: 0,
              revertLane: 0,
              action: W.action,
              hasEagerState: W.hasEagerState,
              eagerState: W.eagerState,
              next: null
            }), gt === wl && (ct = !0);
          else if ((Dl & $) === $) {
            W = W.next, $ === wl && (ct = !0);
            continue;
          } else
            gt = {
              lane: 0,
              revertLane: W.revertLane,
              action: W.action,
              hasEagerState: W.hasEagerState,
              eagerState: W.eagerState,
              next: null
            }, G === null ? (M = G = gt, A = S) : G = G.next = gt, Pt.lanes |= $, Us |= $;
          gt = W.action, Co && c(S, gt), S = W.hasEagerState ? W.eagerState : c(S, gt);
        } else
          $ = {
            lane: gt,
            revertLane: W.revertLane,
            action: W.action,
            hasEagerState: W.hasEagerState,
            eagerState: W.eagerState,
            next: null
          }, G === null ? (M = G = $, A = S) : G = G.next = $, Pt.lanes |= gt, Us |= gt;
        W = W.next;
      } while (W !== null && W !== o);
      if (G === null ? A = S : G.next = M, !Vn(S, l.memoizedState) && (Rn = !0, ct && (c = Cn, c !== null)))
        throw c;
      l.memoizedState = S, l.baseState = A, l.baseQueue = G, d.lastRenderedState = S;
    }
    return v === null && (d.lanes = 0), [l.memoizedState, d.dispatch];
  }
  function qd(l) {
    var o = on(), c = o.queue;
    if (c === null) throw Error(a(311));
    c.lastRenderedReducer = l;
    var d = c.dispatch, v = c.pending, S = o.memoizedState;
    if (v !== null) {
      c.pending = null;
      var A = v = v.next;
      do
        S = l(S, A.action), A = A.next;
      while (A !== v);
      Vn(S, o.memoizedState) || (Rn = !0), o.memoizedState = S, o.baseQueue === null && (o.baseState = S), c.lastRenderedState = S;
    }
    return [S, d];
  }
  function Hf(l, o, c) {
    var d = Pt, v = on(), S = ve;
    if (S) {
      if (c === void 0) throw Error(a(407));
      c = c();
    } else c = o();
    var A = !Vn(
      (Ce || v).memoizedState,
      c
    );
    A && (v.memoizedState = c, Rn = !0), v = v.queue;
    var M = Fy.bind(null, d, v, l);
    if (Ge(2048, 8, M, [l]), v.getSnapshot !== o || A || nn !== null && nn.memoizedState.tag & 1) {
      if (d.flags |= 2048, zi(
        9,
        Bf(),
        Zy.bind(
          null,
          d,
          v,
          c,
          o
        ),
        null
      ), Be === null) throw Error(a(349));
      S || Dl & 124 || Zd(d, o, c);
    }
    return c;
  }
  function Zd(l, o, c) {
    l.flags |= 16384, l = { getSnapshot: o, value: c }, o = Pt.updateQueue, o === null ? (o = Nf(), Pt.updateQueue = o, o.stores = [l]) : (c = o.stores, c === null ? o.stores = [l] : c.push(l));
  }
  function Zy(l, o, c, d) {
    o.value = c, o.getSnapshot = d, Ky(o) && Fd(l);
  }
  function Fy(l, o, c) {
    return c(function() {
      Ky(o) && Fd(l);
    });
  }
  function Ky(l) {
    var o = l.getSnapshot;
    l = l.value;
    try {
      var c = o();
      return !Vn(l, c);
    } catch {
      return !0;
    }
  }
  function Fd(l) {
    var o = Ma(l, 2);
    o !== null && al(o, l, 2);
  }
  function Yf(l) {
    var o = li();
    if (typeof l == "function") {
      var c = l;
      if (l = c(), Co) {
        Wl(!0);
        try {
          c();
        } finally {
          Wl(!1);
        }
      }
    }
    return o.memoizedState = o.baseState = l, o.queue = {
      pending: null,
      lanes: 0,
      dispatch: null,
      lastRenderedReducer: Ua,
      lastRenderedState: l
    }, o;
  }
  function Kd(l, o, c, d) {
    return l.baseState = c, Vd(
      l,
      Ce,
      typeof d == "function" ? d : Ua
    );
  }
  function av(l, o, c, d, v) {
    if (Mr(l)) throw Error(a(485));
    if (l = o.action, l !== null) {
      var S = {
        payload: v,
        action: l,
        next: null,
        isTransition: !0,
        status: "pending",
        value: null,
        reason: null,
        listeners: [],
        then: function(A) {
          S.listeners.push(A);
        }
      };
      Y.T !== null ? c(!0) : S.isTransition = !1, d(S), c = o.pending, c === null ? (S.next = o.pending = S, Pd(o, S)) : (S.next = c.next, o.pending = c.next = S);
    }
  }
  function Pd(l, o) {
    var c = o.action, d = o.payload, v = l.state;
    if (o.isTransition) {
      var S = Y.T, A = {};
      Y.T = A;
      try {
        var M = c(v, d), G = Y.S;
        G !== null && G(A, M), jf(l, o, M);
      } catch (W) {
        Wd(l, o, W);
      } finally {
        Y.T = S;
      }
    } else
      try {
        S = c(v, d), jf(l, o, S);
      } catch (W) {
        Wd(l, o, W);
      }
  }
  function jf(l, o, c) {
    c !== null && typeof c == "object" && typeof c.then == "function" ? c.then(
      function(d) {
        Qd(l, o, d);
      },
      function(d) {
        return Wd(l, o, d);
      }
    ) : Qd(l, o, c);
  }
  function Qd(l, o, c) {
    o.status = "fulfilled", o.value = c, Py(o), l.state = c, o = l.pending, o !== null && (c = o.next, c === o ? l.pending = null : (c = c.next, o.next = c, Pd(l, c)));
  }
  function Wd(l, o, c) {
    var d = l.pending;
    if (l.pending = null, d !== null) {
      d = d.next;
      do
        o.status = "rejected", o.reason = c, Py(o), o = o.next;
      while (o !== d);
    }
    l.action = null;
  }
  function Py(l) {
    l = l.listeners;
    for (var o = 0; o < l.length; o++) (0, l[o])();
  }
  function Jd(l, o) {
    return o;
  }
  function Qy(l, o) {
    if (ve) {
      var c = Be.formState;
      if (c !== null) {
        t: {
          var d = Pt;
          if (ve) {
            if (Ee) {
              e: {
                for (var v = Ee, S = Ml; v.nodeType !== 8; ) {
                  if (!S) {
                    v = null;
                    break e;
                  }
                  if (v = fa(
                    v.nextSibling
                  ), v === null) {
                    v = null;
                    break e;
                  }
                }
                S = v.data, v = S === "F!" || S === "F" ? v : null;
              }
              if (v) {
                Ee = fa(
                  v.nextSibling
                ), d = v.data === "F!";
                break t;
              }
            }
            Cs(d);
          }
          d = !1;
        }
        d && (o = c[0]);
      }
    }
    return c = li(), c.memoizedState = c.baseState = o, d = {
      pending: null,
      lanes: 0,
      dispatch: null,
      lastRenderedReducer: Jd,
      lastRenderedState: o
    }, c.queue = d, c = ip.bind(
      null,
      Pt,
      d
    ), d.dispatch = c, d = Yf(!1), S = kf.bind(
      null,
      Pt,
      !1,
      d.queue
    ), d = li(), v = {
      state: o,
      dispatch: null,
      action: l,
      pending: null
    }, d.queue = v, c = av.bind(
      null,
      Pt,
      v,
      S,
      c
    ), v.dispatch = c, d.memoizedState = l, [o, c, !1];
  }
  function Ga(l) {
    var o = on();
    return $d(o, Ce, l);
  }
  function $d(l, o, c) {
    if (o = Vd(
      l,
      o,
      Jd
    )[0], l = Gf(Ua)[0], typeof o == "object" && o !== null && typeof o.then == "function")
      try {
        var d = Uf(o);
      } catch (A) {
        throw A === xo ? wf : A;
      }
    else d = o;
    o = on();
    var v = o.queue, S = v.dispatch;
    return c !== o.memoizedState && (Pt.flags |= 2048, zi(
      9,
      Bf(),
      GS.bind(null, v, c),
      null
    )), [d, S, l];
  }
  function GS(l, o) {
    l.action = o;
  }
  function tg(l) {
    var o = on(), c = Ce;
    if (c !== null)
      return $d(o, c, l);
    on(), o = o.memoizedState, c = on();
    var d = c.queue.dispatch;
    return c.memoizedState = l, [o, d, !1];
  }
  function zi(l, o, c, d) {
    return l = { tag: l, create: c, deps: d, inst: o, next: null }, o = Pt.updateQueue, o === null && (o = Nf(), Pt.updateQueue = o), c = o.lastEffect, c === null ? o.lastEffect = l.next = l : (d = c.next, c.next = l, l.next = d, o.lastEffect = l), l;
  }
  function Bf() {
    return { destroy: void 0, resource: void 0 };
  }
  function If() {
    return on().memoizedState;
  }
  function Ao(l, o, c, d) {
    var v = li();
    d = d === void 0 ? null : d, Pt.flags |= l, v.memoizedState = zi(
      1 | o,
      Bf(),
      c,
      d
    );
  }
  function Ge(l, o, c, d) {
    var v = on();
    d = d === void 0 ? null : d;
    var S = v.memoizedState.inst;
    Ce !== null && d !== null && jd(d, Ce.memoizedState.deps) ? v.memoizedState = zi(o, S, c, d) : (Pt.flags |= l, v.memoizedState = zi(
      1 | o,
      S,
      c,
      d
    ));
  }
  function sv(l, o) {
    Ao(8390656, 8, l, o);
  }
  function ov(l, o) {
    Ge(2048, 8, l, o);
  }
  function Wy(l, o) {
    return Ge(4, 2, l, o);
  }
  function ra(l, o) {
    return Ge(4, 4, l, o);
  }
  function Jy(l, o) {
    if (typeof o == "function") {
      l = l();
      var c = o(l);
      return function() {
        typeof c == "function" ? c() : o(null);
      };
    }
    if (o != null)
      return l = l(), o.current = l, function() {
        o.current = null;
      };
  }
  function eg(l, o, c) {
    c = c != null ? c.concat([l]) : null, Ge(4, 4, Jy.bind(null, o, l), c);
  }
  function Cr() {
  }
  function Rr(l, o) {
    var c = on();
    o = o === void 0 ? null : o;
    var d = c.memoizedState;
    return o !== null && jd(o, d[1]) ? d[0] : (c.memoizedState = [l, o], l);
  }
  function $y(l, o) {
    var c = on();
    o = o === void 0 ? null : o;
    var d = c.memoizedState;
    if (o !== null && jd(o, d[1]))
      return d[0];
    if (d = l(), Co) {
      Wl(!0);
      try {
        l();
      } finally {
        Wl(!1);
      }
    }
    return c.memoizedState = [d, o], d;
  }
  function Xf(l, o, c) {
    return c === void 0 || Dl & 1073741824 ? l.memoizedState = o : (l.memoizedState = c, l = Up(), Pt.lanes |= l, Us |= l, c);
  }
  function tp(l, o, c, d) {
    return Vn(c, o) ? c : xr.current !== null ? (l = Xf(l, c, d), Vn(l, o) || (Rn = !0), l) : Dl & 42 ? (l = Up(), Pt.lanes |= l, Us |= l, o) : (Rn = !0, l.memoizedState = c);
  }
  function rv(l, o, c, d, v) {
    var S = K.p;
    K.p = S !== 0 && 8 > S ? S : 8;
    var A = Y.T, M = {};
    Y.T = M, kf(l, !1, o, c);
    try {
      var G = v(), W = Y.S;
      if (W !== null && W(M, G), G !== null && typeof G == "object" && typeof G.then == "function") {
        var ct = iv(
          G,
          d
        );
        Ar(
          l,
          o,
          ct,
          ll(l)
        );
      } else
        Ar(
          l,
          o,
          d,
          ll(l)
        );
    } catch (gt) {
      Ar(
        l,
        o,
        { then: function() {
        }, status: "rejected", reason: gt },
        ll()
      );
    } finally {
      K.p = S, Y.T = A;
    }
  }
  function HS() {
  }
  function ng(l, o, c, d) {
    if (l.tag !== 5) throw Error(a(476));
    var v = uv(l).queue;
    rv(
      l,
      v,
      o,
      nt,
      c === null ? HS : function() {
        return Fu(l), c(d);
      }
    );
  }
  function uv(l) {
    var o = l.memoizedState;
    if (o !== null) return o;
    o = {
      memoizedState: nt,
      baseState: nt,
      baseQueue: null,
      queue: {
        pending: null,
        lanes: 0,
        dispatch: null,
        lastRenderedReducer: Ua,
        lastRenderedState: nt
      },
      next: null
    };
    var c = {};
    return o.next = {
      memoizedState: c,
      baseState: c,
      baseQueue: null,
      queue: {
        pending: null,
        lanes: 0,
        dispatch: null,
        lastRenderedReducer: Ua,
        lastRenderedState: c
      },
      next: null
    }, l.memoizedState = o, l = l.alternate, l !== null && (l.memoizedState = o), o;
  }
  function Fu(l) {
    var o = uv(l).next.queue;
    Ar(l, o, {}, ll());
  }
  function Ol() {
    return Ln(Ii);
  }
  function ep() {
    return on().memoizedState;
  }
  function cv() {
    return on().memoizedState;
  }
  function fv(l) {
    for (var o = l.return; o !== null; ) {
      switch (o.tag) {
        case 24:
        case 3:
          var c = ll();
          l = Li(c);
          var d = Na(o, l, c);
          d !== null && (al(d, o, c), Tr(d, o, c)), o = { cache: Xu() }, l.payload = o;
          return;
      }
      o = o.return;
    }
  }
  function np(l, o, c) {
    var d = ll();
    c = {
      lane: d,
      revertLane: 0,
      action: c,
      hasEagerState: !1,
      eagerState: null,
      next: null
    }, Mr(l) ? hv(o, c) : (c = Lu(l, o, c, d), c !== null && (al(c, l, d), lp(c, o, d)));
  }
  function ip(l, o, c) {
    var d = ll();
    Ar(l, o, c, d);
  }
  function Ar(l, o, c, d) {
    var v = {
      lane: d,
      revertLane: 0,
      action: c,
      hasEagerState: !1,
      eagerState: null,
      next: null
    };
    if (Mr(l)) hv(o, v);
    else {
      var S = l.alternate;
      if (l.lanes === 0 && (S === null || S.lanes === 0) && (S = o.lastRenderedReducer, S !== null))
        try {
          var A = o.lastRenderedState, M = S(A, c);
          if (v.hasEagerState = !0, v.eagerState = M, Vn(M, A))
            return vo(l, o, v, 0), Be === null && ia(), !1;
        } catch {
        } finally {
        }
      if (c = Lu(l, o, v, d), c !== null)
        return al(c, l, d), lp(c, o, d), !0;
    }
    return !1;
  }
  function kf(l, o, c, d) {
    if (d = {
      lane: 2,
      revertLane: Xr(),
      action: d,
      hasEagerState: !1,
      eagerState: null,
      next: null
    }, Mr(l)) {
      if (o) throw Error(a(479));
    } else
      o = Lu(
        l,
        c,
        d,
        2
      ), o !== null && al(o, l, 2);
  }
  function Mr(l) {
    var o = l.alternate;
    return l === Pt || o !== null && o === Pt;
  }
  function hv(l, o) {
    el = zf = !0;
    var c = l.pending;
    c === null ? o.next = o : (o.next = c.next, c.next = o), l.pending = o;
  }
  function lp(l, o, c) {
    if (c & 4194048) {
      var d = o.lanes;
      d &= l.pendingLanes, c |= d, o.lanes = c, On(l, c);
    }
  }
  var ig = {
    readContext: Ln,
    use: vn,
    useCallback: ln,
    useContext: ln,
    useEffect: ln,
    useImperativeHandle: ln,
    useLayoutEffect: ln,
    useInsertionEffect: ln,
    useMemo: ln,
    useReducer: ln,
    useRef: ln,
    useState: ln,
    useDebugValue: ln,
    useDeferredValue: ln,
    useTransition: ln,
    useSyncExternalStore: ln,
    useId: ln,
    useHostTransitionStatus: ln,
    useFormState: ln,
    useActionState: ln,
    useOptimistic: ln,
    useMemoCache: ln,
    useCacheRefresh: ln
  }, ap = {
    readContext: Ln,
    use: vn,
    useCallback: function(l, o) {
      return li().memoizedState = [
        l,
        o === void 0 ? null : o
      ], l;
    },
    useContext: Ln,
    useEffect: sv,
    useImperativeHandle: function(l, o, c) {
      c = c != null ? c.concat([l]) : null, Ao(
        4194308,
        4,
        Jy.bind(null, o, l),
        c
      );
    },
    useLayoutEffect: function(l, o) {
      return Ao(4194308, 4, l, o);
    },
    useInsertionEffect: function(l, o) {
      Ao(4, 2, l, o);
    },
    useMemo: function(l, o) {
      var c = li();
      o = o === void 0 ? null : o;
      var d = l();
      if (Co) {
        Wl(!0);
        try {
          l();
        } finally {
          Wl(!1);
        }
      }
      return c.memoizedState = [d, o], d;
    },
    useReducer: function(l, o, c) {
      var d = li();
      if (c !== void 0) {
        var v = c(o);
        if (Co) {
          Wl(!0);
          try {
            c(o);
          } finally {
            Wl(!1);
          }
        }
      } else v = o;
      return d.memoizedState = d.baseState = v, l = {
        pending: null,
        lanes: 0,
        dispatch: null,
        lastRenderedReducer: l,
        lastRenderedState: v
      }, d.queue = l, l = l.dispatch = np.bind(
        null,
        Pt,
        l
      ), [d.memoizedState, l];
    },
    useRef: function(l) {
      var o = li();
      return l = { current: l }, o.memoizedState = l;
    },
    useState: function(l) {
      l = Yf(l);
      var o = l.queue, c = ip.bind(null, Pt, o);
      return o.dispatch = c, [l.memoizedState, c];
    },
    useDebugValue: Cr,
    useDeferredValue: function(l, o) {
      var c = li();
      return Xf(c, l, o);
    },
    useTransition: function() {
      var l = Yf(!1);
      return l = rv.bind(
        null,
        Pt,
        l.queue,
        !0,
        !1
      ), li().memoizedState = l, [!1, l];
    },
    useSyncExternalStore: function(l, o, c) {
      var d = Pt, v = li();
      if (ve) {
        if (c === void 0)
          throw Error(a(407));
        c = c();
      } else {
        if (c = o(), Be === null)
          throw Error(a(349));
        fe & 124 || Zd(d, o, c);
      }
      v.memoizedState = c;
      var S = { value: c, getSnapshot: o };
      return v.queue = S, sv(Fy.bind(null, d, S, l), [
        l
      ]), d.flags |= 2048, zi(
        9,
        Bf(),
        Zy.bind(
          null,
          d,
          S,
          c,
          o
        ),
        null
      ), c;
    },
    useId: function() {
      var l = li(), o = Be.identifierPrefix;
      if (ve) {
        var c = sn, d = aa;
        c = (d & ~(1 << 32 - Bn(d) - 1)).toString(32) + c, o = "«" + o + "R" + c, c = oa++, 0 < c && (o += "H" + c.toString(32)), o += "»";
      } else
        c = Vy++, o = "«" + o + "r" + c.toString(32) + "»";
      return l.memoizedState = o;
    },
    useHostTransitionStatus: Ol,
    useFormState: Qy,
    useActionState: Qy,
    useOptimistic: function(l) {
      var o = li();
      o.memoizedState = o.baseState = l;
      var c = {
        pending: null,
        lanes: 0,
        dispatch: null,
        lastRenderedReducer: null,
        lastRenderedState: null
      };
      return o.queue = c, o = kf.bind(
        null,
        Pt,
        !0,
        c
      ), c.dispatch = o, [l, o];
    },
    useMemoCache: kd,
    useCacheRefresh: function() {
      return li().memoizedState = fv.bind(
        null,
        Pt
      );
    }
  }, sp = {
    readContext: Ln,
    use: vn,
    useCallback: Rr,
    useContext: Ln,
    useEffect: ov,
    useImperativeHandle: eg,
    useInsertionEffect: Wy,
    useLayoutEffect: ra,
    useMemo: $y,
    useReducer: Gf,
    useRef: If,
    useState: function() {
      return Gf(Ua);
    },
    useDebugValue: Cr,
    useDeferredValue: function(l, o) {
      var c = on();
      return tp(
        c,
        Ce.memoizedState,
        l,
        o
      );
    },
    useTransition: function() {
      var l = Gf(Ua)[0], o = on().memoizedState;
      return [
        typeof l == "boolean" ? l : Uf(l),
        o
      ];
    },
    useSyncExternalStore: Hf,
    useId: ep,
    useHostTransitionStatus: Ol,
    useFormState: Ga,
    useActionState: Ga,
    useOptimistic: function(l, o) {
      var c = on();
      return Kd(c, Ce, l, o);
    },
    useMemoCache: kd,
    useCacheRefresh: cv
  }, Ds = {
    readContext: Ln,
    use: vn,
    useCallback: Rr,
    useContext: Ln,
    useEffect: ov,
    useImperativeHandle: eg,
    useInsertionEffect: Wy,
    useLayoutEffect: ra,
    useMemo: $y,
    useReducer: qd,
    useRef: If,
    useState: function() {
      return qd(Ua);
    },
    useDebugValue: Cr,
    useDeferredValue: function(l, o) {
      var c = on();
      return Ce === null ? Xf(c, l, o) : tp(
        c,
        Ce.memoizedState,
        l,
        o
      );
    },
    useTransition: function() {
      var l = qd(Ua)[0], o = on().memoizedState;
      return [
        typeof l == "boolean" ? l : Uf(l),
        o
      ];
    },
    useSyncExternalStore: Hf,
    useId: ep,
    useHostTransitionStatus: Ol,
    useFormState: tg,
    useActionState: tg,
    useOptimistic: function(l, o) {
      var c = on();
      return Ce !== null ? Kd(c, Ce, l, o) : (c.baseState = l, [l, c.queue.dispatch]);
    },
    useMemoCache: kd,
    useCacheRefresh: cv
  }, wr = null, Ku = 0;
  function lg(l) {
    var o = Ku;
    return Ku += 1, wr === null && (wr = []), jy(wr, l, o);
  }
  function Dr(l, o) {
    o = o.props.ref, l.ref = o !== void 0 ? o : null;
  }
  function ai(l, o) {
    throw o.$$typeof === x ? Error(a(525)) : (l = Object.prototype.toString.call(o), Error(
      a(
        31,
        l === "[object Object]" ? "object with keys {" + Object.keys(o).join(", ") + "}" : l
      )
    ));
  }
  function op(l) {
    var o = l._init;
    return o(l._payload);
  }
  function Ni(l) {
    function o(q, V) {
      if (l) {
        var P = q.deletions;
        P === null ? (q.deletions = [V], q.flags |= 16) : P.push(V);
      }
    }
    function c(q, V) {
      if (!l) return null;
      for (; V !== null; )
        o(q, V), V = V.sibling;
      return null;
    }
    function d(q) {
      for (var V = /* @__PURE__ */ new Map(); q !== null; )
        q.key !== null ? V.set(q.key, q) : V.set(q.index, q), q = q.sibling;
      return V;
    }
    function v(q, V) {
      return q = la(q, V), q.index = 0, q.sibling = null, q;
    }
    function S(q, V, P) {
      return q.index = P, l ? (P = q.alternate, P !== null ? (P = P.index, P < V ? (q.flags |= 67108866, V) : P) : (q.flags |= 67108866, V)) : (q.flags |= 1048576, V);
    }
    function A(q) {
      return l && q.alternate === null && (q.flags |= 67108866), q;
    }
    function M(q, V, P, ht) {
      return V === null || V.tag !== 6 ? (V = Uu(P, q.mode, ht), V.return = q, V) : (V = v(V, P), V.return = q, V);
    }
    function G(q, V, P, ht) {
      var Mt = P.type;
      return Mt === L ? ct(
        q,
        V,
        P.props.children,
        ht,
        P.key
      ) : V !== null && (V.elementType === Mt || typeof Mt == "object" && Mt !== null && Mt.$$typeof === yt && op(Mt) === V.type) ? (V = v(V, P.props), Dr(V, P), V.return = q, V) : (V = _t(
        P.type,
        P.key,
        P.props,
        null,
        q.mode,
        ht
      ), Dr(V, P), V.return = q, V);
    }
    function W(q, V, P, ht) {
      return V === null || V.tag !== 4 || V.stateNode.containerInfo !== P.containerInfo || V.stateNode.implementation !== P.implementation ? (V = en(P, q.mode, ht), V.return = q, V) : (V = v(V, P.children || []), V.return = q, V);
    }
    function ct(q, V, P, ht, Mt) {
      return V === null || V.tag !== 7 ? (V = Cl(
        P,
        q.mode,
        ht,
        Mt
      ), V.return = q, V) : (V = v(V, P), V.return = q, V);
    }
    function gt(q, V, P) {
      if (typeof V == "string" && V !== "" || typeof V == "number" || typeof V == "bigint")
        return V = Uu(
          "" + V,
          q.mode,
          P
        ), V.return = q, V;
      if (typeof V == "object" && V !== null) {
        switch (V.$$typeof) {
          case R:
            return P = _t(
              V.type,
              V.key,
              V.props,
              null,
              q.mode,
              P
            ), Dr(P, V), P.return = q, P;
          case w:
            return V = en(
              V,
              q.mode,
              P
            ), V.return = q, V;
          case yt:
            var ht = V._init;
            return V = ht(V._payload), gt(q, V, P);
        }
        if (Rt(V) || pt(V))
          return V = Cl(
            V,
            q.mode,
            P,
            null
          ), V.return = q, V;
        if (typeof V.then == "function")
          return gt(q, lg(V), P);
        if (V.$$typeof === I)
          return gt(
            q,
            Rf(q, V),
            P
          );
        ai(q, V);
      }
      return null;
    }
    function $(q, V, P, ht) {
      var Mt = V !== null ? V.key : null;
      if (typeof P == "string" && P !== "" || typeof P == "number" || typeof P == "bigint")
        return Mt !== null ? null : M(q, V, "" + P, ht);
      if (typeof P == "object" && P !== null) {
        switch (P.$$typeof) {
          case R:
            return P.key === Mt ? G(q, V, P, ht) : null;
          case w:
            return P.key === Mt ? W(q, V, P, ht) : null;
          case yt:
            return Mt = P._init, P = Mt(P._payload), $(q, V, P, ht);
        }
        if (Rt(P) || pt(P))
          return Mt !== null ? null : ct(q, V, P, ht, null);
        if (typeof P.then == "function")
          return $(
            q,
            V,
            lg(P),
            ht
          );
        if (P.$$typeof === I)
          return $(
            q,
            V,
            Rf(q, P),
            ht
          );
        ai(q, P);
      }
      return null;
    }
    function et(q, V, P, ht, Mt) {
      if (typeof ht == "string" && ht !== "" || typeof ht == "number" || typeof ht == "bigint")
        return q = q.get(P) || null, M(V, q, "" + ht, Mt);
      if (typeof ht == "object" && ht !== null) {
        switch (ht.$$typeof) {
          case R:
            return q = q.get(
              ht.key === null ? P : ht.key
            ) || null, G(V, q, ht, Mt);
          case w:
            return q = q.get(
              ht.key === null ? P : ht.key
            ) || null, W(V, q, ht, Mt);
          case yt:
            var ie = ht._init;
            return ht = ie(ht._payload), et(
              q,
              V,
              P,
              ht,
              Mt
            );
        }
        if (Rt(ht) || pt(ht))
          return q = q.get(P) || null, ct(V, q, ht, Mt, null);
        if (typeof ht.then == "function")
          return et(
            q,
            V,
            P,
            lg(ht),
            Mt
          );
        if (ht.$$typeof === I)
          return et(
            q,
            V,
            P,
            Rf(V, ht),
            Mt
          );
        ai(V, ht);
      }
      return null;
    }
    function Gt(q, V, P, ht) {
      for (var Mt = null, ie = null, Lt = V, Bt = V = 0, Gn = null; Lt !== null && Bt < P.length; Bt++) {
        Lt.index > Bt ? (Gn = Lt, Lt = null) : Gn = Lt.sibling;
        var pe = $(
          q,
          Lt,
          P[Bt],
          ht
        );
        if (pe === null) {
          Lt === null && (Lt = Gn);
          break;
        }
        l && Lt && pe.alternate === null && o(q, Lt), V = S(pe, V, Bt), ie === null ? Mt = pe : ie.sibling = pe, ie = pe, Lt = Gn;
      }
      if (Bt === P.length)
        return c(q, Lt), ve && me(q, Bt), Mt;
      if (Lt === null) {
        for (; Bt < P.length; Bt++)
          Lt = gt(q, P[Bt], ht), Lt !== null && (V = S(
            Lt,
            V,
            Bt
          ), ie === null ? Mt = Lt : ie.sibling = Lt, ie = Lt);
        return ve && me(q, Bt), Mt;
      }
      for (Lt = d(Lt); Bt < P.length; Bt++)
        Gn = et(
          Lt,
          q,
          Bt,
          P[Bt],
          ht
        ), Gn !== null && (l && Gn.alternate !== null && Lt.delete(
          Gn.key === null ? Bt : Gn.key
        ), V = S(
          Gn,
          V,
          Bt
        ), ie === null ? Mt = Gn : ie.sibling = Gn, ie = Gn);
      return l && Lt.forEach(function(qo) {
        return o(q, qo);
      }), ve && me(q, Bt), Mt;
    }
    function Ht(q, V, P, ht) {
      if (P == null) throw Error(a(151));
      for (var Mt = null, ie = null, Lt = V, Bt = V = 0, Gn = null, pe = P.next(); Lt !== null && !pe.done; Bt++, pe = P.next()) {
        Lt.index > Bt ? (Gn = Lt, Lt = null) : Gn = Lt.sibling;
        var qo = $(q, Lt, pe.value, ht);
        if (qo === null) {
          Lt === null && (Lt = Gn);
          break;
        }
        l && Lt && qo.alternate === null && o(q, Lt), V = S(qo, V, Bt), ie === null ? Mt = qo : ie.sibling = qo, ie = qo, Lt = Gn;
      }
      if (pe.done)
        return c(q, Lt), ve && me(q, Bt), Mt;
      if (Lt === null) {
        for (; !pe.done; Bt++, pe = P.next())
          pe = gt(q, pe.value, ht), pe !== null && (V = S(pe, V, Bt), ie === null ? Mt = pe : ie.sibling = pe, ie = pe);
        return ve && me(q, Bt), Mt;
      }
      for (Lt = d(Lt); !pe.done; Bt++, pe = P.next())
        pe = et(Lt, q, Bt, pe.value, ht), pe !== null && (l && pe.alternate !== null && Lt.delete(pe.key === null ? Bt : pe.key), V = S(pe, V, Bt), ie === null ? Mt = pe : ie.sibling = pe, ie = pe);
      return l && Lt.forEach(function(PS) {
        return o(q, PS);
      }), ve && me(q, Bt), Mt;
    }
    function Te(q, V, P, ht) {
      if (typeof P == "object" && P !== null && P.type === L && P.key === null && (P = P.props.children), typeof P == "object" && P !== null) {
        switch (P.$$typeof) {
          case R:
            t: {
              for (var Mt = P.key; V !== null; ) {
                if (V.key === Mt) {
                  if (Mt = P.type, Mt === L) {
                    if (V.tag === 7) {
                      c(
                        q,
                        V.sibling
                      ), ht = v(
                        V,
                        P.props.children
                      ), ht.return = q, q = ht;
                      break t;
                    }
                  } else if (V.elementType === Mt || typeof Mt == "object" && Mt !== null && Mt.$$typeof === yt && op(Mt) === V.type) {
                    c(
                      q,
                      V.sibling
                    ), ht = v(V, P.props), Dr(ht, P), ht.return = q, q = ht;
                    break t;
                  }
                  c(q, V);
                  break;
                } else o(q, V);
                V = V.sibling;
              }
              P.type === L ? (ht = Cl(
                P.props.children,
                q.mode,
                ht,
                P.key
              ), ht.return = q, q = ht) : (ht = _t(
                P.type,
                P.key,
                P.props,
                null,
                q.mode,
                ht
              ), Dr(ht, P), ht.return = q, q = ht);
            }
            return A(q);
          case w:
            t: {
              for (Mt = P.key; V !== null; ) {
                if (V.key === Mt)
                  if (V.tag === 4 && V.stateNode.containerInfo === P.containerInfo && V.stateNode.implementation === P.implementation) {
                    c(
                      q,
                      V.sibling
                    ), ht = v(V, P.children || []), ht.return = q, q = ht;
                    break t;
                  } else {
                    c(q, V);
                    break;
                  }
                else o(q, V);
                V = V.sibling;
              }
              ht = en(P, q.mode, ht), ht.return = q, q = ht;
            }
            return A(q);
          case yt:
            return Mt = P._init, P = Mt(P._payload), Te(
              q,
              V,
              P,
              ht
            );
        }
        if (Rt(P))
          return Gt(
            q,
            V,
            P,
            ht
          );
        if (pt(P)) {
          if (Mt = pt(P), typeof Mt != "function") throw Error(a(150));
          return P = Mt.call(P), Ht(
            q,
            V,
            P,
            ht
          );
        }
        if (typeof P.then == "function")
          return Te(
            q,
            V,
            lg(P),
            ht
          );
        if (P.$$typeof === I)
          return Te(
            q,
            V,
            Rf(q, P),
            ht
          );
        ai(q, P);
      }
      return typeof P == "string" && P !== "" || typeof P == "number" || typeof P == "bigint" ? (P = "" + P, V !== null && V.tag === 6 ? (c(q, V.sibling), ht = v(V, P), ht.return = q, q = ht) : (c(q, V), ht = Uu(P, q.mode, ht), ht.return = q, q = ht), A(q)) : c(q, V);
    }
    return function(q, V, P, ht) {
      try {
        Ku = 0;
        var Mt = Te(
          q,
          V,
          P,
          ht
        );
        return wr = null, Mt;
      } catch (Lt) {
        if (Lt === xo || Lt === wf) throw Lt;
        var ie = Di(29, Lt, null, q.mode);
        return ie.lanes = ht, ie.return = q, ie;
      } finally {
      }
    };
  }
  var Or = Ni(!0), Ha = Ni(!1), il = St(null), si = null;
  function Os(l) {
    var o = l.alternate;
    At(He, He.current & 1), At(il, l), si === null && (o === null || xr.current !== null || o.memoizedState !== null) && (si = l);
  }
  function Ya(l) {
    if (l.tag === 22) {
      if (At(He, He.current), At(il, l), si === null) {
        var o = l.alternate;
        o !== null && o.memoizedState !== null && (si = l);
      }
    } else ja();
  }
  function ja() {
    At(He, He.current), At(il, il.current);
  }
  function ua(l) {
    Q(il), si === l && (si = null), Q(He);
  }
  var He = St(0);
  function Vf(l) {
    for (var o = l; o !== null; ) {
      if (o.tag === 13) {
        var c = o.memoizedState;
        if (c !== null && (c = c.dehydrated, c === null || c.data === "$?" || dh(c)))
          return o;
      } else if (o.tag === 19 && o.memoizedProps.revealOrder !== void 0) {
        if (o.flags & 128) return o;
      } else if (o.child !== null) {
        o.child.return = o, o = o.child;
        continue;
      }
      if (o === l) break;
      for (; o.sibling === null; ) {
        if (o.return === null || o.return === l) return null;
        o = o.return;
      }
      o.sibling.return = o.return, o = o.sibling;
    }
    return null;
  }
  function Mo(l, o, c, d) {
    o = l.memoizedState, c = c(d, o), c = c == null ? o : E({}, o, c), l.memoizedState = c, l.lanes === 0 && (l.updateQueue.baseState = c);
  }
  var ag = {
    enqueueSetState: function(l, o, c) {
      l = l._reactInternals;
      var d = ll(), v = Li(d);
      v.payload = o, c != null && (v.callback = c), o = Na(l, v, d), o !== null && (al(o, l, d), Tr(o, l, d));
    },
    enqueueReplaceState: function(l, o, c) {
      l = l._reactInternals;
      var d = ll(), v = Li(d);
      v.tag = 1, v.payload = o, c != null && (v.callback = c), o = Na(l, v, d), o !== null && (al(o, l, d), Tr(o, l, d));
    },
    enqueueForceUpdate: function(l, o) {
      l = l._reactInternals;
      var c = ll(), d = Li(c);
      d.tag = 2, o != null && (d.callback = o), o = Na(l, d, c), o !== null && (al(o, l, c), Tr(o, l, c));
    }
  };
  function Pu(l, o, c, d, v, S, A) {
    return l = l.stateNode, typeof l.shouldComponentUpdate == "function" ? l.shouldComponentUpdate(d, S, A) : o.prototype && o.prototype.isPureReactComponent ? !mo(c, d) || !mo(v, S) : !0;
  }
  function Lr(l, o, c, d) {
    l = o.state, typeof o.componentWillReceiveProps == "function" && o.componentWillReceiveProps(c, d), typeof o.UNSAFE_componentWillReceiveProps == "function" && o.UNSAFE_componentWillReceiveProps(c, d), o.state !== l && ag.enqueueReplaceState(o, o.state, null);
  }
  function wo(l, o) {
    var c = o;
    if ("ref" in o) {
      c = {};
      for (var d in o)
        d !== "ref" && (c[d] = o[d]);
    }
    if (l = l.defaultProps) {
      c === o && (c = E({}, c));
      for (var v in l)
        c[v] === void 0 && (c[v] = l[v]);
    }
    return c;
  }
  var qf = typeof reportError == "function" ? reportError : function(l) {
    if (typeof window == "object" && typeof window.ErrorEvent == "function") {
      var o = new window.ErrorEvent("error", {
        bubbles: !0,
        cancelable: !0,
        message: typeof l == "object" && l !== null && typeof l.message == "string" ? String(l.message) : String(l),
        error: l
      });
      if (!window.dispatchEvent(o)) return;
    } else if (typeof process == "object" && typeof process.emit == "function") {
      process.emit("uncaughtException", l);
      return;
    }
    console.error(l);
  };
  function Qu(l) {
    qf(l);
  }
  function rp(l) {
    console.error(l);
  }
  function Zf(l) {
    qf(l);
  }
  function Ff(l, o) {
    try {
      var c = l.onUncaughtError;
      c(o.value, { componentStack: o.stack });
    } catch (d) {
      setTimeout(function() {
        throw d;
      });
    }
  }
  function up(l, o, c) {
    try {
      var d = l.onCaughtError;
      d(c.value, {
        componentStack: c.stack,
        errorBoundary: o.tag === 1 ? o.stateNode : null
      });
    } catch (v) {
      setTimeout(function() {
        throw v;
      });
    }
  }
  function cp(l, o, c) {
    return c = Li(c), c.tag = 3, c.payload = { element: null }, c.callback = function() {
      Ff(l, o);
    }, c;
  }
  function fp(l) {
    return l = Li(l), l.tag = 3, l;
  }
  function Ui(l, o, c, d) {
    var v = c.type.getDerivedStateFromError;
    if (typeof v == "function") {
      var S = d.value;
      l.payload = function() {
        return v(S);
      }, l.callback = function() {
        up(o, c, d);
      };
    }
    var A = c.stateNode;
    A !== null && typeof A.componentDidCatch == "function" && (l.callback = function() {
      up(o, c, d), typeof v != "function" && (zo === null ? zo = /* @__PURE__ */ new Set([this]) : zo.add(this));
      var M = d.stack;
      this.componentDidCatch(d.value, {
        componentStack: M !== null ? M : ""
      });
    });
  }
  function dv(l, o, c, d, v) {
    if (c.flags |= 32768, d !== null && typeof d == "object" && typeof d.then == "function") {
      if (o = c.alternate, o !== null && Bu(
        o,
        c,
        v,
        !0
      ), c = il.current, c !== null) {
        switch (c.tag) {
          case 13:
            return si === null ? Ir() : c.alternate === null && fn === 0 && (fn = 3), c.flags &= -257, c.flags |= 65536, c.lanes = v, d === Nd ? c.flags |= 16384 : (o = c.updateQueue, o === null ? c.updateQueue = /* @__PURE__ */ new Set([d]) : o.add(d), Og(l, d, v)), !1;
          case 22:
            return c.flags |= 65536, d === Nd ? c.flags |= 16384 : (o = c.updateQueue, o === null ? (o = {
              transitions: null,
              markerInstances: null,
              retryQueue: /* @__PURE__ */ new Set([d])
            }, c.updateQueue = o) : (c = o.retryQueue, c === null ? o.retryQueue = /* @__PURE__ */ new Set([d]) : c.add(d)), Og(l, d, v)), !1;
        }
        throw Error(a(435, c.tag));
      }
      return Og(l, d, v), Ir(), !1;
    }
    if (ve)
      return o = il.current, o !== null ? (!(o.flags & 65536) && (o.flags |= 256), o.flags |= 65536, o.lanes = v, d !== Sr && (l = Error(a(422), { cause: d }), ju(tl(l, c)))) : (d !== Sr && (o = Error(a(423), {
        cause: d
      }), ju(
        tl(o, c)
      )), l = l.current.alternate, l.flags |= 65536, v &= -v, l.lanes |= v, d = tl(d, c), v = cp(
        l.stateNode,
        d,
        v
      ), Xy(l, v), fn !== 4 && (fn = 2)), !1;
    var S = Error(a(520), { cause: d });
    if (S = tl(S, c), ic === null ? ic = [S] : ic.push(S), fn !== 4 && (fn = 2), o === null) return !0;
    d = tl(d, c), c = o;
    do {
      switch (c.tag) {
        case 3:
          return c.flags |= 65536, l = v & -v, c.lanes |= l, l = cp(c.stateNode, d, l), Xy(c, l), !1;
        case 1:
          if (o = c.type, S = c.stateNode, (c.flags & 128) === 0 && (typeof o.getDerivedStateFromError == "function" || S !== null && typeof S.componentDidCatch == "function" && (zo === null || !zo.has(S))))
            return c.flags |= 65536, v &= -v, c.lanes |= v, v = fp(v), Ui(
              v,
              l,
              c,
              d
            ), Xy(c, v), !1;
      }
      c = c.return;
    } while (c !== null);
    return !1;
  }
  var rn = Error(a(461)), Rn = !1;
  function Nn(l, o, c, d) {
    o.child = l === null ? Ha(o, null, c, d) : Or(
      o,
      l.child,
      c,
      d
    );
  }
  function gv(l, o, c, d, v) {
    c = c.render;
    var S = o.ref;
    if ("ref" in d) {
      var A = {};
      for (var M in d)
        M !== "ref" && (A[M] = d[M]);
    } else A = d;
    return bo(o), d = Bd(
      l,
      o,
      c,
      A,
      S,
      v
    ), M = Id(), l !== null && !Rn ? (Zu(l, o, v), Ba(l, o, v)) : (ve && M && vr(o), o.flags |= 1, Nn(l, o, d, v), o.child);
  }
  function Ls(l, o, c, d, v) {
    if (l === null) {
      var S = c.type;
      return typeof S == "function" && !Ef(S) && S.defaultProps === void 0 && c.compare === null ? (o.tag = 15, o.type = S, zr(
        l,
        o,
        S,
        d,
        v
      )) : (l = _t(
        c.type,
        null,
        d,
        o,
        o.mode,
        v
      ), l.ref = o.ref, l.return = o, o.child = l);
    }
    if (S = l.child, !mg(l, v)) {
      var A = S.memoizedProps;
      if (c = c.compare, c = c !== null ? c : mo, c(A, d) && l.ref === o.ref)
        return Ba(l, o, v);
    }
    return o.flags |= 1, l = la(S, d), l.ref = o.ref, l.return = o, o.child = l;
  }
  function zr(l, o, c, d, v) {
    if (l !== null) {
      var S = l.memoizedProps;
      if (mo(S, d) && l.ref === o.ref)
        if (Rn = !1, o.pendingProps = d = S, mg(l, v))
          l.flags & 131072 && (Rn = !0);
        else
          return o.lanes = l.lanes, Ba(l, o, v);
    }
    return og(
      l,
      o,
      c,
      d,
      v
    );
  }
  function sg(l, o, c) {
    var d = o.pendingProps, v = d.children, S = l !== null ? l.memoizedState : null;
    if (d.mode === "hidden") {
      if (o.flags & 128) {
        if (d = S !== null ? S.baseLanes | c : c, l !== null) {
          for (v = o.child = l.child, S = 0; v !== null; )
            S = S | v.lanes | v.childLanes, v = v.sibling;
          o.childLanes = S & ~d;
        } else o.childLanes = 0, o.child = null;
        return Nr(
          l,
          o,
          d,
          c
        );
      }
      if (c & 536870912)
        o.memoizedState = { baseLanes: 0, cachePool: null }, l !== null && Er(
          o,
          S !== null ? S.cachePool : null
        ), S !== null ? zn(o, S) : Vu(), Ya(o);
      else
        return o.lanes = o.childLanes = 536870912, Nr(
          l,
          o,
          S !== null ? S.baseLanes | c : c,
          c
        );
    } else
      S !== null ? (Er(o, S.cachePool), zn(o, S), ja(), o.memoizedState = null) : (l !== null && Er(o, null), Vu(), ja());
    return Nn(l, o, v, c), o.child;
  }
  function Nr(l, o, c, d) {
    var v = Mf();
    return v = v === null ? null : { parent: xn._currentValue, pool: v }, o.memoizedState = {
      baseLanes: c,
      cachePool: v
    }, l !== null && Er(o, null), Vu(), Ya(o), l !== null && Bu(l, o, d, !0), null;
  }
  function Kf(l, o) {
    var c = o.ref;
    if (c === null)
      l !== null && l.ref !== null && (o.flags |= 4194816);
    else {
      if (typeof c != "function" && typeof c != "object")
        throw Error(a(284));
      (l === null || l.ref !== c) && (o.flags |= 4194816);
    }
  }
  function og(l, o, c, d, v) {
    return bo(o), c = Bd(
      l,
      o,
      c,
      d,
      void 0,
      v
    ), d = Id(), l !== null && !Rn ? (Zu(l, o, v), Ba(l, o, v)) : (ve && d && vr(o), o.flags |= 1, Nn(l, o, c, v), o.child);
  }
  function hp(l, o, c, d, v, S) {
    return bo(o), o.updateQueue = null, c = qy(
      o,
      d,
      c,
      v
    ), Ro(l), d = Id(), l !== null && !Rn ? (Zu(l, o, S), Ba(l, o, S)) : (ve && d && vr(o), o.flags |= 1, Nn(l, o, c, S), o.child);
  }
  function rg(l, o, c, d, v) {
    if (bo(o), o.stateNode === null) {
      var S = Nu, A = c.contextType;
      typeof A == "object" && A !== null && (S = Ln(A)), S = new c(d, S), o.memoizedState = S.state !== null && S.state !== void 0 ? S.state : null, S.updater = ag, o.stateNode = S, S._reactInternals = o, S = o.stateNode, S.props = d, S.state = o.memoizedState, S.refs = {}, Gd(o), A = c.contextType, S.context = typeof A == "object" && A !== null ? Ln(A) : Nu, S.state = o.memoizedState, A = c.getDerivedStateFromProps, typeof A == "function" && (Mo(
        o,
        c,
        A,
        d
      ), S.state = o.memoizedState), typeof c.getDerivedStateFromProps == "function" || typeof S.getSnapshotBeforeUpdate == "function" || typeof S.UNSAFE_componentWillMount != "function" && typeof S.componentWillMount != "function" || (A = S.state, typeof S.componentWillMount == "function" && S.componentWillMount(), typeof S.UNSAFE_componentWillMount == "function" && S.UNSAFE_componentWillMount(), A !== S.state && ag.enqueueReplaceState(S, S.state, null), Ms(o, d, S, v), ku(), S.state = o.memoizedState), typeof S.componentDidMount == "function" && (o.flags |= 4194308), d = !0;
    } else if (l === null) {
      S = o.stateNode;
      var M = o.memoizedProps, G = wo(c, M);
      S.props = G;
      var W = S.context, ct = c.contextType;
      A = Nu, typeof ct == "object" && ct !== null && (A = Ln(ct));
      var gt = c.getDerivedStateFromProps;
      ct = typeof gt == "function" || typeof S.getSnapshotBeforeUpdate == "function", M = o.pendingProps !== M, ct || typeof S.UNSAFE_componentWillReceiveProps != "function" && typeof S.componentWillReceiveProps != "function" || (M || W !== A) && Lr(
        o,
        S,
        d,
        A
      ), za = !1;
      var $ = o.memoizedState;
      S.state = $, Ms(o, d, S, v), ku(), W = o.memoizedState, M || $ !== W || za ? (typeof gt == "function" && (Mo(
        o,
        c,
        gt,
        d
      ), W = o.memoizedState), (G = za || Pu(
        o,
        c,
        G,
        d,
        $,
        W,
        A
      )) ? (ct || typeof S.UNSAFE_componentWillMount != "function" && typeof S.componentWillMount != "function" || (typeof S.componentWillMount == "function" && S.componentWillMount(), typeof S.UNSAFE_componentWillMount == "function" && S.UNSAFE_componentWillMount()), typeof S.componentDidMount == "function" && (o.flags |= 4194308)) : (typeof S.componentDidMount == "function" && (o.flags |= 4194308), o.memoizedProps = d, o.memoizedState = W), S.props = d, S.state = W, S.context = A, d = G) : (typeof S.componentDidMount == "function" && (o.flags |= 4194308), d = !1);
    } else {
      S = o.stateNode, Hd(l, o), A = o.memoizedProps, ct = wo(c, A), S.props = ct, gt = o.pendingProps, $ = S.context, W = c.contextType, G = Nu, typeof W == "object" && W !== null && (G = Ln(W)), M = c.getDerivedStateFromProps, (W = typeof M == "function" || typeof S.getSnapshotBeforeUpdate == "function") || typeof S.UNSAFE_componentWillReceiveProps != "function" && typeof S.componentWillReceiveProps != "function" || (A !== gt || $ !== G) && Lr(
        o,
        S,
        d,
        G
      ), za = !1, $ = o.memoizedState, S.state = $, Ms(o, d, S, v), ku();
      var et = o.memoizedState;
      A !== gt || $ !== et || za || l !== null && l.dependencies !== null && Cf(l.dependencies) ? (typeof M == "function" && (Mo(
        o,
        c,
        M,
        d
      ), et = o.memoizedState), (ct = za || Pu(
        o,
        c,
        ct,
        d,
        $,
        et,
        G
      ) || l !== null && l.dependencies !== null && Cf(l.dependencies)) ? (W || typeof S.UNSAFE_componentWillUpdate != "function" && typeof S.componentWillUpdate != "function" || (typeof S.componentWillUpdate == "function" && S.componentWillUpdate(d, et, G), typeof S.UNSAFE_componentWillUpdate == "function" && S.UNSAFE_componentWillUpdate(
        d,
        et,
        G
      )), typeof S.componentDidUpdate == "function" && (o.flags |= 4), typeof S.getSnapshotBeforeUpdate == "function" && (o.flags |= 1024)) : (typeof S.componentDidUpdate != "function" || A === l.memoizedProps && $ === l.memoizedState || (o.flags |= 4), typeof S.getSnapshotBeforeUpdate != "function" || A === l.memoizedProps && $ === l.memoizedState || (o.flags |= 1024), o.memoizedProps = d, o.memoizedState = et), S.props = d, S.state = et, S.context = G, d = ct) : (typeof S.componentDidUpdate != "function" || A === l.memoizedProps && $ === l.memoizedState || (o.flags |= 4), typeof S.getSnapshotBeforeUpdate != "function" || A === l.memoizedProps && $ === l.memoizedState || (o.flags |= 1024), d = !1);
    }
    return S = d, Kf(l, o), d = (o.flags & 128) !== 0, S || d ? (S = o.stateNode, c = d && typeof c.getDerivedStateFromError != "function" ? null : S.render(), o.flags |= 1, l !== null && d ? (o.child = Or(
      o,
      l.child,
      null,
      v
    ), o.child = Or(
      o,
      null,
      c,
      v
    )) : Nn(l, o, c, v), o.memoizedState = S.state, l = o.child) : l = Ba(
      l,
      o,
      v
    ), l;
  }
  function ug(l, o, c, d) {
    return Yu(), o.flags |= 256, Nn(l, o, c, d), o.child;
  }
  var cg = {
    dehydrated: null,
    treeContext: null,
    retryLane: 0,
    hydrationErrors: null
  };
  function dp(l) {
    return { baseLanes: l, cachePool: Ld() };
  }
  function gp(l, o, c) {
    return l = l !== null ? l.childLanes & ~c : 0, o && (l |= Nl), l;
  }
  function mp(l, o, c) {
    var d = o.pendingProps, v = !1, S = (o.flags & 128) !== 0, A;
    if ((A = S) || (A = l !== null && l.memoizedState === null ? !1 : (He.current & 2) !== 0), A && (v = !0, o.flags &= -129), A = (o.flags & 32) !== 0, o.flags &= -33, l === null) {
      if (ve) {
        if (v ? Os(o) : ja(), ve) {
          var M = Ee, G;
          if (G = M) {
            t: {
              for (G = M, M = Ml; G.nodeType !== 8; ) {
                if (!M) {
                  M = null;
                  break t;
                }
                if (G = fa(
                  G.nextSibling
                ), G === null) {
                  M = null;
                  break t;
                }
              }
              M = G;
            }
            M !== null ? (o.memoizedState = {
              dehydrated: M,
              treeContext: xs !== null ? { id: aa, overflow: sn } : null,
              retryLane: 536870912,
              hydrationErrors: null
            }, G = Di(
              18,
              null,
              null,
              0
            ), G.stateNode = M, G.return = o, o.child = G, mn = o, Ee = null, G = !0) : G = !1;
          }
          G || Cs(o);
        }
        if (M = o.memoizedState, M !== null && (M = M.dehydrated, M !== null))
          return dh(M) ? o.lanes = 32 : o.lanes = 536870912, null;
        ua(o);
      }
      return M = d.children, d = d.fallback, v ? (ja(), v = o.mode, M = hg(
        { mode: "hidden", children: M },
        v
      ), d = Cl(
        d,
        v,
        c,
        null
      ), M.return = o, d.return = o, M.sibling = d, o.child = M, v = o.child, v.memoizedState = dp(c), v.childLanes = gp(
        l,
        A,
        c
      ), o.memoizedState = cg, d) : (Os(o), fg(o, M));
    }
    if (G = l.memoizedState, G !== null && (M = G.dehydrated, M !== null)) {
      if (S)
        o.flags & 256 ? (Os(o), o.flags &= -257, o = Do(
          l,
          o,
          c
        )) : o.memoizedState !== null ? (ja(), o.child = l.child, o.flags |= 128, o = null) : (ja(), v = d.fallback, M = o.mode, d = hg(
          { mode: "visible", children: d.children },
          M
        ), v = Cl(
          v,
          M,
          c,
          null
        ), v.flags |= 2, d.return = o, v.return = o, d.sibling = v, o.child = d, Or(
          o,
          l.child,
          null,
          c
        ), d = o.child, d.memoizedState = dp(c), d.childLanes = gp(
          l,
          A,
          c
        ), o.memoizedState = cg, o = v);
      else if (Os(o), dh(M)) {
        if (A = M.nextSibling && M.nextSibling.dataset, A) var W = A.dgst;
        A = W, d = Error(a(419)), d.stack = "", d.digest = A, ju({ value: d, source: null, stack: null }), o = Do(
          l,
          o,
          c
        );
      } else if (Rn || Bu(l, o, c, !1), A = (c & l.childLanes) !== 0, Rn || A) {
        if (A = Be, A !== null && (d = c & -c, d = d & 42 ? 1 : bn(d), d = d & (A.suspendedLanes | c) ? 0 : d, d !== 0 && d !== G.retryLane))
          throw G.retryLane = d, Ma(l, d), al(A, l, d), rn;
        M.data === "$?" || Ir(), o = Do(
          l,
          o,
          c
        );
      } else
        M.data === "$?" ? (o.flags |= 192, o.child = l.child, o = null) : (l = G.treeContext, Ee = fa(
          M.nextSibling
        ), mn = o, ve = !0, Al = null, Ml = !1, l !== null && (Rl[Oi++] = aa, Rl[Oi++] = sn, Rl[Oi++] = xs, aa = l.id, sn = l.overflow, xs = o), o = fg(
          o,
          d.children
        ), o.flags |= 4096);
      return o;
    }
    return v ? (ja(), v = d.fallback, M = o.mode, G = l.child, W = G.sibling, d = la(G, {
      mode: "hidden",
      children: d.children
    }), d.subtreeFlags = G.subtreeFlags & 65011712, W !== null ? v = la(W, v) : (v = Cl(
      v,
      M,
      c,
      null
    ), v.flags |= 2), v.return = o, d.return = o, d.sibling = v, o.child = d, d = v, v = o.child, M = l.child.memoizedState, M === null ? M = dp(c) : (G = M.cachePool, G !== null ? (W = xn._currentValue, G = G.parent !== W ? { parent: W, pool: W } : G) : G = Ld(), M = {
      baseLanes: M.baseLanes | c,
      cachePool: G
    }), v.memoizedState = M, v.childLanes = gp(
      l,
      A,
      c
    ), o.memoizedState = cg, d) : (Os(o), c = l.child, l = c.sibling, c = la(c, {
      mode: "visible",
      children: d.children
    }), c.return = o, c.sibling = null, l !== null && (A = o.deletions, A === null ? (o.deletions = [l], o.flags |= 16) : A.push(l)), o.child = c, o.memoizedState = null, c);
  }
  function fg(l, o) {
    return o = hg(
      { mode: "visible", children: o },
      l.mode
    ), o.return = l, l.child = o;
  }
  function hg(l, o) {
    return l = Di(22, l, null, o), l.lanes = 0, l.stateNode = {
      _visibility: 1,
      _pendingMarkers: null,
      _retryCache: null,
      _transitions: null
    }, l;
  }
  function Do(l, o, c) {
    return Or(o, l.child, null, c), l = fg(
      o,
      o.pendingProps.children
    ), l.flags |= 2, o.memoizedState = null, l;
  }
  function Pf(l, o, c) {
    l.lanes |= o;
    var d = l.alternate;
    d !== null && (d.lanes |= o), Ad(l.return, o, c);
  }
  function dg(l, o, c, d, v) {
    var S = l.memoizedState;
    S === null ? l.memoizedState = {
      isBackwards: o,
      rendering: null,
      renderingStartTime: 0,
      last: d,
      tail: c,
      tailMode: v
    } : (S.isBackwards = o, S.rendering = null, S.renderingStartTime = 0, S.last = d, S.tail = c, S.tailMode = v);
  }
  function gg(l, o, c) {
    var d = o.pendingProps, v = d.revealOrder, S = d.tail;
    if (Nn(l, o, d.children, c), d = He.current, d & 2)
      d = d & 1 | 2, o.flags |= 128;
    else {
      if (l !== null && l.flags & 128)
        t: for (l = o.child; l !== null; ) {
          if (l.tag === 13)
            l.memoizedState !== null && Pf(l, c, o);
          else if (l.tag === 19)
            Pf(l, c, o);
          else if (l.child !== null) {
            l.child.return = l, l = l.child;
            continue;
          }
          if (l === o) break t;
          for (; l.sibling === null; ) {
            if (l.return === null || l.return === o)
              break t;
            l = l.return;
          }
          l.sibling.return = l.return, l = l.sibling;
        }
      d &= 1;
    }
    switch (At(He, d), v) {
      case "forwards":
        for (c = o.child, v = null; c !== null; )
          l = c.alternate, l !== null && Vf(l) === null && (v = c), c = c.sibling;
        c = v, c === null ? (v = o.child, o.child = null) : (v = c.sibling, c.sibling = null), dg(
          o,
          !1,
          v,
          c,
          S
        );
        break;
      case "backwards":
        for (c = null, v = o.child, o.child = null; v !== null; ) {
          if (l = v.alternate, l !== null && Vf(l) === null) {
            o.child = v;
            break;
          }
          l = v.sibling, v.sibling = c, c = v, v = l;
        }
        dg(
          o,
          !0,
          c,
          null,
          S
        );
        break;
      case "together":
        dg(o, !1, null, null, void 0);
        break;
      default:
        o.memoizedState = null;
    }
    return o.child;
  }
  function Ba(l, o, c) {
    if (l !== null && (o.dependencies = l.dependencies), Us |= o.lanes, !(c & o.childLanes))
      if (l !== null) {
        if (Bu(
          l,
          o,
          c,
          !1
        ), (c & o.childLanes) === 0)
          return null;
      } else return null;
    if (l !== null && o.child !== l.child)
      throw Error(a(153));
    if (o.child !== null) {
      for (l = o.child, c = la(l, l.pendingProps), o.child = c, c.return = o; l.sibling !== null; )
        l = l.sibling, c = c.sibling = la(l, l.pendingProps), c.return = o;
      c.sibling = null;
    }
    return o.child;
  }
  function mg(l, o) {
    return l.lanes & o ? !0 : (l = l.dependencies, !!(l !== null && Cf(l)));
  }
  function mv(l, o, c) {
    switch (o.tag) {
      case 3:
        _e(o, o.stateNode.containerInfo), As(o, xn, l.memoizedState.cache), Yu();
        break;
      case 27:
      case 5:
        ke(o);
        break;
      case 4:
        _e(o, o.stateNode.containerInfo);
        break;
      case 10:
        As(
          o,
          o.type,
          o.memoizedProps.value
        );
        break;
      case 13:
        var d = o.memoizedState;
        if (d !== null)
          return d.dehydrated !== null ? (Os(o), o.flags |= 128, null) : c & o.child.childLanes ? mp(l, o, c) : (Os(o), l = Ba(
            l,
            o,
            c
          ), l !== null ? l.sibling : null);
        Os(o);
        break;
      case 19:
        var v = (l.flags & 128) !== 0;
        if (d = (c & o.childLanes) !== 0, d || (Bu(
          l,
          o,
          c,
          !1
        ), d = (c & o.childLanes) !== 0), v) {
          if (d)
            return gg(
              l,
              o,
              c
            );
          o.flags |= 128;
        }
        if (v = o.memoizedState, v !== null && (v.rendering = null, v.tail = null, v.lastEffect = null), At(He, He.current), d) break;
        return null;
      case 22:
      case 23:
        return o.lanes = 0, sg(l, o, c);
      case 24:
        As(o, xn, l.memoizedState.cache);
    }
    return Ba(l, o, c);
  }
  function yv(l, o, c) {
    if (l !== null)
      if (l.memoizedProps !== o.pendingProps)
        Rn = !0;
      else {
        if (!mg(l, c) && !(o.flags & 128))
          return Rn = !1, mv(
            l,
            o,
            c
          );
        Rn = !!(l.flags & 131072);
      }
    else
      Rn = !1, ve && o.flags & 1048576 && Rd(o, Gu, o.index);
    switch (o.lanes = 0, o.tag) {
      case 16:
        t: {
          l = o.pendingProps;
          var d = o.elementType, v = d._init;
          if (d = v(d._payload), o.type = d, typeof d == "function")
            Ef(d) ? (l = wo(d, l), o.tag = 1, o = rg(
              null,
              o,
              d,
              l,
              c
            )) : (o.tag = 0, o = og(
              null,
              o,
              d,
              l,
              c
            ));
          else {
            if (d != null) {
              if (v = d.$$typeof, v === X) {
                o.tag = 11, o = gv(
                  null,
                  o,
                  d,
                  l,
                  c
                );
                break t;
              } else if (v === ft) {
                o.tag = 14, o = Ls(
                  null,
                  o,
                  d,
                  l,
                  c
                );
                break t;
              }
            }
            throw o = Nt(d) || d, Error(a(306, o, ""));
          }
        }
        return o;
      case 0:
        return og(
          l,
          o,
          o.type,
          o.pendingProps,
          c
        );
      case 1:
        return d = o.type, v = wo(
          d,
          o.pendingProps
        ), rg(
          l,
          o,
          d,
          v,
          c
        );
      case 3:
        t: {
          if (_e(
            o,
            o.stateNode.containerInfo
          ), l === null) throw Error(a(387));
          d = o.pendingProps;
          var S = o.memoizedState;
          v = S.element, Hd(l, o), Ms(o, d, null, c);
          var A = o.memoizedState;
          if (d = A.cache, As(o, xn, d), d !== S.cache && Hy(
            o,
            [xn],
            c,
            !0
          ), ku(), d = A.element, S.isDehydrated)
            if (S = {
              element: d,
              isDehydrated: !1,
              cache: A.cache
            }, o.updateQueue.baseState = S, o.memoizedState = S, o.flags & 256) {
              o = ug(
                l,
                o,
                d,
                c
              );
              break t;
            } else if (d !== v) {
              v = tl(
                Error(a(424)),
                o
              ), ju(v), o = ug(
                l,
                o,
                d,
                c
              );
              break t;
            } else {
              switch (l = o.stateNode.containerInfo, l.nodeType) {
                case 9:
                  l = l.body;
                  break;
                default:
                  l = l.nodeName === "HTML" ? l.ownerDocument.body : l;
              }
              for (Ee = fa(l.firstChild), mn = o, ve = !0, Al = null, Ml = !0, c = Ha(
                o,
                null,
                d,
                c
              ), o.child = c; c; )
                c.flags = c.flags & -3 | 4096, c = c.sibling;
            }
          else {
            if (Yu(), d === v) {
              o = Ba(
                l,
                o,
                c
              );
              break t;
            }
            Nn(
              l,
              o,
              d,
              c
            );
          }
          o = o.child;
        }
        return o;
      case 26:
        return Kf(l, o), l === null ? (c = Lv(
          o.type,
          null,
          o.pendingProps,
          null
        )) ? o.memoizedState = c : ve || (c = o.type, l = o.pendingProps, d = Gl(
          Xt.current
        ).createElement(c), d[Ue] = o, d[se] = l, Vt(d, c, l), Tn(d), o.stateNode = d) : o.memoizedState = Lv(
          o.type,
          l.memoizedProps,
          o.pendingProps,
          l.memoizedState
        ), null;
      case 27:
        return ke(o), l === null && ve && (d = o.stateNode = Ct(
          o.type,
          o.pendingProps,
          Xt.current
        ), mn = o, Ml = !0, v = Ee, jo(o.type) ? (Bo = v, Ee = fa(
          d.firstChild
        )) : Ee = v), Nn(
          l,
          o,
          o.pendingProps.children,
          c
        ), Kf(l, o), l === null && (o.flags |= 4194304), o.child;
      case 5:
        return l === null && ve && ((v = d = Ee) && (d = mc(
          d,
          o.type,
          o.pendingProps,
          Ml
        ), d !== null ? (o.stateNode = d, mn = o, Ee = fa(
          d.firstChild
        ), Ml = !1, v = !0) : v = !1), v || Cs(o)), ke(o), v = o.type, S = o.pendingProps, A = l !== null ? l.memoizedProps : null, d = S.children, Pa(v, S) ? d = null : A !== null && Pa(v, A) && (o.flags |= 32), o.memoizedState !== null && (v = Bd(
          l,
          o,
          lv,
          null,
          null,
          c
        ), Ii._currentValue = v), Kf(l, o), Nn(l, o, d, c), o.child;
      case 6:
        return l === null && ve && ((l = c = Ee) && (c = ZS(
          c,
          o.pendingProps,
          Ml
        ), c !== null ? (o.stateNode = c, mn = o, Ee = null, l = !0) : l = !1), l || Cs(o)), null;
      case 13:
        return mp(l, o, c);
      case 4:
        return _e(
          o,
          o.stateNode.containerInfo
        ), d = o.pendingProps, l === null ? o.child = Or(
          o,
          null,
          d,
          c
        ) : Nn(
          l,
          o,
          d,
          c
        ), o.child;
      case 11:
        return gv(
          l,
          o,
          o.type,
          o.pendingProps,
          c
        );
      case 7:
        return Nn(
          l,
          o,
          o.pendingProps,
          c
        ), o.child;
      case 8:
        return Nn(
          l,
          o,
          o.pendingProps.children,
          c
        ), o.child;
      case 12:
        return Nn(
          l,
          o,
          o.pendingProps.children,
          c
        ), o.child;
      case 10:
        return d = o.pendingProps, As(o, o.type, d.value), Nn(
          l,
          o,
          d.children,
          c
        ), o.child;
      case 9:
        return v = o.type._context, d = o.pendingProps.children, bo(o), v = Ln(v), d = d(v), o.flags |= 1, Nn(l, o, d, c), o.child;
      case 14:
        return Ls(
          l,
          o,
          o.type,
          o.pendingProps,
          c
        );
      case 15:
        return zr(
          l,
          o,
          o.type,
          o.pendingProps,
          c
        );
      case 19:
        return gg(l, o, c);
      case 31:
        return d = o.pendingProps, c = o.mode, d = {
          mode: d.mode,
          children: d.children
        }, l === null ? (c = hg(
          d,
          c
        ), c.ref = o.ref, o.child = c, c.return = o, o = c) : (c = la(l.child, d), c.ref = o.ref, o.child = c, c.return = o, o = c), o;
      case 22:
        return sg(l, o, c);
      case 24:
        return bo(o), d = Ln(xn), l === null ? (v = Mf(), v === null && (v = Be, S = Xu(), v.pooledCache = S, S.refCount++, S !== null && (v.pooledCacheLanes |= c), v = S), o.memoizedState = {
          parent: d,
          cache: v
        }, Gd(o), As(o, xn, v)) : (l.lanes & c && (Hd(l, o), Ms(o, null, null, c), ku()), v = l.memoizedState, S = o.memoizedState, v.parent !== d ? (v = { parent: d, cache: d }, o.memoizedState = v, o.lanes === 0 && (o.memoizedState = o.updateQueue.baseState = v), As(o, xn, d)) : (d = S.cache, As(o, xn, d), d !== v.cache && Hy(
          o,
          [xn],
          c,
          !0
        ))), Nn(
          l,
          o,
          o.pendingProps.children,
          c
        ), o.child;
      case 29:
        throw o.pendingProps;
    }
    throw Error(a(156, o.tag));
  }
  function Ia(l) {
    l.flags |= 4;
  }
  function Wu(l, o) {
    if (o.type !== "stylesheet" || o.state.loading & 4)
      l.flags &= -16777217;
    else if (l.flags |= 16777216, !Jp(o)) {
      if (o = il.current, o !== null && ((fe & 4194048) === fe ? si !== null : (fe & 62914560) !== fe && !(fe & 536870912) || o !== si))
        throw br = Nd, zd;
      l.flags |= 8192;
    }
  }
  function Qf(l, o) {
    o !== null && (l.flags |= 4), l.flags & 16384 && (o = l.tag !== 22 ? Pi() : 536870912, l.lanes |= o, nc |= o);
  }
  function Ju(l, o) {
    if (!ve)
      switch (l.tailMode) {
        case "hidden":
          o = l.tail;
          for (var c = null; o !== null; )
            o.alternate !== null && (c = o), o = o.sibling;
          c === null ? l.tail = null : c.sibling = null;
          break;
        case "collapsed":
          c = l.tail;
          for (var d = null; c !== null; )
            c.alternate !== null && (d = c), c = c.sibling;
          d === null ? o || l.tail === null ? l.tail = null : l.tail.sibling = null : d.sibling = null;
      }
  }
  function jt(l) {
    var o = l.alternate !== null && l.alternate.child === l.child, c = 0, d = 0;
    if (o)
      for (var v = l.child; v !== null; )
        c |= v.lanes | v.childLanes, d |= v.subtreeFlags & 65011712, d |= v.flags & 65011712, v.return = l, v = v.sibling;
    else
      for (v = l.child; v !== null; )
        c |= v.lanes | v.childLanes, d |= v.subtreeFlags, d |= v.flags, v.return = l, v = v.sibling;
    return l.subtreeFlags |= d, l.childLanes = c, o;
  }
  function yp(l, o, c) {
    var d = o.pendingProps;
    switch (wa(o), o.tag) {
      case 31:
      case 16:
      case 15:
      case 0:
      case 11:
      case 7:
      case 8:
      case 12:
      case 9:
      case 14:
        return jt(o), null;
      case 1:
        return jt(o), null;
      case 3:
        return c = o.stateNode, d = null, l !== null && (d = l.memoizedState.cache), o.memoizedState.cache !== d && (o.flags |= 2048), Da(xn), De(), c.pendingContext && (c.context = c.pendingContext, c.pendingContext = null), (l === null || l.child === null) && (Hu(o) ? Ia(o) : l === null || l.memoizedState.isDehydrated && !(o.flags & 256) || (o.flags |= 1024, Gy())), jt(o), null;
      case 26:
        return c = o.memoizedState, l === null ? (Ia(o), c !== null ? (jt(o), Wu(o, c)) : (jt(o), o.flags &= -16777217)) : c ? c !== l.memoizedState ? (Ia(o), jt(o), Wu(o, c)) : (jt(o), o.flags &= -16777217) : (l.memoizedProps !== d && Ia(o), jt(o), o.flags &= -16777217), null;
      case 27:
        yl(o), c = Xt.current;
        var v = o.type;
        if (l !== null && o.stateNode != null)
          l.memoizedProps !== d && Ia(o);
        else {
          if (!d) {
            if (o.stateNode === null)
              throw Error(a(166));
            return jt(o), null;
          }
          l = Ut.current, Hu(o) ? Tf(o) : (l = Ct(v, d, c), o.stateNode = l, Ia(o));
        }
        return jt(o), null;
      case 5:
        if (yl(o), c = o.type, l !== null && o.stateNode != null)
          l.memoizedProps !== d && Ia(o);
        else {
          if (!d) {
            if (o.stateNode === null)
              throw Error(a(166));
            return jt(o), null;
          }
          if (l = Ut.current, Hu(o))
            Tf(o);
          else {
            switch (v = Gl(
              Xt.current
            ), l) {
              case 1:
                l = v.createElementNS(
                  "http://www.w3.org/2000/svg",
                  c
                );
                break;
              case 2:
                l = v.createElementNS(
                  "http://www.w3.org/1998/Math/MathML",
                  c
                );
                break;
              default:
                switch (c) {
                  case "svg":
                    l = v.createElementNS(
                      "http://www.w3.org/2000/svg",
                      c
                    );
                    break;
                  case "math":
                    l = v.createElementNS(
                      "http://www.w3.org/1998/Math/MathML",
                      c
                    );
                    break;
                  case "script":
                    l = v.createElement("div"), l.innerHTML = "<script><\/script>", l = l.removeChild(l.firstChild);
                    break;
                  case "select":
                    l = typeof d.is == "string" ? v.createElement("select", { is: d.is }) : v.createElement("select"), d.multiple ? l.multiple = !0 : d.size && (l.size = d.size);
                    break;
                  default:
                    l = typeof d.is == "string" ? v.createElement(c, { is: d.is }) : v.createElement(c);
                }
            }
            l[Ue] = o, l[se] = d;
            t: for (v = o.child; v !== null; ) {
              if (v.tag === 5 || v.tag === 6)
                l.appendChild(v.stateNode);
              else if (v.tag !== 4 && v.tag !== 27 && v.child !== null) {
                v.child.return = v, v = v.child;
                continue;
              }
              if (v === o) break t;
              for (; v.sibling === null; ) {
                if (v.return === null || v.return === o)
                  break t;
                v = v.return;
              }
              v.sibling.return = v.return, v = v.sibling;
            }
            o.stateNode = l;
            t: switch (Vt(l, c, d), c) {
              case "button":
              case "input":
              case "select":
              case "textarea":
                l = !!d.autoFocus;
                break t;
              case "img":
                l = !0;
                break t;
              default:
                l = !1;
            }
            l && Ia(o);
          }
        }
        return jt(o), o.flags &= -16777217, null;
      case 6:
        if (l && o.stateNode != null)
          l.memoizedProps !== d && Ia(o);
        else {
          if (typeof d != "string" && o.stateNode === null)
            throw Error(a(166));
          if (l = Xt.current, Hu(o)) {
            if (l = o.stateNode, c = o.memoizedProps, d = null, v = mn, v !== null)
              switch (v.tag) {
                case 27:
                case 5:
                  d = v.memoizedProps;
              }
            l[Ue] = o, l = !!(l.nodeValue === c || d !== null && d.suppressHydrationWarning === !0 || Kp(l.nodeValue, c)), l || Cs(o);
          } else
            l = Gl(l).createTextNode(
              d
            ), l[Ue] = o, o.stateNode = l;
        }
        return jt(o), null;
      case 13:
        if (d = o.memoizedState, l === null || l.memoizedState !== null && l.memoizedState.dehydrated !== null) {
          if (v = Hu(o), d !== null && d.dehydrated !== null) {
            if (l === null) {
              if (!v) throw Error(a(318));
              if (v = o.memoizedState, v = v !== null ? v.dehydrated : null, !v) throw Error(a(317));
              v[Ue] = o;
            } else
              Yu(), !(o.flags & 128) && (o.memoizedState = null), o.flags |= 4;
            jt(o), v = !1;
          } else
            v = Gy(), l !== null && l.memoizedState !== null && (l.memoizedState.hydrationErrors = v), v = !0;
          if (!v)
            return o.flags & 256 ? (ua(o), o) : (ua(o), null);
        }
        if (ua(o), o.flags & 128)
          return o.lanes = c, o;
        if (c = d !== null, l = l !== null && l.memoizedState !== null, c) {
          d = o.child, v = null, d.alternate !== null && d.alternate.memoizedState !== null && d.alternate.memoizedState.cachePool !== null && (v = d.alternate.memoizedState.cachePool.pool);
          var S = null;
          d.memoizedState !== null && d.memoizedState.cachePool !== null && (S = d.memoizedState.cachePool.pool), S !== v && (d.flags |= 2048);
        }
        return c !== l && c && (o.child.flags |= 8192), Qf(o, o.updateQueue), jt(o), null;
      case 4:
        return De(), l === null && Zp(o.stateNode.containerInfo), jt(o), null;
      case 10:
        return Da(o.type), jt(o), null;
      case 19:
        if (Q(He), v = o.memoizedState, v === null) return jt(o), null;
        if (d = (o.flags & 128) !== 0, S = v.rendering, S === null)
          if (d) Ju(v, !1);
          else {
            if (fn !== 0 || l !== null && l.flags & 128)
              for (l = o.child; l !== null; ) {
                if (S = Vf(l), S !== null) {
                  for (o.flags |= 128, Ju(v, !1), l = S.updateQueue, o.updateQueue = l, Qf(o, l), o.subtreeFlags = 0, l = c, c = o.child; c !== null; )
                    ne(c, l), c = c.sibling;
                  return At(
                    He,
                    He.current & 1 | 2
                  ), o.child;
                }
                l = l.sibling;
              }
            v.tail !== null && yn() > xg && (o.flags |= 128, d = !0, Ju(v, !1), o.lanes = 4194304);
          }
        else {
          if (!d)
            if (l = Vf(S), l !== null) {
              if (o.flags |= 128, d = !0, l = l.updateQueue, o.updateQueue = l, Qf(o, l), Ju(v, !0), v.tail === null && v.tailMode === "hidden" && !S.alternate && !ve)
                return jt(o), null;
            } else
              2 * yn() - v.renderingStartTime > xg && c !== 536870912 && (o.flags |= 128, d = !0, Ju(v, !1), o.lanes = 4194304);
          v.isBackwards ? (S.sibling = o.child, o.child = S) : (l = v.last, l !== null ? l.sibling = S : o.child = S, v.last = S);
        }
        return v.tail !== null ? (o = v.tail, v.rendering = o, v.tail = o.sibling, v.renderingStartTime = yn(), o.sibling = null, l = He.current, At(He, d ? l & 1 | 2 : l & 1), o) : (jt(o), null);
      case 22:
      case 23:
        return ua(o), qu(), d = o.memoizedState !== null, l !== null ? l.memoizedState !== null !== d && (o.flags |= 8192) : d && (o.flags |= 8192), d ? c & 536870912 && !(o.flags & 128) && (jt(o), o.subtreeFlags & 6 && (o.flags |= 8192)) : jt(o), c = o.updateQueue, c !== null && Qf(o, c.retryQueue), c = null, l !== null && l.memoizedState !== null && l.memoizedState.cachePool !== null && (c = l.memoizedState.cachePool.pool), d = null, o.memoizedState !== null && o.memoizedState.cachePool !== null && (d = o.memoizedState.cachePool.pool), d !== c && (o.flags |= 2048), l !== null && Q(La), null;
      case 24:
        return c = null, l !== null && (c = l.memoizedState.cache), o.memoizedState.cache !== c && (o.flags |= 2048), Da(xn), jt(o), null;
      case 25:
        return null;
      case 30:
        return null;
    }
    throw Error(a(156, o.tag));
  }
  function YS(l, o) {
    switch (wa(o), o.tag) {
      case 1:
        return l = o.flags, l & 65536 ? (o.flags = l & -65537 | 128, o) : null;
      case 3:
        return Da(xn), De(), l = o.flags, l & 65536 && !(l & 128) ? (o.flags = l & -65537 | 128, o) : null;
      case 26:
      case 27:
      case 5:
        return yl(o), null;
      case 13:
        if (ua(o), l = o.memoizedState, l !== null && l.dehydrated !== null) {
          if (o.alternate === null)
            throw Error(a(340));
          Yu();
        }
        return l = o.flags, l & 65536 ? (o.flags = l & -65537 | 128, o) : null;
      case 19:
        return Q(He), null;
      case 4:
        return De(), null;
      case 10:
        return Da(o.type), null;
      case 22:
      case 23:
        return ua(o), qu(), l !== null && Q(La), l = o.flags, l & 65536 ? (o.flags = l & -65537 | 128, o) : null;
      case 24:
        return Da(xn), null;
      case 25:
        return null;
      default:
        return null;
    }
  }
  function pp(l, o) {
    switch (wa(o), o.tag) {
      case 3:
        Da(xn), De();
        break;
      case 26:
      case 27:
      case 5:
        yl(o);
        break;
      case 4:
        De();
        break;
      case 13:
        ua(o);
        break;
      case 19:
        Q(He);
        break;
      case 10:
        Da(o.type);
        break;
      case 22:
      case 23:
        ua(o), qu(), l !== null && Q(La);
        break;
      case 24:
        Da(xn);
    }
  }
  function Wf(l, o) {
    try {
      var c = o.updateQueue, d = c !== null ? c.lastEffect : null;
      if (d !== null) {
        var v = d.next;
        c = v;
        do {
          if ((c.tag & l) === l) {
            d = void 0;
            var S = c.create, A = c.inst;
            d = S(), A.destroy = d;
          }
          c = c.next;
        } while (c !== v);
      }
    } catch (M) {
      Le(o, o.return, M);
    }
  }
  function Oo(l, o, c) {
    try {
      var d = o.updateQueue, v = d !== null ? d.lastEffect : null;
      if (v !== null) {
        var S = v.next;
        d = S;
        do {
          if ((d.tag & l) === l) {
            var A = d.inst, M = A.destroy;
            if (M !== void 0) {
              A.destroy = void 0, v = o;
              var G = c, W = M;
              try {
                W();
              } catch (ct) {
                Le(
                  v,
                  G,
                  ct
                );
              }
            }
          }
          d = d.next;
        } while (d !== S);
      }
    } catch (ct) {
      Le(o, o.return, ct);
    }
  }
  function yg(l) {
    var o = l.updateQueue;
    if (o !== null) {
      var c = l.stateNode;
      try {
        Of(o, c);
      } catch (d) {
        Le(l, l.return, d);
      }
    }
  }
  function _p(l, o, c) {
    c.props = wo(
      l.type,
      l.memoizedProps
    ), c.state = l.memoizedState;
    try {
      c.componentWillUnmount();
    } catch (d) {
      Le(l, o, d);
    }
  }
  function $u(l, o) {
    try {
      var c = l.ref;
      if (c !== null) {
        switch (l.tag) {
          case 26:
          case 27:
          case 5:
            var d = l.stateNode;
            break;
          case 30:
            d = l.stateNode;
            break;
          default:
            d = l.stateNode;
        }
        typeof c == "function" ? l.refCleanup = c(d) : c.current = d;
      }
    } catch (v) {
      Le(l, o, v);
    }
  }
  function ca(l, o) {
    var c = l.ref, d = l.refCleanup;
    if (c !== null)
      if (typeof d == "function")
        try {
          d();
        } catch (v) {
          Le(l, o, v);
        } finally {
          l.refCleanup = null, l = l.alternate, l != null && (l.refCleanup = null);
        }
      else if (typeof c == "function")
        try {
          c(null);
        } catch (v) {
          Le(l, o, v);
        }
      else c.current = null;
  }
  function tc(l) {
    var o = l.type, c = l.memoizedProps, d = l.stateNode;
    try {
      t: switch (o) {
        case "button":
        case "input":
        case "select":
        case "textarea":
          c.autoFocus && d.focus();
          break t;
        case "img":
          c.src ? d.src = c.src : c.srcSet && (d.srcset = c.srcSet);
      }
    } catch (v) {
      Le(l, l.return, v);
    }
  }
  function vp(l, o, c) {
    try {
      var d = l.stateNode;
      kS(d, l.type, c, o), d[se] = o;
    } catch (v) {
      Le(l, l.return, v);
    }
  }
  function pv(l) {
    return l.tag === 5 || l.tag === 3 || l.tag === 26 || l.tag === 27 && jo(l.type) || l.tag === 4;
  }
  function Ll(l) {
    t: for (; ; ) {
      for (; l.sibling === null; ) {
        if (l.return === null || pv(l.return)) return null;
        l = l.return;
      }
      for (l.sibling.return = l.return, l = l.sibling; l.tag !== 5 && l.tag !== 6 && l.tag !== 18; ) {
        if (l.tag === 27 && jo(l.type) || l.flags & 2 || l.child === null || l.tag === 4) continue t;
        l.child.return = l, l = l.child;
      }
      if (!(l.flags & 2)) return l.stateNode;
    }
  }
  function Ur(l, o, c) {
    var d = l.tag;
    if (d === 5 || d === 6)
      l = l.stateNode, o ? (c.nodeType === 9 ? c.body : c.nodeName === "HTML" ? c.ownerDocument.body : c).insertBefore(l, o) : (o = c.nodeType === 9 ? c.body : c.nodeName === "HTML" ? c.ownerDocument.body : c, o.appendChild(l), c = c._reactRootContainer, c != null || o.onclick !== null || (o.onclick = Gg));
    else if (d !== 4 && (d === 27 && jo(l.type) && (c = l.stateNode, o = null), l = l.child, l !== null))
      for (Ur(l, o, c), l = l.sibling; l !== null; )
        Ur(l, o, c), l = l.sibling;
  }
  function pg(l, o, c) {
    var d = l.tag;
    if (d === 5 || d === 6)
      l = l.stateNode, o ? c.insertBefore(l, o) : c.appendChild(l);
    else if (d !== 4 && (d === 27 && jo(l.type) && (c = l.stateNode), l = l.child, l !== null))
      for (pg(l, o, c), l = l.sibling; l !== null; )
        pg(l, o, c), l = l.sibling;
  }
  function _g(l) {
    var o = l.stateNode, c = l.memoizedProps;
    try {
      for (var d = l.type, v = o.attributes; v.length; )
        o.removeAttributeNode(v[0]);
      Vt(o, d, c), o[Ue] = l, o[se] = c;
    } catch (S) {
      Le(l, l.return, S);
    }
  }
  var Xa = !1, un = !1, vg = !1, Sg = typeof WeakSet == "function" ? WeakSet : Set, An = null;
  function Sp(l, o) {
    if (l = l.containerInfo, ch = yh, l = wy(l), vf(l)) {
      if ("selectionStart" in l)
        var c = {
          start: l.selectionStart,
          end: l.selectionEnd
        };
      else
        t: {
          c = (c = l.ownerDocument) && c.defaultView || window;
          var d = c.getSelection && c.getSelection();
          if (d && d.rangeCount !== 0) {
            c = d.anchorNode;
            var v = d.anchorOffset, S = d.focusNode;
            d = d.focusOffset;
            try {
              c.nodeType, S.nodeType;
            } catch {
              c = null;
              break t;
            }
            var A = 0, M = -1, G = -1, W = 0, ct = 0, gt = l, $ = null;
            e: for (; ; ) {
              for (var et; gt !== c || v !== 0 && gt.nodeType !== 3 || (M = A + v), gt !== S || d !== 0 && gt.nodeType !== 3 || (G = A + d), gt.nodeType === 3 && (A += gt.nodeValue.length), (et = gt.firstChild) !== null; )
                $ = gt, gt = et;
              for (; ; ) {
                if (gt === l) break e;
                if ($ === c && ++W === v && (M = A), $ === S && ++ct === d && (G = A), (et = gt.nextSibling) !== null) break;
                gt = $, $ = gt.parentNode;
              }
              gt = et;
            }
            c = M === -1 || G === -1 ? null : { start: M, end: G };
          } else c = null;
        }
      c = c || { start: 0, end: 0 };
    } else c = null;
    for (fh = { focusedElem: l, selectionRange: c }, yh = !1, An = o; An !== null; )
      if (o = An, l = o.child, (o.subtreeFlags & 1024) !== 0 && l !== null)
        l.return = o, An = l;
      else
        for (; An !== null; ) {
          switch (o = An, S = o.alternate, l = o.flags, o.tag) {
            case 0:
              break;
            case 11:
            case 15:
              break;
            case 1:
              if (l & 1024 && S !== null) {
                l = void 0, c = o, v = S.memoizedProps, S = S.memoizedState, d = c.stateNode;
                try {
                  var Gt = wo(
                    c.type,
                    v,
                    c.elementType === c.type
                  );
                  l = d.getSnapshotBeforeUpdate(
                    Gt,
                    S
                  ), d.__reactInternalSnapshotBeforeUpdate = l;
                } catch (Ht) {
                  Le(
                    c,
                    c.return,
                    Ht
                  );
                }
              }
              break;
            case 3:
              if (l & 1024) {
                if (l = o.stateNode.containerInfo, c = l.nodeType, c === 9)
                  hh(l);
                else if (c === 1)
                  switch (l.nodeName) {
                    case "HEAD":
                    case "HTML":
                    case "BODY":
                      hh(l);
                      break;
                    default:
                      l.textContent = "";
                  }
              }
              break;
            case 5:
            case 26:
            case 27:
            case 6:
            case 4:
            case 17:
              break;
            default:
              if (l & 1024) throw Error(a(163));
          }
          if (l = o.sibling, l !== null) {
            l.return = o.return, An = l;
            break;
          }
          An = o.return;
        }
  }
  function Ep(l, o, c) {
    var d = c.flags;
    switch (c.tag) {
      case 0:
      case 11:
      case 15:
        Va(l, c), d & 4 && Wf(5, c);
        break;
      case 1:
        if (Va(l, c), d & 4)
          if (l = c.stateNode, o === null)
            try {
              l.componentDidMount();
            } catch (A) {
              Le(c, c.return, A);
            }
          else {
            var v = wo(
              c.type,
              o.memoizedProps
            );
            o = o.memoizedState;
            try {
              l.componentDidUpdate(
                v,
                o,
                l.__reactInternalSnapshotBeforeUpdate
              );
            } catch (A) {
              Le(
                c,
                c.return,
                A
              );
            }
          }
        d & 64 && yg(c), d & 512 && $u(c, c.return);
        break;
      case 3:
        if (Va(l, c), d & 64 && (l = c.updateQueue, l !== null)) {
          if (o = null, c.child !== null)
            switch (c.child.tag) {
              case 27:
              case 5:
                o = c.child.stateNode;
                break;
              case 1:
                o = c.child.stateNode;
            }
          try {
            Of(l, o);
          } catch (A) {
            Le(c, c.return, A);
          }
        }
        break;
      case 27:
        o === null && d & 4 && _g(c);
      case 26:
      case 5:
        Va(l, c), o === null && d & 4 && tc(c), d & 512 && $u(c, c.return);
        break;
      case 12:
        Va(l, c);
        break;
      case 13:
        Va(l, c), d & 4 && Eg(l, c), d & 64 && (l = c.memoizedState, l !== null && (l = l.dehydrated, l !== null && (c = jS.bind(
          null,
          c
        ), FS(l, c))));
        break;
      case 22:
        if (d = c.memoizedState !== null || Xa, !d) {
          o = o !== null && o.memoizedState !== null || un, v = Xa;
          var S = un;
          Xa = d, (un = o) && !S ? Lo(
            l,
            c,
            (c.subtreeFlags & 8772) !== 0
          ) : Va(l, c), Xa = v, un = S;
        }
        break;
      case 30:
        break;
      default:
        Va(l, c);
    }
  }
  function bp(l) {
    var o = l.alternate;
    o !== null && (l.alternate = null, bp(o)), l.child = null, l.deletions = null, l.sibling = null, l.tag === 5 && (o = l.stateNode, o !== null && nf(o)), l.stateNode = null, l.return = null, l.dependencies = null, l.memoizedProps = null, l.memoizedState = null, l.pendingProps = null, l.stateNode = null, l.updateQueue = null;
  }
  var We = null, qn = !1;
  function ka(l, o, c) {
    for (c = c.child; c !== null; )
      oe(l, o, c), c = c.sibling;
  }
  function oe(l, o, c) {
    if (pn && typeof pn.onCommitFiberUnmount == "function")
      try {
        pn.onCommitFiberUnmount(Ql, c);
      } catch {
      }
    switch (c.tag) {
      case 26:
        un || ca(c, o), ka(
          l,
          o,
          c
        ), c.memoizedState ? c.memoizedState.count-- : c.stateNode && (c = c.stateNode, c.parentNode.removeChild(c));
        break;
      case 27:
        un || ca(c, o);
        var d = We, v = qn;
        jo(c.type) && (We = c.stateNode, qn = !1), ka(
          l,
          o,
          c
        ), ji(c.stateNode), We = d, qn = v;
        break;
      case 5:
        un || ca(c, o);
      case 6:
        if (d = We, v = qn, We = null, ka(
          l,
          o,
          c
        ), We = d, qn = v, We !== null)
          if (qn)
            try {
              (We.nodeType === 9 ? We.body : We.nodeName === "HTML" ? We.ownerDocument.body : We).removeChild(c.stateNode);
            } catch (S) {
              Le(
                c,
                o,
                S
              );
            }
          else
            try {
              We.removeChild(c.stateNode);
            } catch (S) {
              Le(
                c,
                o,
                S
              );
            }
        break;
      case 18:
        We !== null && (qn ? (l = We, Yg(
          l.nodeType === 9 ? l.body : l.nodeName === "HTML" ? l.ownerDocument.body : l,
          c.stateNode
        ), Ja(l)) : Yg(We, c.stateNode));
        break;
      case 4:
        d = We, v = qn, We = c.stateNode.containerInfo, qn = !0, ka(
          l,
          o,
          c
        ), We = d, qn = v;
        break;
      case 0:
      case 11:
      case 14:
      case 15:
        un || Oo(2, c, o), un || Oo(4, c, o), ka(
          l,
          o,
          c
        );
        break;
      case 1:
        un || (ca(c, o), d = c.stateNode, typeof d.componentWillUnmount == "function" && _p(
          c,
          o,
          d
        )), ka(
          l,
          o,
          c
        );
        break;
      case 21:
        ka(
          l,
          o,
          c
        );
        break;
      case 22:
        un = (d = un) || c.memoizedState !== null, ka(
          l,
          o,
          c
        ), un = d;
        break;
      default:
        ka(
          l,
          o,
          c
        );
    }
  }
  function Eg(l, o) {
    if (o.memoizedState === null && (l = o.alternate, l !== null && (l = l.memoizedState, l !== null && (l = l.dehydrated, l !== null))))
      try {
        Ja(l);
      } catch (c) {
        Le(o, o.return, c);
      }
  }
  function Tp(l) {
    switch (l.tag) {
      case 13:
      case 19:
        var o = l.stateNode;
        return o === null && (o = l.stateNode = new Sg()), o;
      case 22:
        return l = l.stateNode, o = l._retryCache, o === null && (o = l._retryCache = new Sg()), o;
      default:
        throw Error(a(435, l.tag));
    }
  }
  function bg(l, o) {
    var c = Tp(l);
    o.forEach(function(d) {
      var v = BS.bind(null, l, d);
      c.has(d) || (c.add(d), d.then(v, v));
    });
  }
  function yi(l, o) {
    var c = o.deletions;
    if (c !== null)
      for (var d = 0; d < c.length; d++) {
        var v = c[d], S = l, A = o, M = A;
        t: for (; M !== null; ) {
          switch (M.tag) {
            case 27:
              if (jo(M.type)) {
                We = M.stateNode, qn = !1;
                break t;
              }
              break;
            case 5:
              We = M.stateNode, qn = !1;
              break t;
            case 3:
            case 4:
              We = M.stateNode.containerInfo, qn = !0;
              break t;
          }
          M = M.return;
        }
        if (We === null) throw Error(a(160));
        oe(S, A, v), We = null, qn = !1, S = v.alternate, S !== null && (S.return = null), v.return = null;
      }
    if (o.subtreeFlags & 13878)
      for (o = o.child; o !== null; )
        Jf(o, l), o = o.sibling;
  }
  var pi = null;
  function Jf(l, o) {
    var c = l.alternate, d = l.flags;
    switch (l.tag) {
      case 0:
      case 11:
      case 14:
      case 15:
        yi(o, l), Un(l), d & 4 && (Oo(3, l, l.return), Wf(3, l), Oo(5, l, l.return));
        break;
      case 1:
        yi(o, l), Un(l), d & 512 && (un || c === null || ca(c, c.return)), d & 64 && Xa && (l = l.updateQueue, l !== null && (d = l.callbacks, d !== null && (c = l.shared.hiddenCallbacks, l.shared.hiddenCallbacks = c === null ? d : c.concat(d))));
        break;
      case 26:
        var v = pi;
        if (yi(o, l), Un(l), d & 512 && (un || c === null || ca(c, c.return)), d & 4) {
          var S = c !== null ? c.memoizedState : null;
          if (d = l.memoizedState, c === null)
            if (d === null)
              if (l.stateNode === null) {
                t: {
                  d = l.type, c = l.memoizedProps, v = v.ownerDocument || v;
                  e: switch (d) {
                    case "title":
                      S = v.getElementsByTagName("title")[0], (!S || S[wt] || S[Ue] || S.namespaceURI === "http://www.w3.org/2000/svg" || S.hasAttribute("itemprop")) && (S = v.createElement(d), v.head.insertBefore(
                        S,
                        v.querySelector("head > title")
                      )), Vt(S, d, c), S[Ue] = l, Tn(S), d = S;
                      break t;
                    case "link":
                      var A = Qp(
                        "link",
                        "href",
                        v
                      ).get(d + (c.href || ""));
                      if (A) {
                        for (var M = 0; M < A.length; M++)
                          if (S = A[M], S.getAttribute("href") === (c.href == null || c.href === "" ? null : c.href) && S.getAttribute("rel") === (c.rel == null ? null : c.rel) && S.getAttribute("title") === (c.title == null ? null : c.title) && S.getAttribute("crossorigin") === (c.crossOrigin == null ? null : c.crossOrigin)) {
                            A.splice(M, 1);
                            break e;
                          }
                      }
                      S = v.createElement(d), Vt(S, d, c), v.head.appendChild(S);
                      break;
                    case "meta":
                      if (A = Qp(
                        "meta",
                        "content",
                        v
                      ).get(d + (c.content || ""))) {
                        for (M = 0; M < A.length; M++)
                          if (S = A[M], S.getAttribute("content") === (c.content == null ? null : "" + c.content) && S.getAttribute("name") === (c.name == null ? null : c.name) && S.getAttribute("property") === (c.property == null ? null : c.property) && S.getAttribute("http-equiv") === (c.httpEquiv == null ? null : c.httpEquiv) && S.getAttribute("charset") === (c.charSet == null ? null : c.charSet)) {
                            A.splice(M, 1);
                            break e;
                          }
                      }
                      S = v.createElement(d), Vt(S, d, c), v.head.appendChild(S);
                      break;
                    default:
                      throw Error(a(468, d));
                  }
                  S[Ue] = l, Tn(S), d = S;
                }
                l.stateNode = d;
              } else
                Wp(
                  v,
                  l.type,
                  l.stateNode
                );
            else
              l.stateNode = Nv(
                v,
                d,
                l.memoizedProps
              );
          else
            S !== d ? (S === null ? c.stateNode !== null && (c = c.stateNode, c.parentNode.removeChild(c)) : S.count--, d === null ? Wp(
              v,
              l.type,
              l.stateNode
            ) : Nv(
              v,
              d,
              l.memoizedProps
            )) : d === null && l.stateNode !== null && vp(
              l,
              l.memoizedProps,
              c.memoizedProps
            );
        }
        break;
      case 27:
        yi(o, l), Un(l), d & 512 && (un || c === null || ca(c, c.return)), c !== null && d & 4 && vp(
          l,
          l.memoizedProps,
          c.memoizedProps
        );
        break;
      case 5:
        if (yi(o, l), Un(l), d & 512 && (un || c === null || ca(c, c.return)), l.flags & 32) {
          v = l.stateNode;
          try {
            Cu(v, "");
          } catch (et) {
            Le(l, l.return, et);
          }
        }
        d & 4 && l.stateNode != null && (v = l.memoizedProps, vp(
          l,
          v,
          c !== null ? c.memoizedProps : v
        )), d & 1024 && (vg = !0);
        break;
      case 6:
        if (yi(o, l), Un(l), d & 4) {
          if (l.stateNode === null)
            throw Error(a(162));
          d = l.memoizedProps, c = l.stateNode;
          try {
            c.nodeValue = d;
          } catch (et) {
            Le(l, l.return, et);
          }
        }
        break;
      case 3:
        if (ko = null, v = pi, pi = jg(o.containerInfo), yi(o, l), pi = v, Un(l), d & 4 && c !== null && c.memoizedState.isDehydrated)
          try {
            Ja(o.containerInfo);
          } catch (et) {
            Le(l, l.return, et);
          }
        vg && (vg = !1, xp(l));
        break;
      case 4:
        d = pi, pi = jg(
          l.stateNode.containerInfo
        ), yi(o, l), Un(l), pi = d;
        break;
      case 12:
        yi(o, l), Un(l);
        break;
      case 13:
        yi(o, l), Un(l), l.child.flags & 8192 && l.memoizedState !== null != (c !== null && c.memoizedState !== null) && (Lp = yn()), d & 4 && (d = l.updateQueue, d !== null && (l.updateQueue = null, bg(l, d)));
        break;
      case 22:
        v = l.memoizedState !== null;
        var G = c !== null && c.memoizedState !== null, W = Xa, ct = un;
        if (Xa = W || v, un = ct || G, yi(o, l), un = ct, Xa = W, Un(l), d & 8192)
          t: for (o = l.stateNode, o._visibility = v ? o._visibility & -2 : o._visibility | 1, v && (c === null || G || Xa || un || Je(l)), c = null, o = l; ; ) {
            if (o.tag === 5 || o.tag === 26) {
              if (c === null) {
                G = c = o;
                try {
                  if (S = G.stateNode, v)
                    A = S.style, typeof A.setProperty == "function" ? A.setProperty("display", "none", "important") : A.display = "none";
                  else {
                    M = G.stateNode;
                    var gt = G.memoizedProps.style, $ = gt != null && gt.hasOwnProperty("display") ? gt.display : null;
                    M.style.display = $ == null || typeof $ == "boolean" ? "" : ("" + $).trim();
                  }
                } catch (et) {
                  Le(G, G.return, et);
                }
              }
            } else if (o.tag === 6) {
              if (c === null) {
                G = o;
                try {
                  G.stateNode.nodeValue = v ? "" : G.memoizedProps;
                } catch (et) {
                  Le(G, G.return, et);
                }
              }
            } else if ((o.tag !== 22 && o.tag !== 23 || o.memoizedState === null || o === l) && o.child !== null) {
              o.child.return = o, o = o.child;
              continue;
            }
            if (o === l) break t;
            for (; o.sibling === null; ) {
              if (o.return === null || o.return === l) break t;
              c === o && (c = null), o = o.return;
            }
            c === o && (c = null), o.sibling.return = o.return, o = o.sibling;
          }
        d & 4 && (d = l.updateQueue, d !== null && (c = d.retryQueue, c !== null && (d.retryQueue = null, bg(l, c))));
        break;
      case 19:
        yi(o, l), Un(l), d & 4 && (d = l.updateQueue, d !== null && (l.updateQueue = null, bg(l, d)));
        break;
      case 30:
        break;
      case 21:
        break;
      default:
        yi(o, l), Un(l);
    }
  }
  function Un(l) {
    var o = l.flags;
    if (o & 2) {
      try {
        for (var c, d = l.return; d !== null; ) {
          if (pv(d)) {
            c = d;
            break;
          }
          d = d.return;
        }
        if (c == null) throw Error(a(160));
        switch (c.tag) {
          case 27:
            var v = c.stateNode, S = Ll(l);
            pg(l, S, v);
            break;
          case 5:
            var A = c.stateNode;
            c.flags & 32 && (Cu(A, ""), c.flags &= -33);
            var M = Ll(l);
            pg(l, M, A);
            break;
          case 3:
          case 4:
            var G = c.stateNode.containerInfo, W = Ll(l);
            Ur(
              l,
              W,
              G
            );
            break;
          default:
            throw Error(a(161));
        }
      } catch (ct) {
        Le(l, l.return, ct);
      }
      l.flags &= -3;
    }
    o & 4096 && (l.flags &= -4097);
  }
  function xp(l) {
    if (l.subtreeFlags & 1024)
      for (l = l.child; l !== null; ) {
        var o = l;
        xp(o), o.tag === 5 && o.flags & 1024 && o.stateNode.reset(), l = l.sibling;
      }
  }
  function Va(l, o) {
    if (o.subtreeFlags & 8772)
      for (o = o.child; o !== null; )
        Ep(l, o.alternate, o), o = o.sibling;
  }
  function Je(l) {
    for (l = l.child; l !== null; ) {
      var o = l;
      switch (o.tag) {
        case 0:
        case 11:
        case 14:
        case 15:
          Oo(4, o, o.return), Je(o);
          break;
        case 1:
          ca(o, o.return);
          var c = o.stateNode;
          typeof c.componentWillUnmount == "function" && _p(
            o,
            o.return,
            c
          ), Je(o);
          break;
        case 27:
          ji(o.stateNode);
        case 26:
        case 5:
          ca(o, o.return), Je(o);
          break;
        case 22:
          o.memoizedState === null && Je(o);
          break;
        case 30:
          Je(o);
          break;
        default:
          Je(o);
      }
      l = l.sibling;
    }
  }
  function Lo(l, o, c) {
    for (c = c && (o.subtreeFlags & 8772) !== 0, o = o.child; o !== null; ) {
      var d = o.alternate, v = l, S = o, A = S.flags;
      switch (S.tag) {
        case 0:
        case 11:
        case 15:
          Lo(
            v,
            S,
            c
          ), Wf(4, S);
          break;
        case 1:
          if (Lo(
            v,
            S,
            c
          ), d = S, v = d.stateNode, typeof v.componentDidMount == "function")
            try {
              v.componentDidMount();
            } catch (W) {
              Le(d, d.return, W);
            }
          if (d = S, v = d.updateQueue, v !== null) {
            var M = d.stateNode;
            try {
              var G = v.shared.hiddenCallbacks;
              if (G !== null)
                for (v.shared.hiddenCallbacks = null, v = 0; v < G.length; v++)
                  Yd(G[v], M);
            } catch (W) {
              Le(d, d.return, W);
            }
          }
          c && A & 64 && yg(S), $u(S, S.return);
          break;
        case 27:
          _g(S);
        case 26:
        case 5:
          Lo(
            v,
            S,
            c
          ), c && d === null && A & 4 && tc(S), $u(S, S.return);
          break;
        case 12:
          Lo(
            v,
            S,
            c
          );
          break;
        case 13:
          Lo(
            v,
            S,
            c
          ), c && A & 4 && Eg(v, S);
          break;
        case 22:
          S.memoizedState === null && Lo(
            v,
            S,
            c
          ), $u(S, S.return);
          break;
        case 30:
          break;
        default:
          Lo(
            v,
            S,
            c
          );
      }
      o = o.sibling;
    }
  }
  function zl(l, o) {
    var c = null;
    l !== null && l.memoizedState !== null && l.memoizedState.cachePool !== null && (c = l.memoizedState.cachePool.pool), l = null, o.memoizedState !== null && o.memoizedState.cachePool !== null && (l = o.memoizedState.cachePool.pool), l !== c && (l != null && l.refCount++, c != null && Oa(c));
  }
  function Tg(l, o) {
    l = null, o.alternate !== null && (l = o.alternate.memoizedState.cache), o = o.memoizedState.cache, o !== l && (o.refCount++, l != null && Oa(l));
  }
  function Zn(l, o, c, d) {
    if (o.subtreeFlags & 10256)
      for (o = o.child; o !== null; )
        Cp(
          l,
          o,
          c,
          d
        ), o = o.sibling;
  }
  function Cp(l, o, c, d) {
    var v = o.flags;
    switch (o.tag) {
      case 0:
      case 11:
      case 15:
        Zn(
          l,
          o,
          c,
          d
        ), v & 2048 && Wf(9, o);
        break;
      case 1:
        Zn(
          l,
          o,
          c,
          d
        );
        break;
      case 3:
        Zn(
          l,
          o,
          c,
          d
        ), v & 2048 && (l = null, o.alternate !== null && (l = o.alternate.memoizedState.cache), o = o.memoizedState.cache, o !== l && (o.refCount++, l != null && Oa(l)));
        break;
      case 12:
        if (v & 2048) {
          Zn(
            l,
            o,
            c,
            d
          ), l = o.stateNode;
          try {
            var S = o.memoizedProps, A = S.id, M = S.onPostCommit;
            typeof M == "function" && M(
              A,
              o.alternate === null ? "mount" : "update",
              l.passiveEffectDuration,
              -0
            );
          } catch (G) {
            Le(o, o.return, G);
          }
        } else
          Zn(
            l,
            o,
            c,
            d
          );
        break;
      case 13:
        Zn(
          l,
          o,
          c,
          d
        );
        break;
      case 23:
        break;
      case 22:
        S = o.stateNode, A = o.alternate, o.memoizedState !== null ? S._visibility & 2 ? Zn(
          l,
          o,
          c,
          d
        ) : be(l, o) : S._visibility & 2 ? Zn(
          l,
          o,
          c,
          d
        ) : (S._visibility |= 2, zs(
          l,
          o,
          c,
          d,
          (o.subtreeFlags & 10256) !== 0
        )), v & 2048 && zl(A, o);
        break;
      case 24:
        Zn(
          l,
          o,
          c,
          d
        ), v & 2048 && Tg(o.alternate, o);
        break;
      default:
        Zn(
          l,
          o,
          c,
          d
        );
    }
  }
  function zs(l, o, c, d, v) {
    for (v = v && (o.subtreeFlags & 10256) !== 0, o = o.child; o !== null; ) {
      var S = l, A = o, M = c, G = d, W = A.flags;
      switch (A.tag) {
        case 0:
        case 11:
        case 15:
          zs(
            S,
            A,
            M,
            G,
            v
          ), Wf(8, A);
          break;
        case 23:
          break;
        case 22:
          var ct = A.stateNode;
          A.memoizedState !== null ? ct._visibility & 2 ? zs(
            S,
            A,
            M,
            G,
            v
          ) : be(
            S,
            A
          ) : (ct._visibility |= 2, zs(
            S,
            A,
            M,
            G,
            v
          )), v && W & 2048 && zl(
            A.alternate,
            A
          );
          break;
        case 24:
          zs(
            S,
            A,
            M,
            G,
            v
          ), v && W & 2048 && Tg(A.alternate, A);
          break;
        default:
          zs(
            S,
            A,
            M,
            G,
            v
          );
      }
      o = o.sibling;
    }
  }
  function be(l, o) {
    if (o.subtreeFlags & 10256)
      for (o = o.child; o !== null; ) {
        var c = l, d = o, v = d.flags;
        switch (d.tag) {
          case 22:
            be(c, d), v & 2048 && zl(
              d.alternate,
              d
            );
            break;
          case 24:
            be(c, d), v & 2048 && Tg(d.alternate, d);
            break;
          default:
            be(c, d);
        }
        o = o.sibling;
      }
  }
  var Gr = 8192;
  function cn(l) {
    if (l.subtreeFlags & Gr)
      for (l = l.child; l !== null; )
        _v(l), l = l.sibling;
  }
  function _v(l) {
    switch (l.tag) {
      case 26:
        cn(l), l.flags & Gr && l.memoizedState !== null && Hv(
          pi,
          l.memoizedState,
          l.memoizedProps
        );
        break;
      case 5:
        cn(l);
        break;
      case 3:
      case 4:
        var o = pi;
        pi = jg(l.stateNode.containerInfo), cn(l), pi = o;
        break;
      case 22:
        l.memoizedState === null && (o = l.alternate, o !== null && o.memoizedState !== null ? (o = Gr, Gr = 16777216, cn(l), Gr = o) : cn(l));
        break;
      default:
        cn(l);
    }
  }
  function Rp(l) {
    var o = l.alternate;
    if (o !== null && (l = o.child, l !== null)) {
      o.child = null;
      do
        o = l.sibling, l.sibling = null, l = o;
      while (l !== null);
    }
  }
  function Hr(l) {
    var o = l.deletions;
    if (l.flags & 16) {
      if (o !== null)
        for (var c = 0; c < o.length; c++) {
          var d = o[c];
          An = d, Mp(
            d,
            l
          );
        }
      Rp(l);
    }
    if (l.subtreeFlags & 10256)
      for (l = l.child; l !== null; )
        Ap(l), l = l.sibling;
  }
  function Ap(l) {
    switch (l.tag) {
      case 0:
      case 11:
      case 15:
        Hr(l), l.flags & 2048 && Oo(9, l, l.return);
        break;
      case 3:
        Hr(l);
        break;
      case 12:
        Hr(l);
        break;
      case 22:
        var o = l.stateNode;
        l.memoizedState !== null && o._visibility & 2 && (l.return === null || l.return.tag !== 13) ? (o._visibility &= -3, _i(l)) : Hr(l);
        break;
      default:
        Hr(l);
    }
  }
  function _i(l) {
    var o = l.deletions;
    if (l.flags & 16) {
      if (o !== null)
        for (var c = 0; c < o.length; c++) {
          var d = o[c];
          An = d, Mp(
            d,
            l
          );
        }
      Rp(l);
    }
    for (l = l.child; l !== null; ) {
      switch (o = l, o.tag) {
        case 0:
        case 11:
        case 15:
          Oo(8, o, o.return), _i(o);
          break;
        case 22:
          c = o.stateNode, c._visibility & 2 && (c._visibility &= -3, _i(o));
          break;
        default:
          _i(o);
      }
      l = l.sibling;
    }
  }
  function Mp(l, o) {
    for (; An !== null; ) {
      var c = An;
      switch (c.tag) {
        case 0:
        case 11:
        case 15:
          Oo(8, c, o);
          break;
        case 23:
        case 22:
          if (c.memoizedState !== null && c.memoizedState.cachePool !== null) {
            var d = c.memoizedState.cachePool.pool;
            d != null && d.refCount++;
          }
          break;
        case 24:
          Oa(c.memoizedState.cache);
      }
      if (d = c.child, d !== null) d.return = c, An = d;
      else
        t: for (c = l; An !== null; ) {
          d = An;
          var v = d.sibling, S = d.return;
          if (bp(d), d === c) {
            An = null;
            break t;
          }
          if (v !== null) {
            v.return = S, An = v;
            break t;
          }
          An = S;
        }
    }
  }
  var wp = {
    getCacheForType: function(l) {
      var o = Ln(xn), c = o.data.get(l);
      return c === void 0 && (c = l(), o.data.set(l, c)), c;
    }
  }, vv = typeof WeakMap == "function" ? WeakMap : Map, Re = 0, Be = null, ce = null, fe = 0, Ae = 0, Gi = null, qa = !1, ec = !1, Dp = !1, Ns = 0, fn = 0, Us = 0, Yr = 0, Za = 0, Nl = 0, nc = 0, ic = null, Hi = null, Op = !1, Lp = 0, xg = 1 / 0, lc = null, zo = null, Fn = 0, Fa = null, ac = null, Kn = 0, Cg = 0, Rg = null, zp = null, sc = 0, Np = null;
  function ll() {
    if (Re & 2 && fe !== 0)
      return fe & -fe;
    if (Y.T !== null) {
      var l = wl;
      return l !== 0 ? l : Xr();
    }
    return Wi();
  }
  function Up() {
    Nl === 0 && (Nl = !(fe & 536870912) || ve ? El() : 536870912);
    var l = il.current;
    return l !== null && (l.flags |= 32), Nl;
  }
  function al(l, o, c) {
    (l === Be && (Ae === 2 || Ae === 9) || l.cancelPendingCommit !== null) && (Ka(l, 0), Gs(
      l,
      fe,
      Nl,
      !1
    )), Qi(l, c), (!(Re & 2) || l !== Be) && (l === Be && (!(Re & 2) && (Yr |= c), fn === 4 && Gs(
      l,
      fe,
      Nl,
      !1
    )), Yi(l));
  }
  function oc(l, o, c) {
    if (Re & 6) throw Error(a(327));
    var d = !c && (o & 124) === 0 && (o & l.expiredLanes) === 0 || Ki(l, o), v = d ? Hp(l, o) : Ag(l, o, !0), S = d;
    do {
      if (v === 0) {
        ec && !d && Gs(l, o, 0, !1);
        break;
      } else {
        if (c = l.current.alternate, S && !Sv(c)) {
          v = Ag(l, o, !1), S = !1;
          continue;
        }
        if (v === 2) {
          if (S = o, l.errorRecoveryDisabledLanes & S)
            var A = 0;
          else
            A = l.pendingLanes & -536870913, A = A !== 0 ? A : A & 536870912 ? 536870912 : 0;
          if (A !== 0) {
            o = A;
            t: {
              var M = l;
              v = ic;
              var G = M.current.memoizedState.isDehydrated;
              if (G && (Ka(M, A).flags |= 256), A = Ag(
                M,
                A,
                !1
              ), A !== 2) {
                if (Dp && !G) {
                  M.errorRecoveryDisabledLanes |= S, Yr |= S, v = 4;
                  break t;
                }
                S = Hi, Hi = v, S !== null && (Hi === null ? Hi = S : Hi.push.apply(
                  Hi,
                  S
                ));
              }
              v = A;
            }
            if (S = !1, v !== 2) continue;
          }
        }
        if (v === 1) {
          Ka(l, 0), Gs(l, o, 0, !0);
          break;
        }
        t: {
          switch (d = l, S = v, S) {
            case 0:
            case 1:
              throw Error(a(345));
            case 4:
              if ((o & 4194048) !== o) break;
            case 6:
              Gs(
                d,
                o,
                Nl,
                !qa
              );
              break t;
            case 2:
              Hi = null;
              break;
            case 3:
            case 5:
              break;
            default:
              throw Error(a(329));
          }
          if ((o & 62914560) === o && (v = Lp + 300 - yn(), 10 < v)) {
            if (Gs(
              d,
              o,
              Nl,
              !qa
            ), Sl(d, 0, !0) !== 0) break t;
            d.timeoutHandle = Hg(
              $f.bind(
                null,
                d,
                c,
                Hi,
                lc,
                Op,
                o,
                Nl,
                Yr,
                nc,
                qa,
                S,
                2,
                -0,
                0
              ),
              v
            );
            break t;
          }
          $f(
            d,
            c,
            Hi,
            lc,
            Op,
            o,
            Nl,
            Yr,
            nc,
            qa,
            S,
            0,
            -0,
            0
          );
        }
      }
      break;
    } while (!0);
    Yi(l);
  }
  function $f(l, o, c, d, v, S, A, M, G, W, ct, gt, $, et) {
    if (l.timeoutHandle = -1, gt = o.subtreeFlags, (gt & 8192 || (gt & 16785408) === 16785408) && (vc = { stylesheets: null, count: 0, unsuspend: Gv }, _v(o), gt = $p(), gt !== null)) {
      l.cancelPendingCommit = gt(
        Tv.bind(
          null,
          l,
          o,
          S,
          c,
          d,
          v,
          A,
          M,
          G,
          ct,
          1,
          $,
          et
        )
      ), Gs(l, S, A, !W);
      return;
    }
    Tv(
      l,
      o,
      S,
      c,
      d,
      v,
      A,
      M,
      G
    );
  }
  function Sv(l) {
    for (var o = l; ; ) {
      var c = o.tag;
      if ((c === 0 || c === 11 || c === 15) && o.flags & 16384 && (c = o.updateQueue, c !== null && (c = c.stores, c !== null)))
        for (var d = 0; d < c.length; d++) {
          var v = c[d], S = v.getSnapshot;
          v = v.value;
          try {
            if (!Vn(S(), v)) return !1;
          } catch {
            return !1;
          }
        }
      if (c = o.child, o.subtreeFlags & 16384 && c !== null)
        c.return = o, o = c;
      else {
        if (o === l) break;
        for (; o.sibling === null; ) {
          if (o.return === null || o.return === l) return !0;
          o = o.return;
        }
        o.sibling.return = o.return, o = o.sibling;
      }
    }
    return !0;
  }
  function Gs(l, o, c, d) {
    o &= ~Za, o &= ~Yr, l.suspendedLanes |= o, l.pingedLanes &= ~o, d && (l.warmLanes |= o), d = l.expirationTimes;
    for (var v = o; 0 < v; ) {
      var S = 31 - Bn(v), A = 1 << S;
      d[S] = -1, v &= ~A;
    }
    c !== 0 && ao(l, c, o);
  }
  function jr() {
    return Re & 6 ? !0 : (ih(0), !1);
  }
  function No() {
    if (ce !== null) {
      if (Ae === 0)
        var l = ce.return;
      else
        l = ce, sa = Rs = null, Xd(l), wr = null, Ku = 0, l = ce;
      for (; l !== null; )
        pp(l.alternate, l), l = l.return;
      ce = null;
    }
  }
  function Ka(l, o) {
    var c = l.timeoutHandle;
    c !== -1 && (l.timeoutHandle = -1, VS(c)), c = l.cancelPendingCommit, c !== null && (l.cancelPendingCommit = null, c()), No(), Be = l, ce = c = la(l.current, null), fe = o, Ae = 0, Gi = null, qa = !1, ec = Ki(l, o), Dp = !1, nc = Nl = Za = Yr = Us = fn = 0, Hi = ic = null, Op = !1, o & 8 && (o |= o & 32);
    var d = l.entangledLanes;
    if (d !== 0)
      for (l = l.entanglements, d &= o; 0 < d; ) {
        var v = 31 - Bn(d), S = 1 << v;
        o |= l[v], d &= ~S;
      }
    return Ns = o, ia(), c;
  }
  function Gp(l, o) {
    Pt = null, Y.H = ig, o === xo || o === wf ? (o = By(), Ae = 3) : o === zd ? (o = By(), Ae = 4) : Ae = o === rn ? 8 : o !== null && typeof o == "object" && typeof o.then == "function" ? 6 : 1, Gi = o, ce === null && (fn = 1, Ff(
      l,
      tl(o, l.current)
    ));
  }
  function Ev() {
    var l = Y.H;
    return Y.H = ig, l === null ? ig : l;
  }
  function Br() {
    var l = Y.A;
    return Y.A = wp, l;
  }
  function Ir() {
    fn = 4, qa || (fe & 4194048) !== fe && il.current !== null || (ec = !0), !(Us & 134217727) && !(Yr & 134217727) || Be === null || Gs(
      Be,
      fe,
      Nl,
      !1
    );
  }
  function Ag(l, o, c) {
    var d = Re;
    Re |= 2;
    var v = Ev(), S = Br();
    (Be !== l || fe !== o) && (lc = null, Ka(l, o)), o = !1;
    var A = fn;
    t: do
      try {
        if (Ae !== 0 && ce !== null) {
          var M = ce, G = Gi;
          switch (Ae) {
            case 8:
              No(), A = 6;
              break t;
            case 3:
            case 2:
            case 9:
            case 6:
              il.current === null && (o = !0);
              var W = Ae;
              if (Ae = 0, Gi = null, rc(l, M, G, W), c && ec) {
                A = 0;
                break t;
              }
              break;
            default:
              W = Ae, Ae = 0, Gi = null, rc(l, M, G, W);
          }
        }
        Mg(), A = fn;
        break;
      } catch (ct) {
        Gp(l, ct);
      }
    while (!0);
    return o && l.shellSuspendCounter++, sa = Rs = null, Re = d, Y.H = v, Y.A = S, ce === null && (Be = null, fe = 0, ia()), A;
  }
  function Mg() {
    for (; ce !== null; ) jp(ce);
  }
  function Hp(l, o) {
    var c = Re;
    Re |= 2;
    var d = Ev(), v = Br();
    Be !== l || fe !== o ? (lc = null, xg = yn() + 500, Ka(l, o)) : ec = Ki(
      l,
      o
    );
    t: do
      try {
        if (Ae !== 0 && ce !== null) {
          o = ce;
          var S = Gi;
          e: switch (Ae) {
            case 1:
              Ae = 0, Gi = null, rc(l, o, S, 1);
              break;
            case 2:
            case 9:
              if (Ud(S)) {
                Ae = 0, Gi = null, Bp(o);
                break;
              }
              o = function() {
                Ae !== 2 && Ae !== 9 || Be !== l || (Ae = 7), Yi(l);
              }, S.then(o, o);
              break t;
            case 3:
              Ae = 7;
              break t;
            case 4:
              Ae = 5;
              break t;
            case 7:
              Ud(S) ? (Ae = 0, Gi = null, Bp(o)) : (Ae = 0, Gi = null, rc(l, o, S, 7));
              break;
            case 5:
              var A = null;
              switch (ce.tag) {
                case 26:
                  A = ce.memoizedState;
                case 5:
                case 27:
                  var M = ce;
                  if (!A || Jp(A)) {
                    Ae = 0, Gi = null;
                    var G = M.sibling;
                    if (G !== null) ce = G;
                    else {
                      var W = M.return;
                      W !== null ? (ce = W, th(W)) : ce = null;
                    }
                    break e;
                  }
              }
              Ae = 0, Gi = null, rc(l, o, S, 5);
              break;
            case 6:
              Ae = 0, Gi = null, rc(l, o, S, 6);
              break;
            case 8:
              No(), fn = 6;
              break t;
            default:
              throw Error(a(462));
          }
        }
        Yp();
        break;
      } catch (ct) {
        Gp(l, ct);
      }
    while (!0);
    return sa = Rs = null, Y.H = d, Y.A = v, Re = c, ce !== null ? 0 : (Be = null, fe = 0, ia(), fn);
  }
  function Yp() {
    for (; ce !== null && !xi(); )
      jp(ce);
  }
  function jp(l) {
    var o = yv(l.alternate, l, Ns);
    l.memoizedProps = l.pendingProps, o === null ? th(l) : ce = o;
  }
  function Bp(l) {
    var o = l, c = o.alternate;
    switch (o.tag) {
      case 15:
      case 0:
        o = hp(
          c,
          o,
          o.pendingProps,
          o.type,
          void 0,
          fe
        );
        break;
      case 11:
        o = hp(
          c,
          o,
          o.pendingProps,
          o.type.render,
          o.ref,
          fe
        );
        break;
      case 5:
        Xd(o);
      default:
        pp(c, o), o = ce = ne(o, Ns), o = yv(c, o, Ns);
    }
    l.memoizedProps = l.pendingProps, o === null ? th(l) : ce = o;
  }
  function rc(l, o, c, d) {
    sa = Rs = null, Xd(o), wr = null, Ku = 0;
    var v = o.return;
    try {
      if (dv(
        l,
        v,
        o,
        c,
        fe
      )) {
        fn = 1, Ff(
          l,
          tl(c, l.current)
        ), ce = null;
        return;
      }
    } catch (S) {
      if (v !== null) throw ce = v, S;
      fn = 1, Ff(
        l,
        tl(c, l.current)
      ), ce = null;
      return;
    }
    o.flags & 32768 ? (ve || d === 1 ? l = !0 : ec || fe & 536870912 ? l = !1 : (qa = l = !0, (d === 2 || d === 9 || d === 3 || d === 6) && (d = il.current, d !== null && d.tag === 13 && (d.flags |= 16384))), bv(o, l)) : th(o);
  }
  function th(l) {
    var o = l;
    do {
      if (o.flags & 32768) {
        bv(
          o,
          qa
        );
        return;
      }
      l = o.return;
      var c = yp(
        o.alternate,
        o,
        Ns
      );
      if (c !== null) {
        ce = c;
        return;
      }
      if (o = o.sibling, o !== null) {
        ce = o;
        return;
      }
      ce = o = l;
    } while (o !== null);
    fn === 0 && (fn = 5);
  }
  function bv(l, o) {
    do {
      var c = YS(l.alternate, l);
      if (c !== null) {
        c.flags &= 32767, ce = c;
        return;
      }
      if (c = l.return, c !== null && (c.flags |= 32768, c.subtreeFlags = 0, c.deletions = null), !o && (l = l.sibling, l !== null)) {
        ce = l;
        return;
      }
      ce = l = c;
    } while (l !== null);
    fn = 6, ce = null;
  }
  function Tv(l, o, c, d, v, S, A, M, G) {
    l.cancelPendingCommit = null;
    do
      Dg();
    while (Fn !== 0);
    if (Re & 6) throw Error(a(327));
    if (o !== null) {
      if (o === l.current) throw Error(a(177));
      if (S = o.lanes | o.childLanes, S |= Aa, ef(
        l,
        c,
        S,
        A,
        M,
        G
      ), l === Be && (ce = Be = null, fe = 0), ac = o, Fa = l, Kn = c, Cg = S, Rg = v, zp = d, o.subtreeFlags & 10256 || o.flags & 10256 ? (l.callbackNode = null, l.callbackPriority = 0, IS(_l, function() {
        return Ip(), null;
      })) : (l.callbackNode = null, l.callbackPriority = 0), d = (o.flags & 13878) !== 0, o.subtreeFlags & 13878 || d) {
        d = Y.T, Y.T = null, v = K.p, K.p = 2, A = Re, Re |= 4;
        try {
          Sp(l, o, c);
        } finally {
          Re = A, K.p = v, Y.T = d;
        }
      }
      Fn = 1, xv(), eh(), wg();
    }
  }
  function xv() {
    if (Fn === 1) {
      Fn = 0;
      var l = Fa, o = ac, c = (o.flags & 13878) !== 0;
      if (o.subtreeFlags & 13878 || c) {
        c = Y.T, Y.T = null;
        var d = K.p;
        K.p = 2;
        var v = Re;
        Re |= 4;
        try {
          Jf(o, l);
          var S = fh, A = wy(l.containerInfo), M = S.focusedElem, G = S.selectionRange;
          if (A !== M && M && M.ownerDocument && _f(
            M.ownerDocument.documentElement,
            M
          )) {
            if (G !== null && vf(M)) {
              var W = G.start, ct = G.end;
              if (ct === void 0 && (ct = W), "selectionStart" in M)
                M.selectionStart = W, M.selectionEnd = Math.min(
                  ct,
                  M.value.length
                );
              else {
                var gt = M.ownerDocument || document, $ = gt && gt.defaultView || window;
                if ($.getSelection) {
                  var et = $.getSelection(), Gt = M.textContent.length, Ht = Math.min(G.start, Gt), Te = G.end === void 0 ? Ht : Math.min(G.end, Gt);
                  !et.extend && Ht > Te && (A = Te, Te = Ht, Ht = A);
                  var q = Ke(
                    M,
                    Ht
                  ), V = Ke(
                    M,
                    Te
                  );
                  if (q && V && (et.rangeCount !== 1 || et.anchorNode !== q.node || et.anchorOffset !== q.offset || et.focusNode !== V.node || et.focusOffset !== V.offset)) {
                    var P = gt.createRange();
                    P.setStart(q.node, q.offset), et.removeAllRanges(), Ht > Te ? (et.addRange(P), et.extend(V.node, V.offset)) : (P.setEnd(V.node, V.offset), et.addRange(P));
                  }
                }
              }
            }
            for (gt = [], et = M; et = et.parentNode; )
              et.nodeType === 1 && gt.push({
                element: et,
                left: et.scrollLeft,
                top: et.scrollTop
              });
            for (typeof M.focus == "function" && M.focus(), M = 0; M < gt.length; M++) {
              var ht = gt[M];
              ht.element.scrollLeft = ht.left, ht.element.scrollTop = ht.top;
            }
          }
          yh = !!ch, fh = ch = null;
        } finally {
          Re = v, K.p = d, Y.T = c;
        }
      }
      l.current = o, Fn = 2;
    }
  }
  function eh() {
    if (Fn === 2) {
      Fn = 0;
      var l = Fa, o = ac, c = (o.flags & 8772) !== 0;
      if (o.subtreeFlags & 8772 || c) {
        c = Y.T, Y.T = null;
        var d = K.p;
        K.p = 2;
        var v = Re;
        Re |= 4;
        try {
          Ep(l, o.alternate, o);
        } finally {
          Re = v, K.p = d, Y.T = c;
        }
      }
      Fn = 3;
    }
  }
  function wg() {
    if (Fn === 4 || Fn === 3) {
      Fn = 0, Dn();
      var l = Fa, o = ac, c = Kn, d = zp;
      o.subtreeFlags & 10256 || o.flags & 10256 ? Fn = 5 : (Fn = 0, ac = Fa = null, Cv(l, l.pendingLanes));
      var v = l.pendingLanes;
      if (v === 0 && (zo = null), In(c), o = o.stateNode, pn && typeof pn.onCommitFiberRoot == "function")
        try {
          pn.onCommitFiberRoot(
            Ql,
            o,
            void 0,
            (o.current.flags & 128) === 128
          );
        } catch {
        }
      if (d !== null) {
        o = Y.T, v = K.p, K.p = 2, Y.T = null;
        try {
          for (var S = l.onRecoverableError, A = 0; A < d.length; A++) {
            var M = d[A];
            S(M.value, {
              componentStack: M.stack
            });
          }
        } finally {
          Y.T = o, K.p = v;
        }
      }
      Kn & 3 && Dg(), Yi(l), v = l.pendingLanes, c & 4194090 && v & 42 ? l === Np ? sc++ : (sc = 0, Np = l) : sc = 0, ih(0);
    }
  }
  function Cv(l, o) {
    (l.pooledCacheLanes &= o) === 0 && (o = l.pooledCache, o != null && (l.pooledCache = null, Oa(o)));
  }
  function Dg(l) {
    return xv(), eh(), wg(), Ip();
  }
  function Ip() {
    if (Fn !== 5) return !1;
    var l = Fa, o = Cg;
    Cg = 0;
    var c = In(Kn), d = Y.T, v = K.p;
    try {
      K.p = 32 > c ? 32 : c, Y.T = null, c = Rg, Rg = null;
      var S = Fa, A = Kn;
      if (Fn = 0, ac = Fa = null, Kn = 0, Re & 6) throw Error(a(331));
      var M = Re;
      if (Re |= 4, Ap(S.current), Cp(
        S,
        S.current,
        A,
        c
      ), Re = M, ih(0, !1), pn && typeof pn.onPostCommitFiberRoot == "function")
        try {
          pn.onPostCommitFiberRoot(Ql, S);
        } catch {
        }
      return !0;
    } finally {
      K.p = v, Y.T = d, Cv(l, o);
    }
  }
  function Xp(l, o, c) {
    o = tl(c, o), o = cp(l.stateNode, o, 2), l = Na(l, o, 2), l !== null && (Qi(l, 2), Yi(l));
  }
  function Le(l, o, c) {
    if (l.tag === 3)
      Xp(l, l, c);
    else
      for (; o !== null; ) {
        if (o.tag === 3) {
          Xp(
            o,
            l,
            c
          );
          break;
        } else if (o.tag === 1) {
          var d = o.stateNode;
          if (typeof o.type.getDerivedStateFromError == "function" || typeof d.componentDidCatch == "function" && (zo === null || !zo.has(d))) {
            l = tl(c, l), c = fp(2), d = Na(o, c, 2), d !== null && (Ui(
              c,
              d,
              o,
              l
            ), Qi(d, 2), Yi(d));
            break;
          }
        }
        o = o.return;
      }
  }
  function Og(l, o, c) {
    var d = l.pingCache;
    if (d === null) {
      d = l.pingCache = new vv();
      var v = /* @__PURE__ */ new Set();
      d.set(o, v);
    } else
      v = d.get(o), v === void 0 && (v = /* @__PURE__ */ new Set(), d.set(o, v));
    v.has(c) || (Dp = !0, v.add(c), l = kp.bind(null, l, o, c), o.then(l, l));
  }
  function kp(l, o, c) {
    var d = l.pingCache;
    d !== null && d.delete(o), l.pingedLanes |= l.suspendedLanes & c, l.warmLanes &= ~c, Be === l && (fe & c) === c && (fn === 4 || fn === 3 && (fe & 62914560) === fe && 300 > yn() - Lp ? !(Re & 2) && Ka(l, 0) : Za |= c, nc === fe && (nc = 0)), Yi(l);
  }
  function Vp(l, o) {
    o === 0 && (o = Pi()), l = Ma(l, o), l !== null && (Qi(l, o), Yi(l));
  }
  function jS(l) {
    var o = l.memoizedState, c = 0;
    o !== null && (c = o.retryLane), Vp(l, c);
  }
  function BS(l, o) {
    var c = 0;
    switch (l.tag) {
      case 13:
        var d = l.stateNode, v = l.memoizedState;
        v !== null && (c = v.retryLane);
        break;
      case 19:
        d = l.stateNode;
        break;
      case 22:
        d = l.stateNode._retryCache;
        break;
      default:
        throw Error(a(314));
    }
    d !== null && d.delete(o), Vp(l, c);
  }
  function IS(l, o) {
    return Fi(l, o);
  }
  var Lg = null, Uo = null, nh = !1, uc = !1, zg = !1, Go = 0;
  function Yi(l) {
    l !== Uo && l.next === null && (Uo === null ? Lg = Uo = l : Uo = Uo.next = l), uc = !0, nh || (nh = !0, Mv());
  }
  function ih(l, o) {
    if (!zg && uc) {
      zg = !0;
      do
        for (var c = !1, d = Lg; d !== null; ) {
          if (l !== 0) {
            var v = d.pendingLanes;
            if (v === 0) var S = 0;
            else {
              var A = d.suspendedLanes, M = d.pingedLanes;
              S = (1 << 31 - Bn(42 | l) + 1) - 1, S &= v & ~(A & ~M), S = S & 201326741 ? S & 201326741 | 1 : S ? S | 2 : 0;
            }
            S !== 0 && (c = !0, ah(d, S));
          } else
            S = fe, S = Sl(
              d,
              d === Be ? S : 0,
              d.cancelPendingCommit !== null || d.timeoutHandle !== -1
            ), !(S & 3) || Ki(d, S) || (c = !0, ah(d, S));
          d = d.next;
        }
      while (c);
      zg = !1;
    }
  }
  function Rv() {
    lh();
  }
  function lh() {
    uc = nh = !1;
    var l = 0;
    Go !== 0 && (js() && (l = Go), Go = 0);
    for (var o = yn(), c = null, d = Lg; d !== null; ) {
      var v = d.next, S = qp(d, o);
      S === 0 ? (d.next = null, c === null ? Lg = v : c.next = v, v === null && (Uo = c)) : (c = d, (l !== 0 || S & 3) && (uc = !0)), d = v;
    }
    ih(l);
  }
  function qp(l, o) {
    for (var c = l.suspendedLanes, d = l.pingedLanes, v = l.expirationTimes, S = l.pendingLanes & -62914561; 0 < S; ) {
      var A = 31 - Bn(S), M = 1 << A, G = v[A];
      G === -1 ? (!(M & c) || M & d) && (v[A] = _n(M, o)) : G <= o && (l.expiredLanes |= M), S &= ~M;
    }
    if (o = Be, c = fe, c = Sl(
      l,
      l === o ? c : 0,
      l.cancelPendingCommit !== null || l.timeoutHandle !== -1
    ), d = l.callbackNode, c === 0 || l === o && (Ae === 2 || Ae === 9) || l.cancelPendingCommit !== null)
      return d !== null && d !== null && Kl(d), l.callbackNode = null, l.callbackPriority = 0;
    if (!(c & 3) || Ki(l, c)) {
      if (o = c & -c, o === l.callbackPriority) return o;
      switch (d !== null && Kl(d), In(c)) {
        case 2:
        case 8:
          c = Pl;
          break;
        case 32:
          c = _l;
          break;
        case 268435456:
          c = Fe;
          break;
        default:
          c = _l;
      }
      return d = Av.bind(null, l), c = Fi(c, d), l.callbackPriority = o, l.callbackNode = c, o;
    }
    return d !== null && d !== null && Kl(d), l.callbackPriority = 2, l.callbackNode = null, 2;
  }
  function Av(l, o) {
    if (Fn !== 0 && Fn !== 5)
      return l.callbackNode = null, l.callbackPriority = 0, null;
    var c = l.callbackNode;
    if (Dg() && l.callbackNode !== c)
      return null;
    var d = fe;
    return d = Sl(
      l,
      l === Be ? d : 0,
      l.cancelPendingCommit !== null || l.timeoutHandle !== -1
    ), d === 0 ? null : (oc(l, d, o), qp(l, yn()), l.callbackNode != null && l.callbackNode === c ? Av.bind(null, l) : null);
  }
  function ah(l, o) {
    if (Dg()) return null;
    oc(l, o, !0);
  }
  function Mv() {
    qS(function() {
      Re & 6 ? Fi(
        io,
        Rv
      ) : lh();
    });
  }
  function Xr() {
    return Go === 0 && (Go = El()), Go;
  }
  function Ng(l) {
    return l == null || typeof l == "symbol" || typeof l == "boolean" ? null : typeof l == "function" ? l : cf("" + l);
  }
  function sh(l, o) {
    var c = o.ownerDocument.createElement("input");
    return c.name = o.name, c.value = o.value, l.id && c.setAttribute("form", l.id), o.parentNode.insertBefore(c, o), l = new FormData(l), c.parentNode.removeChild(c), l;
  }
  function wv(l, o, c, d, v) {
    if (o === "submit" && c && c.stateNode === v) {
      var S = Ng(
        (v[se] || null).action
      ), A = d.submitter;
      A && (o = (o = A[se] || null) ? Ng(o.formAction) : A.getAttribute("formAction"), o !== null && (S = o, A = null));
      var M = new yd(
        "action",
        "action",
        null,
        d,
        v
      );
      l.push({
        event: M,
        listeners: [
          {
            instance: null,
            listener: function() {
              if (d.defaultPrevented) {
                if (Go !== 0) {
                  var G = A ? sh(v, A) : new FormData(v);
                  ng(
                    c,
                    {
                      pending: !0,
                      data: G,
                      method: v.method,
                      action: S
                    },
                    null,
                    G
                  );
                }
              } else
                typeof S == "function" && (M.preventDefault(), G = A ? sh(v, A) : new FormData(v), ng(
                  c,
                  {
                    pending: !0,
                    data: G,
                    method: v.method,
                    action: S
                  },
                  S,
                  G
                ));
            },
            currentTarget: v
          }
        ]
      });
    }
  }
  for (var hn = 0; hn < Ou.length; hn++) {
    var oh = Ou[hn], XS = oh.toLowerCase(), ee = oh[0].toUpperCase() + oh.slice(1);
    xl(
      XS,
      "on" + ee
    );
  }
  xl(J_, "onAnimationEnd"), xl(Dy, "onAnimationIteration"), xl($_, "onAnimationStart"), xl("dblclick", "onDoubleClick"), xl("focusin", "onFocus"), xl("focusout", "onBlur"), xl(Oy, "onTransitionRun"), xl(Cd, "onTransitionStart"), xl(tv, "onTransitionCancel"), xl(Ly, "onTransitionEnd"), oo("onMouseEnter", ["mouseout", "mouseover"]), oo("onMouseLeave", ["mouseout", "mouseover"]), oo("onPointerEnter", ["pointerout", "pointerover"]), oo("onPointerLeave", ["pointerout", "pointerover"]), so(
    "onChange",
    "change click focusin focusout input keydown keyup selectionchange".split(" ")
  ), so(
    "onSelect",
    "focusout contextmenu dragend focusin keydown keyup mousedown mouseup selectionchange".split(
      " "
    )
  ), so("onBeforeInput", [
    "compositionend",
    "keypress",
    "textInput",
    "paste"
  ]), so(
    "onCompositionEnd",
    "compositionend focusout keydown keypress keyup mousedown".split(" ")
  ), so(
    "onCompositionStart",
    "compositionstart focusout keydown keypress keyup mousedown".split(" ")
  ), so(
    "onCompositionUpdate",
    "compositionupdate focusout keydown keypress keyup mousedown".split(" ")
  );
  var rh = "abort canplay canplaythrough durationchange emptied encrypted ended error loadeddata loadedmetadata loadstart pause play playing progress ratechange resize seeked seeking stalled suspend timeupdate volumechange waiting".split(
    " "
  ), Ho = new Set(
    "beforetoggle cancel close invalid load scroll scrollend toggle".split(" ").concat(rh)
  );
  function kr(l, o) {
    o = (o & 4) !== 0;
    for (var c = 0; c < l.length; c++) {
      var d = l[c], v = d.event;
      d = d.listeners;
      t: {
        var S = void 0;
        if (o)
          for (var A = d.length - 1; 0 <= A; A--) {
            var M = d[A], G = M.instance, W = M.currentTarget;
            if (M = M.listener, G !== S && v.isPropagationStopped())
              break t;
            S = M, v.currentTarget = W;
            try {
              S(v);
            } catch (ct) {
              qf(ct);
            }
            v.currentTarget = null, S = G;
          }
        else
          for (A = 0; A < d.length; A++) {
            if (M = d[A], G = M.instance, W = M.currentTarget, M = M.listener, G !== S && v.isPropagationStopped())
              break t;
            S = M, v.currentTarget = W;
            try {
              S(v);
            } catch (ct) {
              qf(ct);
            }
            v.currentTarget = null, S = G;
          }
      }
    }
  }
  function Qt(l, o) {
    var c = o[Ai];
    c === void 0 && (c = o[Ai] = /* @__PURE__ */ new Set());
    var d = l + "__bubble";
    c.has(d) || (Ug(o, l, 2, !1), c.add(d));
  }
  function cc(l, o, c) {
    var d = 0;
    o && (d |= 4), Ug(
      c,
      l,
      d,
      o
    );
  }
  var fc = "_reactListening" + Math.random().toString(36).slice(2);
  function Zp(l) {
    if (!l[fc]) {
      l[fc] = !0, af.forEach(function(c) {
        c !== "selectionchange" && (Ho.has(c) || cc(c, !1, l), cc(c, !0, l));
      });
      var o = l.nodeType === 9 ? l : l.ownerDocument;
      o === null || o[fc] || (o[fc] = !0, cc("selectionchange", !1, o));
    }
  }
  function Ug(l, o, c, d) {
    switch (o0(o)) {
      case 2:
        var v = Yv;
        break;
      case 8:
        v = jv;
        break;
      default:
        v = a0;
    }
    c = v.bind(
      null,
      o,
      c,
      l
    ), v = void 0, !dd || o !== "touchstart" && o !== "touchmove" && o !== "wheel" || (v = !0), d ? v !== void 0 ? l.addEventListener(o, c, {
      capture: !0,
      passive: v
    }) : l.addEventListener(o, c, !0) : v !== void 0 ? l.addEventListener(o, c, {
      passive: v
    }) : l.addEventListener(o, c, !1);
  }
  function Ul(l, o, c, d, v) {
    var S = d;
    if (!(o & 1) && !(o & 2) && d !== null)
      t: for (; ; ) {
        if (d === null) return;
        var A = d.tag;
        if (A === 3 || A === 4) {
          var M = d.stateNode.containerInfo;
          if (M === v) break;
          if (A === 4)
            for (A = d.return; A !== null; ) {
              var G = A.tag;
              if ((G === 3 || G === 4) && A.stateNode.containerInfo === v)
                return;
              A = A.return;
            }
          for (; M !== null; ) {
            if (A = Xn(M), A === null) return;
            if (G = A.tag, G === 5 || G === 6 || G === 26 || G === 27) {
              d = S = A;
              continue t;
            }
            M = M.parentNode;
          }
        }
        d = d.return;
      }
    Mu(function() {
      var W = S, ct = hd(c), gt = [];
      t: {
        var $ = zy.get(l);
        if ($ !== void 0) {
          var et = yd, Gt = l;
          switch (l) {
            case "keypress":
              if (kn(c) === 0) break t;
            case "keydown":
            case "keyup":
              et = Jl;
              break;
            case "focusin":
              Gt = "focus", et = gy;
              break;
            case "focusout":
              Gt = "blur", et = gy;
              break;
            case "beforeblur":
            case "afterblur":
              et = gy;
              break;
            case "click":
              if (c.button === 2) break t;
            case "auxclick":
            case "dblclick":
            case "mousedown":
            case "mousemove":
            case "mouseup":
            case "mouseout":
            case "mouseover":
            case "contextmenu":
              et = dy;
              break;
            case "drag":
            case "dragend":
            case "dragenter":
            case "dragexit":
            case "dragleave":
            case "dragover":
            case "dragstart":
            case "drop":
              et = k_;
              break;
            case "touchcancel":
            case "touchend":
            case "touchmove":
            case "touchstart":
              et = py;
              break;
            case J_:
            case Dy:
            case $_:
              et = NS;
              break;
            case Ly:
              et = K_;
              break;
            case "scroll":
            case "scrollend":
              et = I_;
              break;
            case "wheel":
              et = hr;
              break;
            case "copy":
            case "cut":
            case "paste":
              et = df;
              break;
            case "gotpointercapture":
            case "lostpointercapture":
            case "pointercancel":
            case "pointerdown":
            case "pointermove":
            case "pointerout":
            case "pointerover":
            case "pointerup":
              et = gf;
              break;
            case "toggle":
            case "beforetoggle":
              et = P_;
          }
          var Ht = (o & 4) !== 0, Te = !Ht && (l === "scroll" || l === "scrollend"), q = Ht ? $ !== null ? $ + "Capture" : null : $;
          Ht = [];
          for (var V = W, P; V !== null; ) {
            var ht = V;
            if (P = ht.stateNode, ht = ht.tag, ht !== 5 && ht !== 26 && ht !== 27 || P === null || q === null || (ht = rr(V, q), ht != null && Ht.push(
              Hs(V, ht, P)
            )), Te) break;
            V = V.return;
          }
          0 < Ht.length && ($ = new et(
            $,
            Gt,
            null,
            c,
            ct
          ), gt.push({ event: $, listeners: Ht }));
        }
      }
      if (!(o & 7)) {
        t: {
          if ($ = l === "mouseover" || l === "pointerover", et = l === "mouseout" || l === "pointerout", $ && c !== or && (Gt = c.relatedTarget || c.fromElement) && (Xn(Gt) || Gt[an]))
            break t;
          if ((et || $) && ($ = ct.window === ct ? ct : ($ = ct.ownerDocument) ? $.defaultView || $.parentWindow : window, et ? (Gt = c.relatedTarget || c.toElement, et = W, Gt = Gt ? Xn(Gt) : null, Gt !== null && (Te = h(Gt), Ht = Gt.tag, Gt !== Te || Ht !== 5 && Ht !== 27 && Ht !== 6) && (Gt = null)) : (et = null, Gt = W), et !== Gt)) {
            if (Ht = dy, ht = "onMouseLeave", q = "onMouseEnter", V = "mouse", (l === "pointerout" || l === "pointerover") && (Ht = gf, ht = "onPointerLeave", q = "onPointerEnter", V = "pointer"), Te = et == null ? $ : lf(et), P = Gt == null ? $ : lf(Gt), $ = new Ht(
              ht,
              V + "leave",
              et,
              c,
              ct
            ), $.target = Te, $.relatedTarget = P, ht = null, Xn(ct) === W && (Ht = new Ht(
              q,
              V + "enter",
              Gt,
              c,
              ct
            ), Ht.target = P, Ht.relatedTarget = Te, ht = Ht), Te = ht, et && Gt)
              e: {
                for (Ht = et, q = Gt, V = 0, P = Ht; P; P = Yo(P))
                  V++;
                for (P = 0, ht = q; ht; ht = Yo(ht))
                  P++;
                for (; 0 < V - P; )
                  Ht = Yo(Ht), V--;
                for (; 0 < P - V; )
                  q = Yo(q), P--;
                for (; V--; ) {
                  if (Ht === q || q !== null && Ht === q.alternate)
                    break e;
                  Ht = Yo(Ht), q = Yo(q);
                }
                Ht = null;
              }
            else Ht = null;
            et !== null && uh(
              gt,
              $,
              et,
              Ht,
              !1
            ), Gt !== null && Te !== null && uh(
              gt,
              Te,
              Gt,
              Ht,
              !0
            );
          }
        }
        t: {
          if ($ = W ? lf(W) : window, et = $.nodeName && $.nodeName.toLowerCase(), et === "select" || et === "input" && $.type === "file")
            var Mt = Ty;
          else if (Ed($))
            if (xy)
              Mt = Ay;
            else {
              Mt = go;
              var ie = Td;
            }
          else
            et = $.nodeName, !et || et.toLowerCase() !== "input" || $.type !== "checkbox" && $.type !== "radio" ? W && sr(W.elementType) && (Mt = Ty) : Mt = bs;
          if (Mt && (Mt = Mt(l, W))) {
            bd(
              gt,
              Mt,
              c,
              ct
            );
            break t;
          }
          ie && ie(l, $, W), l === "focusout" && W && $.type === "number" && W.memoizedProps.value != null && rf($, "number", $.value);
        }
        switch (ie = W ? lf(W) : window, l) {
          case "focusin":
            (Ed(ie) || ie.contentEditable === "true") && (Ca = ie, ea = W, po = null);
            break;
          case "focusout":
            po = ea = Ca = null;
            break;
          case "mousedown":
            pr = !0;
            break;
          case "contextmenu":
          case "mouseup":
          case "dragend":
            pr = !1, xd(gt, c, ct);
            break;
          case "selectionchange":
            if (yr) break;
          case "keydown":
          case "keyup":
            xd(gt, c, ct);
        }
        var Lt;
        if (mf)
          t: {
            switch (l) {
              case "compositionstart":
                var Bt = "onCompositionStart";
                break t;
              case "compositionend":
                Bt = "onCompositionEnd";
                break t;
              case "compositionupdate":
                Bt = "onCompositionUpdate";
                break t;
            }
            Bt = void 0;
          }
        else
          ho ? pf(l, c) && (Bt = "onCompositionEnd") : l === "keydown" && c.keyCode === 229 && (Bt = "onCompositionStart");
        Bt && (Ta && c.locale !== "ko" && (ho || Bt !== "onCompositionStart" ? Bt === "onCompositionEnd" && ho && (Lt = fy()) : (Ss = ct, wu = "value" in Ss ? Ss.value : Ss.textContent, ho = !0)), ie = hc(W, Bt), 0 < ie.length && (Bt = new my(
          Bt,
          l,
          null,
          c,
          ct
        ), gt.push({ event: Bt, listeners: ie }), Lt ? Bt.data = Lt : (Lt = fo(c), Lt !== null && (Bt.data = Lt)))), (Lt = vy ? Ey(l, c) : dr(l, c)) && (Bt = hc(W, "onBeforeInput"), 0 < Bt.length && (ie = new my(
          "onBeforeInput",
          "beforeinput",
          null,
          c,
          ct
        ), gt.push({
          event: ie,
          listeners: Bt
        }), ie.data = Lt)), wv(
          gt,
          l,
          W,
          c,
          ct
        );
      }
      kr(gt, o);
    });
  }
  function Hs(l, o, c) {
    return {
      instance: l,
      listener: o,
      currentTarget: c
    };
  }
  function hc(l, o) {
    for (var c = o + "Capture", d = []; l !== null; ) {
      var v = l, S = v.stateNode;
      if (v = v.tag, v !== 5 && v !== 26 && v !== 27 || S === null || (v = rr(l, c), v != null && d.unshift(
        Hs(l, v, S)
      ), v = rr(l, o), v != null && d.push(
        Hs(l, v, S)
      )), l.tag === 3) return d;
      l = l.return;
    }
    return [];
  }
  function Yo(l) {
    if (l === null) return null;
    do
      l = l.return;
    while (l && l.tag !== 5 && l.tag !== 27);
    return l || null;
  }
  function uh(l, o, c, d, v) {
    for (var S = o._reactName, A = []; c !== null && c !== d; ) {
      var M = c, G = M.alternate, W = M.stateNode;
      if (M = M.tag, G !== null && G === d) break;
      M !== 5 && M !== 26 && M !== 27 || W === null || (G = W, v ? (W = rr(c, S), W != null && A.unshift(
        Hs(c, W, G)
      )) : v || (W = rr(c, S), W != null && A.push(
        Hs(c, W, G)
      ))), c = c.return;
    }
    A.length !== 0 && l.push({ event: o, listeners: A });
  }
  var sl = /\r\n?/g, Fp = /\u0000|\uFFFD/g;
  function Dv(l) {
    return (typeof l == "string" ? l : "" + l).replace(sl, `
`).replace(Fp, "");
  }
  function Kp(l, o) {
    return o = Dv(o), Dv(l) === o;
  }
  function Gg() {
  }
  function Ft(l, o, c, d, v, S) {
    switch (c) {
      case "children":
        typeof d == "string" ? o === "body" || o === "textarea" && d === "" || Cu(l, d) : (typeof d == "number" || typeof d == "bigint") && o !== "body" && Cu(l, "" + d);
        break;
      case "className":
        sf(l, "class", d);
        break;
      case "tabIndex":
        sf(l, "tabindex", d);
        break;
      case "dir":
      case "role":
      case "viewBox":
      case "width":
      case "height":
        sf(l, c, d);
        break;
      case "style":
        uf(l, d, S);
        break;
      case "data":
        if (o !== "object") {
          sf(l, "data", d);
          break;
        }
      case "src":
      case "href":
        if (d === "" && (o !== "a" || c !== "href")) {
          l.removeAttribute(c);
          break;
        }
        if (d == null || typeof d == "function" || typeof d == "symbol" || typeof d == "boolean") {
          l.removeAttribute(c);
          break;
        }
        d = cf("" + d), l.setAttribute(c, d);
        break;
      case "action":
      case "formAction":
        if (typeof d == "function") {
          l.setAttribute(
            c,
            "javascript:throw new Error('A React form was unexpectedly submitted. If you called form.submit() manually, consider using form.requestSubmit() instead. If you\\'re trying to use event.stopPropagation() in a submit event handler, consider also calling event.preventDefault().')"
          );
          break;
        } else
          typeof S == "function" && (c === "formAction" ? (o !== "input" && Ft(l, o, "name", v.name, v, null), Ft(
            l,
            o,
            "formEncType",
            v.formEncType,
            v,
            null
          ), Ft(
            l,
            o,
            "formMethod",
            v.formMethod,
            v,
            null
          ), Ft(
            l,
            o,
            "formTarget",
            v.formTarget,
            v,
            null
          )) : (Ft(l, o, "encType", v.encType, v, null), Ft(l, o, "method", v.method, v, null), Ft(l, o, "target", v.target, v, null)));
        if (d == null || typeof d == "symbol" || typeof d == "boolean") {
          l.removeAttribute(c);
          break;
        }
        d = cf("" + d), l.setAttribute(c, d);
        break;
      case "onClick":
        d != null && (l.onclick = Gg);
        break;
      case "onScroll":
        d != null && Qt("scroll", l);
        break;
      case "onScrollEnd":
        d != null && Qt("scrollend", l);
        break;
      case "dangerouslySetInnerHTML":
        if (d != null) {
          if (typeof d != "object" || !("__html" in d))
            throw Error(a(61));
          if (c = d.__html, c != null) {
            if (v.children != null) throw Error(a(60));
            l.innerHTML = c;
          }
        }
        break;
      case "multiple":
        l.multiple = d && typeof d != "function" && typeof d != "symbol";
        break;
      case "muted":
        l.muted = d && typeof d != "function" && typeof d != "symbol";
        break;
      case "suppressContentEditableWarning":
      case "suppressHydrationWarning":
      case "defaultValue":
      case "defaultChecked":
      case "innerHTML":
      case "ref":
        break;
      case "autoFocus":
        break;
      case "xlinkHref":
        if (d == null || typeof d == "function" || typeof d == "boolean" || typeof d == "symbol") {
          l.removeAttribute("xlink:href");
          break;
        }
        c = cf("" + d), l.setAttributeNS(
          "http://www.w3.org/1999/xlink",
          "xlink:href",
          c
        );
        break;
      case "contentEditable":
      case "spellCheck":
      case "draggable":
      case "value":
      case "autoReverse":
      case "externalResourcesRequired":
      case "focusable":
      case "preserveAlpha":
        d != null && typeof d != "function" && typeof d != "symbol" ? l.setAttribute(c, "" + d) : l.removeAttribute(c);
        break;
      case "inert":
      case "allowFullScreen":
      case "async":
      case "autoPlay":
      case "controls":
      case "default":
      case "defer":
      case "disabled":
      case "disablePictureInPicture":
      case "disableRemotePlayback":
      case "formNoValidate":
      case "hidden":
      case "loop":
      case "noModule":
      case "noValidate":
      case "open":
      case "playsInline":
      case "readOnly":
      case "required":
      case "reversed":
      case "scoped":
      case "seamless":
      case "itemScope":
        d && typeof d != "function" && typeof d != "symbol" ? l.setAttribute(c, "") : l.removeAttribute(c);
        break;
      case "capture":
      case "download":
        d === !0 ? l.setAttribute(c, "") : d !== !1 && d != null && typeof d != "function" && typeof d != "symbol" ? l.setAttribute(c, d) : l.removeAttribute(c);
        break;
      case "cols":
      case "rows":
      case "size":
      case "span":
        d != null && typeof d != "function" && typeof d != "symbol" && !isNaN(d) && 1 <= d ? l.setAttribute(c, d) : l.removeAttribute(c);
        break;
      case "rowSpan":
      case "start":
        d == null || typeof d == "function" || typeof d == "symbol" || isNaN(d) ? l.removeAttribute(c) : l.setAttribute(c, d);
        break;
      case "popover":
        Qt("beforetoggle", l), Qt("toggle", l), _s(l, "popover", d);
        break;
      case "xlinkActuate":
        Ea(
          l,
          "http://www.w3.org/1999/xlink",
          "xlink:actuate",
          d
        );
        break;
      case "xlinkArcrole":
        Ea(
          l,
          "http://www.w3.org/1999/xlink",
          "xlink:arcrole",
          d
        );
        break;
      case "xlinkRole":
        Ea(
          l,
          "http://www.w3.org/1999/xlink",
          "xlink:role",
          d
        );
        break;
      case "xlinkShow":
        Ea(
          l,
          "http://www.w3.org/1999/xlink",
          "xlink:show",
          d
        );
        break;
      case "xlinkTitle":
        Ea(
          l,
          "http://www.w3.org/1999/xlink",
          "xlink:title",
          d
        );
        break;
      case "xlinkType":
        Ea(
          l,
          "http://www.w3.org/1999/xlink",
          "xlink:type",
          d
        );
        break;
      case "xmlBase":
        Ea(
          l,
          "http://www.w3.org/XML/1998/namespace",
          "xml:base",
          d
        );
        break;
      case "xmlLang":
        Ea(
          l,
          "http://www.w3.org/XML/1998/namespace",
          "xml:lang",
          d
        );
        break;
      case "xmlSpace":
        Ea(
          l,
          "http://www.w3.org/XML/1998/namespace",
          "xml:space",
          d
        );
        break;
      case "is":
        _s(l, "is", d);
        break;
      case "innerText":
      case "textContent":
        break;
      default:
        (!(2 < c.length) || c[0] !== "o" && c[0] !== "O" || c[1] !== "n" && c[1] !== "N") && (c = OS.get(c) || c, _s(l, c, d));
    }
  }
  function lt(l, o, c, d, v, S) {
    switch (c) {
      case "style":
        uf(l, d, S);
        break;
      case "dangerouslySetInnerHTML":
        if (d != null) {
          if (typeof d != "object" || !("__html" in d))
            throw Error(a(61));
          if (c = d.__html, c != null) {
            if (v.children != null) throw Error(a(60));
            l.innerHTML = c;
          }
        }
        break;
      case "children":
        typeof d == "string" ? Cu(l, d) : (typeof d == "number" || typeof d == "bigint") && Cu(l, "" + d);
        break;
      case "onScroll":
        d != null && Qt("scroll", l);
        break;
      case "onScrollEnd":
        d != null && Qt("scrollend", l);
        break;
      case "onClick":
        d != null && (l.onclick = Gg);
        break;
      case "suppressContentEditableWarning":
      case "suppressHydrationWarning":
      case "innerHTML":
      case "ref":
        break;
      case "innerText":
      case "textContent":
        break;
      default:
        if (!Ji.hasOwnProperty(c))
          t: {
            if (c[0] === "o" && c[1] === "n" && (v = c.endsWith("Capture"), o = c.slice(2, v ? c.length - 7 : void 0), S = l[se] || null, S = S != null ? S[c] : null, typeof S == "function" && l.removeEventListener(o, S, v), typeof d == "function")) {
              typeof S != "function" && S !== null && (c in l ? l[c] = null : l.hasAttribute(c) && l.removeAttribute(c)), l.addEventListener(o, d, v);
              break t;
            }
            c in l ? l[c] = d : d === !0 ? l.setAttribute(c, "") : _s(l, c, d);
          }
    }
  }
  function Vt(l, o, c) {
    switch (o) {
      case "div":
      case "span":
      case "svg":
      case "path":
      case "a":
      case "g":
      case "p":
      case "li":
        break;
      case "img":
        Qt("error", l), Qt("load", l);
        var d = !1, v = !1, S;
        for (S in c)
          if (c.hasOwnProperty(S)) {
            var A = c[S];
            if (A != null)
              switch (S) {
                case "src":
                  d = !0;
                  break;
                case "srcSet":
                  v = !0;
                  break;
                case "children":
                case "dangerouslySetInnerHTML":
                  throw Error(a(137, o));
                default:
                  Ft(l, o, S, A, c, null);
              }
          }
        v && Ft(l, o, "srcSet", c.srcSet, c, null), d && Ft(l, o, "src", c.src, c, null);
        return;
      case "input":
        Qt("invalid", l);
        var M = S = A = v = null, G = null, W = null;
        for (d in c)
          if (c.hasOwnProperty(d)) {
            var ct = c[d];
            if (ct != null)
              switch (d) {
                case "name":
                  v = ct;
                  break;
                case "type":
                  A = ct;
                  break;
                case "checked":
                  G = ct;
                  break;
                case "defaultChecked":
                  W = ct;
                  break;
                case "value":
                  S = ct;
                  break;
                case "defaultValue":
                  M = ct;
                  break;
                case "children":
                case "dangerouslySetInnerHTML":
                  if (ct != null)
                    throw Error(a(137, o));
                  break;
                default:
                  Ft(l, o, d, ct, c, null);
              }
          }
        cd(
          l,
          S,
          M,
          G,
          W,
          A,
          v,
          !1
        ), uo(l);
        return;
      case "select":
        Qt("invalid", l), d = A = S = null;
        for (v in c)
          if (c.hasOwnProperty(v) && (M = c[v], M != null))
            switch (v) {
              case "value":
                S = M;
                break;
              case "defaultValue":
                A = M;
                break;
              case "multiple":
                d = M;
              default:
                Ft(l, o, v, M, c, null);
            }
        o = S, c = A, l.multiple = !!d, o != null ? ar(l, !!d, o, !1) : c != null && ar(l, !!d, c, !0);
        return;
      case "textarea":
        Qt("invalid", l), S = v = d = null;
        for (A in c)
          if (c.hasOwnProperty(A) && (M = c[A], M != null))
            switch (A) {
              case "value":
                d = M;
                break;
              case "defaultValue":
                v = M;
                break;
              case "children":
                S = M;
                break;
              case "dangerouslySetInnerHTML":
                if (M != null) throw Error(a(91));
                break;
              default:
                Ft(l, o, A, M, c, null);
            }
        uy(l, d, v, S), uo(l);
        return;
      case "option":
        for (G in c)
          if (c.hasOwnProperty(G) && (d = c[G], d != null))
            switch (G) {
              case "selected":
                l.selected = d && typeof d != "function" && typeof d != "symbol";
                break;
              default:
                Ft(l, o, G, d, c, null);
            }
        return;
      case "dialog":
        Qt("beforetoggle", l), Qt("toggle", l), Qt("cancel", l), Qt("close", l);
        break;
      case "iframe":
      case "object":
        Qt("load", l);
        break;
      case "video":
      case "audio":
        for (d = 0; d < rh.length; d++)
          Qt(rh[d], l);
        break;
      case "image":
        Qt("error", l), Qt("load", l);
        break;
      case "details":
        Qt("toggle", l);
        break;
      case "embed":
      case "source":
      case "link":
        Qt("error", l), Qt("load", l);
      case "area":
      case "base":
      case "br":
      case "col":
      case "hr":
      case "keygen":
      case "meta":
      case "param":
      case "track":
      case "wbr":
      case "menuitem":
        for (W in c)
          if (c.hasOwnProperty(W) && (d = c[W], d != null))
            switch (W) {
              case "children":
              case "dangerouslySetInnerHTML":
                throw Error(a(137, o));
              default:
                Ft(l, o, W, d, c, null);
            }
        return;
      default:
        if (sr(o)) {
          for (ct in c)
            c.hasOwnProperty(ct) && (d = c[ct], d !== void 0 && lt(
              l,
              o,
              ct,
              d,
              c,
              void 0
            ));
          return;
        }
    }
    for (M in c)
      c.hasOwnProperty(M) && (d = c[M], d != null && Ft(l, o, M, d, c, null));
  }
  function kS(l, o, c, d) {
    switch (o) {
      case "div":
      case "span":
      case "svg":
      case "path":
      case "a":
      case "g":
      case "p":
      case "li":
        break;
      case "input":
        var v = null, S = null, A = null, M = null, G = null, W = null, ct = null;
        for (et in c) {
          var gt = c[et];
          if (c.hasOwnProperty(et) && gt != null)
            switch (et) {
              case "checked":
                break;
              case "value":
                break;
              case "defaultValue":
                G = gt;
              default:
                d.hasOwnProperty(et) || Ft(l, o, et, null, d, gt);
            }
        }
        for (var $ in d) {
          var et = d[$];
          if (gt = c[$], d.hasOwnProperty($) && (et != null || gt != null))
            switch ($) {
              case "type":
                S = et;
                break;
              case "name":
                v = et;
                break;
              case "checked":
                W = et;
                break;
              case "defaultChecked":
                ct = et;
                break;
              case "value":
                A = et;
                break;
              case "defaultValue":
                M = et;
                break;
              case "children":
              case "dangerouslySetInnerHTML":
                if (et != null)
                  throw Error(a(137, o));
                break;
              default:
                et !== gt && Ft(
                  l,
                  o,
                  $,
                  et,
                  d,
                  gt
                );
            }
        }
        ud(
          l,
          A,
          M,
          G,
          W,
          ct,
          S,
          v
        );
        return;
      case "select":
        et = A = M = $ = null;
        for (S in c)
          if (G = c[S], c.hasOwnProperty(S) && G != null)
            switch (S) {
              case "value":
                break;
              case "multiple":
                et = G;
              default:
                d.hasOwnProperty(S) || Ft(
                  l,
                  o,
                  S,
                  null,
                  d,
                  G
                );
            }
        for (v in d)
          if (S = d[v], G = c[v], d.hasOwnProperty(v) && (S != null || G != null))
            switch (v) {
              case "value":
                $ = S;
                break;
              case "defaultValue":
                M = S;
                break;
              case "multiple":
                A = S;
              default:
                S !== G && Ft(
                  l,
                  o,
                  v,
                  S,
                  d,
                  G
                );
            }
        o = M, c = A, d = et, $ != null ? ar(l, !!c, $, !1) : !!d != !!c && (o != null ? ar(l, !!c, o, !0) : ar(l, !!c, c ? [] : "", !1));
        return;
      case "textarea":
        et = $ = null;
        for (M in c)
          if (v = c[M], c.hasOwnProperty(M) && v != null && !d.hasOwnProperty(M))
            switch (M) {
              case "value":
                break;
              case "children":
                break;
              default:
                Ft(l, o, M, null, d, v);
            }
        for (A in d)
          if (v = d[A], S = c[A], d.hasOwnProperty(A) && (v != null || S != null))
            switch (A) {
              case "value":
                $ = v;
                break;
              case "defaultValue":
                et = v;
                break;
              case "children":
                break;
              case "dangerouslySetInnerHTML":
                if (v != null) throw Error(a(91));
                break;
              default:
                v !== S && Ft(l, o, A, v, d, S);
            }
        ry(l, $, et);
        return;
      case "option":
        for (var Gt in c)
          if ($ = c[Gt], c.hasOwnProperty(Gt) && $ != null && !d.hasOwnProperty(Gt))
            switch (Gt) {
              case "selected":
                l.selected = !1;
                break;
              default:
                Ft(
                  l,
                  o,
                  Gt,
                  null,
                  d,
                  $
                );
            }
        for (G in d)
          if ($ = d[G], et = c[G], d.hasOwnProperty(G) && $ !== et && ($ != null || et != null))
            switch (G) {
              case "selected":
                l.selected = $ && typeof $ != "function" && typeof $ != "symbol";
                break;
              default:
                Ft(
                  l,
                  o,
                  G,
                  $,
                  d,
                  et
                );
            }
        return;
      case "img":
      case "link":
      case "area":
      case "base":
      case "br":
      case "col":
      case "embed":
      case "hr":
      case "keygen":
      case "meta":
      case "param":
      case "source":
      case "track":
      case "wbr":
      case "menuitem":
        for (var Ht in c)
          $ = c[Ht], c.hasOwnProperty(Ht) && $ != null && !d.hasOwnProperty(Ht) && Ft(l, o, Ht, null, d, $);
        for (W in d)
          if ($ = d[W], et = c[W], d.hasOwnProperty(W) && $ !== et && ($ != null || et != null))
            switch (W) {
              case "children":
              case "dangerouslySetInnerHTML":
                if ($ != null)
                  throw Error(a(137, o));
                break;
              default:
                Ft(
                  l,
                  o,
                  W,
                  $,
                  d,
                  et
                );
            }
        return;
      default:
        if (sr(o)) {
          for (var Te in c)
            $ = c[Te], c.hasOwnProperty(Te) && $ !== void 0 && !d.hasOwnProperty(Te) && lt(
              l,
              o,
              Te,
              void 0,
              d,
              $
            );
          for (ct in d)
            $ = d[ct], et = c[ct], !d.hasOwnProperty(ct) || $ === et || $ === void 0 && et === void 0 || lt(
              l,
              o,
              ct,
              $,
              d,
              et
            );
          return;
        }
    }
    for (var q in c)
      $ = c[q], c.hasOwnProperty(q) && $ != null && !d.hasOwnProperty(q) && Ft(l, o, q, null, d, $);
    for (gt in d)
      $ = d[gt], et = c[gt], !d.hasOwnProperty(gt) || $ === et || $ == null && et == null || Ft(l, o, gt, $, d, et);
  }
  var ch = null, fh = null;
  function Gl(l) {
    return l.nodeType === 9 ? l : l.ownerDocument;
  }
  function Ys(l) {
    switch (l) {
      case "http://www.w3.org/2000/svg":
        return 1;
      case "http://www.w3.org/1998/Math/MathML":
        return 2;
      default:
        return 0;
    }
  }
  function dc(l, o) {
    if (l === 0)
      switch (o) {
        case "svg":
          return 1;
        case "math":
          return 2;
        default:
          return 0;
      }
    return l === 1 && o === "foreignObject" ? 0 : l;
  }
  function Pa(l, o) {
    return l === "textarea" || l === "noscript" || typeof o.children == "string" || typeof o.children == "number" || typeof o.children == "bigint" || typeof o.dangerouslySetInnerHTML == "object" && o.dangerouslySetInnerHTML !== null && o.dangerouslySetInnerHTML.__html != null;
  }
  var gc = null;
  function js() {
    var l = window.event;
    return l && l.type === "popstate" ? l === gc ? !1 : (gc = l, !0) : (gc = null, !1);
  }
  var Hg = typeof setTimeout == "function" ? setTimeout : void 0, VS = typeof clearTimeout == "function" ? clearTimeout : void 0, Ov = typeof Promise == "function" ? Promise : void 0, qS = typeof queueMicrotask == "function" ? queueMicrotask : typeof Ov < "u" ? function(l) {
    return Ov.resolve(null).then(l).catch(Qa);
  } : Hg;
  function Qa(l) {
    setTimeout(function() {
      throw l;
    });
  }
  function jo(l) {
    return l === "head";
  }
  function Yg(l, o) {
    var c = o, d = 0, v = 0;
    do {
      var S = c.nextSibling;
      if (l.removeChild(c), S && S.nodeType === 8)
        if (c = S.data, c === "/$") {
          if (0 < d && 8 > d) {
            c = d;
            var A = l.ownerDocument;
            if (c & 1 && ji(A.documentElement), c & 2 && ji(A.body), c & 4)
              for (c = A.head, ji(c), A = c.firstChild; A; ) {
                var M = A.nextSibling, G = A.nodeName;
                A[wt] || G === "SCRIPT" || G === "STYLE" || G === "LINK" && A.rel.toLowerCase() === "stylesheet" || c.removeChild(A), A = M;
              }
          }
          if (v === 0) {
            l.removeChild(S), Ja(o);
            return;
          }
          v--;
        } else
          c === "$" || c === "$?" || c === "$!" ? v++ : d = c.charCodeAt(0) - 48;
      else d = 0;
      c = S;
    } while (c);
    Ja(o);
  }
  function hh(l) {
    var o = l.firstChild;
    for (o && o.nodeType === 10 && (o = o.nextSibling); o; ) {
      var c = o;
      switch (o = o.nextSibling, c.nodeName) {
        case "HTML":
        case "HEAD":
        case "BODY":
          hh(c), nf(c);
          continue;
        case "SCRIPT":
        case "STYLE":
          continue;
        case "LINK":
          if (c.rel.toLowerCase() === "stylesheet") continue;
      }
      l.removeChild(c);
    }
  }
  function mc(l, o, c, d) {
    for (; l.nodeType === 1; ) {
      var v = c;
      if (l.nodeName.toLowerCase() !== o.toLowerCase()) {
        if (!d && (l.nodeName !== "INPUT" || l.type !== "hidden"))
          break;
      } else if (d) {
        if (!l[wt])
          switch (o) {
            case "meta":
              if (!l.hasAttribute("itemprop")) break;
              return l;
            case "link":
              if (S = l.getAttribute("rel"), S === "stylesheet" && l.hasAttribute("data-precedence"))
                break;
              if (S !== v.rel || l.getAttribute("href") !== (v.href == null || v.href === "" ? null : v.href) || l.getAttribute("crossorigin") !== (v.crossOrigin == null ? null : v.crossOrigin) || l.getAttribute("title") !== (v.title == null ? null : v.title))
                break;
              return l;
            case "style":
              if (l.hasAttribute("data-precedence")) break;
              return l;
            case "script":
              if (S = l.getAttribute("src"), (S !== (v.src == null ? null : v.src) || l.getAttribute("type") !== (v.type == null ? null : v.type) || l.getAttribute("crossorigin") !== (v.crossOrigin == null ? null : v.crossOrigin)) && S && l.hasAttribute("async") && !l.hasAttribute("itemprop"))
                break;
              return l;
            default:
              return l;
          }
      } else if (o === "input" && l.type === "hidden") {
        var S = v.name == null ? null : "" + v.name;
        if (v.type === "hidden" && l.getAttribute("name") === S)
          return l;
      } else return l;
      if (l = fa(l.nextSibling), l === null) break;
    }
    return null;
  }
  function ZS(l, o, c) {
    if (o === "") return null;
    for (; l.nodeType !== 3; )
      if ((l.nodeType !== 1 || l.nodeName !== "INPUT" || l.type !== "hidden") && !c || (l = fa(l.nextSibling), l === null)) return null;
    return l;
  }
  function dh(l) {
    return l.data === "$!" || l.data === "$?" && l.ownerDocument.readyState === "complete";
  }
  function FS(l, o) {
    var c = l.ownerDocument;
    if (l.data !== "$?" || c.readyState === "complete")
      o();
    else {
      var d = function() {
        o(), c.removeEventListener("DOMContentLoaded", d);
      };
      c.addEventListener("DOMContentLoaded", d), l._reactRetry = d;
    }
  }
  function fa(l) {
    for (; l != null; l = l.nextSibling) {
      var o = l.nodeType;
      if (o === 1 || o === 3) break;
      if (o === 8) {
        if (o = l.data, o === "$" || o === "$!" || o === "$?" || o === "F!" || o === "F")
          break;
        if (o === "/$") return null;
      }
    }
    return l;
  }
  var Bo = null;
  function Pn(l) {
    l = l.previousSibling;
    for (var o = 0; l; ) {
      if (l.nodeType === 8) {
        var c = l.data;
        if (c === "$" || c === "$!" || c === "$?") {
          if (o === 0) return l;
          o--;
        } else c === "/$" && o++;
      }
      l = l.previousSibling;
    }
    return null;
  }
  function Ct(l, o, c) {
    switch (o = Gl(c), l) {
      case "html":
        if (l = o.documentElement, !l) throw Error(a(452));
        return l;
      case "head":
        if (l = o.head, !l) throw Error(a(453));
        return l;
      case "body":
        if (l = o.body, !l) throw Error(a(454));
        return l;
      default:
        throw Error(a(451));
    }
  }
  function ji(l) {
    for (var o = l.attributes; o.length; )
      l.removeAttributeNode(o[0]);
    nf(l);
  }
  var dn = /* @__PURE__ */ new Map(), oi = /* @__PURE__ */ new Set();
  function jg(l) {
    return typeof l.getRootNode == "function" ? l.getRootNode() : l.nodeType === 9 ? l : l.ownerDocument;
  }
  var Bs = K.d;
  K.d = {
    f: Bg,
    r: Ig,
    D: Is,
    C: Xg,
    L: Io,
    m: ri,
    X: Xo,
    S: Bi,
    M: Pp
  };
  function Bg() {
    var l = Bs.f(), o = jr();
    return l || o;
  }
  function Ig(l) {
    var o = er(l);
    o !== null && o.tag === 5 && o.type === "form" ? Fu(o) : Bs.r(l);
  }
  var Qn = typeof document > "u" ? null : document;
  function ha(l, o, c) {
    var d = Qn;
    if (d && typeof o == "string" && o) {
      var v = bl(o);
      v = 'link[rel="' + l + '"][href="' + v + '"]', typeof c == "string" && (v += '[crossorigin="' + c + '"]'), oi.has(v) || (oi.add(v), l = { rel: l, crossOrigin: c, href: o }, d.querySelector(v) === null && (o = d.createElement("link"), Vt(o, "link", l), Tn(o), d.head.appendChild(o)));
    }
  }
  function Is(l) {
    Bs.D(l), ha("dns-prefetch", l, null);
  }
  function Xg(l, o) {
    Bs.C(l, o), ha("preconnect", l, o);
  }
  function Io(l, o, c) {
    Bs.L(l, o, c);
    var d = Qn;
    if (d && l && o) {
      var v = 'link[rel="preload"][as="' + bl(o) + '"]';
      o === "image" && c && c.imageSrcSet ? (v += '[imagesrcset="' + bl(
        c.imageSrcSet
      ) + '"]', typeof c.imageSizes == "string" && (v += '[imagesizes="' + bl(
        c.imageSizes
      ) + '"]')) : v += '[href="' + bl(l) + '"]';
      var S = v;
      switch (o) {
        case "style":
          S = yc(l);
          break;
        case "script":
          S = Hl(l);
      }
      dn.has(S) || (l = E(
        {
          rel: "preload",
          href: o === "image" && c && c.imageSrcSet ? void 0 : l,
          as: o
        },
        c
      ), dn.set(S, l), d.querySelector(v) !== null || o === "style" && d.querySelector(pc(S)) || o === "script" && d.querySelector(Vr(S)) || (o = d.createElement("link"), Vt(o, "link", l), Tn(o), d.head.appendChild(o)));
    }
  }
  function ri(l, o) {
    Bs.m(l, o);
    var c = Qn;
    if (c && l) {
      var d = o && typeof o.as == "string" ? o.as : "script", v = 'link[rel="modulepreload"][as="' + bl(d) + '"][href="' + bl(l) + '"]', S = v;
      switch (d) {
        case "audioworklet":
        case "paintworklet":
        case "serviceworker":
        case "sharedworker":
        case "worker":
        case "script":
          S = Hl(l);
      }
      if (!dn.has(S) && (l = E({ rel: "modulepreload", href: l }, o), dn.set(S, l), c.querySelector(v) === null)) {
        switch (d) {
          case "audioworklet":
          case "paintworklet":
          case "serviceworker":
          case "sharedworker":
          case "worker":
          case "script":
            if (c.querySelector(Vr(S)))
              return;
        }
        d = c.createElement("link"), Vt(d, "link", l), Tn(d), c.head.appendChild(d);
      }
    }
  }
  function Bi(l, o, c) {
    Bs.S(l, o, c);
    var d = Qn;
    if (d && l) {
      var v = ps(d).hoistableStyles, S = yc(l);
      o = o || "default";
      var A = v.get(S);
      if (!A) {
        var M = { loading: 0, preload: null };
        if (A = d.querySelector(
          pc(S)
        ))
          M.loading = 5;
        else {
          l = E(
            { rel: "stylesheet", href: l, "data-precedence": o },
            c
          ), (c = dn.get(S)) && Vg(l, c);
          var G = A = d.createElement("link");
          Tn(G), Vt(G, "link", l), G._p = new Promise(function(W, ct) {
            G.onload = W, G.onerror = ct;
          }), G.addEventListener("load", function() {
            M.loading |= 1;
          }), G.addEventListener("error", function() {
            M.loading |= 2;
          }), M.loading |= 4, kg(A, o, d);
        }
        A = {
          type: "stylesheet",
          instance: A,
          count: 1,
          state: M
        }, v.set(S, A);
      }
    }
  }
  function Xo(l, o) {
    Bs.X(l, o);
    var c = Qn;
    if (c && l) {
      var d = ps(c).hoistableScripts, v = Hl(l), S = d.get(v);
      S || (S = c.querySelector(Vr(v)), S || (l = E({ src: l, async: !0 }, o), (o = dn.get(v)) && qg(l, o), S = c.createElement("script"), Tn(S), Vt(S, "link", l), c.head.appendChild(S)), S = {
        type: "script",
        instance: S,
        count: 1,
        state: null
      }, d.set(v, S));
    }
  }
  function Pp(l, o) {
    Bs.M(l, o);
    var c = Qn;
    if (c && l) {
      var d = ps(c).hoistableScripts, v = Hl(l), S = d.get(v);
      S || (S = c.querySelector(Vr(v)), S || (l = E({ src: l, async: !0, type: "module" }, o), (o = dn.get(v)) && qg(l, o), S = c.createElement("script"), Tn(S), Vt(S, "link", l), c.head.appendChild(S)), S = {
        type: "script",
        instance: S,
        count: 1,
        state: null
      }, d.set(v, S));
    }
  }
  function Lv(l, o, c, d) {
    var v = (v = Xt.current) ? jg(v) : null;
    if (!v) throw Error(a(446));
    switch (l) {
      case "meta":
      case "title":
        return null;
      case "style":
        return typeof c.precedence == "string" && typeof c.href == "string" ? (o = yc(c.href), c = ps(
          v
        ).hoistableStyles, d = c.get(o), d || (d = {
          type: "style",
          instance: null,
          count: 0,
          state: null
        }, c.set(o, d)), d) : { type: "void", instance: null, count: 0, state: null };
      case "link":
        if (c.rel === "stylesheet" && typeof c.href == "string" && typeof c.precedence == "string") {
          l = yc(c.href);
          var S = ps(
            v
          ).hoistableStyles, A = S.get(l);
          if (A || (v = v.ownerDocument || v, A = {
            type: "stylesheet",
            instance: null,
            count: 0,
            state: { loading: 0, preload: null }
          }, S.set(l, A), (S = v.querySelector(
            pc(l)
          )) && !S._p && (A.instance = S, A.state.loading = 5), dn.has(l) || (c = {
            rel: "preload",
            as: "style",
            href: c.href,
            crossOrigin: c.crossOrigin,
            integrity: c.integrity,
            media: c.media,
            hrefLang: c.hrefLang,
            referrerPolicy: c.referrerPolicy
          }, dn.set(l, c), S || zv(
            v,
            l,
            c,
            A.state
          ))), o && d === null)
            throw Error(a(528, ""));
          return A;
        }
        if (o && d !== null)
          throw Error(a(529, ""));
        return null;
      case "script":
        return o = c.async, c = c.src, typeof c == "string" && o && typeof o != "function" && typeof o != "symbol" ? (o = Hl(c), c = ps(
          v
        ).hoistableScripts, d = c.get(o), d || (d = {
          type: "script",
          instance: null,
          count: 0,
          state: null
        }, c.set(o, d)), d) : { type: "void", instance: null, count: 0, state: null };
      default:
        throw Error(a(444, l));
    }
  }
  function yc(l) {
    return 'href="' + bl(l) + '"';
  }
  function pc(l) {
    return 'link[rel="stylesheet"][' + l + "]";
  }
  function _c(l) {
    return E({}, l, {
      "data-precedence": l.precedence,
      precedence: null
    });
  }
  function zv(l, o, c, d) {
    l.querySelector('link[rel="preload"][as="style"][' + o + "]") ? d.loading = 1 : (o = l.createElement("link"), d.preload = o, o.addEventListener("load", function() {
      return d.loading |= 1;
    }), o.addEventListener("error", function() {
      return d.loading |= 2;
    }), Vt(o, "link", c), Tn(o), l.head.appendChild(o));
  }
  function Hl(l) {
    return '[src="' + bl(l) + '"]';
  }
  function Vr(l) {
    return "script[async]" + l;
  }
  function Nv(l, o, c) {
    if (o.count++, o.instance === null)
      switch (o.type) {
        case "style":
          var d = l.querySelector(
            'style[data-href~="' + bl(c.href) + '"]'
          );
          if (d)
            return o.instance = d, Tn(d), d;
          var v = E({}, c, {
            "data-href": c.href,
            "data-precedence": c.precedence,
            href: null,
            precedence: null
          });
          return d = (l.ownerDocument || l).createElement(
            "style"
          ), Tn(d), Vt(d, "style", v), kg(d, c.precedence, l), o.instance = d;
        case "stylesheet":
          v = yc(c.href);
          var S = l.querySelector(
            pc(v)
          );
          if (S)
            return o.state.loading |= 4, o.instance = S, Tn(S), S;
          d = _c(c), (v = dn.get(v)) && Vg(d, v), S = (l.ownerDocument || l).createElement("link"), Tn(S);
          var A = S;
          return A._p = new Promise(function(M, G) {
            A.onload = M, A.onerror = G;
          }), Vt(S, "link", d), o.state.loading |= 4, kg(S, c.precedence, l), o.instance = S;
        case "script":
          return S = Hl(c.src), (v = l.querySelector(
            Vr(S)
          )) ? (o.instance = v, Tn(v), v) : (d = c, (v = dn.get(S)) && (d = E({}, c), qg(d, v)), l = l.ownerDocument || l, v = l.createElement("script"), Tn(v), Vt(v, "link", d), l.head.appendChild(v), o.instance = v);
        case "void":
          return null;
        default:
          throw Error(a(443, o.type));
      }
    else
      o.type === "stylesheet" && !(o.state.loading & 4) && (d = o.instance, o.state.loading |= 4, kg(d, c.precedence, l));
    return o.instance;
  }
  function kg(l, o, c) {
    for (var d = c.querySelectorAll(
      'link[rel="stylesheet"][data-precedence],style[data-precedence]'
    ), v = d.length ? d[d.length - 1] : null, S = v, A = 0; A < d.length; A++) {
      var M = d[A];
      if (M.dataset.precedence === o) S = M;
      else if (S !== v) break;
    }
    S ? S.parentNode.insertBefore(l, S.nextSibling) : (o = c.nodeType === 9 ? c.head : c, o.insertBefore(l, o.firstChild));
  }
  function Vg(l, o) {
    l.crossOrigin == null && (l.crossOrigin = o.crossOrigin), l.referrerPolicy == null && (l.referrerPolicy = o.referrerPolicy), l.title == null && (l.title = o.title);
  }
  function qg(l, o) {
    l.crossOrigin == null && (l.crossOrigin = o.crossOrigin), l.referrerPolicy == null && (l.referrerPolicy = o.referrerPolicy), l.integrity == null && (l.integrity = o.integrity);
  }
  var ko = null;
  function Qp(l, o, c) {
    if (ko === null) {
      var d = /* @__PURE__ */ new Map(), v = ko = /* @__PURE__ */ new Map();
      v.set(c, d);
    } else
      v = ko, d = v.get(c), d || (d = /* @__PURE__ */ new Map(), v.set(c, d));
    if (d.has(l)) return d;
    for (d.set(l, null), c = c.getElementsByTagName(l), v = 0; v < c.length; v++) {
      var S = c[v];
      if (!(S[wt] || S[Ue] || l === "link" && S.getAttribute("rel") === "stylesheet") && S.namespaceURI !== "http://www.w3.org/2000/svg") {
        var A = S.getAttribute(o) || "";
        A = l + A;
        var M = d.get(A);
        M ? M.push(S) : d.set(A, [S]);
      }
    }
    return d;
  }
  function Wp(l, o, c) {
    l = l.ownerDocument || l, l.head.insertBefore(
      c,
      o === "title" ? l.querySelector("head > title") : null
    );
  }
  function Uv(l, o, c) {
    if (c === 1 || o.itemProp != null) return !1;
    switch (l) {
      case "meta":
      case "title":
        return !0;
      case "style":
        if (typeof o.precedence != "string" || typeof o.href != "string" || o.href === "")
          break;
        return !0;
      case "link":
        if (typeof o.rel != "string" || typeof o.href != "string" || o.href === "" || o.onLoad || o.onError)
          break;
        switch (o.rel) {
          case "stylesheet":
            return l = o.disabled, typeof o.precedence == "string" && l == null;
          default:
            return !0;
        }
      case "script":
        if (o.async && typeof o.async != "function" && typeof o.async != "symbol" && !o.onLoad && !o.onError && o.src && typeof o.src == "string")
          return !0;
    }
    return !1;
  }
  function Jp(l) {
    return !(l.type === "stylesheet" && !(l.state.loading & 3));
  }
  var vc = null;
  function Gv() {
  }
  function Hv(l, o, c) {
    if (vc === null) throw Error(a(475));
    var d = vc;
    if (o.type === "stylesheet" && (typeof c.media != "string" || matchMedia(c.media).matches !== !1) && !(o.state.loading & 4)) {
      if (o.instance === null) {
        var v = yc(c.href), S = l.querySelector(
          pc(v)
        );
        if (S) {
          l = S._p, l !== null && typeof l == "object" && typeof l.then == "function" && (d.count++, d = gh.bind(d), l.then(d, d)), o.state.loading |= 4, o.instance = S, Tn(S);
          return;
        }
        S = l.ownerDocument || l, c = _c(c), (v = dn.get(v)) && Vg(c, v), S = S.createElement("link"), Tn(S);
        var A = S;
        A._p = new Promise(function(M, G) {
          A.onload = M, A.onerror = G;
        }), Vt(S, "link", c), o.instance = S;
      }
      d.stylesheets === null && (d.stylesheets = /* @__PURE__ */ new Map()), d.stylesheets.set(o, l), (l = o.state.preload) && !(o.state.loading & 3) && (d.count++, o = gh.bind(d), l.addEventListener("load", o), l.addEventListener("error", o));
    }
  }
  function $p() {
    if (vc === null) throw Error(a(475));
    var l = vc;
    return l.stylesheets && l.count === 0 && mh(l, l.stylesheets), 0 < l.count ? function(o) {
      var c = setTimeout(function() {
        if (l.stylesheets && mh(l, l.stylesheets), l.unsuspend) {
          var d = l.unsuspend;
          l.unsuspend = null, d();
        }
      }, 6e4);
      return l.unsuspend = o, function() {
        l.unsuspend = null, clearTimeout(c);
      };
    } : null;
  }
  function gh() {
    if (this.count--, this.count === 0) {
      if (this.stylesheets) mh(this, this.stylesheets);
      else if (this.unsuspend) {
        var l = this.unsuspend;
        this.unsuspend = null, l();
      }
    }
  }
  var Sc = null;
  function mh(l, o) {
    l.stylesheets = null, l.unsuspend !== null && (l.count++, Sc = /* @__PURE__ */ new Map(), o.forEach(ol, l), Sc = null, gh.call(l));
  }
  function ol(l, o) {
    if (!(o.state.loading & 4)) {
      var c = Sc.get(l);
      if (c) var d = c.get(null);
      else {
        c = /* @__PURE__ */ new Map(), Sc.set(l, c);
        for (var v = l.querySelectorAll(
          "link[data-precedence],style[data-precedence]"
        ), S = 0; S < v.length; S++) {
          var A = v[S];
          (A.nodeName === "LINK" || A.getAttribute("media") !== "not all") && (c.set(A.dataset.precedence, A), d = A);
        }
        d && c.set(null, d);
      }
      v = o.instance, A = v.getAttribute("data-precedence"), S = c.get(A) || d, S === d && c.set(null, v), c.set(A, v), this.count++, d = gh.bind(this), v.addEventListener("load", d), v.addEventListener("error", d), S ? S.parentNode.insertBefore(v, S.nextSibling) : (l = l.nodeType === 9 ? l.head : l, l.insertBefore(v, l.firstChild)), o.state.loading |= 4;
    }
  }
  var Ii = {
    $$typeof: I,
    Provider: null,
    Consumer: null,
    _currentValue: nt,
    _currentValue2: nt,
    _threadCount: 0
  };
  function KS(l, o, c, d, v, S, A, M) {
    this.tag = 1, this.containerInfo = l, this.pingCache = this.current = this.pendingChildren = null, this.timeoutHandle = -1, this.callbackNode = this.next = this.pendingContext = this.context = this.cancelPendingCommit = null, this.callbackPriority = 0, this.expirationTimes = Ci(-1), this.entangledLanes = this.shellSuspendCounter = this.errorRecoveryDisabledLanes = this.expiredLanes = this.warmLanes = this.pingedLanes = this.suspendedLanes = this.pendingLanes = 0, this.entanglements = Ci(0), this.hiddenUpdates = Ci(null), this.identifierPrefix = d, this.onUncaughtError = v, this.onCaughtError = S, this.onRecoverableError = A, this.pooledCache = null, this.pooledCacheLanes = 0, this.formState = M, this.incompleteTransitions = /* @__PURE__ */ new Map();
  }
  function t0(l, o, c, d, v, S, A, M, G, W, ct, gt) {
    return l = new KS(
      l,
      o,
      c,
      A,
      M,
      G,
      W,
      gt
    ), o = 1, S === !0 && (o |= 24), S = Di(3, null, null, o), l.current = S, S.stateNode = l, o = Xu(), o.refCount++, l.pooledCache = o, o.refCount++, S.memoizedState = {
      element: d,
      isDehydrated: c,
      cache: o
    }, Gd(S), l;
  }
  function e0(l) {
    return l ? (l = Nu, l) : Nu;
  }
  function n0(l, o, c, d, v, S) {
    v = e0(v), d.context === null ? d.context = v : d.pendingContext = v, d = Li(o), d.payload = { element: c }, S = S === void 0 ? null : S, S !== null && (d.callback = S), c = Na(l, d, o), c !== null && (al(c, l, o), Tr(c, l, o));
  }
  function i0(l, o) {
    if (l = l.memoizedState, l !== null && l.dehydrated !== null) {
      var c = l.retryLane;
      l.retryLane = c !== 0 && c < o ? c : o;
    }
  }
  function Zg(l, o) {
    i0(l, o), (l = l.alternate) && i0(l, o);
  }
  function l0(l) {
    if (l.tag === 13) {
      var o = Ma(l, 67108864);
      o !== null && al(o, l, 67108864), Zg(l, 67108864);
    }
  }
  var yh = !0;
  function Yv(l, o, c, d) {
    var v = Y.T;
    Y.T = null;
    var S = K.p;
    try {
      K.p = 2, a0(l, o, c, d);
    } finally {
      K.p = S, Y.T = v;
    }
  }
  function jv(l, o, c, d) {
    var v = Y.T;
    Y.T = null;
    var S = K.p;
    try {
      K.p = 8, a0(l, o, c, d);
    } finally {
      K.p = S, Y.T = v;
    }
  }
  function a0(l, o, c, d) {
    if (yh) {
      var v = Fg(d);
      if (v === null)
        Ul(
          l,
          o,
          d,
          Kg,
          c
        ), qr(l, d);
      else if (Iv(
        v,
        l,
        o,
        c,
        d
      ))
        d.stopPropagation();
      else if (qr(l, d), o & 4 && -1 < Bv.indexOf(l)) {
        for (; v !== null; ) {
          var S = er(v);
          if (S !== null)
            switch (S.tag) {
              case 3:
                if (S = S.stateNode, S.current.memoizedState.isDehydrated) {
                  var A = ni(S.pendingLanes);
                  if (A !== 0) {
                    var M = S;
                    for (M.pendingLanes |= 2, M.entangledLanes |= 2; A; ) {
                      var G = 1 << 31 - Bn(A);
                      M.entanglements[1] |= G, A &= ~G;
                    }
                    Yi(S), !(Re & 6) && (xg = yn() + 500, ih(0));
                  }
                }
                break;
              case 13:
                M = Ma(S, 2), M !== null && al(M, S, 2), jr(), Zg(S, 2);
            }
          if (S = Fg(d), S === null && Ul(
            l,
            o,
            d,
            Kg,
            c
          ), S === v) break;
          v = S;
        }
        v !== null && d.stopPropagation();
      } else
        Ul(
          l,
          o,
          d,
          null,
          c
        );
    }
  }
  function Fg(l) {
    return l = hd(l), s0(l);
  }
  var Kg = null;
  function s0(l) {
    if (Kg = null, l = Xn(l), l !== null) {
      var o = h(l);
      if (o === null) l = null;
      else {
        var c = o.tag;
        if (c === 13) {
          if (l = g(o), l !== null) return l;
          l = null;
        } else if (c === 3) {
          if (o.stateNode.current.memoizedState.isDehydrated)
            return o.tag === 3 ? o.stateNode.containerInfo : null;
          l = null;
        } else o !== l && (l = null);
      }
    }
    return Kg = l, null;
  }
  function o0(l) {
    switch (l) {
      case "beforetoggle":
      case "cancel":
      case "click":
      case "close":
      case "contextmenu":
      case "copy":
      case "cut":
      case "auxclick":
      case "dblclick":
      case "dragend":
      case "dragstart":
      case "drop":
      case "focusin":
      case "focusout":
      case "input":
      case "invalid":
      case "keydown":
      case "keypress":
      case "keyup":
      case "mousedown":
      case "mouseup":
      case "paste":
      case "pause":
      case "play":
      case "pointercancel":
      case "pointerdown":
      case "pointerup":
      case "ratechange":
      case "reset":
      case "resize":
      case "seeked":
      case "submit":
      case "toggle":
      case "touchcancel":
      case "touchend":
      case "touchstart":
      case "volumechange":
      case "change":
      case "selectionchange":
      case "textInput":
      case "compositionstart":
      case "compositionend":
      case "compositionupdate":
      case "beforeblur":
      case "afterblur":
      case "beforeinput":
      case "blur":
      case "fullscreenchange":
      case "focus":
      case "hashchange":
      case "popstate":
      case "select":
      case "selectstart":
        return 2;
      case "drag":
      case "dragenter":
      case "dragexit":
      case "dragleave":
      case "dragover":
      case "mousemove":
      case "mouseout":
      case "mouseover":
      case "pointermove":
      case "pointerout":
      case "pointerover":
      case "scroll":
      case "touchmove":
      case "wheel":
      case "mouseenter":
      case "mouseleave":
      case "pointerenter":
      case "pointerleave":
        return 8;
      case "message":
        switch (pl()) {
          case io:
            return 2;
          case Pl:
            return 8;
          case _l:
          case Sa:
            return 32;
          case Fe:
            return 268435456;
          default:
            return 32;
        }
      default:
        return 32;
    }
  }
  var Ec = !1, Wa = null, Xs = null, ks = null, ph = /* @__PURE__ */ new Map(), _h = /* @__PURE__ */ new Map(), Vo = [], Bv = "mousedown mouseup touchcancel touchend touchstart auxclick dblclick pointercancel pointerdown pointerup dragend dragstart drop compositionend compositionstart keydown keypress keyup input textInput copy cut paste click change contextmenu reset".split(
    " "
  );
  function qr(l, o) {
    switch (l) {
      case "focusin":
      case "focusout":
        Wa = null;
        break;
      case "dragenter":
      case "dragleave":
        Xs = null;
        break;
      case "mouseover":
      case "mouseout":
        ks = null;
        break;
      case "pointerover":
      case "pointerout":
        ph.delete(o.pointerId);
        break;
      case "gotpointercapture":
      case "lostpointercapture":
        _h.delete(o.pointerId);
    }
  }
  function Zr(l, o, c, d, v, S) {
    return l === null || l.nativeEvent !== S ? (l = {
      blockedOn: o,
      domEventName: c,
      eventSystemFlags: d,
      nativeEvent: S,
      targetContainers: [v]
    }, o !== null && (o = er(o), o !== null && l0(o)), l) : (l.eventSystemFlags |= d, o = l.targetContainers, v !== null && o.indexOf(v) === -1 && o.push(v), l);
  }
  function Iv(l, o, c, d, v) {
    switch (o) {
      case "focusin":
        return Wa = Zr(
          Wa,
          l,
          o,
          c,
          d,
          v
        ), !0;
      case "dragenter":
        return Xs = Zr(
          Xs,
          l,
          o,
          c,
          d,
          v
        ), !0;
      case "mouseover":
        return ks = Zr(
          ks,
          l,
          o,
          c,
          d,
          v
        ), !0;
      case "pointerover":
        var S = v.pointerId;
        return ph.set(
          S,
          Zr(
            ph.get(S) || null,
            l,
            o,
            c,
            d,
            v
          )
        ), !0;
      case "gotpointercapture":
        return S = v.pointerId, _h.set(
          S,
          Zr(
            _h.get(S) || null,
            l,
            o,
            c,
            d,
            v
          )
        ), !0;
    }
    return !1;
  }
  function r0(l) {
    var o = Xn(l.target);
    if (o !== null) {
      var c = h(o);
      if (c !== null) {
        if (o = c.tag, o === 13) {
          if (o = g(c), o !== null) {
            l.blockedOn = o, Ri(l.priority, function() {
              if (c.tag === 13) {
                var d = ll();
                d = bn(d);
                var v = Ma(c, d);
                v !== null && al(v, c, d), Zg(c, d);
              }
            });
            return;
          }
        } else if (o === 3 && c.stateNode.current.memoizedState.isDehydrated) {
          l.blockedOn = c.tag === 3 ? c.stateNode.containerInfo : null;
          return;
        }
      }
    }
    l.blockedOn = null;
  }
  function vh(l) {
    if (l.blockedOn !== null) return !1;
    for (var o = l.targetContainers; 0 < o.length; ) {
      var c = Fg(l.nativeEvent);
      if (c === null) {
        c = l.nativeEvent;
        var d = new c.constructor(
          c.type,
          c
        );
        or = d, c.target.dispatchEvent(d), or = null;
      } else
        return o = er(c), o !== null && l0(o), l.blockedOn = c, !1;
      o.shift();
    }
    return !0;
  }
  function Sh(l, o, c) {
    vh(l) && c.delete(o);
  }
  function bc() {
    Ec = !1, Wa !== null && vh(Wa) && (Wa = null), Xs !== null && vh(Xs) && (Xs = null), ks !== null && vh(ks) && (ks = null), ph.forEach(Sh), _h.forEach(Sh);
  }
  function Pg(l, o) {
    l.blockedOn === o && (l.blockedOn = null, Ec || (Ec = !0, r.unstable_scheduleCallback(
      r.unstable_NormalPriority,
      bc
    )));
  }
  var Fr = null;
  function u0(l) {
    Fr !== l && (Fr = l, r.unstable_scheduleCallback(
      r.unstable_NormalPriority,
      function() {
        Fr === l && (Fr = null);
        for (var o = 0; o < l.length; o += 3) {
          var c = l[o], d = l[o + 1], v = l[o + 2];
          if (typeof d != "function") {
            if (s0(d || c) === null)
              continue;
            break;
          }
          var S = er(c);
          S !== null && (l.splice(o, 3), o -= 3, ng(
            S,
            {
              pending: !0,
              data: v,
              method: c.method,
              action: d
            },
            d,
            v
          ));
        }
      }
    ));
  }
  function Ja(l) {
    function o(G) {
      return Pg(G, l);
    }
    Wa !== null && Pg(Wa, l), Xs !== null && Pg(Xs, l), ks !== null && Pg(ks, l), ph.forEach(o), _h.forEach(o);
    for (var c = 0; c < Vo.length; c++) {
      var d = Vo[c];
      d.blockedOn === l && (d.blockedOn = null);
    }
    for (; 0 < Vo.length && (c = Vo[0], c.blockedOn === null); )
      r0(c), c.blockedOn === null && Vo.shift();
    if (c = (l.ownerDocument || l).$$reactFormReplay, c != null)
      for (d = 0; d < c.length; d += 3) {
        var v = c[d], S = c[d + 1], A = v[se] || null;
        if (typeof S == "function")
          A || u0(c);
        else if (A) {
          var M = null;
          if (S && S.hasAttribute("formAction")) {
            if (v = S, A = S[se] || null)
              M = A.formAction;
            else if (s0(v) !== null) continue;
          } else M = A.action;
          typeof M == "function" ? c[d + 1] = M : (c.splice(d, 3), d -= 3), u0(c);
        }
      }
  }
  function c0(l) {
    this._internalRoot = l;
  }
  Qg.prototype.render = c0.prototype.render = function(l) {
    var o = this._internalRoot;
    if (o === null) throw Error(a(409));
    var c = o.current, d = ll();
    n0(c, d, l, o, null, null);
  }, Qg.prototype.unmount = c0.prototype.unmount = function() {
    var l = this._internalRoot;
    if (l !== null) {
      this._internalRoot = null;
      var o = l.containerInfo;
      n0(l.current, 2, null, l, null, null), jr(), o[an] = null;
    }
  };
  function Qg(l) {
    this._internalRoot = l;
  }
  Qg.prototype.unstable_scheduleHydration = function(l) {
    if (l) {
      var o = Wi();
      l = { blockedOn: null, target: l, priority: o };
      for (var c = 0; c < Vo.length && o !== 0 && o < Vo[c].priority; c++) ;
      Vo.splice(c, 0, l), c === 0 && r0(l);
    }
  };
  var f0 = t.version;
  if (f0 !== "19.1.1")
    throw Error(
      a(
        527,
        f0,
        "19.1.1"
      )
    );
  K.findDOMNode = function(l) {
    var o = l._reactInternals;
    if (o === void 0)
      throw typeof l.render == "function" ? Error(a(188)) : (l = Object.keys(l).join(","), Error(a(268, l)));
    return l = _(o), l = l !== null ? b(l) : null, l = l === null ? null : l.stateNode, l;
  };
  var vi = {
    bundleType: 0,
    version: "19.1.1",
    rendererPackageName: "react-dom",
    currentDispatcherRef: Y,
    reconcilerVersion: "19.1.1"
  };
  if (typeof __REACT_DEVTOOLS_GLOBAL_HOOK__ < "u") {
    var Eh = __REACT_DEVTOOLS_GLOBAL_HOOK__;
    if (!Eh.isDisabled && Eh.supportsFiber)
      try {
        Ql = Eh.inject(
          vi
        ), pn = Eh;
      } catch {
      }
  }
  return K0.createRoot = function(l, o) {
    if (!u(l)) throw Error(a(299));
    var c = !1, d = "", v = Qu, S = rp, A = Zf, M = null;
    return o != null && (o.unstable_strictMode === !0 && (c = !0), o.identifierPrefix !== void 0 && (d = o.identifierPrefix), o.onUncaughtError !== void 0 && (v = o.onUncaughtError), o.onCaughtError !== void 0 && (S = o.onCaughtError), o.onRecoverableError !== void 0 && (A = o.onRecoverableError), o.unstable_transitionCallbacks !== void 0 && (M = o.unstable_transitionCallbacks)), o = t0(
      l,
      1,
      !1,
      null,
      null,
      c,
      d,
      v,
      S,
      A,
      M,
      null
    ), l[an] = o.current, Zp(l), new c0(o);
  }, K0.hydrateRoot = function(l, o, c) {
    if (!u(l)) throw Error(a(299));
    var d = !1, v = "", S = Qu, A = rp, M = Zf, G = null, W = null;
    return c != null && (c.unstable_strictMode === !0 && (d = !0), c.identifierPrefix !== void 0 && (v = c.identifierPrefix), c.onUncaughtError !== void 0 && (S = c.onUncaughtError), c.onCaughtError !== void 0 && (A = c.onCaughtError), c.onRecoverableError !== void 0 && (M = c.onRecoverableError), c.unstable_transitionCallbacks !== void 0 && (G = c.unstable_transitionCallbacks), c.formState !== void 0 && (W = c.formState)), o = t0(
      l,
      1,
      !0,
      o,
      c ?? null,
      d,
      v,
      S,
      A,
      M,
      G,
      W
    ), o.context = e0(null), c = o.current, d = ll(), d = bn(d), v = Li(d), v.callback = null, Na(c, v, d), c = d, o.current.lanes = c, Qi(o, c), Yi(o), l[an] = o.current, Zp(l), new Qg(o);
  }, K0.version = "19.1.1", K0;
}
var P0 = {};
/**
 * @license React
 * react-dom-client.development.js
 *
 * Copyright (c) Meta Platforms, Inc. and affiliates.
 *
 * This source code is licensed under the MIT license found in the
 * LICENSE file in the root directory of this source tree.
 */
var JC;
function JM() {
  return JC || (JC = 1, process.env.NODE_ENV !== "production" && function() {
    function r(e, n) {
      for (e = e.memoizedState; e !== null && 0 < n; )
        e = e.next, n--;
      return e;
    }
    function t(e, n, s, f) {
      if (s >= n.length) return f;
      var m = n[s], p = Ft(e) ? e.slice() : ee({}, e);
      return p[m] = t(e[m], n, s + 1, f), p;
    }
    function i(e, n, s) {
      if (n.length !== s.length)
        console.warn("copyWithRename() expects paths of the same length");
      else {
        for (var f = 0; f < s.length - 1; f++)
          if (n[f] !== s[f]) {
            console.warn(
              "copyWithRename() expects paths to be the same except for the deepest key"
            );
            return;
          }
        return a(e, n, s, 0);
      }
    }
    function a(e, n, s, f) {
      var m = n[f], p = Ft(e) ? e.slice() : ee({}, e);
      return f + 1 === n.length ? (p[s[f]] = p[m], Ft(p) ? p.splice(m, 1) : delete p[m]) : p[m] = a(
        e[m],
        n,
        s,
        f + 1
      ), p;
    }
    function u(e, n, s) {
      var f = n[s], m = Ft(e) ? e.slice() : ee({}, e);
      return s + 1 === n.length ? (Ft(m) ? m.splice(f, 1) : delete m[f], m) : (m[f] = u(e[f], n, s + 1), m);
    }
    function h() {
      return !1;
    }
    function g() {
      return null;
    }
    function y() {
    }
    function _() {
      console.error(
        "Do not call Hooks inside useEffect(...), useMemo(...), or other built-in Hooks. You can only call Hooks at the top level of your React function. For more information, see https://react.dev/link/rules-of-hooks"
      );
    }
    function b() {
      console.error(
        "Context can only be read while React is rendering. In classes, you can read it in the render method or getDerivedStateFromProps. In function components, you can read it directly in the function body, but not inside Hooks like useReducer() or useMemo()."
      );
    }
    function E() {
    }
    function x(e) {
      var n = [];
      return e.forEach(function(s) {
        n.push(s);
      }), n.sort().join(", ");
    }
    function R(e, n, s, f) {
      return new yf(e, n, s, f);
    }
    function w(e, n) {
      e.context === Tc && (Le(e.current, 2, n, e, null, null), zr());
    }
    function L(e, n) {
      if (ts !== null) {
        var s = n.staleFamilies;
        n = n.updatedFamilies, Wu(), mf(
          e.current,
          n,
          s
        ), zr();
      }
    }
    function H(e) {
      ts = e;
    }
    function N(e) {
      return !(!e || e.nodeType !== 1 && e.nodeType !== 9 && e.nodeType !== 11);
    }
    function O(e) {
      var n = e, s = e;
      if (e.alternate) for (; n.return; ) n = n.return;
      else {
        e = n;
        do
          n = e, n.flags & 4098 && (s = n.return), e = n.return;
        while (e);
      }
      return n.tag === 3 ? s : null;
    }
    function U(e) {
      if (e.tag === 13) {
        var n = e.memoizedState;
        if (n === null && (e = e.alternate, e !== null && (n = e.memoizedState)), n !== null) return n.dehydrated;
      }
      return null;
    }
    function I(e) {
      if (O(e) !== e)
        throw Error("Unable to find node on an unmounted component.");
    }
    function X(e) {
      var n = e.alternate;
      if (!n) {
        if (n = O(e), n === null)
          throw Error("Unable to find node on an unmounted component.");
        return n !== e ? null : e;
      }
      for (var s = e, f = n; ; ) {
        var m = s.return;
        if (m === null) break;
        var p = m.alternate;
        if (p === null) {
          if (f = m.return, f !== null) {
            s = f;
            continue;
          }
          break;
        }
        if (m.child === p.child) {
          for (p = m.child; p; ) {
            if (p === s) return I(m), e;
            if (p === f) return I(m), n;
            p = p.sibling;
          }
          throw Error("Unable to find node on an unmounted component.");
        }
        if (s.return !== f.return) s = m, f = p;
        else {
          for (var T = !1, C = m.child; C; ) {
            if (C === s) {
              T = !0, s = m, f = p;
              break;
            }
            if (C === f) {
              T = !0, f = m, s = p;
              break;
            }
            C = C.sibling;
          }
          if (!T) {
            for (C = p.child; C; ) {
              if (C === s) {
                T = !0, s = p, f = m;
                break;
              }
              if (C === f) {
                T = !0, f = p, s = m;
                break;
              }
              C = C.sibling;
            }
            if (!T)
              throw Error(
                "Child was not found in either parent set. This indicates a bug in React related to the return pointer. Please file an issue."
              );
          }
        }
        if (s.alternate !== f)
          throw Error(
            "Return fibers should always be each others' alternates. This error is likely caused by a bug in React. Please file an issue."
          );
      }
      if (s.tag !== 3)
        throw Error("Unable to find node on an unmounted component.");
      return s.stateNode.current === s ? e : n;
    }
    function Z(e) {
      var n = e.tag;
      if (n === 5 || n === 26 || n === 27 || n === 6) return e;
      for (e = e.child; e !== null; ) {
        if (n = Z(e), n !== null) return n;
        e = e.sibling;
      }
      return null;
    }
    function dt(e) {
      return e === null || typeof e != "object" ? null : (e = Kp && e[Kp] || e["@@iterator"], typeof e == "function" ? e : null);
    }
    function ft(e) {
      if (e == null) return null;
      if (typeof e == "function")
        return e.$$typeof === Gg ? null : e.displayName || e.name || null;
      if (typeof e == "string") return e;
      switch (e) {
        case Qt:
          return "Fragment";
        case fc:
          return "Profiler";
        case cc:
          return "StrictMode";
        case hc:
          return "Suspense";
        case Yo:
          return "SuspenseList";
        case Fp:
          return "Activity";
      }
      if (typeof e == "object")
        switch (typeof e.tag == "number" && console.error(
          "Received an unexpected object in getComponentNameFromType(). This is likely a bug in React. Please file an issue."
        ), e.$$typeof) {
          case kr:
            return "Portal";
          case Ul:
            return (e.displayName || "Context") + ".Provider";
          case Ug:
            return (e._context.displayName || "Context") + ".Consumer";
          case Hs:
            var n = e.render;
            return e = e.displayName, e || (e = n.displayName || n.name || "", e = e !== "" ? "ForwardRef(" + e + ")" : "ForwardRef"), e;
          case uh:
            return n = e.displayName || null, n !== null ? n : ft(e.type) || "Memo";
          case sl:
            n = e._payload, e = e._init;
            try {
              return ft(e(n));
            } catch {
            }
        }
      return null;
    }
    function yt(e) {
      return typeof e.tag == "number" ? it(e) : typeof e.name == "string" ? e.name : null;
    }
    function it(e) {
      var n = e.type;
      switch (e.tag) {
        case 31:
          return "Activity";
        case 24:
          return "Cache";
        case 9:
          return (n._context.displayName || "Context") + ".Consumer";
        case 10:
          return (n.displayName || "Context") + ".Provider";
        case 18:
          return "DehydratedFragment";
        case 11:
          return e = n.render, e = e.displayName || e.name || "", n.displayName || (e !== "" ? "ForwardRef(" + e + ")" : "ForwardRef");
        case 7:
          return "Fragment";
        case 26:
        case 27:
        case 5:
          return n;
        case 4:
          return "Portal";
        case 3:
          return "Root";
        case 6:
          return "Text";
        case 16:
          return ft(n);
        case 8:
          return n === cc ? "StrictMode" : "Mode";
        case 22:
          return "Offscreen";
        case 12:
          return "Profiler";
        case 21:
          return "Scope";
        case 13:
          return "Suspense";
        case 19:
          return "SuspenseList";
        case 25:
          return "TracingMarker";
        case 1:
        case 0:
        case 14:
        case 15:
          if (typeof n == "function")
            return n.displayName || n.name || null;
          if (typeof n == "string") return n;
          break;
        case 29:
          if (n = e._debugInfo, n != null) {
            for (var s = n.length - 1; 0 <= s; s--)
              if (typeof n[s].name == "string") return n[s].name;
          }
          if (e.return !== null)
            return it(e.return);
      }
      return null;
    }
    function Et(e) {
      return { current: e };
    }
    function xt(e, n) {
      0 > Gl ? console.error("Unexpected pop.") : (n !== fh[Gl] && console.error("Unexpected Fiber popped."), e.current = ch[Gl], ch[Gl] = null, fh[Gl] = null, Gl--);
    }
    function pt(e, n, s) {
      Gl++, ch[Gl] = e.current, fh[Gl] = s, e.current = n;
    }
    function Tt(e) {
      return e === null && console.error(
        "Expected host context to exist. This error is likely caused by a bug in React. Please file an issue."
      ), e;
    }
    function Nt(e, n) {
      pt(Pa, n, e), pt(dc, e, e), pt(Ys, null, e);
      var s = n.nodeType;
      switch (s) {
        case 9:
        case 11:
          s = s === 9 ? "#document" : "#fragment", n = (n = n.documentElement) && (n = n.namespaceURI) ? Ae(n) : iu;
          break;
        default:
          if (s = n.tagName, n = n.namespaceURI)
            n = Ae(n), n = Gi(
              n,
              s
            );
          else
            switch (s) {
              case "svg":
                n = xm;
                break;
              case "math":
                n = p1;
                break;
              default:
                n = iu;
            }
      }
      s = s.toLowerCase(), s = ry(null, s), s = {
        context: n,
        ancestorInfo: s
      }, xt(Ys, e), pt(Ys, s, e);
    }
    function Rt(e) {
      xt(Ys, e), xt(dc, e), xt(Pa, e);
    }
    function Y() {
      return Tt(Ys.current);
    }
    function K(e) {
      e.memoizedState !== null && pt(gc, e, e);
      var n = Tt(Ys.current), s = e.type, f = Gi(n.context, s);
      s = ry(n.ancestorInfo, s), f = { context: f, ancestorInfo: s }, n !== f && (pt(dc, e, e), pt(Ys, f, e));
    }
    function nt(e) {
      dc.current === e && (xt(Ys, e), xt(dc, e)), gc.current === e && (xt(gc, e), k0._currentValue = jh);
    }
    function vt(e) {
      return typeof Symbol == "function" && Symbol.toStringTag && e[Symbol.toStringTag] || e.constructor.name || "Object";
    }
    function F(e) {
      try {
        return St(e), !1;
      } catch {
        return !0;
      }
    }
    function St(e) {
      return "" + e;
    }
    function Q(e, n) {
      if (F(e))
        return console.error(
          "The provided `%s` attribute is an unsupported type %s. This value must be coerced to a string before using it here.",
          n,
          vt(e)
        ), St(e);
    }
    function At(e, n) {
      if (F(e))
        return console.error(
          "The provided `%s` CSS property is an unsupported type %s. This value must be coerced to a string before using it here.",
          n,
          vt(e)
        ), St(e);
    }
    function Ut(e) {
      if (F(e))
        return console.error(
          "Form field values (value, checked, defaultValue, or defaultChecked props) must be strings, not %s. This value must be coerced to a string before using it here.",
          vt(e)
        ), St(e);
    }
    function kt(e) {
      if (typeof __REACT_DEVTOOLS_GLOBAL_HOOK__ > "u") return !1;
      var n = __REACT_DEVTOOLS_GLOBAL_HOOK__;
      if (n.isDisabled) return !0;
      if (!n.supportsFiber)
        return console.error(
          "The installed version of React DevTools is too old and will not work with the current version of React. Please update React DevTools. https://react.dev/link/react-devtools"
        ), !0;
      try {
        Bo = n.inject(e), Pn = n;
      } catch (s) {
        console.error("React instrumentation encountered an error: %s.", s);
      }
      return !!n.checkDCE;
    }
    function Xt(e) {
      if (typeof FS == "function" && fa(e), Pn && typeof Pn.setStrictMode == "function")
        try {
          Pn.setStrictMode(Bo, e);
        } catch (n) {
          ji || (ji = !0, console.error(
            "React instrumentation encountered an error: %s",
            n
          ));
        }
    }
    function Ze(e) {
      Ct = e;
    }
    function _e() {
      Ct !== null && typeof Ct.markCommitStopped == "function" && Ct.markCommitStopped();
    }
    function De(e) {
      Ct !== null && typeof Ct.markComponentRenderStarted == "function" && Ct.markComponentRenderStarted(e);
    }
    function ke() {
      Ct !== null && typeof Ct.markComponentRenderStopped == "function" && Ct.markComponentRenderStopped();
    }
    function yl(e) {
      Ct !== null && typeof Ct.markRenderStarted == "function" && Ct.markRenderStarted(e);
    }
    function no() {
      Ct !== null && typeof Ct.markRenderStopped == "function" && Ct.markRenderStopped();
    }
    function Fi(e, n) {
      Ct !== null && typeof Ct.markStateUpdateScheduled == "function" && Ct.markStateUpdateScheduled(e, n);
    }
    function Kl(e) {
      return e >>>= 0, e === 0 ? 32 : 31 - (jg(e) / Bs | 0) | 0;
    }
    function xi(e) {
      if (e & 1) return "SyncHydrationLane";
      if (e & 2) return "Sync";
      if (e & 4) return "InputContinuousHydration";
      if (e & 8) return "InputContinuous";
      if (e & 16) return "DefaultHydration";
      if (e & 32) return "Default";
      if (e & 128) return "TransitionHydration";
      if (e & 4194048) return "Transition";
      if (e & 62914560) return "Retry";
      if (e & 67108864) return "SelectiveHydration";
      if (e & 134217728) return "IdleHydration";
      if (e & 268435456) return "Idle";
      if (e & 536870912) return "Offscreen";
      if (e & 1073741824) return "Deferred";
    }
    function Dn(e) {
      var n = e & 42;
      if (n !== 0) return n;
      switch (e & -e) {
        case 1:
          return 1;
        case 2:
          return 2;
        case 4:
          return 4;
        case 8:
          return 8;
        case 16:
          return 16;
        case 32:
          return 32;
        case 64:
          return 64;
        case 128:
          return 128;
        case 256:
        case 512:
        case 1024:
        case 2048:
        case 4096:
        case 8192:
        case 16384:
        case 32768:
        case 65536:
        case 131072:
        case 262144:
        case 524288:
        case 1048576:
        case 2097152:
          return e & 4194048;
        case 4194304:
        case 8388608:
        case 16777216:
        case 33554432:
          return e & 62914560;
        case 67108864:
          return 67108864;
        case 134217728:
          return 134217728;
        case 268435456:
          return 268435456;
        case 536870912:
          return 536870912;
        case 1073741824:
          return 0;
        default:
          return console.error(
            "Should have found matching lanes. This is a bug in React."
          ), e;
      }
    }
    function yn(e, n, s) {
      var f = e.pendingLanes;
      if (f === 0) return 0;
      var m = 0, p = e.suspendedLanes, T = e.pingedLanes;
      e = e.warmLanes;
      var C = f & 134217727;
      return C !== 0 ? (f = C & ~p, f !== 0 ? m = Dn(f) : (T &= C, T !== 0 ? m = Dn(T) : s || (s = C & ~e, s !== 0 && (m = Dn(s))))) : (C = f & ~p, C !== 0 ? m = Dn(C) : T !== 0 ? m = Dn(T) : s || (s = f & ~e, s !== 0 && (m = Dn(s)))), m === 0 ? 0 : n !== 0 && n !== m && !(n & p) && (p = m & -m, s = n & -n, p >= s || p === 32 && (s & 4194048) !== 0) ? n : m;
    }
    function pl(e, n) {
      return (e.pendingLanes & ~(e.suspendedLanes & ~e.pingedLanes) & n) === 0;
    }
    function io(e, n) {
      switch (e) {
        case 1:
        case 2:
        case 4:
        case 8:
        case 64:
          return n + 250;
        case 16:
        case 32:
        case 128:
        case 256:
        case 512:
        case 1024:
        case 2048:
        case 4096:
        case 8192:
        case 16384:
        case 32768:
        case 65536:
        case 131072:
        case 262144:
        case 524288:
        case 1048576:
        case 2097152:
          return n + 5e3;
        case 4194304:
        case 8388608:
        case 16777216:
        case 33554432:
          return -1;
        case 67108864:
        case 134217728:
        case 268435456:
        case 536870912:
        case 1073741824:
          return -1;
        default:
          return console.error(
            "Should have found matching lanes. This is a bug in React."
          ), -1;
      }
    }
    function Pl() {
      var e = Bg;
      return Bg <<= 1, !(Bg & 4194048) && (Bg = 256), e;
    }
    function _l() {
      var e = Ig;
      return Ig <<= 1, !(Ig & 62914560) && (Ig = 4194304), e;
    }
    function Sa(e) {
      for (var n = [], s = 0; 31 > s; s++) n.push(e);
      return n;
    }
    function Fe(e, n) {
      e.pendingLanes |= n, n !== 268435456 && (e.suspendedLanes = 0, e.pingedLanes = 0, e.warmLanes = 0);
    }
    function tf(e, n, s, f, m, p) {
      var T = e.pendingLanes;
      e.pendingLanes = s, e.suspendedLanes = 0, e.pingedLanes = 0, e.warmLanes = 0, e.expiredLanes &= s, e.entangledLanes &= s, e.errorRecoveryDisabledLanes &= s, e.shellSuspendCounter = 0;
      var C = e.entanglements, D = e.expirationTimes, z = e.hiddenUpdates;
      for (s = T & ~s; 0 < s; ) {
        var tt = 31 - oi(s), st = 1 << tt;
        C[tt] = 0, D[tt] = -1;
        var J = z[tt];
        if (J !== null)
          for (z[tt] = null, tt = 0; tt < J.length; tt++) {
            var rt = J[tt];
            rt !== null && (rt.lane &= -536870913);
          }
        s &= ~st;
      }
      f !== 0 && Eu(e, f, 0), p !== 0 && m === 0 && e.tag !== 0 && (e.suspendedLanes |= p & ~(T & ~n));
    }
    function Eu(e, n, s) {
      e.pendingLanes |= n, e.suspendedLanes &= ~n;
      var f = 31 - oi(n);
      e.entangledLanes |= n, e.entanglements[f] = e.entanglements[f] | 1073741824 | s & 4194090;
    }
    function Ql(e, n) {
      var s = e.entangledLanes |= n;
      for (e = e.entanglements; s; ) {
        var f = 31 - oi(s), m = 1 << f;
        m & n | e[f] & n && (e[f] |= n), s &= ~m;
      }
    }
    function pn(e) {
      switch (e) {
        case 2:
          e = 1;
          break;
        case 8:
          e = 4;
          break;
        case 32:
          e = 16;
          break;
        case 256:
        case 512:
        case 1024:
        case 2048:
        case 4096:
        case 8192:
        case 16384:
        case 32768:
        case 65536:
        case 131072:
        case 262144:
        case 524288:
        case 1048576:
        case 2097152:
        case 4194304:
        case 8388608:
        case 16777216:
        case 33554432:
          e = 128;
          break;
        case 268435456:
          e = 134217728;
          break;
        default:
          e = 0;
      }
      return e;
    }
    function Wl(e, n, s) {
      if (dn)
        for (e = e.pendingUpdatersLaneMap; 0 < s; ) {
          var f = 31 - oi(s), m = 1 << f;
          e[f].add(n), s &= ~m;
        }
    }
    function Bn(e, n) {
      if (dn)
        for (var s = e.pendingUpdatersLaneMap, f = e.memoizedUpdaters; 0 < n; ) {
          var m = 31 - oi(n);
          e = 1 << m, m = s[m], 0 < m.size && (m.forEach(function(p) {
            var T = p.alternate;
            T !== null && f.has(T) || f.add(p);
          }), m.clear()), n &= ~e;
        }
    }
    function bu(e) {
      return e &= -e, Qn < e ? ha < e ? e & 134217727 ? Is : Xg : ha : Qn;
    }
    function Tu() {
      var e = Vt.p;
      return e !== 0 ? e : (e = window.event, e === void 0 ? Is : zg(e.type));
    }
    function iy(e, n) {
      var s = Vt.p;
      try {
        return Vt.p = e, n();
      } finally {
        Vt.p = s;
      }
    }
    function lo(e) {
      delete e[ri], delete e[Bi], delete e[Pp], delete e[Lv], delete e[yc];
    }
    function vl(e) {
      var n = e[ri];
      if (n) return n;
      for (var s = e.parentNode; s; ) {
        if (n = s[Xo] || s[ri]) {
          if (s = n.alternate, n.child !== null || s !== null && s.child !== null)
            for (e = sc(e); e !== null; ) {
              if (s = e[ri])
                return s;
              e = sc(e);
            }
          return n;
        }
        e = s, s = e.parentNode;
      }
      return null;
    }
    function ni(e) {
      if (e = e[ri] || e[Xo]) {
        var n = e.tag;
        if (n === 5 || n === 6 || n === 13 || n === 26 || n === 27 || n === 3)
          return e;
      }
      return null;
    }
    function Sl(e) {
      var n = e.tag;
      if (n === 5 || n === 26 || n === 27 || n === 6)
        return e.stateNode;
      throw Error("getNodeFromInstance: Invalid argument.");
    }
    function Ki(e) {
      var n = e[pc];
      return n || (n = e[pc] = { hoistableStyles: /* @__PURE__ */ new Map(), hoistableScripts: /* @__PURE__ */ new Map() }), n;
    }
    function _n(e) {
      e[_c] = !0;
    }
    function El(e, n) {
      Pi(e, n), Pi(e + "Capture", n);
    }
    function Pi(e, n) {
      Hl[e] && console.error(
        "EventRegistry: More than one plugin attempted to publish the same registration name, `%s`.",
        e
      ), Hl[e] = n;
      var s = e.toLowerCase();
      for (Vr[s] = e, e === "onDoubleClick" && (Vr.ondblclick = e), e = 0; e < n.length; e++)
        zv.add(n[e]);
    }
    function Ci(e, n) {
      Nv[n.type] || n.onChange || n.onInput || n.readOnly || n.disabled || n.value == null || console.error(
        e === "select" ? "You provided a `value` prop to a form field without an `onChange` handler. This will render a read-only field. If the field should be mutable use `defaultValue`. Otherwise, set `onChange`." : "You provided a `value` prop to a form field without an `onChange` handler. This will render a read-only field. If the field should be mutable use `defaultValue`. Otherwise, set either `onChange` or `readOnly`."
      ), n.onChange || n.readOnly || n.disabled || n.checked == null || console.error(
        "You provided a `checked` prop to a form field without an `onChange` handler. This will render a read-only field. If the field should be mutable use `defaultChecked`. Otherwise, set either `onChange` or `readOnly`."
      );
    }
    function Qi(e) {
      return js.call(qg, e) ? !0 : js.call(Vg, e) ? !1 : kg.test(e) ? qg[e] = !0 : (Vg[e] = !0, console.error("Invalid attribute name: `%s`", e), !1);
    }
    function ef(e, n, s) {
      if (Qi(n)) {
        if (!e.hasAttribute(n)) {
          switch (typeof s) {
            case "symbol":
            case "object":
              return s;
            case "function":
              return s;
            case "boolean":
              if (s === !1) return s;
          }
          return s === void 0 ? void 0 : null;
        }
        return e = e.getAttribute(n), e === "" && s === !0 ? !0 : (Q(s, n), e === "" + s ? s : e);
      }
    }
    function ao(e, n, s) {
      if (Qi(n))
        if (s === null) e.removeAttribute(n);
        else {
          switch (typeof s) {
            case "undefined":
            case "function":
            case "symbol":
              e.removeAttribute(n);
              return;
            case "boolean":
              var f = n.toLowerCase().slice(0, 5);
              if (f !== "data-" && f !== "aria-") {
                e.removeAttribute(n);
                return;
              }
          }
          Q(s, n), e.setAttribute(n, "" + s);
        }
    }
    function On(e, n, s) {
      if (s === null) e.removeAttribute(n);
      else {
        switch (typeof s) {
          case "undefined":
          case "function":
          case "symbol":
          case "boolean":
            e.removeAttribute(n);
            return;
        }
        Q(s, n), e.setAttribute(n, "" + s);
      }
    }
    function bn(e, n, s, f) {
      if (f === null) e.removeAttribute(s);
      else {
        switch (typeof f) {
          case "undefined":
          case "function":
          case "symbol":
          case "boolean":
            e.removeAttribute(s);
            return;
        }
        Q(f, s), e.setAttributeNS(n, s, "" + f);
      }
    }
    function In() {
    }
    function Wi() {
      if (ko === 0) {
        Qp = console.log, Wp = console.info, Uv = console.warn, Jp = console.error, vc = console.group, Gv = console.groupCollapsed, Hv = console.groupEnd;
        var e = {
          configurable: !0,
          enumerable: !0,
          value: In,
          writable: !0
        };
        Object.defineProperties(console, {
          info: e,
          log: e,
          warn: e,
          error: e,
          group: e,
          groupCollapsed: e,
          groupEnd: e
        });
      }
      ko++;
    }
    function Ri() {
      if (ko--, ko === 0) {
        var e = { configurable: !0, enumerable: !0, writable: !0 };
        Object.defineProperties(console, {
          log: ee({}, e, { value: Qp }),
          info: ee({}, e, { value: Wp }),
          warn: ee({}, e, { value: Uv }),
          error: ee({}, e, { value: Jp }),
          group: ee({}, e, { value: vc }),
          groupCollapsed: ee({}, e, { value: Gv }),
          groupEnd: ee({}, e, { value: Hv })
        });
      }
      0 > ko && console.error(
        "disabledDepth fell below zero. This is a bug in React. Please file an issue."
      );
    }
    function Oe(e) {
      if ($p === void 0)
        try {
          throw Error();
        } catch (s) {
          var n = s.stack.trim().match(/\n( *(at )?)/);
          $p = n && n[1] || "", gh = -1 < s.stack.indexOf(`
    at`) ? " (<anonymous>)" : -1 < s.stack.indexOf("@") ? "@unknown:0:0" : "";
        }
      return `
` + $p + e + gh;
    }
    function Ue(e, n) {
      if (!e || Sc) return "";
      var s = mh.get(e);
      if (s !== void 0) return s;
      Sc = !0, s = Error.prepareStackTrace, Error.prepareStackTrace = void 0;
      var f = null;
      f = lt.H, lt.H = null, Wi();
      try {
        var m = {
          DetermineComponentFrameRoot: function() {
            try {
              if (n) {
                var J = function() {
                  throw Error();
                };
                if (Object.defineProperty(J.prototype, "props", {
                  set: function() {
                    throw Error();
                  }
                }), typeof Reflect == "object" && Reflect.construct) {
                  try {
                    Reflect.construct(J, []);
                  } catch (Dt) {
                    var rt = Dt;
                  }
                  Reflect.construct(e, [], J);
                } else {
                  try {
                    J.call();
                  } catch (Dt) {
                    rt = Dt;
                  }
                  e.call(J.prototype);
                }
              } else {
                try {
                  throw Error();
                } catch (Dt) {
                  rt = Dt;
                }
                (J = e()) && typeof J.catch == "function" && J.catch(function() {
                });
              }
            } catch (Dt) {
              if (Dt && rt && typeof Dt.stack == "string")
                return [Dt.stack, rt.stack];
            }
            return [null, null];
          }
        };
        m.DetermineComponentFrameRoot.displayName = "DetermineComponentFrameRoot";
        var p = Object.getOwnPropertyDescriptor(
          m.DetermineComponentFrameRoot,
          "name"
        );
        p && p.configurable && Object.defineProperty(
          m.DetermineComponentFrameRoot,
          "name",
          { value: "DetermineComponentFrameRoot" }
        );
        var T = m.DetermineComponentFrameRoot(), C = T[0], D = T[1];
        if (C && D) {
          var z = C.split(`
`), tt = D.split(`
`);
          for (T = p = 0; p < z.length && !z[p].includes(
            "DetermineComponentFrameRoot"
          ); )
            p++;
          for (; T < tt.length && !tt[T].includes(
            "DetermineComponentFrameRoot"
          ); )
            T++;
          if (p === z.length || T === tt.length)
            for (p = z.length - 1, T = tt.length - 1; 1 <= p && 0 <= T && z[p] !== tt[T]; )
              T--;
          for (; 1 <= p && 0 <= T; p--, T--)
            if (z[p] !== tt[T]) {
              if (p !== 1 || T !== 1)
                do
                  if (p--, T--, 0 > T || z[p] !== tt[T]) {
                    var st = `
` + z[p].replace(
                      " at new ",
                      " at "
                    );
                    return e.displayName && st.includes("<anonymous>") && (st = st.replace("<anonymous>", e.displayName)), typeof e == "function" && mh.set(e, st), st;
                  }
                while (1 <= p && 0 <= T);
              break;
            }
        }
      } finally {
        Sc = !1, lt.H = f, Ri(), Error.prepareStackTrace = s;
      }
      return z = (z = e ? e.displayName || e.name : "") ? Oe(z) : "", typeof e == "function" && mh.set(e, z), z;
    }
    function se(e) {
      var n = Error.prepareStackTrace;
      if (Error.prepareStackTrace = void 0, e = e.stack, Error.prepareStackTrace = n, e.startsWith(`Error: react-stack-top-frame
`) && (e = e.slice(29)), n = e.indexOf(`
`), n !== -1 && (e = e.slice(n + 1)), n = e.indexOf("react_stack_bottom_frame"), n !== -1 && (n = e.lastIndexOf(
        `
`,
        n
      )), n !== -1)
        e = e.slice(0, n);
      else return "";
      return e;
    }
    function an(e) {
      switch (e.tag) {
        case 26:
        case 27:
        case 5:
          return Oe(e.type);
        case 16:
          return Oe("Lazy");
        case 13:
          return Oe("Suspense");
        case 19:
          return Oe("SuspenseList");
        case 0:
        case 15:
          return Ue(e.type, !1);
        case 11:
          return Ue(e.type.render, !1);
        case 1:
          return Ue(e.type, !0);
        case 31:
          return Oe("Activity");
        default:
          return "";
      }
    }
    function Ai(e) {
      try {
        var n = "";
        do {
          n += an(e);
          var s = e._debugInfo;
          if (s)
            for (var f = s.length - 1; 0 <= f; f--) {
              var m = s[f];
              if (typeof m.name == "string") {
                var p = n, T = m.env, C = Oe(
                  m.name + (T ? " [" + T + "]" : "")
                );
                n = p + C;
              }
            }
          e = e.return;
        } while (e);
        return n;
      } catch (D) {
        return `
Error generating stack: ` + D.message + `
` + D.stack;
      }
    }
    function ad(e) {
      return (e = e ? e.displayName || e.name : "") ? Oe(e) : "";
    }
    function sd() {
      if (ol === null) return null;
      var e = ol._debugOwner;
      return e != null ? yt(e) : null;
    }
    function U_() {
      if (ol === null) return "";
      var e = ol;
      try {
        var n = "";
        switch (e.tag === 6 && (e = e.return), e.tag) {
          case 26:
          case 27:
          case 5:
            n += Oe(e.type);
            break;
          case 13:
            n += Oe("Suspense");
            break;
          case 19:
            n += Oe("SuspenseList");
            break;
          case 31:
            n += Oe("Activity");
            break;
          case 30:
          case 0:
          case 15:
          case 1:
            e._debugOwner || n !== "" || (n += ad(
              e.type
            ));
            break;
          case 11:
            e._debugOwner || n !== "" || (n += ad(
              e.type.render
            ));
        }
        for (; e; )
          if (typeof e.tag == "number") {
            var s = e;
            e = s._debugOwner;
            var f = s._debugStack;
            e && f && (typeof f != "string" && (s._debugStack = f = se(f)), f !== "" && (n += `
` + f));
          } else if (e.debugStack != null) {
            var m = e.debugStack;
            (e = e.owner) && m && (n += `
` + se(m));
          } else break;
        var p = n;
      } catch (T) {
        p = `
Error generating stack: ` + T.message + `
` + T.stack;
      }
      return p;
    }
    function wt(e, n, s, f, m, p, T) {
      var C = ol;
      nf(e);
      try {
        return e !== null && e._debugTask ? e._debugTask.run(
          n.bind(null, s, f, m, p, T)
        ) : n(s, f, m, p, T);
      } finally {
        nf(C);
      }
      throw Error(
        "runWithFiberInDEV should never be called in production. This is a bug in React."
      );
    }
    function nf(e) {
      lt.getCurrentStack = e === null ? null : U_, Ii = !1, ol = e;
    }
    function Xn(e) {
      switch (typeof e) {
        case "bigint":
        case "boolean":
        case "number":
        case "string":
        case "undefined":
          return e;
        case "object":
          return Ut(e), e;
        default:
          return "";
      }
    }
    function er(e) {
      var n = e.type;
      return (e = e.nodeName) && e.toLowerCase() === "input" && (n === "checkbox" || n === "radio");
    }
    function lf(e) {
      var n = er(e) ? "checked" : "value", s = Object.getOwnPropertyDescriptor(
        e.constructor.prototype,
        n
      );
      Ut(e[n]);
      var f = "" + e[n];
      if (!e.hasOwnProperty(n) && typeof s < "u" && typeof s.get == "function" && typeof s.set == "function") {
        var m = s.get, p = s.set;
        return Object.defineProperty(e, n, {
          configurable: !0,
          get: function() {
            return m.call(this);
          },
          set: function(T) {
            Ut(T), f = "" + T, p.call(this, T);
          }
        }), Object.defineProperty(e, n, {
          enumerable: s.enumerable
        }), {
          getValue: function() {
            return f;
          },
          setValue: function(T) {
            Ut(T), f = "" + T;
          },
          stopTracking: function() {
            e._valueTracker = null, delete e[n];
          }
        };
      }
    }
    function ps(e) {
      e._valueTracker || (e._valueTracker = lf(e));
    }
    function Tn(e) {
      if (!e) return !1;
      var n = e._valueTracker;
      if (!n) return !0;
      var s = n.getValue(), f = "";
      return e && (f = er(e) ? e.checked ? "true" : "false" : e.value), e = f, e !== s ? (n.setValue(e), !0) : !1;
    }
    function af(e) {
      if (e = e || (typeof document < "u" ? document : void 0), typeof e > "u") return null;
      try {
        return e.activeElement || e.body;
      } catch {
        return e.body;
      }
    }
    function Ji(e) {
      return e.replace(
        KS,
        function(n) {
          return "\\" + n.charCodeAt(0).toString(16) + " ";
        }
      );
    }
    function so(e, n) {
      n.checked === void 0 || n.defaultChecked === void 0 || e0 || (console.error(
        "%s contains an input of type %s with both checked and defaultChecked props. Input elements must be either controlled or uncontrolled (specify either the checked prop, or the defaultChecked prop, but not both). Decide between using a controlled or uncontrolled input element and remove one of these props. More info: https://react.dev/link/controlled-components",
        sd() || "A component",
        n.type
      ), e0 = !0), n.value === void 0 || n.defaultValue === void 0 || t0 || (console.error(
        "%s contains an input of type %s with both value and defaultValue props. Input elements must be either controlled or uncontrolled (specify either the value prop, or the defaultValue prop, but not both). Decide between using a controlled or uncontrolled input element and remove one of these props. More info: https://react.dev/link/controlled-components",
        sd() || "A component",
        n.type
      ), t0 = !0);
    }
    function oo(e, n, s, f, m, p, T, C) {
      e.name = "", T != null && typeof T != "function" && typeof T != "symbol" && typeof T != "boolean" ? (Q(T, "type"), e.type = T) : e.removeAttribute("type"), n != null ? T === "number" ? (n === 0 && e.value === "" || e.value != n) && (e.value = "" + Xn(n)) : e.value !== "" + Xn(n) && (e.value = "" + Xn(n)) : T !== "submit" && T !== "reset" || e.removeAttribute("value"), n != null ? od(e, T, Xn(n)) : s != null ? od(e, T, Xn(s)) : f != null && e.removeAttribute("value"), m == null && p != null && (e.defaultChecked = !!p), m != null && (e.checked = m && typeof m != "function" && typeof m != "symbol"), C != null && typeof C != "function" && typeof C != "symbol" && typeof C != "boolean" ? (Q(C, "name"), e.name = "" + Xn(C)) : e.removeAttribute("name");
    }
    function G_(e, n, s, f, m, p, T, C) {
      if (p != null && typeof p != "function" && typeof p != "symbol" && typeof p != "boolean" && (Q(p, "type"), e.type = p), n != null || s != null) {
        if (!(p !== "submit" && p !== "reset" || n != null))
          return;
        s = s != null ? "" + Xn(s) : "", n = n != null ? "" + Xn(n) : s, C || n === e.value || (e.value = n), e.defaultValue = n;
      }
      f = f ?? m, f = typeof f != "function" && typeof f != "symbol" && !!f, e.checked = C ? e.checked : !!f, e.defaultChecked = !!f, T != null && typeof T != "function" && typeof T != "symbol" && typeof T != "boolean" && (Q(T, "name"), e.name = T);
    }
    function od(e, n, s) {
      n === "number" && af(e.ownerDocument) === e || e.defaultValue === "" + s || (e.defaultValue = "" + s);
    }
    function ly(e, n) {
      n.value == null && (typeof n.children == "object" && n.children !== null ? oh.Children.forEach(n.children, function(s) {
        s == null || typeof s == "string" || typeof s == "number" || typeof s == "bigint" || i0 || (i0 = !0, console.error(
          "Cannot infer the option value of complex children. Pass a `value` prop or use a plain string as children to <option>."
        ));
      }) : n.dangerouslySetInnerHTML == null || Zg || (Zg = !0, console.error(
        "Pass a `value` prop if you set dangerouslyInnerHTML so React knows which value should be selected."
      ))), n.selected == null || n0 || (console.error(
        "Use the `defaultValue` or `value` props on <select> instead of setting `selected` on <option>."
      ), n0 = !0);
    }
    function H_() {
      var e = sd();
      return e ? `

Check the render method of \`` + e + "`." : "";
    }
    function _s(e, n, s, f) {
      if (e = e.options, n) {
        n = {};
        for (var m = 0; m < s.length; m++)
          n["$" + s[m]] = !0;
        for (s = 0; s < e.length; s++)
          m = n.hasOwnProperty("$" + e[s].value), e[s].selected !== m && (e[s].selected = m), m && f && (e[s].defaultSelected = !0);
      } else {
        for (s = "" + Xn(s), n = null, m = 0; m < e.length; m++) {
          if (e[m].value === s) {
            e[m].selected = !0, f && (e[m].defaultSelected = !0);
            return;
          }
          n !== null || e[m].disabled || (n = e[m]);
        }
        n !== null && (n.selected = !0);
      }
    }
    function sf(e, n) {
      for (e = 0; e < yh.length; e++) {
        var s = yh[e];
        if (n[s] != null) {
          var f = Ft(n[s]);
          n.multiple && !f ? console.error(
            "The `%s` prop supplied to <select> must be an array if `multiple` is true.%s",
            s,
            H_()
          ) : !n.multiple && f && console.error(
            "The `%s` prop supplied to <select> must be a scalar value if `multiple` is false.%s",
            s,
            H_()
          );
        }
      }
      n.value === void 0 || n.defaultValue === void 0 || l0 || (console.error(
        "Select elements must be either controlled or uncontrolled (specify either the value prop, or the defaultValue prop, but not both). Decide between using a controlled or uncontrolled select element and remove one of these props. More info: https://react.dev/link/controlled-components"
      ), l0 = !0);
    }
    function Ea(e, n) {
      n.value === void 0 || n.defaultValue === void 0 || Yv || (console.error(
        "%s contains a textarea with both value and defaultValue props. Textarea elements must be either controlled or uncontrolled (specify either the value prop, or the defaultValue prop, but not both). Decide between using a controlled or uncontrolled textarea and remove one of these props. More info: https://react.dev/link/controlled-components",
        sd() || "A component"
      ), Yv = !0), n.children != null && n.value == null && console.error(
        "Use the `defaultValue` or `value` props instead of setting children on <textarea>."
      );
    }
    function rd(e, n, s) {
      if (n != null && (n = "" + Xn(n), n !== e.value && (e.value = n), s == null)) {
        e.defaultValue !== n && (e.defaultValue = n);
        return;
      }
      e.defaultValue = s != null ? "" + Xn(s) : "";
    }
    function ay(e, n, s, f) {
      if (n == null) {
        if (f != null) {
          if (s != null)
            throw Error(
              "If you supply `defaultValue` on a <textarea>, do not pass children."
            );
          if (Ft(f)) {
            if (1 < f.length)
              throw Error("<textarea> can only have at most one child.");
            f = f[0];
          }
          s = f;
        }
        s == null && (s = ""), n = s;
      }
      s = Xn(n), e.defaultValue = s, f = e.textContent, f === s && f !== "" && f !== null && (e.value = f);
    }
    function nr(e, n) {
      return e.serverProps === void 0 && e.serverTail.length === 0 && e.children.length === 1 && 3 < e.distanceFromLeaf && e.distanceFromLeaf > 15 - n ? nr(e.children[0], n) : e;
    }
    function gi(e) {
      return "  " + "  ".repeat(e);
    }
    function ro(e) {
      return "+ " + "  ".repeat(e);
    }
    function ir(e) {
      return "- " + "  ".repeat(e);
    }
    function sy(e) {
      switch (e.tag) {
        case 26:
        case 27:
        case 5:
          return e.type;
        case 16:
          return "Lazy";
        case 13:
          return "Suspense";
        case 19:
          return "SuspenseList";
        case 0:
        case 15:
          return e = e.type, e.displayName || e.name || null;
        case 11:
          return e = e.type.render, e.displayName || e.name || null;
        case 1:
          return e = e.type, e.displayName || e.name || null;
        default:
          return null;
      }
    }
    function ii(e, n) {
      return jv.test(e) ? (e = JSON.stringify(e), e.length > n - 2 ? 8 > n ? '{"..."}' : "{" + e.slice(0, n - 7) + '..."}' : "{" + e + "}") : e.length > n ? 5 > n ? '{"..."}' : e.slice(0, n - 3) + "..." : e;
    }
    function of(e, n, s) {
      var f = 120 - 2 * s;
      if (n === null)
        return ro(s) + ii(e, f) + `
`;
      if (typeof n == "string") {
        for (var m = 0; m < n.length && m < e.length && n.charCodeAt(m) === e.charCodeAt(m); m++) ;
        return m > f - 8 && 10 < m && (e = "..." + e.slice(m - 8), n = "..." + n.slice(m - 8)), ro(s) + ii(e, f) + `
` + ir(s) + ii(n, f) + `
`;
      }
      return gi(s) + ii(e, f) + `
`;
    }
    function oy(e) {
      return Object.prototype.toString.call(e).replace(/^\[object (.*)\]$/, function(n, s) {
        return s;
      });
    }
    function uo(e, n) {
      switch (typeof e) {
        case "string":
          return e = JSON.stringify(e), e.length > n ? 5 > n ? '"..."' : e.slice(0, n - 4) + '..."' : e;
        case "object":
          if (e === null) return "null";
          if (Ft(e)) return "[...]";
          if (e.$$typeof === Ho)
            return (n = ft(e.type)) ? "<" + n + ">" : "<...>";
          var s = oy(e);
          if (s === "Object") {
            s = "", n -= 2;
            for (var f in e)
              if (e.hasOwnProperty(f)) {
                var m = JSON.stringify(f);
                if (m !== '"' + f + '"' && (f = m), n -= f.length - 2, m = uo(
                  e[f],
                  15 > n ? n : 15
                ), n -= m.length, 0 > n) {
                  s += s === "" ? "..." : ", ...";
                  break;
                }
                s += (s === "" ? "" : ",") + f + ":" + m;
              }
            return "{" + s + "}";
          }
          return s;
        case "function":
          return (n = e.displayName || e.name) ? "function " + n : "function";
        default:
          return String(e);
      }
    }
    function lr(e, n) {
      return typeof e != "string" || jv.test(e) ? "{" + uo(e, n - 2) + "}" : e.length > n - 2 ? 5 > n ? '"..."' : '"' + e.slice(0, n - 5) + '..."' : '"' + e + '"';
    }
    function xu(e, n, s) {
      var f = 120 - s.length - e.length, m = [], p;
      for (p in n)
        if (n.hasOwnProperty(p) && p !== "children") {
          var T = lr(
            n[p],
            120 - s.length - p.length - 1
          );
          f -= p.length + T.length + 2, m.push(p + "=" + T);
        }
      return m.length === 0 ? s + "<" + e + `>
` : 0 < f ? s + "<" + e + " " + m.join(" ") + `>
` : s + "<" + e + `
` + s + "  " + m.join(`
` + s + "  ") + `
` + s + `>
`;
    }
    function DS(e, n, s) {
      var f = "", m = ee({}, n), p;
      for (p in e)
        if (e.hasOwnProperty(p)) {
          delete m[p];
          var T = 120 - 2 * s - p.length - 2, C = uo(e[p], T);
          n.hasOwnProperty(p) ? (T = uo(n[p], T), f += ro(s) + p + ": " + C + `
`, f += ir(s) + p + ": " + T + `
`) : f += ro(s) + p + ": " + C + `
`;
        }
      for (var D in m)
        m.hasOwnProperty(D) && (e = uo(
          m[D],
          120 - 2 * s - D.length - 2
        ), f += ir(s) + D + ": " + e + `
`);
      return f;
    }
    function bl(e, n, s, f) {
      var m = "", p = /* @__PURE__ */ new Map();
      for (z in s)
        s.hasOwnProperty(z) && p.set(
          z.toLowerCase(),
          z
        );
      if (p.size === 1 && p.has("children"))
        m += xu(
          e,
          n,
          gi(f)
        );
      else {
        for (var T in n)
          if (n.hasOwnProperty(T) && T !== "children") {
            var C = 120 - 2 * (f + 1) - T.length - 1, D = p.get(T.toLowerCase());
            if (D !== void 0) {
              p.delete(T.toLowerCase());
              var z = n[T];
              D = s[D];
              var tt = lr(
                z,
                C
              );
              C = lr(
                D,
                C
              ), typeof z == "object" && z !== null && typeof D == "object" && D !== null && oy(z) === "Object" && oy(D) === "Object" && (2 < Object.keys(z).length || 2 < Object.keys(D).length || -1 < tt.indexOf("...") || -1 < C.indexOf("...")) ? m += gi(f + 1) + T + `={{
` + DS(
                z,
                D,
                f + 2
              ) + gi(f + 1) + `}}
` : (m += ro(f + 1) + T + "=" + tt + `
`, m += ir(f + 1) + T + "=" + C + `
`);
            } else
              m += gi(f + 1) + T + "=" + lr(n[T], C) + `
`;
          }
        p.forEach(function(st) {
          if (st !== "children") {
            var J = 120 - 2 * (f + 1) - st.length - 1;
            m += ir(f + 1) + st + "=" + lr(s[st], J) + `
`;
          }
        }), m = m === "" ? gi(f) + "<" + e + `>
` : gi(f) + "<" + e + `
` + m + gi(f) + `>
`;
      }
      return e = s.children, n = n.children, typeof e == "string" || typeof e == "number" || typeof e == "bigint" ? (p = "", (typeof n == "string" || typeof n == "number" || typeof n == "bigint") && (p = "" + n), m += of(p, "" + e, f + 1)) : (typeof n == "string" || typeof n == "number" || typeof n == "bigint") && (m = e == null ? m + of("" + n, null, f + 1) : m + of("" + n, void 0, f + 1)), m;
    }
    function ud(e, n) {
      var s = sy(e);
      if (s === null) {
        for (s = "", e = e.child; e; )
          s += ud(e, n), e = e.sibling;
        return s;
      }
      return gi(n) + "<" + s + `>
`;
    }
    function cd(e, n) {
      var s = nr(e, n);
      if (s !== e && (e.children.length !== 1 || e.children[0] !== s))
        return gi(n) + `...
` + cd(s, n + 1);
      s = "";
      var f = e.fiber._debugInfo;
      if (f)
        for (var m = 0; m < f.length; m++) {
          var p = f[m].name;
          typeof p == "string" && (s += gi(n) + "<" + p + `>
`, n++);
        }
      if (f = "", m = e.fiber.pendingProps, e.fiber.tag === 6)
        f = of(m, e.serverProps, n), n++;
      else if (p = sy(e.fiber), p !== null)
        if (e.serverProps === void 0) {
          f = n;
          var T = 120 - 2 * f - p.length - 2, C = "";
          for (z in m)
            if (m.hasOwnProperty(z) && z !== "children") {
              var D = lr(m[z], 15);
              if (T -= z.length + D.length + 2, 0 > T) {
                C += " ...";
                break;
              }
              C += " " + z + "=" + D;
            }
          f = gi(f) + "<" + p + C + `>
`, n++;
        } else
          e.serverProps === null ? (f = xu(
            p,
            m,
            ro(n)
          ), n++) : typeof e.serverProps == "string" ? console.error(
            "Should not have matched a non HostText fiber to a Text node. This is a bug in React."
          ) : (f = bl(
            p,
            m,
            e.serverProps,
            n
          ), n++);
      var z = "";
      for (m = e.fiber.child, p = 0; m && p < e.children.length; )
        T = e.children[p], T.fiber === m ? (z += cd(T, n), p++) : z += ud(m, n), m = m.sibling;
      for (m && 0 < e.children.length && (z += gi(n) + `...
`), m = e.serverTail, e.serverProps === null && n--, e = 0; e < m.length; e++)
        p = m[e], z = typeof p == "string" ? z + (ir(n) + ii(p, 120 - 2 * n) + `
`) : z + xu(
          p.type,
          p.props,
          ir(n)
        );
      return s + f + z;
    }
    function rf(e) {
      try {
        return `

` + cd(e, 0);
      } catch {
        return "";
      }
    }
    function ar(e, n, s) {
      for (var f = n, m = null, p = 0; f; )
        f === e && (p = 0), m = {
          fiber: f,
          children: m !== null ? [m] : [],
          serverProps: f === n ? s : f === e ? null : void 0,
          serverTail: [],
          distanceFromLeaf: p
        }, p++, f = f.return;
      return m !== null ? rf(m).replaceAll(/^[+-]/gm, ">") : "";
    }
    function ry(e, n) {
      var s = ee({}, e || o0), f = { tag: n };
      return Fg.indexOf(n) !== -1 && (s.aTagInScope = null, s.buttonTagInScope = null, s.nobrTagInScope = null), Kg.indexOf(n) !== -1 && (s.pTagInButtonScope = null), a0.indexOf(n) !== -1 && n !== "address" && n !== "div" && n !== "p" && (s.listItemTagAutoclosing = null, s.dlItemTagAutoclosing = null), s.current = f, n === "form" && (s.formTag = f), n === "a" && (s.aTagInScope = f), n === "button" && (s.buttonTagInScope = f), n === "nobr" && (s.nobrTagInScope = f), n === "p" && (s.pTagInButtonScope = f), n === "li" && (s.listItemTagAutoclosing = f), (n === "dd" || n === "dt") && (s.dlItemTagAutoclosing = f), n === "#document" || n === "html" ? s.containerTagInScope = null : s.containerTagInScope || (s.containerTagInScope = f), e !== null || n !== "#document" && n !== "html" && n !== "body" ? s.implicitRootScope === !0 && (s.implicitRootScope = !1) : s.implicitRootScope = !0, s;
    }
    function uy(e, n, s) {
      switch (n) {
        case "select":
          return e === "hr" || e === "option" || e === "optgroup" || e === "script" || e === "template" || e === "#text";
        case "optgroup":
          return e === "option" || e === "#text";
        case "option":
          return e === "#text";
        case "tr":
          return e === "th" || e === "td" || e === "style" || e === "script" || e === "template";
        case "tbody":
        case "thead":
        case "tfoot":
          return e === "tr" || e === "style" || e === "script" || e === "template";
        case "colgroup":
          return e === "col" || e === "template";
        case "table":
          return e === "caption" || e === "colgroup" || e === "tbody" || e === "tfoot" || e === "thead" || e === "style" || e === "script" || e === "template";
        case "head":
          return e === "base" || e === "basefont" || e === "bgsound" || e === "link" || e === "meta" || e === "title" || e === "noscript" || e === "noframes" || e === "style" || e === "script" || e === "template";
        case "html":
          if (s) break;
          return e === "head" || e === "body" || e === "frameset";
        case "frameset":
          return e === "frame";
        case "#document":
          if (!s) return e === "html";
      }
      switch (e) {
        case "h1":
        case "h2":
        case "h3":
        case "h4":
        case "h5":
        case "h6":
          return n !== "h1" && n !== "h2" && n !== "h3" && n !== "h4" && n !== "h5" && n !== "h6";
        case "rp":
        case "rt":
          return s0.indexOf(n) === -1;
        case "caption":
        case "col":
        case "colgroup":
        case "frameset":
        case "frame":
        case "tbody":
        case "td":
        case "tfoot":
        case "th":
        case "thead":
        case "tr":
          return n == null;
        case "head":
          return s || n === null;
        case "html":
          return s && n === "#document" || n === null;
        case "body":
          return s && (n === "#document" || n === "html") || n === null;
      }
      return !0;
    }
    function Cu(e, n) {
      switch (e) {
        case "address":
        case "article":
        case "aside":
        case "blockquote":
        case "center":
        case "details":
        case "dialog":
        case "dir":
        case "div":
        case "dl":
        case "fieldset":
        case "figcaption":
        case "figure":
        case "footer":
        case "header":
        case "hgroup":
        case "main":
        case "menu":
        case "nav":
        case "ol":
        case "p":
        case "section":
        case "summary":
        case "ul":
        case "pre":
        case "listing":
        case "table":
        case "hr":
        case "xmp":
        case "h1":
        case "h2":
        case "h3":
        case "h4":
        case "h5":
        case "h6":
          return n.pTagInButtonScope;
        case "form":
          return n.formTag || n.pTagInButtonScope;
        case "li":
          return n.listItemTagAutoclosing;
        case "dd":
        case "dt":
          return n.dlItemTagAutoclosing;
        case "button":
          return n.buttonTagInScope;
        case "a":
          return n.aTagInScope;
        case "nobr":
          return n.nobrTagInScope;
      }
      return null;
    }
    function Y_(e, n) {
      for (; e; ) {
        switch (e.tag) {
          case 5:
          case 26:
          case 27:
            if (e.type === n) return e;
        }
        e = e.return;
      }
      return null;
    }
    function fd(e, n) {
      n = n || o0;
      var s = n.current;
      if (n = (s = uy(
        e,
        s && s.tag,
        n.implicitRootScope
      ) ? null : s) ? null : Cu(e, n), n = s || n, !n) return !0;
      var f = n.tag;
      if (n = String(!!s) + "|" + e + "|" + f, Ec[n]) return !1;
      Ec[n] = !0;
      var m = (n = ol) ? Y_(n.return, f) : null, p = n !== null && m !== null ? ar(m, n, null) : "", T = "<" + e + ">";
      return s ? (s = "", f === "table" && e === "tr" && (s += " Add a <tbody>, <thead> or <tfoot> to your code to match the DOM tree generated by the browser."), console.error(
        `In HTML, %s cannot be a child of <%s>.%s
This will cause a hydration error.%s`,
        T,
        f,
        s,
        p
      )) : console.error(
        `In HTML, %s cannot be a descendant of <%s>.
This will cause a hydration error.%s`,
        T,
        f,
        p
      ), n && (e = n.return, m === null || e === null || m === e && e._debugOwner === n._debugOwner || wt(m, function() {
        console.error(
          `<%s> cannot contain a nested %s.
See this log for the ancestor stack trace.`,
          f,
          T
        );
      })), !1;
    }
    function uf(e, n, s) {
      if (s || uy("#text", n, !1))
        return !0;
      if (s = "#text|" + n, Ec[s]) return !1;
      Ec[s] = !0;
      var f = (s = ol) ? Y_(s, n) : null;
      return s = s !== null && f !== null ? ar(
        f,
        s,
        s.tag !== 6 ? { children: null } : null
      ) : "", /\S/.test(e) ? console.error(
        `In HTML, text nodes cannot be a child of <%s>.
This will cause a hydration error.%s`,
        n,
        s
      ) : console.error(
        `In HTML, whitespace text nodes cannot be a child of <%s>. Make sure you don't have any extra whitespace between tags on each line of your source code.
This will cause a hydration error.%s`,
        n,
        s
      ), !1;
    }
    function sr(e, n) {
      if (n) {
        var s = e.firstChild;
        if (s && s === e.lastChild && s.nodeType === 3) {
          s.nodeValue = n;
          return;
        }
      }
      e.textContent = n;
    }
    function OS(e) {
      return e.replace(Vo, function(n, s) {
        return s.toUpperCase();
      });
    }
    function j_(e, n, s) {
      var f = n.indexOf("--") === 0;
      f || (-1 < n.indexOf("-") ? qr.hasOwnProperty(n) && qr[n] || (qr[n] = !0, console.error(
        "Unsupported style property %s. Did you mean %s?",
        n,
        OS(n.replace(_h, "ms-"))
      )) : ph.test(n) ? qr.hasOwnProperty(n) && qr[n] || (qr[n] = !0, console.error(
        "Unsupported vendor-prefixed style property %s. Did you mean %s?",
        n,
        n.charAt(0).toUpperCase() + n.slice(1)
      )) : !Bv.test(s) || Zr.hasOwnProperty(s) && Zr[s] || (Zr[s] = !0, console.error(
        `Style property values shouldn't contain a semicolon. Try "%s: %s" instead.`,
        n,
        s.replace(Bv, "")
      )), typeof s == "number" && (isNaN(s) ? Iv || (Iv = !0, console.error(
        "`NaN` is an invalid value for the `%s` css style property.",
        n
      )) : isFinite(s) || r0 || (r0 = !0, console.error(
        "`Infinity` is an invalid value for the `%s` css style property.",
        n
      )))), s == null || typeof s == "boolean" || s === "" ? f ? e.setProperty(n, "") : n === "float" ? e.cssFloat = "" : e[n] = "" : f ? e.setProperty(n, s) : typeof s != "number" || s === 0 || vh.has(n) ? n === "float" ? e.cssFloat = s : (At(s, n), e[n] = ("" + s).trim()) : e[n] = s + "px";
    }
    function cf(e, n, s) {
      if (n != null && typeof n != "object")
        throw Error(
          "The `style` prop expects a mapping from style properties to values, not a string. For example, style={{marginRight: spacing + 'em'}} when using JSX."
        );
      if (n && Object.freeze(n), e = e.style, s != null) {
        if (n) {
          var f = {};
          if (s) {
            for (var m in s)
              if (s.hasOwnProperty(m) && !n.hasOwnProperty(m))
                for (var p = Wa[m] || [m], T = 0; T < p.length; T++)
                  f[p[T]] = m;
          }
          for (var C in n)
            if (n.hasOwnProperty(C) && (!s || s[C] !== n[C]))
              for (m = Wa[C] || [C], p = 0; p < m.length; p++)
                f[m[p]] = C;
          C = {};
          for (var D in n)
            for (m = Wa[D] || [D], p = 0; p < m.length; p++)
              C[m[p]] = D;
          D = {};
          for (var z in f)
            if (m = f[z], (p = C[z]) && m !== p && (T = m + "," + p, !D[T])) {
              D[T] = !0, T = console;
              var tt = n[m];
              T.error.call(
                T,
                "%s a style property during rerender (%s) when a conflicting property is set (%s) can lead to styling bugs. To avoid this, don't mix shorthand and non-shorthand properties for the same value; instead, replace the shorthand with separate values.",
                tt == null || typeof tt == "boolean" || tt === "" ? "Removing" : "Updating",
                m,
                p
              );
            }
        }
        for (var st in s)
          !s.hasOwnProperty(st) || n != null && n.hasOwnProperty(st) || (st.indexOf("--") === 0 ? e.setProperty(st, "") : st === "float" ? e.cssFloat = "" : e[st] = "");
        for (var J in n)
          z = n[J], n.hasOwnProperty(J) && s[J] !== z && j_(e, J, z);
      } else
        for (f in n)
          n.hasOwnProperty(f) && j_(e, f, n[f]);
    }
    function or(e) {
      if (e.indexOf("-") === -1) return !1;
      switch (e) {
        case "annotation-xml":
        case "color-profile":
        case "font-face":
        case "font-face-src":
        case "font-face-uri":
        case "font-face-format":
        case "font-face-name":
        case "missing-glyph":
          return !1;
        default:
          return !0;
      }
    }
    function hd(e) {
      return Pg.get(e) || e;
    }
    function Ru(e, n) {
      if (js.call(Ja, n) && Ja[n])
        return !0;
      if (Qg.test(n)) {
        if (e = "aria-" + n.slice(4).toLowerCase(), e = u0.hasOwnProperty(e) ? e : null, e == null)
          return console.error(
            "Invalid ARIA attribute `%s`. ARIA attributes follow the pattern aria-* and must be lowercase.",
            n
          ), Ja[n] = !0;
        if (n !== e)
          return console.error(
            "Invalid ARIA attribute `%s`. Did you mean `%s`?",
            n,
            e
          ), Ja[n] = !0;
      }
      if (c0.test(n)) {
        if (e = n.toLowerCase(), e = u0.hasOwnProperty(e) ? e : null, e == null) return Ja[n] = !0, !1;
        n !== e && (console.error(
          "Unknown ARIA attribute `%s`. Did you mean `%s`?",
          n,
          e
        ), Ja[n] = !0);
      }
      return !0;
    }
    function Au(e, n) {
      var s = [], f;
      for (f in n)
        Ru(e, f) || s.push(f);
      n = s.map(function(m) {
        return "`" + m + "`";
      }).join(", "), s.length === 1 ? console.error(
        "Invalid aria prop %s on <%s> tag. For details, see https://react.dev/link/invalid-aria-props",
        n,
        e
      ) : 1 < s.length && console.error(
        "Invalid aria props %s on <%s> tag. For details, see https://react.dev/link/invalid-aria-props",
        n,
        e
      );
    }
    function B_(e, n, s, f) {
      if (js.call(vi, n) && vi[n])
        return !0;
      var m = n.toLowerCase();
      if (m === "onfocusin" || m === "onfocusout")
        return console.error(
          "React uses onFocus and onBlur instead of onFocusIn and onFocusOut. All React events are normalized to bubble, so onFocusIn and onFocusOut are not needed/supported by React."
        ), vi[n] = !0;
      if (typeof s == "function" && (e === "form" && n === "action" || e === "input" && n === "formAction" || e === "button" && n === "formAction"))
        return !0;
      if (f != null) {
        if (e = f.possibleRegistrationNames, f.registrationNameDependencies.hasOwnProperty(n))
          return !0;
        if (f = e.hasOwnProperty(m) ? e[m] : null, f != null)
          return console.error(
            "Invalid event handler property `%s`. Did you mean `%s`?",
            n,
            f
          ), vi[n] = !0;
        if (Eh.test(n))
          return console.error(
            "Unknown event handler property `%s`. It will be ignored.",
            n
          ), vi[n] = !0;
      } else if (Eh.test(n))
        return l.test(n) && console.error(
          "Invalid event handler property `%s`. React events use the camelCase naming convention, for example `onClick`.",
          n
        ), vi[n] = !0;
      if (o.test(n) || c.test(n)) return !0;
      if (m === "innerhtml")
        return console.error(
          "Directly setting property `innerHTML` is not permitted. For more information, lookup documentation on `dangerouslySetInnerHTML`."
        ), vi[n] = !0;
      if (m === "aria")
        return console.error(
          "The `aria` attribute is reserved for future use in React. Pass individual `aria-` attributes instead."
        ), vi[n] = !0;
      if (m === "is" && s !== null && s !== void 0 && typeof s != "string")
        return console.error(
          "Received a `%s` for a string attribute `is`. If this is expected, cast the value to a string.",
          typeof s
        ), vi[n] = !0;
      if (typeof s == "number" && isNaN(s))
        return console.error(
          "Received NaN for the `%s` attribute. If this is expected, cast the value to a string.",
          n
        ), vi[n] = !0;
      if (Fr.hasOwnProperty(m)) {
        if (m = Fr[m], m !== n)
          return console.error(
            "Invalid DOM property `%s`. Did you mean `%s`?",
            n,
            m
          ), vi[n] = !0;
      } else if (n !== m)
        return console.error(
          "React does not recognize the `%s` prop on a DOM element. If you intentionally want it to appear in the DOM as a custom attribute, spell it as lowercase `%s` instead. If you accidentally passed it from a parent component, remove it from the DOM element.",
          n,
          m
        ), vi[n] = !0;
      switch (n) {
        case "dangerouslySetInnerHTML":
        case "children":
        case "style":
        case "suppressContentEditableWarning":
        case "suppressHydrationWarning":
        case "defaultValue":
        case "defaultChecked":
        case "innerHTML":
        case "ref":
          return !0;
        case "innerText":
        case "textContent":
          return !0;
      }
      switch (typeof s) {
        case "boolean":
          switch (n) {
            case "autoFocus":
            case "checked":
            case "multiple":
            case "muted":
            case "selected":
            case "contentEditable":
            case "spellCheck":
            case "draggable":
            case "value":
            case "autoReverse":
            case "externalResourcesRequired":
            case "focusable":
            case "preserveAlpha":
            case "allowFullScreen":
            case "async":
            case "autoPlay":
            case "controls":
            case "default":
            case "defer":
            case "disabled":
            case "disablePictureInPicture":
            case "disableRemotePlayback":
            case "formNoValidate":
            case "hidden":
            case "loop":
            case "noModule":
            case "noValidate":
            case "open":
            case "playsInline":
            case "readOnly":
            case "required":
            case "reversed":
            case "scoped":
            case "seamless":
            case "itemScope":
            case "capture":
            case "download":
            case "inert":
              return !0;
            default:
              return m = n.toLowerCase().slice(0, 5), m === "data-" || m === "aria-" ? !0 : (s ? console.error(
                'Received `%s` for a non-boolean attribute `%s`.\n\nIf you want to write it to the DOM, pass a string instead: %s="%s" or %s={value.toString()}.',
                s,
                n,
                n,
                s,
                n
              ) : console.error(
                'Received `%s` for a non-boolean attribute `%s`.\n\nIf you want to write it to the DOM, pass a string instead: %s="%s" or %s={value.toString()}.\n\nIf you used to conditionally omit it with %s={condition && value}, pass %s={condition ? value : undefined} instead.',
                s,
                n,
                n,
                s,
                n,
                n,
                n
              ), vi[n] = !0);
          }
        case "function":
        case "symbol":
          return vi[n] = !0, !1;
        case "string":
          if (s === "false" || s === "true") {
            switch (n) {
              case "checked":
              case "selected":
              case "multiple":
              case "muted":
              case "allowFullScreen":
              case "async":
              case "autoPlay":
              case "controls":
              case "default":
              case "defer":
              case "disabled":
              case "disablePictureInPicture":
              case "disableRemotePlayback":
              case "formNoValidate":
              case "hidden":
              case "loop":
              case "noModule":
              case "noValidate":
              case "open":
              case "playsInline":
              case "readOnly":
              case "required":
              case "reversed":
              case "scoped":
              case "seamless":
              case "itemScope":
              case "inert":
                break;
              default:
                return !0;
            }
            console.error(
              "Received the string `%s` for the boolean attribute `%s`. %s Did you mean %s={%s}?",
              s,
              n,
              s === "false" ? "The browser will interpret it as a truthy value." : 'Although this works, it will not work as expected if you pass the string "false".',
              n,
              s
            ), vi[n] = !0;
          }
      }
      return !0;
    }
    function cy(e, n, s) {
      var f = [], m;
      for (m in n)
        B_(e, m, n[m], s) || f.push(m);
      n = f.map(function(p) {
        return "`" + p + "`";
      }).join(", "), f.length === 1 ? console.error(
        "Invalid value for prop %s on <%s> tag. Either remove it from the element, or pass a string or number value to keep it in the DOM. For details, see https://react.dev/link/attribute-behavior ",
        n,
        e
      ) : 1 < f.length && console.error(
        "Invalid values for props %s on <%s> tag. Either remove them from the element, or pass a string or number value to keep them in the DOM. For details, see https://react.dev/link/attribute-behavior ",
        n,
        e
      );
    }
    function Mu(e) {
      return d.test("" + e) ? "javascript:throw new Error('React has blocked a javascript: URL as a security precaution.')" : e;
    }
    function rr(e) {
      return e = e.target || e.srcElement || window, e.correspondingUseElement && (e = e.correspondingUseElement), e.nodeType === 3 ? e.parentNode : e;
    }
    function ba(e) {
      var n = ni(e);
      if (n && (e = n.stateNode)) {
        var s = e[Bi] || null;
        t: switch (e = n.stateNode, n.type) {
          case "input":
            if (oo(
              e,
              s.value,
              s.defaultValue,
              s.defaultValue,
              s.checked,
              s.defaultChecked,
              s.type,
              s.name
            ), n = s.name, s.type === "radio" && n != null) {
              for (s = e; s.parentNode; ) s = s.parentNode;
              for (Q(n, "name"), s = s.querySelectorAll(
                'input[name="' + Ji(
                  "" + n
                ) + '"][type="radio"]'
              ), n = 0; n < s.length; n++) {
                var f = s[n];
                if (f !== e && f.form === e.form) {
                  var m = f[Bi] || null;
                  if (!m)
                    throw Error(
                      "ReactDOMInput: Mixing React and non-React radio inputs with the same `name` is not supported."
                    );
                  oo(
                    f,
                    m.value,
                    m.defaultValue,
                    m.defaultValue,
                    m.checked,
                    m.defaultChecked,
                    m.type,
                    m.name
                  );
                }
              }
              for (n = 0; n < s.length; n++)
                f = s[n], f.form === e.form && Tn(f);
            }
            break t;
          case "textarea":
            rd(e, s.value, s.defaultValue);
            break t;
          case "select":
            n = s.value, n != null && _s(e, !!s.multiple, n, !1);
        }
      }
    }
    function dd(e, n, s) {
      if (M) return e(n, s);
      M = !0;
      try {
        var f = e(n);
        return f;
      } finally {
        if (M = !1, (S !== null || A !== null) && (zr(), S && (n = S, e = A, A = S = null, ba(n), e)))
          for (n = 0; n < e.length; n++) ba(e[n]);
      }
    }
    function vs(e, n) {
      var s = e.stateNode;
      if (s === null) return null;
      var f = s[Bi] || null;
      if (f === null) return null;
      s = f[n];
      t: switch (n) {
        case "onClick":
        case "onClickCapture":
        case "onDoubleClick":
        case "onDoubleClickCapture":
        case "onMouseDown":
        case "onMouseDownCapture":
        case "onMouseMove":
        case "onMouseMoveCapture":
        case "onMouseUp":
        case "onMouseUpCapture":
        case "onMouseEnter":
          (f = !f.disabled) || (e = e.type, f = !(e === "button" || e === "input" || e === "select" || e === "textarea")), e = !f;
          break t;
        default:
          e = !1;
      }
      if (e) return null;
      if (s && typeof s != "function")
        throw Error(
          "Expected `" + n + "` listener to be a function, instead got a value of `" + typeof s + "` type."
        );
      return s;
    }
    function Ss() {
      if (et) return et;
      var e, n = $, s = n.length, f, m = "value" in gt ? gt.value : gt.textContent, p = m.length;
      for (e = 0; e < s && n[e] === m[e]; e++) ;
      var T = s - e;
      for (f = 1; f <= T && n[s - f] === m[p - f]; f++) ;
      return et = m.slice(e, 1 < f ? 1 - f : void 0);
    }
    function wu(e) {
      var n = e.keyCode;
      return "charCode" in e ? (e = e.charCode, e === 0 && n === 13 && (e = 13)) : e = n, e === 10 && (e = 13), 32 <= e || e === 13 ? e : 0;
    }
    function ur() {
      return !0;
    }
    function fy() {
      return !1;
    }
    function kn(e) {
      function n(s, f, m, p, T) {
        this._reactName = s, this._targetInst = m, this.type = f, this.nativeEvent = p, this.target = T, this.currentTarget = null;
        for (var C in e)
          e.hasOwnProperty(C) && (s = e[C], this[C] = s ? s(p) : p[C]);
        return this.isDefaultPrevented = (p.defaultPrevented != null ? p.defaultPrevented : p.returnValue === !1) ? ur : fy, this.isPropagationStopped = fy, this;
      }
      return ee(n.prototype, {
        preventDefault: function() {
          this.defaultPrevented = !0;
          var s = this.nativeEvent;
          s && (s.preventDefault ? s.preventDefault() : typeof s.returnValue != "unknown" && (s.returnValue = !1), this.isDefaultPrevented = ur);
        },
        stopPropagation: function() {
          var s = this.nativeEvent;
          s && (s.stopPropagation ? s.stopPropagation() : typeof s.cancelBubble != "unknown" && (s.cancelBubble = !0), this.isPropagationStopped = ur);
        },
        persist: function() {
        },
        isPersistent: ur
      }), n;
    }
    function gd(e) {
      var n = this.nativeEvent;
      return n.getModifierState ? n.getModifierState(e) : (e = qA[e]) ? !!n[e] : !1;
    }
    function md() {
      return gd;
    }
    function mi(e, n) {
      switch (e) {
        case "keyup":
          return iM.indexOf(n.keyCode) !== -1;
        case "keydown":
          return n.keyCode !== PT;
        case "keypress":
        case "mousedown":
        case "focusout":
          return !0;
        default:
          return !1;
      }
    }
    function co(e) {
      return e = e.detail, typeof e == "object" && "data" in e ? e.data : null;
    }
    function yd(e, n) {
      switch (e) {
        case "compositionend":
          return co(n);
        case "keypress":
          return n.which !== WT ? null : ($T = !0, JT);
        case "textInput":
          return e = n.data, e === JT && $T ? null : e;
        default:
          return null;
      }
    }
    function ff(e, n) {
      if (Wg)
        return e === "compositionend" || !QS && mi(e, n) ? (e = Ss(), et = $ = gt = null, Wg = !1, e) : null;
      switch (e) {
        case "paste":
          return null;
        case "keypress":
          if (!(n.ctrlKey || n.altKey || n.metaKey) || n.ctrlKey && n.altKey) {
            if (n.char && 1 < n.char.length)
              return n.char;
            if (n.which)
              return String.fromCharCode(n.which);
          }
          return null;
        case "compositionend":
          return QT && n.locale !== "ko" ? null : n.data;
        default:
          return null;
      }
    }
    function I_(e) {
      var n = e && e.nodeName && e.nodeName.toLowerCase();
      return n === "input" ? !!aM[e.type] : n === "textarea";
    }
    function hy(e) {
      if (!G) return !1;
      e = "on" + e;
      var n = e in document;
      return n || (n = document.createElement("div"), n.setAttribute(e, "return;"), n = typeof n[e] == "function"), n;
    }
    function pd(e, n, s, f) {
      S ? A ? A.push(f) : A = [f] : S = f, n = Jf(n, "onChange"), 0 < n.length && (s = new Ht(
        "onChange",
        "change",
        null,
        s,
        f
      ), e.push({ event: s, listeners: n }));
    }
    function hf(e) {
      ka(e, 0);
    }
    function cr(e) {
      var n = Sl(e);
      if (Tn(n)) return e;
    }
    function dy(e, n) {
      if (e === "change") return n;
    }
    function X_() {
      d0 && (d0.detachEvent("onpropertychange", k_), g0 = d0 = null);
    }
    function k_(e) {
      if (e.propertyName === "value" && cr(g0)) {
        var n = [];
        pd(
          n,
          g0,
          e,
          rr(e)
        ), dd(hf, n);
      }
    }
    function LS(e, n, s) {
      e === "focusin" ? (X_(), d0 = n, g0 = s, d0.attachEvent("onpropertychange", k_)) : e === "focusout" && X_();
    }
    function gy(e) {
      if (e === "selectionchange" || e === "keyup" || e === "keydown")
        return cr(g0);
    }
    function zS(e, n) {
      if (e === "click") return cr(n);
    }
    function NS(e, n) {
      if (e === "input" || e === "change")
        return cr(n);
    }
    function US(e, n) {
      return e === n && (e !== 0 || 1 / e === 1 / n) || e !== e && n !== n;
    }
    function df(e, n) {
      if (rl(e, n)) return !0;
      if (typeof e != "object" || e === null || typeof n != "object" || n === null)
        return !1;
      var s = Object.keys(e), f = Object.keys(n);
      if (s.length !== f.length) return !1;
      for (f = 0; f < s.length; f++) {
        var m = s[f];
        if (!js.call(n, m) || !rl(e[m], n[m]))
          return !1;
      }
      return !0;
    }
    function V_(e) {
      for (; e && e.firstChild; ) e = e.firstChild;
      return e;
    }
    function my(e, n) {
      var s = V_(e);
      e = 0;
      for (var f; s; ) {
        if (s.nodeType === 3) {
          if (f = e + s.textContent.length, e <= n && f >= n)
            return { node: s, offset: n - e };
          e = f;
        }
        t: {
          for (; s; ) {
            if (s.nextSibling) {
              s = s.nextSibling;
              break t;
            }
            s = s.parentNode;
          }
          s = void 0;
        }
        s = V_(s);
      }
    }
    function q_(e, n) {
      return e && n ? e === n ? !0 : e && e.nodeType === 3 ? !1 : n && n.nodeType === 3 ? q_(e, n.parentNode) : "contains" in e ? e.contains(n) : e.compareDocumentPosition ? !!(e.compareDocumentPosition(n) & 16) : !1 : !1;
    }
    function Z_(e) {
      e = e != null && e.ownerDocument != null && e.ownerDocument.defaultView != null ? e.ownerDocument.defaultView : window;
      for (var n = af(e.document); n instanceof e.HTMLIFrameElement; ) {
        try {
          var s = typeof n.contentWindow.location.href == "string";
        } catch {
          s = !1;
        }
        if (s) e = n.contentWindow;
        else break;
        n = af(e.document);
      }
      return n;
    }
    function yy(e) {
      var n = e && e.nodeName && e.nodeName.toLowerCase();
      return n && (n === "input" && (e.type === "text" || e.type === "search" || e.type === "tel" || e.type === "url" || e.type === "password") || n === "textarea" || e.contentEditable === "true");
    }
    function F_(e, n, s) {
      var f = s.window === s ? s.document : s.nodeType === 9 ? s : s.ownerDocument;
      JS || Jg == null || Jg !== af(f) || (f = Jg, "selectionStart" in f && yy(f) ? f = { start: f.selectionStart, end: f.selectionEnd } : (f = (f.ownerDocument && f.ownerDocument.defaultView || window).getSelection(), f = {
        anchorNode: f.anchorNode,
        anchorOffset: f.anchorOffset,
        focusNode: f.focusNode,
        focusOffset: f.focusOffset
      }), m0 && df(m0, f) || (m0 = f, f = Jf(WS, "onSelect"), 0 < f.length && (n = new Ht(
        "onSelect",
        "select",
        null,
        n,
        s
      ), e.push({ event: n, listeners: f }), n.target = Jg)));
    }
    function Es(e, n) {
      var s = {};
      return s[e.toLowerCase()] = n.toLowerCase(), s["Webkit" + e] = "webkit" + n, s["Moz" + e] = "moz" + n, s;
    }
    function fr(e) {
      if ($S[e]) return $S[e];
      if (!$g[e]) return e;
      var n = $g[e], s;
      for (s in n)
        if (n.hasOwnProperty(s) && s in ex)
          return $S[e] = n[s];
      return e;
    }
    function Jl(e, n) {
      sx.set(e, n), El(n, [e]);
    }
    function $i(e, n) {
      if (typeof e == "object" && e !== null) {
        var s = eE.get(e);
        return s !== void 0 ? s : (n = {
          value: e,
          source: n,
          stack: Ai(n)
        }, eE.set(e, n), n);
      }
      return {
        value: e,
        source: n,
        stack: Ai(n)
      };
    }
    function gf() {
      for (var e = tm, n = nE = tm = 0; n < e; ) {
        var s = $a[n];
        $a[n++] = null;
        var f = $a[n];
        $a[n++] = null;
        var m = $a[n];
        $a[n++] = null;
        var p = $a[n];
        if ($a[n++] = null, f !== null && m !== null) {
          var T = f.pending;
          T === null ? m.next = m : (m.next = T.next, T.next = m), f.pending = m;
        }
        p !== 0 && K_(s, m, p);
      }
    }
    function _d(e, n, s, f) {
      $a[tm++] = e, $a[tm++] = n, $a[tm++] = s, $a[tm++] = f, nE |= f, e.lanes |= f, e = e.alternate, e !== null && (e.lanes |= f);
    }
    function py(e, n, s, f) {
      return _d(e, n, s, f), vd(e);
    }
    function Mi(e, n) {
      return _d(e, null, null, n), vd(e);
    }
    function K_(e, n, s) {
      e.lanes |= s;
      var f = e.alternate;
      f !== null && (f.lanes |= s);
      for (var m = !1, p = e.return; p !== null; )
        p.childLanes |= s, f = p.alternate, f !== null && (f.childLanes |= s), p.tag === 22 && (e = p.stateNode, e === null || e._visibility & Xv || (m = !0)), e = p, p = p.return;
      return e.tag === 3 ? (p = e.stateNode, m && n !== null && (m = 31 - oi(s), e = p.hiddenUpdates, f = e[m], f === null ? e[m] = [n] : f.push(n), n.lane = s | 536870912), p) : null;
    }
    function vd(e) {
      if (H0 > MM)
        throw Nh = H0 = 0, Y0 = LE = null, Error(
          "Maximum update depth exceeded. This can happen when a component repeatedly calls setState inside componentWillUpdate or componentDidUpdate. React limits the number of nested updates to prevent infinite loops."
        );
      Nh > wM && (Nh = 0, Y0 = null, console.error(
        "Maximum update depth exceeded. This can happen when a component calls setState inside useEffect, but useEffect either doesn't have a dependency array, or one of the dependencies changes on every render."
      )), e.alternate === null && e.flags & 4098 && ca(e);
      for (var n = e, s = n.return; s !== null; )
        n.alternate === null && n.flags & 4098 && ca(e), n = s, s = n.return;
      return n.tag === 3 ? n.stateNode : null;
    }
    function hr(e) {
      if (ts === null) return e;
      var n = ts(e);
      return n === void 0 ? e : n.current;
    }
    function _y(e) {
      if (ts === null) return e;
      var n = ts(e);
      return n === void 0 ? e != null && typeof e.render == "function" && (n = hr(e.render), e.render !== n) ? (n = { $$typeof: Hs, render: n }, e.displayName !== void 0 && (n.displayName = e.displayName), n) : e : n.current;
    }
    function P_(e, n) {
      if (ts === null) return !1;
      var s = e.elementType;
      n = n.type;
      var f = !1, m = typeof n == "object" && n !== null ? n.$$typeof : null;
      switch (e.tag) {
        case 1:
          typeof n == "function" && (f = !0);
          break;
        case 0:
          (typeof n == "function" || m === sl) && (f = !0);
          break;
        case 11:
          (m === Hs || m === sl) && (f = !0);
          break;
        case 14:
        case 15:
          (m === uh || m === sl) && (f = !0);
          break;
        default:
          return !1;
      }
      return !!(f && (e = ts(s), e !== void 0 && e === ts(n)));
    }
    function Q_(e) {
      ts !== null && typeof WeakSet == "function" && (em === null && (em = /* @__PURE__ */ new WeakSet()), em.add(e));
    }
    function mf(e, n, s) {
      var f = e.alternate, m = e.child, p = e.sibling, T = e.tag, C = e.type, D = null;
      switch (T) {
        case 0:
        case 15:
        case 1:
          D = C;
          break;
        case 11:
          D = C.render;
      }
      if (ts === null)
        throw Error("Expected resolveFamily to be set during hot reload.");
      var z = !1;
      C = !1, D !== null && (D = ts(D), D !== void 0 && (s.has(D) ? C = !0 : n.has(D) && (T === 1 ? C = !0 : z = !0))), em !== null && (em.has(e) || f !== null && em.has(f)) && (C = !0), C && (e._debugNeedsRemount = !0), (C || z) && (f = Mi(e, 2), f !== null && rn(f, e, 2)), m === null || C || mf(
        m,
        n,
        s
      ), p !== null && mf(
        p,
        n,
        s
      );
    }
    function yf(e, n, s, f) {
      this.tag = e, this.key = s, this.sibling = this.child = this.return = this.stateNode = this.type = this.elementType = null, this.index = 0, this.refCleanup = this.ref = null, this.pendingProps = n, this.dependencies = this.memoizedState = this.updateQueue = this.memoizedProps = null, this.mode = f, this.subtreeFlags = this.flags = 0, this.deletions = null, this.childLanes = this.lanes = 0, this.alternate = null, this.actualDuration = -0, this.actualStartTime = -1.1, this.treeBaseDuration = this.selfBaseDuration = -0, this._debugTask = this._debugStack = this._debugOwner = this._debugInfo = null, this._debugNeedsRemount = !1, this._debugHookTypes = null, rx || typeof Object.preventExtensions != "function" || Object.preventExtensions(this);
    }
    function vy(e) {
      return e = e.prototype, !(!e || !e.isReactComponent);
    }
    function Ta(e, n) {
      var s = e.alternate;
      switch (s === null ? (s = R(
        e.tag,
        n,
        e.key,
        e.mode
      ), s.elementType = e.elementType, s.type = e.type, s.stateNode = e.stateNode, s._debugOwner = e._debugOwner, s._debugStack = e._debugStack, s._debugTask = e._debugTask, s._debugHookTypes = e._debugHookTypes, s.alternate = e, e.alternate = s) : (s.pendingProps = n, s.type = e.type, s.flags = 0, s.subtreeFlags = 0, s.deletions = null, s.actualDuration = -0, s.actualStartTime = -1.1), s.flags = e.flags & 65011712, s.childLanes = e.childLanes, s.lanes = e.lanes, s.child = e.child, s.memoizedProps = e.memoizedProps, s.memoizedState = e.memoizedState, s.updateQueue = e.updateQueue, n = e.dependencies, s.dependencies = n === null ? null : {
        lanes: n.lanes,
        firstContext: n.firstContext,
        _debugThenableState: n._debugThenableState
      }, s.sibling = e.sibling, s.index = e.index, s.ref = e.ref, s.refCleanup = e.refCleanup, s.selfBaseDuration = e.selfBaseDuration, s.treeBaseDuration = e.treeBaseDuration, s._debugInfo = e._debugInfo, s._debugNeedsRemount = e._debugNeedsRemount, s.tag) {
        case 0:
        case 15:
          s.type = hr(e.type);
          break;
        case 1:
          s.type = hr(e.type);
          break;
        case 11:
          s.type = _y(e.type);
      }
      return s;
    }
    function Sy(e, n) {
      e.flags &= 65011714;
      var s = e.alternate;
      return s === null ? (e.childLanes = 0, e.lanes = n, e.child = null, e.subtreeFlags = 0, e.memoizedProps = null, e.memoizedState = null, e.updateQueue = null, e.dependencies = null, e.stateNode = null, e.selfBaseDuration = 0, e.treeBaseDuration = 0) : (e.childLanes = s.childLanes, e.lanes = s.lanes, e.child = s.child, e.subtreeFlags = 0, e.deletions = null, e.memoizedProps = s.memoizedProps, e.memoizedState = s.memoizedState, e.updateQueue = s.updateQueue, e.type = s.type, n = s.dependencies, e.dependencies = n === null ? null : {
        lanes: n.lanes,
        firstContext: n.firstContext,
        _debugThenableState: n._debugThenableState
      }, e.selfBaseDuration = s.selfBaseDuration, e.treeBaseDuration = s.treeBaseDuration), e;
    }
    function Sd(e, n, s, f, m, p) {
      var T = 0, C = e;
      if (typeof e == "function")
        vy(e) && (T = 1), C = hr(C);
      else if (typeof e == "string")
        T = Y(), T = rc(e, s, T) ? 26 : e === "html" || e === "head" || e === "body" ? 27 : 5;
      else
        t: switch (e) {
          case Fp:
            return n = R(31, s, n, m), n.elementType = Fp, n.lanes = p, n;
          case Qt:
            return fo(
              s.children,
              m,
              p,
              n
            );
          case cc:
            T = 8, m |= Xi, m |= Vs;
            break;
          case fc:
            return e = s, f = m, typeof e.id != "string" && console.error(
              'Profiler must specify an "id" of type `string` as a prop. Received the type `%s` instead.',
              typeof e.id
            ), n = R(12, e, n, f | Si), n.elementType = fc, n.lanes = p, n.stateNode = { effectDuration: 0, passiveEffectDuration: 0 }, n;
          case hc:
            return n = R(13, s, n, m), n.elementType = hc, n.lanes = p, n;
          case Yo:
            return n = R(19, s, n, m), n.elementType = Yo, n.lanes = p, n;
          default:
            if (typeof e == "object" && e !== null)
              switch (e.$$typeof) {
                case Zp:
                case Ul:
                  T = 10;
                  break t;
                case Ug:
                  T = 9;
                  break t;
                case Hs:
                  T = 11, C = _y(C);
                  break t;
                case uh:
                  T = 14;
                  break t;
                case sl:
                  T = 16, C = null;
                  break t;
              }
            C = "", (e === void 0 || typeof e == "object" && e !== null && Object.keys(e).length === 0) && (C += " You likely forgot to export your component from the file it's defined in, or you might have mixed up default and named imports."), e === null ? s = "null" : Ft(e) ? s = "array" : e !== void 0 && e.$$typeof === Ho ? (s = "<" + (ft(e.type) || "Unknown") + " />", C = " Did you accidentally export a JSX literal instead of a component?") : s = typeof e, (T = f ? yt(f) : null) && (C += `

Check the render method of \`` + T + "`."), T = 29, s = Error(
              "Element type is invalid: expected a string (for built-in components) or a class/function (for composite components) but got: " + (s + "." + C)
            ), C = null;
        }
      return n = R(T, s, n, m), n.elementType = e, n.type = C, n.lanes = p, n._debugOwner = f, n;
    }
    function pf(e, n, s) {
      return n = Sd(
        e.type,
        e.key,
        e.props,
        e._owner,
        n,
        s
      ), n._debugOwner = e._owner, n._debugStack = e._debugStack, n._debugTask = e._debugTask, n;
    }
    function fo(e, n, s, f) {
      return e = R(7, e, f, n), e.lanes = s, e;
    }
    function ho(e, n, s) {
      return e = R(6, e, null, n), e.lanes = s, e;
    }
    function Ey(e, n, s) {
      return n = R(
        4,
        e.children !== null ? e.children : [],
        e.key,
        n
      ), n.lanes = s, n.stateNode = {
        containerInfo: e.containerInfo,
        pendingChildren: null,
        implementation: e.implementation
      }, n;
    }
    function dr(e, n) {
      $l(), nm[im++] = Vv, nm[im++] = kv, kv = e, Vv = n;
    }
    function W_(e, n, s) {
      $l(), es[ns++] = Pr, es[ns++] = Qr, es[ns++] = bh, bh = e;
      var f = Pr;
      e = Qr;
      var m = 32 - oi(f) - 1;
      f &= ~(1 << m), s += 1;
      var p = 32 - oi(n) + m;
      if (30 < p) {
        var T = m - m % 5;
        p = (f & (1 << T) - 1).toString(32), f >>= T, m -= T, Pr = 1 << 32 - oi(n) + m | s << m | f, Qr = p + e;
      } else
        Pr = 1 << p | s << m | f, Qr = e;
    }
    function Ed(e) {
      $l(), e.return !== null && (dr(e, 1), W_(e, 1, 0));
    }
    function bd(e) {
      for (; e === kv; )
        kv = nm[--im], nm[im] = null, Vv = nm[--im], nm[im] = null;
      for (; e === bh; )
        bh = es[--ns], es[ns] = null, Qr = es[--ns], es[ns] = null, Pr = es[--ns], es[ns] = null;
    }
    function $l() {
      xe || console.error(
        "Expected to be hydrating. This is a bug in React. Please file an issue."
      );
    }
    function ta(e, n) {
      if (e.return === null) {
        if (is === null)
          is = {
            fiber: e,
            children: [],
            serverProps: void 0,
            serverTail: [],
            distanceFromLeaf: n
          };
        else {
          if (is.fiber !== e)
            throw Error(
              "Saw multiple hydration diff roots in a pass. This is a bug in React."
            );
          is.distanceFromLeaf > n && (is.distanceFromLeaf = n);
        }
        return is;
      }
      var s = ta(
        e.return,
        n + 1
      ).children;
      return 0 < s.length && s[s.length - 1].fiber === e ? (s = s[s.length - 1], s.distanceFromLeaf > n && (s.distanceFromLeaf = n), s) : (n = {
        fiber: e,
        children: [],
        serverProps: void 0,
        serverTail: [],
        distanceFromLeaf: n
      }, s.push(n), n);
    }
    function by(e, n) {
      Wr || (e = ta(e, 0), e.serverProps = null, n !== null && (n = Cg(n), e.serverTail.push(n)));
    }
    function xa(e) {
      var n = "", s = is;
      throw s !== null && (is = null, n = rf(s)), Du(
        $i(
          Error(
            `Hydration failed because the server rendered HTML didn't match the client. As a result this tree will be regenerated on the client. This can happen if a SSR-ed Client Component used:

- A server/client branch \`if (typeof window !== 'undefined')\`.
- Variable input such as \`Date.now()\` or \`Math.random()\` which changes each time it's called.
- Date formatting in a user's locale which doesn't match the server.
- External changing data without sending a snapshot of it along with the HTML.
- Invalid HTML tag nesting.

It can also happen if the client has a browser extension installed which messes with the HTML before React loaded.

https://react.dev/link/hydration-mismatch` + n
          ),
          e
        )
      ), iE;
    }
    function Ty(e) {
      var n = e.stateNode, s = e.type, f = e.memoizedProps;
      switch (n[ri] = e, n[Bi] = f, Va(s, f), s) {
        case "dialog":
          oe("cancel", n), oe("close", n);
          break;
        case "iframe":
        case "object":
        case "embed":
          oe("load", n);
          break;
        case "video":
        case "audio":
          for (s = 0; s < j0.length; s++)
            oe(j0[s], n);
          break;
        case "source":
          oe("error", n);
          break;
        case "img":
        case "image":
        case "link":
          oe("error", n), oe("load", n);
          break;
        case "details":
          oe("toggle", n);
          break;
        case "input":
          Ci("input", f), oe("invalid", n), so(n, f), G_(
            n,
            f.value,
            f.defaultValue,
            f.checked,
            f.defaultChecked,
            f.type,
            f.name,
            !0
          ), ps(n);
          break;
        case "option":
          ly(n, f);
          break;
        case "select":
          Ci("select", f), oe("invalid", n), sf(n, f);
          break;
        case "textarea":
          Ci("textarea", f), oe("invalid", n), Ea(n, f), ay(
            n,
            f.value,
            f.defaultValue,
            f.children
          ), ps(n);
      }
      s = f.children, typeof s != "string" && typeof s != "number" && typeof s != "bigint" || n.textContent === "" + s || f.suppressHydrationWarning === !0 || Cp(n.textContent, s) ? (f.popover != null && (oe("beforetoggle", n), oe("toggle", n)), f.onScroll != null && oe("scroll", n), f.onScrollEnd != null && oe("scrollend", n), f.onClick != null && (n.onclick = zs), n = !0) : n = !1, n || xa(e);
    }
    function xy(e) {
      for (ul = e.return; ul; )
        switch (ul.tag) {
          case 5:
          case 13:
            Zo = !1;
            return;
          case 27:
          case 3:
            Zo = !0;
            return;
          default:
            ul = ul.return;
        }
    }
    function gr(e) {
      if (e !== ul) return !1;
      if (!xe)
        return xy(e), xe = !0, !1;
      var n = e.tag, s;
      if ((s = n !== 3 && n !== 27) && ((s = n === 5) && (s = e.type, s = !(s !== "form" && s !== "button") || qa(e.type, e.memoizedProps)), s = !s), s && Sn) {
        for (s = Sn; s; ) {
          var f = ta(e, 0), m = Cg(s);
          f.serverTail.push(m), s = m.type === "Suspense" ? zp(s) : Kn(s.nextSibling);
        }
        xa(e);
      }
      if (xy(e), n === 13) {
        if (e = e.memoizedState, e = e !== null ? e.dehydrated : null, !e)
          throw Error(
            "Expected to have a hydrated suspense instance. This error is likely caused by a bug in React. Please file an issue."
          );
        Sn = zp(e);
      } else
        n === 27 ? (n = Sn, Za(e.type) ? (e = VE, VE = null, Sn = e) : Sn = n) : Sn = ul ? Kn(e.stateNode.nextSibling) : null;
      return !0;
    }
    function mr() {
      Sn = ul = null, Wr = xe = !1;
    }
    function Cy() {
      var e = Th;
      return e !== null && (hl === null ? hl = e : hl.push.apply(
        hl,
        e
      ), Th = null), e;
    }
    function Du(e) {
      Th === null ? Th = [e] : Th.push(e);
    }
    function Ry() {
      var e = is;
      if (e !== null) {
        is = null;
        for (var n = rf(e); 0 < e.children.length; )
          e = e.children[0];
        wt(e.fiber, function() {
          console.error(
            `A tree hydrated but some attributes of the server rendered HTML didn't match the client properties. This won't be patched up. This can happen if a SSR-ed Client Component used:

- A server/client branch \`if (typeof window !== 'undefined')\`.
- Variable input such as \`Date.now()\` or \`Math.random()\` which changes each time it's called.
- Date formatting in a user's locale which doesn't match the server.
- External changing data without sending a snapshot of it along with the HTML.
- Invalid HTML tag nesting.

It can also happen if the client has a browser extension installed which messes with the HTML before React loaded.

%s%s`,
            "https://react.dev/link/hydration-mismatch",
            n
          );
        });
      }
    }
    function Td() {
      lm = qv = null, am = !1;
    }
    function go(e, n, s) {
      pt(lE, n._currentValue, e), n._currentValue = s, pt(aE, n._currentRenderer, e), n._currentRenderer !== void 0 && n._currentRenderer !== null && n._currentRenderer !== hx && console.error(
        "Detected multiple renderers concurrently rendering the same context provider. This is currently unsupported."
      ), n._currentRenderer = hx;
    }
    function bs(e, n) {
      e._currentValue = lE.current;
      var s = aE.current;
      xt(aE, n), e._currentRenderer = s, xt(lE, n);
    }
    function Ay(e, n, s) {
      for (; e !== null; ) {
        var f = e.alternate;
        if ((e.childLanes & n) !== n ? (e.childLanes |= n, f !== null && (f.childLanes |= n)) : f !== null && (f.childLanes & n) !== n && (f.childLanes |= n), e === s) break;
        e = e.return;
      }
      e !== s && console.error(
        "Expected to find the propagation root when scheduling context work. This error is likely caused by a bug in React. Please file an issue."
      );
    }
    function My(e, n, s, f) {
      var m = e.child;
      for (m !== null && (m.return = e); m !== null; ) {
        var p = m.dependencies;
        if (p !== null) {
          var T = m.child;
          p = p.firstContext;
          t: for (; p !== null; ) {
            var C = p;
            p = m;
            for (var D = 0; D < n.length; D++)
              if (C.context === n[D]) {
                p.lanes |= s, C = p.alternate, C !== null && (C.lanes |= s), Ay(
                  p.return,
                  s,
                  e
                ), f || (T = null);
                break t;
              }
            p = C.next;
          }
        } else if (m.tag === 18) {
          if (T = m.return, T === null)
            throw Error(
              "We just came from a parent so we must have had a parent. This is a bug in React."
            );
          T.lanes |= s, p = T.alternate, p !== null && (p.lanes |= s), Ay(
            T,
            s,
            e
          ), T = null;
        } else T = m.child;
        if (T !== null) T.return = m;
        else
          for (T = m; T !== null; ) {
            if (T === e) {
              T = null;
              break;
            }
            if (m = T.sibling, m !== null) {
              m.return = T.return, T = m;
              break;
            }
            T = T.return;
          }
        m = T;
      }
    }
    function Vn(e, n, s, f) {
      e = null;
      for (var m = n, p = !1; m !== null; ) {
        if (!p) {
          if (m.flags & 524288) p = !0;
          else if (m.flags & 262144) break;
        }
        if (m.tag === 10) {
          var T = m.alternate;
          if (T === null)
            throw Error("Should have a current fiber. This is a bug in React.");
          if (T = T.memoizedProps, T !== null) {
            var C = m.type;
            rl(m.pendingProps.value, T.value) || (e !== null ? e.push(C) : e = [C]);
          }
        } else if (m === gc.current) {
          if (T = m.alternate, T === null)
            throw Error("Should have a current fiber. This is a bug in React.");
          T.memoizedState.memoizedState !== m.memoizedState.memoizedState && (e !== null ? e.push(k0) : e = [k0]);
        }
        m = m.return;
      }
      e !== null && My(
        n,
        e,
        s,
        f
      ), n.flags |= 262144;
    }
    function mo(e) {
      for (e = e.firstContext; e !== null; ) {
        if (!rl(
          e.context._currentValue,
          e.memoizedValue
        ))
          return !0;
        e = e.next;
      }
      return !1;
    }
    function yo(e) {
      qv = e, lm = null, e = e.dependencies, e !== null && (e.firstContext = null);
    }
    function Ke(e) {
      return am && console.error(
        "Context can only be read while React is rendering. In classes, you can read it in the render method or getDerivedStateFromProps. In function components, you can read it directly in the function body, but not inside Hooks like useReducer() or useMemo()."
      ), wy(qv, e);
    }
    function _f(e, n) {
      return qv === null && yo(e), wy(e, n);
    }
    function wy(e, n) {
      var s = n._currentValue;
      if (n = { context: n, memoizedValue: s, next: null }, lm === null) {
        if (e === null)
          throw Error(
            "Context can only be read while React is rendering. In classes, you can read it in the render method or getDerivedStateFromProps. In function components, you can read it directly in the function body, but not inside Hooks like useReducer() or useMemo()."
          );
        lm = n, e.dependencies = {
          lanes: 0,
          firstContext: n,
          _debugThenableState: null
        }, e.flags |= 524288;
      } else lm = lm.next = n;
      return s;
    }
    function vf() {
      return {
        controller: new dM(),
        data: /* @__PURE__ */ new Map(),
        refCount: 0
      };
    }
    function yr(e) {
      e.controller.signal.aborted && console.warn(
        "A cache instance was retained after it was already freed. This likely indicates a bug in React."
      ), e.refCount++;
    }
    function Ca(e) {
      e.refCount--, 0 > e.refCount && console.warn(
        "A cache instance was released after it was already freed. This likely indicates a bug in React."
      ), e.refCount === 0 && gM(mM, function() {
        e.controller.abort();
      });
    }
    function ea() {
      var e = xh;
      return xh = 0, e;
    }
    function po(e) {
      var n = xh;
      return xh = e, n;
    }
    function pr(e) {
      var n = xh;
      return xh += e, n;
    }
    function xd(e) {
      Yl = sm(), 0 > e.actualStartTime && (e.actualStartTime = Yl);
    }
    function Ts(e) {
      if (0 <= Yl) {
        var n = sm() - Yl;
        e.actualDuration += n, e.selfBaseDuration = n, Yl = -1;
      }
    }
    function _r(e) {
      if (0 <= Yl) {
        var n = sm() - Yl;
        e.actualDuration += n, Yl = -1;
      }
    }
    function Tl() {
      if (0 <= Yl) {
        var e = sm() - Yl;
        Yl = -1, xh += e;
      }
    }
    function na() {
      Yl = sm();
    }
    function Ra(e) {
      for (var n = e.child; n; )
        e.actualDuration += n.actualDuration, n = n.sibling;
    }
    function J_(e, n) {
      if (y0 === null) {
        var s = y0 = [];
        sE = 0, Ch = Sp(), om = {
          status: "pending",
          value: void 0,
          then: function(f) {
            s.push(f);
          }
        };
      }
      return sE++, n.then(Dy, Dy), n;
    }
    function Dy() {
      if (--sE === 0 && y0 !== null) {
        om !== null && (om.status = "fulfilled");
        var e = y0;
        y0 = null, Ch = 0, om = null;
        for (var n = 0; n < e.length; n++) (0, e[n])();
      }
    }
    function $_(e, n) {
      var s = [], f = {
        status: "pending",
        value: null,
        reason: null,
        then: function(m) {
          s.push(m);
        }
      };
      return e.then(
        function() {
          f.status = "fulfilled", f.value = n;
          for (var m = 0; m < s.length; m++) (0, s[m])(n);
        },
        function(m) {
          for (f.status = "rejected", f.reason = m, m = 0; m < s.length; m++)
            (0, s[m])(void 0);
        }
      ), f;
    }
    function Oy() {
      var e = Rh.current;
      return e !== null ? e : Pe.pooledCache;
    }
    function Cd(e, n) {
      n === null ? pt(Rh, Rh.current, e) : pt(Rh, n.pool, e);
    }
    function tv() {
      var e = Oy();
      return e === null ? null : { parent: Wn._currentValue, pool: e };
    }
    function Ly() {
      return { didWarnAboutUncachedPromise: !1, thenables: [] };
    }
    function zy(e) {
      return e = e.status, e === "fulfilled" || e === "rejected";
    }
    function Ou() {
    }
    function xl(e, n, s) {
      lt.actQueue !== null && (lt.didUsePromise = !0);
      var f = e.thenables;
      switch (s = f[s], s === void 0 ? f.push(n) : s !== n && (e.didWarnAboutUncachedPromise || (e.didWarnAboutUncachedPromise = !0, console.error(
        "A component was suspended by an uncached promise. Creating promises inside a Client Component or hook is not yet supported, except via a Suspense-compatible library or framework."
      )), n.then(Ou, Ou), n = s), n.status) {
        case "fulfilled":
          return n.value;
        case "rejected":
          throw e = n.reason, tl(e), e;
        default:
          if (typeof n.status == "string")
            n.then(Ou, Ou);
          else {
            if (e = Pe, e !== null && 100 < e.shellSuspendCounter)
              throw Error(
                "An unknown Component is an async Client Component. Only Server Components can be async at the moment. This error is often caused by accidentally adding `'use client'` to a module that was originally written for the server."
              );
            e = n, e.status = "pending", e.then(
              function(m) {
                if (n.status === "pending") {
                  var p = n;
                  p.status = "fulfilled", p.value = m;
                }
              },
              function(m) {
                if (n.status === "pending") {
                  var p = n;
                  p.status = "rejected", p.reason = m;
                }
              }
            );
          }
          switch (n.status) {
            case "fulfilled":
              return n.value;
            case "rejected":
              throw e = n.reason, tl(e), e;
          }
          throw x0 = n, Wv = !0, T0;
      }
    }
    function Ny() {
      if (x0 === null)
        throw Error(
          "Expected a suspended thenable. This is a bug in React. Please file an issue."
        );
      var e = x0;
      return x0 = null, Wv = !1, e;
    }
    function tl(e) {
      if (e === T0 || e === Qv)
        throw Error(
          "Hooks are not supported inside an async component. This error is often caused by accidentally adding `'use client'` to a module that was originally written for the server."
        );
    }
    function wi(e) {
      e.updateQueue = {
        baseState: e.memoizedState,
        firstBaseUpdate: null,
        lastBaseUpdate: null,
        shared: { pending: null, lanes: 0, hiddenCallbacks: null },
        callbacks: null
      };
    }
    function _o(e, n) {
      e = e.updateQueue, n.updateQueue === e && (n.updateQueue = {
        baseState: e.baseState,
        firstBaseUpdate: e.firstBaseUpdate,
        lastBaseUpdate: e.lastBaseUpdate,
        shared: e.shared,
        callbacks: null
      });
    }
    function Aa(e) {
      return {
        lane: e,
        tag: px,
        payload: null,
        callback: null,
        next: null
      };
    }
    function ia(e, n, s) {
      var f = e.updateQueue;
      if (f === null) return null;
      if (f = f.shared, uE === f && !Sx) {
        var m = it(e);
        console.error(
          `An update (setState, replaceState, or forceUpdate) was scheduled from inside an update function. Update functions should be pure, with zero side-effects. Consider using componentDidUpdate or a callback.

Please update the following component: %s`,
          m
        ), Sx = !0;
      }
      return (ze & fl) !== da ? (m = f.pending, m === null ? n.next = n : (n.next = m.next, m.next = n), f.pending = n, n = vd(e), K_(e, null, s), n) : (_d(e, f, n, s), vd(e));
    }
    function vo(e, n, s) {
      if (n = n.updateQueue, n !== null && (n = n.shared, (s & 4194048) !== 0)) {
        var f = n.lanes;
        f &= e.pendingLanes, s |= f, n.lanes = s, Ql(e, s);
      }
    }
    function Lu(e, n) {
      var s = e.updateQueue, f = e.alternate;
      if (f !== null && (f = f.updateQueue, s === f)) {
        var m = null, p = null;
        if (s = s.firstBaseUpdate, s !== null) {
          do {
            var T = {
              lane: s.lane,
              tag: s.tag,
              payload: s.payload,
              callback: null,
              next: null
            };
            p === null ? m = p = T : p = p.next = T, s = s.next;
          } while (s !== null);
          p === null ? m = p = n : p = p.next = n;
        } else m = p = n;
        s = {
          baseState: f.baseState,
          firstBaseUpdate: m,
          lastBaseUpdate: p,
          shared: f.shared,
          callbacks: f.callbacks
        }, e.updateQueue = s;
        return;
      }
      e = s.lastBaseUpdate, e === null ? s.firstBaseUpdate = n : e.next = n, s.lastBaseUpdate = n;
    }
    function Ma() {
      if (cE) {
        var e = om;
        if (e !== null) throw e;
      }
    }
    function zu(e, n, s, f) {
      cE = !1;
      var m = e.updateQueue;
      xc = !1, uE = m.shared;
      var p = m.firstBaseUpdate, T = m.lastBaseUpdate, C = m.shared.pending;
      if (C !== null) {
        m.shared.pending = null;
        var D = C, z = D.next;
        D.next = null, T === null ? p = z : T.next = z, T = D;
        var tt = e.alternate;
        tt !== null && (tt = tt.updateQueue, C = tt.lastBaseUpdate, C !== T && (C === null ? tt.firstBaseUpdate = z : C.next = z, tt.lastBaseUpdate = D));
      }
      if (p !== null) {
        var st = m.baseState;
        T = 0, tt = z = D = null, C = p;
        do {
          var J = C.lane & -536870913, rt = J !== C.lane;
          if (rt ? (de & J) === J : (f & J) === J) {
            J !== 0 && J === Ch && (cE = !0), tt !== null && (tt = tt.next = {
              lane: 0,
              tag: C.tag,
              payload: C.payload,
              callback: null,
              next: null
            });
            t: {
              J = e;
              var Dt = C, qt = n, Qe = s;
              switch (Dt.tag) {
                case _x:
                  if (Dt = Dt.payload, typeof Dt == "function") {
                    am = !0;
                    var ye = Dt.call(
                      Qe,
                      st,
                      qt
                    );
                    if (J.mode & Xi) {
                      Xt(!0);
                      try {
                        Dt.call(Qe, st, qt);
                      } finally {
                        Xt(!1);
                      }
                    }
                    am = !1, st = ye;
                    break t;
                  }
                  st = Dt;
                  break t;
                case rE:
                  J.flags = J.flags & -65537 | 128;
                case px:
                  if (ye = Dt.payload, typeof ye == "function") {
                    if (am = !0, Dt = ye.call(
                      Qe,
                      st,
                      qt
                    ), J.mode & Xi) {
                      Xt(!0);
                      try {
                        ye.call(Qe, st, qt);
                      } finally {
                        Xt(!1);
                      }
                    }
                    am = !1;
                  } else Dt = ye;
                  if (Dt == null) break t;
                  st = ee({}, st, Dt);
                  break t;
                case vx:
                  xc = !0;
              }
            }
            J = C.callback, J !== null && (e.flags |= 64, rt && (e.flags |= 8192), rt = m.callbacks, rt === null ? m.callbacks = [J] : rt.push(J));
          } else
            rt = {
              lane: J,
              tag: C.tag,
              payload: C.payload,
              callback: C.callback,
              next: null
            }, tt === null ? (z = tt = rt, D = st) : tt = tt.next = rt, T |= J;
          if (C = C.next, C === null) {
            if (C = m.shared.pending, C === null)
              break;
            rt = C, C = rt.next, rt.next = null, m.lastBaseUpdate = rt, m.shared.pending = null;
          }
        } while (!0);
        tt === null && (D = st), m.baseState = D, m.firstBaseUpdate = z, m.lastBaseUpdate = tt, p === null && (m.shared.lanes = 0), Mc |= T, e.lanes = T, e.memoizedState = st;
      }
      uE = null;
    }
    function Sf(e, n) {
      if (typeof e != "function")
        throw Error(
          "Invalid argument passed as callback. Expected a function. Instead received: " + e
        );
      e.call(n);
    }
    function Nu(e, n) {
      var s = e.shared.hiddenCallbacks;
      if (s !== null)
        for (e.shared.hiddenCallbacks = null, e = 0; e < s.length; e++)
          Sf(s[e], n);
    }
    function ev(e, n) {
      var s = e.callbacks;
      if (s !== null)
        for (e.callbacks = null, e = 0; e < s.length; e++)
          Sf(s[e], n);
    }
    function Di(e, n) {
      var s = Po;
      pt(Jv, s, e), pt(rm, n, e), Po = s | n.baseLanes;
    }
    function Ef(e) {
      pt(Jv, Po, e), pt(
        rm,
        rm.current,
        e
      );
    }
    function la(e) {
      Po = Jv.current, xt(rm, e), xt(Jv, e);
    }
    function ne() {
      var e = at;
      ss === null ? ss = [e] : ss.push(e);
    }
    function _t() {
      var e = at;
      if (ss !== null && ($r++, ss[$r] !== e)) {
        var n = it(Kt);
        if (!Ex.has(n) && (Ex.add(n), ss !== null)) {
          for (var s = "", f = 0; f <= $r; f++) {
            var m = ss[f], p = f === $r ? e : m;
            for (m = f + 1 + ". " + m; 30 > m.length; )
              m += " ";
            m += p + `
`, s += m;
          }
          console.error(
            `React has detected a change in the order of Hooks called by %s. This will lead to bugs and errors if not fixed. For more information, read the Rules of Hooks: https://react.dev/link/rules-of-hooks

   Previous render            Next render
   ------------------------------------------------------
%s   ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
`,
            n,
            s
          );
        }
      }
    }
    function Cl(e) {
      e == null || Ft(e) || console.error(
        "%s received a final argument that is not an array (instead, received `%s`). When specified, the final argument must be an array.",
        at,
        typeof e
      );
    }
    function Uu() {
      var e = it(Kt);
      Tx.has(e) || (Tx.add(e), console.error(
        "ReactDOM.useFormState has been renamed to React.useActionState. Please update %s to use React.useActionState.",
        e
      ));
    }
    function en() {
      throw Error(
        `Invalid hook call. Hooks can only be called inside of the body of a function component. This could happen for one of the following reasons:
1. You might have mismatching versions of React and the renderer (such as React DOM)
2. You might be breaking the Rules of Hooks
3. You might have more than one copy of React in the same app
See https://react.dev/link/invalid-hook-call for tips about how to debug and fix this problem.`
      );
    }
    function So(e, n) {
      if (R0) return !1;
      if (n === null)
        return console.error(
          "%s received a final argument during this render, but not during the previous render. Even though the final argument is optional, its type cannot change between renders.",
          at
        ), !1;
      e.length !== n.length && console.error(
        `The final argument passed to %s changed size between renders. The order and size of this array must remain constant.

Previous: %s
Incoming: %s`,
        at,
        "[" + n.join(", ") + "]",
        "[" + e.join(", ") + "]"
      );
      for (var s = 0; s < n.length && s < e.length; s++)
        if (!rl(e[s], n[s])) return !1;
      return !0;
    }
    function Eo(e, n, s, f, m, p) {
      Cc = p, Kt = n, ss = e !== null ? e._debugHookTypes : null, $r = -1, R0 = e !== null && e.type !== n.type, (Object.prototype.toString.call(s) === "[object AsyncFunction]" || Object.prototype.toString.call(s) === "[object AsyncGeneratorFunction]") && (p = it(Kt), fE.has(p) || (fE.add(p), console.error(
        "%s is an async Client Component. Only Server Components can be async at the moment. This error is often caused by accidentally adding `'use client'` to a module that was originally written for the server.",
        p === null ? "An unknown Component" : "<" + p + ">"
      ))), n.memoizedState = null, n.updateQueue = null, n.lanes = 0, lt.H = e !== null && e.memoizedState !== null ? dE : ss !== null ? xx : hE, Mh = p = (n.mode & Xi) !== $e;
      var T = gE(s, f, m);
      if (Mh = !1, cm && (T = Gu(
        n,
        s,
        f,
        m
      )), p) {
        Xt(!0);
        try {
          T = Gu(
            n,
            s,
            f,
            m
          );
        } finally {
          Xt(!1);
        }
      }
      return bf(e, n), T;
    }
    function bf(e, n) {
      n._debugHookTypes = ss, n.dependencies === null ? Jr !== null && (n.dependencies = {
        lanes: 0,
        firstContext: null,
        _debugThenableState: Jr
      }) : n.dependencies._debugThenableState = Jr, lt.H = e1;
      var s = Ve !== null && Ve.next !== null;
      if (Cc = 0, ss = at = Hn = Ve = Kt = null, $r = -1, e !== null && (e.flags & 65011712) !== (n.flags & 65011712) && console.error(
        "Internal React error: Expected static flag was missing. Please notify the React team."
      ), $v = !1, C0 = 0, Jr = null, s)
        throw Error(
          "Rendered fewer hooks than expected. This may be caused by an accidental early return statement."
        );
      e === null || ui || (e = e.dependencies, e !== null && mo(e) && (ui = !0)), Wv ? (Wv = !1, e = !0) : e = !1, e && (n = it(n) || "Unknown", bx.has(n) || fE.has(n) || (bx.add(n), console.error(
        "`use` was called from inside a try/catch block. This is not allowed and can lead to unexpected behavior. To handle errors triggered by `use`, wrap your component in a error boundary."
      )));
    }
    function Gu(e, n, s, f) {
      Kt = e;
      var m = 0;
      do {
        if (cm && (Jr = null), C0 = 0, cm = !1, m >= pM)
          throw Error(
            "Too many re-renders. React limits the number of renders to prevent an infinite loop."
          );
        if (m += 1, R0 = !1, Hn = Ve = null, e.updateQueue != null) {
          var p = e.updateQueue;
          p.lastEffect = null, p.events = null, p.stores = null, p.memoCache != null && (p.memoCache.index = 0);
        }
        $r = -1, lt.H = Cx, p = gE(n, s, f);
      } while (cm);
      return p;
    }
    function Rl() {
      var e = lt.H, n = e.useState()[0];
      return n = typeof n.then == "function" ? vr(n) : n, e = e.useState()[0], (Ve !== null ? Ve.memoizedState : null) !== e && (Kt.flags |= 1024), n;
    }
    function Oi() {
      var e = t1 !== 0;
      return t1 = 0, e;
    }
    function xs(e, n, s) {
      n.updateQueue = e.updateQueue, n.flags = (n.mode & Vs) !== $e ? n.flags & -402655237 : n.flags & -2053, e.lanes &= ~s;
    }
    function aa(e) {
      if ($v) {
        for (e = e.memoizedState; e !== null; ) {
          var n = e.queue;
          n !== null && (n.pending = null), e = e.next;
        }
        $v = !1;
      }
      Cc = 0, ss = Hn = Ve = Kt = null, $r = -1, at = null, cm = !1, C0 = t1 = 0, Jr = null;
    }
    function sn() {
      var e = {
        memoizedState: null,
        baseState: null,
        baseQueue: null,
        queue: null,
        next: null
      };
      return Hn === null ? Kt.memoizedState = Hn = e : Hn = Hn.next = e, Hn;
    }
    function me() {
      if (Ve === null) {
        var e = Kt.alternate;
        e = e !== null ? e.memoizedState : null;
      } else e = Ve.next;
      var n = Hn === null ? Kt.memoizedState : Hn.next;
      if (n !== null)
        Hn = n, Ve = e;
      else {
        if (e === null)
          throw Kt.alternate === null ? Error(
            "Update hook called on initial render. This is likely a bug in React. Please file an issue."
          ) : Error("Rendered more hooks than during the previous render.");
        Ve = e, e = {
          memoizedState: Ve.memoizedState,
          baseState: Ve.baseState,
          baseQueue: Ve.baseQueue,
          queue: Ve.queue,
          next: null
        }, Hn === null ? Kt.memoizedState = Hn = e : Hn = Hn.next = e;
      }
      return Hn;
    }
    function Rd() {
      return { lastEffect: null, events: null, stores: null, memoCache: null };
    }
    function vr(e) {
      var n = C0;
      return C0 += 1, Jr === null && (Jr = Ly()), e = xl(Jr, e, n), n = Kt, (Hn === null ? n.memoizedState : Hn.next) === null && (n = n.alternate, lt.H = n !== null && n.memoizedState !== null ? dE : hE), e;
    }
    function wa(e) {
      if (e !== null && typeof e == "object") {
        if (typeof e.then == "function") return vr(e);
        if (e.$$typeof === Ul) return Ke(e);
      }
      throw Error("An unsupported type was passed to use(): " + String(e));
    }
    function mn(e) {
      var n = null, s = Kt.updateQueue;
      if (s !== null && (n = s.memoCache), n == null) {
        var f = Kt.alternate;
        f !== null && (f = f.updateQueue, f !== null && (f = f.memoCache, f != null && (n = {
          data: f.data.map(function(m) {
            return m.slice();
          }),
          index: 0
        })));
      }
      if (n == null && (n = { data: [], index: 0 }), s === null && (s = Rd(), Kt.updateQueue = s), s.memoCache = n, s = n.data[n.index], s === void 0 || R0)
        for (s = n.data[n.index] = Array(e), f = 0; f < e; f++)
          s[f] = Dv;
      else
        s.length !== e && console.error(
          "Expected a constant size argument for each invocation of useMemoCache. The previous cache was allocated with size %s but size %s was requested.",
          s.length,
          e
        );
      return n.index++, s;
    }
    function Ee(e, n) {
      return typeof n == "function" ? n(e) : n;
    }
    function ve(e, n, s) {
      var f = sn();
      if (s !== void 0) {
        var m = s(n);
        if (Mh) {
          Xt(!0);
          try {
            s(n);
          } finally {
            Xt(!1);
          }
        }
      } else m = n;
      return f.memoizedState = f.baseState = m, e = {
        pending: null,
        lanes: 0,
        dispatch: null,
        lastRenderedReducer: e,
        lastRenderedState: m
      }, f.queue = e, e = e.dispatch = ky.bind(
        null,
        Kt,
        e
      ), [f.memoizedState, e];
    }
    function Al(e) {
      var n = me();
      return Ml(n, Ve, e);
    }
    function Ml(e, n, s) {
      var f = e.queue;
      if (f === null)
        throw Error(
          "Should have a queue. You are likely calling Hooks conditionally, which is not allowed. (https://react.dev/link/invalid-hook-call)"
        );
      f.lastRenderedReducer = s;
      var m = e.baseQueue, p = f.pending;
      if (p !== null) {
        if (m !== null) {
          var T = m.next;
          m.next = p.next, p.next = T;
        }
        n.baseQueue !== m && console.error(
          "Internal error: Expected work-in-progress queue to be a clone. This is a bug in React."
        ), n.baseQueue = m = p, f.pending = null;
      }
      if (p = e.baseState, m === null) e.memoizedState = p;
      else {
        n = m.next;
        var C = T = null, D = null, z = n, tt = !1;
        do {
          var st = z.lane & -536870913;
          if (st !== z.lane ? (de & st) === st : (Cc & st) === st) {
            var J = z.revertLane;
            if (J === 0)
              D !== null && (D = D.next = {
                lane: 0,
                revertLane: 0,
                action: z.action,
                hasEagerState: z.hasEagerState,
                eagerState: z.eagerState,
                next: null
              }), st === Ch && (tt = !0);
            else if ((Cc & J) === J) {
              z = z.next, J === Ch && (tt = !0);
              continue;
            } else
              st = {
                lane: 0,
                revertLane: z.revertLane,
                action: z.action,
                hasEagerState: z.hasEagerState,
                eagerState: z.eagerState,
                next: null
              }, D === null ? (C = D = st, T = p) : D = D.next = st, Kt.lanes |= J, Mc |= J;
            st = z.action, Mh && s(p, st), p = z.hasEagerState ? z.eagerState : s(p, st);
          } else
            J = {
              lane: st,
              revertLane: z.revertLane,
              action: z.action,
              hasEagerState: z.hasEagerState,
              eagerState: z.eagerState,
              next: null
            }, D === null ? (C = D = J, T = p) : D = D.next = J, Kt.lanes |= st, Mc |= st;
          z = z.next;
        } while (z !== null && z !== n);
        if (D === null ? T = p : D.next = C, !rl(p, e.memoizedState) && (ui = !0, tt && (s = om, s !== null)))
          throw s;
        e.memoizedState = p, e.baseState = T, e.baseQueue = D, f.lastRenderedState = p;
      }
      return m === null && (f.lanes = 0), [e.memoizedState, f.dispatch];
    }
    function Sr(e) {
      var n = me(), s = n.queue;
      if (s === null)
        throw Error(
          "Should have a queue. You are likely calling Hooks conditionally, which is not allowed. (https://react.dev/link/invalid-hook-call)"
        );
      s.lastRenderedReducer = e;
      var f = s.dispatch, m = s.pending, p = n.memoizedState;
      if (m !== null) {
        s.pending = null;
        var T = m = m.next;
        do
          p = e(p, T.action), T = T.next;
        while (T !== m);
        rl(p, n.memoizedState) || (ui = !0), n.memoizedState = p, n.baseQueue === null && (n.baseState = p), s.lastRenderedState = p;
      }
      return [p, f];
    }
    function Cs(e, n, s) {
      var f = Kt, m = sn();
      if (xe) {
        if (s === void 0)
          throw Error(
            "Missing getServerSnapshot, which is required for server-rendered content. Will revert to client rendering."
          );
        var p = s();
        um || p === s() || (console.error(
          "The result of getServerSnapshot should be cached to avoid an infinite loop"
        ), um = !0);
      } else {
        if (p = n(), um || (s = n(), rl(p, s) || (console.error(
          "The result of getSnapshot should be cached to avoid an infinite loop"
        ), um = !0)), Pe === null)
          throw Error(
            "Expected a work-in-progress root. This is a bug in React. Please file an issue."
          );
        de & 124 || Uy(f, n, p);
      }
      return m.memoizedState = p, s = { value: p, getSnapshot: n }, m.queue = s, wd(
        Yu.bind(null, f, s, e),
        [e]
      ), f.flags |= 2048, Oa(
        as | Jn,
        To(),
        Hu.bind(
          null,
          f,
          s,
          p,
          n
        ),
        null
      ), p;
    }
    function Tf(e, n, s) {
      var f = Kt, m = me(), p = xe;
      if (p) {
        if (s === void 0)
          throw Error(
            "Missing getServerSnapshot, which is required for server-rendered content. Will revert to client rendering."
          );
        s = s();
      } else if (s = n(), !um) {
        var T = n();
        rl(s, T) || (console.error(
          "The result of getSnapshot should be cached to avoid an infinite loop"
        ), um = !0);
      }
      (T = !rl(
        (Ve || m).memoizedState,
        s
      )) && (m.memoizedState = s, ui = !0), m = m.queue;
      var C = Yu.bind(null, f, m, e);
      if (Cn(2048, Jn, C, [e]), m.getSnapshot !== n || T || Hn !== null && Hn.memoizedState.tag & as) {
        if (f.flags |= 2048, Oa(
          as | Jn,
          To(),
          Hu.bind(
            null,
            f,
            m,
            s,
            n
          ),
          null
        ), Pe === null)
          throw Error(
            "Expected a work-in-progress root. This is a bug in React. Please file an issue."
          );
        p || Cc & 124 || Uy(f, n, s);
      }
      return s;
    }
    function Uy(e, n, s) {
      e.flags |= 16384, e = { getSnapshot: n, value: s }, n = Kt.updateQueue, n === null ? (n = Rd(), Kt.updateQueue = n, n.stores = [e]) : (s = n.stores, s === null ? n.stores = [e] : s.push(e));
    }
    function Hu(e, n, s, f) {
      n.value = s, n.getSnapshot = f, Gy(n) && ju(e);
    }
    function Yu(e, n, s) {
      return s(function() {
        Gy(n) && ju(e);
      });
    }
    function Gy(e) {
      var n = e.getSnapshot;
      e = e.value;
      try {
        var s = n();
        return !rl(e, s);
      } catch {
        return !0;
      }
    }
    function ju(e) {
      var n = Mi(e, 2);
      n !== null && rn(n, e, 2);
    }
    function xf(e) {
      var n = sn();
      if (typeof e == "function") {
        var s = e;
        if (e = s(), Mh) {
          Xt(!0);
          try {
            s();
          } finally {
            Xt(!1);
          }
        }
      }
      return n.memoizedState = n.baseState = e, n.queue = {
        pending: null,
        lanes: 0,
        dispatch: null,
        lastRenderedReducer: Ee,
        lastRenderedState: e
      }, n;
    }
    function Rs(e) {
      e = xf(e);
      var n = e.queue, s = ku.bind(null, Kt, n);
      return n.dispatch = s, [e.memoizedState, s];
    }
    function sa(e) {
      var n = sn();
      n.memoizedState = n.baseState = e;
      var s = {
        pending: null,
        lanes: 0,
        dispatch: null,
        lastRenderedReducer: null,
        lastRenderedState: null
      };
      return n.queue = s, n = Yd.bind(
        null,
        Kt,
        !0,
        s
      ), s.dispatch = n, [e, n];
    }
    function As(e, n) {
      var s = me();
      return Da(s, Ve, e, n);
    }
    function Da(e, n, s, f) {
      return e.baseState = s, Ml(
        e,
        Ve,
        typeof f == "function" ? f : Ee
      );
    }
    function Ad(e, n) {
      var s = me();
      return Ve !== null ? Da(s, Ve, e, n) : (s.baseState = e, [e, s.queue.dispatch]);
    }
    function Hy(e, n, s, f, m) {
      if (Of(e))
        throw Error("Cannot update form state while rendering.");
      if (e = n.action, e !== null) {
        var p = {
          payload: m,
          action: e,
          next: null,
          isTransition: !0,
          status: "pending",
          value: null,
          reason: null,
          listeners: [],
          then: function(T) {
            p.listeners.push(T);
          }
        };
        lt.T !== null ? s(!0) : p.isTransition = !1, f(p), s = n.pending, s === null ? (p.next = n.pending = p, Bu(n, p)) : (p.next = s.next, n.pending = s.next = p);
      }
    }
    function Bu(e, n) {
      var s = n.action, f = n.payload, m = e.state;
      if (n.isTransition) {
        var p = lt.T, T = {};
        lt.T = T, lt.T._updatedFibers = /* @__PURE__ */ new Set();
        try {
          var C = s(m, f), D = lt.S;
          D !== null && D(T, C), Cf(e, n, C);
        } catch (z) {
          Ln(e, n, z);
        } finally {
          lt.T = p, p === null && T._updatedFibers && (e = T._updatedFibers.size, T._updatedFibers.clear(), 10 < e && console.warn(
            "Detected a large number of updates inside startTransition. If this is due to a subscription please re-write it to use React provided hooks. Otherwise concurrent mode guarantees are off the table."
          ));
        }
      } else
        try {
          T = s(m, f), Cf(e, n, T);
        } catch (z) {
          Ln(e, n, z);
        }
    }
    function Cf(e, n, s) {
      s !== null && typeof s == "object" && typeof s.then == "function" ? (s.then(
        function(f) {
          bo(e, n, f);
        },
        function(f) {
          return Ln(e, n, f);
        }
      ), n.isTransition || console.error(
        "An async function with useActionState was called outside of a transition. This is likely not what you intended (for example, isPending will not update correctly). Either call the returned function inside startTransition, or pass it to an `action` or `formAction` prop."
      )) : bo(e, n, s);
    }
    function bo(e, n, s) {
      n.status = "fulfilled", n.value = s, Rf(n), e.state = s, n = e.pending, n !== null && (s = n.next, s === n ? e.pending = null : (s = s.next, n.next = s, Bu(e, s)));
    }
    function Ln(e, n, s) {
      var f = e.pending;
      if (e.pending = null, f !== null) {
        f = f.next;
        do
          n.status = "rejected", n.reason = s, Rf(n), n = n.next;
        while (n !== f);
      }
      e.action = null;
    }
    function Rf(e) {
      e = e.listeners;
      for (var n = 0; n < e.length; n++) (0, e[n])();
    }
    function Yy(e, n) {
      return n;
    }
    function Iu(e, n) {
      if (xe) {
        var s = Pe.formState;
        if (s !== null) {
          t: {
            var f = Kt;
            if (xe) {
              if (Sn) {
                e: {
                  for (var m = Sn, p = Zo; m.nodeType !== 8; ) {
                    if (!p) {
                      m = null;
                      break e;
                    }
                    if (m = Kn(
                      m.nextSibling
                    ), m === null) {
                      m = null;
                      break e;
                    }
                  }
                  p = m.data, m = p === BE || p === TC ? m : null;
                }
                if (m) {
                  Sn = Kn(
                    m.nextSibling
                  ), f = m.data === BE;
                  break t;
                }
              }
              xa(f);
            }
            f = !1;
          }
          f && (n = s[0]);
        }
      }
      return s = sn(), s.memoizedState = s.baseState = n, f = {
        pending: null,
        lanes: 0,
        dispatch: null,
        lastRenderedReducer: Yy,
        lastRenderedState: n
      }, s.queue = f, s = ku.bind(
        null,
        Kt,
        f
      ), f.dispatch = s, f = xf(!1), p = Yd.bind(
        null,
        Kt,
        !1,
        f.queue
      ), f = sn(), m = {
        state: n,
        dispatch: null,
        action: e,
        pending: null
      }, f.queue = m, s = Hy.bind(
        null,
        Kt,
        m,
        p,
        s
      ), m.dispatch = s, f.memoizedState = e, [n, s, !1];
    }
    function Md(e) {
      var n = me();
      return nv(n, Ve, e);
    }
    function nv(e, n, s) {
      if (n = Ml(
        e,
        n,
        Yy
      )[0], e = Al(Ee)[0], typeof n == "object" && n !== null && typeof n.then == "function")
        try {
          var f = vr(n);
        } catch (T) {
          throw T === T0 ? Qv : T;
        }
      else f = n;
      n = me();
      var m = n.queue, p = m.dispatch;
      return s !== n.memoizedState && (Kt.flags |= 2048, Oa(
        as | Jn,
        To(),
        xn.bind(null, m, s),
        null
      )), [f, p, e];
    }
    function xn(e, n) {
      e.action = n;
    }
    function Xu(e) {
      var n = me(), s = Ve;
      if (s !== null)
        return nv(n, s, e);
      me(), n = n.memoizedState, s = me();
      var f = s.queue.dispatch;
      return s.memoizedState = e, [n, f, !1];
    }
    function Oa(e, n, s, f) {
      return e = {
        tag: e,
        create: s,
        deps: f,
        inst: n,
        next: null
      }, n = Kt.updateQueue, n === null && (n = Rd(), Kt.updateQueue = n), s = n.lastEffect, s === null ? n.lastEffect = e.next = e : (f = s.next, s.next = e, e.next = f, n.lastEffect = e), e;
    }
    function To() {
      return { destroy: void 0, resource: void 0 };
    }
    function Af(e) {
      var n = sn();
      return e = { current: e }, n.memoizedState = e;
    }
    function wl(e, n, s, f) {
      var m = sn();
      f = f === void 0 ? null : f, Kt.flags |= e, m.memoizedState = Oa(
        as | n,
        To(),
        s,
        f
      );
    }
    function Cn(e, n, s, f) {
      var m = me();
      f = f === void 0 ? null : f;
      var p = m.memoizedState.inst;
      Ve !== null && f !== null && So(f, Ve.memoizedState.deps) ? m.memoizedState = Oa(n, p, s, f) : (Kt.flags |= e, m.memoizedState = Oa(
        as | n,
        p,
        s,
        f
      ));
    }
    function wd(e, n) {
      (Kt.mode & Vs) !== $e && (Kt.mode & ox) === $e ? wl(276826112, Jn, e, n) : wl(8390656, Jn, e, n);
    }
    function Dd(e, n) {
      var s = 4194308;
      return (Kt.mode & Vs) !== $e && (s |= 134217728), wl(s, Ei, e, n);
    }
    function iv(e, n) {
      if (typeof n == "function") {
        e = e();
        var s = n(e);
        return function() {
          typeof s == "function" ? s() : n(null);
        };
      }
      if (n != null)
        return n.hasOwnProperty("current") || console.error(
          "Expected useImperativeHandle() first argument to either be a ref callback or React.createRef() object. Instead received: %s.",
          "an object with keys {" + Object.keys(n).join(", ") + "}"
        ), e = e(), n.current = e, function() {
          n.current = null;
        };
    }
    function Od(e, n, s) {
      typeof n != "function" && console.error(
        "Expected useImperativeHandle() second argument to be a function that creates a handle. Instead received: %s.",
        n !== null ? typeof n : "null"
      ), s = s != null ? s.concat([e]) : null;
      var f = 4194308;
      (Kt.mode & Vs) !== $e && (f |= 134217728), wl(
        f,
        Ei,
        iv.bind(null, n, e),
        s
      );
    }
    function La(e, n, s) {
      typeof n != "function" && console.error(
        "Expected useImperativeHandle() second argument to be a function that creates a handle. Instead received: %s.",
        n !== null ? typeof n : "null"
      ), s = s != null ? s.concat([e]) : null, Cn(
        4,
        Ei,
        iv.bind(null, n, e),
        s
      );
    }
    function Mf(e, n) {
      return sn().memoizedState = [
        e,
        n === void 0 ? null : n
      ], e;
    }
    function Er(e, n) {
      var s = me();
      n = n === void 0 ? null : n;
      var f = s.memoizedState;
      return n !== null && So(n, f[1]) ? f[0] : (s.memoizedState = [e, n], e);
    }
    function Ld(e, n) {
      var s = sn();
      n = n === void 0 ? null : n;
      var f = e();
      if (Mh) {
        Xt(!0);
        try {
          e();
        } finally {
          Xt(!1);
        }
      }
      return s.memoizedState = [f, n], f;
    }
    function xo(e, n) {
      var s = me();
      n = n === void 0 ? null : n;
      var f = s.memoizedState;
      if (n !== null && So(n, f[1]))
        return f[0];
      if (f = e(), Mh) {
        Xt(!0);
        try {
          e();
        } finally {
          Xt(!1);
        }
      }
      return s.memoizedState = [f, n], f;
    }
    function zd(e, n) {
      var s = sn();
      return Ud(s, e, n);
    }
    function wf(e, n) {
      var s = me();
      return Df(
        s,
        Ve.memoizedState,
        e,
        n
      );
    }
    function Nd(e, n) {
      var s = me();
      return Ve === null ? Ud(s, e, n) : Df(
        s,
        Ve.memoizedState,
        e,
        n
      );
    }
    function Ud(e, n, s) {
      return s === void 0 || Cc & 1073741824 ? e.memoizedState = n : (e.memoizedState = s, e = dv(), Kt.lanes |= e, Mc |= e, s);
    }
    function Df(e, n, s, f) {
      return rl(s, n) ? s : rm.current !== null ? (e = Ud(e, s, f), rl(e, n) || (ui = !0), e) : Cc & 42 ? (e = dv(), Kt.lanes |= e, Mc |= e, n) : (ui = !0, e.memoizedState = s);
    }
    function jy(e, n, s, f, m) {
      var p = Vt.p;
      Vt.p = p !== 0 && p < ha ? p : ha;
      var T = lt.T, C = {};
      lt.T = C, Yd(e, !1, n, s), C._updatedFibers = /* @__PURE__ */ new Set();
      try {
        var D = m(), z = lt.S;
        if (z !== null && z(C, D), D !== null && typeof D == "object" && typeof D.then == "function") {
          var tt = $_(
            D,
            f
          );
          Ms(
            e,
            n,
            tt,
            Ui(e)
          );
        } else
          Ms(
            e,
            n,
            f,
            Ui(e)
          );
      } catch (st) {
        Ms(
          e,
          n,
          { then: function() {
          }, status: "rejected", reason: st },
          Ui(e)
        );
      } finally {
        Vt.p = p, lt.T = T, T === null && C._updatedFibers && (e = C._updatedFibers.size, C._updatedFibers.clear(), 10 < e && console.warn(
          "Detected a large number of updates inside startTransition. If this is due to a subscription please re-write it to use React provided hooks. Otherwise concurrent mode guarantees are off the table."
        ));
      }
    }
    function br(e, n, s, f) {
      if (e.tag !== 5)
        throw Error(
          "Expected the form instance to be a HostComponent. This is a bug in React."
        );
      var m = By(e).queue;
      jy(
        e,
        m,
        n,
        jh,
        s === null ? E : function() {
          return Iy(e), s(f);
        }
      );
    }
    function By(e) {
      var n = e.memoizedState;
      if (n !== null) return n;
      n = {
        memoizedState: jh,
        baseState: jh,
        baseQueue: null,
        queue: {
          pending: null,
          lanes: 0,
          dispatch: null,
          lastRenderedReducer: Ee,
          lastRenderedState: jh
        },
        next: null
      };
      var s = {};
      return n.next = {
        memoizedState: s,
        baseState: s,
        baseQueue: null,
        queue: {
          pending: null,
          lanes: 0,
          dispatch: null,
          lastRenderedReducer: Ee,
          lastRenderedState: s
        },
        next: null
      }, e.memoizedState = n, e = e.alternate, e !== null && (e.memoizedState = n), n;
    }
    function Iy(e) {
      lt.T === null && console.error(
        "requestFormReset was called outside a transition or action. To fix, move to an action, or wrap with startTransition."
      );
      var n = By(e).next.queue;
      Ms(
        e,
        n,
        {},
        Ui(e)
      );
    }
    function za() {
      var e = xf(!1);
      return e = jy.bind(
        null,
        Kt,
        e.queue,
        !0,
        !1
      ), sn().memoizedState = e, [!1, e];
    }
    function Gd() {
      var e = Al(Ee)[0], n = me().memoizedState;
      return [
        typeof e == "boolean" ? e : vr(e),
        n
      ];
    }
    function Hd() {
      var e = Sr(Ee)[0], n = me().memoizedState;
      return [
        typeof e == "boolean" ? e : vr(e),
        n
      ];
    }
    function Li() {
      return Ke(k0);
    }
    function Na() {
      var e = sn(), n = Pe.identifierPrefix;
      if (xe) {
        var s = Qr, f = Pr;
        s = (f & ~(1 << 32 - oi(f) - 1)).toString(32) + s, n = "«" + n + "R" + s, s = t1++, 0 < s && (n += "H" + s.toString(32)), n += "»";
      } else
        s = yM++, n = "«" + n + "r" + s.toString(32) + "»";
      return e.memoizedState = n;
    }
    function Tr() {
      return sn().memoizedState = Xy.bind(
        null,
        Kt
      );
    }
    function Xy(e, n) {
      for (var s = e.return; s !== null; ) {
        switch (s.tag) {
          case 24:
          case 3:
            var f = Ui(s);
            e = Aa(f);
            var m = ia(s, e, f);
            m !== null && (rn(m, s, f), vo(m, s, f)), s = vf(), n != null && m !== null && console.error(
              "The seed argument is not enabled outside experimental channels."
            ), e.payload = { cache: s };
            return;
        }
        s = s.return;
      }
    }
    function ky(e, n, s) {
      var f = arguments;
      typeof f[3] == "function" && console.error(
        "State updates from the useState() and useReducer() Hooks don't support the second callback argument. To execute a side effect after rendering, declare it in the component body with useEffect()."
      ), f = Ui(e);
      var m = {
        lane: f,
        revertLane: 0,
        action: s,
        hasEagerState: !1,
        eagerState: null,
        next: null
      };
      Of(e) ? xr(n, m) : (m = py(e, n, m, f), m !== null && (rn(m, e, f), Lf(m, n, f))), Fi(e, f);
    }
    function ku(e, n, s) {
      var f = arguments;
      typeof f[3] == "function" && console.error(
        "State updates from the useState() and useReducer() Hooks don't support the second callback argument. To execute a side effect after rendering, declare it in the component body with useEffect()."
      ), f = Ui(e), Ms(e, n, s, f), Fi(e, f);
    }
    function Ms(e, n, s, f) {
      var m = {
        lane: f,
        revertLane: 0,
        action: s,
        hasEagerState: !1,
        eagerState: null,
        next: null
      };
      if (Of(e)) xr(n, m);
      else {
        var p = e.alternate;
        if (e.lanes === 0 && (p === null || p.lanes === 0) && (p = n.lastRenderedReducer, p !== null)) {
          var T = lt.H;
          lt.H = Zs;
          try {
            var C = n.lastRenderedState, D = p(C, s);
            if (m.hasEagerState = !0, m.eagerState = D, rl(D, C))
              return _d(e, n, m, 0), Pe === null && gf(), !1;
          } catch {
          } finally {
            lt.H = T;
          }
        }
        if (s = py(e, n, m, f), s !== null)
          return rn(s, e, f), Lf(s, n, f), !0;
      }
      return !1;
    }
    function Yd(e, n, s, f) {
      if (lt.T === null && Ch === 0 && console.error(
        "An optimistic state update occurred outside a transition or action. To fix, move the update to an action, or wrap with startTransition."
      ), f = {
        lane: 2,
        revertLane: Sp(),
        action: f,
        hasEagerState: !1,
        eagerState: null,
        next: null
      }, Of(e)) {
        if (n)
          throw Error("Cannot update optimistic state while rendering.");
        console.error("Cannot call startTransition while rendering.");
      } else
        n = py(
          e,
          s,
          f,
          2
        ), n !== null && rn(n, e, 2);
      Fi(e, 2);
    }
    function Of(e) {
      var n = e.alternate;
      return e === Kt || n !== null && n === Kt;
    }
    function xr(e, n) {
      cm = $v = !0;
      var s = e.pending;
      s === null ? n.next = n : (n.next = s.next, s.next = n), e.pending = n;
    }
    function Lf(e, n, s) {
      if (s & 4194048) {
        var f = n.lanes;
        f &= e.pendingLanes, s |= f, n.lanes = s, Ql(e, s);
      }
    }
    function zn(e) {
      var n = le;
      return e != null && (le = n === null ? e : n.concat(e)), n;
    }
    function Vu(e, n, s) {
      for (var f = Object.keys(e.props), m = 0; m < f.length; m++) {
        var p = f[m];
        if (p !== "children" && p !== "key") {
          n === null && (n = pf(e, s.mode, 0), n._debugInfo = le, n.return = s), wt(
            n,
            function(T) {
              console.error(
                "Invalid prop `%s` supplied to `React.Fragment`. React.Fragment can only have `key` and `children` props.",
                T
              );
            },
            p
          );
          break;
        }
      }
    }
    function qu(e) {
      var n = A0;
      return A0 += 1, fm === null && (fm = Ly()), xl(fm, e, n);
    }
    function Dl(e, n) {
      n = n.props.ref, e.ref = n !== void 0 ? n : null;
    }
    function Pt(e, n) {
      throw n.$$typeof === rh ? Error(
        `A React Element from an older version of React was rendered. This is not supported. It can happen if:
- Multiple copies of the "react" package is used.
- A library pre-bundled an old copy of "react" or "react/jsx-runtime".
- A compiler tries to "inline" JSX instead of using the runtime.`
      ) : (e = Object.prototype.toString.call(n), Error(
        "Objects are not valid as a React child (found: " + (e === "[object Object]" ? "object with keys {" + Object.keys(n).join(", ") + "}" : e) + "). If you meant to render a collection of children, use an array instead."
      ));
    }
    function Ce(e, n) {
      var s = it(e) || "Component";
      Bx[s] || (Bx[s] = !0, n = n.displayName || n.name || "Component", e.tag === 3 ? console.error(
        `Functions are not valid as a React child. This may happen if you return %s instead of <%s /> from render. Or maybe you meant to call this function rather than return it.
  root.render(%s)`,
        n,
        n,
        n
      ) : console.error(
        `Functions are not valid as a React child. This may happen if you return %s instead of <%s /> from render. Or maybe you meant to call this function rather than return it.
  <%s>{%s}</%s>`,
        n,
        n,
        s,
        n,
        s
      ));
    }
    function nn(e, n) {
      var s = it(e) || "Component";
      Ix[s] || (Ix[s] = !0, n = String(n), e.tag === 3 ? console.error(
        `Symbols are not valid as a React child.
  root.render(%s)`,
        n
      ) : console.error(
        `Symbols are not valid as a React child.
  <%s>%s</%s>`,
        s,
        n,
        s
      ));
    }
    function zf(e) {
      function n(j, B) {
        if (e) {
          var k = j.deletions;
          k === null ? (j.deletions = [B], j.flags |= 16) : k.push(B);
        }
      }
      function s(j, B) {
        if (!e) return null;
        for (; B !== null; )
          n(j, B), B = B.sibling;
        return null;
      }
      function f(j) {
        for (var B = /* @__PURE__ */ new Map(); j !== null; )
          j.key !== null ? B.set(j.key, j) : B.set(j.index, j), j = j.sibling;
        return B;
      }
      function m(j, B) {
        return j = Ta(j, B), j.index = 0, j.sibling = null, j;
      }
      function p(j, B, k) {
        return j.index = k, e ? (k = j.alternate, k !== null ? (k = k.index, k < B ? (j.flags |= 67108866, B) : k) : (j.flags |= 67108866, B)) : (j.flags |= 1048576, B);
      }
      function T(j) {
        return e && j.alternate === null && (j.flags |= 67108866), j;
      }
      function C(j, B, k, ut) {
        return B === null || B.tag !== 6 ? (B = ho(
          k,
          j.mode,
          ut
        ), B.return = j, B._debugOwner = j, B._debugTask = j._debugTask, B._debugInfo = le, B) : (B = m(B, k), B.return = j, B._debugInfo = le, B);
      }
      function D(j, B, k, ut) {
        var bt = k.type;
        return bt === Qt ? (B = tt(
          j,
          B,
          k.props.children,
          ut,
          k.key
        ), Vu(k, B, j), B) : B !== null && (B.elementType === bt || P_(B, k) || typeof bt == "object" && bt !== null && bt.$$typeof === sl && Rc(bt) === B.type) ? (B = m(B, k.props), Dl(B, k), B.return = j, B._debugOwner = k._owner, B._debugInfo = le, B) : (B = pf(k, j.mode, ut), Dl(B, k), B.return = j, B._debugInfo = le, B);
      }
      function z(j, B, k, ut) {
        return B === null || B.tag !== 4 || B.stateNode.containerInfo !== k.containerInfo || B.stateNode.implementation !== k.implementation ? (B = Ey(k, j.mode, ut), B.return = j, B._debugInfo = le, B) : (B = m(B, k.children || []), B.return = j, B._debugInfo = le, B);
      }
      function tt(j, B, k, ut, bt) {
        return B === null || B.tag !== 7 ? (B = fo(
          k,
          j.mode,
          ut,
          bt
        ), B.return = j, B._debugOwner = j, B._debugTask = j._debugTask, B._debugInfo = le, B) : (B = m(B, k), B.return = j, B._debugInfo = le, B);
      }
      function st(j, B, k) {
        if (typeof B == "string" && B !== "" || typeof B == "number" || typeof B == "bigint")
          return B = ho(
            "" + B,
            j.mode,
            k
          ), B.return = j, B._debugOwner = j, B._debugTask = j._debugTask, B._debugInfo = le, B;
        if (typeof B == "object" && B !== null) {
          switch (B.$$typeof) {
            case Ho:
              return k = pf(
                B,
                j.mode,
                k
              ), Dl(k, B), k.return = j, j = zn(B._debugInfo), k._debugInfo = le, le = j, k;
            case kr:
              return B = Ey(
                B,
                j.mode,
                k
              ), B.return = j, B._debugInfo = le, B;
            case sl:
              var ut = zn(B._debugInfo);
              return B = Rc(B), j = st(j, B, k), le = ut, j;
          }
          if (Ft(B) || dt(B))
            return k = fo(
              B,
              j.mode,
              k,
              null
            ), k.return = j, k._debugOwner = j, k._debugTask = j._debugTask, j = zn(B._debugInfo), k._debugInfo = le, le = j, k;
          if (typeof B.then == "function")
            return ut = zn(B._debugInfo), j = st(
              j,
              qu(B),
              k
            ), le = ut, j;
          if (B.$$typeof === Ul)
            return st(
              j,
              _f(j, B),
              k
            );
          Pt(j, B);
        }
        return typeof B == "function" && Ce(j, B), typeof B == "symbol" && nn(j, B), null;
      }
      function J(j, B, k, ut) {
        var bt = B !== null ? B.key : null;
        if (typeof k == "string" && k !== "" || typeof k == "number" || typeof k == "bigint")
          return bt !== null ? null : C(j, B, "" + k, ut);
        if (typeof k == "object" && k !== null) {
          switch (k.$$typeof) {
            case Ho:
              return k.key === bt ? (bt = zn(k._debugInfo), j = D(
                j,
                B,
                k,
                ut
              ), le = bt, j) : null;
            case kr:
              return k.key === bt ? z(j, B, k, ut) : null;
            case sl:
              return bt = zn(k._debugInfo), k = Rc(k), j = J(
                j,
                B,
                k,
                ut
              ), le = bt, j;
          }
          if (Ft(k) || dt(k))
            return bt !== null ? null : (bt = zn(k._debugInfo), j = tt(
              j,
              B,
              k,
              ut,
              null
            ), le = bt, j);
          if (typeof k.then == "function")
            return bt = zn(k._debugInfo), j = J(
              j,
              B,
              qu(k),
              ut
            ), le = bt, j;
          if (k.$$typeof === Ul)
            return J(
              j,
              B,
              _f(j, k),
              ut
            );
          Pt(j, k);
        }
        return typeof k == "function" && Ce(j, k), typeof k == "symbol" && nn(j, k), null;
      }
      function rt(j, B, k, ut, bt) {
        if (typeof ut == "string" && ut !== "" || typeof ut == "number" || typeof ut == "bigint")
          return j = j.get(k) || null, C(B, j, "" + ut, bt);
        if (typeof ut == "object" && ut !== null) {
          switch (ut.$$typeof) {
            case Ho:
              return k = j.get(
                ut.key === null ? k : ut.key
              ) || null, j = zn(ut._debugInfo), B = D(
                B,
                k,
                ut,
                bt
              ), le = j, B;
            case kr:
              return j = j.get(
                ut.key === null ? k : ut.key
              ) || null, z(B, j, ut, bt);
            case sl:
              var Wt = zn(ut._debugInfo);
              return ut = Rc(ut), B = rt(
                j,
                B,
                k,
                ut,
                bt
              ), le = Wt, B;
          }
          if (Ft(ut) || dt(ut))
            return k = j.get(k) || null, j = zn(ut._debugInfo), B = tt(
              B,
              k,
              ut,
              bt,
              null
            ), le = j, B;
          if (typeof ut.then == "function")
            return Wt = zn(ut._debugInfo), B = rt(
              j,
              B,
              k,
              qu(ut),
              bt
            ), le = Wt, B;
          if (ut.$$typeof === Ul)
            return rt(
              j,
              B,
              k,
              _f(B, ut),
              bt
            );
          Pt(B, ut);
        }
        return typeof ut == "function" && Ce(B, ut), typeof ut == "symbol" && nn(B, ut), null;
      }
      function Dt(j, B, k, ut) {
        if (typeof k != "object" || k === null) return ut;
        switch (k.$$typeof) {
          case Ho:
          case kr:
            y(j, B, k);
            var bt = k.key;
            if (typeof bt != "string") break;
            if (ut === null) {
              ut = /* @__PURE__ */ new Set(), ut.add(bt);
              break;
            }
            if (!ut.has(bt)) {
              ut.add(bt);
              break;
            }
            wt(B, function() {
              console.error(
                "Encountered two children with the same key, `%s`. Keys should be unique so that components maintain their identity across updates. Non-unique keys may cause children to be duplicated and/or omitted — the behavior is unsupported and could change in a future version.",
                bt
              );
            });
            break;
          case sl:
            k = Rc(k), Dt(j, B, k, ut);
        }
        return ut;
      }
      function qt(j, B, k, ut) {
        for (var bt = null, Wt = null, Ot = null, Jt = B, te = B = 0, tn = null; Jt !== null && te < k.length; te++) {
          Jt.index > te ? (tn = Jt, Jt = null) : tn = Jt.sibling;
          var wn = J(
            j,
            Jt,
            k[te],
            ut
          );
          if (wn === null) {
            Jt === null && (Jt = tn);
            break;
          }
          bt = Dt(
            j,
            wn,
            k[te],
            bt
          ), e && Jt && wn.alternate === null && n(j, Jt), B = p(wn, B, te), Ot === null ? Wt = wn : Ot.sibling = wn, Ot = wn, Jt = tn;
        }
        if (te === k.length)
          return s(j, Jt), xe && dr(j, te), Wt;
        if (Jt === null) {
          for (; te < k.length; te++)
            Jt = st(j, k[te], ut), Jt !== null && (bt = Dt(
              j,
              Jt,
              k[te],
              bt
            ), B = p(
              Jt,
              B,
              te
            ), Ot === null ? Wt = Jt : Ot.sibling = Jt, Ot = Jt);
          return xe && dr(j, te), Wt;
        }
        for (Jt = f(Jt); te < k.length; te++)
          tn = rt(
            Jt,
            j,
            te,
            k[te],
            ut
          ), tn !== null && (bt = Dt(
            j,
            tn,
            k[te],
            bt
          ), e && tn.alternate !== null && Jt.delete(
            tn.key === null ? te : tn.key
          ), B = p(
            tn,
            B,
            te
          ), Ot === null ? Wt = tn : Ot.sibling = tn, Ot = tn);
        return e && Jt.forEach(function(au) {
          return n(j, au);
        }), xe && dr(j, te), Wt;
      }
      function Qe(j, B, k, ut) {
        if (k == null)
          throw Error("An iterable object provided no iterator.");
        for (var bt = null, Wt = null, Ot = B, Jt = B = 0, te = null, tn = null, wn = k.next(); Ot !== null && !wn.done; Jt++, wn = k.next()) {
          Ot.index > Jt ? (te = Ot, Ot = null) : te = Ot.sibling;
          var au = J(j, Ot, wn.value, ut);
          if (au === null) {
            Ot === null && (Ot = te);
            break;
          }
          tn = Dt(
            j,
            au,
            wn.value,
            tn
          ), e && Ot && au.alternate === null && n(j, Ot), B = p(au, B, Jt), Wt === null ? bt = au : Wt.sibling = au, Wt = au, Ot = te;
        }
        if (wn.done)
          return s(j, Ot), xe && dr(j, Jt), bt;
        if (Ot === null) {
          for (; !wn.done; Jt++, wn = k.next())
            Ot = st(j, wn.value, ut), Ot !== null && (tn = Dt(
              j,
              Ot,
              wn.value,
              tn
            ), B = p(
              Ot,
              B,
              Jt
            ), Wt === null ? bt = Ot : Wt.sibling = Ot, Wt = Ot);
          return xe && dr(j, Jt), bt;
        }
        for (Ot = f(Ot); !wn.done; Jt++, wn = k.next())
          te = rt(
            Ot,
            j,
            Jt,
            wn.value,
            ut
          ), te !== null && (tn = Dt(
            j,
            te,
            wn.value,
            tn
          ), e && te.alternate !== null && Ot.delete(
            te.key === null ? Jt : te.key
          ), B = p(
            te,
            B,
            Jt
          ), Wt === null ? bt = te : Wt.sibling = te, Wt = te);
        return e && Ot.forEach(function(kM) {
          return n(j, kM);
        }), xe && dr(j, Jt), bt;
      }
      function ye(j, B, k, ut) {
        if (typeof k == "object" && k !== null && k.type === Qt && k.key === null && (Vu(k, null, j), k = k.props.children), typeof k == "object" && k !== null) {
          switch (k.$$typeof) {
            case Ho:
              var bt = zn(k._debugInfo);
              t: {
                for (var Wt = k.key; B !== null; ) {
                  if (B.key === Wt) {
                    if (Wt = k.type, Wt === Qt) {
                      if (B.tag === 7) {
                        s(
                          j,
                          B.sibling
                        ), ut = m(
                          B,
                          k.props.children
                        ), ut.return = j, ut._debugOwner = k._owner, ut._debugInfo = le, Vu(k, ut, j), j = ut;
                        break t;
                      }
                    } else if (B.elementType === Wt || P_(
                      B,
                      k
                    ) || typeof Wt == "object" && Wt !== null && Wt.$$typeof === sl && Rc(Wt) === B.type) {
                      s(
                        j,
                        B.sibling
                      ), ut = m(B, k.props), Dl(ut, k), ut.return = j, ut._debugOwner = k._owner, ut._debugInfo = le, j = ut;
                      break t;
                    }
                    s(j, B);
                    break;
                  } else n(j, B);
                  B = B.sibling;
                }
                k.type === Qt ? (ut = fo(
                  k.props.children,
                  j.mode,
                  ut,
                  k.key
                ), ut.return = j, ut._debugOwner = j, ut._debugTask = j._debugTask, ut._debugInfo = le, Vu(k, ut, j), j = ut) : (ut = pf(
                  k,
                  j.mode,
                  ut
                ), Dl(ut, k), ut.return = j, ut._debugInfo = le, j = ut);
              }
              return j = T(j), le = bt, j;
            case kr:
              t: {
                for (bt = k, k = bt.key; B !== null; ) {
                  if (B.key === k)
                    if (B.tag === 4 && B.stateNode.containerInfo === bt.containerInfo && B.stateNode.implementation === bt.implementation) {
                      s(
                        j,
                        B.sibling
                      ), ut = m(
                        B,
                        bt.children || []
                      ), ut.return = j, j = ut;
                      break t;
                    } else {
                      s(j, B);
                      break;
                    }
                  else n(j, B);
                  B = B.sibling;
                }
                ut = Ey(
                  bt,
                  j.mode,
                  ut
                ), ut.return = j, j = ut;
              }
              return T(j);
            case sl:
              return bt = zn(k._debugInfo), k = Rc(k), j = ye(
                j,
                B,
                k,
                ut
              ), le = bt, j;
          }
          if (Ft(k))
            return bt = zn(k._debugInfo), j = qt(
              j,
              B,
              k,
              ut
            ), le = bt, j;
          if (dt(k)) {
            if (bt = zn(k._debugInfo), Wt = dt(k), typeof Wt != "function")
              throw Error(
                "An object is not an iterable. This error is likely caused by a bug in React. Please file an issue."
              );
            var Ot = Wt.call(k);
            return Ot === k ? (j.tag !== 0 || Object.prototype.toString.call(j.type) !== "[object GeneratorFunction]" || Object.prototype.toString.call(Ot) !== "[object Generator]") && (Yx || console.error(
              "Using Iterators as children is unsupported and will likely yield unexpected results because enumerating a generator mutates it. You may convert it to an array with `Array.from()` or the `[...spread]` operator before rendering. You can also use an Iterable that can iterate multiple times over the same items."
            ), Yx = !0) : k.entries !== Wt || yE || (console.error(
              "Using Maps as children is not supported. Use an array of keyed ReactElements instead."
            ), yE = !0), j = Qe(
              j,
              B,
              Ot,
              ut
            ), le = bt, j;
          }
          if (typeof k.then == "function")
            return bt = zn(k._debugInfo), j = ye(
              j,
              B,
              qu(k),
              ut
            ), le = bt, j;
          if (k.$$typeof === Ul)
            return ye(
              j,
              B,
              _f(j, k),
              ut
            );
          Pt(j, k);
        }
        return typeof k == "string" && k !== "" || typeof k == "number" || typeof k == "bigint" ? (bt = "" + k, B !== null && B.tag === 6 ? (s(
          j,
          B.sibling
        ), ut = m(B, bt), ut.return = j, j = ut) : (s(j, B), ut = ho(
          bt,
          j.mode,
          ut
        ), ut.return = j, ut._debugOwner = j, ut._debugTask = j._debugTask, ut._debugInfo = le, j = ut), T(j)) : (typeof k == "function" && Ce(j, k), typeof k == "symbol" && nn(j, k), s(j, B));
      }
      return function(j, B, k, ut) {
        var bt = le;
        le = null;
        try {
          A0 = 0;
          var Wt = ye(
            j,
            B,
            k,
            ut
          );
          return fm = null, Wt;
        } catch (tn) {
          if (tn === T0 || tn === Qv) throw tn;
          var Ot = R(29, tn, null, j.mode);
          Ot.lanes = ut, Ot.return = j;
          var Jt = Ot._debugInfo = le;
          if (Ot._debugOwner = j._debugOwner, Ot._debugTask = j._debugTask, Jt != null) {
            for (var te = Jt.length - 1; 0 <= te; te--)
              if (typeof Jt[te].stack == "string") {
                Ot._debugOwner = Jt[te], Ot._debugTask = Jt[te].debugTask;
                break;
              }
          }
          return Ot;
        } finally {
          le = bt;
        }
      };
    }
    function el(e) {
      var n = e.alternate;
      pt(
        $n,
        $n.current & dm,
        e
      ), pt(os, e, e), Ko === null && (n === null || rm.current !== null || n.memoizedState !== null) && (Ko = e);
    }
    function Co(e) {
      if (e.tag === 22) {
        if (pt($n, $n.current, e), pt(os, e, e), Ko === null) {
          var n = e.alternate;
          n !== null && n.memoizedState !== null && (Ko = e);
        }
      } else oa(e);
    }
    function oa(e) {
      pt($n, $n.current, e), pt(
        os,
        os.current,
        e
      );
    }
    function nl(e) {
      xt(os, e), Ko === e && (Ko = null), xt($n, e);
    }
    function ws(e) {
      for (var n = e; n !== null; ) {
        if (n.tag === 13) {
          var s = n.memoizedState;
          if (s !== null && (s = s.dehydrated, s === null || s.data === nu || Fa(s)))
            return n;
        } else if (n.tag === 19 && n.memoizedProps.revealOrder !== void 0) {
          if (n.flags & 128) return n;
        } else if (n.child !== null) {
          n.child.return = n, n = n.child;
          continue;
        }
        if (n === e) break;
        for (; n.sibling === null; ) {
          if (n.return === null || n.return === e) return null;
          n = n.return;
        }
        n.sibling.return = n.return, n = n.sibling;
      }
      return null;
    }
    function Vy(e) {
      if (e !== null && typeof e != "function") {
        var n = String(e);
        $x.has(n) || ($x.add(n), console.error(
          "Expected the last optional `callback` argument to be a function. Instead received: %s.",
          e
        ));
      }
    }
    function ln(e, n, s, f) {
      var m = e.memoizedState, p = s(f, m);
      if (e.mode & Xi) {
        Xt(!0);
        try {
          p = s(f, m);
        } finally {
          Xt(!1);
        }
      }
      p === void 0 && (n = ft(n) || "Component", Px.has(n) || (Px.add(n), console.error(
        "%s.getDerivedStateFromProps(): A valid state object (or null) must be returned. You have returned undefined.",
        n
      ))), m = p == null ? m : ee({}, m, p), e.memoizedState = m, e.lanes === 0 && (e.updateQueue.baseState = m);
    }
    function jd(e, n, s, f, m, p, T) {
      var C = e.stateNode;
      if (typeof C.shouldComponentUpdate == "function") {
        if (s = C.shouldComponentUpdate(
          f,
          p,
          T
        ), e.mode & Xi) {
          Xt(!0);
          try {
            s = C.shouldComponentUpdate(
              f,
              p,
              T
            );
          } finally {
            Xt(!1);
          }
        }
        return s === void 0 && console.error(
          "%s.shouldComponentUpdate(): Returned undefined instead of a boolean value. Make sure to return true or false.",
          ft(n) || "Component"
        ), s;
      }
      return n.prototype && n.prototype.isPureReactComponent ? !df(s, f) || !df(m, p) : !0;
    }
    function Bd(e, n, s, f) {
      var m = n.state;
      typeof n.componentWillReceiveProps == "function" && n.componentWillReceiveProps(s, f), typeof n.UNSAFE_componentWillReceiveProps == "function" && n.UNSAFE_componentWillReceiveProps(s, f), n.state !== m && (e = it(e) || "Component", Vx.has(e) || (Vx.add(e), console.error(
        "%s.componentWillReceiveProps(): Assigning directly to this.state is deprecated (except inside a component's constructor). Use setState instead.",
        e
      )), pE.enqueueReplaceState(
        n,
        n.state,
        null
      ));
    }
    function Ro(e, n) {
      var s = n;
      if ("ref" in n) {
        s = {};
        for (var f in n)
          f !== "ref" && (s[f] = n[f]);
      }
      if (e = e.defaultProps) {
        s === n && (s = ee({}, s));
        for (var m in e)
          s[m] === void 0 && (s[m] = e[m]);
      }
      return s;
    }
    function qy(e) {
      _E(e), console.warn(
        `%s

%s
`,
        gm ? "An error occurred in the <" + gm + "> component." : "An error occurred in one of your React components.",
        `Consider adding an error boundary to your tree to customize error handling behavior.
Visit https://react.dev/link/error-boundaries to learn more about error boundaries.`
      );
    }
    function lv(e) {
      var n = gm ? "The above error occurred in the <" + gm + "> component." : "The above error occurred in one of your React components.", s = "React will try to recreate this component tree from scratch using the error boundary you provided, " + ((vE || "Anonymous") + ".");
      if (typeof e == "object" && e !== null && typeof e.environmentName == "string") {
        var f = e.environmentName;
        e = [
          `%o

%s

%s
`,
          e,
          n,
          s
        ].slice(0), typeof e[0] == "string" ? e.splice(
          0,
          1,
          OC + e[0],
          LC,
          S1 + f + S1,
          zC
        ) : e.splice(
          0,
          0,
          OC,
          LC,
          S1 + f + S1,
          zC
        ), e.unshift(console), f = IM.apply(console.error, e), f();
      } else
        console.error(
          `%o

%s

%s
`,
          e,
          n,
          s
        );
    }
    function Id(e) {
      _E(e);
    }
    function Zu(e, n) {
      try {
        gm = n.source ? it(n.source) : null, vE = null;
        var s = n.value;
        if (lt.actQueue !== null)
          lt.thrownErrors.push(s);
        else {
          var f = e.onUncaughtError;
          f(s, { componentStack: n.stack });
        }
      } catch (m) {
        setTimeout(function() {
          throw m;
        });
      }
    }
    function Xd(e, n, s) {
      try {
        gm = s.source ? it(s.source) : null, vE = it(n);
        var f = e.onCaughtError;
        f(s.value, {
          componentStack: s.stack,
          errorBoundary: n.tag === 1 ? n.stateNode : null
        });
      } catch (m) {
        setTimeout(function() {
          throw m;
        });
      }
    }
    function li(e, n, s) {
      return s = Aa(s), s.tag = rE, s.payload = { element: null }, s.callback = function() {
        wt(n.source, Zu, e, n);
      }, s;
    }
    function on(e) {
      return e = Aa(e), e.tag = rE, e;
    }
    function Nf(e, n, s, f) {
      var m = s.type.getDerivedStateFromError;
      if (typeof m == "function") {
        var p = f.value;
        e.payload = function() {
          return m(p);
        }, e.callback = function() {
          Q_(s), wt(
            f.source,
            Xd,
            n,
            s,
            f
          );
        };
      }
      var T = s.stateNode;
      T !== null && typeof T.componentDidCatch == "function" && (e.callback = function() {
        Q_(s), wt(
          f.source,
          Xd,
          n,
          s,
          f
        ), typeof m != "function" && (Dc === null ? Dc = /* @__PURE__ */ new Set([this]) : Dc.add(this)), _M(this, f), typeof m == "function" || !(s.lanes & 2) && console.error(
          "%s: Error boundaries should implement getDerivedStateFromError(). In that method, return a state update to display an error message or fallback UI.",
          it(s) || "Unknown"
        );
      });
    }
    function Uf(e, n, s, f, m) {
      if (s.flags |= 32768, dn && tc(e, m), f !== null && typeof f == "object" && typeof f.then == "function") {
        if (n = s.alternate, n !== null && Vn(
          n,
          s,
          m,
          !0
        ), xe && (Wr = !0), s = os.current, s !== null) {
          switch (s.tag) {
            case 13:
              return Ko === null ? rg() : s.alternate === null && En === eu && (En = TE), s.flags &= -257, s.flags |= 65536, s.lanes = m, f === oE ? s.flags |= 16384 : (n = s.updateQueue, n === null ? s.updateQueue = /* @__PURE__ */ new Set([f]) : n.add(f), yp(e, f, m)), !1;
            case 22:
              return s.flags |= 65536, f === oE ? s.flags |= 16384 : (n = s.updateQueue, n === null ? (n = {
                transitions: null,
                markerInstances: null,
                retryQueue: /* @__PURE__ */ new Set([f])
              }, s.updateQueue = n) : (s = n.retryQueue, s === null ? n.retryQueue = /* @__PURE__ */ new Set([f]) : s.add(f)), yp(e, f, m)), !1;
          }
          throw Error(
            "Unexpected Suspense handler tag (" + s.tag + "). This is a bug in React."
          );
        }
        return yp(e, f, m), rg(), !1;
      }
      if (xe)
        return Wr = !0, n = os.current, n !== null ? (!(n.flags & 65536) && (n.flags |= 256), n.flags |= 65536, n.lanes = m, f !== iE && Du(
          $i(
            Error(
              "There was an error while hydrating but React was able to recover by instead client rendering from the nearest Suspense boundary.",
              { cause: f }
            ),
            s
          )
        )) : (f !== iE && Du(
          $i(
            Error(
              "There was an error while hydrating but React was able to recover by instead client rendering the entire root.",
              { cause: f }
            ),
            s
          )
        ), e = e.current.alternate, e.flags |= 65536, m &= -m, e.lanes |= m, f = $i(f, s), m = li(
          e.stateNode,
          f,
          m
        ), Lu(e, m), En !== wh && (En = _m)), !1;
      var p = $i(
        Error(
          "There was an error during concurrent rendering but React was able to recover by instead synchronously rendering the entire root.",
          { cause: f }
        ),
        s
      );
      if (U0 === null ? U0 = [p] : U0.push(p), En !== wh && (En = _m), n === null) return !0;
      f = $i(f, s), s = n;
      do {
        switch (s.tag) {
          case 3:
            return s.flags |= 65536, e = m & -m, s.lanes |= e, e = li(
              s.stateNode,
              f,
              e
            ), Lu(s, e), !1;
          case 1:
            if (n = s.type, p = s.stateNode, (s.flags & 128) === 0 && (typeof n.getDerivedStateFromError == "function" || p !== null && typeof p.componentDidCatch == "function" && (Dc === null || !Dc.has(p))))
              return s.flags |= 65536, m &= -m, s.lanes |= m, m = on(m), Nf(
                m,
                e,
                s,
                f
              ), Lu(s, m), !1;
        }
        s = s.return;
      } while (s !== null);
      return !1;
    }
    function vn(e, n, s, f) {
      n.child = e === null ? Xx(n, null, s, f) : hm(
        n,
        e.child,
        s,
        f
      );
    }
    function kd(e, n, s, f, m) {
      s = s.render;
      var p = n.ref;
      if ("ref" in f) {
        var T = {};
        for (var C in f)
          C !== "ref" && (T[C] = f[C]);
      } else T = f;
      return yo(n), De(n), f = Eo(
        e,
        n,
        s,
        T,
        p,
        m
      ), C = Oi(), ke(), e !== null && !ui ? (xs(e, n, m), Ga(e, n, m)) : (xe && C && Ed(n), n.flags |= 1, vn(e, n, f, m), n.child);
    }
    function Ua(e, n, s, f, m) {
      if (e === null) {
        var p = s.type;
        return typeof p == "function" && !vy(p) && p.defaultProps === void 0 && s.compare === null ? (s = hr(p), n.tag = 15, n.type = s, Fd(n, p), Gf(
          e,
          n,
          s,
          f,
          m
        )) : (e = Sd(
          s.type,
          null,
          f,
          n,
          n.mode,
          m
        ), e.ref = n.ref, e.return = n, n.child = e);
      }
      if (p = e.child, !$d(e, m)) {
        var T = p.memoizedProps;
        if (s = s.compare, s = s !== null ? s : df, s(T, f) && e.ref === n.ref)
          return Ga(
            e,
            n,
            m
          );
      }
      return n.flags |= 1, e = Ta(p, f), e.ref = n.ref, e.return = n, n.child = e;
    }
    function Gf(e, n, s, f, m) {
      if (e !== null) {
        var p = e.memoizedProps;
        if (df(p, f) && e.ref === n.ref && n.type === e.type)
          if (ui = !1, n.pendingProps = f = p, $d(e, m))
            e.flags & 131072 && (ui = !0);
          else
            return n.lanes = e.lanes, Ga(e, n, m);
      }
      return Zd(
        e,
        n,
        s,
        f,
        m
      );
    }
    function Vd(e, n, s) {
      var f = n.pendingProps, m = f.children, p = e !== null ? e.memoizedState : null;
      if (f.mode === "hidden") {
        if (n.flags & 128) {
          if (f = p !== null ? p.baseLanes | s : s, e !== null) {
            for (m = n.child = e.child, p = 0; m !== null; )
              p = p | m.lanes | m.childLanes, m = m.sibling;
            n.childLanes = p & ~f;
          } else n.childLanes = 0, n.child = null;
          return qd(
            e,
            n,
            f,
            s
          );
        }
        if (s & 536870912)
          n.memoizedState = { baseLanes: 0, cachePool: null }, e !== null && Cd(
            n,
            p !== null ? p.cachePool : null
          ), p !== null ? Di(n, p) : Ef(n), Co(n);
        else
          return n.lanes = n.childLanes = 536870912, qd(
            e,
            n,
            p !== null ? p.baseLanes | s : s,
            s
          );
      } else
        p !== null ? (Cd(n, p.cachePool), Di(n, p), oa(n), n.memoizedState = null) : (e !== null && Cd(n, null), Ef(n), oa(n));
      return vn(e, n, m, s), n.child;
    }
    function qd(e, n, s, f) {
      var m = Oy();
      return m = m === null ? null : {
        parent: Wn._currentValue,
        pool: m
      }, n.memoizedState = {
        baseLanes: s,
        cachePool: m
      }, e !== null && Cd(n, null), Ef(n), Co(n), e !== null && Vn(e, n, f, !0), null;
    }
    function Hf(e, n) {
      var s = n.ref;
      if (s === null)
        e !== null && e.ref !== null && (n.flags |= 4194816);
      else {
        if (typeof s != "function" && typeof s != "object")
          throw Error(
            "Expected ref to be a function, an object returned by React.createRef(), or undefined/null."
          );
        (e === null || e.ref !== s) && (n.flags |= 4194816);
      }
    }
    function Zd(e, n, s, f, m) {
      if (s.prototype && typeof s.prototype.render == "function") {
        var p = ft(s) || "Unknown";
        eC[p] || (console.error(
          "The <%s /> component appears to have a render method, but doesn't extend React.Component. This is likely to cause errors. Change %s to extend React.Component instead.",
          p,
          p
        ), eC[p] = !0);
      }
      return n.mode & Xi && qs.recordLegacyContextWarning(
        n,
        null
      ), e === null && (Fd(n, n.type), s.contextTypes && (p = ft(s) || "Unknown", iC[p] || (iC[p] = !0, console.error(
        "%s uses the legacy contextTypes API which was removed in React 19. Use React.createContext() with React.useContext() instead. (https://react.dev/link/legacy-context)",
        p
      )))), yo(n), De(n), s = Eo(
        e,
        n,
        s,
        f,
        void 0,
        m
      ), f = Oi(), ke(), e !== null && !ui ? (xs(e, n, m), Ga(e, n, m)) : (xe && f && Ed(n), n.flags |= 1, vn(e, n, s, m), n.child);
    }
    function Zy(e, n, s, f, m, p) {
      return yo(n), De(n), $r = -1, R0 = e !== null && e.type !== n.type, n.updateQueue = null, s = Gu(
        n,
        f,
        s,
        m
      ), bf(e, n), f = Oi(), ke(), e !== null && !ui ? (xs(e, n, p), Ga(e, n, p)) : (xe && f && Ed(n), n.flags |= 1, vn(e, n, s, p), n.child);
    }
    function Fy(e, n, s, f, m) {
      switch (g(n)) {
        case !1:
          var p = n.stateNode, T = new n.type(
            n.memoizedProps,
            p.context
          ).state;
          p.updater.enqueueSetState(p, T, null);
          break;
        case !0:
          n.flags |= 128, n.flags |= 65536, p = Error("Simulated error coming from DevTools");
          var C = m & -m;
          if (n.lanes |= C, T = Pe, T === null)
            throw Error(
              "Expected a work-in-progress root. This is a bug in React. Please file an issue."
            );
          C = on(C), Nf(
            C,
            T,
            n,
            $i(p, n)
          ), Lu(n, C);
      }
      if (yo(n), n.stateNode === null) {
        if (T = Tc, p = s.contextType, "contextType" in s && p !== null && (p === void 0 || p.$$typeof !== Ul) && !Jx.has(s) && (Jx.add(s), C = p === void 0 ? " However, it is set to undefined. This can be caused by a typo or by mixing up named and default imports. This can also happen due to a circular dependency, so try moving the createContext() call to a separate file." : typeof p != "object" ? " However, it is set to a " + typeof p + "." : p.$$typeof === Ug ? " Did you accidentally pass the Context.Consumer instead?" : " However, it is set to an object with keys {" + Object.keys(p).join(", ") + "}.", console.error(
          "%s defines an invalid contextType. contextType should point to the Context object returned by React.createContext().%s",
          ft(s) || "Component",
          C
        )), typeof p == "object" && p !== null && (T = Ke(p)), p = new s(f, T), n.mode & Xi) {
          Xt(!0);
          try {
            p = new s(f, T);
          } finally {
            Xt(!1);
          }
        }
        if (T = n.memoizedState = p.state !== null && p.state !== void 0 ? p.state : null, p.updater = pE, n.stateNode = p, p._reactInternals = n, p._reactInternalInstance = kx, typeof s.getDerivedStateFromProps == "function" && T === null && (T = ft(s) || "Component", qx.has(T) || (qx.add(T), console.error(
          "`%s` uses `getDerivedStateFromProps` but its initial state is %s. This is not recommended. Instead, define the initial state by assigning an object to `this.state` in the constructor of `%s`. This ensures that `getDerivedStateFromProps` arguments have a consistent shape.",
          T,
          p.state === null ? "null" : "undefined",
          T
        ))), typeof s.getDerivedStateFromProps == "function" || typeof p.getSnapshotBeforeUpdate == "function") {
          var D = C = T = null;
          if (typeof p.componentWillMount == "function" && p.componentWillMount.__suppressDeprecationWarning !== !0 ? T = "componentWillMount" : typeof p.UNSAFE_componentWillMount == "function" && (T = "UNSAFE_componentWillMount"), typeof p.componentWillReceiveProps == "function" && p.componentWillReceiveProps.__suppressDeprecationWarning !== !0 ? C = "componentWillReceiveProps" : typeof p.UNSAFE_componentWillReceiveProps == "function" && (C = "UNSAFE_componentWillReceiveProps"), typeof p.componentWillUpdate == "function" && p.componentWillUpdate.__suppressDeprecationWarning !== !0 ? D = "componentWillUpdate" : typeof p.UNSAFE_componentWillUpdate == "function" && (D = "UNSAFE_componentWillUpdate"), T !== null || C !== null || D !== null) {
            p = ft(s) || "Component";
            var z = typeof s.getDerivedStateFromProps == "function" ? "getDerivedStateFromProps()" : "getSnapshotBeforeUpdate()";
            Fx.has(p) || (Fx.add(p), console.error(
              `Unsafe legacy lifecycles will not be called for components using new component APIs.

%s uses %s but also contains the following legacy lifecycles:%s%s%s

The above lifecycles should be removed. Learn more about this warning here:
https://react.dev/link/unsafe-component-lifecycles`,
              p,
              z,
              T !== null ? `
  ` + T : "",
              C !== null ? `
  ` + C : "",
              D !== null ? `
  ` + D : ""
            ));
          }
        }
        p = n.stateNode, T = ft(s) || "Component", p.render || (s.prototype && typeof s.prototype.render == "function" ? console.error(
          "No `render` method found on the %s instance: did you accidentally return an object from the constructor?",
          T
        ) : console.error(
          "No `render` method found on the %s instance: you may have forgotten to define `render`.",
          T
        )), !p.getInitialState || p.getInitialState.isReactClassApproved || p.state || console.error(
          "getInitialState was defined on %s, a plain JavaScript class. This is only supported for classes created using React.createClass. Did you mean to define a state property instead?",
          T
        ), p.getDefaultProps && !p.getDefaultProps.isReactClassApproved && console.error(
          "getDefaultProps was defined on %s, a plain JavaScript class. This is only supported for classes created using React.createClass. Use a static property to define defaultProps instead.",
          T
        ), p.contextType && console.error(
          "contextType was defined as an instance property on %s. Use a static property to define contextType instead.",
          T
        ), s.childContextTypes && !Wx.has(s) && (Wx.add(s), console.error(
          "%s uses the legacy childContextTypes API which was removed in React 19. Use React.createContext() instead. (https://react.dev/link/legacy-context)",
          T
        )), s.contextTypes && !Qx.has(s) && (Qx.add(s), console.error(
          "%s uses the legacy contextTypes API which was removed in React 19. Use React.createContext() with static contextType instead. (https://react.dev/link/legacy-context)",
          T
        )), typeof p.componentShouldUpdate == "function" && console.error(
          "%s has a method called componentShouldUpdate(). Did you mean shouldComponentUpdate()? The name is phrased as a question because the function is expected to return a value.",
          T
        ), s.prototype && s.prototype.isPureReactComponent && typeof p.shouldComponentUpdate < "u" && console.error(
          "%s has a method called shouldComponentUpdate(). shouldComponentUpdate should not be used when extending React.PureComponent. Please extend React.Component if shouldComponentUpdate is used.",
          ft(s) || "A pure component"
        ), typeof p.componentDidUnmount == "function" && console.error(
          "%s has a method called componentDidUnmount(). But there is no such lifecycle method. Did you mean componentWillUnmount()?",
          T
        ), typeof p.componentDidReceiveProps == "function" && console.error(
          "%s has a method called componentDidReceiveProps(). But there is no such lifecycle method. If you meant to update the state in response to changing props, use componentWillReceiveProps(). If you meant to fetch data or run side-effects or mutations after React has updated the UI, use componentDidUpdate().",
          T
        ), typeof p.componentWillRecieveProps == "function" && console.error(
          "%s has a method called componentWillRecieveProps(). Did you mean componentWillReceiveProps()?",
          T
        ), typeof p.UNSAFE_componentWillRecieveProps == "function" && console.error(
          "%s has a method called UNSAFE_componentWillRecieveProps(). Did you mean UNSAFE_componentWillReceiveProps()?",
          T
        ), C = p.props !== f, p.props !== void 0 && C && console.error(
          "When calling super() in `%s`, make sure to pass up the same props that your component's constructor was passed.",
          T
        ), p.defaultProps && console.error(
          "Setting defaultProps as an instance property on %s is not supported and will be ignored. Instead, define defaultProps as a static property on %s.",
          T,
          T
        ), typeof p.getSnapshotBeforeUpdate != "function" || typeof p.componentDidUpdate == "function" || Zx.has(s) || (Zx.add(s), console.error(
          "%s: getSnapshotBeforeUpdate() should be used with componentDidUpdate(). This component defines getSnapshotBeforeUpdate() only.",
          ft(s)
        )), typeof p.getDerivedStateFromProps == "function" && console.error(
          "%s: getDerivedStateFromProps() is defined as an instance method and will be ignored. Instead, declare it as a static method.",
          T
        ), typeof p.getDerivedStateFromError == "function" && console.error(
          "%s: getDerivedStateFromError() is defined as an instance method and will be ignored. Instead, declare it as a static method.",
          T
        ), typeof s.getSnapshotBeforeUpdate == "function" && console.error(
          "%s: getSnapshotBeforeUpdate() is defined as a static method and will be ignored. Instead, declare it as an instance method.",
          T
        ), (C = p.state) && (typeof C != "object" || Ft(C)) && console.error("%s.state: must be set to an object or null", T), typeof p.getChildContext == "function" && typeof s.childContextTypes != "object" && console.error(
          "%s.getChildContext(): childContextTypes must be defined in order to use getChildContext().",
          T
        ), p = n.stateNode, p.props = f, p.state = n.memoizedState, p.refs = {}, wi(n), T = s.contextType, p.context = typeof T == "object" && T !== null ? Ke(T) : Tc, p.state === f && (T = ft(s) || "Component", Kx.has(T) || (Kx.add(T), console.error(
          "%s: It is not recommended to assign props directly to state because updates to props won't be reflected in state. In most cases, it is better to use props directly.",
          T
        ))), n.mode & Xi && qs.recordLegacyContextWarning(
          n,
          p
        ), qs.recordUnsafeLifecycleWarnings(
          n,
          p
        ), p.state = n.memoizedState, T = s.getDerivedStateFromProps, typeof T == "function" && (ln(
          n,
          s,
          T,
          f
        ), p.state = n.memoizedState), typeof s.getDerivedStateFromProps == "function" || typeof p.getSnapshotBeforeUpdate == "function" || typeof p.UNSAFE_componentWillMount != "function" && typeof p.componentWillMount != "function" || (T = p.state, typeof p.componentWillMount == "function" && p.componentWillMount(), typeof p.UNSAFE_componentWillMount == "function" && p.UNSAFE_componentWillMount(), T !== p.state && (console.error(
          "%s.componentWillMount(): Assigning directly to this.state is deprecated (except inside a component's constructor). Use setState instead.",
          it(n) || "Component"
        ), pE.enqueueReplaceState(
          p,
          p.state,
          null
        )), zu(n, f, p, m), Ma(), p.state = n.memoizedState), typeof p.componentDidMount == "function" && (n.flags |= 4194308), (n.mode & Vs) !== $e && (n.flags |= 134217728), p = !0;
      } else if (e === null) {
        p = n.stateNode;
        var tt = n.memoizedProps;
        C = Ro(s, tt), p.props = C;
        var st = p.context;
        D = s.contextType, T = Tc, typeof D == "object" && D !== null && (T = Ke(D)), z = s.getDerivedStateFromProps, D = typeof z == "function" || typeof p.getSnapshotBeforeUpdate == "function", tt = n.pendingProps !== tt, D || typeof p.UNSAFE_componentWillReceiveProps != "function" && typeof p.componentWillReceiveProps != "function" || (tt || st !== T) && Bd(
          n,
          p,
          f,
          T
        ), xc = !1;
        var J = n.memoizedState;
        p.state = J, zu(n, f, p, m), Ma(), st = n.memoizedState, tt || J !== st || xc ? (typeof z == "function" && (ln(
          n,
          s,
          z,
          f
        ), st = n.memoizedState), (C = xc || jd(
          n,
          s,
          C,
          f,
          J,
          st,
          T
        )) ? (D || typeof p.UNSAFE_componentWillMount != "function" && typeof p.componentWillMount != "function" || (typeof p.componentWillMount == "function" && p.componentWillMount(), typeof p.UNSAFE_componentWillMount == "function" && p.UNSAFE_componentWillMount()), typeof p.componentDidMount == "function" && (n.flags |= 4194308), (n.mode & Vs) !== $e && (n.flags |= 134217728)) : (typeof p.componentDidMount == "function" && (n.flags |= 4194308), (n.mode & Vs) !== $e && (n.flags |= 134217728), n.memoizedProps = f, n.memoizedState = st), p.props = f, p.state = st, p.context = T, p = C) : (typeof p.componentDidMount == "function" && (n.flags |= 4194308), (n.mode & Vs) !== $e && (n.flags |= 134217728), p = !1);
      } else {
        p = n.stateNode, _o(e, n), T = n.memoizedProps, D = Ro(s, T), p.props = D, z = n.pendingProps, J = p.context, st = s.contextType, C = Tc, typeof st == "object" && st !== null && (C = Ke(st)), tt = s.getDerivedStateFromProps, (st = typeof tt == "function" || typeof p.getSnapshotBeforeUpdate == "function") || typeof p.UNSAFE_componentWillReceiveProps != "function" && typeof p.componentWillReceiveProps != "function" || (T !== z || J !== C) && Bd(
          n,
          p,
          f,
          C
        ), xc = !1, J = n.memoizedState, p.state = J, zu(n, f, p, m), Ma();
        var rt = n.memoizedState;
        T !== z || J !== rt || xc || e !== null && e.dependencies !== null && mo(e.dependencies) ? (typeof tt == "function" && (ln(
          n,
          s,
          tt,
          f
        ), rt = n.memoizedState), (D = xc || jd(
          n,
          s,
          D,
          f,
          J,
          rt,
          C
        ) || e !== null && e.dependencies !== null && mo(e.dependencies)) ? (st || typeof p.UNSAFE_componentWillUpdate != "function" && typeof p.componentWillUpdate != "function" || (typeof p.componentWillUpdate == "function" && p.componentWillUpdate(f, rt, C), typeof p.UNSAFE_componentWillUpdate == "function" && p.UNSAFE_componentWillUpdate(
          f,
          rt,
          C
        )), typeof p.componentDidUpdate == "function" && (n.flags |= 4), typeof p.getSnapshotBeforeUpdate == "function" && (n.flags |= 1024)) : (typeof p.componentDidUpdate != "function" || T === e.memoizedProps && J === e.memoizedState || (n.flags |= 4), typeof p.getSnapshotBeforeUpdate != "function" || T === e.memoizedProps && J === e.memoizedState || (n.flags |= 1024), n.memoizedProps = f, n.memoizedState = rt), p.props = f, p.state = rt, p.context = C, p = D) : (typeof p.componentDidUpdate != "function" || T === e.memoizedProps && J === e.memoizedState || (n.flags |= 4), typeof p.getSnapshotBeforeUpdate != "function" || T === e.memoizedProps && J === e.memoizedState || (n.flags |= 1024), p = !1);
      }
      if (C = p, Hf(e, n), T = (n.flags & 128) !== 0, C || T) {
        if (C = n.stateNode, nf(n), T && typeof s.getDerivedStateFromError != "function")
          s = null, Yl = -1;
        else {
          if (De(n), s = Mx(C), n.mode & Xi) {
            Xt(!0);
            try {
              Mx(C);
            } finally {
              Xt(!1);
            }
          }
          ke();
        }
        n.flags |= 1, e !== null && T ? (n.child = hm(
          n,
          e.child,
          null,
          m
        ), n.child = hm(
          n,
          null,
          s,
          m
        )) : vn(e, n, s, m), n.memoizedState = C.state, e = n.child;
      } else
        e = Ga(
          e,
          n,
          m
        );
      return m = n.stateNode, p && m.props !== f && (mm || console.error(
        "It looks like %s is reassigning its own `this.props` while rendering. This is not supported and can lead to confusing bugs.",
        it(n) || "a component"
      ), mm = !0), e;
    }
    function Ky(e, n, s, f) {
      return mr(), n.flags |= 256, vn(e, n, s, f), n.child;
    }
    function Fd(e, n) {
      n && n.childContextTypes && console.error(
        `childContextTypes cannot be defined on a function component.
  %s.childContextTypes = ...`,
        n.displayName || n.name || "Component"
      ), typeof n.getDerivedStateFromProps == "function" && (e = ft(n) || "Unknown", lC[e] || (console.error(
        "%s: Function components do not support getDerivedStateFromProps.",
        e
      ), lC[e] = !0)), typeof n.contextType == "object" && n.contextType !== null && (n = ft(n) || "Unknown", nC[n] || (console.error(
        "%s: Function components do not support contextType.",
        n
      ), nC[n] = !0));
    }
    function Yf(e) {
      return { baseLanes: e, cachePool: tv() };
    }
    function Kd(e, n, s) {
      return e = e !== null ? e.childLanes & ~s : 0, n && (e |= ma), e;
    }
    function av(e, n, s) {
      var f, m = n.pendingProps;
      h(n) && (n.flags |= 128);
      var p = !1, T = (n.flags & 128) !== 0;
      if ((f = T) || (f = e !== null && e.memoizedState === null ? !1 : ($n.current & M0) !== 0), f && (p = !0, n.flags &= -129), f = (n.flags & 32) !== 0, n.flags &= -33, e === null) {
        if (xe) {
          if (p ? el(n) : oa(n), xe) {
            var C = Sn, D;
            if (!(D = !C)) {
              t: {
                var z = C;
                for (D = Zo; z.nodeType !== 8; ) {
                  if (!D) {
                    D = null;
                    break t;
                  }
                  if (z = Kn(z.nextSibling), z === null) {
                    D = null;
                    break t;
                  }
                }
                D = z;
              }
              D !== null ? ($l(), n.memoizedState = {
                dehydrated: D,
                treeContext: bh !== null ? { id: Pr, overflow: Qr } : null,
                retryLane: 536870912,
                hydrationErrors: null
              }, z = R(18, null, null, $e), z.stateNode = D, z.return = n, n.child = z, ul = n, Sn = null, D = !0) : D = !1, D = !D;
            }
            D && (by(
              n,
              C
            ), xa(n));
          }
          if (C = n.memoizedState, C !== null && (C = C.dehydrated, C !== null))
            return Fa(C) ? n.lanes = 32 : n.lanes = 536870912, null;
          nl(n);
        }
        return C = m.children, m = m.fallback, p ? (oa(n), p = n.mode, C = jf(
          {
            mode: "hidden",
            children: C
          },
          p
        ), m = fo(
          m,
          p,
          s,
          null
        ), C.return = n, m.return = n, C.sibling = m, n.child = C, p = n.child, p.memoizedState = Yf(s), p.childLanes = Kd(
          e,
          f,
          s
        ), n.memoizedState = EE, m) : (el(n), Pd(
          n,
          C
        ));
      }
      var tt = e.memoizedState;
      if (tt !== null && (C = tt.dehydrated, C !== null)) {
        if (T)
          n.flags & 256 ? (el(n), n.flags &= -257, n = Qd(
            e,
            n,
            s
          )) : n.memoizedState !== null ? (oa(n), n.child = e.child, n.flags |= 128, n = null) : (oa(n), p = m.fallback, C = n.mode, m = jf(
            {
              mode: "visible",
              children: m.children
            },
            C
          ), p = fo(
            p,
            C,
            s,
            null
          ), p.flags |= 2, m.return = n, p.return = n, m.sibling = p, n.child = m, hm(
            n,
            e.child,
            null,
            s
          ), m = n.child, m.memoizedState = Yf(s), m.childLanes = Kd(
            e,
            f,
            s
          ), n.memoizedState = EE, n = p);
        else if (el(n), xe && console.error(
          "We should not be hydrating here. This is a bug in React. Please file a bug."
        ), Fa(C)) {
          if (f = C.nextSibling && C.nextSibling.dataset, f) {
            D = f.dgst;
            var st = f.msg;
            z = f.stck;
            var J = f.cstck;
          }
          C = st, f = D, m = z, D = p = J, p = Error(C || "The server could not finish this Suspense boundary, likely due to an error during server rendering. Switched to client rendering."), p.stack = m || "", p.digest = f, f = D === void 0 ? null : D, m = {
            value: p,
            source: null,
            stack: f
          }, typeof f == "string" && eE.set(
            p,
            m
          ), Du(m), n = Qd(
            e,
            n,
            s
          );
        } else if (ui || Vn(
          e,
          n,
          s,
          !1
        ), f = (s & e.childLanes) !== 0, ui || f) {
          if (f = Pe, f !== null && (m = s & -s, m = m & 42 ? 1 : pn(
            m
          ), m = m & (f.suspendedLanes | s) ? 0 : m, m !== 0 && m !== tt.retryLane))
            throw tt.retryLane = m, Mi(
              e,
              m
            ), rn(
              f,
              e,
              m
            ), tC;
          C.data === nu || rg(), n = Qd(
            e,
            n,
            s
          );
        } else
          C.data === nu ? (n.flags |= 192, n.child = e.child, n = null) : (e = tt.treeContext, Sn = Kn(
            C.nextSibling
          ), ul = n, xe = !0, Th = null, Wr = !1, is = null, Zo = !1, e !== null && ($l(), es[ns++] = Pr, es[ns++] = Qr, es[ns++] = bh, Pr = e.id, Qr = e.overflow, bh = n), n = Pd(
            n,
            m.children
          ), n.flags |= 4096);
        return n;
      }
      return p ? (oa(n), p = m.fallback, C = n.mode, D = e.child, z = D.sibling, m = Ta(
        D,
        {
          mode: "hidden",
          children: m.children
        }
      ), m.subtreeFlags = D.subtreeFlags & 65011712, z !== null ? p = Ta(
        z,
        p
      ) : (p = fo(
        p,
        C,
        s,
        null
      ), p.flags |= 2), p.return = n, m.return = n, m.sibling = p, n.child = m, m = p, p = n.child, C = e.child.memoizedState, C === null ? C = Yf(s) : (D = C.cachePool, D !== null ? (z = Wn._currentValue, D = D.parent !== z ? { parent: z, pool: z } : D) : D = tv(), C = {
        baseLanes: C.baseLanes | s,
        cachePool: D
      }), p.memoizedState = C, p.childLanes = Kd(
        e,
        f,
        s
      ), n.memoizedState = EE, m) : (el(n), s = e.child, e = s.sibling, s = Ta(s, {
        mode: "visible",
        children: m.children
      }), s.return = n, s.sibling = null, e !== null && (f = n.deletions, f === null ? (n.deletions = [e], n.flags |= 16) : f.push(e)), n.child = s, n.memoizedState = null, s);
    }
    function Pd(e, n) {
      return n = jf(
        { mode: "visible", children: n },
        e.mode
      ), n.return = e, e.child = n;
    }
    function jf(e, n) {
      return e = R(22, e, null, n), e.lanes = 0, e.stateNode = {
        _visibility: Xv,
        _pendingMarkers: null,
        _retryCache: null,
        _transitions: null
      }, e;
    }
    function Qd(e, n, s) {
      return hm(n, e.child, null, s), e = Pd(
        n,
        n.pendingProps.children
      ), e.flags |= 2, n.memoizedState = null, e;
    }
    function Wd(e, n, s) {
      e.lanes |= n;
      var f = e.alternate;
      f !== null && (f.lanes |= n), Ay(
        e.return,
        n,
        s
      );
    }
    function Py(e, n) {
      var s = Ft(e);
      return e = !s && typeof dt(e) == "function", s || e ? (s = s ? "array" : "iterable", console.error(
        "A nested %s was passed to row #%s in <SuspenseList />. Wrap it in an additional SuspenseList to configure its revealOrder: <SuspenseList revealOrder=...> ... <SuspenseList revealOrder=...>{%s}</SuspenseList> ... </SuspenseList>",
        s,
        n,
        s
      ), !1) : !0;
    }
    function Jd(e, n, s, f, m) {
      var p = e.memoizedState;
      p === null ? e.memoizedState = {
        isBackwards: n,
        rendering: null,
        renderingStartTime: 0,
        last: f,
        tail: s,
        tailMode: m
      } : (p.isBackwards = n, p.rendering = null, p.renderingStartTime = 0, p.last = f, p.tail = s, p.tailMode = m);
    }
    function Qy(e, n, s) {
      var f = n.pendingProps, m = f.revealOrder, p = f.tail;
      if (f = f.children, m !== void 0 && m !== "forwards" && m !== "backwards" && m !== "together" && !aC[m])
        if (aC[m] = !0, typeof m == "string")
          switch (m.toLowerCase()) {
            case "together":
            case "forwards":
            case "backwards":
              console.error(
                '"%s" is not a valid value for revealOrder on <SuspenseList />. Use lowercase "%s" instead.',
                m,
                m.toLowerCase()
              );
              break;
            case "forward":
            case "backward":
              console.error(
                '"%s" is not a valid value for revealOrder on <SuspenseList />. React uses the -s suffix in the spelling. Use "%ss" instead.',
                m,
                m.toLowerCase()
              );
              break;
            default:
              console.error(
                '"%s" is not a supported revealOrder on <SuspenseList />. Did you mean "together", "forwards" or "backwards"?',
                m
              );
          }
        else
          console.error(
            '%s is not a supported value for revealOrder on <SuspenseList />. Did you mean "together", "forwards" or "backwards"?',
            m
          );
      p === void 0 || SE[p] || (p !== "collapsed" && p !== "hidden" ? (SE[p] = !0, console.error(
        '"%s" is not a supported value for tail on <SuspenseList />. Did you mean "collapsed" or "hidden"?',
        p
      )) : m !== "forwards" && m !== "backwards" && (SE[p] = !0, console.error(
        '<SuspenseList tail="%s" /> is only valid if revealOrder is "forwards" or "backwards". Did you mean to specify revealOrder="forwards"?',
        p
      )));
      t: if ((m === "forwards" || m === "backwards") && f !== void 0 && f !== null && f !== !1)
        if (Ft(f)) {
          for (var T = 0; T < f.length; T++)
            if (!Py(f[T], T)) break t;
        } else if (T = dt(f), typeof T == "function") {
          if (T = T.call(f))
            for (var C = T.next(), D = 0; !C.done; C = T.next()) {
              if (!Py(C.value, D)) break t;
              D++;
            }
        } else
          console.error(
            'A single row was passed to a <SuspenseList revealOrder="%s" />. This is not useful since it needs multiple rows. Did you mean to pass multiple children or an array?',
            m
          );
      if (vn(e, n, f, s), f = $n.current, f & M0)
        f = f & dm | M0, n.flags |= 128;
      else {
        if (e !== null && e.flags & 128)
          t: for (e = n.child; e !== null; ) {
            if (e.tag === 13)
              e.memoizedState !== null && Wd(
                e,
                s,
                n
              );
            else if (e.tag === 19)
              Wd(e, s, n);
            else if (e.child !== null) {
              e.child.return = e, e = e.child;
              continue;
            }
            if (e === n) break t;
            for (; e.sibling === null; ) {
              if (e.return === null || e.return === n)
                break t;
              e = e.return;
            }
            e.sibling.return = e.return, e = e.sibling;
          }
        f &= dm;
      }
      switch (pt($n, f, n), m) {
        case "forwards":
          for (s = n.child, m = null; s !== null; )
            e = s.alternate, e !== null && ws(e) === null && (m = s), s = s.sibling;
          s = m, s === null ? (m = n.child, n.child = null) : (m = s.sibling, s.sibling = null), Jd(
            n,
            !1,
            m,
            s,
            p
          );
          break;
        case "backwards":
          for (s = null, m = n.child, n.child = null; m !== null; ) {
            if (e = m.alternate, e !== null && ws(e) === null) {
              n.child = m;
              break;
            }
            e = m.sibling, m.sibling = s, s = m, m = e;
          }
          Jd(
            n,
            !0,
            s,
            null,
            p
          );
          break;
        case "together":
          Jd(n, !1, null, null, void 0);
          break;
        default:
          n.memoizedState = null;
      }
      return n.child;
    }
    function Ga(e, n, s) {
      if (e !== null && (n.dependencies = e.dependencies), Yl = -1, Mc |= n.lanes, !(s & n.childLanes))
        if (e !== null) {
          if (Vn(
            e,
            n,
            s,
            !1
          ), (s & n.childLanes) === 0)
            return null;
        } else return null;
      if (e !== null && n.child !== e.child)
        throw Error("Resuming work not yet implemented.");
      if (n.child !== null) {
        for (e = n.child, s = Ta(e, e.pendingProps), n.child = s, s.return = n; e.sibling !== null; )
          e = e.sibling, s = s.sibling = Ta(e, e.pendingProps), s.return = n;
        s.sibling = null;
      }
      return n.child;
    }
    function $d(e, n) {
      return e.lanes & n ? !0 : (e = e.dependencies, !!(e !== null && mo(e)));
    }
    function GS(e, n, s) {
      switch (n.tag) {
        case 3:
          Nt(
            n,
            n.stateNode.containerInfo
          ), go(
            n,
            Wn,
            e.memoizedState.cache
          ), mr();
          break;
        case 27:
        case 5:
          K(n);
          break;
        case 4:
          Nt(
            n,
            n.stateNode.containerInfo
          );
          break;
        case 10:
          go(
            n,
            n.type,
            n.memoizedProps.value
          );
          break;
        case 12:
          s & n.childLanes && (n.flags |= 4), n.flags |= 2048;
          var f = n.stateNode;
          f.effectDuration = -0, f.passiveEffectDuration = -0;
          break;
        case 13:
          if (f = n.memoizedState, f !== null)
            return f.dehydrated !== null ? (el(n), n.flags |= 128, null) : s & n.child.childLanes ? av(
              e,
              n,
              s
            ) : (el(n), e = Ga(
              e,
              n,
              s
            ), e !== null ? e.sibling : null);
          el(n);
          break;
        case 19:
          var m = (e.flags & 128) !== 0;
          if (f = (s & n.childLanes) !== 0, f || (Vn(
            e,
            n,
            s,
            !1
          ), f = (s & n.childLanes) !== 0), m) {
            if (f)
              return Qy(
                e,
                n,
                s
              );
            n.flags |= 128;
          }
          if (m = n.memoizedState, m !== null && (m.rendering = null, m.tail = null, m.lastEffect = null), pt(
            $n,
            $n.current,
            n
          ), f) break;
          return null;
        case 22:
        case 23:
          return n.lanes = 0, Vd(e, n, s);
        case 24:
          go(
            n,
            Wn,
            e.memoizedState.cache
          );
      }
      return Ga(e, n, s);
    }
    function tg(e, n, s) {
      if (n._debugNeedsRemount && e !== null) {
        s = Sd(
          n.type,
          n.key,
          n.pendingProps,
          n._debugOwner || null,
          n.mode,
          n.lanes
        ), s._debugStack = n._debugStack, s._debugTask = n._debugTask;
        var f = n.return;
        if (f === null) throw Error("Cannot swap the root fiber.");
        if (e.alternate = null, n.alternate = null, s.index = n.index, s.sibling = n.sibling, s.return = n.return, s.ref = n.ref, s._debugInfo = n._debugInfo, n === f.child)
          f.child = s;
        else {
          var m = f.child;
          if (m === null)
            throw Error("Expected parent to have a child.");
          for (; m.sibling !== n; )
            if (m = m.sibling, m === null)
              throw Error("Expected to find the previous sibling.");
          m.sibling = s;
        }
        return n = f.deletions, n === null ? (f.deletions = [e], f.flags |= 16) : n.push(e), s.flags |= 2, s;
      }
      if (e !== null)
        if (e.memoizedProps !== n.pendingProps || n.type !== e.type)
          ui = !0;
        else {
          if (!$d(e, s) && !(n.flags & 128))
            return ui = !1, GS(
              e,
              n,
              s
            );
          ui = !!(e.flags & 131072);
        }
      else
        ui = !1, (f = xe) && ($l(), f = (n.flags & 1048576) !== 0), f && (f = n.index, $l(), W_(n, Vv, f));
      switch (n.lanes = 0, n.tag) {
        case 16:
          t: if (f = n.pendingProps, e = Rc(n.elementType), n.type = e, typeof e == "function")
            vy(e) ? (f = Ro(
              e,
              f
            ), n.tag = 1, n.type = e = hr(e), n = Fy(
              null,
              n,
              e,
              f,
              s
            )) : (n.tag = 0, Fd(n, e), n.type = e = hr(e), n = Zd(
              null,
              n,
              e,
              f,
              s
            ));
          else {
            if (e != null) {
              if (m = e.$$typeof, m === Hs) {
                n.tag = 11, n.type = e = _y(e), n = kd(
                  null,
                  n,
                  e,
                  f,
                  s
                );
                break t;
              } else if (m === uh) {
                n.tag = 14, n = Ua(
                  null,
                  n,
                  e,
                  f,
                  s
                );
                break t;
              }
            }
            throw n = "", e !== null && typeof e == "object" && e.$$typeof === sl && (n = " Did you wrap a component in React.lazy() more than once?"), e = ft(e) || e, Error(
              "Element type is invalid. Received a promise that resolves to: " + e + ". Lazy element type must resolve to a class or function." + n
            );
          }
          return n;
        case 0:
          return Zd(
            e,
            n,
            n.type,
            n.pendingProps,
            s
          );
        case 1:
          return f = n.type, m = Ro(
            f,
            n.pendingProps
          ), Fy(
            e,
            n,
            f,
            m,
            s
          );
        case 3:
          t: {
            if (Nt(
              n,
              n.stateNode.containerInfo
            ), e === null)
              throw Error(
                "Should have a current fiber. This is a bug in React."
              );
            f = n.pendingProps;
            var p = n.memoizedState;
            m = p.element, _o(e, n), zu(n, f, null, s);
            var T = n.memoizedState;
            if (f = T.cache, go(n, Wn, f), f !== p.cache && My(
              n,
              [Wn],
              s,
              !0
            ), Ma(), f = T.element, p.isDehydrated)
              if (p = {
                element: f,
                isDehydrated: !1,
                cache: T.cache
              }, n.updateQueue.baseState = p, n.memoizedState = p, n.flags & 256) {
                n = Ky(
                  e,
                  n,
                  f,
                  s
                );
                break t;
              } else if (f !== m) {
                m = $i(
                  Error(
                    "This root received an early update, before anything was able hydrate. Switched the entire root to client rendering."
                  ),
                  n
                ), Du(m), n = Ky(
                  e,
                  n,
                  f,
                  s
                );
                break t;
              } else {
                switch (e = n.stateNode.containerInfo, e.nodeType) {
                  case 9:
                    e = e.body;
                    break;
                  default:
                    e = e.nodeName === "HTML" ? e.ownerDocument.body : e;
                }
                for (Sn = Kn(e.firstChild), ul = n, xe = !0, Th = null, Wr = !1, is = null, Zo = !0, e = Xx(
                  n,
                  null,
                  f,
                  s
                ), n.child = e; e; )
                  e.flags = e.flags & -3 | 4096, e = e.sibling;
              }
            else {
              if (mr(), f === m) {
                n = Ga(
                  e,
                  n,
                  s
                );
                break t;
              }
              vn(
                e,
                n,
                f,
                s
              );
            }
            n = n.child;
          }
          return n;
        case 26:
          return Hf(e, n), e === null ? (e = Gs(
            n.type,
            null,
            n.pendingProps,
            null
          )) ? n.memoizedState = e : xe || (e = n.type, s = n.pendingProps, f = Tt(
            Pa.current
          ), f = fe(
            f
          ).createElement(e), f[ri] = n, f[Bi] = s, cn(f, e, s), _n(f), n.stateNode = f) : n.memoizedState = Gs(
            n.type,
            e.memoizedProps,
            n.pendingProps,
            e.memoizedState
          ), null;
        case 27:
          return K(n), e === null && xe && (f = Tt(Pa.current), m = Y(), f = n.stateNode = Up(
            n.type,
            n.pendingProps,
            f,
            m,
            !1
          ), Wr || (m = Be(
            f,
            n.type,
            n.pendingProps,
            m
          ), m !== null && (ta(n, 0).serverProps = m)), ul = n, Zo = !0, m = Sn, Za(n.type) ? (VE = m, Sn = Kn(
            f.firstChild
          )) : Sn = m), vn(
            e,
            n,
            n.pendingProps.children,
            s
          ), Hf(e, n), e === null && (n.flags |= 4194304), n.child;
        case 5:
          return e === null && xe && (p = Y(), f = fd(
            n.type,
            p.ancestorInfo
          ), m = Sn, (T = !m) || (T = zo(
            m,
            n.type,
            n.pendingProps,
            Zo
          ), T !== null ? (n.stateNode = T, Wr || (p = Be(
            T,
            n.type,
            n.pendingProps,
            p
          ), p !== null && (ta(n, 0).serverProps = p)), ul = n, Sn = Kn(
            T.firstChild
          ), Zo = !1, p = !0) : p = !1, T = !p), T && (f && by(n, m), xa(n))), K(n), m = n.type, p = n.pendingProps, T = e !== null ? e.memoizedProps : null, f = p.children, qa(m, p) ? f = null : T !== null && qa(m, T) && (n.flags |= 32), n.memoizedState !== null && (m = Eo(
            e,
            n,
            Rl,
            null,
            null,
            s
          ), k0._currentValue = m), Hf(e, n), vn(
            e,
            n,
            f,
            s
          ), n.child;
        case 6:
          return e === null && xe && (e = n.pendingProps, s = Y(), f = s.ancestorInfo.current, e = f != null ? uf(
            e,
            f.tag,
            s.ancestorInfo.implicitRootScope
          ) : !0, s = Sn, (f = !s) || (f = Fn(
            s,
            n.pendingProps,
            Zo
          ), f !== null ? (n.stateNode = f, ul = n, Sn = null, f = !0) : f = !1, f = !f), f && (e && by(n, s), xa(n))), null;
        case 13:
          return av(e, n, s);
        case 4:
          return Nt(
            n,
            n.stateNode.containerInfo
          ), f = n.pendingProps, e === null ? n.child = hm(
            n,
            null,
            f,
            s
          ) : vn(
            e,
            n,
            f,
            s
          ), n.child;
        case 11:
          return kd(
            e,
            n,
            n.type,
            n.pendingProps,
            s
          );
        case 7:
          return vn(
            e,
            n,
            n.pendingProps,
            s
          ), n.child;
        case 8:
          return vn(
            e,
            n,
            n.pendingProps.children,
            s
          ), n.child;
        case 12:
          return n.flags |= 4, n.flags |= 2048, f = n.stateNode, f.effectDuration = -0, f.passiveEffectDuration = -0, vn(
            e,
            n,
            n.pendingProps.children,
            s
          ), n.child;
        case 10:
          return f = n.type, m = n.pendingProps, p = m.value, "value" in m || sC || (sC = !0, console.error(
            "The `value` prop is required for the `<Context.Provider>`. Did you misspell it or forget to pass it?"
          )), go(n, f, p), vn(
            e,
            n,
            m.children,
            s
          ), n.child;
        case 9:
          return m = n.type._context, f = n.pendingProps.children, typeof f != "function" && console.error(
            "A context consumer was rendered with multiple children, or a child that isn't a function. A context consumer expects a single child that is a function. If you did pass a function, make sure there is no trailing or leading whitespace around it."
          ), yo(n), m = Ke(m), De(n), f = gE(
            f,
            m,
            void 0
          ), ke(), n.flags |= 1, vn(
            e,
            n,
            f,
            s
          ), n.child;
        case 14:
          return Ua(
            e,
            n,
            n.type,
            n.pendingProps,
            s
          );
        case 15:
          return Gf(
            e,
            n,
            n.type,
            n.pendingProps,
            s
          );
        case 19:
          return Qy(
            e,
            n,
            s
          );
        case 31:
          return f = n.pendingProps, s = n.mode, f = {
            mode: f.mode,
            children: f.children
          }, e === null ? (e = jf(
            f,
            s
          ), e.ref = n.ref, n.child = e, e.return = n, n = e) : (e = Ta(e.child, f), e.ref = n.ref, n.child = e, e.return = n, n = e), n;
        case 22:
          return Vd(e, n, s);
        case 24:
          return yo(n), f = Ke(Wn), e === null ? (m = Oy(), m === null && (m = Pe, p = vf(), m.pooledCache = p, yr(p), p !== null && (m.pooledCacheLanes |= s), m = p), n.memoizedState = {
            parent: f,
            cache: m
          }, wi(n), go(n, Wn, m)) : (e.lanes & s && (_o(e, n), zu(n, null, null, s), Ma()), m = e.memoizedState, p = n.memoizedState, m.parent !== f ? (m = {
            parent: f,
            cache: f
          }, n.memoizedState = m, n.lanes === 0 && (n.memoizedState = n.updateQueue.baseState = m), go(n, Wn, f)) : (f = p.cache, go(n, Wn, f), f !== m.cache && My(
            n,
            [Wn],
            s,
            !0
          ))), vn(
            e,
            n,
            n.pendingProps.children,
            s
          ), n.child;
        case 29:
          throw n.pendingProps;
      }
      throw Error(
        "Unknown unit of work tag (" + n.tag + "). This error is likely caused by a bug in React. Please file an issue."
      );
    }
    function zi(e) {
      e.flags |= 4;
    }
    function Bf(e, n) {
      if (n.type !== "stylesheet" || (n.state.loading & rs) !== Yh)
        e.flags &= -16777217;
      else if (e.flags |= 16777216, !th(n)) {
        if (n = os.current, n !== null && ((de & 4194048) === de ? Ko !== null : (de & 62914560) !== de && !(de & 536870912) || n !== Ko))
          throw x0 = oE, yx;
        e.flags |= 8192;
      }
    }
    function If(e, n) {
      n !== null && (e.flags |= 4), e.flags & 16384 && (n = e.tag !== 22 ? _l() : 536870912, e.lanes |= n, Lh |= n);
    }
    function Ao(e, n) {
      if (!xe)
        switch (e.tailMode) {
          case "hidden":
            n = e.tail;
            for (var s = null; n !== null; )
              n.alternate !== null && (s = n), n = n.sibling;
            s === null ? e.tail = null : s.sibling = null;
            break;
          case "collapsed":
            s = e.tail;
            for (var f = null; s !== null; )
              s.alternate !== null && (f = s), s = s.sibling;
            f === null ? n || e.tail === null ? e.tail = null : e.tail.sibling = null : f.sibling = null;
        }
    }
    function Ge(e) {
      var n = e.alternate !== null && e.alternate.child === e.child, s = 0, f = 0;
      if (n)
        if ((e.mode & Si) !== $e) {
          for (var m = e.selfBaseDuration, p = e.child; p !== null; )
            s |= p.lanes | p.childLanes, f |= p.subtreeFlags & 65011712, f |= p.flags & 65011712, m += p.treeBaseDuration, p = p.sibling;
          e.treeBaseDuration = m;
        } else
          for (m = e.child; m !== null; )
            s |= m.lanes | m.childLanes, f |= m.subtreeFlags & 65011712, f |= m.flags & 65011712, m.return = e, m = m.sibling;
      else if ((e.mode & Si) !== $e) {
        m = e.actualDuration, p = e.selfBaseDuration;
        for (var T = e.child; T !== null; )
          s |= T.lanes | T.childLanes, f |= T.subtreeFlags, f |= T.flags, m += T.actualDuration, p += T.treeBaseDuration, T = T.sibling;
        e.actualDuration = m, e.treeBaseDuration = p;
      } else
        for (m = e.child; m !== null; )
          s |= m.lanes | m.childLanes, f |= m.subtreeFlags, f |= m.flags, m.return = e, m = m.sibling;
      return e.subtreeFlags |= f, e.childLanes = s, n;
    }
    function sv(e, n, s) {
      var f = n.pendingProps;
      switch (bd(n), n.tag) {
        case 31:
        case 16:
        case 15:
        case 0:
        case 11:
        case 7:
        case 8:
        case 12:
        case 9:
        case 14:
          return Ge(n), null;
        case 1:
          return Ge(n), null;
        case 3:
          return s = n.stateNode, f = null, e !== null && (f = e.memoizedState.cache), n.memoizedState.cache !== f && (n.flags |= 2048), bs(Wn, n), Rt(n), s.pendingContext && (s.context = s.pendingContext, s.pendingContext = null), (e === null || e.child === null) && (gr(n) ? (Ry(), zi(n)) : e === null || e.memoizedState.isDehydrated && !(n.flags & 256) || (n.flags |= 1024, Cy())), Ge(n), null;
        case 26:
          return s = n.memoizedState, e === null ? (zi(n), s !== null ? (Ge(n), Bf(
            n,
            s
          )) : (Ge(n), n.flags &= -16777217)) : s ? s !== e.memoizedState ? (zi(n), Ge(n), Bf(
            n,
            s
          )) : (Ge(n), n.flags &= -16777217) : (e.memoizedProps !== f && zi(n), Ge(n), n.flags &= -16777217), null;
        case 27:
          nt(n), s = Tt(Pa.current);
          var m = n.type;
          if (e !== null && n.stateNode != null)
            e.memoizedProps !== f && zi(n);
          else {
            if (!f) {
              if (n.stateNode === null)
                throw Error(
                  "We must have new props for new mounts. This error is likely caused by a bug in React. Please file an issue."
                );
              return Ge(n), null;
            }
            e = Y(), gr(n) ? Ty(n) : (e = Up(
              m,
              f,
              s,
              e,
              !0
            ), n.stateNode = e, zi(n));
          }
          return Ge(n), null;
        case 5:
          if (nt(n), s = n.type, e !== null && n.stateNode != null)
            e.memoizedProps !== f && zi(n);
          else {
            if (!f) {
              if (n.stateNode === null)
                throw Error(
                  "We must have new props for new mounts. This error is likely caused by a bug in React. Please file an issue."
                );
              return Ge(n), null;
            }
            if (m = Y(), gr(n))
              Ty(n);
            else {
              switch (e = Tt(Pa.current), fd(s, m.ancestorInfo), m = m.context, e = fe(e), m) {
                case xm:
                  e = e.createElementNS(bc, s);
                  break;
                case p1:
                  e = e.createElementNS(
                    Sh,
                    s
                  );
                  break;
                default:
                  switch (s) {
                    case "svg":
                      e = e.createElementNS(
                        bc,
                        s
                      );
                      break;
                    case "math":
                      e = e.createElementNS(
                        Sh,
                        s
                      );
                      break;
                    case "script":
                      e = e.createElement("div"), e.innerHTML = "<script><\/script>", e = e.removeChild(e.firstChild);
                      break;
                    case "select":
                      e = typeof f.is == "string" ? e.createElement("select", { is: f.is }) : e.createElement("select"), f.multiple ? e.multiple = !0 : f.size && (e.size = f.size);
                      break;
                    default:
                      e = typeof f.is == "string" ? e.createElement(s, {
                        is: f.is
                      }) : e.createElement(s), s.indexOf("-") === -1 && (s !== s.toLowerCase() && console.error(
                        "<%s /> is using incorrect casing. Use PascalCase for React components, or lowercase for HTML elements.",
                        s
                      ), Object.prototype.toString.call(e) !== "[object HTMLUnknownElement]" || js.call(
                        CC,
                        s
                      ) || (CC[s] = !0, console.error(
                        "The tag <%s> is unrecognized in this browser. If you meant to render a React component, start its name with an uppercase letter.",
                        s
                      )));
                  }
              }
              e[ri] = n, e[Bi] = f;
              t: for (m = n.child; m !== null; ) {
                if (m.tag === 5 || m.tag === 6)
                  e.appendChild(m.stateNode);
                else if (m.tag !== 4 && m.tag !== 27 && m.child !== null) {
                  m.child.return = m, m = m.child;
                  continue;
                }
                if (m === n) break t;
                for (; m.sibling === null; ) {
                  if (m.return === null || m.return === n)
                    break t;
                  m = m.return;
                }
                m.sibling.return = m.return, m = m.sibling;
              }
              n.stateNode = e;
              t: switch (cn(e, s, f), s) {
                case "button":
                case "input":
                case "select":
                case "textarea":
                  e = !!f.autoFocus;
                  break t;
                case "img":
                  e = !0;
                  break t;
                default:
                  e = !1;
              }
              e && zi(n);
            }
          }
          return Ge(n), n.flags &= -16777217, null;
        case 6:
          if (e && n.stateNode != null)
            e.memoizedProps !== f && zi(n);
          else {
            if (typeof f != "string" && n.stateNode === null)
              throw Error(
                "We must have new props for new mounts. This error is likely caused by a bug in React. Please file an issue."
              );
            if (e = Tt(Pa.current), s = Y(), gr(n)) {
              e = n.stateNode, s = n.memoizedProps, m = !Wr, f = null;
              var p = ul;
              if (p !== null)
                switch (p.tag) {
                  case 3:
                    m && (m = Rg(
                      e,
                      s,
                      f
                    ), m !== null && (ta(n, 0).serverProps = m));
                    break;
                  case 27:
                  case 5:
                    f = p.memoizedProps, m && (m = Rg(
                      e,
                      s,
                      f
                    ), m !== null && (ta(
                      n,
                      0
                    ).serverProps = m));
                }
              e[ri] = n, e = !!(e.nodeValue === s || f !== null && f.suppressHydrationWarning === !0 || Cp(e.nodeValue, s)), e || xa(n);
            } else
              m = s.ancestorInfo.current, m != null && uf(
                f,
                m.tag,
                s.ancestorInfo.implicitRootScope
              ), e = fe(e).createTextNode(
                f
              ), e[ri] = n, n.stateNode = e;
          }
          return Ge(n), null;
        case 13:
          if (f = n.memoizedState, e === null || e.memoizedState !== null && e.memoizedState.dehydrated !== null) {
            if (m = gr(n), f !== null && f.dehydrated !== null) {
              if (e === null) {
                if (!m)
                  throw Error(
                    "A dehydrated suspense component was completed without a hydrated node. This is probably a bug in React."
                  );
                if (m = n.memoizedState, m = m !== null ? m.dehydrated : null, !m)
                  throw Error(
                    "Expected to have a hydrated suspense instance. This error is likely caused by a bug in React. Please file an issue."
                  );
                m[ri] = n, Ge(n), (n.mode & Si) !== $e && f !== null && (m = n.child, m !== null && (n.treeBaseDuration -= m.treeBaseDuration));
              } else
                Ry(), mr(), !(n.flags & 128) && (n.memoizedState = null), n.flags |= 4, Ge(n), (n.mode & Si) !== $e && f !== null && (m = n.child, m !== null && (n.treeBaseDuration -= m.treeBaseDuration));
              m = !1;
            } else
              m = Cy(), e !== null && e.memoizedState !== null && (e.memoizedState.hydrationErrors = m), m = !0;
            if (!m)
              return n.flags & 256 ? (nl(n), n) : (nl(n), null);
          }
          return nl(n), n.flags & 128 ? (n.lanes = s, (n.mode & Si) !== $e && Ra(n), n) : (s = f !== null, e = e !== null && e.memoizedState !== null, s && (f = n.child, m = null, f.alternate !== null && f.alternate.memoizedState !== null && f.alternate.memoizedState.cachePool !== null && (m = f.alternate.memoizedState.cachePool.pool), p = null, f.memoizedState !== null && f.memoizedState.cachePool !== null && (p = f.memoizedState.cachePool.pool), p !== m && (f.flags |= 2048)), s !== e && s && (n.child.flags |= 8192), If(n, n.updateQueue), Ge(n), (n.mode & Si) !== $e && s && (e = n.child, e !== null && (n.treeBaseDuration -= e.treeBaseDuration)), null);
        case 4:
          return Rt(n), e === null && Tp(
            n.stateNode.containerInfo
          ), Ge(n), null;
        case 10:
          return bs(n.type, n), Ge(n), null;
        case 19:
          if (xt($n, n), m = n.memoizedState, m === null) return Ge(n), null;
          if (f = (n.flags & 128) !== 0, p = m.rendering, p === null)
            if (f) Ao(m, !1);
            else {
              if (En !== eu || e !== null && e.flags & 128)
                for (e = n.child; e !== null; ) {
                  if (p = ws(e), p !== null) {
                    for (n.flags |= 128, Ao(m, !1), e = p.updateQueue, n.updateQueue = e, If(n, e), n.subtreeFlags = 0, e = s, s = n.child; s !== null; )
                      Sy(s, e), s = s.sibling;
                    return pt(
                      $n,
                      $n.current & dm | M0,
                      n
                    ), n.child;
                  }
                  e = e.sibling;
                }
              m.tail !== null && Qa() > a1 && (n.flags |= 128, f = !0, Ao(m, !1), n.lanes = 4194304);
            }
          else {
            if (!f)
              if (e = ws(p), e !== null) {
                if (n.flags |= 128, f = !0, e = e.updateQueue, n.updateQueue = e, If(n, e), Ao(m, !0), m.tail === null && m.tailMode === "hidden" && !p.alternate && !xe)
                  return Ge(n), null;
              } else
                2 * Qa() - m.renderingStartTime > a1 && s !== 536870912 && (n.flags |= 128, f = !0, Ao(m, !1), n.lanes = 4194304);
            m.isBackwards ? (p.sibling = n.child, n.child = p) : (e = m.last, e !== null ? e.sibling = p : n.child = p, m.last = p);
          }
          return m.tail !== null ? (e = m.tail, m.rendering = e, m.tail = e.sibling, m.renderingStartTime = Qa(), e.sibling = null, s = $n.current, s = f ? s & dm | M0 : s & dm, pt($n, s, n), e) : (Ge(n), null);
        case 22:
        case 23:
          return nl(n), la(n), f = n.memoizedState !== null, e !== null ? e.memoizedState !== null !== f && (n.flags |= 8192) : f && (n.flags |= 8192), f ? s & 536870912 && !(n.flags & 128) && (Ge(n), n.subtreeFlags & 6 && (n.flags |= 8192)) : Ge(n), s = n.updateQueue, s !== null && If(n, s.retryQueue), s = null, e !== null && e.memoizedState !== null && e.memoizedState.cachePool !== null && (s = e.memoizedState.cachePool.pool), f = null, n.memoizedState !== null && n.memoizedState.cachePool !== null && (f = n.memoizedState.cachePool.pool), f !== s && (n.flags |= 2048), e !== null && xt(Rh, n), null;
        case 24:
          return s = null, e !== null && (s = e.memoizedState.cache), n.memoizedState.cache !== s && (n.flags |= 2048), bs(Wn, n), Ge(n), null;
        case 25:
          return null;
        case 30:
          return null;
      }
      throw Error(
        "Unknown unit of work tag (" + n.tag + "). This error is likely caused by a bug in React. Please file an issue."
      );
    }
    function ov(e, n) {
      switch (bd(n), n.tag) {
        case 1:
          return e = n.flags, e & 65536 ? (n.flags = e & -65537 | 128, (n.mode & Si) !== $e && Ra(n), n) : null;
        case 3:
          return bs(Wn, n), Rt(n), e = n.flags, e & 65536 && !(e & 128) ? (n.flags = e & -65537 | 128, n) : null;
        case 26:
        case 27:
        case 5:
          return nt(n), null;
        case 13:
          if (nl(n), e = n.memoizedState, e !== null && e.dehydrated !== null) {
            if (n.alternate === null)
              throw Error(
                "Threw in newly mounted dehydrated component. This is likely a bug in React. Please file an issue."
              );
            mr();
          }
          return e = n.flags, e & 65536 ? (n.flags = e & -65537 | 128, (n.mode & Si) !== $e && Ra(n), n) : null;
        case 19:
          return xt($n, n), null;
        case 4:
          return Rt(n), null;
        case 10:
          return bs(n.type, n), null;
        case 22:
        case 23:
          return nl(n), la(n), e !== null && xt(Rh, n), e = n.flags, e & 65536 ? (n.flags = e & -65537 | 128, (n.mode & Si) !== $e && Ra(n), n) : null;
        case 24:
          return bs(Wn, n), null;
        case 25:
          return null;
        default:
          return null;
      }
    }
    function Wy(e, n) {
      switch (bd(n), n.tag) {
        case 3:
          bs(Wn, n), Rt(n);
          break;
        case 26:
        case 27:
        case 5:
          nt(n);
          break;
        case 4:
          Rt(n);
          break;
        case 13:
          nl(n);
          break;
        case 19:
          xt($n, n);
          break;
        case 10:
          bs(n.type, n);
          break;
        case 22:
        case 23:
          nl(n), la(n), e !== null && xt(Rh, n);
          break;
        case 24:
          bs(Wn, n);
      }
    }
    function ra(e) {
      return (e.mode & Si) !== $e;
    }
    function Jy(e, n) {
      ra(e) ? (na(), Cr(n, e), Tl()) : Cr(n, e);
    }
    function eg(e, n, s) {
      ra(e) ? (na(), Rr(
        s,
        e,
        n
      ), Tl()) : Rr(
        s,
        e,
        n
      );
    }
    function Cr(e, n) {
      try {
        var s = n.updateQueue, f = s !== null ? s.lastEffect : null;
        if (f !== null) {
          var m = f.next;
          s = m;
          do {
            if ((s.tag & e) === e && ((e & Jn) !== ls ? Ct !== null && typeof Ct.markComponentPassiveEffectMountStarted == "function" && Ct.markComponentPassiveEffectMountStarted(
              n
            ) : (e & Ei) !== ls && Ct !== null && typeof Ct.markComponentLayoutEffectMountStarted == "function" && Ct.markComponentLayoutEffectMountStarted(
              n
            ), f = void 0, (e & cl) !== ls && (bm = !0), f = wt(
              n,
              vM,
              s
            ), (e & cl) !== ls && (bm = !1), (e & Jn) !== ls ? Ct !== null && typeof Ct.markComponentPassiveEffectMountStopped == "function" && Ct.markComponentPassiveEffectMountStopped() : (e & Ei) !== ls && Ct !== null && typeof Ct.markComponentLayoutEffectMountStopped == "function" && Ct.markComponentLayoutEffectMountStopped(), f !== void 0 && typeof f != "function")) {
              var p = void 0;
              p = s.tag & Ei ? "useLayoutEffect" : s.tag & cl ? "useInsertionEffect" : "useEffect";
              var T = void 0;
              T = f === null ? " You returned null. If your effect does not require clean up, return undefined (or nothing)." : typeof f.then == "function" ? `

It looks like you wrote ` + p + `(async () => ...) or returned a Promise. Instead, write the async function inside your effect and call it immediately:

` + p + `(() => {
  async function fetchData() {
    // You can await here
    const response = await MyAPI.getData(someId);
    // ...
  }
  fetchData();
}, [someId]); // Or [] if effect doesn't need props or state

Learn more about data fetching with Hooks: https://react.dev/link/hooks-data-fetching` : " You returned: " + f, wt(
                n,
                function(C, D) {
                  console.error(
                    "%s must not return anything besides a function, which is used for clean-up.%s",
                    C,
                    D
                  );
                },
                p,
                T
              );
            }
            s = s.next;
          } while (s !== m);
        }
      } catch (C) {
        jt(n, n.return, C);
      }
    }
    function Rr(e, n, s) {
      try {
        var f = n.updateQueue, m = f !== null ? f.lastEffect : null;
        if (m !== null) {
          var p = m.next;
          f = p;
          do {
            if ((f.tag & e) === e) {
              var T = f.inst, C = T.destroy;
              C !== void 0 && (T.destroy = void 0, (e & Jn) !== ls ? Ct !== null && typeof Ct.markComponentPassiveEffectUnmountStarted == "function" && Ct.markComponentPassiveEffectUnmountStarted(
                n
              ) : (e & Ei) !== ls && Ct !== null && typeof Ct.markComponentLayoutEffectUnmountStarted == "function" && Ct.markComponentLayoutEffectUnmountStarted(
                n
              ), (e & cl) !== ls && (bm = !0), m = n, wt(
                m,
                SM,
                m,
                s,
                C
              ), (e & cl) !== ls && (bm = !1), (e & Jn) !== ls ? Ct !== null && typeof Ct.markComponentPassiveEffectUnmountStopped == "function" && Ct.markComponentPassiveEffectUnmountStopped() : (e & Ei) !== ls && Ct !== null && typeof Ct.markComponentLayoutEffectUnmountStopped == "function" && Ct.markComponentLayoutEffectUnmountStopped());
            }
            f = f.next;
          } while (f !== p);
        }
      } catch (D) {
        jt(n, n.return, D);
      }
    }
    function $y(e, n) {
      ra(e) ? (na(), Cr(n, e), Tl()) : Cr(n, e);
    }
    function Xf(e, n, s) {
      ra(e) ? (na(), Rr(
        s,
        e,
        n
      ), Tl()) : Rr(
        s,
        e,
        n
      );
    }
    function tp(e) {
      var n = e.updateQueue;
      if (n !== null) {
        var s = e.stateNode;
        e.type.defaultProps || "ref" in e.memoizedProps || mm || (s.props !== e.memoizedProps && console.error(
          "Expected %s props to match memoized props before processing the update queue. This might either be because of a bug in React, or because a component reassigns its own `this.props`. Please file an issue.",
          it(e) || "instance"
        ), s.state !== e.memoizedState && console.error(
          "Expected %s state to match memoized state before processing the update queue. This might either be because of a bug in React, or because a component reassigns its own `this.state`. Please file an issue.",
          it(e) || "instance"
        ));
        try {
          wt(
            e,
            ev,
            n,
            s
          );
        } catch (f) {
          jt(e, e.return, f);
        }
      }
    }
    function rv(e, n, s) {
      return e.getSnapshotBeforeUpdate(n, s);
    }
    function HS(e, n) {
      var s = n.memoizedProps, f = n.memoizedState;
      n = e.stateNode, e.type.defaultProps || "ref" in e.memoizedProps || mm || (n.props !== e.memoizedProps && console.error(
        "Expected %s props to match memoized props before getSnapshotBeforeUpdate. This might either be because of a bug in React, or because a component reassigns its own `this.props`. Please file an issue.",
        it(e) || "instance"
      ), n.state !== e.memoizedState && console.error(
        "Expected %s state to match memoized state before getSnapshotBeforeUpdate. This might either be because of a bug in React, or because a component reassigns its own `this.state`. Please file an issue.",
        it(e) || "instance"
      ));
      try {
        var m = Ro(
          e.type,
          s,
          e.elementType === e.type
        ), p = wt(
          e,
          rv,
          n,
          m,
          f
        );
        s = oC, p !== void 0 || s.has(e.type) || (s.add(e.type), wt(e, function() {
          console.error(
            "%s.getSnapshotBeforeUpdate(): A snapshot value (or null) must be returned. You have returned undefined.",
            it(e)
          );
        })), n.__reactInternalSnapshotBeforeUpdate = p;
      } catch (T) {
        jt(e, e.return, T);
      }
    }
    function ng(e, n, s) {
      s.props = Ro(
        e.type,
        e.memoizedProps
      ), s.state = e.memoizedState, ra(e) ? (na(), wt(
        e,
        Nx,
        e,
        n,
        s
      ), Tl()) : wt(
        e,
        Nx,
        e,
        n,
        s
      );
    }
    function uv(e) {
      var n = e.ref;
      if (n !== null) {
        switch (e.tag) {
          case 26:
          case 27:
          case 5:
            var s = e.stateNode;
            break;
          case 30:
            s = e.stateNode;
            break;
          default:
            s = e.stateNode;
        }
        if (typeof n == "function")
          if (ra(e))
            try {
              na(), e.refCleanup = n(s);
            } finally {
              Tl();
            }
          else e.refCleanup = n(s);
        else
          typeof n == "string" ? console.error("String refs are no longer supported.") : n.hasOwnProperty("current") || console.error(
            "Unexpected ref object provided for %s. Use either a ref-setter function or React.createRef().",
            it(e)
          ), n.current = s;
      }
    }
    function Fu(e, n) {
      try {
        wt(e, uv, e);
      } catch (s) {
        jt(e, n, s);
      }
    }
    function Ol(e, n) {
      var s = e.ref, f = e.refCleanup;
      if (s !== null)
        if (typeof f == "function")
          try {
            if (ra(e))
              try {
                na(), wt(e, f);
              } finally {
                Tl(e);
              }
            else wt(e, f);
          } catch (m) {
            jt(e, n, m);
          } finally {
            e.refCleanup = null, e = e.alternate, e != null && (e.refCleanup = null);
          }
        else if (typeof s == "function")
          try {
            if (ra(e))
              try {
                na(), wt(e, s, null);
              } finally {
                Tl(e);
              }
            else wt(e, s, null);
          } catch (m) {
            jt(e, n, m);
          }
        else s.current = null;
    }
    function ep(e, n, s, f) {
      var m = e.memoizedProps, p = m.id, T = m.onCommit;
      m = m.onRender, n = n === null ? "mount" : "update", Fv && (n = "nested-update"), typeof m == "function" && m(
        p,
        n,
        e.actualDuration,
        e.treeBaseDuration,
        e.actualStartTime,
        s
      ), typeof T == "function" && T(
        e.memoizedProps.id,
        n,
        f,
        s
      );
    }
    function cv(e, n, s, f) {
      var m = e.memoizedProps;
      e = m.id, m = m.onPostCommit, n = n === null ? "mount" : "update", Fv && (n = "nested-update"), typeof m == "function" && m(
        e,
        n,
        f,
        s
      );
    }
    function fv(e) {
      var n = e.type, s = e.memoizedProps, f = e.stateNode;
      try {
        wt(
          e,
          Ns,
          f,
          n,
          s,
          e
        );
      } catch (m) {
        jt(e, e.return, m);
      }
    }
    function np(e, n, s) {
      try {
        wt(
          e,
          fn,
          e.stateNode,
          e.type,
          s,
          n,
          e
        );
      } catch (f) {
        jt(e, e.return, f);
      }
    }
    function ip(e) {
      return e.tag === 5 || e.tag === 3 || e.tag === 26 || e.tag === 27 && Za(e.type) || e.tag === 4;
    }
    function Ar(e) {
      t: for (; ; ) {
        for (; e.sibling === null; ) {
          if (e.return === null || ip(e.return)) return null;
          e = e.return;
        }
        for (e.sibling.return = e.return, e = e.sibling; e.tag !== 5 && e.tag !== 6 && e.tag !== 18; ) {
          if (e.tag === 27 && Za(e.type) || e.flags & 2 || e.child === null || e.tag === 4) continue t;
          e.child.return = e, e = e.child;
        }
        if (!(e.flags & 2)) return e.stateNode;
      }
    }
    function kf(e, n, s) {
      var f = e.tag;
      if (f === 5 || f === 6)
        e = e.stateNode, n ? (s.nodeType === 9 ? s.body : s.nodeName === "HTML" ? s.ownerDocument.body : s).insertBefore(e, n) : (n = s.nodeType === 9 ? s.body : s.nodeName === "HTML" ? s.ownerDocument.body : s, n.appendChild(e), s = s._reactRootContainer, s != null || n.onclick !== null || (n.onclick = zs));
      else if (f !== 4 && (f === 27 && Za(e.type) && (s = e.stateNode, n = null), e = e.child, e !== null))
        for (kf(e, n, s), e = e.sibling; e !== null; )
          kf(e, n, s), e = e.sibling;
    }
    function Mr(e, n, s) {
      var f = e.tag;
      if (f === 5 || f === 6)
        e = e.stateNode, n ? s.insertBefore(e, n) : s.appendChild(e);
      else if (f !== 4 && (f === 27 && Za(e.type) && (s = e.stateNode), e = e.child, e !== null))
        for (Mr(e, n, s), e = e.sibling; e !== null; )
          Mr(e, n, s), e = e.sibling;
    }
    function hv(e) {
      for (var n, s = e.return; s !== null; ) {
        if (ip(s)) {
          n = s;
          break;
        }
        s = s.return;
      }
      if (n == null)
        throw Error(
          "Expected to find a host parent. This error is likely caused by a bug in React. Please file an issue."
        );
      switch (n.tag) {
        case 27:
          n = n.stateNode, s = Ar(e), Mr(
            e,
            s,
            n
          );
          break;
        case 5:
          s = n.stateNode, n.flags & 32 && (Us(s), n.flags &= -33), n = Ar(e), Mr(
            e,
            n,
            s
          );
          break;
        case 3:
        case 4:
          n = n.stateNode.containerInfo, s = Ar(e), kf(
            e,
            s,
            n
          );
          break;
        default:
          throw Error(
            "Invalid host parent fiber. This error is likely caused by a bug in React. Please file an issue."
          );
      }
    }
    function lp(e) {
      var n = e.stateNode, s = e.memoizedProps;
      try {
        wt(
          e,
          al,
          e.type,
          s,
          n,
          e
        );
      } catch (f) {
        jt(e, e.return, f);
      }
    }
    function ig(e, n) {
      if (e = e.containerInfo, IE = E1, e = Z_(e), yy(e)) {
        if ("selectionStart" in e)
          var s = {
            start: e.selectionStart,
            end: e.selectionEnd
          };
        else
          t: {
            s = (s = e.ownerDocument) && s.defaultView || window;
            var f = s.getSelection && s.getSelection();
            if (f && f.rangeCount !== 0) {
              s = f.anchorNode;
              var m = f.anchorOffset, p = f.focusNode;
              f = f.focusOffset;
              try {
                s.nodeType, p.nodeType;
              } catch {
                s = null;
                break t;
              }
              var T = 0, C = -1, D = -1, z = 0, tt = 0, st = e, J = null;
              e: for (; ; ) {
                for (var rt; st !== s || m !== 0 && st.nodeType !== 3 || (C = T + m), st !== p || f !== 0 && st.nodeType !== 3 || (D = T + f), st.nodeType === 3 && (T += st.nodeValue.length), (rt = st.firstChild) !== null; )
                  J = st, st = rt;
                for (; ; ) {
                  if (st === e) break e;
                  if (J === s && ++z === m && (C = T), J === p && ++tt === f && (D = T), (rt = st.nextSibling) !== null) break;
                  st = J, J = st.parentNode;
                }
                st = rt;
              }
              s = C === -1 || D === -1 ? null : { start: C, end: D };
            } else s = null;
          }
        s = s || { start: 0, end: 0 };
      } else s = null;
      for (XE = {
        focusedElem: e,
        selectionRange: s
      }, E1 = !1, ci = n; ci !== null; )
        if (n = ci, e = n.child, (n.subtreeFlags & 1024) !== 0 && e !== null)
          e.return = n, ci = e;
        else
          for (; ci !== null; ) {
            switch (e = n = ci, s = e.alternate, m = e.flags, e.tag) {
              case 0:
                break;
              case 11:
              case 15:
                break;
              case 1:
                m & 1024 && s !== null && HS(e, s);
                break;
              case 3:
                if (m & 1024) {
                  if (e = e.stateNode.containerInfo, s = e.nodeType, s === 9)
                    lc(e);
                  else if (s === 1)
                    switch (e.nodeName) {
                      case "HEAD":
                      case "HTML":
                      case "BODY":
                        lc(e);
                        break;
                      default:
                        e.textContent = "";
                    }
                }
                break;
              case 5:
              case 26:
              case 27:
              case 6:
              case 4:
              case 17:
                break;
              default:
                if (m & 1024)
                  throw Error(
                    "This unit of work tag should not have side-effects. This error is likely caused by a bug in React. Please file an issue."
                  );
            }
            if (e = n.sibling, e !== null) {
              e.return = n.return, ci = e;
              break;
            }
            ci = n.return;
          }
    }
    function ap(e, n, s) {
      var f = s.flags;
      switch (s.tag) {
        case 0:
        case 11:
        case 15:
          Ha(e, s), f & 4 && Jy(s, Ei | as);
          break;
        case 1:
          if (Ha(e, s), f & 4)
            if (e = s.stateNode, n === null)
              s.type.defaultProps || "ref" in s.memoizedProps || mm || (e.props !== s.memoizedProps && console.error(
                "Expected %s props to match memoized props before componentDidMount. This might either be because of a bug in React, or because a component reassigns its own `this.props`. Please file an issue.",
                it(s) || "instance"
              ), e.state !== s.memoizedState && console.error(
                "Expected %s state to match memoized state before componentDidMount. This might either be because of a bug in React, or because a component reassigns its own `this.state`. Please file an issue.",
                it(s) || "instance"
              )), ra(s) ? (na(), wt(
                s,
                mE,
                s,
                e
              ), Tl()) : wt(
                s,
                mE,
                s,
                e
              );
            else {
              var m = Ro(
                s.type,
                n.memoizedProps
              );
              n = n.memoizedState, s.type.defaultProps || "ref" in s.memoizedProps || mm || (e.props !== s.memoizedProps && console.error(
                "Expected %s props to match memoized props before componentDidUpdate. This might either be because of a bug in React, or because a component reassigns its own `this.props`. Please file an issue.",
                it(s) || "instance"
              ), e.state !== s.memoizedState && console.error(
                "Expected %s state to match memoized state before componentDidUpdate. This might either be because of a bug in React, or because a component reassigns its own `this.state`. Please file an issue.",
                it(s) || "instance"
              )), ra(s) ? (na(), wt(
                s,
                Ox,
                s,
                e,
                m,
                n,
                e.__reactInternalSnapshotBeforeUpdate
              ), Tl()) : wt(
                s,
                Ox,
                s,
                e,
                m,
                n,
                e.__reactInternalSnapshotBeforeUpdate
              );
            }
          f & 64 && tp(s), f & 512 && Fu(s, s.return);
          break;
        case 3:
          if (n = ea(), Ha(e, s), f & 64 && (f = s.updateQueue, f !== null)) {
            if (m = null, s.child !== null)
              switch (s.child.tag) {
                case 27:
                case 5:
                  m = s.child.stateNode;
                  break;
                case 1:
                  m = s.child.stateNode;
              }
            try {
              wt(
                s,
                ev,
                f,
                m
              );
            } catch (T) {
              jt(s, s.return, T);
            }
          }
          e.effectDuration += po(n);
          break;
        case 27:
          n === null && f & 4 && lp(s);
        case 26:
        case 5:
          Ha(e, s), n === null && f & 4 && fv(s), f & 512 && Fu(s, s.return);
          break;
        case 12:
          if (f & 4) {
            f = ea(), Ha(e, s), e = s.stateNode, e.effectDuration += pr(f);
            try {
              wt(
                s,
                ep,
                s,
                n,
                Zv,
                e.effectDuration
              );
            } catch (T) {
              jt(s, s.return, T);
            }
          } else Ha(e, s);
          break;
        case 13:
          Ha(e, s), f & 4 && Ku(e, s), f & 64 && (e = s.memoizedState, e !== null && (e = e.dehydrated, e !== null && (s = Wf.bind(
            null,
            s
          ), ac(e, s))));
          break;
        case 22:
          if (f = s.memoizedState !== null || tu, !f) {
            n = n !== null && n.memoizedState !== null || Mn, m = tu;
            var p = Mn;
            tu = f, (Mn = n) && !p ? Ya(
              e,
              s,
              (s.subtreeFlags & 8772) !== 0
            ) : Ha(e, s), tu = m, Mn = p;
          }
          break;
        case 30:
          break;
        default:
          Ha(e, s);
      }
    }
    function sp(e) {
      var n = e.alternate;
      n !== null && (e.alternate = null, sp(n)), e.child = null, e.deletions = null, e.sibling = null, e.tag === 5 && (n = e.stateNode, n !== null && lo(n)), e.stateNode = null, e._debugOwner = null, e.return = null, e.dependencies = null, e.memoizedProps = null, e.memoizedState = null, e.pendingProps = null, e.stateNode = null, e.updateQueue = null;
    }
    function Ds(e, n, s) {
      for (s = s.child; s !== null; )
        wr(
          e,
          n,
          s
        ), s = s.sibling;
    }
    function wr(e, n, s) {
      if (Pn && typeof Pn.onCommitFiberUnmount == "function")
        try {
          Pn.onCommitFiberUnmount(Bo, s);
        } catch (p) {
          ji || (ji = !0, console.error(
            "React instrumentation encountered an error: %s",
            p
          ));
        }
      switch (s.tag) {
        case 26:
          Mn || Ol(s, n), Ds(
            e,
            n,
            s
          ), s.memoizedState ? s.memoizedState.count-- : s.stateNode && (s = s.stateNode, s.parentNode.removeChild(s));
          break;
        case 27:
          Mn || Ol(s, n);
          var f = Yn, m = jl;
          Za(s.type) && (Yn = s.stateNode, jl = !1), Ds(
            e,
            n,
            s
          ), wt(
            s,
            oc,
            s.stateNode
          ), Yn = f, jl = m;
          break;
        case 5:
          Mn || Ol(s, n);
        case 6:
          if (f = Yn, m = jl, Yn = null, Ds(
            e,
            n,
            s
          ), Yn = f, jl = m, Yn !== null)
            if (jl)
              try {
                wt(
                  s,
                  nc,
                  Yn,
                  s.stateNode
                );
              } catch (p) {
                jt(
                  s,
                  n,
                  p
                );
              }
            else
              try {
                wt(
                  s,
                  Nl,
                  Yn,
                  s.stateNode
                );
              } catch (p) {
                jt(
                  s,
                  n,
                  p
                );
              }
          break;
        case 18:
          Yn !== null && (jl ? (e = Yn, ic(
            e.nodeType === 9 ? e.body : e.nodeName === "HTML" ? e.ownerDocument.body : e,
            s.stateNode
          ), Xr(e)) : ic(Yn, s.stateNode));
          break;
        case 4:
          f = Yn, m = jl, Yn = s.stateNode.containerInfo, jl = !0, Ds(
            e,
            n,
            s
          ), Yn = f, jl = m;
          break;
        case 0:
        case 11:
        case 14:
        case 15:
          Mn || Rr(
            cl,
            s,
            n
          ), Mn || eg(
            s,
            n,
            Ei
          ), Ds(
            e,
            n,
            s
          );
          break;
        case 1:
          Mn || (Ol(s, n), f = s.stateNode, typeof f.componentWillUnmount == "function" && ng(
            s,
            n,
            f
          )), Ds(
            e,
            n,
            s
          );
          break;
        case 21:
          Ds(
            e,
            n,
            s
          );
          break;
        case 22:
          Mn = (f = Mn) || s.memoizedState !== null, Ds(
            e,
            n,
            s
          ), Mn = f;
          break;
        default:
          Ds(
            e,
            n,
            s
          );
      }
    }
    function Ku(e, n) {
      if (n.memoizedState === null && (e = n.alternate, e !== null && (e = e.memoizedState, e !== null && (e = e.dehydrated, e !== null))))
        try {
          wt(
            n,
            ll,
            e
          );
        } catch (s) {
          jt(n, n.return, s);
        }
    }
    function lg(e) {
      switch (e.tag) {
        case 13:
        case 19:
          var n = e.stateNode;
          return n === null && (n = e.stateNode = new rC()), n;
        case 22:
          return e = e.stateNode, n = e._retryCache, n === null && (n = e._retryCache = new rC()), n;
        default:
          throw Error(
            "Unexpected Suspense handler tag (" + e.tag + "). This is a bug in React."
          );
      }
    }
    function Dr(e, n) {
      var s = lg(e);
      n.forEach(function(f) {
        var m = Oo.bind(null, e, f);
        if (!s.has(f)) {
          if (s.add(f), dn)
            if (ym !== null && pm !== null)
              tc(pm, ym);
            else
              throw Error(
                "Expected finished root and lanes to be set. This is a bug in React."
              );
          f.then(m, m);
        }
      });
    }
    function ai(e, n) {
      var s = n.deletions;
      if (s !== null)
        for (var f = 0; f < s.length; f++) {
          var m = e, p = n, T = s[f], C = p;
          t: for (; C !== null; ) {
            switch (C.tag) {
              case 27:
                if (Za(C.type)) {
                  Yn = C.stateNode, jl = !1;
                  break t;
                }
                break;
              case 5:
                Yn = C.stateNode, jl = !1;
                break t;
              case 3:
              case 4:
                Yn = C.stateNode.containerInfo, jl = !0;
                break t;
            }
            C = C.return;
          }
          if (Yn === null)
            throw Error(
              "Expected to find a host parent. This error is likely caused by a bug in React. Please file an issue."
            );
          wr(m, p, T), Yn = null, jl = !1, m = T, p = m.alternate, p !== null && (p.return = null), m.return = null;
        }
      if (n.subtreeFlags & 13878)
        for (n = n.child; n !== null; )
          op(n, e), n = n.sibling;
    }
    function op(e, n) {
      var s = e.alternate, f = e.flags;
      switch (e.tag) {
        case 0:
        case 11:
        case 14:
        case 15:
          ai(n, e), Ni(e), f & 4 && (Rr(
            cl | as,
            e,
            e.return
          ), Cr(cl | as, e), eg(
            e,
            e.return,
            Ei | as
          ));
          break;
        case 1:
          ai(n, e), Ni(e), f & 512 && (Mn || s === null || Ol(s, s.return)), f & 64 && tu && (e = e.updateQueue, e !== null && (f = e.callbacks, f !== null && (s = e.shared.hiddenCallbacks, e.shared.hiddenCallbacks = s === null ? f : s.concat(f))));
          break;
        case 26:
          var m = Fs;
          if (ai(n, e), Ni(e), f & 512 && (Mn || s === null || Ol(s, s.return)), f & 4)
            if (n = s !== null ? s.memoizedState : null, f = e.memoizedState, s === null)
              if (f === null)
                if (e.stateNode === null) {
                  t: {
                    f = e.type, s = e.memoizedProps, n = m.ownerDocument || m;
                    e: switch (f) {
                      case "title":
                        m = n.getElementsByTagName("title")[0], (!m || m[_c] || m[ri] || m.namespaceURI === bc || m.hasAttribute("itemprop")) && (m = n.createElement(f), n.head.insertBefore(
                          m,
                          n.querySelector("head > title")
                        )), cn(m, f, s), m[ri] = e, _n(m), f = m;
                        break t;
                      case "link":
                        var p = jp(
                          "link",
                          "href",
                          n
                        ).get(f + (s.href || ""));
                        if (p) {
                          for (var T = 0; T < p.length; T++)
                            if (m = p[T], m.getAttribute("href") === (s.href == null || s.href === "" ? null : s.href) && m.getAttribute("rel") === (s.rel == null ? null : s.rel) && m.getAttribute("title") === (s.title == null ? null : s.title) && m.getAttribute("crossorigin") === (s.crossOrigin == null ? null : s.crossOrigin)) {
                              p.splice(T, 1);
                              break e;
                            }
                        }
                        m = n.createElement(f), cn(m, f, s), n.head.appendChild(m);
                        break;
                      case "meta":
                        if (p = jp(
                          "meta",
                          "content",
                          n
                        ).get(f + (s.content || ""))) {
                          for (T = 0; T < p.length; T++)
                            if (m = p[T], Q(
                              s.content,
                              "content"
                            ), m.getAttribute("content") === (s.content == null ? null : "" + s.content) && m.getAttribute("name") === (s.name == null ? null : s.name) && m.getAttribute("property") === (s.property == null ? null : s.property) && m.getAttribute("http-equiv") === (s.httpEquiv == null ? null : s.httpEquiv) && m.getAttribute("charset") === (s.charSet == null ? null : s.charSet)) {
                              p.splice(T, 1);
                              break e;
                            }
                        }
                        m = n.createElement(f), cn(m, f, s), n.head.appendChild(m);
                        break;
                      default:
                        throw Error(
                          'getNodesForType encountered a type it did not expect: "' + f + '". This is a bug in React.'
                        );
                    }
                    m[ri] = e, _n(m), f = m;
                  }
                  e.stateNode = f;
                } else
                  Bp(
                    m,
                    e.type,
                    e.stateNode
                  );
              else
                e.stateNode = Ag(
                  m,
                  f,
                  e.memoizedProps
                );
            else
              n !== f ? (n === null ? s.stateNode !== null && (s = s.stateNode, s.parentNode.removeChild(s)) : n.count--, f === null ? Bp(
                m,
                e.type,
                e.stateNode
              ) : Ag(
                m,
                f,
                e.memoizedProps
              )) : f === null && e.stateNode !== null && np(
                e,
                e.memoizedProps,
                s.memoizedProps
              );
          break;
        case 27:
          ai(n, e), Ni(e), f & 512 && (Mn || s === null || Ol(s, s.return)), s !== null && f & 4 && np(
            e,
            e.memoizedProps,
            s.memoizedProps
          );
          break;
        case 5:
          if (ai(n, e), Ni(e), f & 512 && (Mn || s === null || Ol(s, s.return)), e.flags & 32) {
            n = e.stateNode;
            try {
              wt(e, Us, n);
            } catch (tt) {
              jt(e, e.return, tt);
            }
          }
          f & 4 && e.stateNode != null && (n = e.memoizedProps, np(
            e,
            n,
            s !== null ? s.memoizedProps : n
          )), f & 1024 && (bE = !0, e.type !== "form" && console.error(
            "Unexpected host component type. Expected a form. This is a bug in React."
          ));
          break;
        case 6:
          if (ai(n, e), Ni(e), f & 4) {
            if (e.stateNode === null)
              throw Error(
                "This should have a text node initialized. This error is likely caused by a bug in React. Please file an issue."
              );
            f = e.memoizedProps, s = s !== null ? s.memoizedProps : f, n = e.stateNode;
            try {
              wt(
                e,
                Yr,
                n,
                s,
                f
              );
            } catch (tt) {
              jt(e, e.return, tt);
            }
          }
          break;
        case 3:
          if (m = ea(), _1 = null, p = Fs, Fs = $f(n.containerInfo), ai(n, e), Fs = p, Ni(e), f & 4 && s !== null && s.memoizedState.isDehydrated)
            try {
              wt(
                e,
                Np,
                n.containerInfo
              );
            } catch (tt) {
              jt(e, e.return, tt);
            }
          bE && (bE = !1, Or(e)), n.effectDuration += po(m);
          break;
        case 4:
          f = Fs, Fs = $f(
            e.stateNode.containerInfo
          ), ai(n, e), Ni(e), Fs = f;
          break;
        case 12:
          f = ea(), ai(n, e), Ni(e), e.stateNode.effectDuration += pr(f);
          break;
        case 13:
          ai(n, e), Ni(e), e.child.flags & 8192 && e.memoizedState !== null != (s !== null && s.memoizedState !== null) && (ME = Qa()), f & 4 && (f = e.updateQueue, f !== null && (e.updateQueue = null, Dr(e, f)));
          break;
        case 22:
          m = e.memoizedState !== null;
          var C = s !== null && s.memoizedState !== null, D = tu, z = Mn;
          if (tu = D || m, Mn = z || C, ai(n, e), Mn = z, tu = D, Ni(e), f & 8192)
            t: for (n = e.stateNode, n._visibility = m ? n._visibility & ~Xv : n._visibility | Xv, m && (s === null || C || tu || Mn || si(e)), s = null, n = e; ; ) {
              if (n.tag === 5 || n.tag === 26) {
                if (s === null) {
                  C = s = n;
                  try {
                    p = C.stateNode, m ? wt(C, Hi, p) : wt(
                      C,
                      Lp,
                      C.stateNode,
                      C.memoizedProps
                    );
                  } catch (tt) {
                    jt(C, C.return, tt);
                  }
                }
              } else if (n.tag === 6) {
                if (s === null) {
                  C = n;
                  try {
                    T = C.stateNode, m ? wt(C, Op, T) : wt(
                      C,
                      xg,
                      T,
                      C.memoizedProps
                    );
                  } catch (tt) {
                    jt(C, C.return, tt);
                  }
                }
              } else if ((n.tag !== 22 && n.tag !== 23 || n.memoizedState === null || n === e) && n.child !== null) {
                n.child.return = n, n = n.child;
                continue;
              }
              if (n === e) break t;
              for (; n.sibling === null; ) {
                if (n.return === null || n.return === e)
                  break t;
                s === n && (s = null), n = n.return;
              }
              s === n && (s = null), n.sibling.return = n.return, n = n.sibling;
            }
          f & 4 && (f = e.updateQueue, f !== null && (s = f.retryQueue, s !== null && (f.retryQueue = null, Dr(e, s))));
          break;
        case 19:
          ai(n, e), Ni(e), f & 4 && (f = e.updateQueue, f !== null && (e.updateQueue = null, Dr(e, f)));
          break;
        case 30:
          break;
        case 21:
          break;
        default:
          ai(n, e), Ni(e);
      }
    }
    function Ni(e) {
      var n = e.flags;
      if (n & 2) {
        try {
          wt(e, hv, e);
        } catch (s) {
          jt(e, e.return, s);
        }
        e.flags &= -3;
      }
      n & 4096 && (e.flags &= -4097);
    }
    function Or(e) {
      if (e.subtreeFlags & 1024)
        for (e = e.child; e !== null; ) {
          var n = e;
          Or(n), n.tag === 5 && n.flags & 1024 && n.stateNode.reset(), e = e.sibling;
        }
    }
    function Ha(e, n) {
      if (n.subtreeFlags & 8772)
        for (n = n.child; n !== null; )
          ap(e, n.alternate, n), n = n.sibling;
    }
    function il(e) {
      switch (e.tag) {
        case 0:
        case 11:
        case 14:
        case 15:
          eg(
            e,
            e.return,
            Ei
          ), si(e);
          break;
        case 1:
          Ol(e, e.return);
          var n = e.stateNode;
          typeof n.componentWillUnmount == "function" && ng(
            e,
            e.return,
            n
          ), si(e);
          break;
        case 27:
          wt(
            e,
            oc,
            e.stateNode
          );
        case 26:
        case 5:
          Ol(e, e.return), si(e);
          break;
        case 22:
          e.memoizedState === null && si(e);
          break;
        case 30:
          si(e);
          break;
        default:
          si(e);
      }
    }
    function si(e) {
      for (e = e.child; e !== null; )
        il(e), e = e.sibling;
    }
    function Os(e, n, s, f) {
      var m = s.flags;
      switch (s.tag) {
        case 0:
        case 11:
        case 15:
          Ya(
            e,
            s,
            f
          ), Jy(s, Ei);
          break;
        case 1:
          if (Ya(
            e,
            s,
            f
          ), n = s.stateNode, typeof n.componentDidMount == "function" && wt(
            s,
            mE,
            s,
            n
          ), n = s.updateQueue, n !== null) {
            e = s.stateNode;
            try {
              wt(
                s,
                Nu,
                n,
                e
              );
            } catch (p) {
              jt(s, s.return, p);
            }
          }
          f && m & 64 && tp(s), Fu(s, s.return);
          break;
        case 27:
          lp(s);
        case 26:
        case 5:
          Ya(
            e,
            s,
            f
          ), f && n === null && m & 4 && fv(s), Fu(s, s.return);
          break;
        case 12:
          if (f && m & 4) {
            m = ea(), Ya(
              e,
              s,
              f
            ), f = s.stateNode, f.effectDuration += pr(m);
            try {
              wt(
                s,
                ep,
                s,
                n,
                Zv,
                f.effectDuration
              );
            } catch (p) {
              jt(s, s.return, p);
            }
          } else
            Ya(
              e,
              s,
              f
            );
          break;
        case 13:
          Ya(
            e,
            s,
            f
          ), f && m & 4 && Ku(e, s);
          break;
        case 22:
          s.memoizedState === null && Ya(
            e,
            s,
            f
          ), Fu(s, s.return);
          break;
        case 30:
          break;
        default:
          Ya(
            e,
            s,
            f
          );
      }
    }
    function Ya(e, n, s) {
      for (s = s && (n.subtreeFlags & 8772) !== 0, n = n.child; n !== null; )
        Os(
          e,
          n.alternate,
          n,
          s
        ), n = n.sibling;
    }
    function ja(e, n) {
      var s = null;
      e !== null && e.memoizedState !== null && e.memoizedState.cachePool !== null && (s = e.memoizedState.cachePool.pool), e = null, n.memoizedState !== null && n.memoizedState.cachePool !== null && (e = n.memoizedState.cachePool.pool), e !== s && (e != null && yr(e), s != null && Ca(s));
    }
    function ua(e, n) {
      e = null, n.alternate !== null && (e = n.alternate.memoizedState.cache), n = n.memoizedState.cache, n !== e && (yr(n), e != null && Ca(e));
    }
    function He(e, n, s, f) {
      if (n.subtreeFlags & 10256)
        for (n = n.child; n !== null; )
          Vf(
            e,
            n,
            s,
            f
          ), n = n.sibling;
    }
    function Vf(e, n, s, f) {
      var m = n.flags;
      switch (n.tag) {
        case 0:
        case 11:
        case 15:
          He(
            e,
            n,
            s,
            f
          ), m & 2048 && $y(n, Jn | as);
          break;
        case 1:
          He(
            e,
            n,
            s,
            f
          );
          break;
        case 3:
          var p = ea();
          He(
            e,
            n,
            s,
            f
          ), m & 2048 && (s = null, n.alternate !== null && (s = n.alternate.memoizedState.cache), n = n.memoizedState.cache, n !== s && (yr(n), s != null && Ca(s))), e.passiveEffectDuration += po(p);
          break;
        case 12:
          if (m & 2048) {
            m = ea(), He(
              e,
              n,
              s,
              f
            ), e = n.stateNode, e.passiveEffectDuration += pr(m);
            try {
              wt(
                n,
                cv,
                n,
                n.alternate,
                Zv,
                e.passiveEffectDuration
              );
            } catch (C) {
              jt(n, n.return, C);
            }
          } else
            He(
              e,
              n,
              s,
              f
            );
          break;
        case 13:
          He(
            e,
            n,
            s,
            f
          );
          break;
        case 23:
          break;
        case 22:
          p = n.stateNode;
          var T = n.alternate;
          n.memoizedState !== null ? p._visibility & Kr ? He(
            e,
            n,
            s,
            f
          ) : Pu(
            e,
            n
          ) : p._visibility & Kr ? He(
            e,
            n,
            s,
            f
          ) : (p._visibility |= Kr, Mo(
            e,
            n,
            s,
            f,
            (n.subtreeFlags & 10256) !== 0
          )), m & 2048 && ja(T, n);
          break;
        case 24:
          He(
            e,
            n,
            s,
            f
          ), m & 2048 && ua(n.alternate, n);
          break;
        default:
          He(
            e,
            n,
            s,
            f
          );
      }
    }
    function Mo(e, n, s, f, m) {
      for (m = m && (n.subtreeFlags & 10256) !== 0, n = n.child; n !== null; )
        ag(
          e,
          n,
          s,
          f,
          m
        ), n = n.sibling;
    }
    function ag(e, n, s, f, m) {
      var p = n.flags;
      switch (n.tag) {
        case 0:
        case 11:
        case 15:
          Mo(
            e,
            n,
            s,
            f,
            m
          ), $y(n, Jn);
          break;
        case 23:
          break;
        case 22:
          var T = n.stateNode;
          n.memoizedState !== null ? T._visibility & Kr ? Mo(
            e,
            n,
            s,
            f,
            m
          ) : Pu(
            e,
            n
          ) : (T._visibility |= Kr, Mo(
            e,
            n,
            s,
            f,
            m
          )), m && p & 2048 && ja(
            n.alternate,
            n
          );
          break;
        case 24:
          Mo(
            e,
            n,
            s,
            f,
            m
          ), m && p & 2048 && ua(n.alternate, n);
          break;
        default:
          Mo(
            e,
            n,
            s,
            f,
            m
          );
      }
    }
    function Pu(e, n) {
      if (n.subtreeFlags & 10256)
        for (n = n.child; n !== null; ) {
          var s = e, f = n, m = f.flags;
          switch (f.tag) {
            case 22:
              Pu(
                s,
                f
              ), m & 2048 && ja(
                f.alternate,
                f
              );
              break;
            case 24:
              Pu(
                s,
                f
              ), m & 2048 && ua(
                f.alternate,
                f
              );
              break;
            default:
              Pu(
                s,
                f
              );
          }
          n = n.sibling;
        }
    }
    function Lr(e) {
      if (e.subtreeFlags & w0)
        for (e = e.child; e !== null; )
          wo(e), e = e.sibling;
    }
    function wo(e) {
      switch (e.tag) {
        case 26:
          Lr(e), e.flags & w0 && e.memoizedState !== null && Tv(
            Fs,
            e.memoizedState,
            e.memoizedProps
          );
          break;
        case 5:
          Lr(e);
          break;
        case 3:
        case 4:
          var n = Fs;
          Fs = $f(
            e.stateNode.containerInfo
          ), Lr(e), Fs = n;
          break;
        case 22:
          e.memoizedState === null && (n = e.alternate, n !== null && n.memoizedState !== null ? (n = w0, w0 = 16777216, Lr(e), w0 = n) : Lr(e));
          break;
        default:
          Lr(e);
      }
    }
    function qf(e) {
      var n = e.alternate;
      if (n !== null && (e = n.child, e !== null)) {
        n.child = null;
        do
          n = e.sibling, e.sibling = null, e = n;
        while (e !== null);
      }
    }
    function Qu(e) {
      var n = e.deletions;
      if (e.flags & 16) {
        if (n !== null)
          for (var s = 0; s < n.length; s++) {
            var f = n[s];
            ci = f, up(
              f,
              e
            );
          }
        qf(e);
      }
      if (e.subtreeFlags & 10256)
        for (e = e.child; e !== null; )
          rp(e), e = e.sibling;
    }
    function rp(e) {
      switch (e.tag) {
        case 0:
        case 11:
        case 15:
          Qu(e), e.flags & 2048 && Xf(
            e,
            e.return,
            Jn | as
          );
          break;
        case 3:
          var n = ea();
          Qu(e), e.stateNode.passiveEffectDuration += po(n);
          break;
        case 12:
          n = ea(), Qu(e), e.stateNode.passiveEffectDuration += pr(n);
          break;
        case 22:
          n = e.stateNode, e.memoizedState !== null && n._visibility & Kr && (e.return === null || e.return.tag !== 13) ? (n._visibility &= ~Kr, Zf(e)) : Qu(e);
          break;
        default:
          Qu(e);
      }
    }
    function Zf(e) {
      var n = e.deletions;
      if (e.flags & 16) {
        if (n !== null)
          for (var s = 0; s < n.length; s++) {
            var f = n[s];
            ci = f, up(
              f,
              e
            );
          }
        qf(e);
      }
      for (e = e.child; e !== null; )
        Ff(e), e = e.sibling;
    }
    function Ff(e) {
      switch (e.tag) {
        case 0:
        case 11:
        case 15:
          Xf(
            e,
            e.return,
            Jn
          ), Zf(e);
          break;
        case 22:
          var n = e.stateNode;
          n._visibility & Kr && (n._visibility &= ~Kr, Zf(e));
          break;
        default:
          Zf(e);
      }
    }
    function up(e, n) {
      for (; ci !== null; ) {
        var s = ci, f = s;
        switch (f.tag) {
          case 0:
          case 11:
          case 15:
            Xf(
              f,
              n,
              Jn
            );
            break;
          case 23:
          case 22:
            f.memoizedState !== null && f.memoizedState.cachePool !== null && (f = f.memoizedState.cachePool.pool, f != null && yr(f));
            break;
          case 24:
            Ca(f.memoizedState.cache);
        }
        if (f = s.child, f !== null) f.return = s, ci = f;
        else
          t: for (s = e; ci !== null; ) {
            f = ci;
            var m = f.sibling, p = f.return;
            if (sp(f), f === s) {
              ci = null;
              break t;
            }
            if (m !== null) {
              m.return = p, ci = m;
              break t;
            }
            ci = p;
          }
      }
    }
    function cp() {
      bM.forEach(function(e) {
        return e();
      });
    }
    function fp() {
      var e = typeof IS_REACT_ACT_ENVIRONMENT < "u" ? IS_REACT_ACT_ENVIRONMENT : void 0;
      return e || lt.actQueue === null || console.error(
        "The current testing environment is not configured to support act(...)"
      ), e;
    }
    function Ui(e) {
      if ((ze & fl) !== da && de !== 0)
        return de & -de;
      var n = lt.T;
      return n !== null ? (n._updatedFibers || (n._updatedFibers = /* @__PURE__ */ new Set()), n._updatedFibers.add(e), e = Ch, e !== 0 ? e : Sp()) : Tu();
    }
    function dv() {
      ma === 0 && (ma = !(de & 536870912) || xe ? Pl() : 536870912);
      var e = os.current;
      return e !== null && (e.flags |= 32), ma;
    }
    function rn(e, n, s) {
      if (bm && console.error("useInsertionEffect must not schedule updates."), zE && (s1 = !0), (e === Pe && (Ye === Dh || Ye === Oh) || e.cancelPendingCommit !== null) && (Nr(e, 0), Ls(
        e,
        de,
        ma,
        !1
      )), Fe(e, s), ze & fl && e === Pe) {
        if (Ii)
          switch (n.tag) {
            case 0:
            case 11:
            case 15:
              e = he && it(he) || "Unknown", _C.has(e) || (_C.add(e), n = it(n) || "Unknown", console.error(
                "Cannot update a component (`%s`) while rendering a different component (`%s`). To locate the bad setState() call inside `%s`, follow the stack trace as described in https://react.dev/link/setstate-in-render",
                n,
                e,
                e
              ));
              break;
            case 1:
              pC || (console.error(
                "Cannot update during an existing state transition (such as within `render`). Render methods should be a pure function of props and state."
              ), pC = !0);
          }
      } else
        dn && Wl(e, n, s), pv(n), e === Pe && ((ze & fl) === da && (wc |= s), En === wh && Ls(
          e,
          de,
          ma,
          !1
        )), Ll(e);
    }
    function Rn(e, n, s) {
      if ((ze & (fl | Ks)) !== da)
        throw Error("Should not already be working.");
      var f = !s && (n & 124) === 0 && (n & e.expiredLanes) === 0 || pl(e, n), m = f ? dp(e, n) : ug(e, n, !0), p = f;
      do {
        if (m === eu) {
          Sm && !f && Ls(e, n, 0, !1);
          break;
        } else {
          if (s = e.current.alternate, p && !gv(s)) {
            m = ug(e, n, !1), p = !1;
            continue;
          }
          if (m === _m) {
            if (p = n, e.errorRecoveryDisabledLanes & p)
              var T = 0;
            else
              T = e.pendingLanes & -536870913, T = T !== 0 ? T : T & 536870912 ? 536870912 : 0;
            if (T !== 0) {
              n = T;
              t: {
                m = e;
                var C = T;
                T = U0;
                var D = m.current.memoizedState.isDehydrated;
                if (D && (Nr(
                  m,
                  C
                ).flags |= 256), C = ug(
                  m,
                  C,
                  !1
                ), C !== _m) {
                  if (RE && !D) {
                    m.errorRecoveryDisabledLanes |= p, wc |= p, m = wh;
                    break t;
                  }
                  m = hl, hl = T, m !== null && (hl === null ? hl = m : hl.push.apply(
                    hl,
                    m
                  ));
                }
                m = C;
              }
              if (p = !1, m !== _m) continue;
            }
          }
          if (m === O0) {
            Nr(e, 0), Ls(e, n, 0, !0);
            break;
          }
          t: {
            switch (f = e, m) {
              case eu:
              case O0:
                throw Error("Root did not complete. This is a bug in React.");
              case wh:
                if ((n & 4194048) !== n) break;
              case i1:
                Ls(
                  f,
                  n,
                  ma,
                  !Ac
                );
                break t;
              case _m:
                hl = null;
                break;
              case TE:
              case uC:
                break;
              default:
                throw Error("Unknown root exit status.");
            }
            if (lt.actQueue !== null)
              gg(
                f,
                s,
                n,
                hl,
                G0,
                l1,
                ma,
                wc,
                Lh
              );
            else {
              if ((n & 62914560) === n && (p = ME + fC - Qa(), 10 < p)) {
                if (Ls(
                  f,
                  n,
                  ma,
                  !Ac
                ), yn(f, 0, !0) !== 0) break t;
                f.timeoutHandle = RC(
                  Nn.bind(
                    null,
                    f,
                    s,
                    hl,
                    G0,
                    l1,
                    n,
                    ma,
                    wc,
                    Lh,
                    Ac,
                    m,
                    RM,
                    dx,
                    0
                  ),
                  p
                );
                break t;
              }
              Nn(
                f,
                s,
                hl,
                G0,
                l1,
                n,
                ma,
                wc,
                Lh,
                Ac,
                m,
                xM,
                dx,
                0
              );
            }
          }
        }
        break;
      } while (!0);
      Ll(e);
    }
    function Nn(e, n, s, f, m, p, T, C, D, z, tt, st, J, rt) {
      if (e.timeoutHandle = Hh, st = n.subtreeFlags, (st & 8192 || (st & 16785408) === 16785408) && (X0 = { stylesheets: null, count: 0, unsuspend: bv }, wo(n), st = xv(), st !== null)) {
        e.cancelPendingCommit = st(
          gg.bind(
            null,
            e,
            n,
            p,
            s,
            f,
            m,
            T,
            C,
            D,
            tt,
            CM,
            J,
            rt
          )
        ), Ls(
          e,
          p,
          T,
          !z
        );
        return;
      }
      gg(
        e,
        n,
        p,
        s,
        f,
        m,
        T,
        C,
        D
      );
    }
    function gv(e) {
      for (var n = e; ; ) {
        var s = n.tag;
        if ((s === 0 || s === 11 || s === 15) && n.flags & 16384 && (s = n.updateQueue, s !== null && (s = s.stores, s !== null)))
          for (var f = 0; f < s.length; f++) {
            var m = s[f], p = m.getSnapshot;
            m = m.value;
            try {
              if (!rl(p(), m)) return !1;
            } catch {
              return !1;
            }
          }
        if (s = n.child, n.subtreeFlags & 16384 && s !== null)
          s.return = n, n = s;
        else {
          if (n === e) break;
          for (; n.sibling === null; ) {
            if (n.return === null || n.return === e) return !0;
            n = n.return;
          }
          n.sibling.return = n.return, n = n.sibling;
        }
      }
      return !0;
    }
    function Ls(e, n, s, f) {
      n &= ~AE, n &= ~wc, e.suspendedLanes |= n, e.pingedLanes &= ~n, f && (e.warmLanes |= n), f = e.expirationTimes;
      for (var m = n; 0 < m; ) {
        var p = 31 - oi(m), T = 1 << p;
        f[p] = -1, m &= ~T;
      }
      s !== 0 && Eu(e, s, n);
    }
    function zr() {
      return (ze & (fl | Ks)) === da ? (Ur(0), !1) : !0;
    }
    function sg() {
      if (he !== null) {
        if (Ye === Bl)
          var e = he.return;
        else
          e = he, Td(), aa(e), fm = null, A0 = 0, e = he;
        for (; e !== null; )
          Wy(e.alternate, e), e = e.return;
        he = null;
      }
    }
    function Nr(e, n) {
      var s = e.timeoutHandle;
      s !== Hh && (e.timeoutHandle = Hh, jM(s)), s = e.cancelPendingCommit, s !== null && (e.cancelPendingCommit = null, s()), sg(), Pe = e, he = s = Ta(e.current, null), de = n, Ye = Bl, ga = null, Ac = !1, Sm = pl(e, n), RE = !1, En = eu, Lh = ma = AE = wc = Mc = 0, hl = U0 = null, l1 = !1, n & 8 && (n |= n & 32);
      var f = e.entangledLanes;
      if (f !== 0)
        for (e = e.entanglements, f &= n; 0 < f; ) {
          var m = 31 - oi(f), p = 1 << m;
          n |= e[m], f &= ~p;
        }
      return Po = n, gf(), n = fx(), 1e3 < n - cx && (lt.recentlyCreatedOwnerStacks = 0, cx = n), qs.discardPendingWarnings(), s;
    }
    function Kf(e, n) {
      Kt = null, lt.H = e1, lt.getCurrentStack = null, Ii = !1, ol = null, n === T0 || n === Qv ? (n = Ny(), Ye = z0) : n === yx ? (n = Ny(), Ye = cC) : Ye = n === tC ? CE : n !== null && typeof n == "object" && typeof n.then == "function" ? vm : L0, ga = n;
      var s = he;
      if (s === null)
        En = O0, Zu(
          e,
          $i(n, e.current)
        );
      else
        switch (s.mode & Si && Ts(s), ke(), Ye) {
          case L0:
            Ct !== null && typeof Ct.markComponentErrored == "function" && Ct.markComponentErrored(
              s,
              n,
              de
            );
            break;
          case Dh:
          case Oh:
          case z0:
          case vm:
          case N0:
            Ct !== null && typeof Ct.markComponentSuspended == "function" && Ct.markComponentSuspended(
              s,
              n,
              de
            );
        }
    }
    function og() {
      var e = lt.H;
      return lt.H = e1, e === null ? e1 : e;
    }
    function hp() {
      var e = lt.A;
      return lt.A = EM, e;
    }
    function rg() {
      En = wh, Ac || (de & 4194048) !== de && os.current !== null || (Sm = !0), !(Mc & 134217727) && !(wc & 134217727) || Pe === null || Ls(
        Pe,
        de,
        ma,
        !1
      );
    }
    function ug(e, n, s) {
      var f = ze;
      ze |= fl;
      var m = og(), p = hp();
      if (Pe !== e || de !== n) {
        if (dn) {
          var T = e.memoizedUpdaters;
          0 < T.size && (tc(e, de), T.clear()), Bn(e, n);
        }
        G0 = null, Nr(e, n);
      }
      yl(n), n = !1, T = En;
      t: do
        try {
          if (Ye !== Bl && he !== null) {
            var C = he, D = ga;
            switch (Ye) {
              case CE:
                sg(), T = i1;
                break t;
              case z0:
              case Dh:
              case Oh:
              case vm:
                os.current === null && (n = !0);
                var z = Ye;
                if (Ye = Bl, ga = null, Do(e, C, D, z), s && Sm) {
                  T = eu;
                  break t;
                }
                break;
              default:
                z = Ye, Ye = Bl, ga = null, Do(e, C, D, z);
            }
          }
          cg(), T = En;
          break;
        } catch (tt) {
          Kf(e, tt);
        }
      while (!0);
      return n && e.shellSuspendCounter++, Td(), ze = f, lt.H = m, lt.A = p, no(), he === null && (Pe = null, de = 0, gf()), T;
    }
    function cg() {
      for (; he !== null; ) mp(he);
    }
    function dp(e, n) {
      var s = ze;
      ze |= fl;
      var f = og(), m = hp();
      if (Pe !== e || de !== n) {
        if (dn) {
          var p = e.memoizedUpdaters;
          0 < p.size && (tc(e, de), p.clear()), Bn(e, n);
        }
        G0 = null, a1 = Qa() + hC, Nr(e, n);
      } else
        Sm = pl(
          e,
          n
        );
      yl(n);
      t: do
        try {
          if (Ye !== Bl && he !== null)
            e: switch (n = he, p = ga, Ye) {
              case L0:
                Ye = Bl, ga = null, Do(
                  e,
                  n,
                  p,
                  L0
                );
                break;
              case Dh:
              case Oh:
                if (zy(p)) {
                  Ye = Bl, ga = null, fg(n);
                  break;
                }
                n = function() {
                  Ye !== Dh && Ye !== Oh || Pe !== e || (Ye = N0), Ll(e);
                }, p.then(n, n);
                break t;
              case z0:
                Ye = N0;
                break t;
              case cC:
                Ye = xE;
                break t;
              case N0:
                zy(p) ? (Ye = Bl, ga = null, fg(n)) : (Ye = Bl, ga = null, Do(
                  e,
                  n,
                  p,
                  N0
                ));
                break;
              case xE:
                var T = null;
                switch (he.tag) {
                  case 26:
                    T = he.memoizedState;
                  case 5:
                  case 27:
                    var C = he;
                    if (!T || th(T)) {
                      Ye = Bl, ga = null;
                      var D = C.sibling;
                      if (D !== null) he = D;
                      else {
                        var z = C.return;
                        z !== null ? (he = z, Pf(z)) : he = null;
                      }
                      break e;
                    }
                    break;
                  default:
                    console.error(
                      "Unexpected type of fiber triggered a suspensey commit. This is a bug in React."
                    );
                }
                Ye = Bl, ga = null, Do(
                  e,
                  n,
                  p,
                  xE
                );
                break;
              case vm:
                Ye = Bl, ga = null, Do(
                  e,
                  n,
                  p,
                  vm
                );
                break;
              case CE:
                sg(), En = i1;
                break t;
              default:
                throw Error(
                  "Unexpected SuspendedReason. This is a bug in React."
                );
            }
          lt.actQueue !== null ? cg() : gp();
          break;
        } catch (tt) {
          Kf(e, tt);
        }
      while (!0);
      return Td(), lt.H = f, lt.A = m, ze = s, he !== null ? (Ct !== null && typeof Ct.markRenderYielded == "function" && Ct.markRenderYielded(), eu) : (no(), Pe = null, de = 0, gf(), En);
    }
    function gp() {
      for (; he !== null && !Ov(); )
        mp(he);
    }
    function mp(e) {
      var n = e.alternate;
      (e.mode & Si) !== $e ? (xd(e), n = wt(
        e,
        tg,
        n,
        e,
        Po
      ), Ts(e)) : n = wt(
        e,
        tg,
        n,
        e,
        Po
      ), e.memoizedProps = e.pendingProps, n === null ? Pf(e) : he = n;
    }
    function fg(e) {
      var n = wt(e, hg, e);
      e.memoizedProps = e.pendingProps, n === null ? Pf(e) : he = n;
    }
    function hg(e) {
      var n = e.alternate, s = (e.mode & Si) !== $e;
      switch (s && xd(e), e.tag) {
        case 15:
        case 0:
          n = Zy(
            n,
            e,
            e.pendingProps,
            e.type,
            void 0,
            de
          );
          break;
        case 11:
          n = Zy(
            n,
            e,
            e.pendingProps,
            e.type.render,
            e.ref,
            de
          );
          break;
        case 5:
          aa(e);
        default:
          Wy(n, e), e = he = Sy(e, Po), n = tg(n, e, Po);
      }
      return s && Ts(e), n;
    }
    function Do(e, n, s, f) {
      Td(), aa(n), fm = null, A0 = 0;
      var m = n.return;
      try {
        if (Uf(
          e,
          m,
          n,
          s,
          de
        )) {
          En = O0, Zu(
            e,
            $i(s, e.current)
          ), he = null;
          return;
        }
      } catch (p) {
        if (m !== null) throw he = m, p;
        En = O0, Zu(
          e,
          $i(s, e.current)
        ), he = null;
        return;
      }
      n.flags & 32768 ? (xe || f === L0 ? e = !0 : Sm || de & 536870912 ? e = !1 : (Ac = e = !0, (f === Dh || f === Oh || f === z0 || f === vm) && (f = os.current, f !== null && f.tag === 13 && (f.flags |= 16384))), dg(n, e)) : Pf(n);
    }
    function Pf(e) {
      var n = e;
      do {
        if (n.flags & 32768) {
          dg(
            n,
            Ac
          );
          return;
        }
        var s = n.alternate;
        if (e = n.return, xd(n), s = wt(
          n,
          sv,
          s,
          n,
          Po
        ), (n.mode & Si) !== $e && _r(n), s !== null) {
          he = s;
          return;
        }
        if (n = n.sibling, n !== null) {
          he = n;
          return;
        }
        he = n = e;
      } while (n !== null);
      En === eu && (En = uC);
    }
    function dg(e, n) {
      do {
        var s = ov(e.alternate, e);
        if (s !== null) {
          s.flags &= 32767, he = s;
          return;
        }
        if ((e.mode & Si) !== $e) {
          _r(e), s = e.actualDuration;
          for (var f = e.child; f !== null; )
            s += f.actualDuration, f = f.sibling;
          e.actualDuration = s;
        }
        if (s = e.return, s !== null && (s.flags |= 32768, s.subtreeFlags = 0, s.deletions = null), !n && (e = e.sibling, e !== null)) {
          he = e;
          return;
        }
        he = e = s;
      } while (e !== null);
      En = i1, he = null;
    }
    function gg(e, n, s, f, m, p, T, C, D) {
      e.cancelPendingCommit = null;
      do
        Wu();
      while (bi !== zh);
      if (qs.flushLegacyContextWarning(), qs.flushPendingUnsafeLifecycleWarnings(), (ze & (fl | Ks)) !== da)
        throw Error("Should not already be working.");
      if (Ct !== null && typeof Ct.markCommitStarted == "function" && Ct.markCommitStarted(s), n === null) _e();
      else {
        if (s === 0 && console.error(
          "finishedLanes should not be empty during a commit. This is a bug in React."
        ), n === e.current)
          throw Error(
            "Cannot commit the same tree as before. This error is likely caused by a bug in React. Please file an issue."
          );
        if (p = n.lanes | n.childLanes, p |= nE, tf(
          e,
          s,
          p,
          T,
          C,
          D
        ), e === Pe && (he = Pe = null, de = 0), Em = n, Oc = e, Lc = s, DE = p, OE = m, yC = f, n.subtreeFlags & 10256 || n.flags & 10256 ? (e.callbackNode = null, e.callbackPriority = 0, vp(mc, function() {
          return Qf(), null;
        })) : (e.callbackNode = null, e.callbackPriority = 0), Zv = sm(), f = (n.flags & 13878) !== 0, n.subtreeFlags & 13878 || f) {
          f = lt.T, lt.T = null, m = Vt.p, Vt.p = Qn, T = ze, ze |= Ks;
          try {
            ig(e, n, s);
          } finally {
            ze = T, Vt.p = m, lt.T = f;
          }
        }
        bi = dC, Ba(), mg(), mv();
      }
    }
    function Ba() {
      if (bi === dC) {
        bi = zh;
        var e = Oc, n = Em, s = Lc, f = (n.flags & 13878) !== 0;
        if (n.subtreeFlags & 13878 || f) {
          f = lt.T, lt.T = null;
          var m = Vt.p;
          Vt.p = Qn;
          var p = ze;
          ze |= Ks;
          try {
            ym = s, pm = e, op(n, e), pm = ym = null, s = XE;
            var T = Z_(e.containerInfo), C = s.focusedElem, D = s.selectionRange;
            if (T !== C && C && C.ownerDocument && q_(
              C.ownerDocument.documentElement,
              C
            )) {
              if (D !== null && yy(C)) {
                var z = D.start, tt = D.end;
                if (tt === void 0 && (tt = z), "selectionStart" in C)
                  C.selectionStart = z, C.selectionEnd = Math.min(
                    tt,
                    C.value.length
                  );
                else {
                  var st = C.ownerDocument || document, J = st && st.defaultView || window;
                  if (J.getSelection) {
                    var rt = J.getSelection(), Dt = C.textContent.length, qt = Math.min(
                      D.start,
                      Dt
                    ), Qe = D.end === void 0 ? qt : Math.min(D.end, Dt);
                    !rt.extend && qt > Qe && (T = Qe, Qe = qt, qt = T);
                    var ye = my(
                      C,
                      qt
                    ), j = my(
                      C,
                      Qe
                    );
                    if (ye && j && (rt.rangeCount !== 1 || rt.anchorNode !== ye.node || rt.anchorOffset !== ye.offset || rt.focusNode !== j.node || rt.focusOffset !== j.offset)) {
                      var B = st.createRange();
                      B.setStart(ye.node, ye.offset), rt.removeAllRanges(), qt > Qe ? (rt.addRange(B), rt.extend(j.node, j.offset)) : (B.setEnd(j.node, j.offset), rt.addRange(B));
                    }
                  }
                }
              }
              for (st = [], rt = C; rt = rt.parentNode; )
                rt.nodeType === 1 && st.push({
                  element: rt,
                  left: rt.scrollLeft,
                  top: rt.scrollTop
                });
              for (typeof C.focus == "function" && C.focus(), C = 0; C < st.length; C++) {
                var k = st[C];
                k.element.scrollLeft = k.left, k.element.scrollTop = k.top;
              }
            }
            E1 = !!IE, XE = IE = null;
          } finally {
            ze = p, Vt.p = m, lt.T = f;
          }
        }
        e.current = n, bi = gC;
      }
    }
    function mg() {
      if (bi === gC) {
        bi = zh;
        var e = Oc, n = Em, s = Lc, f = (n.flags & 8772) !== 0;
        if (n.subtreeFlags & 8772 || f) {
          f = lt.T, lt.T = null;
          var m = Vt.p;
          Vt.p = Qn;
          var p = ze;
          ze |= Ks;
          try {
            Ct !== null && typeof Ct.markLayoutEffectsStarted == "function" && Ct.markLayoutEffectsStarted(s), ym = s, pm = e, ap(
              e,
              n.alternate,
              n
            ), pm = ym = null, Ct !== null && typeof Ct.markLayoutEffectsStopped == "function" && Ct.markLayoutEffectsStopped();
          } finally {
            ze = p, Vt.p = m, lt.T = f;
          }
        }
        bi = mC;
      }
    }
    function mv() {
      if (bi === AM || bi === mC) {
        bi = zh, qS();
        var e = Oc, n = Em, s = Lc, f = yC, m = (n.subtreeFlags & 10256) !== 0 || (n.flags & 10256) !== 0;
        m ? bi = wE : (bi = zh, Em = Oc = null, Ia(e, e.pendingLanes), Nh = 0, Y0 = null);
        var p = e.pendingLanes;
        if (p === 0 && (Dc = null), m || $u(e), m = bu(s), n = n.stateNode, Pn && typeof Pn.onCommitFiberRoot == "function")
          try {
            var T = (n.current.flags & 128) === 128;
            switch (m) {
              case Qn:
                var C = Yg;
                break;
              case ha:
                C = hh;
                break;
              case Is:
                C = mc;
                break;
              case Xg:
                C = dh;
                break;
              default:
                C = mc;
            }
            Pn.onCommitFiberRoot(
              Bo,
              n,
              C,
              T
            );
          } catch (st) {
            ji || (ji = !0, console.error(
              "React instrumentation encountered an error: %s",
              st
            ));
          }
        if (dn && e.memoizedUpdaters.clear(), cp(), f !== null) {
          T = lt.T, C = Vt.p, Vt.p = Qn, lt.T = null;
          try {
            var D = e.onRecoverableError;
            for (n = 0; n < f.length; n++) {
              var z = f[n], tt = yv(z.stack);
              wt(
                z.source,
                D,
                z.value,
                tt
              );
            }
          } finally {
            lt.T = T, Vt.p = C;
          }
        }
        Lc & 3 && Wu(), Ll(e), p = e.pendingLanes, s & 4194090 && p & 42 ? (Kv = !0, e === LE ? H0++ : (H0 = 0, LE = e)) : H0 = 0, Ur(0), _e();
      }
    }
    function yv(e) {
      return e = { componentStack: e }, Object.defineProperty(e, "digest", {
        get: function() {
          console.error(
            'You are accessing "digest" from the errorInfo object passed to onRecoverableError. This property is no longer provided as part of errorInfo but can be accessed as a property of the Error instance itself.'
          );
        }
      }), e;
    }
    function Ia(e, n) {
      (e.pooledCacheLanes &= n) === 0 && (n = e.pooledCache, n != null && (e.pooledCache = null, Ca(n)));
    }
    function Wu(e) {
      return Ba(), mg(), mv(), Qf();
    }
    function Qf() {
      if (bi !== wE) return !1;
      var e = Oc, n = DE;
      DE = 0;
      var s = bu(Lc), f = Is > s ? Is : s;
      s = lt.T;
      var m = Vt.p;
      try {
        Vt.p = f, lt.T = null, f = OE, OE = null;
        var p = Oc, T = Lc;
        if (bi = zh, Em = Oc = null, Lc = 0, (ze & (fl | Ks)) !== da)
          throw Error("Cannot flush passive effects while already rendering.");
        zE = !0, s1 = !1, Ct !== null && typeof Ct.markPassiveEffectsStarted == "function" && Ct.markPassiveEffectsStarted(T);
        var C = ze;
        if (ze |= Ks, rp(p.current), Vf(
          p,
          p.current,
          T,
          f
        ), Ct !== null && typeof Ct.markPassiveEffectsStopped == "function" && Ct.markPassiveEffectsStopped(), $u(p), ze = C, Ur(0, !1), s1 ? p === Y0 ? Nh++ : (Nh = 0, Y0 = p) : Nh = 0, s1 = zE = !1, Pn && typeof Pn.onPostCommitFiberRoot == "function")
          try {
            Pn.onPostCommitFiberRoot(Bo, p);
          } catch (z) {
            ji || (ji = !0, console.error(
              "React instrumentation encountered an error: %s",
              z
            ));
          }
        var D = p.current.stateNode;
        return D.effectDuration = 0, D.passiveEffectDuration = 0, !0;
      } finally {
        Vt.p = m, lt.T = s, Ia(e, n);
      }
    }
    function Ju(e, n, s) {
      n = $i(s, n), n = li(e.stateNode, n, 2), e = ia(e, n, 2), e !== null && (Fe(e, 2), Ll(e));
    }
    function jt(e, n, s) {
      if (bm = !1, e.tag === 3)
        Ju(e, e, s);
      else {
        for (; n !== null; ) {
          if (n.tag === 3) {
            Ju(
              n,
              e,
              s
            );
            return;
          }
          if (n.tag === 1) {
            var f = n.stateNode;
            if (typeof n.type.getDerivedStateFromError == "function" || typeof f.componentDidCatch == "function" && (Dc === null || !Dc.has(f))) {
              e = $i(s, e), s = on(2), f = ia(n, s, 2), f !== null && (Nf(
                s,
                f,
                n,
                e
              ), Fe(f, 2), Ll(f));
              return;
            }
          }
          n = n.return;
        }
        console.error(
          `Internal React error: Attempted to capture a commit phase error inside a detached tree. This indicates a bug in React. Potential causes include deleting the same fiber more than once, committing an already-finished tree, or an inconsistent return pointer.

Error message:

%s`,
          s
        );
      }
    }
    function yp(e, n, s) {
      var f = e.pingCache;
      if (f === null) {
        f = e.pingCache = new TM();
        var m = /* @__PURE__ */ new Set();
        f.set(n, m);
      } else
        m = f.get(n), m === void 0 && (m = /* @__PURE__ */ new Set(), f.set(n, m));
      m.has(s) || (RE = !0, m.add(s), f = YS.bind(null, e, n, s), dn && tc(e, s), n.then(f, f));
    }
    function YS(e, n, s) {
      var f = e.pingCache;
      f !== null && f.delete(n), e.pingedLanes |= e.suspendedLanes & s, e.warmLanes &= ~s, fp() && lt.actQueue === null && console.error(
        `A suspended resource finished loading inside a test, but the event was not wrapped in act(...).

When testing, code that resolves suspended data should be wrapped into act(...):

act(() => {
  /* finish loading suspended data */
});
/* assert on the output */

This ensures that you're testing the behavior the user would see in the browser. Learn more at https://react.dev/link/wrap-tests-with-act`
      ), Pe === e && (de & s) === s && (En === wh || En === TE && (de & 62914560) === de && Qa() - ME < fC ? (ze & fl) === da && Nr(e, 0) : AE |= s, Lh === de && (Lh = 0)), Ll(e);
    }
    function pp(e, n) {
      n === 0 && (n = _l()), e = Mi(e, n), e !== null && (Fe(e, n), Ll(e));
    }
    function Wf(e) {
      var n = e.memoizedState, s = 0;
      n !== null && (s = n.retryLane), pp(e, s);
    }
    function Oo(e, n) {
      var s = 0;
      switch (e.tag) {
        case 13:
          var f = e.stateNode, m = e.memoizedState;
          m !== null && (s = m.retryLane);
          break;
        case 19:
          f = e.stateNode;
          break;
        case 22:
          f = e.stateNode._retryCache;
          break;
        default:
          throw Error(
            "Pinged unknown suspense boundary type. This is probably a bug in React."
          );
      }
      f !== null && f.delete(n), pp(e, s);
    }
    function yg(e, n, s) {
      if (n.subtreeFlags & 67117056)
        for (n = n.child; n !== null; ) {
          var f = e, m = n, p = m.type === cc;
          p = s || p, m.tag !== 22 ? m.flags & 67108864 ? p && wt(
            m,
            _p,
            f,
            m,
            (m.mode & ox) === $e
          ) : yg(
            f,
            m,
            p
          ) : m.memoizedState === null && (p && m.flags & 8192 ? wt(
            m,
            _p,
            f,
            m
          ) : m.subtreeFlags & 67108864 && wt(
            m,
            yg,
            f,
            m,
            p
          )), n = n.sibling;
        }
    }
    function _p(e, n) {
      var s = 2 < arguments.length && arguments[2] !== void 0 ? arguments[2] : !0;
      Xt(!0);
      try {
        il(n), s && Ff(n), Os(e, n.alternate, n, !1), s && ag(e, n, 0, null, !1, 0);
      } finally {
        Xt(!1);
      }
    }
    function $u(e) {
      var n = !0;
      e.current.mode & (Xi | Vs) || (n = !1), yg(
        e,
        e.current,
        n
      );
    }
    function ca(e) {
      if ((ze & fl) === da) {
        var n = e.tag;
        if (n === 3 || n === 1 || n === 0 || n === 11 || n === 14 || n === 15) {
          if (n = it(e) || "ReactComponent", o1 !== null) {
            if (o1.has(n)) return;
            o1.add(n);
          } else o1 = /* @__PURE__ */ new Set([n]);
          wt(e, function() {
            console.error(
              "Can't perform a React state update on a component that hasn't mounted yet. This indicates that you have a side-effect in your render function that asynchronously later calls tries to update the component. Move this work to useEffect instead."
            );
          });
        }
      }
    }
    function tc(e, n) {
      dn && e.memoizedUpdaters.forEach(function(s) {
        Wl(e, s, n);
      });
    }
    function vp(e, n) {
      var s = lt.actQueue;
      return s !== null ? (s.push(n), DM) : Hg(e, n);
    }
    function pv(e) {
      fp() && lt.actQueue === null && wt(e, function() {
        console.error(
          `An update to %s inside a test was not wrapped in act(...).

When testing, code that causes React state updates should be wrapped into act(...):

act(() => {
  /* fire events that update state */
});
/* assert on the output */

This ensures that you're testing the behavior the user would see in the browser. Learn more at https://react.dev/link/wrap-tests-with-act`,
          it(e)
        );
      });
    }
    function Ll(e) {
      e !== Tm && e.next === null && (Tm === null ? r1 = Tm = e : Tm = Tm.next = e), u1 = !0, lt.actQueue !== null ? UE || (UE = !0, An()) : NE || (NE = !0, An());
    }
    function Ur(e, n) {
      if (!GE && u1) {
        GE = !0;
        do
          for (var s = !1, f = r1; f !== null; ) {
            if (e !== 0) {
              var m = f.pendingLanes;
              if (m === 0) var p = 0;
              else {
                var T = f.suspendedLanes, C = f.pingedLanes;
                p = (1 << 31 - oi(42 | e) + 1) - 1, p &= m & ~(T & ~C), p = p & 201326741 ? p & 201326741 | 1 : p ? p | 2 : 0;
              }
              p !== 0 && (s = !0, vg(f, p));
            } else
              p = de, p = yn(
                f,
                f === Pe ? p : 0,
                f.cancelPendingCommit !== null || f.timeoutHandle !== Hh
              ), !(p & 3) || pl(f, p) || (s = !0, vg(f, p));
            f = f.next;
          }
        while (s);
        GE = !1;
      }
    }
    function pg() {
      _g();
    }
    function _g() {
      u1 = UE = NE = !1;
      var e = 0;
      Uh !== 0 && (ec() && (e = Uh), Uh = 0);
      for (var n = Qa(), s = null, f = r1; f !== null; ) {
        var m = f.next, p = Xa(f, n);
        p === 0 ? (f.next = null, s === null ? r1 = m : s.next = m, m === null && (Tm = s)) : (s = f, (e !== 0 || p & 3) && (u1 = !0)), f = m;
      }
      Ur(e);
    }
    function Xa(e, n) {
      for (var s = e.suspendedLanes, f = e.pingedLanes, m = e.expirationTimes, p = e.pendingLanes & -62914561; 0 < p; ) {
        var T = 31 - oi(p), C = 1 << T, D = m[T];
        D === -1 ? (!(C & s) || C & f) && (m[T] = io(C, n)) : D <= n && (e.expiredLanes |= C), p &= ~C;
      }
      if (n = Pe, s = de, s = yn(
        e,
        e === n ? s : 0,
        e.cancelPendingCommit !== null || e.timeoutHandle !== Hh
      ), f = e.callbackNode, s === 0 || e === n && (Ye === Dh || Ye === Oh) || e.cancelPendingCommit !== null)
        return f !== null && Sg(f), e.callbackNode = null, e.callbackPriority = 0;
      if (!(s & 3) || pl(e, s)) {
        if (n = s & -s, n !== e.callbackPriority || lt.actQueue !== null && f !== HE)
          Sg(f);
        else return n;
        switch (bu(s)) {
          case Qn:
          case ha:
            s = hh;
            break;
          case Is:
            s = mc;
            break;
          case Xg:
            s = dh;
            break;
          default:
            s = mc;
        }
        return f = un.bind(null, e), lt.actQueue !== null ? (lt.actQueue.push(f), s = HE) : s = Hg(s, f), e.callbackPriority = n, e.callbackNode = s, n;
      }
      return f !== null && Sg(f), e.callbackPriority = 2, e.callbackNode = null, 2;
    }
    function un(e, n) {
      if (Kv = Fv = !1, bi !== zh && bi !== wE)
        return e.callbackNode = null, e.callbackPriority = 0, null;
      var s = e.callbackNode;
      if (Wu() && e.callbackNode !== s)
        return null;
      var f = de;
      return f = yn(
        e,
        e === Pe ? f : 0,
        e.cancelPendingCommit !== null || e.timeoutHandle !== Hh
      ), f === 0 ? null : (Rn(
        e,
        f,
        n
      ), Xa(e, Qa()), e.callbackNode != null && e.callbackNode === s ? un.bind(null, e) : null);
    }
    function vg(e, n) {
      if (Wu()) return null;
      Fv = Kv, Kv = !1, Rn(e, n, !0);
    }
    function Sg(e) {
      e !== HE && e !== null && VS(e);
    }
    function An() {
      lt.actQueue !== null && lt.actQueue.push(function() {
        return _g(), null;
      }), BM(function() {
        (ze & (fl | Ks)) !== da ? Hg(
          Yg,
          pg
        ) : _g();
      });
    }
    function Sp() {
      return Uh === 0 && (Uh = Pl()), Uh;
    }
    function Ep(e) {
      return e == null || typeof e == "symbol" || typeof e == "boolean" ? null : typeof e == "function" ? e : (Q(e, "action"), Mu("" + e));
    }
    function bp(e, n) {
      var s = n.ownerDocument.createElement("input");
      return s.name = n.name, s.value = n.value, e.id && s.setAttribute("form", e.id), n.parentNode.insertBefore(s, n), e = new FormData(e), s.parentNode.removeChild(s), e;
    }
    function We(e, n, s, f, m) {
      if (n === "submit" && s && s.stateNode === m) {
        var p = Ep(
          (m[Bi] || null).action
        ), T = f.submitter;
        T && (n = (n = T[Bi] || null) ? Ep(n.formAction) : T.getAttribute("formAction"), n !== null && (p = n, T = null));
        var C = new Ht(
          "action",
          "action",
          null,
          f,
          m
        );
        e.push({
          event: C,
          listeners: [
            {
              instance: null,
              listener: function() {
                if (f.defaultPrevented) {
                  if (Uh !== 0) {
                    var D = T ? bp(
                      m,
                      T
                    ) : new FormData(m), z = {
                      pending: !0,
                      data: D,
                      method: m.method,
                      action: p
                    };
                    Object.freeze(z), br(
                      s,
                      z,
                      null,
                      D
                    );
                  }
                } else
                  typeof p == "function" && (C.preventDefault(), D = T ? bp(
                    m,
                    T
                  ) : new FormData(m), z = {
                    pending: !0,
                    data: D,
                    method: m.method,
                    action: p
                  }, Object.freeze(z), br(
                    s,
                    z,
                    p,
                    D
                  ));
              },
              currentTarget: m
            }
          ]
        });
      }
    }
    function qn(e, n, s) {
      e.currentTarget = s;
      try {
        n(e);
      } catch (f) {
        _E(f);
      }
      e.currentTarget = null;
    }
    function ka(e, n) {
      n = (n & 4) !== 0;
      for (var s = 0; s < e.length; s++) {
        var f = e[s];
        t: {
          var m = void 0, p = f.event;
          if (f = f.listeners, n)
            for (var T = f.length - 1; 0 <= T; T--) {
              var C = f[T], D = C.instance, z = C.currentTarget;
              if (C = C.listener, D !== m && p.isPropagationStopped())
                break t;
              D !== null ? wt(
                D,
                qn,
                p,
                C,
                z
              ) : qn(p, C, z), m = D;
            }
          else
            for (T = 0; T < f.length; T++) {
              if (C = f[T], D = C.instance, z = C.currentTarget, C = C.listener, D !== m && p.isPropagationStopped())
                break t;
              D !== null ? wt(
                D,
                qn,
                p,
                C,
                z
              ) : qn(p, C, z), m = D;
            }
        }
      }
    }
    function oe(e, n) {
      YE.has(e) || console.error(
        'Did not expect a listenToNonDelegatedEvent() call for "%s". This is a bug in React. Please file an issue.',
        e
      );
      var s = n[Pp];
      s === void 0 && (s = n[Pp] = /* @__PURE__ */ new Set());
      var f = e + "__bubble";
      s.has(f) || (bg(n, e, 2, !1), s.add(f));
    }
    function Eg(e, n, s) {
      YE.has(e) && !n && console.error(
        'Did not expect a listenToNativeEvent() call for "%s" in the bubble phase. This is a bug in React. Please file an issue.',
        e
      );
      var f = 0;
      n && (f |= 4), bg(
        s,
        e,
        f,
        n
      );
    }
    function Tp(e) {
      if (!e[c1]) {
        e[c1] = !0, zv.forEach(function(s) {
          s !== "selectionchange" && (YE.has(s) || Eg(s, !1, e), Eg(s, !0, e));
        });
        var n = e.nodeType === 9 ? e : e.ownerDocument;
        n === null || n[c1] || (n[c1] = !0, Eg("selectionchange", !1, n));
      }
    }
    function bg(e, n, s, f) {
      switch (zg(n)) {
        case Qn:
          var m = IS;
          break;
        case ha:
          m = Lg;
          break;
        default:
          m = Uo;
      }
      s = m.bind(
        null,
        n,
        s,
        e
      ), m = void 0, !W || n !== "touchstart" && n !== "touchmove" && n !== "wheel" || (m = !0), f ? m !== void 0 ? e.addEventListener(n, s, {
        capture: !0,
        passive: m
      }) : e.addEventListener(n, s, !0) : m !== void 0 ? e.addEventListener(n, s, {
        passive: m
      }) : e.addEventListener(
        n,
        s,
        !1
      );
    }
    function yi(e, n, s, f, m) {
      var p = f;
      if (!(n & 1) && !(n & 2) && f !== null)
        t: for (; ; ) {
          if (f === null) return;
          var T = f.tag;
          if (T === 3 || T === 4) {
            var C = f.stateNode.containerInfo;
            if (C === m) break;
            if (T === 4)
              for (T = f.return; T !== null; ) {
                var D = T.tag;
                if ((D === 3 || D === 4) && T.stateNode.containerInfo === m)
                  return;
                T = T.return;
              }
            for (; C !== null; ) {
              if (T = vl(C), T === null) return;
              if (D = T.tag, D === 5 || D === 6 || D === 26 || D === 27) {
                f = p = T;
                continue t;
              }
              C = C.parentNode;
            }
          }
          f = f.return;
        }
      dd(function() {
        var z = p, tt = rr(s), st = [];
        t: {
          var J = sx.get(e);
          if (J !== void 0) {
            var rt = Ht, Dt = e;
            switch (e) {
              case "keypress":
                if (wu(s) === 0) break t;
              case "keydown":
              case "keyup":
                rt = FA;
                break;
              case "focusin":
                Dt = "focus", rt = pe;
                break;
              case "focusout":
                Dt = "blur", rt = pe;
                break;
              case "beforeblur":
              case "afterblur":
                rt = pe;
                break;
              case "click":
                if (s.button === 2) break t;
              case "auxclick":
              case "dblclick":
              case "mousedown":
              case "mousemove":
              case "mouseup":
              case "mouseout":
              case "mouseover":
              case "contextmenu":
                rt = ie;
                break;
              case "drag":
              case "dragend":
              case "dragenter":
              case "dragexit":
              case "dragleave":
              case "dragover":
              case "dragstart":
              case "drop":
                rt = Bt;
                break;
              case "touchcancel":
              case "touchend":
              case "touchmove":
              case "touchstart":
                rt = QA;
                break;
              case nx:
              case ix:
              case lx:
                rt = PS;
                break;
              case ax:
                rt = JA;
                break;
              case "scroll":
              case "scrollend":
                rt = q;
                break;
              case "wheel":
                rt = tM;
                break;
              case "copy":
              case "cut":
              case "paste":
                rt = BA;
                break;
              case "gotpointercapture":
              case "lostpointercapture":
              case "pointercancel":
              case "pointerdown":
              case "pointermove":
              case "pointerout":
              case "pointerover":
              case "pointerup":
                rt = KT;
                break;
              case "toggle":
              case "beforetoggle":
                rt = nM;
            }
            var qt = (n & 4) !== 0, Qe = !qt && (e === "scroll" || e === "scrollend"), ye = qt ? J !== null ? J + "Capture" : null : J;
            qt = [];
            for (var j = z, B; j !== null; ) {
              var k = j;
              if (B = k.stateNode, k = k.tag, k !== 5 && k !== 26 && k !== 27 || B === null || ye === null || (k = vs(j, ye), k != null && qt.push(
                pi(
                  j,
                  k,
                  B
                )
              )), Qe) break;
              j = j.return;
            }
            0 < qt.length && (J = new rt(
              J,
              Dt,
              null,
              s,
              tt
            ), st.push({
              event: J,
              listeners: qt
            }));
          }
        }
        if (!(n & 7)) {
          t: {
            if (J = e === "mouseover" || e === "pointerover", rt = e === "mouseout" || e === "pointerout", J && s !== v && (Dt = s.relatedTarget || s.fromElement) && (vl(Dt) || Dt[Xo]))
              break t;
            if ((rt || J) && (J = tt.window === tt ? tt : (J = tt.ownerDocument) ? J.defaultView || J.parentWindow : window, rt ? (Dt = s.relatedTarget || s.toElement, rt = z, Dt = Dt ? vl(Dt) : null, Dt !== null && (Qe = O(Dt), qt = Dt.tag, Dt !== Qe || qt !== 5 && qt !== 27 && qt !== 6) && (Dt = null)) : (rt = null, Dt = z), rt !== Dt)) {
              if (qt = ie, k = "onMouseLeave", ye = "onMouseEnter", j = "mouse", (e === "pointerout" || e === "pointerover") && (qt = KT, k = "onPointerLeave", ye = "onPointerEnter", j = "pointer"), Qe = rt == null ? J : Sl(rt), B = Dt == null ? J : Sl(Dt), J = new qt(
                k,
                j + "leave",
                rt,
                s,
                tt
              ), J.target = Qe, J.relatedTarget = B, k = null, vl(tt) === z && (qt = new qt(
                ye,
                j + "enter",
                Dt,
                s,
                tt
              ), qt.target = B, qt.relatedTarget = Qe, k = qt), Qe = k, rt && Dt)
                e: {
                  for (qt = rt, ye = Dt, j = 0, B = qt; B; B = Un(B))
                    j++;
                  for (B = 0, k = ye; k; k = Un(k))
                    B++;
                  for (; 0 < j - B; )
                    qt = Un(qt), j--;
                  for (; 0 < B - j; )
                    ye = Un(ye), B--;
                  for (; j--; ) {
                    if (qt === ye || ye !== null && qt === ye.alternate)
                      break e;
                    qt = Un(qt), ye = Un(ye);
                  }
                  qt = null;
                }
              else qt = null;
              rt !== null && xp(
                st,
                J,
                rt,
                qt,
                !1
              ), Dt !== null && Qe !== null && xp(
                st,
                Qe,
                Dt,
                qt,
                !0
              );
            }
          }
          t: {
            if (J = z ? Sl(z) : window, rt = J.nodeName && J.nodeName.toLowerCase(), rt === "select" || rt === "input" && J.type === "file")
              var ut = dy;
            else if (I_(J))
              if (tx)
                ut = NS;
              else {
                ut = gy;
                var bt = LS;
              }
            else
              rt = J.nodeName, !rt || rt.toLowerCase() !== "input" || J.type !== "checkbox" && J.type !== "radio" ? z && or(z.elementType) && (ut = dy) : ut = zS;
            if (ut && (ut = ut(e, z))) {
              pd(
                st,
                ut,
                s,
                tt
              );
              break t;
            }
            bt && bt(e, J, z), e === "focusout" && z && J.type === "number" && z.memoizedProps.value != null && od(J, "number", J.value);
          }
          switch (bt = z ? Sl(z) : window, e) {
            case "focusin":
              (I_(bt) || bt.contentEditable === "true") && (Jg = bt, WS = z, m0 = null);
              break;
            case "focusout":
              m0 = WS = Jg = null;
              break;
            case "mousedown":
              JS = !0;
              break;
            case "contextmenu":
            case "mouseup":
            case "dragend":
              JS = !1, F_(
                st,
                s,
                tt
              );
              break;
            case "selectionchange":
              if (sM) break;
            case "keydown":
            case "keyup":
              F_(
                st,
                s,
                tt
              );
          }
          var Wt;
          if (QS)
            t: {
              switch (e) {
                case "compositionstart":
                  var Ot = "onCompositionStart";
                  break t;
                case "compositionend":
                  Ot = "onCompositionEnd";
                  break t;
                case "compositionupdate":
                  Ot = "onCompositionUpdate";
                  break t;
              }
              Ot = void 0;
            }
          else
            Wg ? mi(e, s) && (Ot = "onCompositionEnd") : e === "keydown" && s.keyCode === PT && (Ot = "onCompositionStart");
          Ot && (QT && s.locale !== "ko" && (Wg || Ot !== "onCompositionStart" ? Ot === "onCompositionEnd" && Wg && (Wt = Ss()) : (gt = tt, $ = "value" in gt ? gt.value : gt.textContent, Wg = !0)), bt = Jf(
            z,
            Ot
          ), 0 < bt.length && (Ot = new FT(
            Ot,
            e,
            null,
            s,
            tt
          ), st.push({
            event: Ot,
            listeners: bt
          }), Wt ? Ot.data = Wt : (Wt = co(s), Wt !== null && (Ot.data = Wt)))), (Wt = lM ? yd(e, s) : ff(e, s)) && (Ot = Jf(
            z,
            "onBeforeInput"
          ), 0 < Ot.length && (bt = new XA(
            "onBeforeInput",
            "beforeinput",
            null,
            s,
            tt
          ), st.push({
            event: bt,
            listeners: Ot
          }), bt.data = Wt)), We(
            st,
            e,
            z,
            s,
            tt
          );
        }
        ka(st, n);
      });
    }
    function pi(e, n, s) {
      return {
        instance: e,
        listener: n,
        currentTarget: s
      };
    }
    function Jf(e, n) {
      for (var s = n + "Capture", f = []; e !== null; ) {
        var m = e, p = m.stateNode;
        if (m = m.tag, m !== 5 && m !== 26 && m !== 27 || p === null || (m = vs(e, s), m != null && f.unshift(
          pi(e, m, p)
        ), m = vs(e, n), m != null && f.push(
          pi(e, m, p)
        )), e.tag === 3) return f;
        e = e.return;
      }
      return [];
    }
    function Un(e) {
      if (e === null) return null;
      do
        e = e.return;
      while (e && e.tag !== 5 && e.tag !== 27);
      return e || null;
    }
    function xp(e, n, s, f, m) {
      for (var p = n._reactName, T = []; s !== null && s !== f; ) {
        var C = s, D = C.alternate, z = C.stateNode;
        if (C = C.tag, D !== null && D === f) break;
        C !== 5 && C !== 26 && C !== 27 || z === null || (D = z, m ? (z = vs(s, p), z != null && T.unshift(
          pi(s, z, D)
        )) : m || (z = vs(s, p), z != null && T.push(
          pi(s, z, D)
        ))), s = s.return;
      }
      T.length !== 0 && e.push({ event: n, listeners: T });
    }
    function Va(e, n) {
      Au(e, n), e !== "input" && e !== "textarea" && e !== "select" || n == null || n.value !== null || f0 || (f0 = !0, e === "select" && n.multiple ? console.error(
        "`value` prop on `%s` should not be null. Consider using an empty array when `multiple` is set to `true` to clear the component or `undefined` for uncontrolled components.",
        e
      ) : console.error(
        "`value` prop on `%s` should not be null. Consider using an empty string to clear the component or `undefined` for uncontrolled components.",
        e
      ));
      var s = {
        registrationNameDependencies: Hl,
        possibleRegistrationNames: Vr
      };
      or(e) || typeof n.is == "string" || cy(e, n, s), n.contentEditable && !n.suppressContentEditableWarning && n.children != null && console.error(
        "A component is `contentEditable` and contains `children` managed by React. It is now your responsibility to guarantee that none of those nodes are unexpectedly modified or duplicated. This is probably not intentional."
      );
    }
    function Je(e, n, s, f) {
      n !== s && (s = Zn(s), Zn(n) !== s && (f[e] = n));
    }
    function Lo(e, n, s) {
      n.forEach(function(f) {
        s[Rp(f)] = f === "style" ? Hr(e) : e.getAttribute(f);
      });
    }
    function zl(e, n) {
      n === !1 ? console.error(
        "Expected `%s` listener to be a function, instead got `false`.\n\nIf you used to conditionally omit it with %s={condition && value}, pass %s={condition ? value : undefined} instead.",
        e,
        e,
        e
      ) : console.error(
        "Expected `%s` listener to be a function, instead got a value of `%s` type.",
        e,
        typeof n
      );
    }
    function Tg(e, n) {
      return e = e.namespaceURI === Sh || e.namespaceURI === bc ? e.ownerDocument.createElementNS(
        e.namespaceURI,
        e.tagName
      ) : e.ownerDocument.createElement(e.tagName), e.innerHTML = n, e.innerHTML;
    }
    function Zn(e) {
      return F(e) && (console.error(
        "The provided HTML markup uses a value of unsupported type %s. This value must be coerced to a string before using it here.",
        vt(e)
      ), St(e)), (typeof e == "string" ? e : "" + e).replace(OM, `
`).replace(LM, "");
    }
    function Cp(e, n) {
      return n = Zn(n), Zn(e) === n;
    }
    function zs() {
    }
    function be(e, n, s, f, m, p) {
      switch (s) {
        case "children":
          typeof f == "string" ? (uf(f, n, !1), n === "body" || n === "textarea" && f === "" || sr(e, f)) : (typeof f == "number" || typeof f == "bigint") && (uf("" + f, n, !1), n !== "body" && sr(e, "" + f));
          break;
        case "className":
          On(e, "class", f);
          break;
        case "tabIndex":
          On(e, "tabindex", f);
          break;
        case "dir":
        case "role":
        case "viewBox":
        case "width":
        case "height":
          On(e, s, f);
          break;
        case "style":
          cf(e, f, p);
          break;
        case "data":
          if (n !== "object") {
            On(e, "data", f);
            break;
          }
        case "src":
        case "href":
          if (f === "" && (n !== "a" || s !== "href")) {
            console.error(
              s === "src" ? 'An empty string ("") was passed to the %s attribute. This may cause the browser to download the whole page again over the network. To fix this, either do not render the element at all or pass null to %s instead of an empty string.' : 'An empty string ("") was passed to the %s attribute. To fix this, either do not render the element at all or pass null to %s instead of an empty string.',
              s,
              s
            ), e.removeAttribute(s);
            break;
          }
          if (f == null || typeof f == "function" || typeof f == "symbol" || typeof f == "boolean") {
            e.removeAttribute(s);
            break;
          }
          Q(f, s), f = Mu("" + f), e.setAttribute(s, f);
          break;
        case "action":
        case "formAction":
          if (f != null && (n === "form" ? s === "formAction" ? console.error(
            "You can only pass the formAction prop to <input> or <button>. Use the action prop on <form>."
          ) : typeof f == "function" && (m.encType == null && m.method == null || d1 || (d1 = !0, console.error(
            "Cannot specify a encType or method for a form that specifies a function as the action. React provides those automatically. They will get overridden."
          )), m.target == null || h1 || (h1 = !0, console.error(
            "Cannot specify a target for a form that specifies a function as the action. The function will always be executed in the same window."
          ))) : n === "input" || n === "button" ? s === "action" ? console.error(
            "You can only pass the action prop to <form>. Use the formAction prop on <input> or <button>."
          ) : n !== "input" || m.type === "submit" || m.type === "image" || f1 ? n !== "button" || m.type == null || m.type === "submit" || f1 ? typeof f == "function" && (m.name == null || EC || (EC = !0, console.error(
            'Cannot specify a "name" prop for a button that specifies a function as a formAction. React needs it to encode which action should be invoked. It will get overridden.'
          )), m.formEncType == null && m.formMethod == null || d1 || (d1 = !0, console.error(
            "Cannot specify a formEncType or formMethod for a button that specifies a function as a formAction. React provides those automatically. They will get overridden."
          )), m.formTarget == null || h1 || (h1 = !0, console.error(
            "Cannot specify a formTarget for a button that specifies a function as a formAction. The function will always be executed in the same window."
          ))) : (f1 = !0, console.error(
            'A button can only specify a formAction along with type="submit" or no type.'
          )) : (f1 = !0, console.error(
            'An input can only specify a formAction along with type="submit" or type="image".'
          )) : console.error(
            s === "action" ? "You can only pass the action prop to <form>." : "You can only pass the formAction prop to <input> or <button>."
          )), typeof f == "function") {
            e.setAttribute(
              s,
              "javascript:throw new Error('A React form was unexpectedly submitted. If you called form.submit() manually, consider using form.requestSubmit() instead. If you\\'re trying to use event.stopPropagation() in a submit event handler, consider also calling event.preventDefault().')"
            );
            break;
          } else
            typeof p == "function" && (s === "formAction" ? (n !== "input" && be(e, n, "name", m.name, m, null), be(
              e,
              n,
              "formEncType",
              m.formEncType,
              m,
              null
            ), be(
              e,
              n,
              "formMethod",
              m.formMethod,
              m,
              null
            ), be(
              e,
              n,
              "formTarget",
              m.formTarget,
              m,
              null
            )) : (be(
              e,
              n,
              "encType",
              m.encType,
              m,
              null
            ), be(e, n, "method", m.method, m, null), be(
              e,
              n,
              "target",
              m.target,
              m,
              null
            )));
          if (f == null || typeof f == "symbol" || typeof f == "boolean") {
            e.removeAttribute(s);
            break;
          }
          Q(f, s), f = Mu("" + f), e.setAttribute(s, f);
          break;
        case "onClick":
          f != null && (typeof f != "function" && zl(s, f), e.onclick = zs);
          break;
        case "onScroll":
          f != null && (typeof f != "function" && zl(s, f), oe("scroll", e));
          break;
        case "onScrollEnd":
          f != null && (typeof f != "function" && zl(s, f), oe("scrollend", e));
          break;
        case "dangerouslySetInnerHTML":
          if (f != null) {
            if (typeof f != "object" || !("__html" in f))
              throw Error(
                "`props.dangerouslySetInnerHTML` must be in the form `{__html: ...}`. Please visit https://react.dev/link/dangerously-set-inner-html for more information."
              );
            if (s = f.__html, s != null) {
              if (m.children != null)
                throw Error(
                  "Can only set one of `children` or `props.dangerouslySetInnerHTML`."
                );
              e.innerHTML = s;
            }
          }
          break;
        case "multiple":
          e.multiple = f && typeof f != "function" && typeof f != "symbol";
          break;
        case "muted":
          e.muted = f && typeof f != "function" && typeof f != "symbol";
          break;
        case "suppressContentEditableWarning":
        case "suppressHydrationWarning":
        case "defaultValue":
        case "defaultChecked":
        case "innerHTML":
        case "ref":
          break;
        case "autoFocus":
          break;
        case "xlinkHref":
          if (f == null || typeof f == "function" || typeof f == "boolean" || typeof f == "symbol") {
            e.removeAttribute("xlink:href");
            break;
          }
          Q(f, s), s = Mu("" + f), e.setAttributeNS(Gh, "xlink:href", s);
          break;
        case "contentEditable":
        case "spellCheck":
        case "draggable":
        case "value":
        case "autoReverse":
        case "externalResourcesRequired":
        case "focusable":
        case "preserveAlpha":
          f != null && typeof f != "function" && typeof f != "symbol" ? (Q(f, s), e.setAttribute(s, "" + f)) : e.removeAttribute(s);
          break;
        case "inert":
          f !== "" || g1[s] || (g1[s] = !0, console.error(
            "Received an empty string for a boolean attribute `%s`. This will treat the attribute as if it were false. Either pass `false` to silence this warning, or pass `true` if you used an empty string in earlier versions of React to indicate this attribute is true.",
            s
          ));
        case "allowFullScreen":
        case "async":
        case "autoPlay":
        case "controls":
        case "default":
        case "defer":
        case "disabled":
        case "disablePictureInPicture":
        case "disableRemotePlayback":
        case "formNoValidate":
        case "hidden":
        case "loop":
        case "noModule":
        case "noValidate":
        case "open":
        case "playsInline":
        case "readOnly":
        case "required":
        case "reversed":
        case "scoped":
        case "seamless":
        case "itemScope":
          f && typeof f != "function" && typeof f != "symbol" ? e.setAttribute(s, "") : e.removeAttribute(s);
          break;
        case "capture":
        case "download":
          f === !0 ? e.setAttribute(s, "") : f !== !1 && f != null && typeof f != "function" && typeof f != "symbol" ? (Q(f, s), e.setAttribute(s, f)) : e.removeAttribute(s);
          break;
        case "cols":
        case "rows":
        case "size":
        case "span":
          f != null && typeof f != "function" && typeof f != "symbol" && !isNaN(f) && 1 <= f ? (Q(f, s), e.setAttribute(s, f)) : e.removeAttribute(s);
          break;
        case "rowSpan":
        case "start":
          f == null || typeof f == "function" || typeof f == "symbol" || isNaN(f) ? e.removeAttribute(s) : (Q(f, s), e.setAttribute(s, f));
          break;
        case "popover":
          oe("beforetoggle", e), oe("toggle", e), ao(e, "popover", f);
          break;
        case "xlinkActuate":
          bn(
            e,
            Gh,
            "xlink:actuate",
            f
          );
          break;
        case "xlinkArcrole":
          bn(
            e,
            Gh,
            "xlink:arcrole",
            f
          );
          break;
        case "xlinkRole":
          bn(
            e,
            Gh,
            "xlink:role",
            f
          );
          break;
        case "xlinkShow":
          bn(
            e,
            Gh,
            "xlink:show",
            f
          );
          break;
        case "xlinkTitle":
          bn(
            e,
            Gh,
            "xlink:title",
            f
          );
          break;
        case "xlinkType":
          bn(
            e,
            Gh,
            "xlink:type",
            f
          );
          break;
        case "xmlBase":
          bn(
            e,
            jE,
            "xml:base",
            f
          );
          break;
        case "xmlLang":
          bn(
            e,
            jE,
            "xml:lang",
            f
          );
          break;
        case "xmlSpace":
          bn(
            e,
            jE,
            "xml:space",
            f
          );
          break;
        case "is":
          p != null && console.error(
            'Cannot update the "is" prop after it has been initialized.'
          ), ao(e, "is", f);
          break;
        case "innerText":
        case "textContent":
          break;
        case "popoverTarget":
          bC || f == null || typeof f != "object" || (bC = !0, console.error(
            "The `popoverTarget` prop expects the ID of an Element as a string. Received %s instead.",
            f
          ));
        default:
          !(2 < s.length) || s[0] !== "o" && s[0] !== "O" || s[1] !== "n" && s[1] !== "N" ? (s = hd(s), ao(e, s, f)) : Hl.hasOwnProperty(s) && f != null && typeof f != "function" && zl(s, f);
      }
    }
    function Gr(e, n, s, f, m, p) {
      switch (s) {
        case "style":
          cf(e, f, p);
          break;
        case "dangerouslySetInnerHTML":
          if (f != null) {
            if (typeof f != "object" || !("__html" in f))
              throw Error(
                "`props.dangerouslySetInnerHTML` must be in the form `{__html: ...}`. Please visit https://react.dev/link/dangerously-set-inner-html for more information."
              );
            if (s = f.__html, s != null) {
              if (m.children != null)
                throw Error(
                  "Can only set one of `children` or `props.dangerouslySetInnerHTML`."
                );
              e.innerHTML = s;
            }
          }
          break;
        case "children":
          typeof f == "string" ? sr(e, f) : (typeof f == "number" || typeof f == "bigint") && sr(e, "" + f);
          break;
        case "onScroll":
          f != null && (typeof f != "function" && zl(s, f), oe("scroll", e));
          break;
        case "onScrollEnd":
          f != null && (typeof f != "function" && zl(s, f), oe("scrollend", e));
          break;
        case "onClick":
          f != null && (typeof f != "function" && zl(s, f), e.onclick = zs);
          break;
        case "suppressContentEditableWarning":
        case "suppressHydrationWarning":
        case "innerHTML":
        case "ref":
          break;
        case "innerText":
        case "textContent":
          break;
        default:
          if (Hl.hasOwnProperty(s))
            f != null && typeof f != "function" && zl(s, f);
          else
            t: {
              if (s[0] === "o" && s[1] === "n" && (m = s.endsWith("Capture"), n = s.slice(2, m ? s.length - 7 : void 0), p = e[Bi] || null, p = p != null ? p[s] : null, typeof p == "function" && e.removeEventListener(n, p, m), typeof f == "function")) {
                typeof p != "function" && p !== null && (s in e ? e[s] = null : e.hasAttribute(s) && e.removeAttribute(s)), e.addEventListener(n, f, m);
                break t;
              }
              s in e ? e[s] = f : f === !0 ? e.setAttribute(s, "") : ao(e, s, f);
            }
      }
    }
    function cn(e, n, s) {
      switch (Va(n, s), n) {
        case "div":
        case "span":
        case "svg":
        case "path":
        case "a":
        case "g":
        case "p":
        case "li":
          break;
        case "img":
          oe("error", e), oe("load", e);
          var f = !1, m = !1, p;
          for (p in s)
            if (s.hasOwnProperty(p)) {
              var T = s[p];
              if (T != null)
                switch (p) {
                  case "src":
                    f = !0;
                    break;
                  case "srcSet":
                    m = !0;
                    break;
                  case "children":
                  case "dangerouslySetInnerHTML":
                    throw Error(
                      n + " is a void element tag and must neither have `children` nor use `dangerouslySetInnerHTML`."
                    );
                  default:
                    be(e, n, p, T, s, null);
                }
            }
          m && be(e, n, "srcSet", s.srcSet, s, null), f && be(e, n, "src", s.src, s, null);
          return;
        case "input":
          Ci("input", s), oe("invalid", e);
          var C = p = T = m = null, D = null, z = null;
          for (f in s)
            if (s.hasOwnProperty(f)) {
              var tt = s[f];
              if (tt != null)
                switch (f) {
                  case "name":
                    m = tt;
                    break;
                  case "type":
                    T = tt;
                    break;
                  case "checked":
                    D = tt;
                    break;
                  case "defaultChecked":
                    z = tt;
                    break;
                  case "value":
                    p = tt;
                    break;
                  case "defaultValue":
                    C = tt;
                    break;
                  case "children":
                  case "dangerouslySetInnerHTML":
                    if (tt != null)
                      throw Error(
                        n + " is a void element tag and must neither have `children` nor use `dangerouslySetInnerHTML`."
                      );
                    break;
                  default:
                    be(e, n, f, tt, s, null);
                }
            }
          so(e, s), G_(
            e,
            p,
            C,
            D,
            z,
            T,
            m,
            !1
          ), ps(e);
          return;
        case "select":
          Ci("select", s), oe("invalid", e), f = T = p = null;
          for (m in s)
            if (s.hasOwnProperty(m) && (C = s[m], C != null))
              switch (m) {
                case "value":
                  p = C;
                  break;
                case "defaultValue":
                  T = C;
                  break;
                case "multiple":
                  f = C;
                default:
                  be(
                    e,
                    n,
                    m,
                    C,
                    s,
                    null
                  );
              }
          sf(e, s), n = p, s = T, e.multiple = !!f, n != null ? _s(e, !!f, n, !1) : s != null && _s(e, !!f, s, !0);
          return;
        case "textarea":
          Ci("textarea", s), oe("invalid", e), p = m = f = null;
          for (T in s)
            if (s.hasOwnProperty(T) && (C = s[T], C != null))
              switch (T) {
                case "value":
                  f = C;
                  break;
                case "defaultValue":
                  m = C;
                  break;
                case "children":
                  p = C;
                  break;
                case "dangerouslySetInnerHTML":
                  if (C != null)
                    throw Error(
                      "`dangerouslySetInnerHTML` does not make sense on <textarea>."
                    );
                  break;
                default:
                  be(
                    e,
                    n,
                    T,
                    C,
                    s,
                    null
                  );
              }
          Ea(e, s), ay(e, f, m, p), ps(e);
          return;
        case "option":
          ly(e, s);
          for (D in s)
            if (s.hasOwnProperty(D) && (f = s[D], f != null))
              switch (D) {
                case "selected":
                  e.selected = f && typeof f != "function" && typeof f != "symbol";
                  break;
                default:
                  be(e, n, D, f, s, null);
              }
          return;
        case "dialog":
          oe("beforetoggle", e), oe("toggle", e), oe("cancel", e), oe("close", e);
          break;
        case "iframe":
        case "object":
          oe("load", e);
          break;
        case "video":
        case "audio":
          for (f = 0; f < j0.length; f++)
            oe(j0[f], e);
          break;
        case "image":
          oe("error", e), oe("load", e);
          break;
        case "details":
          oe("toggle", e);
          break;
        case "embed":
        case "source":
        case "link":
          oe("error", e), oe("load", e);
        case "area":
        case "base":
        case "br":
        case "col":
        case "hr":
        case "keygen":
        case "meta":
        case "param":
        case "track":
        case "wbr":
        case "menuitem":
          for (z in s)
            if (s.hasOwnProperty(z) && (f = s[z], f != null))
              switch (z) {
                case "children":
                case "dangerouslySetInnerHTML":
                  throw Error(
                    n + " is a void element tag and must neither have `children` nor use `dangerouslySetInnerHTML`."
                  );
                default:
                  be(e, n, z, f, s, null);
              }
          return;
        default:
          if (or(n)) {
            for (tt in s)
              s.hasOwnProperty(tt) && (f = s[tt], f !== void 0 && Gr(
                e,
                n,
                tt,
                f,
                s,
                void 0
              ));
            return;
          }
      }
      for (C in s)
        s.hasOwnProperty(C) && (f = s[C], f != null && be(e, n, C, f, s, null));
    }
    function _v(e, n, s, f) {
      switch (Va(n, f), n) {
        case "div":
        case "span":
        case "svg":
        case "path":
        case "a":
        case "g":
        case "p":
        case "li":
          break;
        case "input":
          var m = null, p = null, T = null, C = null, D = null, z = null, tt = null;
          for (rt in s) {
            var st = s[rt];
            if (s.hasOwnProperty(rt) && st != null)
              switch (rt) {
                case "checked":
                  break;
                case "value":
                  break;
                case "defaultValue":
                  D = st;
                default:
                  f.hasOwnProperty(rt) || be(
                    e,
                    n,
                    rt,
                    null,
                    f,
                    st
                  );
              }
          }
          for (var J in f) {
            var rt = f[J];
            if (st = s[J], f.hasOwnProperty(J) && (rt != null || st != null))
              switch (J) {
                case "type":
                  p = rt;
                  break;
                case "name":
                  m = rt;
                  break;
                case "checked":
                  z = rt;
                  break;
                case "defaultChecked":
                  tt = rt;
                  break;
                case "value":
                  T = rt;
                  break;
                case "defaultValue":
                  C = rt;
                  break;
                case "children":
                case "dangerouslySetInnerHTML":
                  if (rt != null)
                    throw Error(
                      n + " is a void element tag and must neither have `children` nor use `dangerouslySetInnerHTML`."
                    );
                  break;
                default:
                  rt !== st && be(
                    e,
                    n,
                    J,
                    rt,
                    f,
                    st
                  );
              }
          }
          n = s.type === "checkbox" || s.type === "radio" ? s.checked != null : s.value != null, f = f.type === "checkbox" || f.type === "radio" ? f.checked != null : f.value != null, n || !f || SC || (console.error(
            "A component is changing an uncontrolled input to be controlled. This is likely caused by the value changing from undefined to a defined value, which should not happen. Decide between using a controlled or uncontrolled input element for the lifetime of the component. More info: https://react.dev/link/controlled-components"
          ), SC = !0), !n || f || vC || (console.error(
            "A component is changing a controlled input to be uncontrolled. This is likely caused by the value changing from a defined to undefined, which should not happen. Decide between using a controlled or uncontrolled input element for the lifetime of the component. More info: https://react.dev/link/controlled-components"
          ), vC = !0), oo(
            e,
            T,
            C,
            D,
            z,
            tt,
            p,
            m
          );
          return;
        case "select":
          rt = T = C = J = null;
          for (p in s)
            if (D = s[p], s.hasOwnProperty(p) && D != null)
              switch (p) {
                case "value":
                  break;
                case "multiple":
                  rt = D;
                default:
                  f.hasOwnProperty(p) || be(
                    e,
                    n,
                    p,
                    null,
                    f,
                    D
                  );
              }
          for (m in f)
            if (p = f[m], D = s[m], f.hasOwnProperty(m) && (p != null || D != null))
              switch (m) {
                case "value":
                  J = p;
                  break;
                case "defaultValue":
                  C = p;
                  break;
                case "multiple":
                  T = p;
                default:
                  p !== D && be(
                    e,
                    n,
                    m,
                    p,
                    f,
                    D
                  );
              }
          f = C, n = T, s = rt, J != null ? _s(e, !!n, J, !1) : !!s != !!n && (f != null ? _s(e, !!n, f, !0) : _s(e, !!n, n ? [] : "", !1));
          return;
        case "textarea":
          rt = J = null;
          for (C in s)
            if (m = s[C], s.hasOwnProperty(C) && m != null && !f.hasOwnProperty(C))
              switch (C) {
                case "value":
                  break;
                case "children":
                  break;
                default:
                  be(e, n, C, null, f, m);
              }
          for (T in f)
            if (m = f[T], p = s[T], f.hasOwnProperty(T) && (m != null || p != null))
              switch (T) {
                case "value":
                  J = m;
                  break;
                case "defaultValue":
                  rt = m;
                  break;
                case "children":
                  break;
                case "dangerouslySetInnerHTML":
                  if (m != null)
                    throw Error(
                      "`dangerouslySetInnerHTML` does not make sense on <textarea>."
                    );
                  break;
                default:
                  m !== p && be(e, n, T, m, f, p);
              }
          rd(e, J, rt);
          return;
        case "option":
          for (var Dt in s)
            if (J = s[Dt], s.hasOwnProperty(Dt) && J != null && !f.hasOwnProperty(Dt))
              switch (Dt) {
                case "selected":
                  e.selected = !1;
                  break;
                default:
                  be(
                    e,
                    n,
                    Dt,
                    null,
                    f,
                    J
                  );
              }
          for (D in f)
            if (J = f[D], rt = s[D], f.hasOwnProperty(D) && J !== rt && (J != null || rt != null))
              switch (D) {
                case "selected":
                  e.selected = J && typeof J != "function" && typeof J != "symbol";
                  break;
                default:
                  be(
                    e,
                    n,
                    D,
                    J,
                    f,
                    rt
                  );
              }
          return;
        case "img":
        case "link":
        case "area":
        case "base":
        case "br":
        case "col":
        case "embed":
        case "hr":
        case "keygen":
        case "meta":
        case "param":
        case "source":
        case "track":
        case "wbr":
        case "menuitem":
          for (var qt in s)
            J = s[qt], s.hasOwnProperty(qt) && J != null && !f.hasOwnProperty(qt) && be(
              e,
              n,
              qt,
              null,
              f,
              J
            );
          for (z in f)
            if (J = f[z], rt = s[z], f.hasOwnProperty(z) && J !== rt && (J != null || rt != null))
              switch (z) {
                case "children":
                case "dangerouslySetInnerHTML":
                  if (J != null)
                    throw Error(
                      n + " is a void element tag and must neither have `children` nor use `dangerouslySetInnerHTML`."
                    );
                  break;
                default:
                  be(
                    e,
                    n,
                    z,
                    J,
                    f,
                    rt
                  );
              }
          return;
        default:
          if (or(n)) {
            for (var Qe in s)
              J = s[Qe], s.hasOwnProperty(Qe) && J !== void 0 && !f.hasOwnProperty(Qe) && Gr(
                e,
                n,
                Qe,
                void 0,
                f,
                J
              );
            for (tt in f)
              J = f[tt], rt = s[tt], !f.hasOwnProperty(tt) || J === rt || J === void 0 && rt === void 0 || Gr(
                e,
                n,
                tt,
                J,
                f,
                rt
              );
            return;
          }
      }
      for (var ye in s)
        J = s[ye], s.hasOwnProperty(ye) && J != null && !f.hasOwnProperty(ye) && be(e, n, ye, null, f, J);
      for (st in f)
        J = f[st], rt = s[st], !f.hasOwnProperty(st) || J === rt || J == null && rt == null || be(e, n, st, J, f, rt);
    }
    function Rp(e) {
      switch (e) {
        case "class":
          return "className";
        case "for":
          return "htmlFor";
        default:
          return e;
      }
    }
    function Hr(e) {
      var n = {};
      e = e.style;
      for (var s = 0; s < e.length; s++) {
        var f = e[s];
        n[f] = e.getPropertyValue(f);
      }
      return n;
    }
    function Ap(e, n, s) {
      if (n != null && typeof n != "object")
        console.error(
          "The `style` prop expects a mapping from style properties to values, not a string. For example, style={{marginRight: spacing + 'em'}} when using JSX."
        );
      else {
        var f, m = f = "", p;
        for (p in n)
          if (n.hasOwnProperty(p)) {
            var T = n[p];
            T != null && typeof T != "boolean" && T !== "" && (p.indexOf("--") === 0 ? (At(T, p), f += m + p + ":" + ("" + T).trim()) : typeof T != "number" || T === 0 || vh.has(p) ? (At(T, p), f += m + p.replace(Xs, "-$1").toLowerCase().replace(ks, "-ms-") + ":" + ("" + T).trim()) : f += m + p.replace(Xs, "-$1").toLowerCase().replace(ks, "-ms-") + ":" + T + "px", m = ";");
          }
        f = f || null, n = e.getAttribute("style"), n !== f && (f = Zn(f), Zn(n) !== f && (s.style = Hr(e)));
      }
    }
    function _i(e, n, s, f, m, p) {
      if (m.delete(s), e = e.getAttribute(s), e === null)
        switch (typeof f) {
          case "undefined":
          case "function":
          case "symbol":
          case "boolean":
            return;
        }
      else if (f != null)
        switch (typeof f) {
          case "function":
          case "symbol":
          case "boolean":
            break;
          default:
            if (Q(f, n), e === "" + f)
              return;
        }
      Je(n, e, f, p);
    }
    function Mp(e, n, s, f, m, p) {
      if (m.delete(s), e = e.getAttribute(s), e === null) {
        switch (typeof f) {
          case "function":
          case "symbol":
            return;
        }
        if (!f) return;
      } else
        switch (typeof f) {
          case "function":
          case "symbol":
            break;
          default:
            if (f) return;
        }
      Je(n, e, f, p);
    }
    function wp(e, n, s, f, m, p) {
      if (m.delete(s), e = e.getAttribute(s), e === null)
        switch (typeof f) {
          case "undefined":
          case "function":
          case "symbol":
            return;
        }
      else if (f != null)
        switch (typeof f) {
          case "function":
          case "symbol":
            break;
          default:
            if (Q(f, s), e === "" + f)
              return;
        }
      Je(n, e, f, p);
    }
    function vv(e, n, s, f, m, p) {
      if (m.delete(s), e = e.getAttribute(s), e === null)
        switch (typeof f) {
          case "undefined":
          case "function":
          case "symbol":
          case "boolean":
            return;
          default:
            if (isNaN(f)) return;
        }
      else if (f != null)
        switch (typeof f) {
          case "function":
          case "symbol":
          case "boolean":
            break;
          default:
            if (!isNaN(f) && (Q(f, n), e === "" + f))
              return;
        }
      Je(n, e, f, p);
    }
    function Re(e, n, s, f, m, p) {
      if (m.delete(s), e = e.getAttribute(s), e === null)
        switch (typeof f) {
          case "undefined":
          case "function":
          case "symbol":
          case "boolean":
            return;
        }
      else if (f != null)
        switch (typeof f) {
          case "function":
          case "symbol":
          case "boolean":
            break;
          default:
            if (Q(f, n), s = Mu("" + f), e === s)
              return;
        }
      Je(n, e, f, p);
    }
    function Be(e, n, s, f) {
      for (var m = {}, p = /* @__PURE__ */ new Set(), T = e.attributes, C = 0; C < T.length; C++)
        switch (T[C].name.toLowerCase()) {
          case "value":
            break;
          case "checked":
            break;
          case "selected":
            break;
          default:
            p.add(T[C].name);
        }
      if (or(n)) {
        for (var D in s)
          if (s.hasOwnProperty(D)) {
            var z = s[D];
            if (z != null) {
              if (Hl.hasOwnProperty(D))
                typeof z != "function" && zl(D, z);
              else if (s.suppressHydrationWarning !== !0)
                switch (D) {
                  case "children":
                    typeof z != "string" && typeof z != "number" || Je(
                      "children",
                      e.textContent,
                      z,
                      m
                    );
                    continue;
                  case "suppressContentEditableWarning":
                  case "suppressHydrationWarning":
                  case "defaultValue":
                  case "defaultChecked":
                  case "innerHTML":
                  case "ref":
                    continue;
                  case "dangerouslySetInnerHTML":
                    T = e.innerHTML, z = z ? z.__html : void 0, z != null && (z = Tg(e, z), Je(
                      D,
                      T,
                      z,
                      m
                    ));
                    continue;
                  case "style":
                    p.delete(D), Ap(e, z, m);
                    continue;
                  case "offsetParent":
                  case "offsetTop":
                  case "offsetLeft":
                  case "offsetWidth":
                  case "offsetHeight":
                  case "isContentEditable":
                  case "outerText":
                  case "outerHTML":
                    p.delete(D.toLowerCase()), console.error(
                      "Assignment to read-only property will result in a no-op: `%s`",
                      D
                    );
                    continue;
                  case "className":
                    p.delete("class"), T = ef(
                      e,
                      "class",
                      z
                    ), Je(
                      "className",
                      T,
                      z,
                      m
                    );
                    continue;
                  default:
                    f.context === iu && n !== "svg" && n !== "math" ? p.delete(D.toLowerCase()) : p.delete(D), T = ef(
                      e,
                      D,
                      z
                    ), Je(
                      D,
                      T,
                      z,
                      m
                    );
                }
            }
          }
      } else
        for (z in s)
          if (s.hasOwnProperty(z) && (D = s[z], D != null)) {
            if (Hl.hasOwnProperty(z))
              typeof D != "function" && zl(z, D);
            else if (s.suppressHydrationWarning !== !0)
              switch (z) {
                case "children":
                  typeof D != "string" && typeof D != "number" || Je(
                    "children",
                    e.textContent,
                    D,
                    m
                  );
                  continue;
                case "suppressContentEditableWarning":
                case "suppressHydrationWarning":
                case "value":
                case "checked":
                case "selected":
                case "defaultValue":
                case "defaultChecked":
                case "innerHTML":
                case "ref":
                  continue;
                case "dangerouslySetInnerHTML":
                  T = e.innerHTML, D = D ? D.__html : void 0, D != null && (D = Tg(e, D), T !== D && (m[z] = { __html: T }));
                  continue;
                case "className":
                  _i(
                    e,
                    z,
                    "class",
                    D,
                    p,
                    m
                  );
                  continue;
                case "tabIndex":
                  _i(
                    e,
                    z,
                    "tabindex",
                    D,
                    p,
                    m
                  );
                  continue;
                case "style":
                  p.delete(z), Ap(e, D, m);
                  continue;
                case "multiple":
                  p.delete(z), Je(
                    z,
                    e.multiple,
                    D,
                    m
                  );
                  continue;
                case "muted":
                  p.delete(z), Je(
                    z,
                    e.muted,
                    D,
                    m
                  );
                  continue;
                case "autoFocus":
                  p.delete("autofocus"), Je(
                    z,
                    e.autofocus,
                    D,
                    m
                  );
                  continue;
                case "data":
                  if (n !== "object") {
                    p.delete(z), T = e.getAttribute("data"), Je(
                      z,
                      T,
                      D,
                      m
                    );
                    continue;
                  }
                case "src":
                case "href":
                  if (!(D !== "" || n === "a" && z === "href" || n === "object" && z === "data")) {
                    console.error(
                      z === "src" ? 'An empty string ("") was passed to the %s attribute. This may cause the browser to download the whole page again over the network. To fix this, either do not render the element at all or pass null to %s instead of an empty string.' : 'An empty string ("") was passed to the %s attribute. To fix this, either do not render the element at all or pass null to %s instead of an empty string.',
                      z,
                      z
                    );
                    continue;
                  }
                  Re(
                    e,
                    z,
                    z,
                    D,
                    p,
                    m
                  );
                  continue;
                case "action":
                case "formAction":
                  if (T = e.getAttribute(z), typeof D == "function") {
                    p.delete(z.toLowerCase()), z === "formAction" ? (p.delete("name"), p.delete("formenctype"), p.delete("formmethod"), p.delete("formtarget")) : (p.delete("enctype"), p.delete("method"), p.delete("target"));
                    continue;
                  } else if (T === zM) {
                    p.delete(z.toLowerCase()), Je(
                      z,
                      "function",
                      D,
                      m
                    );
                    continue;
                  }
                  Re(
                    e,
                    z,
                    z.toLowerCase(),
                    D,
                    p,
                    m
                  );
                  continue;
                case "xlinkHref":
                  Re(
                    e,
                    z,
                    "xlink:href",
                    D,
                    p,
                    m
                  );
                  continue;
                case "contentEditable":
                  wp(
                    e,
                    z,
                    "contenteditable",
                    D,
                    p,
                    m
                  );
                  continue;
                case "spellCheck":
                  wp(
                    e,
                    z,
                    "spellcheck",
                    D,
                    p,
                    m
                  );
                  continue;
                case "draggable":
                case "autoReverse":
                case "externalResourcesRequired":
                case "focusable":
                case "preserveAlpha":
                  wp(
                    e,
                    z,
                    z,
                    D,
                    p,
                    m
                  );
                  continue;
                case "allowFullScreen":
                case "async":
                case "autoPlay":
                case "controls":
                case "default":
                case "defer":
                case "disabled":
                case "disablePictureInPicture":
                case "disableRemotePlayback":
                case "formNoValidate":
                case "hidden":
                case "loop":
                case "noModule":
                case "noValidate":
                case "open":
                case "playsInline":
                case "readOnly":
                case "required":
                case "reversed":
                case "scoped":
                case "seamless":
                case "itemScope":
                  Mp(
                    e,
                    z,
                    z.toLowerCase(),
                    D,
                    p,
                    m
                  );
                  continue;
                case "capture":
                case "download":
                  t: {
                    C = e;
                    var tt = T = z, st = m;
                    if (p.delete(tt), C = C.getAttribute(tt), C === null)
                      switch (typeof D) {
                        case "undefined":
                        case "function":
                        case "symbol":
                          break t;
                        default:
                          if (D === !1) break t;
                      }
                    else if (D != null)
                      switch (typeof D) {
                        case "function":
                        case "symbol":
                          break;
                        case "boolean":
                          if (D === !0 && C === "") break t;
                          break;
                        default:
                          if (Q(D, T), C === "" + D)
                            break t;
                      }
                    Je(
                      T,
                      C,
                      D,
                      st
                    );
                  }
                  continue;
                case "cols":
                case "rows":
                case "size":
                case "span":
                  t: {
                    if (C = e, tt = T = z, st = m, p.delete(tt), C = C.getAttribute(tt), C === null)
                      switch (typeof D) {
                        case "undefined":
                        case "function":
                        case "symbol":
                        case "boolean":
                          break t;
                        default:
                          if (isNaN(D) || 1 > D) break t;
                      }
                    else if (D != null)
                      switch (typeof D) {
                        case "function":
                        case "symbol":
                        case "boolean":
                          break;
                        default:
                          if (!(isNaN(D) || 1 > D) && (Q(D, T), C === "" + D))
                            break t;
                      }
                    Je(
                      T,
                      C,
                      D,
                      st
                    );
                  }
                  continue;
                case "rowSpan":
                  vv(
                    e,
                    z,
                    "rowspan",
                    D,
                    p,
                    m
                  );
                  continue;
                case "start":
                  vv(
                    e,
                    z,
                    z,
                    D,
                    p,
                    m
                  );
                  continue;
                case "xHeight":
                  _i(
                    e,
                    z,
                    "x-height",
                    D,
                    p,
                    m
                  );
                  continue;
                case "xlinkActuate":
                  _i(
                    e,
                    z,
                    "xlink:actuate",
                    D,
                    p,
                    m
                  );
                  continue;
                case "xlinkArcrole":
                  _i(
                    e,
                    z,
                    "xlink:arcrole",
                    D,
                    p,
                    m
                  );
                  continue;
                case "xlinkRole":
                  _i(
                    e,
                    z,
                    "xlink:role",
                    D,
                    p,
                    m
                  );
                  continue;
                case "xlinkShow":
                  _i(
                    e,
                    z,
                    "xlink:show",
                    D,
                    p,
                    m
                  );
                  continue;
                case "xlinkTitle":
                  _i(
                    e,
                    z,
                    "xlink:title",
                    D,
                    p,
                    m
                  );
                  continue;
                case "xlinkType":
                  _i(
                    e,
                    z,
                    "xlink:type",
                    D,
                    p,
                    m
                  );
                  continue;
                case "xmlBase":
                  _i(
                    e,
                    z,
                    "xml:base",
                    D,
                    p,
                    m
                  );
                  continue;
                case "xmlLang":
                  _i(
                    e,
                    z,
                    "xml:lang",
                    D,
                    p,
                    m
                  );
                  continue;
                case "xmlSpace":
                  _i(
                    e,
                    z,
                    "xml:space",
                    D,
                    p,
                    m
                  );
                  continue;
                case "inert":
                  D !== "" || g1[z] || (g1[z] = !0, console.error(
                    "Received an empty string for a boolean attribute `%s`. This will treat the attribute as if it were false. Either pass `false` to silence this warning, or pass `true` if you used an empty string in earlier versions of React to indicate this attribute is true.",
                    z
                  )), Mp(
                    e,
                    z,
                    z,
                    D,
                    p,
                    m
                  );
                  continue;
                default:
                  if (!(2 < z.length) || z[0] !== "o" && z[0] !== "O" || z[1] !== "n" && z[1] !== "N") {
                    C = hd(z), T = !1, f.context === iu && n !== "svg" && n !== "math" ? p.delete(C.toLowerCase()) : (tt = z.toLowerCase(), tt = Fr.hasOwnProperty(
                      tt
                    ) && Fr[tt] || null, tt !== null && tt !== z && (T = !0, p.delete(tt)), p.delete(C));
                    t: if (tt = e, st = C, C = D, Qi(st))
                      if (tt.hasAttribute(st))
                        tt = tt.getAttribute(
                          st
                        ), Q(
                          C,
                          st
                        ), C = tt === "" + C ? C : tt;
                      else {
                        switch (typeof C) {
                          case "function":
                          case "symbol":
                            break t;
                          case "boolean":
                            if (tt = st.toLowerCase().slice(0, 5), tt !== "data-" && tt !== "aria-")
                              break t;
                        }
                        C = C === void 0 ? void 0 : null;
                      }
                    else C = void 0;
                    T || Je(
                      z,
                      C,
                      D,
                      m
                    );
                  }
              }
          }
      return 0 < p.size && s.suppressHydrationWarning !== !0 && Lo(e, p, m), Object.keys(m).length === 0 ? null : m;
    }
    function ce(e, n) {
      switch (e.length) {
        case 0:
          return "";
        case 1:
          return e[0];
        case 2:
          return e[0] + " " + n + " " + e[1];
        default:
          return e.slice(0, -1).join(", ") + ", " + n + " " + e[e.length - 1];
      }
    }
    function fe(e) {
      return e.nodeType === 9 ? e : e.ownerDocument;
    }
    function Ae(e) {
      switch (e) {
        case bc:
          return xm;
        case Sh:
          return p1;
        default:
          return iu;
      }
    }
    function Gi(e, n) {
      if (e === iu)
        switch (n) {
          case "svg":
            return xm;
          case "math":
            return p1;
          default:
            return iu;
        }
      return e === xm && n === "foreignObject" ? iu : e;
    }
    function qa(e, n) {
      return e === "textarea" || e === "noscript" || typeof n.children == "string" || typeof n.children == "number" || typeof n.children == "bigint" || typeof n.dangerouslySetInnerHTML == "object" && n.dangerouslySetInnerHTML !== null && n.dangerouslySetInnerHTML.__html != null;
    }
    function ec() {
      var e = window.event;
      return e && e.type === "popstate" ? e === kE ? !1 : (kE = e, !0) : (kE = null, !1);
    }
    function Dp(e) {
      setTimeout(function() {
        throw e;
      });
    }
    function Ns(e, n, s) {
      switch (n) {
        case "button":
        case "input":
        case "select":
        case "textarea":
          s.autoFocus && e.focus();
          break;
        case "img":
          s.src ? e.src = s.src : s.srcSet && (e.srcset = s.srcSet);
      }
    }
    function fn(e, n, s, f) {
      _v(e, n, s, f), e[Bi] = f;
    }
    function Us(e) {
      sr(e, "");
    }
    function Yr(e, n, s) {
      e.nodeValue = s;
    }
    function Za(e) {
      return e === "head";
    }
    function Nl(e, n) {
      e.removeChild(n);
    }
    function nc(e, n) {
      (e.nodeType === 9 ? e.body : e.nodeName === "HTML" ? e.ownerDocument.body : e).removeChild(n);
    }
    function ic(e, n) {
      var s = n, f = 0, m = 0;
      do {
        var p = s.nextSibling;
        if (e.removeChild(s), p && p.nodeType === 8)
          if (s = p.data, s === y1) {
            if (0 < f && 8 > f) {
              s = f;
              var T = e.ownerDocument;
              if (s & UM && oc(T.documentElement), s & GM && oc(T.body), s & HM)
                for (s = T.head, oc(s), T = s.firstChild; T; ) {
                  var C = T.nextSibling, D = T.nodeName;
                  T[_c] || D === "SCRIPT" || D === "STYLE" || D === "LINK" && T.rel.toLowerCase() === "stylesheet" || s.removeChild(T), T = C;
                }
            }
            if (m === 0) {
              e.removeChild(p), Xr(n);
              return;
            }
            m--;
          } else
            s === m1 || s === nu || s === B0 ? m++ : f = s.charCodeAt(0) - 48;
        else f = 0;
        s = p;
      } while (s);
      Xr(n);
    }
    function Hi(e) {
      e = e.style, typeof e.setProperty == "function" ? e.setProperty("display", "none", "important") : e.display = "none";
    }
    function Op(e) {
      e.nodeValue = "";
    }
    function Lp(e, n) {
      n = n[YM], n = n != null && n.hasOwnProperty("display") ? n.display : null, e.style.display = n == null || typeof n == "boolean" ? "" : ("" + n).trim();
    }
    function xg(e, n) {
      e.nodeValue = n;
    }
    function lc(e) {
      var n = e.firstChild;
      for (n && n.nodeType === 10 && (n = n.nextSibling); n; ) {
        var s = n;
        switch (n = n.nextSibling, s.nodeName) {
          case "HTML":
          case "HEAD":
          case "BODY":
            lc(s), lo(s);
            continue;
          case "SCRIPT":
          case "STYLE":
            continue;
          case "LINK":
            if (s.rel.toLowerCase() === "stylesheet") continue;
        }
        e.removeChild(s);
      }
    }
    function zo(e, n, s, f) {
      for (; e.nodeType === 1; ) {
        var m = s;
        if (e.nodeName.toLowerCase() !== n.toLowerCase()) {
          if (!f && (e.nodeName !== "INPUT" || e.type !== "hidden"))
            break;
        } else if (f) {
          if (!e[_c])
            switch (n) {
              case "meta":
                if (!e.hasAttribute("itemprop")) break;
                return e;
              case "link":
                if (p = e.getAttribute("rel"), p === "stylesheet" && e.hasAttribute("data-precedence"))
                  break;
                if (p !== m.rel || e.getAttribute("href") !== (m.href == null || m.href === "" ? null : m.href) || e.getAttribute("crossorigin") !== (m.crossOrigin == null ? null : m.crossOrigin) || e.getAttribute("title") !== (m.title == null ? null : m.title))
                  break;
                return e;
              case "style":
                if (e.hasAttribute("data-precedence")) break;
                return e;
              case "script":
                if (p = e.getAttribute("src"), (p !== (m.src == null ? null : m.src) || e.getAttribute("type") !== (m.type == null ? null : m.type) || e.getAttribute("crossorigin") !== (m.crossOrigin == null ? null : m.crossOrigin)) && p && e.hasAttribute("async") && !e.hasAttribute("itemprop"))
                  break;
                return e;
              default:
                return e;
            }
        } else if (n === "input" && e.type === "hidden") {
          Q(m.name, "name");
          var p = m.name == null ? null : "" + m.name;
          if (m.type === "hidden" && e.getAttribute("name") === p)
            return e;
        } else return e;
        if (e = Kn(e.nextSibling), e === null) break;
      }
      return null;
    }
    function Fn(e, n, s) {
      if (n === "") return null;
      for (; e.nodeType !== 3; )
        if ((e.nodeType !== 1 || e.nodeName !== "INPUT" || e.type !== "hidden") && !s || (e = Kn(e.nextSibling), e === null)) return null;
      return e;
    }
    function Fa(e) {
      return e.data === B0 || e.data === nu && e.ownerDocument.readyState === xC;
    }
    function ac(e, n) {
      var s = e.ownerDocument;
      if (e.data !== nu || s.readyState === xC)
        n();
      else {
        var f = function() {
          n(), s.removeEventListener("DOMContentLoaded", f);
        };
        s.addEventListener("DOMContentLoaded", f), e._reactRetry = f;
      }
    }
    function Kn(e) {
      for (; e != null; e = e.nextSibling) {
        var n = e.nodeType;
        if (n === 1 || n === 3) break;
        if (n === 8) {
          if (n = e.data, n === m1 || n === B0 || n === nu || n === BE || n === TC)
            break;
          if (n === y1) return null;
        }
      }
      return e;
    }
    function Cg(e) {
      if (e.nodeType === 1) {
        for (var n = e.nodeName.toLowerCase(), s = {}, f = e.attributes, m = 0; m < f.length; m++) {
          var p = f[m];
          s[Rp(p.name)] = p.name.toLowerCase() === "style" ? Hr(e) : p.value;
        }
        return { type: n, props: s };
      }
      return e.nodeType === 8 ? { type: "Suspense", props: {} } : e.nodeValue;
    }
    function Rg(e, n, s) {
      return s === null || s[NM] !== !0 ? (e.nodeValue === n ? e = null : (n = Zn(n), e = Zn(e.nodeValue) === n ? null : e.nodeValue), e) : null;
    }
    function zp(e) {
      e = e.nextSibling;
      for (var n = 0; e; ) {
        if (e.nodeType === 8) {
          var s = e.data;
          if (s === y1) {
            if (n === 0)
              return Kn(e.nextSibling);
            n--;
          } else
            s !== m1 && s !== B0 && s !== nu || n++;
        }
        e = e.nextSibling;
      }
      return null;
    }
    function sc(e) {
      e = e.previousSibling;
      for (var n = 0; e; ) {
        if (e.nodeType === 8) {
          var s = e.data;
          if (s === m1 || s === B0 || s === nu) {
            if (n === 0) return e;
            n--;
          } else s === y1 && n++;
        }
        e = e.previousSibling;
      }
      return null;
    }
    function Np(e) {
      Xr(e);
    }
    function ll(e) {
      Xr(e);
    }
    function Up(e, n, s, f, m) {
      switch (m && fd(e, f.ancestorInfo), n = fe(s), e) {
        case "html":
          if (e = n.documentElement, !e)
            throw Error(
              "React expected an <html> element (document.documentElement) to exist in the Document but one was not found. React never removes the documentElement for any Document it renders into so the cause is likely in some other script running on this page."
            );
          return e;
        case "head":
          if (e = n.head, !e)
            throw Error(
              "React expected a <head> element (document.head) to exist in the Document but one was not found. React never removes the head for any Document it renders into so the cause is likely in some other script running on this page."
            );
          return e;
        case "body":
          if (e = n.body, !e)
            throw Error(
              "React expected a <body> element (document.body) to exist in the Document but one was not found. React never removes the body for any Document it renders into so the cause is likely in some other script running on this page."
            );
          return e;
        default:
          throw Error(
            "resolveSingletonInstance was called with an element type that is not supported. This is a bug in React."
          );
      }
    }
    function al(e, n, s, f) {
      if (!s[Xo] && ni(s)) {
        var m = s.tagName.toLowerCase();
        console.error(
          "You are mounting a new %s component when a previous one has not first unmounted. It is an error to render more than one %s component at a time and attributes and children of these components will likely fail in unpredictable ways. Please only render a single instance of <%s> and if you need to mount a new one, ensure any previous ones have unmounted first.",
          m,
          m,
          m
        );
      }
      switch (e) {
        case "html":
        case "head":
        case "body":
          break;
        default:
          console.error(
            "acquireSingletonInstance was called with an element type that is not supported. This is a bug in React."
          );
      }
      for (m = s.attributes; m.length; )
        s.removeAttributeNode(m[0]);
      cn(s, e, n), s[ri] = f, s[Bi] = n;
    }
    function oc(e) {
      for (var n = e.attributes; n.length; )
        e.removeAttributeNode(n[0]);
      lo(e);
    }
    function $f(e) {
      return typeof e.getRootNode == "function" ? e.getRootNode() : e.nodeType === 9 ? e : e.ownerDocument;
    }
    function Sv(e, n, s) {
      var f = Cm;
      if (f && typeof n == "string" && n) {
        var m = Ji(n);
        m = 'link[rel="' + e + '"][href="' + m + '"]', typeof s == "string" && (m += '[crossorigin="' + s + '"]'), DC.has(m) || (DC.add(m), e = { rel: e, crossOrigin: s, href: n }, f.querySelector(m) === null && (n = f.createElement("link"), cn(n, "link", e), _n(n), f.head.appendChild(n)));
      }
    }
    function Gs(e, n, s, f) {
      var m = (m = Pa.current) ? $f(m) : null;
      if (!m)
        throw Error(
          '"resourceRoot" was expected to exist. This is a bug in React.'
        );
      switch (e) {
        case "meta":
        case "title":
          return null;
        case "style":
          return typeof s.precedence == "string" && typeof s.href == "string" ? (s = No(s.href), n = Ki(m).hoistableStyles, f = n.get(s), f || (f = {
            type: "style",
            instance: null,
            count: 0,
            state: null
          }, n.set(s, f)), f) : { type: "void", instance: null, count: 0, state: null };
        case "link":
          if (s.rel === "stylesheet" && typeof s.href == "string" && typeof s.precedence == "string") {
            e = No(s.href);
            var p = Ki(m).hoistableStyles, T = p.get(e);
            if (!T && (m = m.ownerDocument || m, T = {
              type: "stylesheet",
              instance: null,
              count: 0,
              state: { loading: Yh, preload: null }
            }, p.set(e, T), (p = m.querySelector(
              Ka(e)
            )) && !p._p && (T.instance = p, T.state.loading = I0 | rs), !us.has(e))) {
              var C = {
                rel: "preload",
                as: "style",
                href: s.href,
                crossOrigin: s.crossOrigin,
                integrity: s.integrity,
                media: s.media,
                hrefLang: s.hrefLang,
                referrerPolicy: s.referrerPolicy
              };
              us.set(e, C), p || Ev(
                m,
                e,
                C,
                T.state
              );
            }
            if (n && f === null)
              throw s = `

  - ` + jr(n) + `
  + ` + jr(s), Error(
                "Expected <link> not to update to be updated to a stylesheet with precedence. Check the `rel`, `href`, and `precedence` props of this component. Alternatively, check whether two different <link> components render in the same slot or share the same key." + s
              );
            return T;
          }
          if (n && f !== null)
            throw s = `

  - ` + jr(n) + `
  + ` + jr(s), Error(
              "Expected stylesheet with precedence to not be updated to a different kind of <link>. Check the `rel`, `href`, and `precedence` props of this component. Alternatively, check whether two different <link> components render in the same slot or share the same key." + s
            );
          return null;
        case "script":
          return n = s.async, s = s.src, typeof s == "string" && n && typeof n != "function" && typeof n != "symbol" ? (s = Br(s), n = Ki(m).hoistableScripts, f = n.get(s), f || (f = {
            type: "script",
            instance: null,
            count: 0,
            state: null
          }, n.set(s, f)), f) : { type: "void", instance: null, count: 0, state: null };
        default:
          throw Error(
            'getResource encountered a type it did not expect: "' + e + '". this is a bug in React.'
          );
      }
    }
    function jr(e) {
      var n = 0, s = "<link";
      return typeof e.rel == "string" ? (n++, s += ' rel="' + e.rel + '"') : js.call(e, "rel") && (n++, s += ' rel="' + (e.rel === null ? "null" : "invalid type " + typeof e.rel) + '"'), typeof e.href == "string" ? (n++, s += ' href="' + e.href + '"') : js.call(e, "href") && (n++, s += ' href="' + (e.href === null ? "null" : "invalid type " + typeof e.href) + '"'), typeof e.precedence == "string" ? (n++, s += ' precedence="' + e.precedence + '"') : js.call(e, "precedence") && (n++, s += " precedence={" + (e.precedence === null ? "null" : "invalid type " + typeof e.precedence) + "}"), Object.getOwnPropertyNames(e).length > n && (s += " ..."), s + " />";
    }
    function No(e) {
      return 'href="' + Ji(e) + '"';
    }
    function Ka(e) {
      return 'link[rel="stylesheet"][' + e + "]";
    }
    function Gp(e) {
      return ee({}, e, {
        "data-precedence": e.precedence,
        precedence: null
      });
    }
    function Ev(e, n, s, f) {
      e.querySelector(
        'link[rel="preload"][as="style"][' + n + "]"
      ) ? f.loading = I0 : (n = e.createElement("link"), f.preload = n, n.addEventListener("load", function() {
        return f.loading |= I0;
      }), n.addEventListener("error", function() {
        return f.loading |= MC;
      }), cn(n, "link", s), _n(n), e.head.appendChild(n));
    }
    function Br(e) {
      return '[src="' + Ji(e) + '"]';
    }
    function Ir(e) {
      return "script[async]" + e;
    }
    function Ag(e, n, s) {
      if (n.count++, n.instance === null)
        switch (n.type) {
          case "style":
            var f = e.querySelector(
              'style[data-href~="' + Ji(s.href) + '"]'
            );
            if (f)
              return n.instance = f, _n(f), f;
            var m = ee({}, s, {
              "data-href": s.href,
              "data-precedence": s.precedence,
              href: null,
              precedence: null
            });
            return f = (e.ownerDocument || e).createElement("style"), _n(f), cn(f, "style", m), Mg(f, s.precedence, e), n.instance = f;
          case "stylesheet":
            m = No(s.href);
            var p = e.querySelector(
              Ka(m)
            );
            if (p)
              return n.state.loading |= rs, n.instance = p, _n(p), p;
            f = Gp(s), (m = us.get(m)) && Hp(f, m), p = (e.ownerDocument || e).createElement("link"), _n(p);
            var T = p;
            return T._p = new Promise(function(C, D) {
              T.onload = C, T.onerror = D;
            }), cn(p, "link", f), n.state.loading |= rs, Mg(p, s.precedence, e), n.instance = p;
          case "script":
            return p = Br(s.src), (m = e.querySelector(
              Ir(p)
            )) ? (n.instance = m, _n(m), m) : (f = s, (m = us.get(p)) && (f = ee({}, s), Yp(f, m)), e = e.ownerDocument || e, m = e.createElement("script"), _n(m), cn(m, "link", f), e.head.appendChild(m), n.instance = m);
          case "void":
            return null;
          default:
            throw Error(
              'acquireResource encountered a resource type it did not expect: "' + n.type + '". this is a bug in React.'
            );
        }
      else
        n.type === "stylesheet" && (n.state.loading & rs) === Yh && (f = n.instance, n.state.loading |= rs, Mg(f, s.precedence, e));
      return n.instance;
    }
    function Mg(e, n, s) {
      for (var f = s.querySelectorAll(
        'link[rel="stylesheet"][data-precedence],style[data-precedence]'
      ), m = f.length ? f[f.length - 1] : null, p = m, T = 0; T < f.length; T++) {
        var C = f[T];
        if (C.dataset.precedence === n) p = C;
        else if (p !== m) break;
      }
      p ? p.parentNode.insertBefore(e, p.nextSibling) : (n = s.nodeType === 9 ? s.head : s, n.insertBefore(e, n.firstChild));
    }
    function Hp(e, n) {
      e.crossOrigin == null && (e.crossOrigin = n.crossOrigin), e.referrerPolicy == null && (e.referrerPolicy = n.referrerPolicy), e.title == null && (e.title = n.title);
    }
    function Yp(e, n) {
      e.crossOrigin == null && (e.crossOrigin = n.crossOrigin), e.referrerPolicy == null && (e.referrerPolicy = n.referrerPolicy), e.integrity == null && (e.integrity = n.integrity);
    }
    function jp(e, n, s) {
      if (_1 === null) {
        var f = /* @__PURE__ */ new Map(), m = _1 = /* @__PURE__ */ new Map();
        m.set(s, f);
      } else
        m = _1, f = m.get(s), f || (f = /* @__PURE__ */ new Map(), m.set(s, f));
      if (f.has(e)) return f;
      for (f.set(e, null), s = s.getElementsByTagName(e), m = 0; m < s.length; m++) {
        var p = s[m];
        if (!(p[_c] || p[ri] || e === "link" && p.getAttribute("rel") === "stylesheet") && p.namespaceURI !== bc) {
          var T = p.getAttribute(n) || "";
          T = e + T;
          var C = f.get(T);
          C ? C.push(p) : f.set(T, [p]);
        }
      }
      return f;
    }
    function Bp(e, n, s) {
      e = e.ownerDocument || e, e.head.insertBefore(
        s,
        n === "title" ? e.querySelector("head > title") : null
      );
    }
    function rc(e, n, s) {
      var f = !s.ancestorInfo.containerTagInScope;
      if (s.context === xm || n.itemProp != null)
        return !f || n.itemProp == null || e !== "meta" && e !== "title" && e !== "style" && e !== "link" && e !== "script" || console.error(
          "Cannot render a <%s> outside the main document if it has an `itemProp` prop. `itemProp` suggests the tag belongs to an `itemScope` which can appear anywhere in the DOM. If you were intending for React to hoist this <%s> remove the `itemProp` prop. Otherwise, try moving this tag into the <head> or <body> of the Document.",
          e,
          e
        ), !1;
      switch (e) {
        case "meta":
        case "title":
          return !0;
        case "style":
          if (typeof n.precedence != "string" || typeof n.href != "string" || n.href === "") {
            f && console.error(
              'Cannot render a <style> outside the main document without knowing its precedence and a unique href key. React can hoist and deduplicate <style> tags if you provide a `precedence` prop along with an `href` prop that does not conflict with the `href` values used in any other hoisted <style> or <link rel="stylesheet" ...> tags.  Note that hoisting <style> tags is considered an advanced feature that most will not use directly. Consider moving the <style> tag to the <head> or consider adding a `precedence="default"` and `href="some unique resource identifier"`.'
            );
            break;
          }
          return !0;
        case "link":
          if (typeof n.rel != "string" || typeof n.href != "string" || n.href === "" || n.onLoad || n.onError) {
            if (n.rel === "stylesheet" && typeof n.precedence == "string") {
              e = n.href;
              var m = n.onError, p = n.disabled;
              s = [], n.onLoad && s.push("`onLoad`"), m && s.push("`onError`"), p != null && s.push("`disabled`"), m = ce(s, "and"), m += s.length === 1 ? " prop" : " props", p = s.length === 1 ? "an " + m : "the " + m, s.length && console.error(
                'React encountered a <link rel="stylesheet" href="%s" ... /> with a `precedence` prop that also included %s. The presence of loading and error handlers indicates an intent to manage the stylesheet loading state from your from your Component code and React will not hoist or deduplicate this stylesheet. If your intent was to have React hoist and deduplciate this stylesheet using the `precedence` prop remove the %s, otherwise remove the `precedence` prop.',
                e,
                p,
                m
              );
            }
            f && (typeof n.rel != "string" || typeof n.href != "string" || n.href === "" ? console.error(
              "Cannot render a <link> outside the main document without a `rel` and `href` prop. Try adding a `rel` and/or `href` prop to this <link> or moving the link into the <head> tag"
            ) : (n.onError || n.onLoad) && console.error(
              "Cannot render a <link> with onLoad or onError listeners outside the main document. Try removing onLoad={...} and onError={...} or moving it into the root <head> tag or somewhere in the <body>."
            ));
            break;
          }
          switch (n.rel) {
            case "stylesheet":
              return e = n.precedence, n = n.disabled, typeof e != "string" && f && console.error(
                'Cannot render a <link rel="stylesheet" /> outside the main document without knowing its precedence. Consider adding precedence="default" or moving it into the root <head> tag.'
              ), typeof e == "string" && n == null;
            default:
              return !0;
          }
        case "script":
          if (e = n.async && typeof n.async != "function" && typeof n.async != "symbol", !e || n.onLoad || n.onError || !n.src || typeof n.src != "string") {
            f && (e ? n.onLoad || n.onError ? console.error(
              "Cannot render a <script> with onLoad or onError listeners outside the main document. Try removing onLoad={...} and onError={...} or moving it into the root <head> tag or somewhere in the <body>."
            ) : console.error(
              "Cannot render a <script> outside the main document without `async={true}` and a non-empty `src` prop. Ensure there is a valid `src` and either make the script async or move it into the root <head> tag or somewhere in the <body>."
            ) : console.error(
              'Cannot render a sync or defer <script> outside the main document without knowing its order. Try adding async="" or moving it into the root <head> tag.'
            ));
            break;
          }
          return !0;
        case "noscript":
        case "template":
          f && console.error(
            "Cannot render <%s> outside the main document. Try moving it into the root <head> tag.",
            e
          );
      }
      return !1;
    }
    function th(e) {
      return !(e.type === "stylesheet" && (e.state.loading & wC) === Yh);
    }
    function bv() {
    }
    function Tv(e, n, s) {
      if (X0 === null)
        throw Error(
          "Internal React Error: suspendedState null when it was expected to exists. Please report this as a React bug."
        );
      var f = X0;
      if (n.type === "stylesheet" && (typeof s.media != "string" || matchMedia(s.media).matches !== !1) && (n.state.loading & rs) === Yh) {
        if (n.instance === null) {
          var m = No(s.href), p = e.querySelector(
            Ka(m)
          );
          if (p) {
            e = p._p, e !== null && typeof e == "object" && typeof e.then == "function" && (f.count++, f = eh.bind(f), e.then(f, f)), n.state.loading |= rs, n.instance = p, _n(p);
            return;
          }
          p = e.ownerDocument || e, s = Gp(s), (m = us.get(m)) && Hp(s, m), p = p.createElement("link"), _n(p);
          var T = p;
          T._p = new Promise(function(C, D) {
            T.onload = C, T.onerror = D;
          }), cn(p, "link", s), n.instance = p;
        }
        f.stylesheets === null && (f.stylesheets = /* @__PURE__ */ new Map()), f.stylesheets.set(n, e), (e = n.state.preload) && (n.state.loading & wC) === Yh && (f.count++, n = eh.bind(f), e.addEventListener("load", n), e.addEventListener("error", n));
      }
    }
    function xv() {
      if (X0 === null)
        throw Error(
          "Internal React Error: suspendedState null when it was expected to exists. Please report this as a React bug."
        );
      var e = X0;
      return e.stylesheets && e.count === 0 && wg(e, e.stylesheets), 0 < e.count ? function(n) {
        var s = setTimeout(function() {
          if (e.stylesheets && wg(e, e.stylesheets), e.unsuspend) {
            var f = e.unsuspend;
            e.unsuspend = null, f();
          }
        }, 6e4);
        return e.unsuspend = n, function() {
          e.unsuspend = null, clearTimeout(s);
        };
      } : null;
    }
    function eh() {
      if (this.count--, this.count === 0) {
        if (this.stylesheets)
          wg(this, this.stylesheets);
        else if (this.unsuspend) {
          var e = this.unsuspend;
          this.unsuspend = null, e();
        }
      }
    }
    function wg(e, n) {
      e.stylesheets = null, e.unsuspend !== null && (e.count++, v1 = /* @__PURE__ */ new Map(), n.forEach(Cv, e), v1 = null, eh.call(e));
    }
    function Cv(e, n) {
      if (!(n.state.loading & rs)) {
        var s = v1.get(e);
        if (s) var f = s.get(qE);
        else {
          s = /* @__PURE__ */ new Map(), v1.set(e, s);
          for (var m = e.querySelectorAll(
            "link[data-precedence],style[data-precedence]"
          ), p = 0; p < m.length; p++) {
            var T = m[p];
            (T.nodeName === "LINK" || T.getAttribute("media") !== "not all") && (s.set(T.dataset.precedence, T), f = T);
          }
          f && s.set(qE, f);
        }
        m = n.instance, T = m.getAttribute("data-precedence"), p = s.get(T) || f, p === f && s.set(qE, m), s.set(T, m), this.count++, f = eh.bind(this), m.addEventListener("load", f), m.addEventListener("error", f), p ? p.parentNode.insertBefore(m, p.nextSibling) : (e = e.nodeType === 9 ? e.head : e, e.insertBefore(m, e.firstChild)), n.state.loading |= rs;
      }
    }
    function Dg(e, n, s, f, m, p, T, C) {
      for (this.tag = 1, this.containerInfo = e, this.pingCache = this.current = this.pendingChildren = null, this.timeoutHandle = Hh, this.callbackNode = this.next = this.pendingContext = this.context = this.cancelPendingCommit = null, this.callbackPriority = 0, this.expirationTimes = Sa(-1), this.entangledLanes = this.shellSuspendCounter = this.errorRecoveryDisabledLanes = this.expiredLanes = this.warmLanes = this.pingedLanes = this.suspendedLanes = this.pendingLanes = 0, this.entanglements = Sa(0), this.hiddenUpdates = Sa(null), this.identifierPrefix = f, this.onUncaughtError = m, this.onCaughtError = p, this.onRecoverableError = T, this.pooledCache = null, this.pooledCacheLanes = 0, this.formState = C, this.incompleteTransitions = /* @__PURE__ */ new Map(), this.passiveEffectDuration = this.effectDuration = -0, this.memoizedUpdaters = /* @__PURE__ */ new Set(), e = this.pendingUpdatersLaneMap = [], n = 0; 31 > n; n++) e.push(/* @__PURE__ */ new Set());
      this._debugRootType = s ? "hydrateRoot()" : "createRoot()";
    }
    function Ip(e, n, s, f, m, p, T, C, D, z, tt, st) {
      return e = new Dg(
        e,
        n,
        s,
        T,
        C,
        D,
        z,
        st
      ), n = cM, p === !0 && (n |= Xi | Vs), dn && (n |= Si), p = R(3, null, null, n), e.current = p, p.stateNode = e, n = vf(), yr(n), e.pooledCache = n, yr(n), p.memoizedState = {
        element: f,
        isDehydrated: s,
        cache: n
      }, wi(p), e;
    }
    function Xp(e) {
      return e ? (e = Tc, e) : Tc;
    }
    function Le(e, n, s, f, m, p) {
      if (Pn && typeof Pn.onScheduleFiberRoot == "function")
        try {
          Pn.onScheduleFiberRoot(Bo, f, s);
        } catch (T) {
          ji || (ji = !0, console.error(
            "React instrumentation encountered an error: %s",
            T
          ));
        }
      Ct !== null && typeof Ct.markRenderScheduled == "function" && Ct.markRenderScheduled(n), m = Xp(m), f.context === null ? f.context = m : f.pendingContext = m, Ii && ol !== null && !NC && (NC = !0, console.error(
        `Render methods should be a pure function of props and state; triggering nested component updates from render is not allowed. If necessary, trigger nested updates in componentDidUpdate.

Check the render method of %s.`,
        it(ol) || "Unknown"
      )), f = Aa(n), f.payload = { element: s }, p = p === void 0 ? null : p, p !== null && (typeof p != "function" && console.error(
        "Expected the last optional `callback` argument to be a function. Instead received: %s.",
        p
      ), f.callback = p), s = ia(e, f, n), s !== null && (rn(s, e, n), vo(s, e, n));
    }
    function Og(e, n) {
      if (e = e.memoizedState, e !== null && e.dehydrated !== null) {
        var s = e.retryLane;
        e.retryLane = s !== 0 && s < n ? s : n;
      }
    }
    function kp(e, n) {
      Og(e, n), (e = e.alternate) && Og(e, n);
    }
    function Vp(e) {
      if (e.tag === 13) {
        var n = Mi(e, 67108864);
        n !== null && rn(n, e, 67108864), kp(e, 67108864);
      }
    }
    function jS() {
      return ol;
    }
    function BS() {
      for (var e = /* @__PURE__ */ new Map(), n = 1, s = 0; 31 > s; s++) {
        var f = xi(n);
        e.set(n, f), n *= 2;
      }
      return e;
    }
    function IS(e, n, s, f) {
      var m = lt.T;
      lt.T = null;
      var p = Vt.p;
      try {
        Vt.p = Qn, Uo(e, n, s, f);
      } finally {
        Vt.p = p, lt.T = m;
      }
    }
    function Lg(e, n, s, f) {
      var m = lt.T;
      lt.T = null;
      var p = Vt.p;
      try {
        Vt.p = ha, Uo(e, n, s, f);
      } finally {
        Vt.p = p, lt.T = m;
      }
    }
    function Uo(e, n, s, f) {
      if (E1) {
        var m = nh(f);
        if (m === null)
          yi(
            e,
            n,
            f,
            b1,
            s
          ), Go(e, f);
        else if (ih(
          m,
          e,
          n,
          s,
          f
        ))
          f.stopPropagation();
        else if (Go(e, f), n & 4 && -1 < XM.indexOf(e)) {
          for (; m !== null; ) {
            var p = ni(m);
            if (p !== null)
              switch (p.tag) {
                case 3:
                  if (p = p.stateNode, p.current.memoizedState.isDehydrated) {
                    var T = Dn(p.pendingLanes);
                    if (T !== 0) {
                      var C = p;
                      for (C.pendingLanes |= 2, C.entangledLanes |= 2; T; ) {
                        var D = 1 << 31 - oi(T);
                        C.entanglements[1] |= D, T &= ~D;
                      }
                      Ll(p), (ze & (fl | Ks)) === da && (a1 = Qa() + hC, Ur(0));
                    }
                  }
                  break;
                case 13:
                  C = Mi(p, 2), C !== null && rn(C, p, 2), zr(), kp(p, 2);
              }
            if (p = nh(f), p === null && yi(
              e,
              n,
              f,
              b1,
              s
            ), p === m) break;
            m = p;
          }
          m !== null && f.stopPropagation();
        } else
          yi(
            e,
            n,
            f,
            null,
            s
          );
      }
    }
    function nh(e) {
      return e = rr(e), uc(e);
    }
    function uc(e) {
      if (b1 = null, e = vl(e), e !== null) {
        var n = O(e);
        if (n === null) e = null;
        else {
          var s = n.tag;
          if (s === 13) {
            if (e = U(n), e !== null) return e;
            e = null;
          } else if (s === 3) {
            if (n.stateNode.current.memoizedState.isDehydrated)
              return n.tag === 3 ? n.stateNode.containerInfo : null;
            e = null;
          } else n !== e && (e = null);
        }
      }
      return b1 = e, null;
    }
    function zg(e) {
      switch (e) {
        case "beforetoggle":
        case "cancel":
        case "click":
        case "close":
        case "contextmenu":
        case "copy":
        case "cut":
        case "auxclick":
        case "dblclick":
        case "dragend":
        case "dragstart":
        case "drop":
        case "focusin":
        case "focusout":
        case "input":
        case "invalid":
        case "keydown":
        case "keypress":
        case "keyup":
        case "mousedown":
        case "mouseup":
        case "paste":
        case "pause":
        case "play":
        case "pointercancel":
        case "pointerdown":
        case "pointerup":
        case "ratechange":
        case "reset":
        case "resize":
        case "seeked":
        case "submit":
        case "toggle":
        case "touchcancel":
        case "touchend":
        case "touchstart":
        case "volumechange":
        case "change":
        case "selectionchange":
        case "textInput":
        case "compositionstart":
        case "compositionend":
        case "compositionupdate":
        case "beforeblur":
        case "afterblur":
        case "beforeinput":
        case "blur":
        case "fullscreenchange":
        case "focus":
        case "hashchange":
        case "popstate":
        case "select":
        case "selectstart":
          return Qn;
        case "drag":
        case "dragenter":
        case "dragexit":
        case "dragleave":
        case "dragover":
        case "mousemove":
        case "mouseout":
        case "mouseover":
        case "pointermove":
        case "pointerout":
        case "pointerover":
        case "scroll":
        case "touchmove":
        case "wheel":
        case "mouseenter":
        case "mouseleave":
        case "pointerenter":
        case "pointerleave":
          return ha;
        case "message":
          switch (jo()) {
            case Yg:
              return Qn;
            case hh:
              return ha;
            case mc:
            case ZS:
              return Is;
            case dh:
              return Xg;
            default:
              return Is;
          }
        default:
          return Is;
      }
    }
    function Go(e, n) {
      switch (e) {
        case "focusin":
        case "focusout":
          zc = null;
          break;
        case "dragenter":
        case "dragleave":
          Nc = null;
          break;
        case "mouseover":
        case "mouseout":
          Uc = null;
          break;
        case "pointerover":
        case "pointerout":
          V0.delete(n.pointerId);
          break;
        case "gotpointercapture":
        case "lostpointercapture":
          q0.delete(n.pointerId);
      }
    }
    function Yi(e, n, s, f, m, p) {
      return e === null || e.nativeEvent !== p ? (e = {
        blockedOn: n,
        domEventName: s,
        eventSystemFlags: f,
        nativeEvent: p,
        targetContainers: [m]
      }, n !== null && (n = ni(n), n !== null && Vp(n)), e) : (e.eventSystemFlags |= f, n = e.targetContainers, m !== null && n.indexOf(m) === -1 && n.push(m), e);
    }
    function ih(e, n, s, f, m) {
      switch (n) {
        case "focusin":
          return zc = Yi(
            zc,
            e,
            n,
            s,
            f,
            m
          ), !0;
        case "dragenter":
          return Nc = Yi(
            Nc,
            e,
            n,
            s,
            f,
            m
          ), !0;
        case "mouseover":
          return Uc = Yi(
            Uc,
            e,
            n,
            s,
            f,
            m
          ), !0;
        case "pointerover":
          var p = m.pointerId;
          return V0.set(
            p,
            Yi(
              V0.get(p) || null,
              e,
              n,
              s,
              f,
              m
            )
          ), !0;
        case "gotpointercapture":
          return p = m.pointerId, q0.set(
            p,
            Yi(
              q0.get(p) || null,
              e,
              n,
              s,
              f,
              m
            )
          ), !0;
      }
      return !1;
    }
    function Rv(e) {
      var n = vl(e.target);
      if (n !== null) {
        var s = O(n);
        if (s !== null) {
          if (n = s.tag, n === 13) {
            if (n = U(s), n !== null) {
              e.blockedOn = n, iy(e.priority, function() {
                if (s.tag === 13) {
                  var f = Ui(s);
                  f = pn(f);
                  var m = Mi(
                    s,
                    f
                  );
                  m !== null && rn(m, s, f), kp(s, f);
                }
              });
              return;
            }
          } else if (n === 3 && s.stateNode.current.memoizedState.isDehydrated) {
            e.blockedOn = s.tag === 3 ? s.stateNode.containerInfo : null;
            return;
          }
        }
      }
      e.blockedOn = null;
    }
    function lh(e) {
      if (e.blockedOn !== null) return !1;
      for (var n = e.targetContainers; 0 < n.length; ) {
        var s = nh(e.nativeEvent);
        if (s === null) {
          s = e.nativeEvent;
          var f = new s.constructor(
            s.type,
            s
          ), m = f;
          v !== null && console.error(
            "Expected currently replaying event to be null. This error is likely caused by a bug in React. Please file an issue."
          ), v = m, s.target.dispatchEvent(f), v === null && console.error(
            "Expected currently replaying event to not be null. This error is likely caused by a bug in React. Please file an issue."
          ), v = null;
        } else
          return n = ni(s), n !== null && Vp(n), e.blockedOn = s, !1;
        n.shift();
      }
      return !0;
    }
    function qp(e, n, s) {
      lh(e) && s.delete(n);
    }
    function Av() {
      ZE = !1, zc !== null && lh(zc) && (zc = null), Nc !== null && lh(Nc) && (Nc = null), Uc !== null && lh(Uc) && (Uc = null), V0.forEach(qp), q0.forEach(qp);
    }
    function ah(e, n) {
      e.blockedOn === n && (e.blockedOn = null, ZE || (ZE = !0, hn.unstable_scheduleCallback(
        hn.unstable_NormalPriority,
        Av
      )));
    }
    function Mv(e) {
      T1 !== e && (T1 = e, hn.unstable_scheduleCallback(
        hn.unstable_NormalPriority,
        function() {
          T1 === e && (T1 = null);
          for (var n = 0; n < e.length; n += 3) {
            var s = e[n], f = e[n + 1], m = e[n + 2];
            if (typeof f != "function") {
              if (uc(f || s) === null)
                continue;
              break;
            }
            var p = ni(s);
            p !== null && (e.splice(n, 3), n -= 3, s = {
              pending: !0,
              data: m,
              method: s.method,
              action: f
            }, Object.freeze(s), br(
              p,
              s,
              f,
              m
            ));
          }
        }
      ));
    }
    function Xr(e) {
      function n(D) {
        return ah(D, e);
      }
      zc !== null && ah(zc, e), Nc !== null && ah(Nc, e), Uc !== null && ah(Uc, e), V0.forEach(n), q0.forEach(n);
      for (var s = 0; s < Gc.length; s++) {
        var f = Gc[s];
        f.blockedOn === e && (f.blockedOn = null);
      }
      for (; 0 < Gc.length && (s = Gc[0], s.blockedOn === null); )
        Rv(s), s.blockedOn === null && Gc.shift();
      if (s = (e.ownerDocument || e).$$reactFormReplay, s != null)
        for (f = 0; f < s.length; f += 3) {
          var m = s[f], p = s[f + 1], T = m[Bi] || null;
          if (typeof p == "function")
            T || Mv(s);
          else if (T) {
            var C = null;
            if (p && p.hasAttribute("formAction")) {
              if (m = p, T = p[Bi] || null)
                C = T.formAction;
              else if (uc(m) !== null) continue;
            } else C = T.action;
            typeof C == "function" ? s[f + 1] = C : (s.splice(f, 3), f -= 3), Mv(s);
          }
        }
    }
    function Ng(e) {
      this._internalRoot = e;
    }
    function sh(e) {
      this._internalRoot = e;
    }
    function wv(e) {
      e[Xo] && (e._reactRootContainer ? console.error(
        "You are calling ReactDOMClient.createRoot() on a container that was previously passed to ReactDOM.render(). This is not supported."
      ) : console.error(
        "You are calling ReactDOMClient.createRoot() on a container that has already been passed to createRoot() before. Instead, call root.render() on the existing root instead if you want to update it."
      ));
    }
    typeof __REACT_DEVTOOLS_GLOBAL_HOOK__ < "u" && typeof __REACT_DEVTOOLS_GLOBAL_HOOK__.registerInternalModuleStart == "function" && __REACT_DEVTOOLS_GLOBAL_HOOK__.registerInternalModuleStart(Error());
    var hn = c2(), oh = ql, XS = u2, ee = Object.assign, rh = Symbol.for("react.element"), Ho = Symbol.for("react.transitional.element"), kr = Symbol.for("react.portal"), Qt = Symbol.for("react.fragment"), cc = Symbol.for("react.strict_mode"), fc = Symbol.for("react.profiler"), Zp = Symbol.for("react.provider"), Ug = Symbol.for("react.consumer"), Ul = Symbol.for("react.context"), Hs = Symbol.for("react.forward_ref"), hc = Symbol.for("react.suspense"), Yo = Symbol.for("react.suspense_list"), uh = Symbol.for("react.memo"), sl = Symbol.for("react.lazy"), Fp = Symbol.for("react.activity"), Dv = Symbol.for("react.memo_cache_sentinel"), Kp = Symbol.iterator, Gg = Symbol.for("react.client.reference"), Ft = Array.isArray, lt = oh.__CLIENT_INTERNALS_DO_NOT_USE_OR_WARN_USERS_THEY_CANNOT_UPGRADE, Vt = XS.__DOM_INTERNALS_DO_NOT_USE_OR_WARN_USERS_THEY_CANNOT_UPGRADE, kS = Object.freeze({
      pending: !1,
      data: null,
      method: null,
      action: null
    }), ch = [], fh = [], Gl = -1, Ys = Et(null), dc = Et(null), Pa = Et(null), gc = Et(null), js = Object.prototype.hasOwnProperty, Hg = hn.unstable_scheduleCallback, VS = hn.unstable_cancelCallback, Ov = hn.unstable_shouldYield, qS = hn.unstable_requestPaint, Qa = hn.unstable_now, jo = hn.unstable_getCurrentPriorityLevel, Yg = hn.unstable_ImmediatePriority, hh = hn.unstable_UserBlockingPriority, mc = hn.unstable_NormalPriority, ZS = hn.unstable_LowPriority, dh = hn.unstable_IdlePriority, FS = hn.log, fa = hn.unstable_setDisableYieldValue, Bo = null, Pn = null, Ct = null, ji = !1, dn = typeof __REACT_DEVTOOLS_GLOBAL_HOOK__ < "u", oi = Math.clz32 ? Math.clz32 : Kl, jg = Math.log, Bs = Math.LN2, Bg = 256, Ig = 4194304, Qn = 2, ha = 8, Is = 32, Xg = 268435456, Io = Math.random().toString(36).slice(2), ri = "__reactFiber$" + Io, Bi = "__reactProps$" + Io, Xo = "__reactContainer$" + Io, Pp = "__reactEvents$" + Io, Lv = "__reactListeners$" + Io, yc = "__reactHandles$" + Io, pc = "__reactResources$" + Io, _c = "__reactMarker$" + Io, zv = /* @__PURE__ */ new Set(), Hl = {}, Vr = {}, Nv = {
      button: !0,
      checkbox: !0,
      image: !0,
      hidden: !0,
      radio: !0,
      reset: !0,
      submit: !0
    }, kg = RegExp(
      "^[:A-Z_a-z\\u00C0-\\u00D6\\u00D8-\\u00F6\\u00F8-\\u02FF\\u0370-\\u037D\\u037F-\\u1FFF\\u200C-\\u200D\\u2070-\\u218F\\u2C00-\\u2FEF\\u3001-\\uD7FF\\uF900-\\uFDCF\\uFDF0-\\uFFFD][:A-Z_a-z\\u00C0-\\u00D6\\u00D8-\\u00F6\\u00F8-\\u02FF\\u0370-\\u037D\\u037F-\\u1FFF\\u200C-\\u200D\\u2070-\\u218F\\u2C00-\\u2FEF\\u3001-\\uD7FF\\uF900-\\uFDCF\\uFDF0-\\uFFFD\\-.0-9\\u00B7\\u0300-\\u036F\\u203F-\\u2040]*$"
    ), Vg = {}, qg = {}, ko = 0, Qp, Wp, Uv, Jp, vc, Gv, Hv;
    In.__reactDisabledLog = !0;
    var $p, gh, Sc = !1, mh = new (typeof WeakMap == "function" ? WeakMap : Map)(), ol = null, Ii = !1, KS = /[\n"\\]/g, t0 = !1, e0 = !1, n0 = !1, i0 = !1, Zg = !1, l0 = !1, yh = ["value", "defaultValue"], Yv = !1, jv = /["'&<>\n\t]|^\s|\s$/, a0 = "address applet area article aside base basefont bgsound blockquote body br button caption center col colgroup dd details dir div dl dt embed fieldset figcaption figure footer form frame frameset h1 h2 h3 h4 h5 h6 head header hgroup hr html iframe img input isindex li link listing main marquee menu menuitem meta nav noembed noframes noscript object ol p param plaintext pre script section select source style summary table tbody td template textarea tfoot th thead title tr track ul wbr xmp".split(
      " "
    ), Fg = "applet caption html table td th marquee object template foreignObject desc title".split(
      " "
    ), Kg = Fg.concat(["button"]), s0 = "dd dt li option optgroup p rp rt".split(" "), o0 = {
      current: null,
      formTag: null,
      aTagInScope: null,
      buttonTagInScope: null,
      nobrTagInScope: null,
      pTagInButtonScope: null,
      listItemTagAutoclosing: null,
      dlItemTagAutoclosing: null,
      containerTagInScope: null,
      implicitRootScope: !1
    }, Ec = {}, Wa = {
      animation: "animationDelay animationDirection animationDuration animationFillMode animationIterationCount animationName animationPlayState animationTimingFunction".split(
        " "
      ),
      background: "backgroundAttachment backgroundClip backgroundColor backgroundImage backgroundOrigin backgroundPositionX backgroundPositionY backgroundRepeat backgroundSize".split(
        " "
      ),
      backgroundPosition: ["backgroundPositionX", "backgroundPositionY"],
      border: "borderBottomColor borderBottomStyle borderBottomWidth borderImageOutset borderImageRepeat borderImageSlice borderImageSource borderImageWidth borderLeftColor borderLeftStyle borderLeftWidth borderRightColor borderRightStyle borderRightWidth borderTopColor borderTopStyle borderTopWidth".split(
        " "
      ),
      borderBlockEnd: [
        "borderBlockEndColor",
        "borderBlockEndStyle",
        "borderBlockEndWidth"
      ],
      borderBlockStart: [
        "borderBlockStartColor",
        "borderBlockStartStyle",
        "borderBlockStartWidth"
      ],
      borderBottom: [
        "borderBottomColor",
        "borderBottomStyle",
        "borderBottomWidth"
      ],
      borderColor: [
        "borderBottomColor",
        "borderLeftColor",
        "borderRightColor",
        "borderTopColor"
      ],
      borderImage: [
        "borderImageOutset",
        "borderImageRepeat",
        "borderImageSlice",
        "borderImageSource",
        "borderImageWidth"
      ],
      borderInlineEnd: [
        "borderInlineEndColor",
        "borderInlineEndStyle",
        "borderInlineEndWidth"
      ],
      borderInlineStart: [
        "borderInlineStartColor",
        "borderInlineStartStyle",
        "borderInlineStartWidth"
      ],
      borderLeft: ["borderLeftColor", "borderLeftStyle", "borderLeftWidth"],
      borderRadius: [
        "borderBottomLeftRadius",
        "borderBottomRightRadius",
        "borderTopLeftRadius",
        "borderTopRightRadius"
      ],
      borderRight: [
        "borderRightColor",
        "borderRightStyle",
        "borderRightWidth"
      ],
      borderStyle: [
        "borderBottomStyle",
        "borderLeftStyle",
        "borderRightStyle",
        "borderTopStyle"
      ],
      borderTop: ["borderTopColor", "borderTopStyle", "borderTopWidth"],
      borderWidth: [
        "borderBottomWidth",
        "borderLeftWidth",
        "borderRightWidth",
        "borderTopWidth"
      ],
      columnRule: ["columnRuleColor", "columnRuleStyle", "columnRuleWidth"],
      columns: ["columnCount", "columnWidth"],
      flex: ["flexBasis", "flexGrow", "flexShrink"],
      flexFlow: ["flexDirection", "flexWrap"],
      font: "fontFamily fontFeatureSettings fontKerning fontLanguageOverride fontSize fontSizeAdjust fontStretch fontStyle fontVariant fontVariantAlternates fontVariantCaps fontVariantEastAsian fontVariantLigatures fontVariantNumeric fontVariantPosition fontWeight lineHeight".split(
        " "
      ),
      fontVariant: "fontVariantAlternates fontVariantCaps fontVariantEastAsian fontVariantLigatures fontVariantNumeric fontVariantPosition".split(
        " "
      ),
      gap: ["columnGap", "rowGap"],
      grid: "gridAutoColumns gridAutoFlow gridAutoRows gridTemplateAreas gridTemplateColumns gridTemplateRows".split(
        " "
      ),
      gridArea: [
        "gridColumnEnd",
        "gridColumnStart",
        "gridRowEnd",
        "gridRowStart"
      ],
      gridColumn: ["gridColumnEnd", "gridColumnStart"],
      gridColumnGap: ["columnGap"],
      gridGap: ["columnGap", "rowGap"],
      gridRow: ["gridRowEnd", "gridRowStart"],
      gridRowGap: ["rowGap"],
      gridTemplate: [
        "gridTemplateAreas",
        "gridTemplateColumns",
        "gridTemplateRows"
      ],
      listStyle: ["listStyleImage", "listStylePosition", "listStyleType"],
      margin: ["marginBottom", "marginLeft", "marginRight", "marginTop"],
      marker: ["markerEnd", "markerMid", "markerStart"],
      mask: "maskClip maskComposite maskImage maskMode maskOrigin maskPositionX maskPositionY maskRepeat maskSize".split(
        " "
      ),
      maskPosition: ["maskPositionX", "maskPositionY"],
      outline: ["outlineColor", "outlineStyle", "outlineWidth"],
      overflow: ["overflowX", "overflowY"],
      padding: ["paddingBottom", "paddingLeft", "paddingRight", "paddingTop"],
      placeContent: ["alignContent", "justifyContent"],
      placeItems: ["alignItems", "justifyItems"],
      placeSelf: ["alignSelf", "justifySelf"],
      textDecoration: [
        "textDecorationColor",
        "textDecorationLine",
        "textDecorationStyle"
      ],
      textEmphasis: ["textEmphasisColor", "textEmphasisStyle"],
      transition: [
        "transitionDelay",
        "transitionDuration",
        "transitionProperty",
        "transitionTimingFunction"
      ],
      wordWrap: ["overflowWrap"]
    }, Xs = /([A-Z])/g, ks = /^ms-/, ph = /^(?:webkit|moz|o)[A-Z]/, _h = /^-ms-/, Vo = /-(.)/g, Bv = /;\s*$/, qr = {}, Zr = {}, Iv = !1, r0 = !1, vh = new Set(
      "animationIterationCount aspectRatio borderImageOutset borderImageSlice borderImageWidth boxFlex boxFlexGroup boxOrdinalGroup columnCount columns flex flexGrow flexPositive flexShrink flexNegative flexOrder gridArea gridRow gridRowEnd gridRowSpan gridRowStart gridColumn gridColumnEnd gridColumnSpan gridColumnStart fontWeight lineClamp lineHeight opacity order orphans scale tabSize widows zIndex zoom fillOpacity floodOpacity stopOpacity strokeDasharray strokeDashoffset strokeMiterlimit strokeOpacity strokeWidth MozAnimationIterationCount MozBoxFlex MozBoxFlexGroup MozLineClamp msAnimationIterationCount msFlex msZoom msFlexGrow msFlexNegative msFlexOrder msFlexPositive msFlexShrink msGridColumn msGridColumnSpan msGridRow msGridRowSpan WebkitAnimationIterationCount WebkitBoxFlex WebKitBoxFlexGroup WebkitBoxOrdinalGroup WebkitColumnCount WebkitColumns WebkitFlex WebkitFlexGrow WebkitFlexPositive WebkitFlexShrink WebkitLineClamp".split(
        " "
      )
    ), Sh = "http://www.w3.org/1998/Math/MathML", bc = "http://www.w3.org/2000/svg", Pg = /* @__PURE__ */ new Map([
      ["acceptCharset", "accept-charset"],
      ["htmlFor", "for"],
      ["httpEquiv", "http-equiv"],
      ["crossOrigin", "crossorigin"],
      ["accentHeight", "accent-height"],
      ["alignmentBaseline", "alignment-baseline"],
      ["arabicForm", "arabic-form"],
      ["baselineShift", "baseline-shift"],
      ["capHeight", "cap-height"],
      ["clipPath", "clip-path"],
      ["clipRule", "clip-rule"],
      ["colorInterpolation", "color-interpolation"],
      ["colorInterpolationFilters", "color-interpolation-filters"],
      ["colorProfile", "color-profile"],
      ["colorRendering", "color-rendering"],
      ["dominantBaseline", "dominant-baseline"],
      ["enableBackground", "enable-background"],
      ["fillOpacity", "fill-opacity"],
      ["fillRule", "fill-rule"],
      ["floodColor", "flood-color"],
      ["floodOpacity", "flood-opacity"],
      ["fontFamily", "font-family"],
      ["fontSize", "font-size"],
      ["fontSizeAdjust", "font-size-adjust"],
      ["fontStretch", "font-stretch"],
      ["fontStyle", "font-style"],
      ["fontVariant", "font-variant"],
      ["fontWeight", "font-weight"],
      ["glyphName", "glyph-name"],
      ["glyphOrientationHorizontal", "glyph-orientation-horizontal"],
      ["glyphOrientationVertical", "glyph-orientation-vertical"],
      ["horizAdvX", "horiz-adv-x"],
      ["horizOriginX", "horiz-origin-x"],
      ["imageRendering", "image-rendering"],
      ["letterSpacing", "letter-spacing"],
      ["lightingColor", "lighting-color"],
      ["markerEnd", "marker-end"],
      ["markerMid", "marker-mid"],
      ["markerStart", "marker-start"],
      ["overlinePosition", "overline-position"],
      ["overlineThickness", "overline-thickness"],
      ["paintOrder", "paint-order"],
      ["panose-1", "panose-1"],
      ["pointerEvents", "pointer-events"],
      ["renderingIntent", "rendering-intent"],
      ["shapeRendering", "shape-rendering"],
      ["stopColor", "stop-color"],
      ["stopOpacity", "stop-opacity"],
      ["strikethroughPosition", "strikethrough-position"],
      ["strikethroughThickness", "strikethrough-thickness"],
      ["strokeDasharray", "stroke-dasharray"],
      ["strokeDashoffset", "stroke-dashoffset"],
      ["strokeLinecap", "stroke-linecap"],
      ["strokeLinejoin", "stroke-linejoin"],
      ["strokeMiterlimit", "stroke-miterlimit"],
      ["strokeOpacity", "stroke-opacity"],
      ["strokeWidth", "stroke-width"],
      ["textAnchor", "text-anchor"],
      ["textDecoration", "text-decoration"],
      ["textRendering", "text-rendering"],
      ["transformOrigin", "transform-origin"],
      ["underlinePosition", "underline-position"],
      ["underlineThickness", "underline-thickness"],
      ["unicodeBidi", "unicode-bidi"],
      ["unicodeRange", "unicode-range"],
      ["unitsPerEm", "units-per-em"],
      ["vAlphabetic", "v-alphabetic"],
      ["vHanging", "v-hanging"],
      ["vIdeographic", "v-ideographic"],
      ["vMathematical", "v-mathematical"],
      ["vectorEffect", "vector-effect"],
      ["vertAdvY", "vert-adv-y"],
      ["vertOriginX", "vert-origin-x"],
      ["vertOriginY", "vert-origin-y"],
      ["wordSpacing", "word-spacing"],
      ["writingMode", "writing-mode"],
      ["xmlnsXlink", "xmlns:xlink"],
      ["xHeight", "x-height"]
    ]), Fr = {
      accept: "accept",
      acceptcharset: "acceptCharset",
      "accept-charset": "acceptCharset",
      accesskey: "accessKey",
      action: "action",
      allowfullscreen: "allowFullScreen",
      alt: "alt",
      as: "as",
      async: "async",
      autocapitalize: "autoCapitalize",
      autocomplete: "autoComplete",
      autocorrect: "autoCorrect",
      autofocus: "autoFocus",
      autoplay: "autoPlay",
      autosave: "autoSave",
      capture: "capture",
      cellpadding: "cellPadding",
      cellspacing: "cellSpacing",
      challenge: "challenge",
      charset: "charSet",
      checked: "checked",
      children: "children",
      cite: "cite",
      class: "className",
      classid: "classID",
      classname: "className",
      cols: "cols",
      colspan: "colSpan",
      content: "content",
      contenteditable: "contentEditable",
      contextmenu: "contextMenu",
      controls: "controls",
      controlslist: "controlsList",
      coords: "coords",
      crossorigin: "crossOrigin",
      dangerouslysetinnerhtml: "dangerouslySetInnerHTML",
      data: "data",
      datetime: "dateTime",
      default: "default",
      defaultchecked: "defaultChecked",
      defaultvalue: "defaultValue",
      defer: "defer",
      dir: "dir",
      disabled: "disabled",
      disablepictureinpicture: "disablePictureInPicture",
      disableremoteplayback: "disableRemotePlayback",
      download: "download",
      draggable: "draggable",
      enctype: "encType",
      enterkeyhint: "enterKeyHint",
      fetchpriority: "fetchPriority",
      for: "htmlFor",
      form: "form",
      formmethod: "formMethod",
      formaction: "formAction",
      formenctype: "formEncType",
      formnovalidate: "formNoValidate",
      formtarget: "formTarget",
      frameborder: "frameBorder",
      headers: "headers",
      height: "height",
      hidden: "hidden",
      high: "high",
      href: "href",
      hreflang: "hrefLang",
      htmlfor: "htmlFor",
      httpequiv: "httpEquiv",
      "http-equiv": "httpEquiv",
      icon: "icon",
      id: "id",
      imagesizes: "imageSizes",
      imagesrcset: "imageSrcSet",
      inert: "inert",
      innerhtml: "innerHTML",
      inputmode: "inputMode",
      integrity: "integrity",
      is: "is",
      itemid: "itemID",
      itemprop: "itemProp",
      itemref: "itemRef",
      itemscope: "itemScope",
      itemtype: "itemType",
      keyparams: "keyParams",
      keytype: "keyType",
      kind: "kind",
      label: "label",
      lang: "lang",
      list: "list",
      loop: "loop",
      low: "low",
      manifest: "manifest",
      marginwidth: "marginWidth",
      marginheight: "marginHeight",
      max: "max",
      maxlength: "maxLength",
      media: "media",
      mediagroup: "mediaGroup",
      method: "method",
      min: "min",
      minlength: "minLength",
      multiple: "multiple",
      muted: "muted",
      name: "name",
      nomodule: "noModule",
      nonce: "nonce",
      novalidate: "noValidate",
      open: "open",
      optimum: "optimum",
      pattern: "pattern",
      placeholder: "placeholder",
      playsinline: "playsInline",
      poster: "poster",
      preload: "preload",
      profile: "profile",
      radiogroup: "radioGroup",
      readonly: "readOnly",
      referrerpolicy: "referrerPolicy",
      rel: "rel",
      required: "required",
      reversed: "reversed",
      role: "role",
      rows: "rows",
      rowspan: "rowSpan",
      sandbox: "sandbox",
      scope: "scope",
      scoped: "scoped",
      scrolling: "scrolling",
      seamless: "seamless",
      selected: "selected",
      shape: "shape",
      size: "size",
      sizes: "sizes",
      span: "span",
      spellcheck: "spellCheck",
      src: "src",
      srcdoc: "srcDoc",
      srclang: "srcLang",
      srcset: "srcSet",
      start: "start",
      step: "step",
      style: "style",
      summary: "summary",
      tabindex: "tabIndex",
      target: "target",
      title: "title",
      type: "type",
      usemap: "useMap",
      value: "value",
      width: "width",
      wmode: "wmode",
      wrap: "wrap",
      about: "about",
      accentheight: "accentHeight",
      "accent-height": "accentHeight",
      accumulate: "accumulate",
      additive: "additive",
      alignmentbaseline: "alignmentBaseline",
      "alignment-baseline": "alignmentBaseline",
      allowreorder: "allowReorder",
      alphabetic: "alphabetic",
      amplitude: "amplitude",
      arabicform: "arabicForm",
      "arabic-form": "arabicForm",
      ascent: "ascent",
      attributename: "attributeName",
      attributetype: "attributeType",
      autoreverse: "autoReverse",
      azimuth: "azimuth",
      basefrequency: "baseFrequency",
      baselineshift: "baselineShift",
      "baseline-shift": "baselineShift",
      baseprofile: "baseProfile",
      bbox: "bbox",
      begin: "begin",
      bias: "bias",
      by: "by",
      calcmode: "calcMode",
      capheight: "capHeight",
      "cap-height": "capHeight",
      clip: "clip",
      clippath: "clipPath",
      "clip-path": "clipPath",
      clippathunits: "clipPathUnits",
      cliprule: "clipRule",
      "clip-rule": "clipRule",
      color: "color",
      colorinterpolation: "colorInterpolation",
      "color-interpolation": "colorInterpolation",
      colorinterpolationfilters: "colorInterpolationFilters",
      "color-interpolation-filters": "colorInterpolationFilters",
      colorprofile: "colorProfile",
      "color-profile": "colorProfile",
      colorrendering: "colorRendering",
      "color-rendering": "colorRendering",
      contentscripttype: "contentScriptType",
      contentstyletype: "contentStyleType",
      cursor: "cursor",
      cx: "cx",
      cy: "cy",
      d: "d",
      datatype: "datatype",
      decelerate: "decelerate",
      descent: "descent",
      diffuseconstant: "diffuseConstant",
      direction: "direction",
      display: "display",
      divisor: "divisor",
      dominantbaseline: "dominantBaseline",
      "dominant-baseline": "dominantBaseline",
      dur: "dur",
      dx: "dx",
      dy: "dy",
      edgemode: "edgeMode",
      elevation: "elevation",
      enablebackground: "enableBackground",
      "enable-background": "enableBackground",
      end: "end",
      exponent: "exponent",
      externalresourcesrequired: "externalResourcesRequired",
      fill: "fill",
      fillopacity: "fillOpacity",
      "fill-opacity": "fillOpacity",
      fillrule: "fillRule",
      "fill-rule": "fillRule",
      filter: "filter",
      filterres: "filterRes",
      filterunits: "filterUnits",
      floodopacity: "floodOpacity",
      "flood-opacity": "floodOpacity",
      floodcolor: "floodColor",
      "flood-color": "floodColor",
      focusable: "focusable",
      fontfamily: "fontFamily",
      "font-family": "fontFamily",
      fontsize: "fontSize",
      "font-size": "fontSize",
      fontsizeadjust: "fontSizeAdjust",
      "font-size-adjust": "fontSizeAdjust",
      fontstretch: "fontStretch",
      "font-stretch": "fontStretch",
      fontstyle: "fontStyle",
      "font-style": "fontStyle",
      fontvariant: "fontVariant",
      "font-variant": "fontVariant",
      fontweight: "fontWeight",
      "font-weight": "fontWeight",
      format: "format",
      from: "from",
      fx: "fx",
      fy: "fy",
      g1: "g1",
      g2: "g2",
      glyphname: "glyphName",
      "glyph-name": "glyphName",
      glyphorientationhorizontal: "glyphOrientationHorizontal",
      "glyph-orientation-horizontal": "glyphOrientationHorizontal",
      glyphorientationvertical: "glyphOrientationVertical",
      "glyph-orientation-vertical": "glyphOrientationVertical",
      glyphref: "glyphRef",
      gradienttransform: "gradientTransform",
      gradientunits: "gradientUnits",
      hanging: "hanging",
      horizadvx: "horizAdvX",
      "horiz-adv-x": "horizAdvX",
      horizoriginx: "horizOriginX",
      "horiz-origin-x": "horizOriginX",
      ideographic: "ideographic",
      imagerendering: "imageRendering",
      "image-rendering": "imageRendering",
      in2: "in2",
      in: "in",
      inlist: "inlist",
      intercept: "intercept",
      k1: "k1",
      k2: "k2",
      k3: "k3",
      k4: "k4",
      k: "k",
      kernelmatrix: "kernelMatrix",
      kernelunitlength: "kernelUnitLength",
      kerning: "kerning",
      keypoints: "keyPoints",
      keysplines: "keySplines",
      keytimes: "keyTimes",
      lengthadjust: "lengthAdjust",
      letterspacing: "letterSpacing",
      "letter-spacing": "letterSpacing",
      lightingcolor: "lightingColor",
      "lighting-color": "lightingColor",
      limitingconeangle: "limitingConeAngle",
      local: "local",
      markerend: "markerEnd",
      "marker-end": "markerEnd",
      markerheight: "markerHeight",
      markermid: "markerMid",
      "marker-mid": "markerMid",
      markerstart: "markerStart",
      "marker-start": "markerStart",
      markerunits: "markerUnits",
      markerwidth: "markerWidth",
      mask: "mask",
      maskcontentunits: "maskContentUnits",
      maskunits: "maskUnits",
      mathematical: "mathematical",
      mode: "mode",
      numoctaves: "numOctaves",
      offset: "offset",
      opacity: "opacity",
      operator: "operator",
      order: "order",
      orient: "orient",
      orientation: "orientation",
      origin: "origin",
      overflow: "overflow",
      overlineposition: "overlinePosition",
      "overline-position": "overlinePosition",
      overlinethickness: "overlineThickness",
      "overline-thickness": "overlineThickness",
      paintorder: "paintOrder",
      "paint-order": "paintOrder",
      panose1: "panose1",
      "panose-1": "panose1",
      pathlength: "pathLength",
      patterncontentunits: "patternContentUnits",
      patterntransform: "patternTransform",
      patternunits: "patternUnits",
      pointerevents: "pointerEvents",
      "pointer-events": "pointerEvents",
      points: "points",
      pointsatx: "pointsAtX",
      pointsaty: "pointsAtY",
      pointsatz: "pointsAtZ",
      popover: "popover",
      popovertarget: "popoverTarget",
      popovertargetaction: "popoverTargetAction",
      prefix: "prefix",
      preservealpha: "preserveAlpha",
      preserveaspectratio: "preserveAspectRatio",
      primitiveunits: "primitiveUnits",
      property: "property",
      r: "r",
      radius: "radius",
      refx: "refX",
      refy: "refY",
      renderingintent: "renderingIntent",
      "rendering-intent": "renderingIntent",
      repeatcount: "repeatCount",
      repeatdur: "repeatDur",
      requiredextensions: "requiredExtensions",
      requiredfeatures: "requiredFeatures",
      resource: "resource",
      restart: "restart",
      result: "result",
      results: "results",
      rotate: "rotate",
      rx: "rx",
      ry: "ry",
      scale: "scale",
      security: "security",
      seed: "seed",
      shaperendering: "shapeRendering",
      "shape-rendering": "shapeRendering",
      slope: "slope",
      spacing: "spacing",
      specularconstant: "specularConstant",
      specularexponent: "specularExponent",
      speed: "speed",
      spreadmethod: "spreadMethod",
      startoffset: "startOffset",
      stddeviation: "stdDeviation",
      stemh: "stemh",
      stemv: "stemv",
      stitchtiles: "stitchTiles",
      stopcolor: "stopColor",
      "stop-color": "stopColor",
      stopopacity: "stopOpacity",
      "stop-opacity": "stopOpacity",
      strikethroughposition: "strikethroughPosition",
      "strikethrough-position": "strikethroughPosition",
      strikethroughthickness: "strikethroughThickness",
      "strikethrough-thickness": "strikethroughThickness",
      string: "string",
      stroke: "stroke",
      strokedasharray: "strokeDasharray",
      "stroke-dasharray": "strokeDasharray",
      strokedashoffset: "strokeDashoffset",
      "stroke-dashoffset": "strokeDashoffset",
      strokelinecap: "strokeLinecap",
      "stroke-linecap": "strokeLinecap",
      strokelinejoin: "strokeLinejoin",
      "stroke-linejoin": "strokeLinejoin",
      strokemiterlimit: "strokeMiterlimit",
      "stroke-miterlimit": "strokeMiterlimit",
      strokewidth: "strokeWidth",
      "stroke-width": "strokeWidth",
      strokeopacity: "strokeOpacity",
      "stroke-opacity": "strokeOpacity",
      suppresscontenteditablewarning: "suppressContentEditableWarning",
      suppresshydrationwarning: "suppressHydrationWarning",
      surfacescale: "surfaceScale",
      systemlanguage: "systemLanguage",
      tablevalues: "tableValues",
      targetx: "targetX",
      targety: "targetY",
      textanchor: "textAnchor",
      "text-anchor": "textAnchor",
      textdecoration: "textDecoration",
      "text-decoration": "textDecoration",
      textlength: "textLength",
      textrendering: "textRendering",
      "text-rendering": "textRendering",
      to: "to",
      transform: "transform",
      transformorigin: "transformOrigin",
      "transform-origin": "transformOrigin",
      typeof: "typeof",
      u1: "u1",
      u2: "u2",
      underlineposition: "underlinePosition",
      "underline-position": "underlinePosition",
      underlinethickness: "underlineThickness",
      "underline-thickness": "underlineThickness",
      unicode: "unicode",
      unicodebidi: "unicodeBidi",
      "unicode-bidi": "unicodeBidi",
      unicoderange: "unicodeRange",
      "unicode-range": "unicodeRange",
      unitsperem: "unitsPerEm",
      "units-per-em": "unitsPerEm",
      unselectable: "unselectable",
      valphabetic: "vAlphabetic",
      "v-alphabetic": "vAlphabetic",
      values: "values",
      vectoreffect: "vectorEffect",
      "vector-effect": "vectorEffect",
      version: "version",
      vertadvy: "vertAdvY",
      "vert-adv-y": "vertAdvY",
      vertoriginx: "vertOriginX",
      "vert-origin-x": "vertOriginX",
      vertoriginy: "vertOriginY",
      "vert-origin-y": "vertOriginY",
      vhanging: "vHanging",
      "v-hanging": "vHanging",
      videographic: "vIdeographic",
      "v-ideographic": "vIdeographic",
      viewbox: "viewBox",
      viewtarget: "viewTarget",
      visibility: "visibility",
      vmathematical: "vMathematical",
      "v-mathematical": "vMathematical",
      vocab: "vocab",
      widths: "widths",
      wordspacing: "wordSpacing",
      "word-spacing": "wordSpacing",
      writingmode: "writingMode",
      "writing-mode": "writingMode",
      x1: "x1",
      x2: "x2",
      x: "x",
      xchannelselector: "xChannelSelector",
      xheight: "xHeight",
      "x-height": "xHeight",
      xlinkactuate: "xlinkActuate",
      "xlink:actuate": "xlinkActuate",
      xlinkarcrole: "xlinkArcrole",
      "xlink:arcrole": "xlinkArcrole",
      xlinkhref: "xlinkHref",
      "xlink:href": "xlinkHref",
      xlinkrole: "xlinkRole",
      "xlink:role": "xlinkRole",
      xlinkshow: "xlinkShow",
      "xlink:show": "xlinkShow",
      xlinktitle: "xlinkTitle",
      "xlink:title": "xlinkTitle",
      xlinktype: "xlinkType",
      "xlink:type": "xlinkType",
      xmlbase: "xmlBase",
      "xml:base": "xmlBase",
      xmllang: "xmlLang",
      "xml:lang": "xmlLang",
      xmlns: "xmlns",
      "xml:space": "xmlSpace",
      xmlnsxlink: "xmlnsXlink",
      "xmlns:xlink": "xmlnsXlink",
      xmlspace: "xmlSpace",
      y1: "y1",
      y2: "y2",
      y: "y",
      ychannelselector: "yChannelSelector",
      z: "z",
      zoomandpan: "zoomAndPan"
    }, u0 = {
      "aria-current": 0,
      "aria-description": 0,
      "aria-details": 0,
      "aria-disabled": 0,
      "aria-hidden": 0,
      "aria-invalid": 0,
      "aria-keyshortcuts": 0,
      "aria-label": 0,
      "aria-roledescription": 0,
      "aria-autocomplete": 0,
      "aria-checked": 0,
      "aria-expanded": 0,
      "aria-haspopup": 0,
      "aria-level": 0,
      "aria-modal": 0,
      "aria-multiline": 0,
      "aria-multiselectable": 0,
      "aria-orientation": 0,
      "aria-placeholder": 0,
      "aria-pressed": 0,
      "aria-readonly": 0,
      "aria-required": 0,
      "aria-selected": 0,
      "aria-sort": 0,
      "aria-valuemax": 0,
      "aria-valuemin": 0,
      "aria-valuenow": 0,
      "aria-valuetext": 0,
      "aria-atomic": 0,
      "aria-busy": 0,
      "aria-live": 0,
      "aria-relevant": 0,
      "aria-dropeffect": 0,
      "aria-grabbed": 0,
      "aria-activedescendant": 0,
      "aria-colcount": 0,
      "aria-colindex": 0,
      "aria-colspan": 0,
      "aria-controls": 0,
      "aria-describedby": 0,
      "aria-errormessage": 0,
      "aria-flowto": 0,
      "aria-labelledby": 0,
      "aria-owns": 0,
      "aria-posinset": 0,
      "aria-rowcount": 0,
      "aria-rowindex": 0,
      "aria-rowspan": 0,
      "aria-setsize": 0
    }, Ja = {}, c0 = RegExp(
      "^(aria)-[:A-Z_a-z\\u00C0-\\u00D6\\u00D8-\\u00F6\\u00F8-\\u02FF\\u0370-\\u037D\\u037F-\\u1FFF\\u200C-\\u200D\\u2070-\\u218F\\u2C00-\\u2FEF\\u3001-\\uD7FF\\uF900-\\uFDCF\\uFDF0-\\uFFFD\\-.0-9\\u00B7\\u0300-\\u036F\\u203F-\\u2040]*$"
    ), Qg = RegExp(
      "^(aria)[A-Z][:A-Z_a-z\\u00C0-\\u00D6\\u00D8-\\u00F6\\u00F8-\\u02FF\\u0370-\\u037D\\u037F-\\u1FFF\\u200C-\\u200D\\u2070-\\u218F\\u2C00-\\u2FEF\\u3001-\\uD7FF\\uF900-\\uFDCF\\uFDF0-\\uFFFD\\-.0-9\\u00B7\\u0300-\\u036F\\u203F-\\u2040]*$"
    ), f0 = !1, vi = {}, Eh = /^on./, l = /^on[^A-Z]/, o = RegExp(
      "^(aria)-[:A-Z_a-z\\u00C0-\\u00D6\\u00D8-\\u00F6\\u00F8-\\u02FF\\u0370-\\u037D\\u037F-\\u1FFF\\u200C-\\u200D\\u2070-\\u218F\\u2C00-\\u2FEF\\u3001-\\uD7FF\\uF900-\\uFDCF\\uFDF0-\\uFFFD\\-.0-9\\u00B7\\u0300-\\u036F\\u203F-\\u2040]*$"
    ), c = RegExp(
      "^(aria)[A-Z][:A-Z_a-z\\u00C0-\\u00D6\\u00D8-\\u00F6\\u00F8-\\u02FF\\u0370-\\u037D\\u037F-\\u1FFF\\u200C-\\u200D\\u2070-\\u218F\\u2C00-\\u2FEF\\u3001-\\uD7FF\\uF900-\\uFDCF\\uFDF0-\\uFFFD\\-.0-9\\u00B7\\u0300-\\u036F\\u203F-\\u2040]*$"
    ), d = /^[\u0000-\u001F ]*j[\r\n\t]*a[\r\n\t]*v[\r\n\t]*a[\r\n\t]*s[\r\n\t]*c[\r\n\t]*r[\r\n\t]*i[\r\n\t]*p[\r\n\t]*t[\r\n\t]*:/i, v = null, S = null, A = null, M = !1, G = !(typeof window > "u" || typeof window.document > "u" || typeof window.document.createElement > "u"), W = !1;
    if (G)
      try {
        var ct = {};
        Object.defineProperty(ct, "passive", {
          get: function() {
            W = !0;
          }
        }), window.addEventListener("test", ct, ct), window.removeEventListener("test", ct, ct);
      } catch {
        W = !1;
      }
    var gt = null, $ = null, et = null, Gt = {
      eventPhase: 0,
      bubbles: 0,
      cancelable: 0,
      timeStamp: function(e) {
        return e.timeStamp || Date.now();
      },
      defaultPrevented: 0,
      isTrusted: 0
    }, Ht = kn(Gt), Te = ee({}, Gt, { view: 0, detail: 0 }), q = kn(Te), V, P, ht, Mt = ee({}, Te, {
      screenX: 0,
      screenY: 0,
      clientX: 0,
      clientY: 0,
      pageX: 0,
      pageY: 0,
      ctrlKey: 0,
      shiftKey: 0,
      altKey: 0,
      metaKey: 0,
      getModifierState: md,
      button: 0,
      buttons: 0,
      relatedTarget: function(e) {
        return e.relatedTarget === void 0 ? e.fromElement === e.srcElement ? e.toElement : e.fromElement : e.relatedTarget;
      },
      movementX: function(e) {
        return "movementX" in e ? e.movementX : (e !== ht && (ht && e.type === "mousemove" ? (V = e.screenX - ht.screenX, P = e.screenY - ht.screenY) : P = V = 0, ht = e), V);
      },
      movementY: function(e) {
        return "movementY" in e ? e.movementY : P;
      }
    }), ie = kn(Mt), Lt = ee({}, Mt, { dataTransfer: 0 }), Bt = kn(Lt), Gn = ee({}, Te, { relatedTarget: 0 }), pe = kn(Gn), qo = ee({}, Gt, {
      animationName: 0,
      elapsedTime: 0,
      pseudoElement: 0
    }), PS = kn(qo), jA = ee({}, Gt, {
      clipboardData: function(e) {
        return "clipboardData" in e ? e.clipboardData : window.clipboardData;
      }
    }), BA = kn(jA), IA = ee({}, Gt, { data: 0 }), FT = kn(
      IA
    ), XA = FT, kA = {
      Esc: "Escape",
      Spacebar: " ",
      Left: "ArrowLeft",
      Up: "ArrowUp",
      Right: "ArrowRight",
      Down: "ArrowDown",
      Del: "Delete",
      Win: "OS",
      Menu: "ContextMenu",
      Apps: "ContextMenu",
      Scroll: "ScrollLock",
      MozPrintableKey: "Unidentified"
    }, VA = {
      8: "Backspace",
      9: "Tab",
      12: "Clear",
      13: "Enter",
      16: "Shift",
      17: "Control",
      18: "Alt",
      19: "Pause",
      20: "CapsLock",
      27: "Escape",
      32: " ",
      33: "PageUp",
      34: "PageDown",
      35: "End",
      36: "Home",
      37: "ArrowLeft",
      38: "ArrowUp",
      39: "ArrowRight",
      40: "ArrowDown",
      45: "Insert",
      46: "Delete",
      112: "F1",
      113: "F2",
      114: "F3",
      115: "F4",
      116: "F5",
      117: "F6",
      118: "F7",
      119: "F8",
      120: "F9",
      121: "F10",
      122: "F11",
      123: "F12",
      144: "NumLock",
      145: "ScrollLock",
      224: "Meta"
    }, qA = {
      Alt: "altKey",
      Control: "ctrlKey",
      Meta: "metaKey",
      Shift: "shiftKey"
    }, ZA = ee({}, Te, {
      key: function(e) {
        if (e.key) {
          var n = kA[e.key] || e.key;
          if (n !== "Unidentified") return n;
        }
        return e.type === "keypress" ? (e = wu(e), e === 13 ? "Enter" : String.fromCharCode(e)) : e.type === "keydown" || e.type === "keyup" ? VA[e.keyCode] || "Unidentified" : "";
      },
      code: 0,
      location: 0,
      ctrlKey: 0,
      shiftKey: 0,
      altKey: 0,
      metaKey: 0,
      repeat: 0,
      locale: 0,
      getModifierState: md,
      charCode: function(e) {
        return e.type === "keypress" ? wu(e) : 0;
      },
      keyCode: function(e) {
        return e.type === "keydown" || e.type === "keyup" ? e.keyCode : 0;
      },
      which: function(e) {
        return e.type === "keypress" ? wu(e) : e.type === "keydown" || e.type === "keyup" ? e.keyCode : 0;
      }
    }), FA = kn(ZA), KA = ee({}, Mt, {
      pointerId: 0,
      width: 0,
      height: 0,
      pressure: 0,
      tangentialPressure: 0,
      tiltX: 0,
      tiltY: 0,
      twist: 0,
      pointerType: 0,
      isPrimary: 0
    }), KT = kn(KA), PA = ee({}, Te, {
      touches: 0,
      targetTouches: 0,
      changedTouches: 0,
      altKey: 0,
      metaKey: 0,
      ctrlKey: 0,
      shiftKey: 0,
      getModifierState: md
    }), QA = kn(PA), WA = ee({}, Gt, {
      propertyName: 0,
      elapsedTime: 0,
      pseudoElement: 0
    }), JA = kn(WA), $A = ee({}, Mt, {
      deltaX: function(e) {
        return "deltaX" in e ? e.deltaX : "wheelDeltaX" in e ? -e.wheelDeltaX : 0;
      },
      deltaY: function(e) {
        return "deltaY" in e ? e.deltaY : "wheelDeltaY" in e ? -e.wheelDeltaY : "wheelDelta" in e ? -e.wheelDelta : 0;
      },
      deltaZ: 0,
      deltaMode: 0
    }), tM = kn($A), eM = ee({}, Gt, {
      newState: 0,
      oldState: 0
    }), nM = kn(eM), iM = [9, 13, 27, 32], PT = 229, QS = G && "CompositionEvent" in window, h0 = null;
    G && "documentMode" in document && (h0 = document.documentMode);
    var lM = G && "TextEvent" in window && !h0, QT = G && (!QS || h0 && 8 < h0 && 11 >= h0), WT = 32, JT = String.fromCharCode(WT), $T = !1, Wg = !1, aM = {
      color: !0,
      date: !0,
      datetime: !0,
      "datetime-local": !0,
      email: !0,
      month: !0,
      number: !0,
      password: !0,
      range: !0,
      search: !0,
      tel: !0,
      text: !0,
      time: !0,
      url: !0,
      week: !0
    }, d0 = null, g0 = null, tx = !1;
    G && (tx = hy("input") && (!document.documentMode || 9 < document.documentMode));
    var rl = typeof Object.is == "function" ? Object.is : US, sM = G && "documentMode" in document && 11 >= document.documentMode, Jg = null, WS = null, m0 = null, JS = !1, $g = {
      animationend: Es("Animation", "AnimationEnd"),
      animationiteration: Es("Animation", "AnimationIteration"),
      animationstart: Es("Animation", "AnimationStart"),
      transitionrun: Es("Transition", "TransitionRun"),
      transitionstart: Es("Transition", "TransitionStart"),
      transitioncancel: Es("Transition", "TransitionCancel"),
      transitionend: Es("Transition", "TransitionEnd")
    }, $S = {}, ex = {};
    G && (ex = document.createElement("div").style, "AnimationEvent" in window || (delete $g.animationend.animation, delete $g.animationiteration.animation, delete $g.animationstart.animation), "TransitionEvent" in window || delete $g.transitionend.transition);
    var nx = fr("animationend"), ix = fr("animationiteration"), lx = fr("animationstart"), oM = fr("transitionrun"), rM = fr("transitionstart"), uM = fr("transitioncancel"), ax = fr("transitionend"), sx = /* @__PURE__ */ new Map(), tE = "abort auxClick beforeToggle cancel canPlay canPlayThrough click close contextMenu copy cut drag dragEnd dragEnter dragExit dragLeave dragOver dragStart drop durationChange emptied encrypted ended error gotPointerCapture input invalid keyDown keyPress keyUp load loadedData loadedMetadata loadStart lostPointerCapture mouseDown mouseMove mouseOut mouseOver mouseUp paste pause play playing pointerCancel pointerDown pointerMove pointerOut pointerOver pointerUp progress rateChange reset resize seeked seeking stalled submit suspend timeUpdate touchCancel touchEnd touchStart volumeChange scroll toggle touchMove waiting wheel".split(
      " "
    );
    tE.push("scrollEnd");
    var eE = /* @__PURE__ */ new WeakMap(), Xv = 1, Kr = 2, $a = [], tm = 0, nE = 0, Tc = {};
    Object.freeze(Tc);
    var ts = null, em = null, $e = 0, cM = 1, Si = 2, Xi = 8, Vs = 16, ox = 64, rx = !1;
    try {
      var ux = Object.preventExtensions({});
    } catch {
      rx = !0;
    }
    var nm = [], im = 0, kv = null, Vv = 0, es = [], ns = 0, bh = null, Pr = 1, Qr = "", ul = null, Sn = null, xe = !1, Wr = !1, is = null, Th = null, Zo = !1, iE = Error(
      "Hydration Mismatch Exception: This is not a real error, and should not leak into userspace. If you're seeing this, it's likely a bug in React."
    ), cx = 0;
    if (typeof performance == "object" && typeof performance.now == "function")
      var fM = performance, fx = function() {
        return fM.now();
      };
    else {
      var hM = Date;
      fx = function() {
        return hM.now();
      };
    }
    var lE = Et(null), aE = Et(null), hx = {}, qv = null, lm = null, am = !1, dM = typeof AbortController < "u" ? AbortController : function() {
      var e = [], n = this.signal = {
        aborted: !1,
        addEventListener: function(s, f) {
          e.push(f);
        }
      };
      this.abort = function() {
        n.aborted = !0, e.forEach(function(s) {
          return s();
        });
      };
    }, gM = hn.unstable_scheduleCallback, mM = hn.unstable_NormalPriority, Wn = {
      $$typeof: Ul,
      Consumer: null,
      Provider: null,
      _currentValue: null,
      _currentValue2: null,
      _threadCount: 0,
      _currentRenderer: null,
      _currentRenderer2: null
    }, sm = hn.unstable_now, dx = -0, Zv = -0, Yl = -1.1, xh = -0, Fv = !1, Kv = !1, y0 = null, sE = 0, Ch = 0, om = null, gx = lt.S;
    lt.S = function(e, n) {
      typeof n == "object" && n !== null && typeof n.then == "function" && J_(e, n), gx !== null && gx(e, n);
    };
    var Rh = Et(null), qs = {
      recordUnsafeLifecycleWarnings: function() {
      },
      flushPendingUnsafeLifecycleWarnings: function() {
      },
      recordLegacyContextWarning: function() {
      },
      flushLegacyContextWarning: function() {
      },
      discardPendingWarnings: function() {
      }
    }, p0 = [], _0 = [], v0 = [], S0 = [], E0 = [], b0 = [], Ah = /* @__PURE__ */ new Set();
    qs.recordUnsafeLifecycleWarnings = function(e, n) {
      Ah.has(e.type) || (typeof n.componentWillMount == "function" && n.componentWillMount.__suppressDeprecationWarning !== !0 && p0.push(e), e.mode & Xi && typeof n.UNSAFE_componentWillMount == "function" && _0.push(e), typeof n.componentWillReceiveProps == "function" && n.componentWillReceiveProps.__suppressDeprecationWarning !== !0 && v0.push(e), e.mode & Xi && typeof n.UNSAFE_componentWillReceiveProps == "function" && S0.push(e), typeof n.componentWillUpdate == "function" && n.componentWillUpdate.__suppressDeprecationWarning !== !0 && E0.push(e), e.mode & Xi && typeof n.UNSAFE_componentWillUpdate == "function" && b0.push(e));
    }, qs.flushPendingUnsafeLifecycleWarnings = function() {
      var e = /* @__PURE__ */ new Set();
      0 < p0.length && (p0.forEach(function(C) {
        e.add(
          it(C) || "Component"
        ), Ah.add(C.type);
      }), p0 = []);
      var n = /* @__PURE__ */ new Set();
      0 < _0.length && (_0.forEach(function(C) {
        n.add(
          it(C) || "Component"
        ), Ah.add(C.type);
      }), _0 = []);
      var s = /* @__PURE__ */ new Set();
      0 < v0.length && (v0.forEach(function(C) {
        s.add(
          it(C) || "Component"
        ), Ah.add(C.type);
      }), v0 = []);
      var f = /* @__PURE__ */ new Set();
      0 < S0.length && (S0.forEach(
        function(C) {
          f.add(
            it(C) || "Component"
          ), Ah.add(C.type);
        }
      ), S0 = []);
      var m = /* @__PURE__ */ new Set();
      0 < E0.length && (E0.forEach(function(C) {
        m.add(
          it(C) || "Component"
        ), Ah.add(C.type);
      }), E0 = []);
      var p = /* @__PURE__ */ new Set();
      if (0 < b0.length && (b0.forEach(function(C) {
        p.add(
          it(C) || "Component"
        ), Ah.add(C.type);
      }), b0 = []), 0 < n.size) {
        var T = x(
          n
        );
        console.error(
          `Using UNSAFE_componentWillMount in strict mode is not recommended and may indicate bugs in your code. See https://react.dev/link/unsafe-component-lifecycles for details.

* Move code with side effects to componentDidMount, and set initial state in the constructor.

Please update the following components: %s`,
          T
        );
      }
      0 < f.size && (T = x(
        f
      ), console.error(
        `Using UNSAFE_componentWillReceiveProps in strict mode is not recommended and may indicate bugs in your code. See https://react.dev/link/unsafe-component-lifecycles for details.

* Move data fetching code or side effects to componentDidUpdate.
* If you're updating state whenever props change, refactor your code to use memoization techniques or move it to static getDerivedStateFromProps. Learn more at: https://react.dev/link/derived-state

Please update the following components: %s`,
        T
      )), 0 < p.size && (T = x(
        p
      ), console.error(
        `Using UNSAFE_componentWillUpdate in strict mode is not recommended and may indicate bugs in your code. See https://react.dev/link/unsafe-component-lifecycles for details.

* Move data fetching code or side effects to componentDidUpdate.

Please update the following components: %s`,
        T
      )), 0 < e.size && (T = x(e), console.warn(
        `componentWillMount has been renamed, and is not recommended for use. See https://react.dev/link/unsafe-component-lifecycles for details.

* Move code with side effects to componentDidMount, and set initial state in the constructor.
* Rename componentWillMount to UNSAFE_componentWillMount to suppress this warning in non-strict mode. In React 18.x, only the UNSAFE_ name will work. To rename all deprecated lifecycles to their new names, you can run \`npx react-codemod rename-unsafe-lifecycles\` in your project source folder.

Please update the following components: %s`,
        T
      )), 0 < s.size && (T = x(
        s
      ), console.warn(
        `componentWillReceiveProps has been renamed, and is not recommended for use. See https://react.dev/link/unsafe-component-lifecycles for details.

* Move data fetching code or side effects to componentDidUpdate.
* If you're updating state whenever props change, refactor your code to use memoization techniques or move it to static getDerivedStateFromProps. Learn more at: https://react.dev/link/derived-state
* Rename componentWillReceiveProps to UNSAFE_componentWillReceiveProps to suppress this warning in non-strict mode. In React 18.x, only the UNSAFE_ name will work. To rename all deprecated lifecycles to their new names, you can run \`npx react-codemod rename-unsafe-lifecycles\` in your project source folder.

Please update the following components: %s`,
        T
      )), 0 < m.size && (T = x(m), console.warn(
        `componentWillUpdate has been renamed, and is not recommended for use. See https://react.dev/link/unsafe-component-lifecycles for details.

* Move data fetching code or side effects to componentDidUpdate.
* Rename componentWillUpdate to UNSAFE_componentWillUpdate to suppress this warning in non-strict mode. In React 18.x, only the UNSAFE_ name will work. To rename all deprecated lifecycles to their new names, you can run \`npx react-codemod rename-unsafe-lifecycles\` in your project source folder.

Please update the following components: %s`,
        T
      ));
    };
    var Pv = /* @__PURE__ */ new Map(), mx = /* @__PURE__ */ new Set();
    qs.recordLegacyContextWarning = function(e, n) {
      for (var s = null, f = e; f !== null; )
        f.mode & Xi && (s = f), f = f.return;
      s === null ? console.error(
        "Expected to find a StrictMode component in a strict mode tree. This error is likely caused by a bug in React. Please file an issue."
      ) : !mx.has(e.type) && (f = Pv.get(s), e.type.contextTypes != null || e.type.childContextTypes != null || n !== null && typeof n.getChildContext == "function") && (f === void 0 && (f = [], Pv.set(s, f)), f.push(e));
    }, qs.flushLegacyContextWarning = function() {
      Pv.forEach(function(e) {
        if (e.length !== 0) {
          var n = e[0], s = /* @__PURE__ */ new Set();
          e.forEach(function(m) {
            s.add(it(m) || "Component"), mx.add(m.type);
          });
          var f = x(s);
          wt(n, function() {
            console.error(
              `Legacy context API has been detected within a strict-mode tree.

The old API will be supported in all 16.x releases, but applications using it should migrate to the new version.

Please update the following components: %s

Learn more about this warning here: https://react.dev/link/legacy-context`,
              f
            );
          });
        }
      });
    }, qs.discardPendingWarnings = function() {
      p0 = [], _0 = [], v0 = [], S0 = [], E0 = [], b0 = [], Pv = /* @__PURE__ */ new Map();
    };
    var T0 = Error(
      "Suspense Exception: This is not a real error! It's an implementation detail of `use` to interrupt the current render. You must either rethrow it immediately, or move the `use` call outside of the `try/catch` block. Capturing without rethrowing will lead to unexpected behavior.\n\nTo handle async errors, wrap your component in an error boundary, or call the promise's `.catch` method and pass the result to `use`."
    ), yx = Error(
      "Suspense Exception: This is not a real error, and should not leak into userspace. If you're seeing this, it's likely a bug in React."
    ), Qv = Error(
      "Suspense Exception: This is not a real error! It's an implementation detail of `useActionState` to interrupt the current render. You must either rethrow it immediately, or move the `useActionState` call outside of the `try/catch` block. Capturing without rethrowing will lead to unexpected behavior.\n\nTo handle async errors, wrap your component in an error boundary."
    ), oE = {
      then: function() {
        console.error(
          'Internal React error: A listener was unexpectedly attached to a "noop" thenable. This is a bug in React. Please file an issue.'
        );
      }
    }, x0 = null, Wv = !1, ls = 0, as = 1, cl = 2, Ei = 4, Jn = 8, px = 0, _x = 1, vx = 2, rE = 3, xc = !1, Sx = !1, uE = null, cE = !1, rm = Et(null), Jv = Et(0), um, Ex = /* @__PURE__ */ new Set(), bx = /* @__PURE__ */ new Set(), fE = /* @__PURE__ */ new Set(), Tx = /* @__PURE__ */ new Set(), Cc = 0, Kt = null, Ve = null, Hn = null, $v = !1, cm = !1, Mh = !1, t1 = 0, C0 = 0, Jr = null, yM = 0, pM = 25, at = null, ss = null, $r = -1, R0 = !1, e1 = {
      readContext: Ke,
      use: wa,
      useCallback: en,
      useContext: en,
      useEffect: en,
      useImperativeHandle: en,
      useLayoutEffect: en,
      useInsertionEffect: en,
      useMemo: en,
      useReducer: en,
      useRef: en,
      useState: en,
      useDebugValue: en,
      useDeferredValue: en,
      useTransition: en,
      useSyncExternalStore: en,
      useId: en,
      useHostTransitionStatus: en,
      useFormState: en,
      useActionState: en,
      useOptimistic: en,
      useMemoCache: en,
      useCacheRefresh: en
    }, hE = null, xx = null, dE = null, Cx = null, Fo = null, Zs = null, n1 = null;
    hE = {
      readContext: function(e) {
        return Ke(e);
      },
      use: wa,
      useCallback: function(e, n) {
        return at = "useCallback", ne(), Cl(n), Mf(e, n);
      },
      useContext: function(e) {
        return at = "useContext", ne(), Ke(e);
      },
      useEffect: function(e, n) {
        return at = "useEffect", ne(), Cl(n), wd(e, n);
      },
      useImperativeHandle: function(e, n, s) {
        return at = "useImperativeHandle", ne(), Cl(s), Od(e, n, s);
      },
      useInsertionEffect: function(e, n) {
        at = "useInsertionEffect", ne(), Cl(n), wl(4, cl, e, n);
      },
      useLayoutEffect: function(e, n) {
        return at = "useLayoutEffect", ne(), Cl(n), Dd(e, n);
      },
      useMemo: function(e, n) {
        at = "useMemo", ne(), Cl(n);
        var s = lt.H;
        lt.H = Fo;
        try {
          return Ld(e, n);
        } finally {
          lt.H = s;
        }
      },
      useReducer: function(e, n, s) {
        at = "useReducer", ne();
        var f = lt.H;
        lt.H = Fo;
        try {
          return ve(e, n, s);
        } finally {
          lt.H = f;
        }
      },
      useRef: function(e) {
        return at = "useRef", ne(), Af(e);
      },
      useState: function(e) {
        at = "useState", ne();
        var n = lt.H;
        lt.H = Fo;
        try {
          return Rs(e);
        } finally {
          lt.H = n;
        }
      },
      useDebugValue: function() {
        at = "useDebugValue", ne();
      },
      useDeferredValue: function(e, n) {
        return at = "useDeferredValue", ne(), zd(e, n);
      },
      useTransition: function() {
        return at = "useTransition", ne(), za();
      },
      useSyncExternalStore: function(e, n, s) {
        return at = "useSyncExternalStore", ne(), Cs(
          e,
          n,
          s
        );
      },
      useId: function() {
        return at = "useId", ne(), Na();
      },
      useFormState: function(e, n) {
        return at = "useFormState", ne(), Uu(), Iu(e, n);
      },
      useActionState: function(e, n) {
        return at = "useActionState", ne(), Iu(e, n);
      },
      useOptimistic: function(e) {
        return at = "useOptimistic", ne(), sa(e);
      },
      useHostTransitionStatus: Li,
      useMemoCache: mn,
      useCacheRefresh: function() {
        return at = "useCacheRefresh", ne(), Tr();
      }
    }, xx = {
      readContext: function(e) {
        return Ke(e);
      },
      use: wa,
      useCallback: function(e, n) {
        return at = "useCallback", _t(), Mf(e, n);
      },
      useContext: function(e) {
        return at = "useContext", _t(), Ke(e);
      },
      useEffect: function(e, n) {
        return at = "useEffect", _t(), wd(e, n);
      },
      useImperativeHandle: function(e, n, s) {
        return at = "useImperativeHandle", _t(), Od(e, n, s);
      },
      useInsertionEffect: function(e, n) {
        at = "useInsertionEffect", _t(), wl(4, cl, e, n);
      },
      useLayoutEffect: function(e, n) {
        return at = "useLayoutEffect", _t(), Dd(e, n);
      },
      useMemo: function(e, n) {
        at = "useMemo", _t();
        var s = lt.H;
        lt.H = Fo;
        try {
          return Ld(e, n);
        } finally {
          lt.H = s;
        }
      },
      useReducer: function(e, n, s) {
        at = "useReducer", _t();
        var f = lt.H;
        lt.H = Fo;
        try {
          return ve(e, n, s);
        } finally {
          lt.H = f;
        }
      },
      useRef: function(e) {
        return at = "useRef", _t(), Af(e);
      },
      useState: function(e) {
        at = "useState", _t();
        var n = lt.H;
        lt.H = Fo;
        try {
          return Rs(e);
        } finally {
          lt.H = n;
        }
      },
      useDebugValue: function() {
        at = "useDebugValue", _t();
      },
      useDeferredValue: function(e, n) {
        return at = "useDeferredValue", _t(), zd(e, n);
      },
      useTransition: function() {
        return at = "useTransition", _t(), za();
      },
      useSyncExternalStore: function(e, n, s) {
        return at = "useSyncExternalStore", _t(), Cs(
          e,
          n,
          s
        );
      },
      useId: function() {
        return at = "useId", _t(), Na();
      },
      useActionState: function(e, n) {
        return at = "useActionState", _t(), Iu(e, n);
      },
      useFormState: function(e, n) {
        return at = "useFormState", _t(), Uu(), Iu(e, n);
      },
      useOptimistic: function(e) {
        return at = "useOptimistic", _t(), sa(e);
      },
      useHostTransitionStatus: Li,
      useMemoCache: mn,
      useCacheRefresh: function() {
        return at = "useCacheRefresh", _t(), Tr();
      }
    }, dE = {
      readContext: function(e) {
        return Ke(e);
      },
      use: wa,
      useCallback: function(e, n) {
        return at = "useCallback", _t(), Er(e, n);
      },
      useContext: function(e) {
        return at = "useContext", _t(), Ke(e);
      },
      useEffect: function(e, n) {
        at = "useEffect", _t(), Cn(2048, Jn, e, n);
      },
      useImperativeHandle: function(e, n, s) {
        return at = "useImperativeHandle", _t(), La(e, n, s);
      },
      useInsertionEffect: function(e, n) {
        return at = "useInsertionEffect", _t(), Cn(4, cl, e, n);
      },
      useLayoutEffect: function(e, n) {
        return at = "useLayoutEffect", _t(), Cn(4, Ei, e, n);
      },
      useMemo: function(e, n) {
        at = "useMemo", _t();
        var s = lt.H;
        lt.H = Zs;
        try {
          return xo(e, n);
        } finally {
          lt.H = s;
        }
      },
      useReducer: function(e, n, s) {
        at = "useReducer", _t();
        var f = lt.H;
        lt.H = Zs;
        try {
          return Al(e, n, s);
        } finally {
          lt.H = f;
        }
      },
      useRef: function() {
        return at = "useRef", _t(), me().memoizedState;
      },
      useState: function() {
        at = "useState", _t();
        var e = lt.H;
        lt.H = Zs;
        try {
          return Al(Ee);
        } finally {
          lt.H = e;
        }
      },
      useDebugValue: function() {
        at = "useDebugValue", _t();
      },
      useDeferredValue: function(e, n) {
        return at = "useDeferredValue", _t(), wf(e, n);
      },
      useTransition: function() {
        return at = "useTransition", _t(), Gd();
      },
      useSyncExternalStore: function(e, n, s) {
        return at = "useSyncExternalStore", _t(), Tf(
          e,
          n,
          s
        );
      },
      useId: function() {
        return at = "useId", _t(), me().memoizedState;
      },
      useFormState: function(e) {
        return at = "useFormState", _t(), Uu(), Md(e);
      },
      useActionState: function(e) {
        return at = "useActionState", _t(), Md(e);
      },
      useOptimistic: function(e, n) {
        return at = "useOptimistic", _t(), As(e, n);
      },
      useHostTransitionStatus: Li,
      useMemoCache: mn,
      useCacheRefresh: function() {
        return at = "useCacheRefresh", _t(), me().memoizedState;
      }
    }, Cx = {
      readContext: function(e) {
        return Ke(e);
      },
      use: wa,
      useCallback: function(e, n) {
        return at = "useCallback", _t(), Er(e, n);
      },
      useContext: function(e) {
        return at = "useContext", _t(), Ke(e);
      },
      useEffect: function(e, n) {
        at = "useEffect", _t(), Cn(2048, Jn, e, n);
      },
      useImperativeHandle: function(e, n, s) {
        return at = "useImperativeHandle", _t(), La(e, n, s);
      },
      useInsertionEffect: function(e, n) {
        return at = "useInsertionEffect", _t(), Cn(4, cl, e, n);
      },
      useLayoutEffect: function(e, n) {
        return at = "useLayoutEffect", _t(), Cn(4, Ei, e, n);
      },
      useMemo: function(e, n) {
        at = "useMemo", _t();
        var s = lt.H;
        lt.H = n1;
        try {
          return xo(e, n);
        } finally {
          lt.H = s;
        }
      },
      useReducer: function(e, n, s) {
        at = "useReducer", _t();
        var f = lt.H;
        lt.H = n1;
        try {
          return Sr(e, n, s);
        } finally {
          lt.H = f;
        }
      },
      useRef: function() {
        return at = "useRef", _t(), me().memoizedState;
      },
      useState: function() {
        at = "useState", _t();
        var e = lt.H;
        lt.H = n1;
        try {
          return Sr(Ee);
        } finally {
          lt.H = e;
        }
      },
      useDebugValue: function() {
        at = "useDebugValue", _t();
      },
      useDeferredValue: function(e, n) {
        return at = "useDeferredValue", _t(), Nd(e, n);
      },
      useTransition: function() {
        return at = "useTransition", _t(), Hd();
      },
      useSyncExternalStore: function(e, n, s) {
        return at = "useSyncExternalStore", _t(), Tf(
          e,
          n,
          s
        );
      },
      useId: function() {
        return at = "useId", _t(), me().memoizedState;
      },
      useFormState: function(e) {
        return at = "useFormState", _t(), Uu(), Xu(e);
      },
      useActionState: function(e) {
        return at = "useActionState", _t(), Xu(e);
      },
      useOptimistic: function(e, n) {
        return at = "useOptimistic", _t(), Ad(e, n);
      },
      useHostTransitionStatus: Li,
      useMemoCache: mn,
      useCacheRefresh: function() {
        return at = "useCacheRefresh", _t(), me().memoizedState;
      }
    }, Fo = {
      readContext: function(e) {
        return b(), Ke(e);
      },
      use: function(e) {
        return _(), wa(e);
      },
      useCallback: function(e, n) {
        return at = "useCallback", _(), ne(), Mf(e, n);
      },
      useContext: function(e) {
        return at = "useContext", _(), ne(), Ke(e);
      },
      useEffect: function(e, n) {
        return at = "useEffect", _(), ne(), wd(e, n);
      },
      useImperativeHandle: function(e, n, s) {
        return at = "useImperativeHandle", _(), ne(), Od(e, n, s);
      },
      useInsertionEffect: function(e, n) {
        at = "useInsertionEffect", _(), ne(), wl(4, cl, e, n);
      },
      useLayoutEffect: function(e, n) {
        return at = "useLayoutEffect", _(), ne(), Dd(e, n);
      },
      useMemo: function(e, n) {
        at = "useMemo", _(), ne();
        var s = lt.H;
        lt.H = Fo;
        try {
          return Ld(e, n);
        } finally {
          lt.H = s;
        }
      },
      useReducer: function(e, n, s) {
        at = "useReducer", _(), ne();
        var f = lt.H;
        lt.H = Fo;
        try {
          return ve(e, n, s);
        } finally {
          lt.H = f;
        }
      },
      useRef: function(e) {
        return at = "useRef", _(), ne(), Af(e);
      },
      useState: function(e) {
        at = "useState", _(), ne();
        var n = lt.H;
        lt.H = Fo;
        try {
          return Rs(e);
        } finally {
          lt.H = n;
        }
      },
      useDebugValue: function() {
        at = "useDebugValue", _(), ne();
      },
      useDeferredValue: function(e, n) {
        return at = "useDeferredValue", _(), ne(), zd(e, n);
      },
      useTransition: function() {
        return at = "useTransition", _(), ne(), za();
      },
      useSyncExternalStore: function(e, n, s) {
        return at = "useSyncExternalStore", _(), ne(), Cs(
          e,
          n,
          s
        );
      },
      useId: function() {
        return at = "useId", _(), ne(), Na();
      },
      useFormState: function(e, n) {
        return at = "useFormState", _(), ne(), Iu(e, n);
      },
      useActionState: function(e, n) {
        return at = "useActionState", _(), ne(), Iu(e, n);
      },
      useOptimistic: function(e) {
        return at = "useOptimistic", _(), ne(), sa(e);
      },
      useMemoCache: function(e) {
        return _(), mn(e);
      },
      useHostTransitionStatus: Li,
      useCacheRefresh: function() {
        return at = "useCacheRefresh", ne(), Tr();
      }
    }, Zs = {
      readContext: function(e) {
        return b(), Ke(e);
      },
      use: function(e) {
        return _(), wa(e);
      },
      useCallback: function(e, n) {
        return at = "useCallback", _(), _t(), Er(e, n);
      },
      useContext: function(e) {
        return at = "useContext", _(), _t(), Ke(e);
      },
      useEffect: function(e, n) {
        at = "useEffect", _(), _t(), Cn(2048, Jn, e, n);
      },
      useImperativeHandle: function(e, n, s) {
        return at = "useImperativeHandle", _(), _t(), La(e, n, s);
      },
      useInsertionEffect: function(e, n) {
        return at = "useInsertionEffect", _(), _t(), Cn(4, cl, e, n);
      },
      useLayoutEffect: function(e, n) {
        return at = "useLayoutEffect", _(), _t(), Cn(4, Ei, e, n);
      },
      useMemo: function(e, n) {
        at = "useMemo", _(), _t();
        var s = lt.H;
        lt.H = Zs;
        try {
          return xo(e, n);
        } finally {
          lt.H = s;
        }
      },
      useReducer: function(e, n, s) {
        at = "useReducer", _(), _t();
        var f = lt.H;
        lt.H = Zs;
        try {
          return Al(e, n, s);
        } finally {
          lt.H = f;
        }
      },
      useRef: function() {
        return at = "useRef", _(), _t(), me().memoizedState;
      },
      useState: function() {
        at = "useState", _(), _t();
        var e = lt.H;
        lt.H = Zs;
        try {
          return Al(Ee);
        } finally {
          lt.H = e;
        }
      },
      useDebugValue: function() {
        at = "useDebugValue", _(), _t();
      },
      useDeferredValue: function(e, n) {
        return at = "useDeferredValue", _(), _t(), wf(e, n);
      },
      useTransition: function() {
        return at = "useTransition", _(), _t(), Gd();
      },
      useSyncExternalStore: function(e, n, s) {
        return at = "useSyncExternalStore", _(), _t(), Tf(
          e,
          n,
          s
        );
      },
      useId: function() {
        return at = "useId", _(), _t(), me().memoizedState;
      },
      useFormState: function(e) {
        return at = "useFormState", _(), _t(), Md(e);
      },
      useActionState: function(e) {
        return at = "useActionState", _(), _t(), Md(e);
      },
      useOptimistic: function(e, n) {
        return at = "useOptimistic", _(), _t(), As(e, n);
      },
      useMemoCache: function(e) {
        return _(), mn(e);
      },
      useHostTransitionStatus: Li,
      useCacheRefresh: function() {
        return at = "useCacheRefresh", _t(), me().memoizedState;
      }
    }, n1 = {
      readContext: function(e) {
        return b(), Ke(e);
      },
      use: function(e) {
        return _(), wa(e);
      },
      useCallback: function(e, n) {
        return at = "useCallback", _(), _t(), Er(e, n);
      },
      useContext: function(e) {
        return at = "useContext", _(), _t(), Ke(e);
      },
      useEffect: function(e, n) {
        at = "useEffect", _(), _t(), Cn(2048, Jn, e, n);
      },
      useImperativeHandle: function(e, n, s) {
        return at = "useImperativeHandle", _(), _t(), La(e, n, s);
      },
      useInsertionEffect: function(e, n) {
        return at = "useInsertionEffect", _(), _t(), Cn(4, cl, e, n);
      },
      useLayoutEffect: function(e, n) {
        return at = "useLayoutEffect", _(), _t(), Cn(4, Ei, e, n);
      },
      useMemo: function(e, n) {
        at = "useMemo", _(), _t();
        var s = lt.H;
        lt.H = Zs;
        try {
          return xo(e, n);
        } finally {
          lt.H = s;
        }
      },
      useReducer: function(e, n, s) {
        at = "useReducer", _(), _t();
        var f = lt.H;
        lt.H = Zs;
        try {
          return Sr(e, n, s);
        } finally {
          lt.H = f;
        }
      },
      useRef: function() {
        return at = "useRef", _(), _t(), me().memoizedState;
      },
      useState: function() {
        at = "useState", _(), _t();
        var e = lt.H;
        lt.H = Zs;
        try {
          return Sr(Ee);
        } finally {
          lt.H = e;
        }
      },
      useDebugValue: function() {
        at = "useDebugValue", _(), _t();
      },
      useDeferredValue: function(e, n) {
        return at = "useDeferredValue", _(), _t(), Nd(e, n);
      },
      useTransition: function() {
        return at = "useTransition", _(), _t(), Hd();
      },
      useSyncExternalStore: function(e, n, s) {
        return at = "useSyncExternalStore", _(), _t(), Tf(
          e,
          n,
          s
        );
      },
      useId: function() {
        return at = "useId", _(), _t(), me().memoizedState;
      },
      useFormState: function(e) {
        return at = "useFormState", _(), _t(), Xu(e);
      },
      useActionState: function(e) {
        return at = "useActionState", _(), _t(), Xu(e);
      },
      useOptimistic: function(e, n) {
        return at = "useOptimistic", _(), _t(), Ad(e, n);
      },
      useMemoCache: function(e) {
        return _(), mn(e);
      },
      useHostTransitionStatus: Li,
      useCacheRefresh: function() {
        return at = "useCacheRefresh", _t(), me().memoizedState;
      }
    };
    var Rx = {
      react_stack_bottom_frame: function(e, n, s) {
        var f = Ii;
        Ii = !0;
        try {
          return e(n, s);
        } finally {
          Ii = f;
        }
      }
    }, gE = Rx.react_stack_bottom_frame.bind(Rx), Ax = {
      react_stack_bottom_frame: function(e) {
        var n = Ii;
        Ii = !0;
        try {
          return e.render();
        } finally {
          Ii = n;
        }
      }
    }, Mx = Ax.react_stack_bottom_frame.bind(Ax), wx = {
      react_stack_bottom_frame: function(e, n) {
        try {
          n.componentDidMount();
        } catch (s) {
          jt(e, e.return, s);
        }
      }
    }, mE = wx.react_stack_bottom_frame.bind(
      wx
    ), Dx = {
      react_stack_bottom_frame: function(e, n, s, f, m) {
        try {
          n.componentDidUpdate(s, f, m);
        } catch (p) {
          jt(e, e.return, p);
        }
      }
    }, Ox = Dx.react_stack_bottom_frame.bind(
      Dx
    ), Lx = {
      react_stack_bottom_frame: function(e, n) {
        var s = n.stack;
        e.componentDidCatch(n.value, {
          componentStack: s !== null ? s : ""
        });
      }
    }, _M = Lx.react_stack_bottom_frame.bind(
      Lx
    ), zx = {
      react_stack_bottom_frame: function(e, n, s) {
        try {
          s.componentWillUnmount();
        } catch (f) {
          jt(e, n, f);
        }
      }
    }, Nx = zx.react_stack_bottom_frame.bind(
      zx
    ), Ux = {
      react_stack_bottom_frame: function(e) {
        e.resourceKind != null && console.error(
          "Expected only SimpleEffects when enableUseEffectCRUDOverload is disabled, got %s",
          e.resourceKind
        );
        var n = e.create;
        return e = e.inst, n = n(), e.destroy = n;
      }
    }, vM = Ux.react_stack_bottom_frame.bind(Ux), Gx = {
      react_stack_bottom_frame: function(e, n, s) {
        try {
          s();
        } catch (f) {
          jt(e, n, f);
        }
      }
    }, SM = Gx.react_stack_bottom_frame.bind(Gx), Hx = {
      react_stack_bottom_frame: function(e) {
        var n = e._init;
        return n(e._payload);
      }
    }, Rc = Hx.react_stack_bottom_frame.bind(Hx), fm = null, A0 = 0, le = null, yE, Yx = yE = !1, jx = {}, Bx = {}, Ix = {};
    y = function(e, n, s) {
      if (s !== null && typeof s == "object" && s._store && (!s._store.validated && s.key == null || s._store.validated === 2)) {
        if (typeof s._store != "object")
          throw Error(
            "React Component in warnForMissingKey should have a _store. This error is likely caused by a bug in React. Please file an issue."
          );
        s._store.validated = 1;
        var f = it(e), m = f || "null";
        if (!jx[m]) {
          jx[m] = !0, s = s._owner, e = e._debugOwner;
          var p = "";
          e && typeof e.tag == "number" && (m = it(e)) && (p = `

Check the render method of \`` + m + "`."), p || f && (p = `

Check the top-level render call using <` + f + ">.");
          var T = "";
          s != null && e !== s && (f = null, typeof s.tag == "number" ? f = it(s) : typeof s.name == "string" && (f = s.name), f && (T = " It was passed a child from " + f + ".")), wt(n, function() {
            console.error(
              'Each child in a list should have a unique "key" prop.%s%s See https://react.dev/link/warning-keys for more information.',
              p,
              T
            );
          });
        }
      }
    };
    var hm = zf(!0), Xx = zf(!1), os = Et(null), Ko = null, dm = 1, M0 = 2, $n = Et(0), kx = {}, Vx = /* @__PURE__ */ new Set(), qx = /* @__PURE__ */ new Set(), Zx = /* @__PURE__ */ new Set(), Fx = /* @__PURE__ */ new Set(), Kx = /* @__PURE__ */ new Set(), Px = /* @__PURE__ */ new Set(), Qx = /* @__PURE__ */ new Set(), Wx = /* @__PURE__ */ new Set(), Jx = /* @__PURE__ */ new Set(), $x = /* @__PURE__ */ new Set();
    Object.freeze(kx);
    var pE = {
      enqueueSetState: function(e, n, s) {
        e = e._reactInternals;
        var f = Ui(e), m = Aa(f);
        m.payload = n, s != null && (Vy(s), m.callback = s), n = ia(e, m, f), n !== null && (rn(n, e, f), vo(n, e, f)), Fi(e, f);
      },
      enqueueReplaceState: function(e, n, s) {
        e = e._reactInternals;
        var f = Ui(e), m = Aa(f);
        m.tag = _x, m.payload = n, s != null && (Vy(s), m.callback = s), n = ia(e, m, f), n !== null && (rn(n, e, f), vo(n, e, f)), Fi(e, f);
      },
      enqueueForceUpdate: function(e, n) {
        e = e._reactInternals;
        var s = Ui(e), f = Aa(s);
        f.tag = vx, n != null && (Vy(n), f.callback = n), n = ia(e, f, s), n !== null && (rn(n, e, s), vo(n, e, s)), Ct !== null && typeof Ct.markForceUpdateScheduled == "function" && Ct.markForceUpdateScheduled(e, s);
      }
    }, _E = typeof reportError == "function" ? reportError : function(e) {
      if (typeof window == "object" && typeof window.ErrorEvent == "function") {
        var n = new window.ErrorEvent("error", {
          bubbles: !0,
          cancelable: !0,
          message: typeof e == "object" && e !== null && typeof e.message == "string" ? String(e.message) : String(e),
          error: e
        });
        if (!window.dispatchEvent(n)) return;
      } else if (typeof process == "object" && typeof process.emit == "function") {
        process.emit("uncaughtException", e);
        return;
      }
      console.error(e);
    }, gm = null, vE = null, tC = Error(
      "This is not a real error. It's an implementation detail of React's selective hydration feature. If this leaks into userspace, it's a bug in React. Please file an issue."
    ), ui = !1, eC = {}, nC = {}, iC = {}, lC = {}, mm = !1, aC = {}, SE = {}, EE = {
      dehydrated: null,
      treeContext: null,
      retryLane: 0,
      hydrationErrors: null
    }, sC = !1, oC = null;
    oC = /* @__PURE__ */ new Set();
    var tu = !1, Mn = !1, bE = !1, rC = typeof WeakSet == "function" ? WeakSet : Set, ci = null, ym = null, pm = null, Yn = null, jl = !1, Fs = null, w0 = 8192, EM = {
      getCacheForType: function(e) {
        var n = Ke(Wn), s = n.data.get(e);
        return s === void 0 && (s = e(), n.data.set(e, s)), s;
      },
      getOwner: function() {
        return ol;
      }
    };
    if (typeof Symbol == "function" && Symbol.for) {
      var D0 = Symbol.for;
      D0("selector.component"), D0("selector.has_pseudo_class"), D0("selector.role"), D0("selector.test_id"), D0("selector.text");
    }
    var bM = [], TM = typeof WeakMap == "function" ? WeakMap : Map, da = 0, fl = 2, Ks = 4, eu = 0, O0 = 1, _m = 2, TE = 3, wh = 4, i1 = 6, uC = 5, ze = da, Pe = null, he = null, de = 0, Bl = 0, L0 = 1, Dh = 2, z0 = 3, cC = 4, xE = 5, vm = 6, N0 = 7, CE = 8, Oh = 9, Ye = Bl, ga = null, Ac = !1, Sm = !1, RE = !1, Po = 0, En = eu, Mc = 0, wc = 0, AE = 0, ma = 0, Lh = 0, U0 = null, hl = null, l1 = !1, ME = 0, fC = 300, a1 = 1 / 0, hC = 500, G0 = null, Dc = null, xM = 0, CM = 1, RM = 2, zh = 0, dC = 1, gC = 2, mC = 3, AM = 4, wE = 5, bi = 0, Oc = null, Em = null, Lc = 0, DE = 0, OE = null, yC = null, MM = 50, H0 = 0, LE = null, zE = !1, s1 = !1, wM = 50, Nh = 0, Y0 = null, bm = !1, o1 = null, pC = !1, _C = /* @__PURE__ */ new Set(), DM = {}, r1 = null, Tm = null, NE = !1, UE = !1, u1 = !1, GE = !1, Uh = 0, HE = {};
    (function() {
      for (var e = 0; e < tE.length; e++) {
        var n = tE[e], s = n.toLowerCase();
        n = n[0].toUpperCase() + n.slice(1), Jl(s, "on" + n);
      }
      Jl(nx, "onAnimationEnd"), Jl(ix, "onAnimationIteration"), Jl(lx, "onAnimationStart"), Jl("dblclick", "onDoubleClick"), Jl("focusin", "onFocus"), Jl("focusout", "onBlur"), Jl(oM, "onTransitionRun"), Jl(rM, "onTransitionStart"), Jl(uM, "onTransitionCancel"), Jl(ax, "onTransitionEnd");
    })(), Pi("onMouseEnter", ["mouseout", "mouseover"]), Pi("onMouseLeave", ["mouseout", "mouseover"]), Pi("onPointerEnter", ["pointerout", "pointerover"]), Pi("onPointerLeave", ["pointerout", "pointerover"]), El(
      "onChange",
      "change click focusin focusout input keydown keyup selectionchange".split(
        " "
      )
    ), El(
      "onSelect",
      "focusout contextmenu dragend focusin keydown keyup mousedown mouseup selectionchange".split(
        " "
      )
    ), El("onBeforeInput", [
      "compositionend",
      "keypress",
      "textInput",
      "paste"
    ]), El(
      "onCompositionEnd",
      "compositionend focusout keydown keypress keyup mousedown".split(" ")
    ), El(
      "onCompositionStart",
      "compositionstart focusout keydown keypress keyup mousedown".split(" ")
    ), El(
      "onCompositionUpdate",
      "compositionupdate focusout keydown keypress keyup mousedown".split(" ")
    );
    var j0 = "abort canplay canplaythrough durationchange emptied encrypted ended error loadeddata loadedmetadata loadstart pause play playing progress ratechange resize seeked seeking stalled suspend timeupdate volumechange waiting".split(
      " "
    ), YE = new Set(
      "beforetoggle cancel close invalid load scroll scrollend toggle".split(" ").concat(j0)
    ), c1 = "_reactListening" + Math.random().toString(36).slice(2), vC = !1, SC = !1, f1 = !1, EC = !1, h1 = !1, d1 = !1, bC = !1, g1 = {}, OM = /\r\n?/g, LM = /\u0000|\uFFFD/g, Gh = "http://www.w3.org/1999/xlink", jE = "http://www.w3.org/XML/1998/namespace", zM = "javascript:throw new Error('React form unexpectedly submitted.')", NM = "suppressHydrationWarning", m1 = "$", y1 = "/$", nu = "$?", B0 = "$!", UM = 1, GM = 2, HM = 4, BE = "F!", TC = "F", xC = "complete", YM = "style", iu = 0, xm = 1, p1 = 2, IE = null, XE = null, CC = { dialog: !0, webview: !0 }, kE = null, RC = typeof setTimeout == "function" ? setTimeout : void 0, jM = typeof clearTimeout == "function" ? clearTimeout : void 0, Hh = -1, AC = typeof Promise == "function" ? Promise : void 0, BM = typeof queueMicrotask == "function" ? queueMicrotask : typeof AC < "u" ? function(e) {
      return AC.resolve(null).then(e).catch(Dp);
    } : RC, VE = null, Yh = 0, I0 = 1, MC = 2, wC = 3, rs = 4, us = /* @__PURE__ */ new Map(), DC = /* @__PURE__ */ new Set(), lu = Vt.d;
    Vt.d = {
      f: function() {
        var e = lu.f(), n = zr();
        return e || n;
      },
      r: function(e) {
        var n = ni(e);
        n !== null && n.tag === 5 && n.type === "form" ? Iy(n) : lu.r(e);
      },
      D: function(e) {
        lu.D(e), Sv("dns-prefetch", e, null);
      },
      C: function(e, n) {
        lu.C(e, n), Sv("preconnect", e, n);
      },
      L: function(e, n, s) {
        lu.L(e, n, s);
        var f = Cm;
        if (f && e && n) {
          var m = 'link[rel="preload"][as="' + Ji(n) + '"]';
          n === "image" && s && s.imageSrcSet ? (m += '[imagesrcset="' + Ji(
            s.imageSrcSet
          ) + '"]', typeof s.imageSizes == "string" && (m += '[imagesizes="' + Ji(
            s.imageSizes
          ) + '"]')) : m += '[href="' + Ji(e) + '"]';
          var p = m;
          switch (n) {
            case "style":
              p = No(e);
              break;
            case "script":
              p = Br(e);
          }
          us.has(p) || (e = ee(
            {
              rel: "preload",
              href: n === "image" && s && s.imageSrcSet ? void 0 : e,
              as: n
            },
            s
          ), us.set(p, e), f.querySelector(m) !== null || n === "style" && f.querySelector(
            Ka(p)
          ) || n === "script" && f.querySelector(Ir(p)) || (n = f.createElement("link"), cn(n, "link", e), _n(n), f.head.appendChild(n)));
        }
      },
      m: function(e, n) {
        lu.m(e, n);
        var s = Cm;
        if (s && e) {
          var f = n && typeof n.as == "string" ? n.as : "script", m = 'link[rel="modulepreload"][as="' + Ji(f) + '"][href="' + Ji(e) + '"]', p = m;
          switch (f) {
            case "audioworklet":
            case "paintworklet":
            case "serviceworker":
            case "sharedworker":
            case "worker":
            case "script":
              p = Br(e);
          }
          if (!us.has(p) && (e = ee({ rel: "modulepreload", href: e }, n), us.set(p, e), s.querySelector(m) === null)) {
            switch (f) {
              case "audioworklet":
              case "paintworklet":
              case "serviceworker":
              case "sharedworker":
              case "worker":
              case "script":
                if (s.querySelector(Ir(p)))
                  return;
            }
            f = s.createElement("link"), cn(f, "link", e), _n(f), s.head.appendChild(f);
          }
        }
      },
      X: function(e, n) {
        lu.X(e, n);
        var s = Cm;
        if (s && e) {
          var f = Ki(s).hoistableScripts, m = Br(e), p = f.get(m);
          p || (p = s.querySelector(
            Ir(m)
          ), p || (e = ee({ src: e, async: !0 }, n), (n = us.get(m)) && Yp(e, n), p = s.createElement("script"), _n(p), cn(p, "link", e), s.head.appendChild(p)), p = {
            type: "script",
            instance: p,
            count: 1,
            state: null
          }, f.set(m, p));
        }
      },
      S: function(e, n, s) {
        lu.S(e, n, s);
        var f = Cm;
        if (f && e) {
          var m = Ki(f).hoistableStyles, p = No(e);
          n = n || "default";
          var T = m.get(p);
          if (!T) {
            var C = { loading: Yh, preload: null };
            if (T = f.querySelector(
              Ka(p)
            ))
              C.loading = I0 | rs;
            else {
              e = ee(
                {
                  rel: "stylesheet",
                  href: e,
                  "data-precedence": n
                },
                s
              ), (s = us.get(p)) && Hp(e, s);
              var D = T = f.createElement("link");
              _n(D), cn(D, "link", e), D._p = new Promise(function(z, tt) {
                D.onload = z, D.onerror = tt;
              }), D.addEventListener("load", function() {
                C.loading |= I0;
              }), D.addEventListener("error", function() {
                C.loading |= MC;
              }), C.loading |= rs, Mg(T, n, f);
            }
            T = {
              type: "stylesheet",
              instance: T,
              count: 1,
              state: C
            }, m.set(p, T);
          }
        }
      },
      M: function(e, n) {
        lu.M(e, n);
        var s = Cm;
        if (s && e) {
          var f = Ki(s).hoistableScripts, m = Br(e), p = f.get(m);
          p || (p = s.querySelector(
            Ir(m)
          ), p || (e = ee({ src: e, async: !0, type: "module" }, n), (n = us.get(m)) && Yp(e, n), p = s.createElement("script"), _n(p), cn(p, "link", e), s.head.appendChild(p)), p = {
            type: "script",
            instance: p,
            count: 1,
            state: null
          }, f.set(m, p));
        }
      }
    };
    var Cm = typeof document > "u" ? null : document, _1 = null, X0 = null, qE = null, v1 = null, jh = kS, k0 = {
      $$typeof: Ul,
      Provider: null,
      Consumer: null,
      _currentValue: jh,
      _currentValue2: jh,
      _threadCount: 0
    }, OC = "%c%s%c ", LC = "background: #e6e6e6;background: light-dark(rgba(0,0,0,0.1), rgba(255,255,255,0.25));color: #000000;color: light-dark(#000000, #ffffff);border-radius: 2px", zC = "", S1 = " ", IM = Function.prototype.bind, NC = !1, UC = null, GC = null, HC = null, YC = null, jC = null, BC = null, IC = null, XC = null, kC = null;
    UC = function(e, n, s, f) {
      n = r(e, n), n !== null && (s = t(n.memoizedState, s, 0, f), n.memoizedState = s, n.baseState = s, e.memoizedProps = ee({}, e.memoizedProps), s = Mi(e, 2), s !== null && rn(s, e, 2));
    }, GC = function(e, n, s) {
      n = r(e, n), n !== null && (s = u(n.memoizedState, s, 0), n.memoizedState = s, n.baseState = s, e.memoizedProps = ee({}, e.memoizedProps), s = Mi(e, 2), s !== null && rn(s, e, 2));
    }, HC = function(e, n, s, f) {
      n = r(e, n), n !== null && (s = i(n.memoizedState, s, f), n.memoizedState = s, n.baseState = s, e.memoizedProps = ee({}, e.memoizedProps), s = Mi(e, 2), s !== null && rn(s, e, 2));
    }, YC = function(e, n, s) {
      e.pendingProps = t(e.memoizedProps, n, 0, s), e.alternate && (e.alternate.pendingProps = e.pendingProps), n = Mi(e, 2), n !== null && rn(n, e, 2);
    }, jC = function(e, n) {
      e.pendingProps = u(e.memoizedProps, n, 0), e.alternate && (e.alternate.pendingProps = e.pendingProps), n = Mi(e, 2), n !== null && rn(n, e, 2);
    }, BC = function(e, n, s) {
      e.pendingProps = i(
        e.memoizedProps,
        n,
        s
      ), e.alternate && (e.alternate.pendingProps = e.pendingProps), n = Mi(e, 2), n !== null && rn(n, e, 2);
    }, IC = function(e) {
      var n = Mi(e, 2);
      n !== null && rn(n, e, 2);
    }, XC = function(e) {
      g = e;
    }, kC = function(e) {
      h = e;
    };
    var E1 = !0, b1 = null, ZE = !1, zc = null, Nc = null, Uc = null, V0 = /* @__PURE__ */ new Map(), q0 = /* @__PURE__ */ new Map(), Gc = [], XM = "mousedown mouseup touchcancel touchend touchstart auxclick dblclick pointercancel pointerdown pointerup dragend dragstart drop compositionend compositionstart keydown keypress keyup input textInput copy cut paste click change contextmenu reset".split(
      " "
    ), T1 = null;
    if (sh.prototype.render = Ng.prototype.render = function(e) {
      var n = this._internalRoot;
      if (n === null) throw Error("Cannot update an unmounted root.");
      var s = arguments;
      typeof s[1] == "function" ? console.error(
        "does not support the second callback argument. To execute a side effect after rendering, declare it in a component body with useEffect()."
      ) : N(s[1]) ? console.error(
        "You passed a container to the second argument of root.render(...). You don't need to pass it again since you already passed it to create the root."
      ) : typeof s[1] < "u" && console.error(
        "You passed a second argument to root.render(...) but it only accepts one argument."
      ), s = e;
      var f = n.current, m = Ui(f);
      Le(f, m, s, n, null, null);
    }, sh.prototype.unmount = Ng.prototype.unmount = function() {
      var e = arguments;
      if (typeof e[0] == "function" && console.error(
        "does not support a callback argument. To execute a side effect after rendering, declare it in a component body with useEffect()."
      ), e = this._internalRoot, e !== null) {
        this._internalRoot = null;
        var n = e.containerInfo;
        (ze & (fl | Ks)) !== da && console.error(
          "Attempted to synchronously unmount a root while React was already rendering. React cannot finish unmounting the root until the current render has completed, which may lead to a race condition."
        ), Le(e.current, 2, null, e, null, null), zr(), n[Xo] = null;
      }
    }, sh.prototype.unstable_scheduleHydration = function(e) {
      if (e) {
        var n = Tu();
        e = { blockedOn: null, target: e, priority: n };
        for (var s = 0; s < Gc.length && n !== 0 && n < Gc[s].priority; s++) ;
        Gc.splice(s, 0, e), s === 0 && Rv(e);
      }
    }, function() {
      var e = oh.version;
      if (e !== "19.1.1")
        throw Error(
          `Incompatible React versions: The "react" and "react-dom" packages must have the exact same version. Instead got:
  - react:      ` + (e + `
  - react-dom:  19.1.1
Learn more: https://react.dev/warnings/version-mismatch`)
        );
    }(), typeof Map == "function" && Map.prototype != null && typeof Map.prototype.forEach == "function" && typeof Set == "function" && Set.prototype != null && typeof Set.prototype.clear == "function" && typeof Set.prototype.forEach == "function" || console.error(
      "React depends on Map and Set built-in types. Make sure that you load a polyfill in older browsers. https://react.dev/link/react-polyfills"
    ), Vt.findDOMNode = function(e) {
      var n = e._reactInternals;
      if (n === void 0)
        throw typeof e.render == "function" ? Error("Unable to find node on an unmounted component.") : (e = Object.keys(e).join(","), Error(
          "Argument appears to not be a ReactComponent. Keys: " + e
        ));
      return e = X(n), e = e !== null ? Z(e) : null, e = e === null ? null : e.stateNode, e;
    }, !function() {
      var e = {
        bundleType: 1,
        version: "19.1.1",
        rendererPackageName: "react-dom",
        currentDispatcherRef: lt,
        reconcilerVersion: "19.1.1"
      };
      return e.overrideHookState = UC, e.overrideHookStateDeletePath = GC, e.overrideHookStateRenamePath = HC, e.overrideProps = YC, e.overridePropsDeletePath = jC, e.overridePropsRenamePath = BC, e.scheduleUpdate = IC, e.setErrorHandler = XC, e.setSuspenseHandler = kC, e.scheduleRefresh = L, e.scheduleRoot = w, e.setRefreshHandler = H, e.getCurrentFiber = jS, e.getLaneLabelMap = BS, e.injectProfilingHooks = Ze, kt(e);
    }() && G && window.top === window.self && (-1 < navigator.userAgent.indexOf("Chrome") && navigator.userAgent.indexOf("Edge") === -1 || -1 < navigator.userAgent.indexOf("Firefox"))) {
      var VC = window.location.protocol;
      /^(https?|file):$/.test(VC) && console.info(
        "%cDownload the React DevTools for a better development experience: https://react.dev/link/react-devtools" + (VC === "file:" ? `
You might need to use a local HTTP server (instead of file://): https://react.dev/link/react-devtools-faq` : ""),
        "font-weight:bold"
      );
    }
    P0.createRoot = function(e, n) {
      if (!N(e))
        throw Error("Target container is not a DOM element.");
      wv(e);
      var s = !1, f = "", m = qy, p = lv, T = Id, C = null;
      return n != null && (n.hydrate ? console.warn(
        "hydrate through createRoot is deprecated. Use ReactDOMClient.hydrateRoot(container, <App />) instead."
      ) : typeof n == "object" && n !== null && n.$$typeof === Ho && console.error(
        `You passed a JSX element to createRoot. You probably meant to call root.render instead. Example usage:

  let root = createRoot(domContainer);
  root.render(<App />);`
      ), n.unstable_strictMode === !0 && (s = !0), n.identifierPrefix !== void 0 && (f = n.identifierPrefix), n.onUncaughtError !== void 0 && (m = n.onUncaughtError), n.onCaughtError !== void 0 && (p = n.onCaughtError), n.onRecoverableError !== void 0 && (T = n.onRecoverableError), n.unstable_transitionCallbacks !== void 0 && (C = n.unstable_transitionCallbacks)), n = Ip(
        e,
        1,
        !1,
        null,
        null,
        s,
        f,
        m,
        p,
        T,
        C,
        null
      ), e[Xo] = n.current, Tp(e), new Ng(n);
    }, P0.hydrateRoot = function(e, n, s) {
      if (!N(e))
        throw Error("Target container is not a DOM element.");
      wv(e), n === void 0 && console.error(
        "Must provide initial children as second argument to hydrateRoot. Example usage: hydrateRoot(domContainer, <App />)"
      );
      var f = !1, m = "", p = qy, T = lv, C = Id, D = null, z = null;
      return s != null && (s.unstable_strictMode === !0 && (f = !0), s.identifierPrefix !== void 0 && (m = s.identifierPrefix), s.onUncaughtError !== void 0 && (p = s.onUncaughtError), s.onCaughtError !== void 0 && (T = s.onCaughtError), s.onRecoverableError !== void 0 && (C = s.onRecoverableError), s.unstable_transitionCallbacks !== void 0 && (D = s.unstable_transitionCallbacks), s.formState !== void 0 && (z = s.formState)), n = Ip(
        e,
        1,
        !0,
        n,
        s ?? null,
        f,
        m,
        p,
        T,
        C,
        D,
        z
      ), n.context = Xp(null), s = n.current, f = Ui(s), f = pn(f), m = Aa(f), m.callback = null, ia(s, m, f), s = f, n.current.lanes = s, Fe(n, s), Ll(n), e[Xo] = n.current, Tp(e), new sh(n);
    }, P0.version = "19.1.1", typeof __REACT_DEVTOOLS_GLOBAL_HOOK__ < "u" && typeof __REACT_DEVTOOLS_GLOBAL_HOOK__.registerInternalModuleStop == "function" && __REACT_DEVTOOLS_GLOBAL_HOOK__.registerInternalModuleStop(Error());
  }()), P0;
}
function f2() {
  if (!(typeof __REACT_DEVTOOLS_GLOBAL_HOOK__ > "u" || typeof __REACT_DEVTOOLS_GLOBAL_HOOK__.checkDCE != "function")) {
    if (process.env.NODE_ENV !== "production")
      throw new Error("^_^");
    try {
      __REACT_DEVTOOLS_GLOBAL_HOOK__.checkDCE(f2);
    } catch (r) {
      console.error(r);
    }
  }
}
process.env.NODE_ENV === "production" ? (f2(), Mb.exports = WM()) : Mb.exports = JM();
var $M = Mb.exports;
const tw = /* @__PURE__ */ ZM($M), $C = (r) => {
  let t;
  const i = /* @__PURE__ */ new Set(), a = (b, E) => {
    const x = typeof b == "function" ? b(t) : b;
    if (!Object.is(x, t)) {
      const R = t;
      t = E ?? (typeof x != "object" || x === null) ? x : Object.assign({}, t, x), i.forEach((w) => w(t, R));
    }
  }, u = () => t, y = { setState: a, getState: u, getInitialState: () => _, subscribe: (b) => (i.add(b), () => i.delete(b)) }, _ = t = r(a, u, y);
  return y;
}, ew = (r) => r ? $C(r) : $C, nw = (r) => r;
function iw(r, t = nw) {
  const i = ql.useSyncExternalStore(
    r.subscribe,
    ql.useCallback(() => t(r.getState()), [r, t]),
    ql.useCallback(() => t(r.getInitialState()), [r, t])
  );
  return ql.useDebugValue(i), i;
}
const tR = (r) => {
  const t = ew(r), i = (a) => iw(t, a);
  return Object.assign(i, t), i;
}, lw = (r) => r ? tR(r) : tR, jn = {
  /**
   * Triggered when an item is added to the collection.
   * @event module:ol/Collection.CollectionEvent#add
   * @api
   */
  ADD: "add",
  /**
   * Triggered when an item is removed from the collection.
   * @event module:ol/Collection.CollectionEvent#remove
   * @api
   */
  REMOVE: "remove"
}, qm = {
  /**
   * Triggered when a property is changed.
   * @event module:ol/Object.ObjectEvent#propertychange
   * @api
   */
  PROPERTYCHANGE: "propertychange"
}, $t = {
  /**
   * Generic change event. Triggered when the revision counter is increased.
   * @event module:ol/events/Event~BaseEvent#change
   * @api
   */
  CHANGE: "change",
  /**
   * Generic error event. Triggered when an error occurs.
   * @event module:ol/events/Event~BaseEvent#error
   * @api
   */
  ERROR: "error",
  CONTEXTMENU: "contextmenu",
  CLICK: "click",
  DBLCLICK: "dblclick",
  KEYDOWN: "keydown",
  KEYPRESS: "keypress",
  LOAD: "load",
  TOUCHMOVE: "touchmove",
  WHEEL: "wheel"
};
class sS {
  constructor() {
    this.disposed = !1;
  }
  /**
   * Clean up.
   */
  dispose() {
    this.disposed || (this.disposed = !0, this.disposeInternal());
  }
  /**
   * Extension point for disposable objects.
   * @protected
   */
  disposeInternal() {
  }
}
function aw(r, t, i) {
  let a, u;
  i = i || hu;
  let h = 0, g = r.length, y = !1;
  for (; h < g; )
    a = h + (g - h >> 1), u = +i(r[a], t), u < 0 ? h = a + 1 : (g = a, y = !u);
  return y ? h : ~h;
}
function hu(r, t) {
  return r > t ? 1 : r < t ? -1 : 0;
}
function sw(r, t) {
  return r < t ? 1 : r > t ? -1 : 0;
}
function tT(r, t, i) {
  if (r[0] <= t)
    return 0;
  const a = r.length;
  if (t <= r[a - 1])
    return a - 1;
  if (typeof i == "function") {
    for (let u = 1; u < a; ++u) {
      const h = r[u];
      if (h === t)
        return u;
      if (h < t)
        return i(t, r[u - 1], h) > 0 ? u - 1 : u;
    }
    return a - 1;
  }
  if (i > 0) {
    for (let u = 1; u < a; ++u)
      if (r[u] < t)
        return u - 1;
    return a - 1;
  }
  if (i < 0) {
    for (let u = 1; u < a; ++u)
      if (r[u] <= t)
        return u;
    return a - 1;
  }
  for (let u = 1; u < a; ++u) {
    if (r[u] == t)
      return u;
    if (r[u] < t)
      return r[u - 1] - t < t - r[u] ? u - 1 : u;
  }
  return a - 1;
}
function ow(r, t, i) {
  for (; t < i; ) {
    const a = r[t];
    r[t] = r[i], r[i] = a, ++t, --i;
  }
}
function ds(r, t) {
  const i = Array.isArray(t) ? t : [t], a = i.length;
  for (let u = 0; u < a; u++)
    r[r.length] = i[u];
}
function Wc(r, t) {
  const i = r.length;
  if (i !== t.length)
    return !1;
  for (let a = 0; a < i; a++)
    if (r[a] !== t[a])
      return !1;
  return !0;
}
function rw(r, t, i) {
  const a = t || hu;
  return r.every(function(u, h) {
    if (h === 0)
      return !0;
    const g = a(r[h - 1], u);
    return !(g > 0 || g === 0);
  });
}
function _u() {
  return !0;
}
function td() {
  return !1;
}
function h_() {
}
function h2(r) {
  let t, i, a;
  return function() {
    const u = Array.prototype.slice.call(arguments);
    return (!i || this !== a || !Wc(u, i)) && (a = this, i = u, t = r.apply(this, arguments)), t;
  };
}
function uw(r) {
  function t() {
    let i;
    try {
      i = r();
    } catch (a) {
      return Promise.reject(a);
    }
    return i instanceof Promise ? i : Promise.resolve(i);
  }
  return t();
}
function Jm(r) {
  for (const t in r)
    delete r[t];
}
function Zm(r) {
  let t;
  for (t in r)
    return !1;
  return !t;
}
class ys {
  /**
   * @param {string} type Type.
   */
  constructor(t) {
    this.propagationStopped, this.defaultPrevented, this.type = t, this.target = null;
  }
  /**
   * Prevent default. This means that no emulated `click`, `singleclick` or `doubleclick` events
   * will be fired.
   * @api
   */
  preventDefault() {
    this.defaultPrevented = !0;
  }
  /**
   * Stop event propagation.
   * @api
   */
  stopPropagation() {
    this.propagationStopped = !0;
  }
}
class oS extends sS {
  /**
   * @param {*} [target] Default event target for dispatched events.
   */
  constructor(t) {
    super(), this.eventTarget_ = t, this.pendingRemovals_ = null, this.dispatching_ = null, this.listeners_ = null;
  }
  /**
   * @param {string} type Type.
   * @param {import("../events.js").Listener} listener Listener.
   */
  addEventListener(t, i) {
    if (!t || !i)
      return;
    const a = this.listeners_ || (this.listeners_ = {}), u = a[t] || (a[t] = []);
    u.includes(i) || u.push(i);
  }
  /**
   * Dispatches an event and calls all listeners listening for events
   * of this type. The event parameter can either be a string or an
   * Object with a `type` property.
   *
   * @param {import("./Event.js").default|string} event Event object.
   * @return {boolean|undefined} `false` if anyone called preventDefault on the
   *     event object or if any of the listeners returned false.
   * @api
   */
  dispatchEvent(t) {
    const i = typeof t == "string", a = i ? t : t.type, u = this.listeners_ && this.listeners_[a];
    if (!u)
      return;
    const h = i ? new ys(t) : (
      /** @type {Event} */
      t
    );
    h.target || (h.target = this.eventTarget_ || this);
    const g = this.dispatching_ || (this.dispatching_ = {}), y = this.pendingRemovals_ || (this.pendingRemovals_ = {});
    a in g || (g[a] = 0, y[a] = 0), ++g[a];
    let _;
    for (let b = 0, E = u.length; b < E; ++b)
      if ("handleEvent" in u[b] ? _ = /** @type {import("../events.js").ListenerObject} */
      u[b].handleEvent(h) : _ = /** @type {import("../events.js").ListenerFunction} */
      u[b].call(this, h), _ === !1 || h.propagationStopped) {
        _ = !1;
        break;
      }
    if (--g[a] === 0) {
      let b = y[a];
      for (delete y[a]; b--; )
        this.removeEventListener(a, h_);
      delete g[a];
    }
    return _;
  }
  /**
   * Clean up.
   * @override
   */
  disposeInternal() {
    this.listeners_ && Jm(this.listeners_);
  }
  /**
   * Get the listeners for a specified event type. Listeners are returned in the
   * order that they will be called in.
   *
   * @param {string} type Type.
   * @return {Array<import("../events.js").Listener>|undefined} Listeners.
   */
  getListeners(t) {
    return this.listeners_ && this.listeners_[t] || void 0;
  }
  /**
   * @param {string} [type] Type. If not provided,
   *     `true` will be returned if this event target has any listeners.
   * @return {boolean} Has listeners.
   */
  hasListener(t) {
    return this.listeners_ ? t ? t in this.listeners_ : Object.keys(this.listeners_).length > 0 : !1;
  }
  /**
   * @param {string} type Type.
   * @param {import("../events.js").Listener} listener Listener.
   */
  removeEventListener(t, i) {
    if (!this.listeners_)
      return;
    const a = this.listeners_[t];
    if (!a)
      return;
    const u = a.indexOf(i);
    u !== -1 && (this.pendingRemovals_ && t in this.pendingRemovals_ ? (a[u] = h_, ++this.pendingRemovals_[t]) : (a.splice(u, 1), a.length === 0 && delete this.listeners_[t]));
  }
}
function ge(r, t, i, a, u) {
  if (u) {
    const g = i;
    i = function(y) {
      return r.removeEventListener(t, i), g.call(a ?? this, y);
    };
  } else a && a !== r && (i = i.bind(a));
  const h = {
    target: r,
    type: t,
    listener: i
  };
  return r.addEventListener(t, i), h;
}
function k1(r, t, i, a) {
  return ge(r, t, i, a, !0);
}
function qe(r) {
  r && r.target && (r.target.removeEventListener(r.type, r.listener), Jm(r));
}
class A_ extends oS {
  constructor() {
    super(), this.on = /** @type {ObservableOnSignature<import("./events").EventsKey>} */
    this.onInternal, this.once = /** @type {ObservableOnSignature<import("./events").EventsKey>} */
    this.onceInternal, this.un = /** @type {ObservableOnSignature<void>} */
    this.unInternal, this.revision_ = 0;
  }
  /**
   * Increases the revision counter and dispatches a 'change' event.
   * @api
   */
  changed() {
    ++this.revision_, this.dispatchEvent($t.CHANGE);
  }
  /**
   * Get the version number for this object.  Each time the object is modified,
   * its version number will be incremented.
   * @return {number} Revision.
   * @api
   */
  getRevision() {
    return this.revision_;
  }
  /**
   * @param {string|Array<string>} type Type.
   * @param {function((Event|import("./events/Event").default)): ?} listener Listener.
   * @return {import("./events.js").EventsKey|Array<import("./events.js").EventsKey>} Event key.
   * @protected
   */
  onInternal(t, i) {
    if (Array.isArray(t)) {
      const a = t.length, u = new Array(a);
      for (let h = 0; h < a; ++h)
        u[h] = ge(this, t[h], i);
      return u;
    }
    return ge(
      this,
      /** @type {string} */
      t,
      i
    );
  }
  /**
   * @param {string|Array<string>} type Type.
   * @param {function((Event|import("./events/Event").default)): ?} listener Listener.
   * @return {import("./events.js").EventsKey|Array<import("./events.js").EventsKey>} Event key.
   * @protected
   */
  onceInternal(t, i) {
    let a;
    if (Array.isArray(t)) {
      const u = t.length;
      a = new Array(u);
      for (let h = 0; h < u; ++h)
        a[h] = k1(this, t[h], i);
    } else
      a = k1(
        this,
        /** @type {string} */
        t,
        i
      );
    return i.ol_key = a, a;
  }
  /**
   * Unlisten for a certain type of event.
   * @param {string|Array<string>} type Type.
   * @param {function((Event|import("./events/Event").default)): ?} listener Listener.
   * @protected
   */
  unInternal(t, i) {
    const a = (
      /** @type {Object} */
      i.ol_key
    );
    if (a)
      cw(a);
    else if (Array.isArray(t))
      for (let u = 0, h = t.length; u < h; ++u)
        this.removeEventListener(t[u], i);
    else
      this.removeEventListener(t, i);
  }
}
A_.prototype.on;
A_.prototype.once;
A_.prototype.un;
function cw(r) {
  if (Array.isArray(r))
    for (let t = 0, i = r.length; t < i; ++t)
      qe(r[t]);
  else
    qe(
      /** @type {import("./events.js").EventsKey} */
      r
    );
}
function we() {
  throw new Error("Unimplemented abstract method.");
}
let fw = 0;
function ue(r) {
  return r.ol_uid || (r.ol_uid = String(++fw));
}
class eR extends ys {
  /**
   * @param {string} type The event type.
   * @param {string} key The property name.
   * @param {*} oldValue The old value for `key`.
   */
  constructor(t, i, a) {
    super(t), this.key = i, this.oldValue = a;
  }
}
class tr extends A_ {
  /**
   * @param {Object<string, *>} [values] An object with key-value pairs.
   */
  constructor(t) {
    super(), this.on, this.once, this.un, ue(this), this.values_ = null, t !== void 0 && this.setProperties(t);
  }
  /**
   * Gets a value.
   * @param {string} key Key name.
   * @return {*} Value.
   * @api
   */
  get(t) {
    let i;
    return this.values_ && this.values_.hasOwnProperty(t) && (i = this.values_[t]), i;
  }
  /**
   * Get a list of object property names.
   * @return {Array<string>} List of property names.
   * @api
   */
  getKeys() {
    return this.values_ && Object.keys(this.values_) || [];
  }
  /**
   * Get an object of all property names and values.
   * @return {Object<string, *>} Object.
   * @api
   */
  getProperties() {
    return this.values_ && Object.assign({}, this.values_) || {};
  }
  /**
   * Get an object of all property names and values.
   * @return {Object<string, *>?} Object.
   */
  getPropertiesInternal() {
    return this.values_;
  }
  /**
   * @return {boolean} The object has properties.
   */
  hasProperties() {
    return !!this.values_;
  }
  /**
   * @param {string} key Key name.
   * @param {*} oldValue Old value.
   */
  notify(t, i) {
    let a;
    a = `change:${t}`, this.hasListener(a) && this.dispatchEvent(new eR(a, t, i)), a = qm.PROPERTYCHANGE, this.hasListener(a) && this.dispatchEvent(new eR(a, t, i));
  }
  /**
   * @param {string} key Key name.
   * @param {import("./events.js").Listener} listener Listener.
   */
  addChangeListener(t, i) {
    this.addEventListener(`change:${t}`, i);
  }
  /**
   * @param {string} key Key name.
   * @param {import("./events.js").Listener} listener Listener.
   */
  removeChangeListener(t, i) {
    this.removeEventListener(`change:${t}`, i);
  }
  /**
   * Sets a value.
   * @param {string} key Key name.
   * @param {*} value Value.
   * @param {boolean} [silent] Update without triggering an event.
   * @api
   */
  set(t, i, a) {
    const u = this.values_ || (this.values_ = {});
    if (a)
      u[t] = i;
    else {
      const h = u[t];
      u[t] = i, h !== i && this.notify(t, h);
    }
  }
  /**
   * Sets a collection of key-value pairs.  Note that this changes any existing
   * properties and adds new ones (it does not remove any existing properties).
   * @param {Object<string, *>} values Values.
   * @param {boolean} [silent] Update without triggering an event.
   * @api
   */
  setProperties(t, i) {
    for (const a in t)
      this.set(a, t[a], i);
  }
  /**
   * Apply any properties from another object without triggering events.
   * @param {BaseObject} source The source object.
   * @protected
   */
  applyProperties(t) {
    t.values_ && Object.assign(this.values_ || (this.values_ = {}), t.values_);
  }
  /**
   * Unsets a property.
   * @param {string} key Key name.
   * @param {boolean} [silent] Unset without triggering an event.
   * @api
   */
  unset(t, i) {
    if (this.values_ && t in this.values_) {
      const a = this.values_[t];
      delete this.values_[t], Zm(this.values_) && (this.values_ = null), i || this.notify(t, a);
    }
  }
}
const nR = {
  LENGTH: "length"
};
class C1 extends ys {
  /**
   * @param {import("./CollectionEventType.js").default} type Type.
   * @param {T} element Element.
   * @param {number} index The index of the added or removed element.
   */
  constructor(t, i, a) {
    super(t), this.element = i, this.index = a;
  }
}
class Js extends tr {
  /**
   * @param {Array<T>} [array] Array.
   * @param {Options} [options] Collection options.
   */
  constructor(t, i) {
    if (super(), this.on, this.once, this.un, i = i || {}, this.unique_ = !!i.unique, this.array_ = t || [], this.unique_)
      for (let a = 0, u = this.array_.length; a < u; ++a)
        this.assertUnique_(this.array_[a], a);
    this.updateLength_();
  }
  /**
   * Remove all elements from the collection.
   * @api
   */
  clear() {
    for (; this.getLength() > 0; )
      this.pop();
  }
  /**
   * Add elements to the collection.  This pushes each item in the provided array
   * to the end of the collection.
   * @param {!Array<T>} arr Array.
   * @return {Collection<T>} This collection.
   * @api
   */
  extend(t) {
    for (let i = 0, a = t.length; i < a; ++i)
      this.push(t[i]);
    return this;
  }
  /**
   * Iterate over each element, calling the provided callback.
   * @param {function(T, number, Array<T>): *} f The function to call
   *     for every element. This function takes 3 arguments (the element, the
   *     index and the array). The return value is ignored.
   * @api
   */
  forEach(t) {
    const i = this.array_;
    for (let a = 0, u = i.length; a < u; ++a)
      t(i[a], a, i);
  }
  /**
   * Get a reference to the underlying Array object. Warning: if the array
   * is mutated, no events will be dispatched by the collection, and the
   * collection's "length" property won't be in sync with the actual length
   * of the array.
   * @return {!Array<T>} Array.
   * @api
   */
  getArray() {
    return this.array_;
  }
  /**
   * Get the element at the provided index.
   * @param {number} index Index.
   * @return {T} Element.
   * @api
   */
  item(t) {
    return this.array_[t];
  }
  /**
   * Get the length of this collection.
   * @return {number} The length of the array.
   * @observable
   * @api
   */
  getLength() {
    return this.get(nR.LENGTH);
  }
  /**
   * Insert an element at the provided index.
   * @param {number} index Index.
   * @param {T} elem Element.
   * @api
   */
  insertAt(t, i) {
    if (t < 0 || t > this.getLength())
      throw new Error("Index out of bounds: " + t);
    this.unique_ && this.assertUnique_(i), this.array_.splice(t, 0, i), this.updateLength_(), this.dispatchEvent(
      new C1(jn.ADD, i, t)
    );
  }
  /**
   * Remove the last element of the collection and return it.
   * Return `undefined` if the collection is empty.
   * @return {T|undefined} Element.
   * @api
   */
  pop() {
    return this.removeAt(this.getLength() - 1);
  }
  /**
   * Insert the provided element at the end of the collection.
   * @param {T} elem Element.
   * @return {number} New length of the collection.
   * @api
   */
  push(t) {
    this.unique_ && this.assertUnique_(t);
    const i = this.getLength();
    return this.insertAt(i, t), this.getLength();
  }
  /**
   * Remove the first occurrence of an element from the collection.
   * @param {T} elem Element.
   * @return {T|undefined} The removed element or undefined if none found.
   * @api
   */
  remove(t) {
    const i = this.array_;
    for (let a = 0, u = i.length; a < u; ++a)
      if (i[a] === t)
        return this.removeAt(a);
  }
  /**
   * Remove the element at the provided index and return it.
   * Return `undefined` if the collection does not contain this index.
   * @param {number} index Index.
   * @return {T|undefined} Value.
   * @api
   */
  removeAt(t) {
    if (t < 0 || t >= this.getLength())
      return;
    const i = this.array_[t];
    return this.array_.splice(t, 1), this.updateLength_(), this.dispatchEvent(
      /** @type {CollectionEvent<T>} */
      new C1(jn.REMOVE, i, t)
    ), i;
  }
  /**
   * Set the element at the provided index.
   * @param {number} index Index.
   * @param {T} elem Element.
   * @api
   */
  setAt(t, i) {
    const a = this.getLength();
    if (t >= a) {
      this.insertAt(t, i);
      return;
    }
    if (t < 0)
      throw new Error("Index out of bounds: " + t);
    this.unique_ && this.assertUnique_(i, t);
    const u = this.array_[t];
    this.array_[t] = i, this.dispatchEvent(
      /** @type {CollectionEvent<T>} */
      new C1(jn.REMOVE, u, t)
    ), this.dispatchEvent(
      /** @type {CollectionEvent<T>} */
      new C1(jn.ADD, i, t)
    );
  }
  /**
   * @private
   */
  updateLength_() {
    this.set(nR.LENGTH, this.array_.length);
  }
  /**
   * @private
   * @param {T} elem Element.
   * @param {number} [except] Optional index to ignore.
   */
  assertUnique_(t, i) {
    for (let a = 0, u = this.array_.length; a < u; ++a)
      if (this.array_[a] === t && a !== i)
        throw new Error("Duplicate item added to a unique collection");
  }
}
function je(r, t) {
  if (!r)
    throw new Error(t);
}
class cs extends tr {
  /**
   * @param {Geometry|ObjectWithGeometry<Geometry>} [geometryOrProperties]
   *     You may pass a Geometry object directly, or an object literal containing
   *     properties. If you pass an object literal, you may include a Geometry
   *     associated with a `geometry` key.
   */
  constructor(t) {
    if (super(), this.on, this.once, this.un, this.id_ = void 0, this.geometryName_ = "geometry", this.style_ = null, this.styleFunction_ = void 0, this.geometryChangeKey_ = null, this.addChangeListener(this.geometryName_, this.handleGeometryChanged_), t)
      if (typeof /** @type {?} */
      t.getSimplifiedGeometry == "function") {
        const i = (
          /** @type {Geometry} */
          t
        );
        this.setGeometry(i);
      } else {
        const i = t;
        this.setProperties(i);
      }
  }
  /**
   * Clone this feature. If the original feature has a geometry it
   * is also cloned. The feature id is not set in the clone.
   * @return {Feature<Geometry>} The clone.
   * @api
   */
  clone() {
    const t = (
      /** @type {Feature<Geometry>} */
      new cs(this.hasProperties() ? this.getProperties() : null)
    );
    t.setGeometryName(this.getGeometryName());
    const i = this.getGeometry();
    i && t.setGeometry(
      /** @type {Geometry} */
      i.clone()
    );
    const a = this.getStyle();
    return a && t.setStyle(a), t;
  }
  /**
   * Get the feature's default geometry.  A feature may have any number of named
   * geometries.  The "default" geometry (the one that is rendered by default) is
   * set when calling {@link module:ol/Feature~Feature#setGeometry}.
   * @return {Geometry|undefined} The default geometry for the feature.
   * @api
   * @observable
   */
  getGeometry() {
    return (
      /** @type {Geometry|undefined} */
      this.get(this.geometryName_)
    );
  }
  /**
   * Get the feature identifier.  This is a stable identifier for the feature and
   * is either set when reading data from a remote source or set explicitly by
   * calling {@link module:ol/Feature~Feature#setId}.
   * @return {number|string|undefined} Id.
   * @api
   */
  getId() {
    return this.id_;
  }
  /**
   * Get the name of the feature's default geometry.  By default, the default
   * geometry is named `geometry`.
   * @return {string} Get the property name associated with the default geometry
   *     for this feature.
   * @api
   */
  getGeometryName() {
    return this.geometryName_;
  }
  /**
   * Get the feature's style. Will return what was provided to the
   * {@link module:ol/Feature~Feature#setStyle} method.
   * @return {import("./style/Style.js").StyleLike|undefined} The feature style.
   * @api
   */
  getStyle() {
    return this.style_;
  }
  /**
   * Get the feature's style function.
   * @return {import("./style/Style.js").StyleFunction|undefined} Return a function
   * representing the current style of this feature.
   * @api
   */
  getStyleFunction() {
    return this.styleFunction_;
  }
  /**
   * @private
   */
  handleGeometryChange_() {
    this.changed();
  }
  /**
   * @private
   */
  handleGeometryChanged_() {
    this.geometryChangeKey_ && (qe(this.geometryChangeKey_), this.geometryChangeKey_ = null);
    const t = this.getGeometry();
    t && (this.geometryChangeKey_ = ge(
      t,
      $t.CHANGE,
      this.handleGeometryChange_,
      this
    )), this.changed();
  }
  /**
   * Set the default geometry for the feature.  This will update the property
   * with the name returned by {@link module:ol/Feature~Feature#getGeometryName}.
   * @param {Geometry|undefined} geometry The new geometry.
   * @api
   * @observable
   */
  setGeometry(t) {
    this.set(this.geometryName_, t);
  }
  /**
   * Set the style for the feature to override the layer style.  This can be a
   * single style object, an array of styles, or a function that takes a
   * resolution and returns an array of styles. To unset the feature style, call
   * `setStyle()` without arguments or a falsey value.
   * @param {import("./style/Style.js").StyleLike} [style] Style for this feature.
   * @api
   * @fires module:ol/events/Event~BaseEvent#event:change
   */
  setStyle(t) {
    this.style_ = t, this.styleFunction_ = t ? hw(t) : void 0, this.changed();
  }
  /**
   * Set the feature id.  The feature id is considered stable and may be used when
   * requesting features or comparing identifiers returned from a remote source.
   * The feature id can be used with the
   * {@link module:ol/source/Vector~VectorSource#getFeatureById} method.
   * @param {number|string|undefined} id The feature id.
   * @api
   * @fires module:ol/events/Event~BaseEvent#event:change
   */
  setId(t) {
    this.id_ = t, this.changed();
  }
  /**
   * Set the property name to be used when getting the feature's default geometry.
   * When calling {@link module:ol/Feature~Feature#getGeometry}, the value of the property with
   * this name will be returned.
   * @param {string} name The property name of the default geometry.
   * @api
   */
  setGeometryName(t) {
    this.removeChangeListener(this.geometryName_, this.handleGeometryChanged_), this.geometryName_ = t, this.addChangeListener(this.geometryName_, this.handleGeometryChanged_), this.handleGeometryChanged_();
  }
}
function hw(r) {
  if (typeof r == "function")
    return r;
  let t;
  return Array.isArray(r) ? t = r : (je(
    typeof /** @type {?} */
    r.getZIndex == "function",
    "Expected an `ol/style/Style` or an array of `ol/style/Style.js`"
  ), t = [
    /** @type {import("./style/Style.js").default} */
    r
  ]), function() {
    return t;
  };
}
const fi = {
  UNKNOWN: 0,
  INTERSECTING: 1,
  ABOVE: 2,
  RIGHT: 4,
  BELOW: 8,
  LEFT: 16
};
function Vh(r) {
  const t = _a();
  for (let i = 0, a = r.length; i < a; ++i)
    r_(t, r[i]);
  return t;
}
function dw(r, t, i) {
  const a = Math.min.apply(null, r), u = Math.min.apply(null, t), h = Math.max.apply(null, r), g = Math.max.apply(null, t);
  return $o(a, u, h, g, i);
}
function rS(r, t, i) {
  return i ? (i[0] = r[0] - t, i[1] = r[1] - t, i[2] = r[2] + t, i[3] = r[3] + t, i) : [
    r[0] - t,
    r[1] - t,
    r[2] + t,
    r[3] + t
  ];
}
function d2(r, t) {
  return t ? (t[0] = r[0], t[1] = r[1], t[2] = r[2], t[3] = r[3], t) : r.slice();
}
function ed(r, t, i) {
  let a, u;
  return t < r[0] ? a = r[0] - t : r[2] < t ? a = t - r[2] : a = 0, i < r[1] ? u = r[1] - i : r[3] < i ? u = i - r[3] : u = 0, a * a + u * u;
}
function Fm(r, t) {
  return eT(r, t[0], t[1]);
}
function e_(r, t) {
  return r[0] <= t[0] && t[2] <= r[2] && r[1] <= t[1] && t[3] <= r[3];
}
function eT(r, t, i) {
  return r[0] <= t && t <= r[2] && r[1] <= i && i <= r[3];
}
function wb(r, t) {
  const i = r[0], a = r[1], u = r[2], h = r[3], g = t[0], y = t[1];
  let _ = fi.UNKNOWN;
  return g < i ? _ = _ | fi.LEFT : g > u && (_ = _ | fi.RIGHT), y < a ? _ = _ | fi.BELOW : y > h && (_ = _ | fi.ABOVE), _ === fi.UNKNOWN && (_ = fi.INTERSECTING), _;
}
function _a() {
  return [1 / 0, 1 / 0, -1 / 0, -1 / 0];
}
function $o(r, t, i, a, u) {
  return u ? (u[0] = r, u[1] = t, u[2] = i, u[3] = a, u) : [r, t, i, a];
}
function $m(r) {
  return $o(1 / 0, 1 / 0, -1 / 0, -1 / 0, r);
}
function g2(r, t) {
  const i = r[0], a = r[1];
  return $o(i, a, i, a, t);
}
function nT(r, t, i, a, u) {
  const h = $m(u);
  return y2(h, r, t, i, a);
}
function d_(r, t) {
  return r[0] == t[0] && r[2] == t[2] && r[1] == t[1] && r[3] == t[3];
}
function m2(r, t) {
  return t[0] < r[0] && (r[0] = t[0]), t[2] > r[2] && (r[2] = t[2]), t[1] < r[1] && (r[1] = t[1]), t[3] > r[3] && (r[3] = t[3]), r;
}
function r_(r, t) {
  t[0] < r[0] && (r[0] = t[0]), t[0] > r[2] && (r[2] = t[0]), t[1] < r[1] && (r[1] = t[1]), t[1] > r[3] && (r[3] = t[1]);
}
function y2(r, t, i, a, u) {
  for (; i < a; i += u)
    gw(r, t[i], t[i + 1]);
  return r;
}
function gw(r, t, i) {
  r[0] = Math.min(r[0], t), r[1] = Math.min(r[1], i), r[2] = Math.max(r[2], t), r[3] = Math.max(r[3], i);
}
function iT(r, t) {
  let i;
  return i = t(uS(r)), i || (i = t(cS(r)), i) || (i = t(fS(r)), i) || (i = t(nd(r)), i) ? i : !1;
}
function Db(r) {
  let t = 0;
  return M_(r) || (t = Xe(r) * ml(r)), t;
}
function uS(r) {
  return [r[0], r[1]];
}
function cS(r) {
  return [r[2], r[1]];
}
function Pc(r) {
  return [(r[0] + r[2]) / 2, (r[1] + r[3]) / 2];
}
function mw(r, t) {
  let i;
  if (t === "bottom-left")
    i = uS(r);
  else if (t === "bottom-right")
    i = cS(r);
  else if (t === "top-left")
    i = nd(r);
  else if (t === "top-right")
    i = fS(r);
  else
    throw new Error("Invalid corner");
  return i;
}
function Ob(r, t, i, a, u) {
  const [h, g, y, _, b, E, x, R] = p2(
    r,
    t,
    i,
    a
  );
  return $o(
    Math.min(h, y, b, x),
    Math.min(g, _, E, R),
    Math.max(h, y, b, x),
    Math.max(g, _, E, R),
    u
  );
}
function p2(r, t, i, a) {
  const u = t * a[0] / 2, h = t * a[1] / 2, g = Math.cos(i), y = Math.sin(i), _ = u * g, b = u * y, E = h * g, x = h * y, R = r[0], w = r[1];
  return [
    R - _ + x,
    w - b - E,
    R - _ - x,
    w - b + E,
    R + _ - x,
    w + b + E,
    R + _ + x,
    w + b - E,
    R - _ + x,
    w - b - E
  ];
}
function ml(r) {
  return r[3] - r[1];
}
function Ph(r, t, i) {
  const a = i || _a();
  return qi(r, t) ? (r[0] > t[0] ? a[0] = r[0] : a[0] = t[0], r[1] > t[1] ? a[1] = r[1] : a[1] = t[1], r[2] < t[2] ? a[2] = r[2] : a[2] = t[2], r[3] < t[3] ? a[3] = r[3] : a[3] = t[3]) : $m(a), a;
}
function nd(r) {
  return [r[0], r[3]];
}
function fS(r) {
  return [r[2], r[3]];
}
function Xe(r) {
  return r[2] - r[0];
}
function qi(r, t) {
  return r[0] <= t[2] && r[2] >= t[0] && r[1] <= t[3] && r[3] >= t[1];
}
function M_(r) {
  return r[2] < r[0] || r[3] < r[1];
}
function yw(r, t) {
  return t ? (t[0] = r[0], t[1] = r[1], t[2] = r[2], t[3] = r[3], t) : r;
}
function pw(r, t, i) {
  let a = !1;
  const u = wb(r, t), h = wb(r, i);
  if (u === fi.INTERSECTING || h === fi.INTERSECTING)
    a = !0;
  else {
    const g = r[0], y = r[1], _ = r[2], b = r[3], E = t[0], x = t[1], R = i[0], w = i[1], L = (w - x) / (R - E);
    let H, N;
    h & fi.ABOVE && !(u & fi.ABOVE) && (H = R - (w - b) / L, a = H >= g && H <= _), !a && h & fi.RIGHT && !(u & fi.RIGHT) && (N = w - (R - _) * L, a = N >= y && N <= b), !a && h & fi.BELOW && !(u & fi.BELOW) && (H = R - (w - y) / L, a = H >= g && H <= _), !a && h & fi.LEFT && !(u & fi.LEFT) && (N = w - (R - g) * L, a = N >= y && N <= b);
  }
  return a;
}
function _w(r, t, i, a) {
  if (M_(r))
    return $m(i);
  let u = [];
  u = [
    r[0],
    r[1],
    r[2],
    r[1],
    r[2],
    r[3],
    r[0],
    r[3]
  ], t(u, u, 2);
  const h = [], g = [];
  for (let y = 0, _ = u.length; y < _; y += 2)
    h.push(u[y]), g.push(u[y + 1]);
  return dw(h, g, i);
}
function _2(r, t) {
  const i = t.getExtent(), a = Pc(r);
  if (t.canWrapX() && (a[0] < i[0] || a[0] >= i[2])) {
    const u = Xe(i), g = Math.floor(
      (a[0] - i[0]) / u
    ) * u;
    r[0] -= g, r[2] -= g;
  }
  return r;
}
function v2(r, t, i) {
  if (t.canWrapX()) {
    const a = t.getExtent();
    if (!isFinite(r[0]) || !isFinite(r[2]))
      return [[a[0], r[1], a[2], r[3]]];
    _2(r, t);
    const u = Xe(a);
    if (Xe(r) > u && !i)
      return [[a[0], r[1], a[2], r[3]]];
    if (r[0] < a[0])
      return [
        [r[0] + u, r[1], a[2], r[3]],
        [a[0], r[1], r[2], r[3]]
      ];
    if (r[2] > a[2])
      return [
        [r[0], r[1], a[2], r[3]],
        [a[0], r[1], r[2] - u, r[3]]
      ];
  }
  return [r];
}
function gn(r, t, i) {
  return Math.min(Math.max(r, t), i);
}
function vw(r, t, i, a, u, h) {
  const g = u - i, y = h - a;
  if (g !== 0 || y !== 0) {
    const _ = ((r - i) * g + (t - a) * y) / (g * g + y * y);
    _ > 1 ? (i = u, a = h) : _ > 0 && (i += g * _, a += y * _);
  }
  return du(r, t, i, a);
}
function du(r, t, i, a) {
  const u = i - r, h = a - t;
  return u * u + h * h;
}
function Sw(r) {
  const t = r.length;
  for (let a = 0; a < t; a++) {
    let u = a, h = Math.abs(r[a][a]);
    for (let y = a + 1; y < t; y++) {
      const _ = Math.abs(r[y][a]);
      _ > h && (h = _, u = y);
    }
    if (h === 0)
      return null;
    const g = r[u];
    r[u] = r[a], r[a] = g;
    for (let y = a + 1; y < t; y++) {
      const _ = -r[y][a] / r[a][a];
      for (let b = a; b < t + 1; b++)
        a == b ? r[y][b] = 0 : r[y][b] += _ * r[a][b];
    }
  }
  const i = new Array(t);
  for (let a = t - 1; a >= 0; a--) {
    i[a] = r[a][t] / r[a][a];
    for (let u = a - 1; u >= 0; u--)
      r[u][t] -= r[u][a] * i[a];
  }
  return i;
}
function iR(r) {
  return r * 180 / Math.PI;
}
function Fc(r) {
  return r * Math.PI / 180;
}
function Qh(r, t) {
  const i = r % t;
  return i * t < 0 ? i + t : i;
}
function ya(r, t, i) {
  return r + i * (t - r);
}
function w_(r, t) {
  const i = Math.pow(10, t);
  return Math.round(r * i) / i;
}
function R1(r, t) {
  return Math.floor(w_(r, t));
}
function A1(r, t) {
  return Math.ceil(w_(r, t));
}
function Lb(r, t, i) {
  if (r >= t && r < i)
    return r;
  const a = i - t;
  return ((r - t) % a + a) % a + t;
}
const Ew = 63710088e-1;
function lR(r, t, i) {
  i = i || Ew;
  const a = Fc(r[1]), u = Fc(t[1]), h = (u - a) / 2, g = Fc(t[0] - r[0]) / 2, y = Math.sin(h) * Math.sin(h) + Math.sin(g) * Math.sin(g) * Math.cos(a) * Math.cos(u);
  return 2 * i * Math.atan2(Math.sqrt(y), Math.sqrt(1 - y));
}
function S2(...r) {
  console.warn(...r);
}
function bw(r, t) {
  return r[0] += +t[0], r[1] += +t[1], r;
}
function Tw(r, t) {
  const i = t.getRadius(), a = t.getCenter(), u = a[0], h = a[1], g = r[0], y = r[1];
  let _ = g - u;
  const b = y - h;
  _ === 0 && b === 0 && (_ = 1);
  const E = Math.sqrt(_ * _ + b * b), x = u + i * _ / E, R = h + i * b / E;
  return [x, R];
}
function xw(r, t) {
  const i = r[0], a = r[1], u = t[0], h = t[1], g = u[0], y = u[1], _ = h[0], b = h[1], E = _ - g, x = b - y, R = E === 0 && x === 0 ? 0 : (E * (i - g) + x * (a - y)) / (E * E + x * x || 0);
  let w, L;
  return R <= 0 ? (w = g, L = y) : R >= 1 ? (w = _, L = b) : (w = g + R * E, L = y + R * x), [w, L];
}
function V1(r, t) {
  let i = !0;
  for (let a = r.length - 1; a >= 0; --a)
    if (r[a] != t[a]) {
      i = !1;
      break;
    }
  return i;
}
function lT(r, t) {
  const i = Math.cos(t), a = Math.sin(t), u = r[0] * i - r[1] * a, h = r[1] * i + r[0] * a;
  return r[0] = u, r[1] = h, r;
}
function Cw(r, t) {
  return r[0] *= t, r[1] *= t, r;
}
function u_(r, t) {
  const i = r[0] - t[0], a = r[1] - t[1];
  return i * i + a * a;
}
function E2(r, t) {
  return Math.sqrt(u_(r, t));
}
function b2(r, t) {
  if (t.canWrapX()) {
    const i = Xe(t.getExtent()), a = Rw(r, t, i);
    a && (r[0] -= a * i);
  }
  return r;
}
function Rw(r, t, i) {
  const a = t.getExtent();
  let u = 0;
  return t.canWrapX() && (r[0] < a[0] || r[0] > a[2]) && (i = i || Xe(a), u = Math.floor(
    (r[0] - a[0]) / i
  )), u;
}
const aT = {
  // use the radius of the Normal sphere
  radians: 6370997 / (2 * Math.PI),
  degrees: 2 * Math.PI * 6370997 / 360,
  ft: 0.3048,
  m: 1,
  "us-ft": 1200 / 3937
};
class sT {
  /**
   * @param {Options} options Projection options.
   */
  constructor(t) {
    this.code_ = t.code, this.units_ = /** @type {import("./Units.js").Units} */
    t.units, this.extent_ = t.extent !== void 0 ? t.extent : null, this.worldExtent_ = t.worldExtent !== void 0 ? t.worldExtent : null, this.axisOrientation_ = t.axisOrientation !== void 0 ? t.axisOrientation : "enu", this.global_ = t.global !== void 0 ? t.global : !1, this.canWrapX_ = !!(this.global_ && this.extent_), this.getPointResolutionFunc_ = t.getPointResolution, this.defaultTileGrid_ = null, this.metersPerUnit_ = t.metersPerUnit;
  }
  /**
   * @return {boolean} The projection is suitable for wrapping the x-axis
   */
  canWrapX() {
    return this.canWrapX_;
  }
  /**
   * Get the code for this projection, e.g. 'EPSG:4326'.
   * @return {string} Code.
   * @api
   */
  getCode() {
    return this.code_;
  }
  /**
   * Get the validity extent for this projection.
   * @return {import("../extent.js").Extent} Extent.
   * @api
   */
  getExtent() {
    return this.extent_;
  }
  /**
   * Get the units of this projection.
   * @return {import("./Units.js").Units} Units.
   * @api
   */
  getUnits() {
    return this.units_;
  }
  /**
   * Get the amount of meters per unit of this projection.  If the projection is
   * not configured with `metersPerUnit` or a units identifier, the return is
   * `undefined`.
   * @return {number|undefined} Meters.
   * @api
   */
  getMetersPerUnit() {
    return this.metersPerUnit_ || aT[this.units_];
  }
  /**
   * Get the world extent for this projection.
   * @return {import("../extent.js").Extent} Extent.
   * @api
   */
  getWorldExtent() {
    return this.worldExtent_;
  }
  /**
   * Get the axis orientation of this projection.
   * Example values are:
   * enu - the default easting, northing, elevation.
   * neu - northing, easting, up - useful for "lat/long" geographic coordinates,
   *     or south orientated transverse mercator.
   * wnu - westing, northing, up - some planetary coordinate systems have
   *     "west positive" coordinate systems
   * @return {string} Axis orientation.
   * @api
   */
  getAxisOrientation() {
    return this.axisOrientation_;
  }
  /**
   * Is this projection a global projection which spans the whole world?
   * @return {boolean} Whether the projection is global.
   * @api
   */
  isGlobal() {
    return this.global_;
  }
  /**
   * Set if the projection is a global projection which spans the whole world
   * @param {boolean} global Whether the projection is global.
   * @api
   */
  setGlobal(t) {
    this.global_ = t, this.canWrapX_ = !!(t && this.extent_);
  }
  /**
   * @return {import("../tilegrid/TileGrid.js").default} The default tile grid.
   */
  getDefaultTileGrid() {
    return this.defaultTileGrid_;
  }
  /**
   * @param {import("../tilegrid/TileGrid.js").default} tileGrid The default tile grid.
   */
  setDefaultTileGrid(t) {
    this.defaultTileGrid_ = t;
  }
  /**
   * Set the validity extent for this projection.
   * @param {import("../extent.js").Extent} extent Extent.
   * @api
   */
  setExtent(t) {
    this.extent_ = t, this.canWrapX_ = !!(this.global_ && t);
  }
  /**
   * Set the world extent for this projection.
   * @param {import("../extent.js").Extent} worldExtent World extent
   *     [minlon, minlat, maxlon, maxlat].
   * @api
   */
  setWorldExtent(t) {
    this.worldExtent_ = t;
  }
  /**
   * Set the getPointResolution function (see {@link module:ol/proj.getPointResolution}
   * for this projection.
   * @param {function(number, import("../coordinate.js").Coordinate):number} func Function
   * @api
   */
  setGetPointResolution(t) {
    this.getPointResolutionFunc_ = t;
  }
  /**
   * Get the custom point resolution function for this projection (if set).
   * @return {GetPointResolution|undefined} The custom point
   * resolution function (if set).
   */
  getPointResolutionFunc() {
    return this.getPointResolutionFunc_;
  }
}
const D_ = 6378137, Ym = Math.PI * D_, Aw = [-Ym, -Ym, Ym, Ym], Mw = [-180, -85, 180, 85], M1 = D_ * Math.log(Math.tan(Math.PI / 2));
class Rm extends sT {
  /**
   * @param {string} code Code.
   */
  constructor(t) {
    super({
      code: t,
      units: "m",
      extent: Aw,
      global: !0,
      worldExtent: Mw,
      getPointResolution: function(i, a) {
        return i / Math.cosh(a[1] / D_);
      }
    });
  }
}
const aR = [
  new Rm("EPSG:3857"),
  new Rm("EPSG:102100"),
  new Rm("EPSG:102113"),
  new Rm("EPSG:900913"),
  new Rm("http://www.opengis.net/def/crs/EPSG/0/3857"),
  new Rm("http://www.opengis.net/gml/srs/epsg.xml#3857")
];
function ww(r, t, i, a) {
  const u = r.length;
  i = i > 1 ? i : 2, a = a ?? i, t === void 0 && (i > 2 ? t = r.slice() : t = new Array(u));
  for (let h = 0; h < u; h += a) {
    t[h] = Ym * r[h] / 180;
    let g = D_ * Math.log(Math.tan(Math.PI * (+r[h + 1] + 90) / 360));
    g > M1 ? g = M1 : g < -M1 && (g = -M1), t[h + 1] = g;
  }
  return t;
}
function Dw(r, t, i, a) {
  const u = r.length;
  i = i > 1 ? i : 2, a = a ?? i, t === void 0 && (i > 2 ? t = r.slice() : t = new Array(u));
  for (let h = 0; h < u; h += a)
    t[h] = 180 * r[h] / Ym, t[h + 1] = 360 * Math.atan(Math.exp(r[h + 1] / D_)) / Math.PI - 90;
  return t;
}
const Ow = 6378137, sR = [-180, -90, 180, 90], Lw = Math.PI * Ow / 180;
class Bh extends sT {
  /**
   * @param {string} code Code.
   * @param {string} [axisOrientation] Axis orientation.
   */
  constructor(t, i) {
    super({
      code: t,
      units: "degrees",
      extent: sR,
      axisOrientation: i,
      global: !0,
      metersPerUnit: Lw,
      worldExtent: sR
    });
  }
}
const oR = [
  new Bh("CRS:84"),
  new Bh("EPSG:4326", "neu"),
  new Bh("urn:ogc:def:crs:OGC:1.3:CRS84"),
  new Bh("urn:ogc:def:crs:OGC:2:84"),
  new Bh("http://www.opengis.net/def/crs/OGC/1.3/CRS84"),
  new Bh("http://www.opengis.net/gml/srs/epsg.xml#4326", "neu"),
  new Bh("http://www.opengis.net/def/crs/EPSG/0/4326", "neu")
];
let zb = {};
function zw(r) {
  return zb[r] || zb[r.replace(/urn:(x-)?ogc:def:crs:EPSG:(.*:)?(\w+)$/, "EPSG:$3")] || null;
}
function Nw(r, t) {
  zb[r] = t;
}
let Im = {};
function g_(r, t, i) {
  const a = r.getCode(), u = t.getCode();
  a in Im || (Im[a] = {}), Im[a][u] = i;
}
function PE(r, t) {
  return r in Im && t in Im[r] ? Im[r][t] : null;
}
const q1 = 0.9996, gs = 669438e-8, hS = gs * gs, dS = hS * gs, qh = gs / (1 - gs), rR = Math.sqrt(1 - gs), Km = (1 - rR) / (1 + rR), T2 = Km * Km, oT = T2 * Km, rT = oT * Km, x2 = rT * Km, C2 = 1 - gs / 4 - 3 * hS / 64 - 5 * dS / 256, Uw = 3 * gs / 8 + 3 * hS / 32 + 45 * dS / 1024, Gw = 15 * hS / 256 + 45 * dS / 1024, Hw = 35 * dS / 3072, Yw = 3 / 2 * Km - 27 / 32 * oT + 269 / 512 * x2, jw = 21 / 16 * T2 - 55 / 32 * rT, Bw = 151 / 96 * oT - 417 / 128 * x2, Iw = 1097 / 512 * rT, Z1 = 6378137;
function Xw(r, t, i) {
  const a = r - 5e5, g = (i.north ? t : t - 1e7) / q1 / (Z1 * C2), y = g + Yw * Math.sin(2 * g) + jw * Math.sin(4 * g) + Bw * Math.sin(6 * g) + Iw * Math.sin(8 * g), _ = Math.sin(y), b = _ * _, E = Math.cos(y), x = _ / E, R = x * x, w = R * R, L = 1 - gs * b, H = Math.sqrt(1 - gs * b), N = Z1 / H, O = (1 - gs) / L, U = qh * E ** 2, I = U * U, X = a / (N * q1), Z = X * X, dt = Z * X, ft = dt * X, yt = ft * X, it = yt * X, Et = y - x / O * (Z / 2 - ft / 24 * (5 + 3 * R + 10 * U - 4 * I - 9 * qh)) + it / 720 * (61 + 90 * R + 298 * U + 45 * w - 252 * qh - 3 * I);
  let xt = (X - dt / 6 * (1 + 2 * R + U) + yt / 120 * (5 - 2 * U + 28 * R - 3 * I + 8 * qh + 24 * w)) / E;
  return xt = Lb(
    xt + Fc(R2(i.number)),
    -Math.PI,
    Math.PI
  ), [iR(xt), iR(Et)];
}
const uR = -80, cR = 84, kw = -180, Vw = 180;
function qw(r, t, i) {
  r = Lb(r, kw, Vw), t < uR ? t = uR : t > cR && (t = cR);
  const a = Fc(t), u = Math.sin(a), h = Math.cos(a), g = u / h, y = g * g, _ = y * y, b = Fc(r), E = R2(i.number), x = Fc(E), R = Z1 / Math.sqrt(1 - gs * u ** 2), w = qh * h ** 2, L = h * Lb(b - x, -Math.PI, Math.PI), H = L * L, N = H * L, O = N * L, U = O * L, I = U * L, X = Z1 * (C2 * a - Uw * Math.sin(2 * a) + Gw * Math.sin(4 * a) - Hw * Math.sin(6 * a)), Z = q1 * R * (L + N / 6 * (1 - y + w) + U / 120 * (5 - 18 * y + _ + 72 * w - 58 * qh)) + 5e5;
  let dt = q1 * (X + R * g * (H / 2 + O / 24 * (5 - y + 9 * w + 4 * w ** 2) + I / 720 * (61 - 58 * y + _ + 600 * w - 330 * qh)));
  return i.north || (dt += 1e7), [Z, dt];
}
function R2(r) {
  return (r - 1) * 6 - 180 + 3;
}
const Zw = [
  /^EPSG:(\d+)$/,
  /^urn:ogc:def:crs:EPSG::(\d+)$/,
  /^http:\/\/www\.opengis\.net\/def\/crs\/EPSG\/0\/(\d+)$/
];
function A2(r) {
  let t = 0;
  for (const u of Zw) {
    const h = r.match(u);
    if (h) {
      t = parseInt(h[1]);
      break;
    }
  }
  if (!t)
    return null;
  let i = 0, a = !1;
  return t > 32700 && t < 32761 ? i = t - 32700 : t > 32600 && t < 32661 && (a = !0, i = t - 32600), i ? { number: i, north: a } : null;
}
function fR(r, t) {
  return function(i, a, u, h) {
    const g = i.length;
    u = u > 1 ? u : 2, h = h ?? u, a || (u > 2 ? a = i.slice() : a = new Array(g));
    for (let y = 0; y < g; y += h) {
      const _ = i[y], b = i[y + 1], E = r(_, b, t);
      a[y] = E[0], a[y + 1] = E[1];
    }
    return a;
  };
}
function Fw(r) {
  return A2(r) ? new sT({ code: r, units: "m" }) : null;
}
function Kw(r) {
  const t = A2(r.getCode());
  return t ? {
    forward: fR(qw, t),
    inverse: fR(Xw, t)
  } : null;
}
const Pw = [Kw], Qw = [Fw];
let Nb = !0;
function Ww(r) {
  Nb = !1;
}
function uT(r, t) {
  if (t !== void 0) {
    for (let i = 0, a = r.length; i < a; ++i)
      t[i] = r[i];
    t = t;
  } else
    t = r.slice();
  return t;
}
function Ub(r) {
  Nw(r.getCode(), r), g_(r, r, uT);
}
function Jw(r) {
  r.forEach(Ub);
}
function Zi(r) {
  if (typeof r != "string")
    return r;
  const t = zw(r);
  if (t)
    return t;
  for (const i of Qw) {
    const a = i(r);
    if (a)
      return a;
  }
  return null;
}
function hR(r, t, i, a) {
  r = Zi(r);
  let u;
  const h = r.getPointResolutionFunc();
  if (h)
    u = h(t, i);
  else {
    const g = r.getUnits();
    if (g == "degrees" && !a || a == "degrees")
      u = t;
    else {
      const y = O_(
        r,
        Zi("EPSG:4326")
      );
      if (!y && g !== "degrees")
        u = t * r.getMetersPerUnit();
      else {
        let b = [
          i[0] - t / 2,
          i[1],
          i[0] + t / 2,
          i[1],
          i[0],
          i[1] - t / 2,
          i[0],
          i[1] + t / 2
        ];
        b = y(b, b, 2);
        const E = lR(b.slice(0, 2), b.slice(2, 4)), x = lR(b.slice(4, 6), b.slice(6, 8));
        u = (E + x) / 2;
      }
      const _ = r.getMetersPerUnit();
      _ !== void 0 && (u /= _);
    }
  }
  return u;
}
function dR(r) {
  Jw(r), r.forEach(function(t) {
    r.forEach(function(i) {
      t !== i && g_(t, i, uT);
    });
  });
}
function $w(r, t, i, a) {
  r.forEach(function(u) {
    t.forEach(function(h) {
      g_(u, h, i), g_(h, u, a);
    });
  });
}
function cT(r, t) {
  return r ? typeof r == "string" ? Zi(r) : (
    /** @type {Projection} */
    r
  ) : Zi(t);
}
function tD(r) {
  return (
    /**
     * @param {Array<number>} input Input.
     * @param {Array<number>} [output] Output.
     * @param {number} [dimension] Dimensions that should be transformed.
     * @param {number} [stride] Stride.
     * @return {Array<number>} Output.
     */
    function(t, i, a, u) {
      const h = t.length;
      a = a !== void 0 ? a : 2, u = u ?? a, i = i !== void 0 ? i : new Array(h);
      for (let g = 0; g < h; g += u) {
        const y = r(t.slice(g, g + a)), _ = y.length;
        for (let b = 0, E = u; b < E; ++b)
          i[g + b] = b >= _ ? t[g + b] : y[b];
      }
      return i;
    }
  );
}
function QE(r, t) {
  if (r === t)
    return !0;
  const i = r.getUnits() === t.getUnits();
  return (r.getCode() === t.getCode() || O_(r, t) === uT) && i;
}
function O_(r, t) {
  const i = r.getCode(), a = t.getCode();
  let u = PE(i, a);
  if (u)
    return u;
  let h = null, g = null;
  for (const _ of Pw)
    h || (h = _(r)), g || (g = _(t));
  if (!h && !g)
    return null;
  const y = "EPSG:4326";
  if (g)
    if (h)
      u = WE(
        h.inverse,
        g.forward
      );
    else {
      const _ = PE(i, y);
      _ && (u = WE(
        _,
        g.forward
      ));
    }
  else {
    const _ = PE(y, a);
    _ && (u = WE(
      h.inverse,
      _
    ));
  }
  return u && (Ub(r), Ub(t), g_(r, t, u)), u;
}
function WE(r, t) {
  return function(i, a, u, h) {
    return a = r(i, a, u, h), t(a, a, u, h);
  };
}
function m_(r, t) {
  const i = Zi(r), a = Zi(t);
  return O_(i, a);
}
function gS(r, t, i) {
  const a = m_(t, i);
  if (!a) {
    const u = Zi(t).getCode(), h = Zi(i).getCode();
    throw new Error(
      `No transform available between ${u} and ${h}`
    );
  }
  return a(r, void 0, r.length);
}
function M2(r, t, i, a) {
  const u = m_(t, i);
  return _w(r, u, void 0);
}
let ms = null;
function eD(r) {
  ms = Zi(r);
}
function Wh() {
  return ms;
}
function nD() {
  eD("EPSG:4326");
}
function F1(r, t) {
  return ms ? gS(r, t, ms) : r;
}
function dl(r, t) {
  return ms ? gS(r, ms, t) : (Nb && !V1(r, [0, 0]) && r[0] >= -180 && r[0] <= 180 && r[1] >= -90 && r[1] <= 90 && (Nb = !1, S2(
    "Call useGeographic() from ol/proj once to work with [longitude, latitude] coordinates."
  )), r);
}
function y_(r, t) {
  return ms ? M2(r, t, ms) : r;
}
function Vc(r, t) {
  return ms ? M2(r, ms, t) : r;
}
function iD(r, t) {
  if (!ms)
    return r;
  const i = Zi(t).getMetersPerUnit(), a = ms.getMetersPerUnit();
  return i && a ? r * i / a : r;
}
function lD() {
  dR(aR), dR(oR), $w(
    oR,
    aR,
    ww,
    Dw
  );
}
lD();
new Array(6);
function $s() {
  return [1, 0, 0, 1, 0, 0];
}
function aD(r, t) {
  return r[0] = t[0], r[1] = t[1], r[2] = t[2], r[3] = t[3], r[4] = t[4], r[5] = t[5], r;
}
function hi(r, t) {
  const i = t[0], a = t[1];
  return t[0] = r[0] * i + r[2] * a + r[4], t[1] = r[1] * i + r[3] * a + r[5], t;
}
function vu(r, t, i, a, u, h, g, y) {
  const _ = Math.sin(h), b = Math.cos(h);
  return r[0] = a * b, r[1] = u * _, r[2] = -a * _, r[3] = u * b, r[4] = g * a * b - y * a * _ + t, r[5] = g * u * _ + y * u * b + i, r;
}
function w2(r, t) {
  const i = sD(t);
  je(i !== 0, "Transformation matrix cannot be inverted");
  const a = t[0], u = t[1], h = t[2], g = t[3], y = t[4], _ = t[5];
  return r[0] = g / i, r[1] = -u / i, r[2] = -h / i, r[3] = a / i, r[4] = (h * _ - g * y) / i, r[5] = -(a * _ - u * y) / i, r;
}
function sD(r) {
  return r[0] * r[3] - r[1] * r[2];
}
const oD = [1e5, 1e5, 1e5, 1e5, 2, 2];
function rD(r) {
  return "matrix(" + r.join(", ") + ")";
}
function gR(r) {
  return r.substring(7, r.length - 1).split(",").map(parseFloat);
}
function uD(r, t) {
  const i = gR(r), a = gR(t);
  for (let u = 0; u < 6; ++u)
    if (Math.round((i[u] - a[u]) * oD[u]) !== 0)
      return !1;
  return !0;
}
function Kc(r, t, i, a, u, h, g) {
  h = h || [], g = g || 2;
  let y = 0;
  for (let _ = t; _ < i; _ += a) {
    const b = r[_], E = r[_ + 1];
    h[y++] = u[0] * b + u[2] * E + u[4], h[y++] = u[1] * b + u[3] * E + u[5];
    for (let x = 2; x < g; x++)
      h[y++] = r[_ + x];
  }
  return h && h.length != y && (h.length = y), h;
}
function fT(r, t, i, a, u, h, g) {
  g = g || [];
  const y = Math.cos(u), _ = Math.sin(u), b = h[0], E = h[1];
  let x = 0;
  for (let R = t; R < i; R += a) {
    const w = r[R] - b, L = r[R + 1] - E;
    g[x++] = b + w * y - L * _, g[x++] = E + w * _ + L * y;
    for (let H = R + 2; H < R + a; ++H)
      g[x++] = r[H];
  }
  return g && g.length != x && (g.length = x), g;
}
function cD(r, t, i, a, u, h, g, y) {
  y = y || [];
  const _ = g[0], b = g[1];
  let E = 0;
  for (let x = t; x < i; x += a) {
    const R = r[x] - _, w = r[x + 1] - b;
    y[E++] = _ + u * R, y[E++] = b + h * w;
    for (let L = x + 2; L < x + a; ++L)
      y[E++] = r[L];
  }
  return y && y.length != E && (y.length = E), y;
}
function fD(r, t, i, a, u, h, g) {
  g = g || [];
  let y = 0;
  for (let _ = t; _ < i; _ += a) {
    g[y++] = r[_] + u, g[y++] = r[_ + 1] + h;
    for (let b = _ + 2; b < _ + a; ++b)
      g[y++] = r[b];
  }
  return g && g.length != y && (g.length = y), g;
}
const mR = $s(), hD = [NaN, NaN];
class D2 extends tr {
  constructor() {
    super(), this.extent_ = _a(), this.extentRevision_ = -1, this.simplifiedGeometryMaxMinSquaredTolerance = 0, this.simplifiedGeometryRevision = 0, this.simplifyTransformedInternal = h2(
      (t, i, a) => {
        if (!a)
          return this.getSimplifiedGeometry(i);
        const u = this.clone();
        return u.applyTransform(a), u.getSimplifiedGeometry(i);
      }
    );
  }
  /**
   * Get a transformed and simplified version of the geometry.
   * @abstract
   * @param {number} squaredTolerance Squared tolerance.
   * @param {import("../proj.js").TransformFunction} [transform] Optional transform function.
   * @return {Geometry} Simplified geometry.
   */
  simplifyTransformed(t, i) {
    return this.simplifyTransformedInternal(
      this.getRevision(),
      t,
      i
    );
  }
  /**
   * Make a complete copy of the geometry.
   * @abstract
   * @return {!Geometry} Clone.
   */
  clone() {
    return we();
  }
  /**
   * @abstract
   * @param {number} x X.
   * @param {number} y Y.
   * @param {import("../coordinate.js").Coordinate} closestPoint Closest point.
   * @param {number} minSquaredDistance Minimum squared distance.
   * @return {number} Minimum squared distance.
   */
  closestPointXY(t, i, a, u) {
    return we();
  }
  /**
   * @param {number} x X.
   * @param {number} y Y.
   * @return {boolean} Contains (x, y).
   */
  containsXY(t, i) {
    return this.closestPointXY(t, i, hD, Number.MIN_VALUE) === 0;
  }
  /**
   * Return the closest point of the geometry to the passed point as
   * {@link module:ol/coordinate~Coordinate coordinate}.
   * @param {import("../coordinate.js").Coordinate} point Point.
   * @param {import("../coordinate.js").Coordinate} [closestPoint] Closest point.
   * @return {import("../coordinate.js").Coordinate} Closest point.
   * @api
   */
  getClosestPoint(t, i) {
    return i = i || [NaN, NaN], this.closestPointXY(t[0], t[1], i, 1 / 0), i;
  }
  /**
   * Returns true if this geometry includes the specified coordinate. If the
   * coordinate is on the boundary of the geometry, returns false.
   * @param {import("../coordinate.js").Coordinate} coordinate Coordinate.
   * @return {boolean} Contains coordinate.
   * @api
   */
  intersectsCoordinate(t) {
    return this.containsXY(t[0], t[1]);
  }
  /**
   * @abstract
   * @param {import("../extent.js").Extent} extent Extent.
   * @protected
   * @return {import("../extent.js").Extent} extent Extent.
   */
  computeExtent(t) {
    return we();
  }
  /**
   * Get the extent of the geometry.
   * @param {import("../extent.js").Extent} [extent] Extent.
   * @return {import("../extent.js").Extent} extent Extent.
   * @api
   */
  getExtent(t) {
    if (this.extentRevision_ != this.getRevision()) {
      const i = this.computeExtent(this.extent_);
      (isNaN(i[0]) || isNaN(i[1])) && $m(i), this.extentRevision_ = this.getRevision();
    }
    return yw(this.extent_, t);
  }
  /**
   * Rotate the geometry around a given coordinate. This modifies the geometry
   * coordinates in place.
   * @abstract
   * @param {number} angle Rotation angle in radians.
   * @param {import("../coordinate.js").Coordinate} anchor The rotation center.
   * @api
   */
  rotate(t, i) {
    we();
  }
  /**
   * Scale the geometry (with an optional origin).  This modifies the geometry
   * coordinates in place.
   * @abstract
   * @param {number} sx The scaling factor in the x-direction.
   * @param {number} [sy] The scaling factor in the y-direction (defaults to sx).
   * @param {import("../coordinate.js").Coordinate} [anchor] The scale origin (defaults to the center
   *     of the geometry extent).
   * @api
   */
  scale(t, i, a) {
    we();
  }
  /**
   * Create a simplified version of this geometry.  For linestrings, this uses
   * the [Douglas Peucker](https://en.wikipedia.org/wiki/Ramer-Douglas-Peucker_algorithm)
   * algorithm.  For polygons, a quantization-based
   * simplification is used to preserve topology.
   * @param {number} tolerance The tolerance distance for simplification.
   * @return {Geometry} A new, simplified version of the original geometry.
   * @api
   */
  simplify(t) {
    return this.getSimplifiedGeometry(t * t);
  }
  /**
   * Create a simplified version of this geometry using the Douglas Peucker
   * algorithm.
   * See https://en.wikipedia.org/wiki/Ramer-Douglas-Peucker_algorithm.
   * @abstract
   * @param {number} squaredTolerance Squared tolerance.
   * @return {Geometry} Simplified geometry.
   */
  getSimplifiedGeometry(t) {
    return we();
  }
  /**
   * Get the type of this geometry.
   * @abstract
   * @return {Type} Geometry type.
   */
  getType() {
    return we();
  }
  /**
   * Apply a transform function to the coordinates of the geometry.
   * The geometry is modified in place.
   * If you do not want the geometry modified in place, first `clone()` it and
   * then use this function on the clone.
   * @abstract
   * @param {import("../proj.js").TransformFunction} transformFn Transform function.
   * Called with a flat array of geometry coordinates.
   */
  applyTransform(t) {
    we();
  }
  /**
   * Test if the geometry and the passed extent intersect.
   * @abstract
   * @param {import("../extent.js").Extent} extent Extent.
   * @return {boolean} `true` if the geometry and the extent intersect.
   */
  intersectsExtent(t) {
    return we();
  }
  /**
   * Translate the geometry.  This modifies the geometry coordinates in place.  If
   * instead you want a new geometry, first `clone()` this geometry.
   * @abstract
   * @param {number} deltaX Delta X.
   * @param {number} deltaY Delta Y.
   * @api
   */
  translate(t, i) {
    we();
  }
  /**
   * Transform each coordinate of the geometry from one coordinate reference
   * system to another. The geometry is modified in place.
   * For example, a line will be transformed to a line and a circle to a circle.
   * If you do not want the geometry modified in place, first `clone()` it and
   * then use this function on the clone.
   *
   * @param {import("../proj.js").ProjectionLike} source The current projection.  Can be a
   *     string identifier or a {@link module:ol/proj/Projection~Projection} object.
   * @param {import("../proj.js").ProjectionLike} destination The desired projection.  Can be a
   *     string identifier or a {@link module:ol/proj/Projection~Projection} object.
   * @return {this} This geometry.  Note that original geometry is
   *     modified in place.
   * @api
   */
  transform(t, i) {
    const a = Zi(t), u = a.getUnits() == "tile-pixels" ? function(h, g, y) {
      const _ = a.getExtent(), b = a.getWorldExtent(), E = ml(b) / ml(_);
      vu(
        mR,
        b[0],
        b[3],
        E,
        -E,
        0,
        0,
        0
      );
      const x = Kc(
        h,
        0,
        h.length,
        y,
        mR,
        g
      ), R = m_(a, i);
      return R ? R(x, x, y) : x;
    } : m_(a, i);
    return this.applyTransform(u), this;
  }
}
class Jc extends D2 {
  constructor() {
    super(), this.layout = "XY", this.stride = 2, this.flatCoordinates;
  }
  /**
   * @param {import("../extent.js").Extent} extent Extent.
   * @protected
   * @return {import("../extent.js").Extent} extent Extent.
   * @override
   */
  computeExtent(t) {
    return nT(
      this.flatCoordinates,
      0,
      this.flatCoordinates.length,
      this.stride,
      t
    );
  }
  /**
   * @abstract
   * @return {Array<*> | null} Coordinates.
   */
  getCoordinates() {
    return we();
  }
  /**
   * Return the first coordinate of the geometry.
   * @return {import("../coordinate.js").Coordinate} First coordinate.
   * @api
   */
  getFirstCoordinate() {
    return this.flatCoordinates.slice(0, this.stride);
  }
  /**
   * @return {Array<number>} Flat coordinates.
   */
  getFlatCoordinates() {
    return this.flatCoordinates;
  }
  /**
   * Return the last coordinate of the geometry.
   * @return {import("../coordinate.js").Coordinate} Last point.
   * @api
   */
  getLastCoordinate() {
    return this.flatCoordinates.slice(
      this.flatCoordinates.length - this.stride
    );
  }
  /**
   * Return the {@link import("./Geometry.js").GeometryLayout layout} of the geometry.
   * @return {import("./Geometry.js").GeometryLayout} Layout.
   * @api
   */
  getLayout() {
    return this.layout;
  }
  /**
   * Create a simplified version of this geometry using the Douglas Peucker algorithm.
   * @param {number} squaredTolerance Squared tolerance.
   * @return {SimpleGeometry} Simplified geometry.
   * @override
   */
  getSimplifiedGeometry(t) {
    if (this.simplifiedGeometryRevision !== this.getRevision() && (this.simplifiedGeometryMaxMinSquaredTolerance = 0, this.simplifiedGeometryRevision = this.getRevision()), t < 0 || this.simplifiedGeometryMaxMinSquaredTolerance !== 0 && t <= this.simplifiedGeometryMaxMinSquaredTolerance)
      return this;
    const i = this.getSimplifiedGeometryInternal(t);
    return i.getFlatCoordinates().length < this.flatCoordinates.length ? i : (this.simplifiedGeometryMaxMinSquaredTolerance = t, this);
  }
  /**
   * @param {number} squaredTolerance Squared tolerance.
   * @return {SimpleGeometry} Simplified geometry.
   * @protected
   */
  getSimplifiedGeometryInternal(t) {
    return this;
  }
  /**
   * @return {number} Stride.
   */
  getStride() {
    return this.stride;
  }
  /**
   * @param {import("./Geometry.js").GeometryLayout} layout Layout.
   * @param {Array<number>} flatCoordinates Flat coordinates.
   */
  setFlatCoordinates(t, i) {
    this.stride = K1(t), this.layout = t, this.flatCoordinates = i;
  }
  /**
   * @abstract
   * @param {!Array<*>} coordinates Coordinates.
   * @param {import("./Geometry.js").GeometryLayout} [layout] Layout.
   */
  setCoordinates(t, i) {
    we();
  }
  /**
   * @param {import("./Geometry.js").GeometryLayout|undefined} layout Layout.
   * @param {Array<*>} coordinates Coordinates.
   * @param {number} nesting Nesting.
   * @protected
   */
  setLayout(t, i, a) {
    let u;
    if (t)
      u = K1(t);
    else {
      for (let h = 0; h < a; ++h) {
        if (i.length === 0) {
          this.layout = "XY", this.stride = 2;
          return;
        }
        i = /** @type {Array<unknown>} */
        i[0];
      }
      u = i.length, t = dD(u);
    }
    this.layout = t, this.stride = u;
  }
  /**
   * Apply a transform function to the coordinates of the geometry.
   * The geometry is modified in place.
   * If you do not want the geometry modified in place, first `clone()` it and
   * then use this function on the clone.
   * @param {import("../proj.js").TransformFunction} transformFn Transform function.
   * Called with a flat array of geometry coordinates.
   * @api
   * @override
   */
  applyTransform(t) {
    this.flatCoordinates && (t(
      this.flatCoordinates,
      this.flatCoordinates,
      this.layout.startsWith("XYZ") ? 3 : 2,
      this.stride
    ), this.changed());
  }
  /**
   * Rotate the geometry around a given coordinate. This modifies the geometry
   * coordinates in place.
   * @param {number} angle Rotation angle in counter-clockwise radians.
   * @param {import("../coordinate.js").Coordinate} anchor The rotation center.
   * @api
   * @override
   */
  rotate(t, i) {
    const a = this.getFlatCoordinates();
    if (a) {
      const u = this.getStride();
      fT(
        a,
        0,
        a.length,
        u,
        t,
        i,
        a
      ), this.changed();
    }
  }
  /**
   * Scale the geometry (with an optional origin).  This modifies the geometry
   * coordinates in place.
   * @param {number} sx The scaling factor in the x-direction.
   * @param {number} [sy] The scaling factor in the y-direction (defaults to sx).
   * @param {import("../coordinate.js").Coordinate} [anchor] The scale origin (defaults to the center
   *     of the geometry extent).
   * @api
   * @override
   */
  scale(t, i, a) {
    i === void 0 && (i = t), a || (a = Pc(this.getExtent()));
    const u = this.getFlatCoordinates();
    if (u) {
      const h = this.getStride();
      cD(
        u,
        0,
        u.length,
        h,
        t,
        i,
        a,
        u
      ), this.changed();
    }
  }
  /**
   * Translate the geometry.  This modifies the geometry coordinates in place.  If
   * instead you want a new geometry, first `clone()` this geometry.
   * @param {number} deltaX Delta X.
   * @param {number} deltaY Delta Y.
   * @api
   * @override
   */
  translate(t, i) {
    const a = this.getFlatCoordinates();
    if (a) {
      const u = this.getStride();
      fD(
        a,
        0,
        a.length,
        u,
        t,
        i,
        a
      ), this.changed();
    }
  }
}
function dD(r) {
  let t;
  return r == 2 ? t = "XY" : r == 3 ? t = "XYZ" : r == 4 && (t = "XYZM"), /** @type {import("./Geometry.js").GeometryLayout} */
  t;
}
function K1(r) {
  let t;
  return r == "XY" ? t = 2 : r == "XYZ" || r == "XYM" ? t = 3 : r == "XYZM" && (t = 4), /** @type {number} */
  t;
}
function gD(r, t, i) {
  const a = r.getFlatCoordinates();
  if (!a)
    return null;
  const u = r.getStride();
  return Kc(
    a,
    0,
    a.length,
    u,
    t,
    i
  );
}
function O2(r, t, i, a) {
  let u = 0;
  const h = r[i - a], g = r[i - a + 1];
  let y = 0, _ = 0;
  for (; t < i; t += a) {
    const b = r[t] - h, E = r[t + 1] - g;
    u += _ * b - y * E, y = b, _ = E;
  }
  return u / 2;
}
function L2(r, t, i, a) {
  let u = 0;
  for (let h = 0, g = i.length; h < g; ++h) {
    const y = i[h];
    u += O2(r, t, y, a), t = y;
  }
  return u;
}
function mD(r, t, i, a) {
  let u = 0;
  for (let h = 0, g = i.length; h < g; ++h) {
    const y = i[h];
    u += L2(r, t, y, a), t = y[y.length - 1];
  }
  return u;
}
function yR(r, t, i, a, u, h, g) {
  const y = r[t], _ = r[t + 1], b = r[i] - y, E = r[i + 1] - _;
  let x;
  if (b === 0 && E === 0)
    x = t;
  else {
    const R = ((u - y) * b + (h - _) * E) / (b * b + E * E);
    if (R > 1)
      x = i;
    else if (R > 0) {
      for (let w = 0; w < a; ++w)
        g[w] = ya(
          r[t + w],
          r[i + w],
          R
        );
      g.length = a;
      return;
    } else
      x = t;
  }
  for (let R = 0; R < a; ++R)
    g[R] = r[x + R];
  g.length = a;
}
function hT(r, t, i, a, u) {
  let h = r[t], g = r[t + 1];
  for (t += a; t < i; t += a) {
    const y = r[t], _ = r[t + 1], b = du(h, g, y, _);
    b > u && (u = b), h = y, g = _;
  }
  return u;
}
function dT(r, t, i, a, u) {
  for (let h = 0, g = i.length; h < g; ++h) {
    const y = i[h];
    u = hT(r, t, y, a, u), t = y;
  }
  return u;
}
function yD(r, t, i, a, u) {
  for (let h = 0, g = i.length; h < g; ++h) {
    const y = i[h];
    u = dT(r, t, y, a, u), t = y[y.length - 1];
  }
  return u;
}
function gT(r, t, i, a, u, h, g, y, _, b, E) {
  if (t == i)
    return b;
  let x, R;
  if (u === 0) {
    if (R = du(
      g,
      y,
      r[t],
      r[t + 1]
    ), R < b) {
      for (x = 0; x < a; ++x)
        _[x] = r[t + x];
      return _.length = a, R;
    }
    return b;
  }
  E = E || [NaN, NaN];
  let w = t + a;
  for (; w < i; )
    if (yR(
      r,
      w - a,
      w,
      a,
      g,
      y,
      E
    ), R = du(g, y, E[0], E[1]), R < b) {
      for (b = R, x = 0; x < a; ++x)
        _[x] = E[x];
      _.length = a, w += a;
    } else
      w += a * Math.max(
        (Math.sqrt(R) - Math.sqrt(b)) / u | 0,
        1
      );
  if (h && (yR(
    r,
    i - a,
    t,
    a,
    g,
    y,
    E
  ), R = du(g, y, E[0], E[1]), R < b)) {
    for (b = R, x = 0; x < a; ++x)
      _[x] = E[x];
    _.length = a;
  }
  return b;
}
function mT(r, t, i, a, u, h, g, y, _, b, E) {
  E = E || [NaN, NaN];
  for (let x = 0, R = i.length; x < R; ++x) {
    const w = i[x];
    b = gT(
      r,
      t,
      w,
      a,
      u,
      h,
      g,
      y,
      _,
      b,
      E
    ), t = w;
  }
  return b;
}
function pD(r, t, i, a, u, h, g, y, _, b, E) {
  E = E || [NaN, NaN];
  for (let x = 0, R = i.length; x < R; ++x) {
    const w = i[x];
    b = mT(
      r,
      t,
      w,
      a,
      u,
      h,
      g,
      y,
      _,
      b,
      E
    ), t = w[w.length - 1];
  }
  return b;
}
function z2(r, t, i, a) {
  for (let u = 0, h = i.length; u < h; ++u)
    r[t++] = i[u];
  return t;
}
function mS(r, t, i, a) {
  for (let u = 0, h = i.length; u < h; ++u) {
    const g = i[u];
    for (let y = 0; y < a; ++y)
      r[t++] = g[y];
  }
  return t;
}
function yT(r, t, i, a, u) {
  u = u || [];
  let h = 0;
  for (let g = 0, y = i.length; g < y; ++g) {
    const _ = mS(
      r,
      t,
      i[g],
      a
    );
    u[h++] = _, t = _;
  }
  return u.length = h, u;
}
function _D(r, t, i, a, u) {
  u = u || [];
  let h = 0;
  for (let g = 0, y = i.length; g < y; ++g) {
    const _ = yT(
      r,
      t,
      i[g],
      a,
      u[h]
    );
    _.length === 0 && (_[0] = t), u[h++] = _, t = _[_.length - 1];
  }
  return u.length = h, u;
}
function qc(r, t, i, a, u) {
  u = u !== void 0 ? u : [];
  let h = 0;
  for (let g = t; g < i; g += a)
    u[h++] = r.slice(g, g + a);
  return u.length = h, u;
}
function p_(r, t, i, a, u) {
  u = u !== void 0 ? u : [];
  let h = 0;
  for (let g = 0, y = i.length; g < y; ++g) {
    const _ = i[g];
    u[h++] = qc(
      r,
      t,
      _,
      a,
      u[h]
    ), t = _;
  }
  return u.length = h, u;
}
function Gb(r, t, i, a, u) {
  u = u !== void 0 ? u : [];
  let h = 0;
  for (let g = 0, y = i.length; g < y; ++g) {
    const _ = i[g];
    u[h++] = _.length === 1 && _[0] === t ? [] : p_(
      r,
      t,
      _,
      a,
      u[h]
    ), t = _[_.length - 1];
  }
  return u.length = h, u;
}
function yS(r, t, i, a, u, h, g) {
  const y = (i - t) / a;
  if (y < 3) {
    for (; t < i; t += a)
      h[g++] = r[t], h[g++] = r[t + 1];
    return g;
  }
  const _ = new Array(y);
  _[0] = 1, _[y - 1] = 1;
  const b = [t, i - a];
  let E = 0;
  for (; b.length > 0; ) {
    const x = b.pop(), R = b.pop();
    let w = 0;
    const L = r[R], H = r[R + 1], N = r[x], O = r[x + 1];
    for (let U = R + a; U < x; U += a) {
      const I = r[U], X = r[U + 1], Z = vw(I, X, L, H, N, O);
      Z > w && (E = U, w = Z);
    }
    w > u && (_[(E - t) / a] = 1, R + a < E && b.push(R, E), E + a < x && b.push(E, x));
  }
  for (let x = 0; x < y; ++x)
    _[x] && (h[g++] = r[t + x * a], h[g++] = r[t + x * a + 1]);
  return g;
}
function N2(r, t, i, a, u, h, g, y) {
  for (let _ = 0, b = i.length; _ < b; ++_) {
    const E = i[_];
    g = yS(
      r,
      t,
      E,
      a,
      u,
      h,
      g
    ), y.push(g), t = E;
  }
  return g;
}
function Xh(r, t) {
  return t * Math.round(r / t);
}
function vD(r, t, i, a, u, h, g) {
  if (t == i)
    return g;
  let y = Xh(r[t], u), _ = Xh(r[t + 1], u);
  t += a, h[g++] = y, h[g++] = _;
  let b, E;
  do
    if (b = Xh(r[t], u), E = Xh(r[t + 1], u), t += a, t == i)
      return h[g++] = b, h[g++] = E, g;
  while (b == y && E == _);
  for (; t < i; ) {
    const x = Xh(r[t], u), R = Xh(r[t + 1], u);
    if (t += a, x == b && R == E)
      continue;
    const w = b - y, L = E - _, H = x - y, N = R - _;
    if (w * N == L * H && (w < 0 && H < w || w == H || w > 0 && H > w) && (L < 0 && N < L || L == N || L > 0 && N > L)) {
      b = x, E = R;
      continue;
    }
    h[g++] = b, h[g++] = E, y = b, _ = E, b = x, E = R;
  }
  return h[g++] = b, h[g++] = E, g;
}
function pT(r, t, i, a, u, h, g, y) {
  for (let _ = 0, b = i.length; _ < b; ++_) {
    const E = i[_];
    g = vD(
      r,
      t,
      E,
      a,
      u,
      h,
      g
    ), y.push(g), t = E;
  }
  return g;
}
function SD(r, t, i, a, u, h, g, y) {
  for (let _ = 0, b = i.length; _ < b; ++_) {
    const E = i[_], x = [];
    g = pT(
      r,
      t,
      E,
      a,
      u,
      h,
      g,
      x
    ), y.push(x), t = E[E.length - 1];
  }
  return g;
}
class __ extends Jc {
  /**
   * @param {Array<import("../coordinate.js").Coordinate>|Array<number>} coordinates Coordinates.
   *     For internal use, flat coordinates in combination with `layout` are also accepted.
   * @param {import("./Geometry.js").GeometryLayout} [layout] Layout.
   */
  constructor(t, i) {
    super(), this.maxDelta_ = -1, this.maxDeltaRevision_ = -1, i !== void 0 && !Array.isArray(t[0]) ? this.setFlatCoordinates(
      i,
      /** @type {Array<number>} */
      t
    ) : this.setCoordinates(
      /** @type {Array<import("../coordinate.js").Coordinate>} */
      t,
      i
    );
  }
  /**
   * Make a complete copy of the geometry.
   * @return {!LinearRing} Clone.
   * @api
   * @override
   */
  clone() {
    return new __(this.flatCoordinates.slice(), this.layout);
  }
  /**
   * @param {number} x X.
   * @param {number} y Y.
   * @param {import("../coordinate.js").Coordinate} closestPoint Closest point.
   * @param {number} minSquaredDistance Minimum squared distance.
   * @return {number} Minimum squared distance.
   * @override
   */
  closestPointXY(t, i, a, u) {
    return u < ed(this.getExtent(), t, i) ? u : (this.maxDeltaRevision_ != this.getRevision() && (this.maxDelta_ = Math.sqrt(
      hT(
        this.flatCoordinates,
        0,
        this.flatCoordinates.length,
        this.stride,
        0
      )
    ), this.maxDeltaRevision_ = this.getRevision()), gT(
      this.flatCoordinates,
      0,
      this.flatCoordinates.length,
      this.stride,
      this.maxDelta_,
      !0,
      t,
      i,
      a,
      u
    ));
  }
  /**
   * Return the area of the linear ring on projected plane.
   * @return {number} Area (on projected plane).
   * @api
   */
  getArea() {
    return O2(
      this.flatCoordinates,
      0,
      this.flatCoordinates.length,
      this.stride
    );
  }
  /**
   * Return the coordinates of the linear ring.
   * @return {Array<import("../coordinate.js").Coordinate>} Coordinates.
   * @api
   * @override
   */
  getCoordinates() {
    return qc(
      this.flatCoordinates,
      0,
      this.flatCoordinates.length,
      this.stride
    );
  }
  /**
   * @param {number} squaredTolerance Squared tolerance.
   * @return {LinearRing} Simplified LinearRing.
   * @protected
   * @override
   */
  getSimplifiedGeometryInternal(t) {
    const i = [];
    return i.length = yS(
      this.flatCoordinates,
      0,
      this.flatCoordinates.length,
      this.stride,
      t,
      i,
      0
    ), new __(i, "XY");
  }
  /**
   * Get the type of this geometry.
   * @return {import("./Geometry.js").Type} Geometry type.
   * @api
   * @override
   */
  getType() {
    return "LinearRing";
  }
  /**
   * Test if the geometry and the passed extent intersect.
   * @param {import("../extent.js").Extent} extent Extent.
   * @return {boolean} `true` if the geometry and the extent intersect.
   * @api
   * @override
   */
  intersectsExtent(t) {
    return !1;
  }
  /**
   * Set the coordinates of the linear ring.
   * @param {!Array<import("../coordinate.js").Coordinate>} coordinates Coordinates.
   * @param {import("./Geometry.js").GeometryLayout} [layout] Layout.
   * @api
   * @override
   */
  setCoordinates(t, i) {
    this.setLayout(i, t, 1), this.flatCoordinates || (this.flatCoordinates = []), this.flatCoordinates.length = mS(
      this.flatCoordinates,
      0,
      t,
      this.stride
    ), this.changed();
  }
}
class gu extends Jc {
  /**
   * @param {import("../coordinate.js").Coordinate} coordinates Coordinates.
   * @param {import("./Geometry.js").GeometryLayout} [layout] Layout.
   */
  constructor(t, i) {
    super(), this.setCoordinates(t, i);
  }
  /**
   * Make a complete copy of the geometry.
   * @return {!Point} Clone.
   * @api
   * @override
   */
  clone() {
    const t = new gu(this.flatCoordinates.slice(), this.layout);
    return t.applyProperties(this), t;
  }
  /**
   * @param {number} x X.
   * @param {number} y Y.
   * @param {import("../coordinate.js").Coordinate} closestPoint Closest point.
   * @param {number} minSquaredDistance Minimum squared distance.
   * @return {number} Minimum squared distance.
   * @override
   */
  closestPointXY(t, i, a, u) {
    const h = this.flatCoordinates, g = du(
      t,
      i,
      h[0],
      h[1]
    );
    if (g < u) {
      const y = this.stride;
      for (let _ = 0; _ < y; ++_)
        a[_] = h[_];
      return a.length = y, g;
    }
    return u;
  }
  /**
   * Return the coordinate of the point.
   * @return {import("../coordinate.js").Coordinate} Coordinates.
   * @api
   * @override
   */
  getCoordinates() {
    return this.flatCoordinates.slice();
  }
  /**
   * @param {import("../extent.js").Extent} extent Extent.
   * @protected
   * @return {import("../extent.js").Extent} extent Extent.
   * @override
   */
  computeExtent(t) {
    return g2(this.flatCoordinates, t);
  }
  /**
   * Get the type of this geometry.
   * @return {import("./Geometry.js").Type} Geometry type.
   * @api
   * @override
   */
  getType() {
    return "Point";
  }
  /**
   * Test if the geometry and the passed extent intersect.
   * @param {import("../extent.js").Extent} extent Extent.
   * @return {boolean} `true` if the geometry and the extent intersect.
   * @api
   * @override
   */
  intersectsExtent(t) {
    return eT(t, this.flatCoordinates[0], this.flatCoordinates[1]);
  }
  /**
   * @param {!Array<*>} coordinates Coordinates.
   * @param {import("./Geometry.js").GeometryLayout} [layout] Layout.
   * @api
   * @override
   */
  setCoordinates(t, i) {
    this.setLayout(i, t, 0), this.flatCoordinates || (this.flatCoordinates = []), this.flatCoordinates.length = z2(
      this.flatCoordinates,
      0,
      t,
      this.stride
    ), this.changed();
  }
}
function ED(r, t, i, a, u) {
  return !iT(
    u,
    /**
     * @param {import("../../coordinate.js").Coordinate} coordinate Coordinate.
     * @return {boolean} Contains (x, y).
     */
    function(g) {
      return !Zh(
        r,
        t,
        i,
        a,
        g[0],
        g[1]
      );
    }
  );
}
function Zh(r, t, i, a, u, h) {
  let g = 0, y = r[i - a], _ = r[i - a + 1];
  for (; t < i; t += a) {
    const b = r[t], E = r[t + 1];
    _ <= h ? E > h && (b - y) * (h - _) - (u - y) * (E - _) > 0 && g++ : E <= h && (b - y) * (h - _) - (u - y) * (E - _) < 0 && g--, y = b, _ = E;
  }
  return g !== 0;
}
function _T(r, t, i, a, u, h) {
  if (i.length === 0 || !Zh(r, t, i[0], a, u, h))
    return !1;
  for (let g = 1, y = i.length; g < y; ++g)
    if (Zh(r, i[g - 1], i[g], a, u, h))
      return !1;
  return !0;
}
function bD(r, t, i, a, u, h) {
  if (i.length === 0)
    return !1;
  for (let g = 0, y = i.length; g < y; ++g) {
    const _ = i[g];
    if (_T(r, t, _, a, u, h))
      return !0;
    t = _[_.length - 1];
  }
  return !1;
}
function vT(r, t, i, a, u, h, g) {
  let y, _, b, E, x, R, w;
  const L = u[h + 1], H = [];
  for (let U = 0, I = i.length; U < I; ++U) {
    const X = i[U];
    for (E = r[X - a], R = r[X - a + 1], y = t; y < X; y += a)
      x = r[y], w = r[y + 1], (L <= R && w <= L || R <= L && L <= w) && (b = (L - R) / (w - R) * (x - E) + E, H.push(b)), E = x, R = w;
  }
  let N = NaN, O = -1 / 0;
  for (H.sort(hu), E = H[0], y = 1, _ = H.length; y < _; ++y) {
    x = H[y];
    const U = Math.abs(x - E);
    U > O && (b = (E + x) / 2, _T(r, t, i, a, b, L) && (N = b, O = U)), E = x;
  }
  return isNaN(N) && (N = u[h]), g ? (g.push(N, L, O), g) : [N, L, O];
}
function U2(r, t, i, a, u) {
  let h = [];
  for (let g = 0, y = i.length; g < y; ++g) {
    const _ = i[g];
    h = vT(
      r,
      t,
      _,
      a,
      u,
      2 * g,
      h
    ), t = _[_.length - 1];
  }
  return h;
}
function G2(r, t, i, a, u) {
  let h;
  for (t += a; t < i; t += a)
    if (h = u(
      r.slice(t - a, t),
      r.slice(t, t + a)
    ), h)
      return h;
  return !1;
}
function pR(r, t) {
  const [i, a] = r, [u, h] = t, g = ((i[0] - u[0]) * (u[1] - h[1]) - (i[1] - u[1]) * (u[0] - h[0])) / ((i[0] - a[0]) * (u[1] - h[1]) - (i[1] - a[1]) * (u[0] - h[0])), y = ((i[0] - u[0]) * (i[1] - a[1]) - (i[1] - u[1]) * (i[0] - a[0])) / ((i[0] - a[0]) * (u[1] - h[1]) - (i[1] - a[1]) * (u[0] - h[0]));
  if (0 <= g && g <= 1 && 0 <= y && y <= 1)
    return [i[0] + g * (a[0] - i[0]), i[1] + g * (a[1] - i[1])];
}
function pS(r, t, i, a, u, h) {
  return h = h ?? y2(_a(), r, t, i, a), qi(u, h) ? h[0] >= u[0] && h[2] <= u[2] || h[1] >= u[1] && h[3] <= u[3] ? !0 : G2(
    r,
    t,
    i,
    a,
    /**
     * @param {import("../../coordinate.js").Coordinate} point1 Start point.
     * @param {import("../../coordinate.js").Coordinate} point2 End point.
     * @return {boolean} `true` if the segment and the extent intersect,
     *     `false` otherwise.
     */
    function(g, y) {
      return pw(u, g, y);
    }
  ) : !1;
}
function TD(r, t, i, a, u) {
  for (let h = 0, g = i.length; h < g; ++h) {
    if (pS(r, t, i[h], a, u))
      return !0;
    t = i[h];
  }
  return !1;
}
function H2(r, t, i, a, u) {
  return !!(pS(r, t, i, a, u) || Zh(
    r,
    t,
    i,
    a,
    u[0],
    u[1]
  ) || Zh(
    r,
    t,
    i,
    a,
    u[0],
    u[3]
  ) || Zh(
    r,
    t,
    i,
    a,
    u[2],
    u[1]
  ) || Zh(
    r,
    t,
    i,
    a,
    u[2],
    u[3]
  ));
}
function Y2(r, t, i, a, u) {
  if (!H2(r, t, i[0], a, u))
    return !1;
  if (i.length === 1)
    return !0;
  for (let h = 1, g = i.length; h < g; ++h)
    if (ED(
      r,
      i[h - 1],
      i[h],
      a,
      u
    ) && !pS(
      r,
      i[h - 1],
      i[h],
      a,
      u
    ))
      return !1;
  return !0;
}
function xD(r, t, i, a, u) {
  for (let h = 0, g = i.length; h < g; ++h) {
    const y = i[h];
    if (Y2(r, t, y, a, u))
      return !0;
    t = y[y.length - 1];
  }
  return !1;
}
function CD(r, t, i, a) {
  for (; t < i - a; ) {
    for (let u = 0; u < a; ++u) {
      const h = r[t + u];
      r[t + u] = r[i - a + u], r[i - a + u] = h;
    }
    t += a, i -= a;
  }
}
function ST(r, t, i, a) {
  let u = 0, h = r[i - a], g = r[i - a + 1];
  for (; t < i; t += a) {
    const y = r[t], _ = r[t + 1];
    u += (y - h) * (_ + g), h = y, g = _;
  }
  return u === 0 ? void 0 : u > 0;
}
function j2(r, t, i, a, u) {
  u = u !== void 0 ? u : !1;
  for (let h = 0, g = i.length; h < g; ++h) {
    const y = i[h], _ = ST(
      r,
      t,
      y,
      a
    );
    if (h === 0) {
      if (u && _ || !u && !_)
        return !1;
    } else if (u && !_ || !u && _)
      return !1;
    t = y;
  }
  return !0;
}
function RD(r, t, i, a, u) {
  for (let h = 0, g = i.length; h < g; ++h) {
    const y = i[h];
    if (!j2(r, t, y, a, u))
      return !1;
    y.length && (t = y[y.length - 1]);
  }
  return !0;
}
function Hb(r, t, i, a, u) {
  u = u !== void 0 ? u : !1;
  for (let h = 0, g = i.length; h < g; ++h) {
    const y = i[h], _ = ST(
      r,
      t,
      y,
      a
    );
    (h === 0 ? u && _ || !u && !_ : u && !_ || !u && _) && CD(r, t, y, a), t = y;
  }
  return t;
}
function _R(r, t, i, a, u) {
  for (let h = 0, g = i.length; h < g; ++h)
    t = Hb(
      r,
      t,
      i[h],
      a,
      u
    );
  return t;
}
function AD(r, t) {
  const i = [];
  let a = 0, u = 0, h;
  for (let g = 0, y = t.length; g < y; ++g) {
    const _ = t[g], b = ST(r, a, _, 2);
    if (h === void 0 && (h = b), b === h)
      i.push(t.slice(u, g + 1));
    else {
      if (i.length === 0)
        continue;
      i[i.length - 1].push(t[u]);
    }
    u = g + 1, a = _;
  }
  return i;
}
class va extends Jc {
  /**
   * @param {!Array<Array<import("../coordinate.js").Coordinate>>|!Array<number>} coordinates
   *     Array of linear rings that define the polygon. The first linear ring of the
   *     array defines the outer-boundary or surface of the polygon. Each subsequent
   *     linear ring defines a hole in the surface of the polygon. A linear ring is
   *     an array of vertices' coordinates where the first coordinate and the last are
   *     equivalent. (For internal use, flat coordinates in combination with
   *     `layout` and `ends` are also accepted.)
   * @param {import("./Geometry.js").GeometryLayout} [layout] Layout.
   * @param {Array<number>} [ends] Ends (for internal use with flat coordinates).
   */
  constructor(t, i, a) {
    super(), this.ends_ = [], this.flatInteriorPointRevision_ = -1, this.flatInteriorPoint_ = null, this.maxDelta_ = -1, this.maxDeltaRevision_ = -1, this.orientedRevision_ = -1, this.orientedFlatCoordinates_ = null, i !== void 0 && a ? (this.setFlatCoordinates(
      i,
      /** @type {Array<number>} */
      t
    ), this.ends_ = a) : this.setCoordinates(
      /** @type {Array<Array<import("../coordinate.js").Coordinate>>} */
      t,
      i
    );
  }
  /**
   * Append the passed linear ring to this polygon.
   * @param {LinearRing} linearRing Linear ring.
   * @api
   */
  appendLinearRing(t) {
    this.flatCoordinates ? ds(this.flatCoordinates, t.getFlatCoordinates()) : this.flatCoordinates = t.getFlatCoordinates().slice(), this.ends_.push(this.flatCoordinates.length), this.changed();
  }
  /**
   * Make a complete copy of the geometry.
   * @return {!Polygon} Clone.
   * @api
   * @override
   */
  clone() {
    const t = new va(
      this.flatCoordinates.slice(),
      this.layout,
      this.ends_.slice()
    );
    return t.applyProperties(this), t;
  }
  /**
   * @param {number} x X.
   * @param {number} y Y.
   * @param {import("../coordinate.js").Coordinate} closestPoint Closest point.
   * @param {number} minSquaredDistance Minimum squared distance.
   * @return {number} Minimum squared distance.
   * @override
   */
  closestPointXY(t, i, a, u) {
    return u < ed(this.getExtent(), t, i) ? u : (this.maxDeltaRevision_ != this.getRevision() && (this.maxDelta_ = Math.sqrt(
      dT(
        this.flatCoordinates,
        0,
        this.ends_,
        this.stride,
        0
      )
    ), this.maxDeltaRevision_ = this.getRevision()), mT(
      this.flatCoordinates,
      0,
      this.ends_,
      this.stride,
      this.maxDelta_,
      !0,
      t,
      i,
      a,
      u
    ));
  }
  /**
   * @param {number} x X.
   * @param {number} y Y.
   * @return {boolean} Contains (x, y).
   * @override
   */
  containsXY(t, i) {
    return _T(
      this.getOrientedFlatCoordinates(),
      0,
      this.ends_,
      this.stride,
      t,
      i
    );
  }
  /**
   * Return the area of the polygon on projected plane.
   * @return {number} Area (on projected plane).
   * @api
   */
  getArea() {
    return L2(
      this.getOrientedFlatCoordinates(),
      0,
      this.ends_,
      this.stride
    );
  }
  /**
   * Get the coordinate array for this geometry.  This array has the structure
   * of a GeoJSON coordinate array for polygons.
   *
   * @param {boolean} [right] Orient coordinates according to the right-hand
   *     rule (counter-clockwise for exterior and clockwise for interior rings).
   *     If `false`, coordinates will be oriented according to the left-hand rule
   *     (clockwise for exterior and counter-clockwise for interior rings).
   *     By default, coordinate orientation will depend on how the geometry was
   *     constructed.
   * @return {Array<Array<import("../coordinate.js").Coordinate>>} Coordinates.
   * @api
   * @override
   */
  getCoordinates(t) {
    let i;
    return t !== void 0 ? (i = this.getOrientedFlatCoordinates().slice(), Hb(i, 0, this.ends_, this.stride, t)) : i = this.flatCoordinates, p_(i, 0, this.ends_, this.stride);
  }
  /**
   * @return {Array<number>} Ends.
   */
  getEnds() {
    return this.ends_;
  }
  /**
   * @return {Array<number>} Interior point.
   */
  getFlatInteriorPoint() {
    if (this.flatInteriorPointRevision_ != this.getRevision()) {
      const t = Pc(this.getExtent());
      this.flatInteriorPoint_ = vT(
        this.getOrientedFlatCoordinates(),
        0,
        this.ends_,
        this.stride,
        t,
        0
      ), this.flatInteriorPointRevision_ = this.getRevision();
    }
    return (
      /** @type {import("../coordinate.js").Coordinate} */
      this.flatInteriorPoint_
    );
  }
  /**
   * Return an interior point of the polygon.
   * @return {Point} Interior point as XYM coordinate, where M is the
   * length of the horizontal intersection that the point belongs to.
   * @api
   */
  getInteriorPoint() {
    return new gu(this.getFlatInteriorPoint(), "XYM");
  }
  /**
   * Return the number of rings of the polygon,  this includes the exterior
   * ring and any interior rings.
   *
   * @return {number} Number of rings.
   * @api
   */
  getLinearRingCount() {
    return this.ends_.length;
  }
  /**
   * Return the Nth linear ring of the polygon geometry. Return `null` if the
   * given index is out of range.
   * The exterior linear ring is available at index `0` and the interior rings
   * at index `1` and beyond.
   *
   * @param {number} index Index.
   * @return {LinearRing|null} Linear ring.
   * @api
   */
  getLinearRing(t) {
    return t < 0 || this.ends_.length <= t ? null : new __(
      this.flatCoordinates.slice(
        t === 0 ? 0 : this.ends_[t - 1],
        this.ends_[t]
      ),
      this.layout
    );
  }
  /**
   * Return the linear rings of the polygon.
   * @return {Array<LinearRing>} Linear rings.
   * @api
   */
  getLinearRings() {
    const t = this.layout, i = this.flatCoordinates, a = this.ends_, u = [];
    let h = 0;
    for (let g = 0, y = a.length; g < y; ++g) {
      const _ = a[g], b = new __(
        i.slice(h, _),
        t
      );
      u.push(b), h = _;
    }
    return u;
  }
  /**
   * @return {Array<number>} Oriented flat coordinates.
   */
  getOrientedFlatCoordinates() {
    if (this.orientedRevision_ != this.getRevision()) {
      const t = this.flatCoordinates;
      j2(t, 0, this.ends_, this.stride) ? this.orientedFlatCoordinates_ = t : (this.orientedFlatCoordinates_ = t.slice(), this.orientedFlatCoordinates_.length = Hb(
        this.orientedFlatCoordinates_,
        0,
        this.ends_,
        this.stride
      )), this.orientedRevision_ = this.getRevision();
    }
    return (
      /** @type {Array<number>} */
      this.orientedFlatCoordinates_
    );
  }
  /**
   * @param {number} squaredTolerance Squared tolerance.
   * @return {Polygon} Simplified Polygon.
   * @protected
   * @override
   */
  getSimplifiedGeometryInternal(t) {
    const i = [], a = [];
    return i.length = pT(
      this.flatCoordinates,
      0,
      this.ends_,
      this.stride,
      Math.sqrt(t),
      i,
      0,
      a
    ), new va(i, "XY", a);
  }
  /**
   * Get the type of this geometry.
   * @return {import("./Geometry.js").Type} Geometry type.
   * @api
   * @override
   */
  getType() {
    return "Polygon";
  }
  /**
   * Test if the geometry and the passed extent intersect.
   * @param {import("../extent.js").Extent} extent Extent.
   * @return {boolean} `true` if the geometry and the extent intersect.
   * @api
   * @override
   */
  intersectsExtent(t) {
    return Y2(
      this.getOrientedFlatCoordinates(),
      0,
      this.ends_,
      this.stride,
      t
    );
  }
  /**
   * Set the coordinates of the polygon.
   * @param {!Array<Array<import("../coordinate.js").Coordinate>>} coordinates Coordinates.
   * @param {import("./Geometry.js").GeometryLayout} [layout] Layout.
   * @api
   * @override
   */
  setCoordinates(t, i) {
    this.setLayout(i, t, 2), this.flatCoordinates || (this.flatCoordinates = []);
    const a = yT(
      this.flatCoordinates,
      0,
      t,
      this.stride,
      this.ends_
    );
    this.flatCoordinates.length = a.length === 0 ? 0 : a[a.length - 1], this.changed();
  }
}
function vR(r) {
  if (M_(r))
    throw new Error("Cannot create polygon from empty extent");
  const t = r[0], i = r[1], a = r[2], u = r[3], h = [
    t,
    i,
    t,
    u,
    a,
    u,
    a,
    i,
    t,
    i
  ];
  return new va(h, "XY", [h.length]);
}
function MD(r, t, i) {
  t = t || 32;
  const a = r.getStride(), u = r.getLayout(), h = r.getCenter(), g = a * (t + 1), y = new Array(g);
  for (let E = 0; E < g; E += a) {
    y[E] = 0, y[E + 1] = 0;
    for (let x = 2; x < a; x++)
      y[E + x] = h[x];
  }
  const _ = [y.length], b = new va(y, u, _);
  return wD(b, h, r.getRadius()), b;
}
function wD(r, t, i, a) {
  const u = r.getFlatCoordinates(), h = r.getStride(), g = u.length / h - 1, y = 0;
  for (let _ = 0; _ <= g; ++_) {
    const b = _ * h, E = y + Qh(_, g) * 2 * Math.PI / g;
    u[b] = t[0] + i * Math.cos(E), u[b + 1] = t[1] + i * Math.sin(E);
  }
  r.changed();
}
function P1(r, t, i, a, u, h, g) {
  let y, _;
  const b = (i - t) / a;
  if (b === 1)
    y = t;
  else if (b === 2)
    y = t, _ = u;
  else if (b !== 0) {
    let E = r[t], x = r[t + 1], R = 0;
    const w = [0];
    for (let N = t + a; N < i; N += a) {
      const O = r[N], U = r[N + 1];
      R += Math.sqrt((O - E) * (O - E) + (U - x) * (U - x)), w.push(R), E = O, x = U;
    }
    const L = u * R, H = aw(w, L);
    H < 0 ? (_ = (L - w[-H - 2]) / (w[-H - 1] - w[-H - 2]), y = t + (-H - 2) * a) : y = t + H * a;
  }
  g = g > 1 ? g : 2, h = h || new Array(g);
  for (let E = 0; E < g; ++E)
    h[E] = y === void 0 ? NaN : _ === void 0 ? r[y + E] : ya(r[y + E], r[y + a + E], _);
  return h;
}
function Yb(r, t, i, a, u, h) {
  if (i == t)
    return null;
  let g;
  if (u < r[t + a - 1])
    return h ? (g = r.slice(t, t + a), g[a - 1] = u, g) : null;
  if (r[i - 1] < u)
    return h ? (g = r.slice(i - a, i), g[a - 1] = u, g) : null;
  if (u == r[t + a - 1])
    return r.slice(t, t + a);
  let y = t / a, _ = i / a;
  for (; y < _; ) {
    const R = y + _ >> 1;
    u < r[(R + 1) * a - 1] ? _ = R : y = R + 1;
  }
  const b = r[y * a - 1];
  if (u == b)
    return r.slice((y - 1) * a, (y - 1) * a + a);
  const E = r[(y + 1) * a - 1], x = (u - b) / (E - b);
  g = [];
  for (let R = 0; R < a - 1; ++R)
    g.push(
      ya(
        r[(y - 1) * a + R],
        r[y * a + R],
        x
      )
    );
  return g.push(u), g;
}
function DD(r, t, i, a, u, h, g) {
  if (g)
    return Yb(
      r,
      t,
      i[i.length - 1],
      a,
      u,
      h
    );
  let y;
  if (u < r[a - 1])
    return h ? (y = r.slice(0, a), y[a - 1] = u, y) : null;
  if (r[r.length - 1] < u)
    return h ? (y = r.slice(r.length - a), y[a - 1] = u, y) : null;
  for (let _ = 0, b = i.length; _ < b; ++_) {
    const E = i[_];
    if (t != E) {
      if (u < r[t + a - 1])
        return null;
      if (u <= r[E - 1])
        return Yb(
          r,
          t,
          E,
          a,
          u,
          !1
        );
      t = E;
    }
  }
  return null;
}
function ET(r, t, i, a) {
  let u = r[t], h = r[t + 1], g = 0;
  for (let y = t + a; y < i; y += a) {
    const _ = r[y], b = r[y + 1];
    g += Math.sqrt((_ - u) * (_ - u) + (b - h) * (b - h)), u = _, h = b;
  }
  return g;
}
class to extends Jc {
  /**
   * @param {Array<import("../coordinate.js").Coordinate>|Array<number>} coordinates Coordinates.
   *     For internal use, flat coordinates in combination with `layout` are also accepted.
   * @param {import("./Geometry.js").GeometryLayout} [layout] Layout.
   */
  constructor(t, i) {
    super(), this.flatMidpoint_ = null, this.flatMidpointRevision_ = -1, this.maxDelta_ = -1, this.maxDeltaRevision_ = -1, i !== void 0 && !Array.isArray(t[0]) ? this.setFlatCoordinates(
      i,
      /** @type {Array<number>} */
      t
    ) : this.setCoordinates(
      /** @type {Array<import("../coordinate.js").Coordinate>} */
      t,
      i
    );
  }
  /**
   * Append the passed coordinate to the coordinates of the linestring.
   * @param {import("../coordinate.js").Coordinate} coordinate Coordinate.
   * @api
   */
  appendCoordinate(t) {
    ds(this.flatCoordinates, t), this.changed();
  }
  /**
   * Make a complete copy of the geometry.
   * @return {!LineString} Clone.
   * @api
   * @override
   */
  clone() {
    const t = new to(
      this.flatCoordinates.slice(),
      this.layout
    );
    return t.applyProperties(this), t;
  }
  /**
   * @param {number} x X.
   * @param {number} y Y.
   * @param {import("../coordinate.js").Coordinate} closestPoint Closest point.
   * @param {number} minSquaredDistance Minimum squared distance.
   * @return {number} Minimum squared distance.
   * @override
   */
  closestPointXY(t, i, a, u) {
    return u < ed(this.getExtent(), t, i) ? u : (this.maxDeltaRevision_ != this.getRevision() && (this.maxDelta_ = Math.sqrt(
      hT(
        this.flatCoordinates,
        0,
        this.flatCoordinates.length,
        this.stride,
        0
      )
    ), this.maxDeltaRevision_ = this.getRevision()), gT(
      this.flatCoordinates,
      0,
      this.flatCoordinates.length,
      this.stride,
      this.maxDelta_,
      !1,
      t,
      i,
      a,
      u
    ));
  }
  /**
   * Iterate over each segment, calling the provided callback.
   * If the callback returns a truthy value the function returns that
   * value immediately. Otherwise the function returns `false`.
   *
   * @param {function(this: S, import("../coordinate.js").Coordinate, import("../coordinate.js").Coordinate): T} callback Function
   *     called for each segment. The function will receive two arguments, the start and end coordinates of the segment.
   * @return {T|boolean} Value.
   * @template T,S
   * @api
   */
  forEachSegment(t) {
    return G2(
      this.flatCoordinates,
      0,
      this.flatCoordinates.length,
      this.stride,
      t
    );
  }
  /**
   * Returns the coordinate at `m` using linear interpolation, or `null` if no
   * such coordinate exists.
   *
   * `extrapolate` controls extrapolation beyond the range of Ms in the
   * MultiLineString. If `extrapolate` is `true` then Ms less than the first
   * M will return the first coordinate and Ms greater than the last M will
   * return the last coordinate.
   *
   * @param {number} m M.
   * @param {boolean} [extrapolate] Extrapolate. Default is `false`.
   * @return {import("../coordinate.js").Coordinate|null} Coordinate.
   * @api
   */
  getCoordinateAtM(t, i) {
    return this.layout != "XYM" && this.layout != "XYZM" ? null : (i = i !== void 0 ? i : !1, Yb(
      this.flatCoordinates,
      0,
      this.flatCoordinates.length,
      this.stride,
      t,
      i
    ));
  }
  /**
   * Return the coordinates of the linestring.
   * @return {Array<import("../coordinate.js").Coordinate>} Coordinates.
   * @api
   * @override
   */
  getCoordinates() {
    return qc(
      this.flatCoordinates,
      0,
      this.flatCoordinates.length,
      this.stride
    );
  }
  /**
   * Return the coordinate at the provided fraction along the linestring.
   * The `fraction` is a number between 0 and 1, where 0 is the start of the
   * linestring and 1 is the end.
   * @param {number} fraction Fraction.
   * @param {import("../coordinate.js").Coordinate} [dest] Optional coordinate whose values will
   *     be modified. If not provided, a new coordinate will be returned.
   * @return {import("../coordinate.js").Coordinate} Coordinate of the interpolated point.
   * @api
   */
  getCoordinateAt(t, i) {
    return P1(
      this.flatCoordinates,
      0,
      this.flatCoordinates.length,
      this.stride,
      t,
      i,
      this.stride
    );
  }
  /**
   * Return the length of the linestring on projected plane.
   * @return {number} Length (on projected plane).
   * @api
   */
  getLength() {
    return ET(
      this.flatCoordinates,
      0,
      this.flatCoordinates.length,
      this.stride
    );
  }
  /**
   * @return {Array<number>} Flat midpoint.
   */
  getFlatMidpoint() {
    return this.flatMidpointRevision_ != this.getRevision() && (this.flatMidpoint_ = this.getCoordinateAt(
      0.5,
      this.flatMidpoint_ ?? void 0
    ), this.flatMidpointRevision_ = this.getRevision()), /** @type {Array<number>} */
    this.flatMidpoint_;
  }
  /**
   * @param {number} squaredTolerance Squared tolerance.
   * @return {LineString} Simplified LineString.
   * @protected
   * @override
   */
  getSimplifiedGeometryInternal(t) {
    const i = [];
    return i.length = yS(
      this.flatCoordinates,
      0,
      this.flatCoordinates.length,
      this.stride,
      t,
      i,
      0
    ), new to(i, "XY");
  }
  /**
   * Get the type of this geometry.
   * @return {import("./Geometry.js").Type} Geometry type.
   * @api
   * @override
   */
  getType() {
    return "LineString";
  }
  /**
   * Test if the geometry and the passed extent intersect.
   * @param {import("../extent.js").Extent} extent Extent.
   * @return {boolean} `true` if the geometry and the extent intersect.
   * @api
   * @override
   */
  intersectsExtent(t) {
    return pS(
      this.flatCoordinates,
      0,
      this.flatCoordinates.length,
      this.stride,
      t,
      this.getExtent()
    );
  }
  /**
   * Set the coordinates of the linestring.
   * @param {!Array<import("../coordinate.js").Coordinate>} coordinates Coordinates.
   * @param {import("./Geometry.js").GeometryLayout} [layout] Layout.
   * @api
   * @override
   */
  setCoordinates(t, i) {
    this.setLayout(i, t, 1), this.flatCoordinates || (this.flatCoordinates = []), this.flatCoordinates.length = mS(
      this.flatCoordinates,
      0,
      t,
      this.stride
    ), this.changed();
  }
}
const fs = {
  /**
   * Triggered before a layer is rendered.
   * @event module:ol/render/Event~RenderEvent#prerender
   * @api
   */
  PRERENDER: "prerender",
  /**
   * Triggered after a layer is rendered.
   * @event module:ol/render/Event~RenderEvent#postrender
   * @api
   */
  POSTRENDER: "postrender",
  /**
   * Triggered before layers are composed.  When dispatched by the map, the event object will not have
   * a `context` set.  When dispatched by a layer, the event object will have a `context` set.  Only
   * WebGL layers currently dispatch this event.
   * @event module:ol/render/Event~RenderEvent#precompose
   * @api
   */
  PRECOMPOSE: "precompose",
  /**
   * Triggered after layers are composed.  When dispatched by the map, the event object will not have
   * a `context` set.  When dispatched by a layer, the event object will have a `context` set.  Only
   * WebGL layers currently dispatch this event.
   * @event module:ol/render/Event~RenderEvent#postcompose
   * @api
   */
  POSTCOMPOSE: "postcompose",
  /**
   * Triggered when rendering is complete, i.e. all sources and tiles have
   * finished loading for the current viewport, and all tiles are faded in.
   * The event object will not have a `context` set.
   * @event module:ol/render/Event~RenderEvent#rendercomplete
   * @api
   */
  RENDERCOMPLETE: "rendercomplete"
}, Jh = typeof navigator < "u" && typeof navigator.userAgent < "u" ? navigator.userAgent.toLowerCase() : "", OD = Jh.includes("safari") && !Jh.includes("chrom");
OD && (Jh.includes("version/15.4") || /cpu (os|iphone os) 15_4 like mac os x/.test(Jh));
const LD = Jh.includes("webkit") && !Jh.includes("edge"), B2 = Jh.includes("macintosh"), zD = typeof devicePixelRatio < "u" ? devicePixelRatio : 1, bT = typeof WorkerGlobalScope < "u" && typeof OffscreenCanvas < "u" && self instanceof WorkerGlobalScope, I2 = typeof Image < "u" && Image.prototype.decode, X2 = function() {
  let r = !1;
  try {
    const t = Object.defineProperty({}, "passive", {
      get: function() {
        r = !0;
      }
    });
    window.addEventListener("_", null, t), window.removeEventListener("_", null, t);
  } catch {
  }
  return r;
}(), re = {
  IDLE: 0,
  LOADING: 1,
  LOADED: 2,
  ERROR: 3
};
function ei(r, t, i, a) {
  let u;
  return i && i.length ? u = /** @type {HTMLCanvasElement} */
  i.shift() : bT ? u = new OffscreenCanvas(r || 300, t || 300) : u = document.createElement("canvas"), r && (u.width = r), t && (u.height = t), /** @type {CanvasRenderingContext2D} */
  u.getContext("2d", a);
}
let JE;
function Q1() {
  return JE || (JE = ei(1, 1)), JE;
}
function _S(r) {
  const t = r.canvas;
  t.width = 1, t.height = 1, r.clearRect(0, 0, 1, 1);
}
function SR(r, t) {
  const i = t.parentNode;
  i && i.replaceChild(r, t);
}
function ND(r) {
  for (; r.lastChild; )
    r.lastChild.remove();
}
function UD(r, t) {
  const i = r.childNodes;
  for (let a = 0; ; ++a) {
    const u = i[a], h = t[a];
    if (!u && !h)
      break;
    if (u !== h) {
      if (!u) {
        r.appendChild(h);
        continue;
      }
      if (!h) {
        r.removeChild(u), --a;
        continue;
      }
      r.insertBefore(h, u);
    }
  }
}
const TT = [NaN, NaN, NaN, 0];
let $E;
function GD() {
  return $E || ($E = ei(1, 1, void 0, {
    willReadFrequently: !0,
    desynchronized: !0
  })), $E;
}
const HD = /^rgba?\(\s*(\d+%?)\s+(\d+%?)\s+(\d+%?)(?:\s*\/\s*(\d+%|\d*\.\d+|[01]))?\s*\)$/i, YD = /^rgba?\(\s*(\d+)\s*,\s*(\d+)\s*,\s*(\d+)(?:\s*,\s*(\d+%|\d*\.\d+|[01]))?\s*\)$/i, jD = /^rgba?\(\s*(\d+%)\s*,\s*(\d+%)\s*,\s*(\d+%)(?:\s*,\s*(\d+%|\d*\.\d+|[01]))?\s*\)$/i, BD = /^#([\da-f]{3,4}|[\da-f]{6}|[\da-f]{8})$/i;
function w1(r, t) {
  return r.endsWith("%") ? Number(r.substring(0, r.length - 1)) / t : Number(r);
}
function c_(r) {
  throw new Error('failed to parse "' + r + '" as color');
}
function k2(r) {
  if (r.toLowerCase().startsWith("rgb")) {
    const h = r.match(YD) || r.match(HD) || r.match(jD);
    if (h) {
      const g = h[4], y = 100 / 255;
      return [
        gn(w1(h[1], y) + 0.5 | 0, 0, 255),
        gn(w1(h[2], y) + 0.5 | 0, 0, 255),
        gn(w1(h[3], y) + 0.5 | 0, 0, 255),
        g !== void 0 ? gn(w1(g, 100), 0, 1) : 1
      ];
    }
    c_(r);
  }
  if (r.startsWith("#")) {
    if (BD.test(r)) {
      const h = r.substring(1), g = h.length <= 4 ? 1 : 2, y = [0, 0, 0, 255];
      for (let _ = 0, b = h.length; _ < b; _ += g) {
        let E = parseInt(h.substring(_, _ + g), 16);
        g === 1 && (E += E << 4), y[_ / g] = E;
      }
      return y[3] = y[3] / 255, y;
    }
    c_(r);
  }
  const t = GD();
  t.fillStyle = "#abcdef";
  let i = t.fillStyle;
  t.fillStyle = r, t.fillStyle === i && (t.fillStyle = "#fedcba", i = t.fillStyle, t.fillStyle = r, t.fillStyle === i && c_(r));
  const a = t.fillStyle;
  if (a.startsWith("#") || a.startsWith("rgba"))
    return k2(a);
  t.clearRect(0, 0, 1, 1), t.fillRect(0, 0, 1, 1);
  const u = Array.from(t.getImageData(0, 0, 1, 1).data);
  return u[3] = w_(u[3] / 255, 3), u;
}
function ID(r) {
  return typeof r == "string" ? r : CT(r);
}
const XD = 1024, Q0 = {};
let tb = 0;
function kD(r) {
  if (r.length === 4)
    return r;
  const t = r.slice();
  return t[3] = 1, t;
}
function eb(r) {
  return r > 31308e-7 ? Math.pow(r, 1 / 2.4) * 269.025 - 14.025 : r * 3294.6;
}
function nb(r) {
  return r > 0.2068965 ? Math.pow(r, 3) : (r - 4 / 29) * (108 / 841);
}
function ib(r) {
  return r > 10.314724 ? Math.pow((r + 14.025) / 269.025, 2.4) : r / 3294.6;
}
function lb(r) {
  return r > 88564e-7 ? Math.pow(r, 1 / 3) : r / (108 / 841) + 4 / 29;
}
function ER(r) {
  const t = ib(r[0]), i = ib(r[1]), a = ib(r[2]), u = lb(t * 0.222488403 + i * 0.716873169 + a * 0.06060791), h = 500 * (lb(t * 0.452247074 + i * 0.399439023 + a * 0.148375274) - u), g = 200 * (u - lb(t * 0.016863605 + i * 0.117638439 + a * 0.865350722)), y = Math.atan2(g, h) * (180 / Math.PI);
  return [
    116 * u - 16,
    Math.sqrt(h * h + g * g),
    y < 0 ? y + 360 : y,
    r[3]
  ];
}
function VD(r) {
  const t = (r[0] + 16) / 116, i = r[1], a = r[2] * Math.PI / 180, u = nb(t), h = nb(t + i / 500 * Math.cos(a)), g = nb(t - i / 200 * Math.sin(a)), y = eb(h * 3.021973625 - u * 1.617392459 - g * 0.404875592), _ = eb(h * -0.943766287 + u * 1.916279586 + g * 0.027607165), b = eb(h * 0.069407491 - u * 0.22898585 + g * 1.159737864);
  return [
    gn(y + 0.5 | 0, 0, 255),
    gn(_ + 0.5 | 0, 0, 255),
    gn(b + 0.5 | 0, 0, 255),
    r[3]
  ];
}
function xT(r) {
  if (r === "none")
    return TT;
  if (Q0.hasOwnProperty(r))
    return Q0[r];
  if (tb >= XD) {
    let i = 0;
    for (const a in Q0)
      i++ & 3 || (delete Q0[a], --tb);
  }
  const t = k2(r);
  t.length !== 4 && c_(r);
  for (const i of t)
    isNaN(i) && c_(r);
  return Q0[r] = t, ++tb, t;
}
function Pm(r) {
  return Array.isArray(r) ? r : xT(r);
}
function CT(r) {
  let t = r[0];
  t != (t | 0) && (t = t + 0.5 | 0);
  let i = r[1];
  i != (i | 0) && (i = i + 0.5 | 0);
  let a = r[2];
  a != (a | 0) && (a = a + 0.5 | 0);
  const u = r[3] === void 0 ? 1 : Math.round(r[3] * 1e3) / 1e3;
  return "rgba(" + t + "," + i + "," + a + "," + u + ")";
}
function qD(r, t, i) {
  const a = (
    /** @type {HTMLImageElement} */
    r
  );
  let u = !0, h = !1, g = !1;
  const y = [
    k1(a, $t.LOAD, function() {
      g = !0, h || t();
    })
  ];
  return a.src && I2 ? (h = !0, a.decode().then(function() {
    u && t();
  }).catch(function(_) {
    u && (g ? t() : i());
  })) : y.push(k1(a, $t.ERROR, i)), function() {
    u = !1, y.forEach(qe);
  };
}
function ZD(r, t) {
  return new Promise((i, a) => {
    function u() {
      g(), i(r);
    }
    function h() {
      g(), a(new Error("Image load error"));
    }
    function g() {
      r.removeEventListener("load", u), r.removeEventListener("error", h);
    }
    r.addEventListener("load", u), r.addEventListener("error", h);
  });
}
function FD(r, t) {
  return t && (r.src = t), r.src && I2 ? new Promise(
    (i, a) => r.decode().then(() => i(r)).catch(
      (u) => r.complete && r.width ? i(r) : a(u)
    )
  ) : ZD(r);
}
class KD {
  constructor() {
    this.cache_ = {}, this.patternCache_ = {}, this.cacheSize_ = 0, this.maxCacheSize_ = 1024;
  }
  /**
   * FIXME empty description for jsdoc
   */
  clear() {
    this.cache_ = {}, this.patternCache_ = {}, this.cacheSize_ = 0;
  }
  /**
   * @return {boolean} Can expire cache.
   */
  canExpireCache() {
    return this.cacheSize_ > this.maxCacheSize_;
  }
  /**
   * FIXME empty description for jsdoc
   */
  expire() {
    if (this.canExpireCache()) {
      let t = 0;
      for (const i in this.cache_) {
        const a = this.cache_[i];
        !(t++ & 3) && !a.hasListener() && (delete this.cache_[i], delete this.patternCache_[i], --this.cacheSize_);
      }
    }
  }
  /**
   * @param {string} src Src.
   * @param {?string} crossOrigin Cross origin.
   * @param {import("../color.js").Color|string|null} color Color.
   * @return {import("./IconImage.js").default} Icon image.
   */
  get(t, i, a) {
    const u = ab(t, i, a);
    return u in this.cache_ ? this.cache_[u] : null;
  }
  /**
   * @param {string} src Src.
   * @param {?string} crossOrigin Cross origin.
   * @param {import("../color.js").Color|string|null} color Color.
   * @return {CanvasPattern} Icon image.
   */
  getPattern(t, i, a) {
    const u = ab(t, i, a);
    return u in this.patternCache_ ? this.patternCache_[u] : null;
  }
  /**
   * @param {string} src Src.
   * @param {?string} crossOrigin Cross origin.
   * @param {import("../color.js").Color|string|null} color Color.
   * @param {import("./IconImage.js").default|null} iconImage Icon image.
   * @param {boolean} [pattern] Also cache a `'repeat'` pattern with this `iconImage`.
   */
  set(t, i, a, u, h) {
    const g = ab(t, i, a), y = g in this.cache_;
    this.cache_[g] = u, h && (u.getImageState() === re.IDLE && u.load(), u.getImageState() === re.LOADING ? u.ready().then(() => {
      this.patternCache_[g] = Q1().createPattern(
        u.getImage(1),
        "repeat"
      );
    }) : this.patternCache_[g] = Q1().createPattern(
      u.getImage(1),
      "repeat"
    )), y || ++this.cacheSize_;
  }
  /**
   * Set the cache size of the icon cache. Default is `1024`. Change this value when
   * your map uses more than 1024 different icon images and you are not caching icon
   * styles on the application level.
   * @param {number} maxCacheSize Cache max size.
   * @api
   */
  setSize(t) {
    this.maxCacheSize_ = t, this.expire();
  }
}
function ab(r, t, i) {
  const a = i ? Pm(i) : "null";
  return t + ":" + r + ":" + a;
}
const hs = new KD();
let W0 = null;
class V2 extends oS {
  /**
   * @param {HTMLImageElement|HTMLCanvasElement|ImageBitmap|null} image Image.
   * @param {string|undefined} src Src.
   * @param {?string} crossOrigin Cross origin.
   * @param {import("../ImageState.js").default|undefined} imageState Image state.
   * @param {import("../color.js").Color|string|null} color Color.
   */
  constructor(t, i, a, u, h) {
    super(), this.hitDetectionImage_ = null, this.image_ = t, this.crossOrigin_ = a, this.canvas_ = {}, this.color_ = h, this.imageState_ = u === void 0 ? re.IDLE : u, this.size_ = t && t.width && t.height ? [t.width, t.height] : null, this.src_ = i, this.tainted_, this.ready_ = null;
  }
  /**
   * @private
   */
  initializeImage_() {
    this.image_ = new Image(), this.crossOrigin_ !== null && (this.image_.crossOrigin = this.crossOrigin_);
  }
  /**
   * @private
   * @return {boolean} The image canvas is tainted.
   */
  isTainted_() {
    if (this.tainted_ === void 0 && this.imageState_ === re.LOADED) {
      W0 || (W0 = ei(1, 1, void 0, {
        willReadFrequently: !0
      })), W0.drawImage(this.image_, 0, 0);
      try {
        W0.getImageData(0, 0, 1, 1), this.tainted_ = !1;
      } catch {
        W0 = null, this.tainted_ = !0;
      }
    }
    return this.tainted_ === !0;
  }
  /**
   * @private
   */
  dispatchChangeEvent_() {
    this.dispatchEvent($t.CHANGE);
  }
  /**
   * @private
   */
  handleImageError_() {
    this.imageState_ = re.ERROR, this.dispatchChangeEvent_();
  }
  /**
   * @private
   */
  handleImageLoad_() {
    this.imageState_ = re.LOADED, this.size_ = [this.image_.width, this.image_.height], this.dispatchChangeEvent_();
  }
  /**
   * @param {number} pixelRatio Pixel ratio.
   * @return {HTMLImageElement|HTMLCanvasElement|ImageBitmap} Image or Canvas element or image bitmap.
   */
  getImage(t) {
    return this.image_ || this.initializeImage_(), this.replaceColor_(t), this.canvas_[t] ? this.canvas_[t] : this.image_;
  }
  /**
   * @param {number} pixelRatio Pixel ratio.
   * @return {number} Image or Canvas element.
   */
  getPixelRatio(t) {
    return this.replaceColor_(t), this.canvas_[t] ? t : 1;
  }
  /**
   * @return {import("../ImageState.js").default} Image state.
   */
  getImageState() {
    return this.imageState_;
  }
  /**
   * @return {HTMLImageElement|HTMLCanvasElement|ImageBitmap} Image element.
   */
  getHitDetectionImage() {
    if (this.image_ || this.initializeImage_(), !this.hitDetectionImage_)
      if (this.isTainted_()) {
        const t = this.size_[0], i = this.size_[1], a = ei(t, i);
        a.fillRect(0, 0, t, i), this.hitDetectionImage_ = a.canvas;
      } else
        this.hitDetectionImage_ = this.image_;
    return this.hitDetectionImage_;
  }
  /**
   * Get the size of the icon (in pixels).
   * @return {import("../size.js").Size} Image size.
   */
  getSize() {
    return this.size_;
  }
  /**
   * @return {string|undefined} Image src.
   */
  getSrc() {
    return this.src_;
  }
  /**
   * Load not yet loaded URI.
   */
  load() {
    if (this.imageState_ === re.IDLE) {
      this.image_ || this.initializeImage_(), this.imageState_ = re.LOADING;
      try {
        this.src_ !== void 0 && (this.image_.src = this.src_);
      } catch {
        this.handleImageError_();
      }
      this.image_ instanceof HTMLImageElement && FD(this.image_, this.src_).then((t) => {
        this.image_ = t, this.handleImageLoad_();
      }).catch(this.handleImageError_.bind(this));
    }
  }
  /**
   * @param {number} pixelRatio Pixel ratio.
   * @private
   */
  replaceColor_(t) {
    if (!this.color_ || this.canvas_[t] || this.imageState_ !== re.LOADED)
      return;
    const i = this.image_, a = ei(
      Math.ceil(i.width * t),
      Math.ceil(i.height * t)
    ), u = a.canvas;
    a.scale(t, t), a.drawImage(i, 0, 0), a.globalCompositeOperation = "multiply", a.fillStyle = ID(this.color_), a.fillRect(0, 0, u.width / t, u.height / t), a.globalCompositeOperation = "destination-in", a.drawImage(i, 0, 0), this.canvas_[t] = u;
  }
  /**
   * @return {Promise<void>} Promise that resolves when the image is loaded.
   */
  ready() {
    return this.ready_ || (this.ready_ = new Promise((t) => {
      if (this.imageState_ === re.LOADED || this.imageState_ === re.ERROR)
        t();
      else {
        const i = () => {
          (this.imageState_ === re.LOADED || this.imageState_ === re.ERROR) && (this.removeEventListener($t.CHANGE, i), t());
        };
        this.addEventListener($t.CHANGE, i);
      }
    })), this.ready_;
  }
}
function RT(r, t, i, a, u, h) {
  let g = t === void 0 ? void 0 : hs.get(t, i, u);
  return g || (g = new V2(
    r,
    r && "src" in r ? r.src || void 0 : t,
    i,
    a,
    u
  ), hs.set(t, i, u, g, h)), h && g && !hs.getPattern(t, i, u) && hs.set(t, i, u, g, h), g;
}
function Jo(r) {
  return r ? Array.isArray(r) ? CT(r) : typeof r == "object" && "src" in r ? PD(r) : r : null;
}
function PD(r) {
  if (!r.offset || !r.size)
    return hs.getPattern(r.src, "anonymous", r.color);
  const t = r.src + ":" + r.offset, i = hs.getPattern(
    t,
    void 0,
    r.color
  );
  if (i)
    return i;
  const a = hs.get(r.src, "anonymous", null);
  if (a.getImageState() !== re.LOADED)
    return null;
  const u = ei(
    r.size[0],
    r.size[1]
  );
  return u.drawImage(
    a.getImage(1),
    r.offset[0],
    r.offset[1],
    r.size[0],
    r.size[1],
    0,
    0,
    r.size[0],
    r.size[1]
  ), RT(
    u.canvas,
    t,
    void 0,
    re.LOADED,
    r.color,
    !0
  ), hs.getPattern(t, void 0, r.color);
}
class q2 {
  /**
   * Render a geometry with a custom renderer.
   *
   * @param {import("../geom/SimpleGeometry.js").default} geometry Geometry.
   * @param {import("../Feature.js").FeatureLike} feature Feature.
   * @param {Function} renderer Renderer.
   * @param {Function} hitDetectionRenderer Renderer.
   * @param {number} [index] Render order index.
   */
  drawCustom(t, i, a, u, h) {
  }
  /**
   * Render a geometry.
   *
   * @param {import("../geom/Geometry.js").default} geometry The geometry to render.
   */
  drawGeometry(t) {
  }
  /**
   * Set the rendering style.
   *
   * @param {import("../style/Style.js").default} style The rendering style.
   */
  setStyle(t) {
  }
  /**
   * @param {import("../geom/Circle.js").default} circleGeometry Circle geometry.
   * @param {import("../Feature.js").default} feature Feature.
   * @param {number} [index] Render order index.
   */
  drawCircle(t, i, a) {
  }
  /**
   * @param {import("../Feature.js").default} feature Feature.
   * @param {import("../style/Style.js").default} style Style.
   * @param {number} [index] Render order index.
   */
  drawFeature(t, i, a) {
  }
  /**
   * @param {import("../geom/GeometryCollection.js").default} geometryCollectionGeometry Geometry collection.
   * @param {import("../Feature.js").default} feature Feature.
   * @param {number} [index] Render order index.
   */
  drawGeometryCollection(t, i, a) {
  }
  /**
   * @param {import("../geom/LineString.js").default|import("./Feature.js").default} lineStringGeometry Line string geometry.
   * @param {import("../Feature.js").FeatureLike} feature Feature.
   * @param {number} [index] Render order index.
   */
  drawLineString(t, i, a) {
  }
  /**
   * @param {import("../geom/MultiLineString.js").default|import("./Feature.js").default} multiLineStringGeometry MultiLineString geometry.
   * @param {import("../Feature.js").FeatureLike} feature Feature.
   * @param {number} [index] Render order index.
   */
  drawMultiLineString(t, i, a) {
  }
  /**
   * @param {import("../geom/MultiPoint.js").default|import("./Feature.js").default} multiPointGeometry MultiPoint geometry.
   * @param {import("../Feature.js").FeatureLike} feature Feature.
   * @param {number} [index] Render order index.
   */
  drawMultiPoint(t, i, a) {
  }
  /**
   * @param {import("../geom/MultiPolygon.js").default} multiPolygonGeometry MultiPolygon geometry.
   * @param {import("../Feature.js").FeatureLike} feature Feature.
   * @param {number} [index] Render order index.
   */
  drawMultiPolygon(t, i, a) {
  }
  /**
   * @param {import("../geom/Point.js").default|import("./Feature.js").default} pointGeometry Point geometry.
   * @param {import("../Feature.js").FeatureLike} feature Feature.
   * @param {number} [index] Render order index.
   */
  drawPoint(t, i, a) {
  }
  /**
   * @param {import("../geom/Polygon.js").default|import("./Feature.js").default} polygonGeometry Polygon geometry.
   * @param {import("../Feature.js").FeatureLike} feature Feature.
   * @param {number} [index] Render order index.
   */
  drawPolygon(t, i, a) {
  }
  /**
   * @param {import("../geom/SimpleGeometry.js").default|import("./Feature.js").default} geometry Geometry.
   * @param {import("../Feature.js").FeatureLike} feature Feature.
   * @param {number} [index] Render order index.
   */
  drawText(t, i, a) {
  }
  /**
   * @param {import("../style/Fill.js").default} fillStyle Fill style.
   * @param {import("../style/Stroke.js").default} strokeStyle Stroke style.
   */
  setFillStrokeStyle(t, i) {
  }
  /**
   * @param {import("../style/Image.js").default} imageStyle Image style.
   * @param {import("../render/canvas.js").DeclutterImageWithText} [declutterImageWithText] Shared data for combined decluttering with a text style.
   */
  setImageStyle(t, i) {
  }
  /**
   * @param {import("../style/Text.js").default} textStyle Text style.
   * @param {import("../render/canvas.js").DeclutterImageWithText} [declutterImageWithText] Shared data for combined decluttering with an image style.
   */
  setTextStyle(t, i) {
  }
}
const D1 = "ol-hidden", vS = "ol-unselectable", AT = "ol-control", bR = "ol-collapsed", QD = new RegExp(
  [
    "^\\s*(?=(?:(?:[-a-z]+\\s*){0,2}(italic|oblique))?)",
    "(?=(?:(?:[-a-z]+\\s*){0,2}(small-caps))?)",
    "(?=(?:(?:[-a-z]+\\s*){0,2}(bold(?:er)?|lighter|[1-9]00 ))?)",
    "(?:(?:normal|\\1|\\2|\\3)\\s*){0,3}((?:xx?-)?",
    "(?:small|large)|medium|smaller|larger|[\\.\\d]+(?:\\%|in|[cem]m|ex|p[ctx]))",
    "(?:\\s*\\/\\s*(normal|[\\.\\d]+(?:\\%|in|[cem]m|ex|p[ctx])?))",
    `?\\s*([-,\\"\\'\\sa-z0-9]+?)\\s*$`
  ].join(""),
  "i"
), TR = [
  "style",
  "variant",
  "weight",
  "size",
  "lineHeight",
  "family"
], jb = {
  normal: 400,
  bold: 700
}, Bb = function(r) {
  const t = r.match(QD);
  if (!t)
    return null;
  const i = (
    /** @type {FontParameters} */
    {
      lineHeight: "normal",
      size: "1.2em",
      style: "normal",
      weight: "400",
      variant: "normal"
    }
  );
  for (let a = 0, u = TR.length; a < u; ++a) {
    const h = t[a + 1];
    h !== void 0 && (i[TR[a]] = typeof h == "string" ? h.trim() : h);
  }
  return isNaN(Number(i.weight)) && i.weight in jb && (i.weight = jb[i.weight]), i.families = i.family.split(/,\s?/).map((a) => a.trim().replace(/^['"]|['"]$/g, "")), i;
}, Z2 = "10px sans-serif", Xl = "#000", Qm = "round", mu = [], yu = 0, Wm = "round", v_ = 10, S_ = "#000", E_ = "center", W1 = "middle", Fh = [0, 0, 0, 0], b_ = 1, Nm = new tr();
let J0 = null, xR;
const Ib = {}, WD = /* @__PURE__ */ new Set([
  "serif",
  "sans-serif",
  "monospace",
  "cursive",
  "fantasy",
  "system-ui",
  "ui-serif",
  "ui-sans-serif",
  "ui-monospace",
  "ui-rounded",
  "emoji",
  "math",
  "fangsong"
]);
function JD(r, t, i) {
  return `${r} ${t} 16px "${i}"`;
}
const $D = /* @__PURE__ */ function() {
  let t, i;
  async function a(h) {
    await i.ready;
    const g = await i.load(h);
    if (g.length === 0)
      return !1;
    const y = Bb(h), _ = y.families[0].toLowerCase(), b = y.weight;
    return g.some(
      /**
       * @param {import('../css.js').FontParameters} f Font.
       * @return {boolean} Font matches.
       */
      (E) => {
        const x = E.family.replace(/^['"]|['"]$/g, "").toLowerCase(), R = jb[E.weight] || E.weight;
        return x === _ && E.style === y.style && R == b;
      }
    );
  }
  async function u() {
    await i.ready;
    let h = !0;
    const g = Nm.getProperties(), y = Object.keys(g).filter(
      (_) => g[_] < 100
    );
    for (let _ = y.length - 1; _ >= 0; --_) {
      const b = y[_];
      let E = g[b];
      E < 100 && (await a(b) ? (Jm(Ib), Nm.set(b, 100)) : (E += 10, Nm.set(b, E, !0), E < 100 && (h = !1)));
    }
    t = void 0, h || (t = setTimeout(u, 100));
  }
  return async function(h) {
    i || (i = bT ? self.fonts : document.fonts);
    const g = Bb(h);
    if (!g)
      return;
    const y = g.families;
    let _ = !1;
    for (const b of y) {
      if (WD.has(b))
        continue;
      const E = JD(g.style, g.weight, b);
      Nm.get(E) === void 0 && (Nm.set(E, 0, !0), _ = !0);
    }
    _ && (clearTimeout(t), t = setTimeout(u, 100));
  };
}(), tO = /* @__PURE__ */ function() {
  let r;
  return function(t) {
    let i = Ib[t];
    if (i == null) {
      if (bT) {
        const a = Bb(t), u = F2(t, "Žg");
        i = (isNaN(Number(a.lineHeight)) ? 1.2 : Number(a.lineHeight)) * (u.actualBoundingBoxAscent + u.actualBoundingBoxDescent);
      } else
        r || (r = document.createElement("div"), r.innerHTML = "M", r.style.minHeight = "0", r.style.maxHeight = "none", r.style.height = "auto", r.style.padding = "0", r.style.border = "none", r.style.position = "absolute", r.style.display = "block", r.style.left = "-99999px"), r.style.font = t, document.body.appendChild(r), i = r.offsetHeight, document.body.removeChild(r);
      Ib[t] = i;
    }
    return i;
  };
}();
function F2(r, t) {
  return J0 || (J0 = ei(1, 1)), r != xR && (J0.font = r, xR = J0.font), J0.measureText(t);
}
function K2(r, t) {
  return F2(r, t).width;
}
function CR(r, t, i) {
  if (t in i)
    return i[t];
  const a = t.split(`
`).reduce((u, h) => Math.max(u, K2(r, h)), 0);
  return i[t] = a, a;
}
function eO(r, t) {
  const i = [], a = [], u = [];
  let h = 0, g = 0, y = 0, _ = 0;
  for (let b = 0, E = t.length; b <= E; b += 2) {
    const x = t[b];
    if (x === `
` || b === E) {
      h = Math.max(h, g), u.push(g), g = 0, y += _, _ = 0;
      continue;
    }
    const R = t[b + 1] || r.font, w = K2(R, x);
    i.push(w), g += w;
    const L = tO(R);
    a.push(L), _ = Math.max(_, L);
  }
  return { width: h, height: y, widths: i, heights: a, lineWidths: u };
}
function nO(r, t, i, a, u, h, g, y, _, b, E) {
  r.save(), i !== 1 && (r.globalAlpha === void 0 ? r.globalAlpha = (x) => x.globalAlpha *= i : r.globalAlpha *= i), t && r.transform.apply(r, t), /** @type {*} */
  a.contextInstructions ? (r.translate(_, b), r.scale(E[0], E[1]), iO(
    /** @type {Label} */
    a,
    r
  )) : E[0] < 0 || E[1] < 0 ? (r.translate(_, b), r.scale(E[0], E[1]), r.drawImage(
    /** @type {HTMLCanvasElement|HTMLImageElement|HTMLVideoElement} */
    a,
    u,
    h,
    g,
    y,
    0,
    0,
    g,
    y
  )) : r.drawImage(
    /** @type {HTMLCanvasElement|HTMLImageElement|HTMLVideoElement} */
    a,
    u,
    h,
    g,
    y,
    _,
    b,
    g * E[0],
    y * E[1]
  ), r.restore();
}
function iO(r, t) {
  const i = r.contextInstructions;
  for (let a = 0, u = i.length; a < u; a += 2)
    Array.isArray(i[a + 1]) ? t[i[a]].apply(
      t,
      i[a + 1]
    ) : t[i[a]] = i[a + 1];
}
class lO extends q2 {
  /**
   * @param {CanvasRenderingContext2D} context Context.
   * @param {number} pixelRatio Pixel ratio.
   * @param {import("../../extent.js").Extent} extent Extent.
   * @param {import("../../transform.js").Transform} transform Transform.
   * @param {number} viewRotation View rotation.
   * @param {number} [squaredTolerance] Optional squared tolerance for simplification.
   * @param {import("../../proj.js").TransformFunction} [userTransform] Transform from user to view projection.
   */
  constructor(t, i, a, u, h, g, y) {
    super(), this.context_ = t, this.pixelRatio_ = i, this.extent_ = a, this.transform_ = u, this.transformRotation_ = u ? w_(Math.atan2(u[1], u[0]), 10) : 0, this.viewRotation_ = h, this.squaredTolerance_ = g, this.userTransform_ = y, this.contextFillState_ = null, this.contextStrokeState_ = null, this.contextTextState_ = null, this.fillState_ = null, this.strokeState_ = null, this.image_ = null, this.imageAnchorX_ = 0, this.imageAnchorY_ = 0, this.imageHeight_ = 0, this.imageOpacity_ = 0, this.imageOriginX_ = 0, this.imageOriginY_ = 0, this.imageRotateWithView_ = !1, this.imageRotation_ = 0, this.imageScale_ = [0, 0], this.imageWidth_ = 0, this.text_ = "", this.textOffsetX_ = 0, this.textOffsetY_ = 0, this.textRotateWithView_ = !1, this.textRotation_ = 0, this.textScale_ = [0, 0], this.textFillState_ = null, this.textStrokeState_ = null, this.textState_ = null, this.pixelCoordinates_ = [], this.tmpLocalTransform_ = $s();
  }
  /**
   * @param {Array<number>} flatCoordinates Flat coordinates.
   * @param {number} offset Offset.
   * @param {number} end End.
   * @param {number} stride Stride.
   * @private
   */
  drawImages_(t, i, a, u) {
    if (!this.image_)
      return;
    const h = Kc(
      t,
      i,
      a,
      u,
      this.transform_,
      this.pixelCoordinates_
    ), g = this.context_, y = this.tmpLocalTransform_, _ = g.globalAlpha;
    this.imageOpacity_ != 1 && (g.globalAlpha = _ * this.imageOpacity_);
    let b = this.imageRotation_;
    this.transformRotation_ === 0 && (b -= this.viewRotation_), this.imageRotateWithView_ && (b += this.viewRotation_);
    for (let E = 0, x = h.length; E < x; E += 2) {
      const R = h[E] - this.imageAnchorX_, w = h[E + 1] - this.imageAnchorY_;
      if (b !== 0 || this.imageScale_[0] != 1 || this.imageScale_[1] != 1) {
        const L = R + this.imageAnchorX_, H = w + this.imageAnchorY_;
        vu(
          y,
          L,
          H,
          1,
          1,
          b,
          -L,
          -H
        ), g.save(), g.transform.apply(g, y), g.translate(L, H), g.scale(this.imageScale_[0], this.imageScale_[1]), g.drawImage(
          this.image_,
          this.imageOriginX_,
          this.imageOriginY_,
          this.imageWidth_,
          this.imageHeight_,
          -this.imageAnchorX_,
          -this.imageAnchorY_,
          this.imageWidth_,
          this.imageHeight_
        ), g.restore();
      } else
        g.drawImage(
          this.image_,
          this.imageOriginX_,
          this.imageOriginY_,
          this.imageWidth_,
          this.imageHeight_,
          R,
          w,
          this.imageWidth_,
          this.imageHeight_
        );
    }
    this.imageOpacity_ != 1 && (g.globalAlpha = _);
  }
  /**
   * @param {Array<number>} flatCoordinates Flat coordinates.
   * @param {number} offset Offset.
   * @param {number} end End.
   * @param {number} stride Stride.
   * @private
   */
  drawText_(t, i, a, u) {
    if (!this.textState_ || this.text_ === "")
      return;
    this.textFillState_ && this.setContextFillState_(this.textFillState_), this.textStrokeState_ && this.setContextStrokeState_(this.textStrokeState_), this.setContextTextState_(this.textState_);
    const h = Kc(
      t,
      i,
      a,
      u,
      this.transform_,
      this.pixelCoordinates_
    ), g = this.context_;
    let y = this.textRotation_;
    for (this.transformRotation_ === 0 && (y -= this.viewRotation_), this.textRotateWithView_ && (y += this.viewRotation_); i < a; i += u) {
      const _ = h[i] + this.textOffsetX_, b = h[i + 1] + this.textOffsetY_;
      y !== 0 || this.textScale_[0] != 1 || this.textScale_[1] != 1 ? (g.save(), g.translate(_ - this.textOffsetX_, b - this.textOffsetY_), g.rotate(y), g.translate(this.textOffsetX_, this.textOffsetY_), g.scale(this.textScale_[0], this.textScale_[1]), this.textStrokeState_ && g.strokeText(this.text_, 0, 0), this.textFillState_ && g.fillText(this.text_, 0, 0), g.restore()) : (this.textStrokeState_ && g.strokeText(this.text_, _, b), this.textFillState_ && g.fillText(this.text_, _, b));
    }
  }
  /**
   * @param {Array<number>} flatCoordinates Flat coordinates.
   * @param {number} offset Offset.
   * @param {number} end End.
   * @param {number} stride Stride.
   * @param {boolean} close Close.
   * @private
   * @return {number} end End.
   */
  moveToLineTo_(t, i, a, u, h) {
    const g = this.context_, y = Kc(
      t,
      i,
      a,
      u,
      this.transform_,
      this.pixelCoordinates_
    );
    g.moveTo(y[0], y[1]);
    let _ = y.length;
    h && (_ -= 2);
    for (let b = 2; b < _; b += 2)
      g.lineTo(y[b], y[b + 1]);
    return h && g.closePath(), a;
  }
  /**
   * @param {Array<number>} flatCoordinates Flat coordinates.
   * @param {number} offset Offset.
   * @param {Array<number>} ends Ends.
   * @param {number} stride Stride.
   * @private
   * @return {number} End.
   */
  drawRings_(t, i, a, u) {
    for (let h = 0, g = a.length; h < g; ++h)
      i = this.moveToLineTo_(
        t,
        i,
        a[h],
        u,
        !0
      );
    return i;
  }
  /**
   * Render a circle geometry into the canvas.  Rendering is immediate and uses
   * the current fill and stroke styles.
   *
   * @param {import("../../geom/Circle.js").default} geometry Circle geometry.
   * @api
   * @override
   */
  drawCircle(t) {
    if (this.squaredTolerance_ && (t = /** @type {import("../../geom/Circle.js").default} */
    t.simplifyTransformed(
      this.squaredTolerance_,
      this.userTransform_
    )), !!qi(this.extent_, t.getExtent())) {
      if (this.fillState_ || this.strokeState_) {
        this.fillState_ && this.setContextFillState_(this.fillState_), this.strokeState_ && this.setContextStrokeState_(this.strokeState_);
        const i = gD(
          t,
          this.transform_,
          this.pixelCoordinates_
        ), a = i[2] - i[0], u = i[3] - i[1], h = Math.sqrt(a * a + u * u), g = this.context_;
        g.beginPath(), g.arc(
          i[0],
          i[1],
          h,
          0,
          2 * Math.PI
        ), this.fillState_ && g.fill(), this.strokeState_ && g.stroke();
      }
      this.text_ !== "" && this.drawText_(t.getCenter(), 0, 2, 2);
    }
  }
  /**
   * Set the rendering style.  Note that since this is an immediate rendering API,
   * any `zIndex` on the provided style will be ignored.
   *
   * @param {import("../../style/Style.js").default} style The rendering style.
   * @api
   * @override
   */
  setStyle(t) {
    this.setFillStrokeStyle(t.getFill(), t.getStroke()), this.setImageStyle(t.getImage()), this.setTextStyle(t.getText());
  }
  /**
   * @param {import("../../transform.js").Transform} transform Transform.
   */
  setTransform(t) {
    this.transform_ = t;
  }
  /**
   * Render a geometry into the canvas.  Call
   * {@link module:ol/render/canvas/Immediate~CanvasImmediateRenderer#setStyle renderer.setStyle()} first to set the rendering style.
   *
   * @param {import("../../geom/Geometry.js").default|import("../Feature.js").default} geometry The geometry to render.
   * @api
   * @override
   */
  drawGeometry(t) {
    switch (t.getType()) {
      case "Point":
        this.drawPoint(
          /** @type {import("../../geom/Point.js").default} */
          t
        );
        break;
      case "LineString":
        this.drawLineString(
          /** @type {import("../../geom/LineString.js").default} */
          t
        );
        break;
      case "Polygon":
        this.drawPolygon(
          /** @type {import("../../geom/Polygon.js").default} */
          t
        );
        break;
      case "MultiPoint":
        this.drawMultiPoint(
          /** @type {import("../../geom/MultiPoint.js").default} */
          t
        );
        break;
      case "MultiLineString":
        this.drawMultiLineString(
          /** @type {import("../../geom/MultiLineString.js").default} */
          t
        );
        break;
      case "MultiPolygon":
        this.drawMultiPolygon(
          /** @type {import("../../geom/MultiPolygon.js").default} */
          t
        );
        break;
      case "GeometryCollection":
        this.drawGeometryCollection(
          /** @type {import("../../geom/GeometryCollection.js").default} */
          t
        );
        break;
      case "Circle":
        this.drawCircle(
          /** @type {import("../../geom/Circle.js").default} */
          t
        );
        break;
    }
  }
  /**
   * Render a feature into the canvas.  Note that any `zIndex` on the provided
   * style will be ignored - features are rendered immediately in the order that
   * this method is called.  If you need `zIndex` support, you should be using an
   * {@link module:ol/layer/Vector~VectorLayer} instead.
   *
   * @param {import("../../Feature.js").default} feature Feature.
   * @param {import("../../style/Style.js").default} style Style.
   * @api
   * @override
   */
  drawFeature(t, i) {
    const a = i.getGeometryFunction()(t);
    a && (this.setStyle(i), this.drawGeometry(a));
  }
  /**
   * Render a GeometryCollection to the canvas.  Rendering is immediate and
   * uses the current styles appropriate for each geometry in the collection.
   *
   * @param {import("../../geom/GeometryCollection.js").default} geometry Geometry collection.
   * @override
   */
  drawGeometryCollection(t) {
    const i = t.getGeometriesArray();
    for (let a = 0, u = i.length; a < u; ++a)
      this.drawGeometry(i[a]);
  }
  /**
   * Render a Point geometry into the canvas.  Rendering is immediate and uses
   * the current style.
   *
   * @param {import("../../geom/Point.js").default|import("../Feature.js").default} geometry Point geometry.
   * @override
   */
  drawPoint(t) {
    this.squaredTolerance_ && (t = /** @type {import("../../geom/Point.js").default} */
    t.simplifyTransformed(
      this.squaredTolerance_,
      this.userTransform_
    ));
    const i = t.getFlatCoordinates(), a = t.getStride();
    this.image_ && this.drawImages_(i, 0, i.length, a), this.text_ !== "" && this.drawText_(i, 0, i.length, a);
  }
  /**
   * Render a MultiPoint geometry  into the canvas.  Rendering is immediate and
   * uses the current style.
   *
   * @param {import("../../geom/MultiPoint.js").default|import("../Feature.js").default} geometry MultiPoint geometry.
   * @override
   */
  drawMultiPoint(t) {
    this.squaredTolerance_ && (t = /** @type {import("../../geom/MultiPoint.js").default} */
    t.simplifyTransformed(
      this.squaredTolerance_,
      this.userTransform_
    ));
    const i = t.getFlatCoordinates(), a = t.getStride();
    this.image_ && this.drawImages_(i, 0, i.length, a), this.text_ !== "" && this.drawText_(i, 0, i.length, a);
  }
  /**
   * Render a LineString into the canvas.  Rendering is immediate and uses
   * the current style.
   *
   * @param {import("../../geom/LineString.js").default|import("../Feature.js").default} geometry LineString geometry.
   * @override
   */
  drawLineString(t) {
    if (this.squaredTolerance_ && (t = /** @type {import("../../geom/LineString.js").default} */
    t.simplifyTransformed(
      this.squaredTolerance_,
      this.userTransform_
    )), !!qi(this.extent_, t.getExtent())) {
      if (this.strokeState_) {
        this.setContextStrokeState_(this.strokeState_);
        const i = this.context_, a = t.getFlatCoordinates();
        i.beginPath(), this.moveToLineTo_(
          a,
          0,
          a.length,
          t.getStride(),
          !1
        ), i.stroke();
      }
      if (this.text_ !== "") {
        const i = t.getFlatMidpoint();
        this.drawText_(i, 0, 2, 2);
      }
    }
  }
  /**
   * Render a MultiLineString geometry into the canvas.  Rendering is immediate
   * and uses the current style.
   *
   * @param {import("../../geom/MultiLineString.js").default|import("../Feature.js").default} geometry MultiLineString geometry.
   * @override
   */
  drawMultiLineString(t) {
    this.squaredTolerance_ && (t = /** @type {import("../../geom/MultiLineString.js").default} */
    t.simplifyTransformed(
      this.squaredTolerance_,
      this.userTransform_
    ));
    const i = t.getExtent();
    if (qi(this.extent_, i)) {
      if (this.strokeState_) {
        this.setContextStrokeState_(this.strokeState_);
        const a = this.context_, u = t.getFlatCoordinates();
        let h = 0;
        const g = (
          /** @type {Array<number>} */
          t.getEnds()
        ), y = t.getStride();
        a.beginPath();
        for (let _ = 0, b = g.length; _ < b; ++_)
          h = this.moveToLineTo_(
            u,
            h,
            g[_],
            y,
            !1
          );
        a.stroke();
      }
      if (this.text_ !== "") {
        const a = t.getFlatMidpoints();
        this.drawText_(a, 0, a.length, 2);
      }
    }
  }
  /**
   * Render a Polygon geometry into the canvas.  Rendering is immediate and uses
   * the current style.
   *
   * @param {import("../../geom/Polygon.js").default|import("../Feature.js").default} geometry Polygon geometry.
   * @override
   */
  drawPolygon(t) {
    if (this.squaredTolerance_ && (t = /** @type {import("../../geom/Polygon.js").default} */
    t.simplifyTransformed(
      this.squaredTolerance_,
      this.userTransform_
    )), !!qi(this.extent_, t.getExtent())) {
      if (this.strokeState_ || this.fillState_) {
        this.fillState_ && this.setContextFillState_(this.fillState_), this.strokeState_ && this.setContextStrokeState_(this.strokeState_);
        const i = this.context_;
        i.beginPath(), this.drawRings_(
          t.getOrientedFlatCoordinates(),
          0,
          /** @type {Array<number>} */
          t.getEnds(),
          t.getStride()
        ), this.fillState_ && i.fill(), this.strokeState_ && i.stroke();
      }
      if (this.text_ !== "") {
        const i = t.getFlatInteriorPoint();
        this.drawText_(i, 0, 2, 2);
      }
    }
  }
  /**
   * Render MultiPolygon geometry into the canvas.  Rendering is immediate and
   * uses the current style.
   * @param {import("../../geom/MultiPolygon.js").default} geometry MultiPolygon geometry.
   * @override
   */
  drawMultiPolygon(t) {
    if (this.squaredTolerance_ && (t = /** @type {import("../../geom/MultiPolygon.js").default} */
    t.simplifyTransformed(
      this.squaredTolerance_,
      this.userTransform_
    )), !!qi(this.extent_, t.getExtent())) {
      if (this.strokeState_ || this.fillState_) {
        this.fillState_ && this.setContextFillState_(this.fillState_), this.strokeState_ && this.setContextStrokeState_(this.strokeState_);
        const i = this.context_, a = t.getOrientedFlatCoordinates();
        let u = 0;
        const h = t.getEndss(), g = t.getStride();
        i.beginPath();
        for (let y = 0, _ = h.length; y < _; ++y) {
          const b = h[y];
          u = this.drawRings_(a, u, b, g);
        }
        this.fillState_ && i.fill(), this.strokeState_ && i.stroke();
      }
      if (this.text_ !== "") {
        const i = t.getFlatInteriorPoints();
        this.drawText_(i, 0, i.length, 2);
      }
    }
  }
  /**
   * @param {import("../canvas.js").FillState} fillState Fill state.
   * @private
   */
  setContextFillState_(t) {
    const i = this.context_, a = this.contextFillState_;
    a ? a.fillStyle != t.fillStyle && (a.fillStyle = t.fillStyle, i.fillStyle = t.fillStyle) : (i.fillStyle = t.fillStyle, this.contextFillState_ = {
      fillStyle: t.fillStyle
    });
  }
  /**
   * @param {import("../canvas.js").StrokeState} strokeState Stroke state.
   * @private
   */
  setContextStrokeState_(t) {
    const i = this.context_, a = this.contextStrokeState_;
    a ? (a.lineCap != t.lineCap && (a.lineCap = t.lineCap, i.lineCap = t.lineCap), Wc(a.lineDash, t.lineDash) || i.setLineDash(
      a.lineDash = t.lineDash
    ), a.lineDashOffset != t.lineDashOffset && (a.lineDashOffset = t.lineDashOffset, i.lineDashOffset = t.lineDashOffset), a.lineJoin != t.lineJoin && (a.lineJoin = t.lineJoin, i.lineJoin = t.lineJoin), a.lineWidth != t.lineWidth && (a.lineWidth = t.lineWidth, i.lineWidth = t.lineWidth), a.miterLimit != t.miterLimit && (a.miterLimit = t.miterLimit, i.miterLimit = t.miterLimit), a.strokeStyle != t.strokeStyle && (a.strokeStyle = t.strokeStyle, i.strokeStyle = t.strokeStyle)) : (i.lineCap = t.lineCap, i.setLineDash(t.lineDash), i.lineDashOffset = t.lineDashOffset, i.lineJoin = t.lineJoin, i.lineWidth = t.lineWidth, i.miterLimit = t.miterLimit, i.strokeStyle = t.strokeStyle, this.contextStrokeState_ = {
      lineCap: t.lineCap,
      lineDash: t.lineDash,
      lineDashOffset: t.lineDashOffset,
      lineJoin: t.lineJoin,
      lineWidth: t.lineWidth,
      miterLimit: t.miterLimit,
      strokeStyle: t.strokeStyle
    });
  }
  /**
   * @param {import("../canvas.js").TextState} textState Text state.
   * @private
   */
  setContextTextState_(t) {
    const i = this.context_, a = this.contextTextState_, u = t.textAlign ? t.textAlign : E_;
    a ? (a.font != t.font && (a.font = t.font, i.font = t.font), a.textAlign != u && (a.textAlign = u, i.textAlign = u), a.textBaseline != t.textBaseline && (a.textBaseline = t.textBaseline, i.textBaseline = t.textBaseline)) : (i.font = t.font, i.textAlign = u, i.textBaseline = t.textBaseline, this.contextTextState_ = {
      font: t.font,
      textAlign: u,
      textBaseline: t.textBaseline
    });
  }
  /**
   * Set the fill and stroke style for subsequent draw operations.  To clear
   * either fill or stroke styles, pass null for the appropriate parameter.
   *
   * @param {import("../../style/Fill.js").default} fillStyle Fill style.
   * @param {import("../../style/Stroke.js").default} strokeStyle Stroke style.
   * @override
   */
  setFillStrokeStyle(t, i) {
    if (!t)
      this.fillState_ = null;
    else {
      const a = t.getColor();
      this.fillState_ = {
        fillStyle: Jo(
          a || Xl
        )
      };
    }
    if (!i)
      this.strokeState_ = null;
    else {
      const a = i.getColor(), u = i.getLineCap(), h = i.getLineDash(), g = i.getLineDashOffset(), y = i.getLineJoin(), _ = i.getWidth(), b = i.getMiterLimit(), E = h || mu;
      this.strokeState_ = {
        lineCap: u !== void 0 ? u : Qm,
        lineDash: this.pixelRatio_ === 1 ? E : E.map((x) => x * this.pixelRatio_),
        lineDashOffset: (g || yu) * this.pixelRatio_,
        lineJoin: y !== void 0 ? y : Wm,
        lineWidth: (_ !== void 0 ? _ : b_) * this.pixelRatio_,
        miterLimit: b !== void 0 ? b : v_,
        strokeStyle: Jo(
          a || S_
        )
      };
    }
  }
  /**
   * Set the image style for subsequent draw operations.  Pass null to remove
   * the image style.
   *
   * @param {import("../../style/Image.js").default} imageStyle Image style.
   * @override
   */
  setImageStyle(t) {
    let i;
    if (!t || !(i = t.getSize())) {
      this.image_ = null;
      return;
    }
    const a = t.getPixelRatio(this.pixelRatio_), u = t.getAnchor(), h = t.getOrigin();
    this.image_ = t.getImage(this.pixelRatio_), this.imageAnchorX_ = u[0] * a, this.imageAnchorY_ = u[1] * a, this.imageHeight_ = i[1] * a, this.imageOpacity_ = t.getOpacity(), this.imageOriginX_ = h[0], this.imageOriginY_ = h[1], this.imageRotateWithView_ = t.getRotateWithView(), this.imageRotation_ = t.getRotation();
    const g = t.getScaleArray();
    this.imageScale_ = [
      g[0] * this.pixelRatio_ / a,
      g[1] * this.pixelRatio_ / a
    ], this.imageWidth_ = i[0] * a;
  }
  /**
   * Set the text style for subsequent draw operations.  Pass null to
   * remove the text style.
   *
   * @param {import("../../style/Text.js").default} textStyle Text style.
   * @override
   */
  setTextStyle(t) {
    if (!t)
      this.text_ = "";
    else {
      const i = t.getFill();
      if (!i)
        this.textFillState_ = null;
      else {
        const w = i.getColor();
        this.textFillState_ = {
          fillStyle: Jo(
            w || Xl
          )
        };
      }
      const a = t.getStroke();
      if (!a)
        this.textStrokeState_ = null;
      else {
        const w = a.getColor(), L = a.getLineCap(), H = a.getLineDash(), N = a.getLineDashOffset(), O = a.getLineJoin(), U = a.getWidth(), I = a.getMiterLimit();
        this.textStrokeState_ = {
          lineCap: L !== void 0 ? L : Qm,
          lineDash: H || mu,
          lineDashOffset: N || yu,
          lineJoin: O !== void 0 ? O : Wm,
          lineWidth: U !== void 0 ? U : b_,
          miterLimit: I !== void 0 ? I : v_,
          strokeStyle: Jo(
            w || S_
          )
        };
      }
      const u = t.getFont(), h = t.getOffsetX(), g = t.getOffsetY(), y = t.getRotateWithView(), _ = t.getRotation(), b = t.getScaleArray(), E = t.getText(), x = t.getTextAlign(), R = t.getTextBaseline();
      this.textState_ = {
        font: u !== void 0 ? u : Z2,
        textAlign: x !== void 0 ? x : E_,
        textBaseline: R !== void 0 ? R : W1
      }, this.text_ = E !== void 0 ? Array.isArray(E) ? E.reduce((w, L, H) => w += H % 2 ? " " : L, "") : E : "", this.textOffsetX_ = h !== void 0 ? this.pixelRatio_ * h : 0, this.textOffsetY_ = g !== void 0 ? this.pixelRatio_ * g : 0, this.textRotateWithView_ = y !== void 0 ? y : !1, this.textRotation_ = _ !== void 0 ? _ : 0, this.textScale_ = [
        this.pixelRatio_ * b[0],
        this.pixelRatio_ * b[1]
      ];
    }
  }
}
const aO = 0.5, P2 = {
  Point: dO,
  LineString: cO,
  Polygon: mO,
  MultiPoint: gO,
  MultiLineString: fO,
  MultiPolygon: hO,
  GeometryCollection: uO,
  Circle: oO
};
function sO(r, t) {
  return parseInt(ue(r), 10) - parseInt(ue(t), 10);
}
function RR(r, t) {
  const i = Q2(r, t);
  return i * i;
}
function Q2(r, t) {
  return aO * r / t;
}
function oO(r, t, i, a, u) {
  const h = i.getFill(), g = i.getStroke();
  if (h || g) {
    const _ = r.getBuilder(i.getZIndex(), "Circle");
    _.setFillStrokeStyle(h, g), _.drawCircle(t, a, u);
  }
  const y = i.getText();
  if (y && y.getText()) {
    const _ = r.getBuilder(i.getZIndex(), "Text");
    _.setTextStyle(y), _.drawText(t, a);
  }
}
function AR(r, t, i, a, u, h, g, y) {
  const _ = [], b = i.getImage();
  if (b) {
    let R = !0;
    const w = b.getImageState();
    w == re.LOADED || w == re.ERROR ? R = !1 : w == re.IDLE && b.load(), R && _.push(b.ready());
  }
  const E = i.getFill();
  E && E.loading() && _.push(E.ready());
  const x = _.length > 0;
  return x && Promise.all(_).then(() => u(null)), rO(
    r,
    t,
    i,
    a,
    h,
    g,
    y
  ), x;
}
function rO(r, t, i, a, u, h, g) {
  const y = i.getGeometryFunction()(t);
  if (!y)
    return;
  const _ = y.simplifyTransformed(
    a,
    u
  );
  if (i.getRenderer())
    W2(r, _, i, t, g);
  else {
    const E = P2[_.getType()];
    E(
      r,
      _,
      i,
      t,
      g,
      h
    );
  }
}
function W2(r, t, i, a, u) {
  if (t.getType() == "GeometryCollection") {
    const g = (
      /** @type {import("../geom/GeometryCollection.js").default} */
      t.getGeometries()
    );
    for (let y = 0, _ = g.length; y < _; ++y)
      W2(r, g[y], i, a, u);
    return;
  }
  r.getBuilder(i.getZIndex(), "Default").drawCustom(
    /** @type {import("../geom/SimpleGeometry.js").default} */
    t,
    a,
    i.getRenderer(),
    i.getHitDetectionRenderer(),
    u
  );
}
function uO(r, t, i, a, u, h) {
  const g = t.getGeometriesArray();
  let y, _;
  for (y = 0, _ = g.length; y < _; ++y) {
    const b = P2[g[y].getType()];
    b(
      r,
      g[y],
      i,
      a,
      u,
      h
    );
  }
}
function cO(r, t, i, a, u) {
  const h = i.getStroke();
  if (h) {
    const y = r.getBuilder(
      i.getZIndex(),
      "LineString"
    );
    y.setFillStrokeStyle(null, h), y.drawLineString(t, a, u);
  }
  const g = i.getText();
  if (g && g.getText()) {
    const y = r.getBuilder(i.getZIndex(), "Text");
    y.setTextStyle(g), y.drawText(t, a, u);
  }
}
function fO(r, t, i, a, u) {
  const h = i.getStroke();
  if (h) {
    const y = r.getBuilder(
      i.getZIndex(),
      "LineString"
    );
    y.setFillStrokeStyle(null, h), y.drawMultiLineString(t, a, u);
  }
  const g = i.getText();
  if (g && g.getText()) {
    const y = r.getBuilder(i.getZIndex(), "Text");
    y.setTextStyle(g), y.drawText(t, a, u);
  }
}
function hO(r, t, i, a, u) {
  const h = i.getFill(), g = i.getStroke();
  if (g || h) {
    const _ = r.getBuilder(i.getZIndex(), "Polygon");
    _.setFillStrokeStyle(h, g), _.drawMultiPolygon(t, a, u);
  }
  const y = i.getText();
  if (y && y.getText()) {
    const _ = r.getBuilder(i.getZIndex(), "Text");
    _.setTextStyle(y), _.drawText(t, a, u);
  }
}
function dO(r, t, i, a, u, h) {
  const g = i.getImage(), y = i.getText(), _ = y && y.getText(), b = h && g && _ ? {} : void 0;
  if (g) {
    if (g.getImageState() != re.LOADED)
      return;
    const E = r.getBuilder(i.getZIndex(), "Image");
    E.setImageStyle(g, b), E.drawPoint(t, a, u);
  }
  if (_) {
    const E = r.getBuilder(i.getZIndex(), "Text");
    E.setTextStyle(y, b), E.drawText(t, a, u);
  }
}
function gO(r, t, i, a, u, h) {
  const g = i.getImage(), y = g && g.getOpacity() !== 0, _ = i.getText(), b = _ && _.getText(), E = h && y && b ? {} : void 0;
  if (y) {
    if (g.getImageState() != re.LOADED)
      return;
    const x = r.getBuilder(i.getZIndex(), "Image");
    x.setImageStyle(g, E), x.drawMultiPoint(t, a, u);
  }
  if (b) {
    const x = r.getBuilder(i.getZIndex(), "Text");
    x.setTextStyle(_, E), x.drawText(t, a, u);
  }
}
function mO(r, t, i, a, u) {
  const h = i.getFill(), g = i.getStroke();
  if (h || g) {
    const _ = r.getBuilder(i.getZIndex(), "Polygon");
    _.setFillStrokeStyle(h, g), _.drawPolygon(t, a, u);
  }
  const y = i.getText();
  if (y && y.getText()) {
    const _ = r.getBuilder(i.getZIndex(), "Text");
    _.setTextStyle(y), _.drawText(t, a, u);
  }
}
let yO = !1;
function pO(r, t, i, a, u, h, g) {
  const y = new XMLHttpRequest();
  y.open(
    "GET",
    typeof r == "function" ? r(i, a, u) : r,
    !0
  ), t.getType() == "arraybuffer" && (y.responseType = "arraybuffer"), y.withCredentials = yO, y.onload = function(_) {
    if (!y.status || y.status >= 200 && y.status < 300) {
      const b = t.getType();
      try {
        let E;
        b == "text" || b == "json" ? E = y.responseText : b == "xml" ? E = y.responseXML || y.responseText : b == "arraybuffer" && (E = /** @type {ArrayBuffer} */
        y.response), E ? h(
          /** @type {Array<FeatureType>} */
          t.readFeatures(E, {
            extent: i,
            featureProjection: u
          }),
          t.readProjection(E)
        ) : g();
      } catch {
        g();
      }
    } else
      g();
  }, y.onerror = g, y.send();
}
function MR(r, t) {
  return function(i, a, u, h, g) {
    pO(
      r,
      t,
      i,
      a,
      u,
      /**
       * @param {Array<FeatureType>} features The loaded features.
       * @param {import("./proj/Projection.js").default} dataProjection Data
       * projection.
       */
      (y, _) => {
        this.addFeatures(y), h !== void 0 && h(y);
      },
      () => {
        this.changed(), g !== void 0 && g();
      }
    );
  };
}
function _O(r, t) {
  return [[-1 / 0, -1 / 0, 1 / 0, 1 / 0]];
}
function J2(r, t, i, a) {
  const u = [];
  let h = _a();
  for (let g = 0, y = i.length; g < y; ++g) {
    const _ = i[g];
    h = nT(
      r,
      t,
      _[0],
      a
    ), u.push((h[0] + h[2]) / 2, (h[1] + h[3]) / 2), t = _[_.length - 1];
  }
  return u;
}
class SS extends Jc {
  /**
   * @param {!import("../coordinate.js").Coordinate} center Center.
   *     For internal use, flat coordinates in combination with `layout` and no
   *     `radius` are also accepted.
   * @param {number} [radius] Radius in units of the projection.
   * @param {import("./Geometry.js").GeometryLayout} [layout] Layout.
   */
  constructor(t, i, a) {
    super(), a !== void 0 && i === void 0 ? this.setFlatCoordinates(a, t) : (i = i || 0, this.setCenterAndRadius(t, i, a));
  }
  /**
   * Make a complete copy of the geometry.
   * @return {!Circle} Clone.
   * @api
   * @override
   */
  clone() {
    const t = new SS(
      this.flatCoordinates.slice(),
      void 0,
      this.layout
    );
    return t.applyProperties(this), t;
  }
  /**
   * @param {number} x X.
   * @param {number} y Y.
   * @param {import("../coordinate.js").Coordinate} closestPoint Closest point.
   * @param {number} minSquaredDistance Minimum squared distance.
   * @return {number} Minimum squared distance.
   * @override
   */
  closestPointXY(t, i, a, u) {
    const h = this.flatCoordinates, g = t - h[0], y = i - h[1], _ = g * g + y * y;
    if (_ < u) {
      if (_ === 0)
        for (let b = 0; b < this.stride; ++b)
          a[b] = h[b];
      else {
        const b = this.getRadius() / Math.sqrt(_);
        a[0] = h[0] + b * g, a[1] = h[1] + b * y;
        for (let E = 2; E < this.stride; ++E)
          a[E] = h[E];
      }
      return a.length = this.stride, _;
    }
    return u;
  }
  /**
   * @param {number} x X.
   * @param {number} y Y.
   * @return {boolean} Contains (x, y).
   * @override
   */
  containsXY(t, i) {
    const a = this.flatCoordinates, u = t - a[0], h = i - a[1];
    return u * u + h * h <= this.getRadiusSquared_();
  }
  /**
   * Return the center of the circle as {@link module:ol/coordinate~Coordinate coordinate}.
   * @return {import("../coordinate.js").Coordinate} Center.
   * @api
   */
  getCenter() {
    return this.flatCoordinates.slice(0, this.stride);
  }
  /**
   * @param {import("../extent.js").Extent} extent Extent.
   * @protected
   * @return {import("../extent.js").Extent} extent Extent.
   * @override
   */
  computeExtent(t) {
    const i = this.flatCoordinates, a = i[this.stride] - i[0];
    return $o(
      i[0] - a,
      i[1] - a,
      i[0] + a,
      i[1] + a,
      t
    );
  }
  /**
   * Return the radius of the circle.
   * @return {number} Radius.
   * @api
   */
  getRadius() {
    return Math.sqrt(this.getRadiusSquared_());
  }
  /**
   * @private
   * @return {number} Radius squared.
   */
  getRadiusSquared_() {
    const t = this.flatCoordinates[this.stride] - this.flatCoordinates[0], i = this.flatCoordinates[this.stride + 1] - this.flatCoordinates[1];
    return t * t + i * i;
  }
  /**
   * Get the type of this geometry.
   * @return {import("./Geometry.js").Type} Geometry type.
   * @api
   * @override
   */
  getType() {
    return "Circle";
  }
  /**
   * Test if the geometry and the passed extent intersect.
   * @param {import("../extent.js").Extent} extent Extent.
   * @return {boolean} `true` if the geometry and the extent intersect.
   * @api
   * @override
   */
  intersectsExtent(t) {
    const i = this.getExtent();
    if (qi(t, i)) {
      const a = this.getCenter();
      return t[0] <= a[0] && t[2] >= a[0] || t[1] <= a[1] && t[3] >= a[1] ? !0 : iT(t, this.intersectsCoordinate.bind(this));
    }
    return !1;
  }
  /**
   * Set the center of the circle as {@link module:ol/coordinate~Coordinate coordinate}.
   * @param {import("../coordinate.js").Coordinate} center Center.
   * @api
   */
  setCenter(t) {
    const i = this.stride, a = this.flatCoordinates[i] - this.flatCoordinates[0], u = t.slice();
    u[i] = u[0] + a;
    for (let h = 1; h < i; ++h)
      u[i + h] = t[h];
    this.setFlatCoordinates(this.layout, u), this.changed();
  }
  /**
   * Set the center (as {@link module:ol/coordinate~Coordinate coordinate}) and the radius (as
   * number) of the circle.
   * @param {!import("../coordinate.js").Coordinate} center Center.
   * @param {number} radius Radius.
   * @param {import("./Geometry.js").GeometryLayout} [layout] Layout.
   * @api
   */
  setCenterAndRadius(t, i, a) {
    this.setLayout(a, t, 0), this.flatCoordinates || (this.flatCoordinates = []);
    const u = this.flatCoordinates;
    let h = z2(u, 0, t, this.stride);
    u[h++] = u[0] + i;
    for (let g = 1, y = this.stride; g < y; ++g)
      u[h++] = u[g];
    u.length = h, this.changed();
  }
  /**
   * @override
   */
  getCoordinates() {
    return null;
  }
  /**
   * @override
   */
  setCoordinates(t, i) {
  }
  /**
   * Set the radius of the circle. The radius is in the units of the projection.
   * @param {number} radius Radius.
   * @api
   */
  setRadius(t) {
    this.flatCoordinates[this.stride] = this.flatCoordinates[0] + t, this.changed();
  }
  /**
   * Rotate the geometry around a given coordinate. This modifies the geometry
   * coordinates in place.
   * @param {number} angle Rotation angle in counter-clockwise radians.
   * @param {import("../coordinate.js").Coordinate} anchor The rotation center.
   * @api
   * @override
   */
  rotate(t, i) {
    const a = this.getCenter(), u = this.getStride();
    this.setCenter(
      fT(a, 0, a.length, u, t, i, a)
    ), this.changed();
  }
}
SS.prototype.transform;
class J1 extends D2 {
  /**
   * @param {Array<Geometry>} geometries Geometries.
   */
  constructor(t) {
    super(), this.geometries_ = t, this.changeEventsKeys_ = [], this.listenGeometriesChange_();
  }
  /**
   * @private
   */
  unlistenGeometriesChange_() {
    this.changeEventsKeys_.forEach(qe), this.changeEventsKeys_.length = 0;
  }
  /**
   * @private
   */
  listenGeometriesChange_() {
    const t = this.geometries_;
    for (let i = 0, a = t.length; i < a; ++i)
      this.changeEventsKeys_.push(
        ge(t[i], $t.CHANGE, this.changed, this)
      );
  }
  /**
   * Make a complete copy of the geometry.
   * @return {!GeometryCollection} Clone.
   * @api
   * @override
   */
  clone() {
    const t = new J1(
      sb(this.geometries_)
    );
    return t.applyProperties(this), t;
  }
  /**
   * @param {number} x X.
   * @param {number} y Y.
   * @param {import("../coordinate.js").Coordinate} closestPoint Closest point.
   * @param {number} minSquaredDistance Minimum squared distance.
   * @return {number} Minimum squared distance.
   * @override
   */
  closestPointXY(t, i, a, u) {
    if (u < ed(this.getExtent(), t, i))
      return u;
    const h = this.geometries_;
    for (let g = 0, y = h.length; g < y; ++g)
      u = h[g].closestPointXY(
        t,
        i,
        a,
        u
      );
    return u;
  }
  /**
   * @param {number} x X.
   * @param {number} y Y.
   * @return {boolean} Contains (x, y).
   * @override
   */
  containsXY(t, i) {
    const a = this.geometries_;
    for (let u = 0, h = a.length; u < h; ++u)
      if (a[u].containsXY(t, i))
        return !0;
    return !1;
  }
  /**
   * @param {import("../extent.js").Extent} extent Extent.
   * @protected
   * @return {import("../extent.js").Extent} extent Extent.
   * @override
   */
  computeExtent(t) {
    $m(t);
    const i = this.geometries_;
    for (let a = 0, u = i.length; a < u; ++a)
      m2(t, i[a].getExtent());
    return t;
  }
  /**
   * Return the geometries that make up this geometry collection.
   * @return {Array<Geometry>} Geometries.
   * @api
   */
  getGeometries() {
    return sb(this.geometries_);
  }
  /**
   * @return {Array<Geometry>} Geometries.
   */
  getGeometriesArray() {
    return this.geometries_;
  }
  /**
   * @return {Array<Geometry>} Geometries.
   */
  getGeometriesArrayRecursive() {
    let t = [];
    const i = this.geometries_;
    for (let a = 0, u = i.length; a < u; ++a)
      i[a].getType() === this.getType() ? t = t.concat(
        /** @type {GeometryCollection} */
        i[a].getGeometriesArrayRecursive()
      ) : t.push(i[a]);
    return t;
  }
  /**
   * Create a simplified version of this geometry using the Douglas Peucker algorithm.
   * @param {number} squaredTolerance Squared tolerance.
   * @return {GeometryCollection} Simplified GeometryCollection.
   * @override
   */
  getSimplifiedGeometry(t) {
    if (this.simplifiedGeometryRevision !== this.getRevision() && (this.simplifiedGeometryMaxMinSquaredTolerance = 0, this.simplifiedGeometryRevision = this.getRevision()), t < 0 || this.simplifiedGeometryMaxMinSquaredTolerance !== 0 && t < this.simplifiedGeometryMaxMinSquaredTolerance)
      return this;
    const i = [], a = this.geometries_;
    let u = !1;
    for (let h = 0, g = a.length; h < g; ++h) {
      const y = a[h], _ = y.getSimplifiedGeometry(t);
      i.push(_), _ !== y && (u = !0);
    }
    return u ? new J1(
      i
    ) : (this.simplifiedGeometryMaxMinSquaredTolerance = t, this);
  }
  /**
   * Get the type of this geometry.
   * @return {import("./Geometry.js").Type} Geometry type.
   * @api
   * @override
   */
  getType() {
    return "GeometryCollection";
  }
  /**
   * Test if the geometry and the passed extent intersect.
   * @param {import("../extent.js").Extent} extent Extent.
   * @return {boolean} `true` if the geometry and the extent intersect.
   * @api
   * @override
   */
  intersectsExtent(t) {
    const i = this.geometries_;
    for (let a = 0, u = i.length; a < u; ++a)
      if (i[a].intersectsExtent(t))
        return !0;
    return !1;
  }
  /**
   * @return {boolean} Is empty.
   */
  isEmpty() {
    return this.geometries_.length === 0;
  }
  /**
   * Rotate the geometry around a given coordinate. This modifies the geometry
   * coordinates in place.
   * @param {number} angle Rotation angle in radians.
   * @param {import("../coordinate.js").Coordinate} anchor The rotation center.
   * @api
   * @override
   */
  rotate(t, i) {
    const a = this.geometries_;
    for (let u = 0, h = a.length; u < h; ++u)
      a[u].rotate(t, i);
    this.changed();
  }
  /**
   * Scale the geometry (with an optional origin).  This modifies the geometry
   * coordinates in place.
   * @abstract
   * @param {number} sx The scaling factor in the x-direction.
   * @param {number} [sy] The scaling factor in the y-direction (defaults to sx).
   * @param {import("../coordinate.js").Coordinate} [anchor] The scale origin (defaults to the center
   *     of the geometry extent).
   * @api
   * @override
   */
  scale(t, i, a) {
    a || (a = Pc(this.getExtent()));
    const u = this.geometries_;
    for (let h = 0, g = u.length; h < g; ++h)
      u[h].scale(t, i, a);
    this.changed();
  }
  /**
   * Set the geometries that make up this geometry collection.
   * @param {Array<Geometry>} geometries Geometries.
   * @api
   */
  setGeometries(t) {
    this.setGeometriesArray(sb(t));
  }
  /**
   * @param {Array<Geometry>} geometries Geometries.
   */
  setGeometriesArray(t) {
    this.unlistenGeometriesChange_(), this.geometries_ = t, this.listenGeometriesChange_(), this.changed();
  }
  /**
   * Apply a transform function to the coordinates of the geometry.
   * The geometry is modified in place.
   * If you do not want the geometry modified in place, first `clone()` it and
   * then use this function on the clone.
   * @param {import("../proj.js").TransformFunction} transformFn Transform function.
   * Called with a flat array of geometry coordinates.
   * @api
   * @override
   */
  applyTransform(t) {
    const i = this.geometries_;
    for (let a = 0, u = i.length; a < u; ++a)
      i[a].applyTransform(t);
    this.changed();
  }
  /**
   * Translate the geometry.  This modifies the geometry coordinates in place.  If
   * instead you want a new geometry, first `clone()` this geometry.
   * @param {number} deltaX Delta X.
   * @param {number} deltaY Delta Y.
   * @api
   * @override
   */
  translate(t, i) {
    const a = this.geometries_;
    for (let u = 0, h = a.length; u < h; ++u)
      a[u].translate(t, i);
    this.changed();
  }
  /**
   * Clean up.
   * @override
   */
  disposeInternal() {
    this.unlistenGeometriesChange_(), super.disposeInternal();
  }
}
function sb(r) {
  return r.map((t) => t.clone());
}
class T_ extends Jc {
  /**
   * @param {Array<Array<import("../coordinate.js").Coordinate>|LineString>|Array<number>} coordinates
   *     Coordinates or LineString geometries. (For internal use, flat coordinates in
   *     combination with `layout` and `ends` are also accepted.)
   * @param {import("./Geometry.js").GeometryLayout} [layout] Layout.
   * @param {Array<number>} [ends] Flat coordinate ends for internal use.
   */
  constructor(t, i, a) {
    if (super(), this.ends_ = [], this.maxDelta_ = -1, this.maxDeltaRevision_ = -1, Array.isArray(t[0]))
      this.setCoordinates(
        /** @type {Array<Array<import("../coordinate.js").Coordinate>>} */
        t,
        i
      );
    else if (i !== void 0 && a)
      this.setFlatCoordinates(
        i,
        /** @type {Array<number>} */
        t
      ), this.ends_ = a;
    else {
      const u = (
        /** @type {Array<LineString>} */
        t
      ), h = [], g = [];
      for (let _ = 0, b = u.length; _ < b; ++_) {
        const E = u[_];
        ds(h, E.getFlatCoordinates()), g.push(h.length);
      }
      const y = u.length === 0 ? this.getLayout() : u[0].getLayout();
      this.setFlatCoordinates(y, h), this.ends_ = g;
    }
  }
  /**
   * Append the passed linestring to the multilinestring.
   * @param {LineString} lineString LineString.
   * @api
   */
  appendLineString(t) {
    ds(this.flatCoordinates, t.getFlatCoordinates().slice()), this.ends_.push(this.flatCoordinates.length), this.changed();
  }
  /**
   * Make a complete copy of the geometry.
   * @return {!MultiLineString} Clone.
   * @api
   * @override
   */
  clone() {
    const t = new T_(
      this.flatCoordinates.slice(),
      this.layout,
      this.ends_.slice()
    );
    return t.applyProperties(this), t;
  }
  /**
   * @param {number} x X.
   * @param {number} y Y.
   * @param {import("../coordinate.js").Coordinate} closestPoint Closest point.
   * @param {number} minSquaredDistance Minimum squared distance.
   * @return {number} Minimum squared distance.
   * @override
   */
  closestPointXY(t, i, a, u) {
    return u < ed(this.getExtent(), t, i) ? u : (this.maxDeltaRevision_ != this.getRevision() && (this.maxDelta_ = Math.sqrt(
      dT(
        this.flatCoordinates,
        0,
        this.ends_,
        this.stride,
        0
      )
    ), this.maxDeltaRevision_ = this.getRevision()), mT(
      this.flatCoordinates,
      0,
      this.ends_,
      this.stride,
      this.maxDelta_,
      !1,
      t,
      i,
      a,
      u
    ));
  }
  /**
   * Returns the coordinate at `m` using linear interpolation, or `null` if no
   * such coordinate exists.
   *
   * `extrapolate` controls extrapolation beyond the range of Ms in the
   * MultiLineString. If `extrapolate` is `true` then Ms less than the first
   * M will return the first coordinate and Ms greater than the last M will
   * return the last coordinate.
   *
   * `interpolate` controls interpolation between consecutive LineStrings
   * within the MultiLineString. If `interpolate` is `true` the coordinates
   * will be linearly interpolated between the last coordinate of one LineString
   * and the first coordinate of the next LineString.  If `interpolate` is
   * `false` then the function will return `null` for Ms falling between
   * LineStrings.
   *
   * @param {number} m M.
   * @param {boolean} [extrapolate] Extrapolate. Default is `false`.
   * @param {boolean} [interpolate] Interpolate. Default is `false`.
   * @return {import("../coordinate.js").Coordinate|null} Coordinate.
   * @api
   */
  getCoordinateAtM(t, i, a) {
    return this.layout != "XYM" && this.layout != "XYZM" || this.flatCoordinates.length === 0 ? null : (i = i !== void 0 ? i : !1, a = a !== void 0 ? a : !1, DD(
      this.flatCoordinates,
      0,
      this.ends_,
      this.stride,
      t,
      i,
      a
    ));
  }
  /**
   * Return the coordinates of the multilinestring.
   * @return {Array<Array<import("../coordinate.js").Coordinate>>} Coordinates.
   * @api
   * @override
   */
  getCoordinates() {
    return p_(
      this.flatCoordinates,
      0,
      this.ends_,
      this.stride
    );
  }
  /**
   * @return {Array<number>} Ends.
   */
  getEnds() {
    return this.ends_;
  }
  /**
   * Return the linestring at the specified index.
   * @param {number} index Index.
   * @return {LineString} LineString.
   * @api
   */
  getLineString(t) {
    return t < 0 || this.ends_.length <= t ? null : new to(
      this.flatCoordinates.slice(
        t === 0 ? 0 : this.ends_[t - 1],
        this.ends_[t]
      ),
      this.layout
    );
  }
  /**
   * Return the linestrings of this multilinestring.
   * @return {Array<LineString>} LineStrings.
   * @api
   */
  getLineStrings() {
    const t = this.flatCoordinates, i = this.ends_, a = this.layout, u = [];
    let h = 0;
    for (let g = 0, y = i.length; g < y; ++g) {
      const _ = i[g], b = new to(
        t.slice(h, _),
        a
      );
      u.push(b), h = _;
    }
    return u;
  }
  /**
   * Return the sum of all line string lengths
   * @return {number} Length (on projected plane).
   * @api
   */
  getLength() {
    const t = this.ends_;
    let i = 0, a = 0;
    for (let u = 0, h = t.length; u < h; ++u)
      a += ET(
        this.flatCoordinates,
        i,
        t[u],
        this.stride
      ), i = t[u];
    return a;
  }
  /**
   * @return {Array<number>} Flat midpoints.
   */
  getFlatMidpoints() {
    const t = [], i = this.flatCoordinates;
    let a = 0;
    const u = this.ends_, h = this.stride;
    for (let g = 0, y = u.length; g < y; ++g) {
      const _ = u[g], b = P1(
        i,
        a,
        _,
        h,
        0.5
      );
      ds(t, b), a = _;
    }
    return t;
  }
  /**
   * @param {number} squaredTolerance Squared tolerance.
   * @return {MultiLineString} Simplified MultiLineString.
   * @protected
   * @override
   */
  getSimplifiedGeometryInternal(t) {
    const i = [], a = [];
    return i.length = N2(
      this.flatCoordinates,
      0,
      this.ends_,
      this.stride,
      t,
      i,
      0,
      a
    ), new T_(i, "XY", a);
  }
  /**
   * Get the type of this geometry.
   * @return {import("./Geometry.js").Type} Geometry type.
   * @api
   * @override
   */
  getType() {
    return "MultiLineString";
  }
  /**
   * Test if the geometry and the passed extent intersect.
   * @param {import("../extent.js").Extent} extent Extent.
   * @return {boolean} `true` if the geometry and the extent intersect.
   * @api
   * @override
   */
  intersectsExtent(t) {
    return TD(
      this.flatCoordinates,
      0,
      this.ends_,
      this.stride,
      t
    );
  }
  /**
   * Set the coordinates of the multilinestring.
   * @param {!Array<Array<import("../coordinate.js").Coordinate>>} coordinates Coordinates.
   * @param {import("./Geometry.js").GeometryLayout} [layout] Layout.
   * @api
   * @override
   */
  setCoordinates(t, i) {
    this.setLayout(i, t, 2), this.flatCoordinates || (this.flatCoordinates = []);
    const a = yT(
      this.flatCoordinates,
      0,
      t,
      this.stride,
      this.ends_
    );
    this.flatCoordinates.length = a.length === 0 ? 0 : a[a.length - 1], this.changed();
  }
}
class ES extends Jc {
  /**
   * @param {Array<import("../coordinate.js").Coordinate>|Array<number>} coordinates Coordinates.
   *     For internal use, flat coordinates in combination with `layout` are also accepted.
   * @param {import("./Geometry.js").GeometryLayout} [layout] Layout.
   */
  constructor(t, i) {
    super(), i && !Array.isArray(t[0]) ? this.setFlatCoordinates(
      i,
      /** @type {Array<number>} */
      t
    ) : this.setCoordinates(
      /** @type {Array<import("../coordinate.js").Coordinate>} */
      t,
      i
    );
  }
  /**
   * Append the passed point to this multipoint.
   * @param {Point} point Point.
   * @api
   */
  appendPoint(t) {
    ds(this.flatCoordinates, t.getFlatCoordinates()), this.changed();
  }
  /**
   * Make a complete copy of the geometry.
   * @return {!MultiPoint} Clone.
   * @api
   * @override
   */
  clone() {
    const t = new ES(
      this.flatCoordinates.slice(),
      this.layout
    );
    return t.applyProperties(this), t;
  }
  /**
   * @param {number} x X.
   * @param {number} y Y.
   * @param {import("../coordinate.js").Coordinate} closestPoint Closest point.
   * @param {number} minSquaredDistance Minimum squared distance.
   * @return {number} Minimum squared distance.
   * @override
   */
  closestPointXY(t, i, a, u) {
    if (u < ed(this.getExtent(), t, i))
      return u;
    const h = this.flatCoordinates, g = this.stride;
    for (let y = 0, _ = h.length; y < _; y += g) {
      const b = du(
        t,
        i,
        h[y],
        h[y + 1]
      );
      if (b < u) {
        u = b;
        for (let E = 0; E < g; ++E)
          a[E] = h[y + E];
        a.length = g;
      }
    }
    return u;
  }
  /**
   * Return the coordinates of the multipoint.
   * @return {Array<import("../coordinate.js").Coordinate>} Coordinates.
   * @api
   * @override
   */
  getCoordinates() {
    return qc(
      this.flatCoordinates,
      0,
      this.flatCoordinates.length,
      this.stride
    );
  }
  /**
   * Return the point at the specified index.
   * @param {number} index Index.
   * @return {Point} Point.
   * @api
   */
  getPoint(t) {
    const i = this.flatCoordinates.length / this.stride;
    return t < 0 || i <= t ? null : new gu(
      this.flatCoordinates.slice(
        t * this.stride,
        (t + 1) * this.stride
      ),
      this.layout
    );
  }
  /**
   * Return the points of this multipoint.
   * @return {Array<Point>} Points.
   * @api
   */
  getPoints() {
    const t = this.flatCoordinates, i = this.layout, a = this.stride, u = [];
    for (let h = 0, g = t.length; h < g; h += a) {
      const y = new gu(t.slice(h, h + a), i);
      u.push(y);
    }
    return u;
  }
  /**
   * Get the type of this geometry.
   * @return {import("./Geometry.js").Type} Geometry type.
   * @api
   * @override
   */
  getType() {
    return "MultiPoint";
  }
  /**
   * Test if the geometry and the passed extent intersect.
   * @param {import("../extent.js").Extent} extent Extent.
   * @return {boolean} `true` if the geometry and the extent intersect.
   * @api
   * @override
   */
  intersectsExtent(t) {
    const i = this.flatCoordinates, a = this.stride;
    for (let u = 0, h = i.length; u < h; u += a) {
      const g = i[u], y = i[u + 1];
      if (eT(t, g, y))
        return !0;
    }
    return !1;
  }
  /**
   * Set the coordinates of the multipoint.
   * @param {!Array<import("../coordinate.js").Coordinate>} coordinates Coordinates.
   * @param {import("./Geometry.js").GeometryLayout} [layout] Layout.
   * @api
   * @override
   */
  setCoordinates(t, i) {
    this.setLayout(i, t, 1), this.flatCoordinates || (this.flatCoordinates = []), this.flatCoordinates.length = mS(
      this.flatCoordinates,
      0,
      t,
      this.stride
    ), this.changed();
  }
}
class x_ extends Jc {
  /**
   * @param {Array<Array<Array<import("../coordinate.js").Coordinate>>|Polygon>|Array<number>} coordinates Coordinates.
   *     For internal use, flat coordinates in combination with `layout` and `endss` are also accepted.
   * @param {import("./Geometry.js").GeometryLayout} [layout] Layout.
   * @param {Array<Array<number>>} [endss] Array of ends for internal use with flat coordinates.
   */
  constructor(t, i, a) {
    if (super(), this.endss_ = [], this.flatInteriorPointsRevision_ = -1, this.flatInteriorPoints_ = null, this.maxDelta_ = -1, this.maxDeltaRevision_ = -1, this.orientedRevision_ = -1, this.orientedFlatCoordinates_ = null, !a && !Array.isArray(t[0])) {
      const u = (
        /** @type {Array<Polygon>} */
        t
      ), h = [], g = [];
      for (let y = 0, _ = u.length; y < _; ++y) {
        const b = u[y], E = h.length, x = b.getEnds();
        for (let R = 0, w = x.length; R < w; ++R)
          x[R] += E;
        ds(h, b.getFlatCoordinates()), g.push(x);
      }
      i = u.length === 0 ? this.getLayout() : u[0].getLayout(), t = h, a = g;
    }
    i !== void 0 && a ? (this.setFlatCoordinates(
      i,
      /** @type {Array<number>} */
      t
    ), this.endss_ = a) : this.setCoordinates(
      /** @type {Array<Array<Array<import("../coordinate.js").Coordinate>>>} */
      t,
      i
    );
  }
  /**
   * Append the passed polygon to this multipolygon.
   * @param {Polygon} polygon Polygon.
   * @api
   */
  appendPolygon(t) {
    let i;
    if (!this.flatCoordinates)
      this.flatCoordinates = t.getFlatCoordinates().slice(), i = t.getEnds().slice(), this.endss_.push();
    else {
      const a = this.flatCoordinates.length;
      ds(this.flatCoordinates, t.getFlatCoordinates()), i = t.getEnds().slice();
      for (let u = 0, h = i.length; u < h; ++u)
        i[u] += a;
    }
    this.endss_.push(i), this.changed();
  }
  /**
   * Make a complete copy of the geometry.
   * @return {!MultiPolygon} Clone.
   * @api
   * @override
   */
  clone() {
    const t = this.endss_.length, i = new Array(t);
    for (let u = 0; u < t; ++u)
      i[u] = this.endss_[u].slice();
    const a = new x_(
      this.flatCoordinates.slice(),
      this.layout,
      i
    );
    return a.applyProperties(this), a;
  }
  /**
   * @param {number} x X.
   * @param {number} y Y.
   * @param {import("../coordinate.js").Coordinate} closestPoint Closest point.
   * @param {number} minSquaredDistance Minimum squared distance.
   * @return {number} Minimum squared distance.
   * @override
   */
  closestPointXY(t, i, a, u) {
    return u < ed(this.getExtent(), t, i) ? u : (this.maxDeltaRevision_ != this.getRevision() && (this.maxDelta_ = Math.sqrt(
      yD(
        this.flatCoordinates,
        0,
        this.endss_,
        this.stride,
        0
      )
    ), this.maxDeltaRevision_ = this.getRevision()), pD(
      this.getOrientedFlatCoordinates(),
      0,
      this.endss_,
      this.stride,
      this.maxDelta_,
      !0,
      t,
      i,
      a,
      u
    ));
  }
  /**
   * @param {number} x X.
   * @param {number} y Y.
   * @return {boolean} Contains (x, y).
   * @override
   */
  containsXY(t, i) {
    return bD(
      this.getOrientedFlatCoordinates(),
      0,
      this.endss_,
      this.stride,
      t,
      i
    );
  }
  /**
   * Return the area of the multipolygon on projected plane.
   * @return {number} Area (on projected plane).
   * @api
   */
  getArea() {
    return mD(
      this.getOrientedFlatCoordinates(),
      0,
      this.endss_,
      this.stride
    );
  }
  /**
   * Get the coordinate array for this geometry.  This array has the structure
   * of a GeoJSON coordinate array for multi-polygons.
   *
   * @param {boolean} [right] Orient coordinates according to the right-hand
   *     rule (counter-clockwise for exterior and clockwise for interior rings).
   *     If `false`, coordinates will be oriented according to the left-hand rule
   *     (clockwise for exterior and counter-clockwise for interior rings).
   *     By default, coordinate orientation will depend on how the geometry was
   *     constructed.
   * @return {Array<Array<Array<import("../coordinate.js").Coordinate>>>} Coordinates.
   * @api
   * @override
   */
  getCoordinates(t) {
    let i;
    return t !== void 0 ? (i = this.getOrientedFlatCoordinates().slice(), _R(
      i,
      0,
      this.endss_,
      this.stride,
      t
    )) : i = this.flatCoordinates, Gb(
      i,
      0,
      this.endss_,
      this.stride
    );
  }
  /**
   * @return {Array<Array<number>>} Endss.
   */
  getEndss() {
    return this.endss_;
  }
  /**
   * @return {Array<number>} Flat interior points.
   */
  getFlatInteriorPoints() {
    if (this.flatInteriorPointsRevision_ != this.getRevision()) {
      const t = J2(
        this.flatCoordinates,
        0,
        this.endss_,
        this.stride
      );
      this.flatInteriorPoints_ = U2(
        this.getOrientedFlatCoordinates(),
        0,
        this.endss_,
        this.stride,
        t
      ), this.flatInteriorPointsRevision_ = this.getRevision();
    }
    return (
      /** @type {Array<number>} */
      this.flatInteriorPoints_
    );
  }
  /**
   * Return the interior points as {@link module:ol/geom/MultiPoint~MultiPoint multipoint}.
   * @return {MultiPoint} Interior points as XYM coordinates, where M is
   * the length of the horizontal intersection that the point belongs to.
   * @api
   */
  getInteriorPoints() {
    return new ES(this.getFlatInteriorPoints().slice(), "XYM");
  }
  /**
   * @return {Array<number>} Oriented flat coordinates.
   */
  getOrientedFlatCoordinates() {
    if (this.orientedRevision_ != this.getRevision()) {
      const t = this.flatCoordinates;
      RD(t, 0, this.endss_, this.stride) ? this.orientedFlatCoordinates_ = t : (this.orientedFlatCoordinates_ = t.slice(), this.orientedFlatCoordinates_.length = _R(
        this.orientedFlatCoordinates_,
        0,
        this.endss_,
        this.stride
      )), this.orientedRevision_ = this.getRevision();
    }
    return (
      /** @type {Array<number>} */
      this.orientedFlatCoordinates_
    );
  }
  /**
   * @param {number} squaredTolerance Squared tolerance.
   * @return {MultiPolygon} Simplified MultiPolygon.
   * @protected
   * @override
   */
  getSimplifiedGeometryInternal(t) {
    const i = [], a = [];
    return i.length = SD(
      this.flatCoordinates,
      0,
      this.endss_,
      this.stride,
      Math.sqrt(t),
      i,
      0,
      a
    ), new x_(i, "XY", a);
  }
  /**
   * Return the polygon at the specified index.
   * @param {number} index Index.
   * @return {Polygon} Polygon.
   * @api
   */
  getPolygon(t) {
    if (t < 0 || this.endss_.length <= t)
      return null;
    let i;
    if (t === 0)
      i = 0;
    else {
      const h = this.endss_[t - 1];
      i = h[h.length - 1];
    }
    const a = this.endss_[t].slice(), u = a[a.length - 1];
    if (i !== 0)
      for (let h = 0, g = a.length; h < g; ++h)
        a[h] -= i;
    return new va(
      this.flatCoordinates.slice(i, u),
      this.layout,
      a
    );
  }
  /**
   * Return the polygons of this multipolygon.
   * @return {Array<Polygon>} Polygons.
   * @api
   */
  getPolygons() {
    const t = this.layout, i = this.flatCoordinates, a = this.endss_, u = [];
    let h = 0;
    for (let g = 0, y = a.length; g < y; ++g) {
      const _ = a[g].slice(), b = _[_.length - 1];
      if (h !== 0)
        for (let x = 0, R = _.length; x < R; ++x)
          _[x] -= h;
      const E = new va(
        i.slice(h, b),
        t,
        _
      );
      u.push(E), h = b;
    }
    return u;
  }
  /**
   * Get the type of this geometry.
   * @return {import("./Geometry.js").Type} Geometry type.
   * @api
   * @override
   */
  getType() {
    return "MultiPolygon";
  }
  /**
   * Test if the geometry and the passed extent intersect.
   * @param {import("../extent.js").Extent} extent Extent.
   * @return {boolean} `true` if the geometry and the extent intersect.
   * @api
   * @override
   */
  intersectsExtent(t) {
    return xD(
      this.getOrientedFlatCoordinates(),
      0,
      this.endss_,
      this.stride,
      t
    );
  }
  /**
   * Set the coordinates of the multipolygon.
   * @param {!Array<Array<Array<import("../coordinate.js").Coordinate>>>} coordinates Coordinates.
   * @param {import("./Geometry.js").GeometryLayout} [layout] Layout.
   * @api
   * @override
   */
  setCoordinates(t, i) {
    this.setLayout(i, t, 3), this.flatCoordinates || (this.flatCoordinates = []);
    const a = _D(
      this.flatCoordinates,
      0,
      t,
      this.stride,
      this.endss_
    );
    if (a.length === 0)
      this.flatCoordinates.length = 0;
    else {
      const u = a[a.length - 1];
      this.flatCoordinates.length = u.length === 0 ? 0 : u[u.length - 1];
    }
    this.changed();
  }
}
const wR = $s();
class Qs {
  /**
   * @param {Type} type Geometry type.
   * @param {Array<number>} flatCoordinates Flat coordinates. These always need
   *     to be right-handed for polygons.
   * @param {Array<number>} ends Ends.
   * @param {number} stride Stride.
   * @param {Object<string, *>} properties Properties.
   * @param {number|string|undefined} id Feature id.
   */
  constructor(t, i, a, u, h, g) {
    this.styleFunction, this.extent_, this.id_ = g, this.type_ = t, this.flatCoordinates_ = i, this.flatInteriorPoints_ = null, this.flatMidpoints_ = null, this.ends_ = a || null, this.properties_ = h, this.squaredTolerance_, this.stride_ = u, this.simplifiedGeometry_;
  }
  /**
   * Get a feature property by its key.
   * @param {string} key Key
   * @return {*} Value for the requested key.
   * @api
   */
  get(t) {
    return this.properties_[t];
  }
  /**
   * Get the extent of this feature's geometry.
   * @return {import("../extent.js").Extent} Extent.
   * @api
   */
  getExtent() {
    return this.extent_ || (this.extent_ = this.type_ === "Point" ? g2(this.flatCoordinates_) : nT(
      this.flatCoordinates_,
      0,
      this.flatCoordinates_.length,
      2
    )), this.extent_;
  }
  /**
   * @return {Array<number>} Flat interior points.
   */
  getFlatInteriorPoint() {
    if (!this.flatInteriorPoints_) {
      const t = Pc(this.getExtent());
      this.flatInteriorPoints_ = vT(
        this.flatCoordinates_,
        0,
        this.ends_,
        2,
        t,
        0
      );
    }
    return this.flatInteriorPoints_;
  }
  /**
   * @return {Array<number>} Flat interior points.
   */
  getFlatInteriorPoints() {
    if (!this.flatInteriorPoints_) {
      const t = AD(this.flatCoordinates_, this.ends_), i = J2(this.flatCoordinates_, 0, t, 2);
      this.flatInteriorPoints_ = U2(
        this.flatCoordinates_,
        0,
        t,
        2,
        i
      );
    }
    return this.flatInteriorPoints_;
  }
  /**
   * @return {Array<number>} Flat midpoint.
   */
  getFlatMidpoint() {
    return this.flatMidpoints_ || (this.flatMidpoints_ = P1(
      this.flatCoordinates_,
      0,
      this.flatCoordinates_.length,
      2,
      0.5
    )), this.flatMidpoints_;
  }
  /**
   * @return {Array<number>} Flat midpoints.
   */
  getFlatMidpoints() {
    if (!this.flatMidpoints_) {
      this.flatMidpoints_ = [];
      const t = this.flatCoordinates_;
      let i = 0;
      const a = (
        /** @type {Array<number>} */
        this.ends_
      );
      for (let u = 0, h = a.length; u < h; ++u) {
        const g = a[u], y = P1(t, i, g, 2, 0.5);
        ds(this.flatMidpoints_, y), i = g;
      }
    }
    return this.flatMidpoints_;
  }
  /**
   * Get the feature identifier.  This is a stable identifier for the feature and
   * is set when reading data from a remote source.
   * @return {number|string|undefined} Id.
   * @api
   */
  getId() {
    return this.id_;
  }
  /**
   * @return {Array<number>} Flat coordinates.
   */
  getOrientedFlatCoordinates() {
    return this.flatCoordinates_;
  }
  /**
   * For API compatibility with {@link module:ol/Feature~Feature}, this method is useful when
   * determining the geometry type in style function (see {@link #getType}).
   * @return {RenderFeature} Feature.
   * @api
   */
  getGeometry() {
    return this;
  }
  /**
   * @param {number} squaredTolerance Squared tolerance.
   * @return {RenderFeature} Simplified geometry.
   */
  getSimplifiedGeometry(t) {
    return this;
  }
  /**
   * Get a transformed and simplified version of the geometry.
   * @param {number} squaredTolerance Squared tolerance.
   * @param {import("../proj.js").TransformFunction} [transform] Optional transform function.
   * @return {RenderFeature} Simplified geometry.
   */
  simplifyTransformed(t, i) {
    return this;
  }
  /**
   * Get the feature properties.
   * @return {Object<string, *>} Feature properties.
   * @api
   */
  getProperties() {
    return this.properties_;
  }
  /**
   * Get an object of all property names and values.  This has the same behavior as getProperties,
   * but is here to conform with the {@link module:ol/Feature~Feature} interface.
   * @return {Object<string, *>?} Object.
   */
  getPropertiesInternal() {
    return this.properties_;
  }
  /**
   * @return {number} Stride.
   */
  getStride() {
    return this.stride_;
  }
  /**
   * @return {import('../style/Style.js').StyleFunction|undefined} Style
   */
  getStyleFunction() {
    return this.styleFunction;
  }
  /**
   * Get the type of this feature's geometry.
   * @return {Type} Geometry type.
   * @api
   */
  getType() {
    return this.type_;
  }
  /**
   * Transform geometry coordinates from tile pixel space to projected.
   *
   * @param {import("../proj.js").ProjectionLike} projection The data projection
   */
  transform(t) {
    t = Zi(t);
    const i = t.getExtent(), a = t.getWorldExtent();
    if (i && a) {
      const u = ml(a) / ml(i);
      vu(
        wR,
        a[0],
        a[3],
        u,
        -u,
        0,
        0,
        0
      ), Kc(
        this.flatCoordinates_,
        0,
        this.flatCoordinates_.length,
        2,
        wR,
        this.flatCoordinates_
      );
    }
  }
  /**
   * Apply a transform function to the coordinates of the geometry.
   * The geometry is modified in place.
   * If you do not want the geometry modified in place, first `clone()` it and
   * then use this function on the clone.
   * @param {import("../proj.js").TransformFunction} transformFn Transform function.
   */
  applyTransform(t) {
    t(this.flatCoordinates_, this.flatCoordinates_, this.stride_);
  }
  /**
   * @return {RenderFeature} A cloned render feature.
   */
  clone() {
    var t;
    return new Qs(
      this.type_,
      this.flatCoordinates_.slice(),
      (t = this.ends_) == null ? void 0 : t.slice(),
      this.stride_,
      Object.assign({}, this.properties_),
      this.id_
    );
  }
  /**
   * @return {Array<number>|null} Ends.
   */
  getEnds() {
    return this.ends_;
  }
  /**
   * Add transform and resolution based geometry simplification to this instance.
   * @return {RenderFeature} This render feature.
   */
  enableSimplifyTransformed() {
    return this.simplifyTransformed = h2((t, i) => {
      if (t === this.squaredTolerance_)
        return this.simplifiedGeometry_;
      this.simplifiedGeometry_ = this.clone(), i && this.simplifiedGeometry_.applyTransform(i);
      const a = this.simplifiedGeometry_.getFlatCoordinates();
      let u;
      switch (this.type_) {
        case "LineString":
          a.length = yS(
            a,
            0,
            this.simplifiedGeometry_.flatCoordinates_.length,
            this.simplifiedGeometry_.stride_,
            t,
            a,
            0
          ), u = [a.length];
          break;
        case "MultiLineString":
          u = [], a.length = N2(
            a,
            0,
            this.simplifiedGeometry_.ends_,
            this.simplifiedGeometry_.stride_,
            t,
            a,
            0,
            u
          );
          break;
        case "Polygon":
          u = [], a.length = pT(
            a,
            0,
            this.simplifiedGeometry_.ends_,
            this.simplifiedGeometry_.stride_,
            Math.sqrt(t),
            a,
            0,
            u
          );
          break;
      }
      return u && (this.simplifiedGeometry_ = new Qs(
        this.type_,
        a,
        u,
        2,
        this.properties_,
        this.id_
      )), this.squaredTolerance_ = t, this.simplifiedGeometry_;
    }), this;
  }
}
Qs.prototype.getFlatCoordinates = Qs.prototype.getOrientedFlatCoordinates;
function $2(r, t, i = 0, a = r.length - 1, u = vO) {
  for (; a > i; ) {
    if (a - i > 600) {
      const _ = a - i + 1, b = t - i + 1, E = Math.log(_), x = 0.5 * Math.exp(2 * E / 3), R = 0.5 * Math.sqrt(E * x * (_ - x) / _) * (b - _ / 2 < 0 ? -1 : 1), w = Math.max(i, Math.floor(t - b * x / _ + R)), L = Math.min(a, Math.floor(t + (_ - b) * x / _ + R));
      $2(r, t, w, L, u);
    }
    const h = r[t];
    let g = i, y = a;
    for ($0(r, i, t), u(r[a], h) > 0 && $0(r, i, a); g < y; ) {
      for ($0(r, g, y), g++, y--; u(r[g], h) < 0; ) g++;
      for (; u(r[y], h) > 0; ) y--;
    }
    u(r[i], h) === 0 ? $0(r, i, y) : (y++, $0(r, y, a)), y <= t && (i = y + 1), t <= y && (a = y - 1);
  }
}
function $0(r, t, i) {
  const a = r[t];
  r[t] = r[i], r[i] = a;
}
function vO(r, t) {
  return r < t ? -1 : r > t ? 1 : 0;
}
let tA = class {
  constructor(t = 9) {
    this._maxEntries = Math.max(4, t), this._minEntries = Math.max(2, Math.ceil(this._maxEntries * 0.4)), this.clear();
  }
  all() {
    return this._all(this.data, []);
  }
  search(t) {
    let i = this.data;
    const a = [];
    if (!L1(t, i)) return a;
    const u = this.toBBox, h = [];
    for (; i; ) {
      for (let g = 0; g < i.children.length; g++) {
        const y = i.children[g], _ = i.leaf ? u(y) : y;
        L1(t, _) && (i.leaf ? a.push(y) : rb(t, _) ? this._all(y, a) : h.push(y));
      }
      i = h.pop();
    }
    return a;
  }
  collides(t) {
    let i = this.data;
    if (!L1(t, i)) return !1;
    const a = [];
    for (; i; ) {
      for (let u = 0; u < i.children.length; u++) {
        const h = i.children[u], g = i.leaf ? this.toBBox(h) : h;
        if (L1(t, g)) {
          if (i.leaf || rb(t, g)) return !0;
          a.push(h);
        }
      }
      i = a.pop();
    }
    return !1;
  }
  load(t) {
    if (!(t && t.length)) return this;
    if (t.length < this._minEntries) {
      for (let a = 0; a < t.length; a++)
        this.insert(t[a]);
      return this;
    }
    let i = this._build(t.slice(), 0, t.length - 1, 0);
    if (!this.data.children.length)
      this.data = i;
    else if (this.data.height === i.height)
      this._splitRoot(this.data, i);
    else {
      if (this.data.height < i.height) {
        const a = this.data;
        this.data = i, i = a;
      }
      this._insert(i, this.data.height - i.height - 1, !0);
    }
    return this;
  }
  insert(t) {
    return t && this._insert(t, this.data.height - 1), this;
  }
  clear() {
    return this.data = Um([]), this;
  }
  remove(t, i) {
    if (!t) return this;
    let a = this.data;
    const u = this.toBBox(t), h = [], g = [];
    let y, _, b;
    for (; a || h.length; ) {
      if (a || (a = h.pop(), _ = h[h.length - 1], y = g.pop(), b = !0), a.leaf) {
        const E = SO(t, a.children, i);
        if (E !== -1)
          return a.children.splice(E, 1), h.push(a), this._condense(h), this;
      }
      !b && !a.leaf && rb(a, u) ? (h.push(a), g.push(y), y = 0, _ = a, a = a.children[0]) : _ ? (y++, a = _.children[y], b = !1) : a = null;
    }
    return this;
  }
  toBBox(t) {
    return t;
  }
  compareMinX(t, i) {
    return t.minX - i.minX;
  }
  compareMinY(t, i) {
    return t.minY - i.minY;
  }
  toJSON() {
    return this.data;
  }
  fromJSON(t) {
    return this.data = t, this;
  }
  _all(t, i) {
    const a = [];
    for (; t; )
      t.leaf ? i.push(...t.children) : a.push(...t.children), t = a.pop();
    return i;
  }
  _build(t, i, a, u) {
    const h = a - i + 1;
    let g = this._maxEntries, y;
    if (h <= g)
      return y = Um(t.slice(i, a + 1)), Am(y, this.toBBox), y;
    u || (u = Math.ceil(Math.log(h) / Math.log(g)), g = Math.ceil(h / Math.pow(g, u - 1))), y = Um([]), y.leaf = !1, y.height = u;
    const _ = Math.ceil(h / g), b = _ * Math.ceil(Math.sqrt(g));
    DR(t, i, a, b, this.compareMinX);
    for (let E = i; E <= a; E += b) {
      const x = Math.min(E + b - 1, a);
      DR(t, E, x, _, this.compareMinY);
      for (let R = E; R <= x; R += _) {
        const w = Math.min(R + _ - 1, x);
        y.children.push(this._build(t, R, w, u - 1));
      }
    }
    return Am(y, this.toBBox), y;
  }
  _chooseSubtree(t, i, a, u) {
    for (; u.push(i), !(i.leaf || u.length - 1 === a); ) {
      let h = 1 / 0, g = 1 / 0, y;
      for (let _ = 0; _ < i.children.length; _++) {
        const b = i.children[_], E = ob(b), x = TO(t, b) - E;
        x < g ? (g = x, h = E < h ? E : h, y = b) : x === g && E < h && (h = E, y = b);
      }
      i = y || i.children[0];
    }
    return i;
  }
  _insert(t, i, a) {
    const u = a ? t : this.toBBox(t), h = [], g = this._chooseSubtree(u, this.data, i, h);
    for (g.children.push(t), i_(g, u); i >= 0 && h[i].children.length > this._maxEntries; )
      this._split(h, i), i--;
    this._adjustParentBBoxes(u, h, i);
  }
  // split overflowed node into two
  _split(t, i) {
    const a = t[i], u = a.children.length, h = this._minEntries;
    this._chooseSplitAxis(a, h, u);
    const g = this._chooseSplitIndex(a, h, u), y = Um(a.children.splice(g, a.children.length - g));
    y.height = a.height, y.leaf = a.leaf, Am(a, this.toBBox), Am(y, this.toBBox), i ? t[i - 1].children.push(y) : this._splitRoot(a, y);
  }
  _splitRoot(t, i) {
    this.data = Um([t, i]), this.data.height = t.height + 1, this.data.leaf = !1, Am(this.data, this.toBBox);
  }
  _chooseSplitIndex(t, i, a) {
    let u, h = 1 / 0, g = 1 / 0;
    for (let y = i; y <= a - i; y++) {
      const _ = n_(t, 0, y, this.toBBox), b = n_(t, y, a, this.toBBox), E = xO(_, b), x = ob(_) + ob(b);
      E < h ? (h = E, u = y, g = x < g ? x : g) : E === h && x < g && (g = x, u = y);
    }
    return u || a - i;
  }
  // sorts node children by the best axis for split
  _chooseSplitAxis(t, i, a) {
    const u = t.leaf ? this.compareMinX : EO, h = t.leaf ? this.compareMinY : bO, g = this._allDistMargin(t, i, a, u), y = this._allDistMargin(t, i, a, h);
    g < y && t.children.sort(u);
  }
  // total margin of all possible split distributions where each node is at least m full
  _allDistMargin(t, i, a, u) {
    t.children.sort(u);
    const h = this.toBBox, g = n_(t, 0, i, h), y = n_(t, a - i, a, h);
    let _ = O1(g) + O1(y);
    for (let b = i; b < a - i; b++) {
      const E = t.children[b];
      i_(g, t.leaf ? h(E) : E), _ += O1(g);
    }
    for (let b = a - i - 1; b >= i; b--) {
      const E = t.children[b];
      i_(y, t.leaf ? h(E) : E), _ += O1(y);
    }
    return _;
  }
  _adjustParentBBoxes(t, i, a) {
    for (let u = a; u >= 0; u--)
      i_(i[u], t);
  }
  _condense(t) {
    for (let i = t.length - 1, a; i >= 0; i--)
      t[i].children.length === 0 ? i > 0 ? (a = t[i - 1].children, a.splice(a.indexOf(t[i]), 1)) : this.clear() : Am(t[i], this.toBBox);
  }
};
function SO(r, t, i) {
  if (!i) return t.indexOf(r);
  for (let a = 0; a < t.length; a++)
    if (i(r, t[a])) return a;
  return -1;
}
function Am(r, t) {
  n_(r, 0, r.children.length, t, r);
}
function n_(r, t, i, a, u) {
  u || (u = Um(null)), u.minX = 1 / 0, u.minY = 1 / 0, u.maxX = -1 / 0, u.maxY = -1 / 0;
  for (let h = t; h < i; h++) {
    const g = r.children[h];
    i_(u, r.leaf ? a(g) : g);
  }
  return u;
}
function i_(r, t) {
  return r.minX = Math.min(r.minX, t.minX), r.minY = Math.min(r.minY, t.minY), r.maxX = Math.max(r.maxX, t.maxX), r.maxY = Math.max(r.maxY, t.maxY), r;
}
function EO(r, t) {
  return r.minX - t.minX;
}
function bO(r, t) {
  return r.minY - t.minY;
}
function ob(r) {
  return (r.maxX - r.minX) * (r.maxY - r.minY);
}
function O1(r) {
  return r.maxX - r.minX + (r.maxY - r.minY);
}
function TO(r, t) {
  return (Math.max(t.maxX, r.maxX) - Math.min(t.minX, r.minX)) * (Math.max(t.maxY, r.maxY) - Math.min(t.minY, r.minY));
}
function xO(r, t) {
  const i = Math.max(r.minX, t.minX), a = Math.max(r.minY, t.minY), u = Math.min(r.maxX, t.maxX), h = Math.min(r.maxY, t.maxY);
  return Math.max(0, u - i) * Math.max(0, h - a);
}
function rb(r, t) {
  return r.minX <= t.minX && r.minY <= t.minY && t.maxX <= r.maxX && t.maxY <= r.maxY;
}
function L1(r, t) {
  return t.minX <= r.maxX && t.minY <= r.maxY && t.maxX >= r.minX && t.maxY >= r.minY;
}
function Um(r) {
  return {
    children: r,
    height: 1,
    leaf: !0,
    minX: 1 / 0,
    minY: 1 / 0,
    maxX: -1 / 0,
    maxY: -1 / 0
  };
}
function DR(r, t, i, a, u) {
  const h = [t, i];
  for (; h.length; ) {
    if (i = h.pop(), t = h.pop(), i - t <= a) continue;
    const g = t + Math.ceil((i - t) / a / 2) * a;
    $2(r, g, t, i, u), h.push(t, g, g, i);
  }
}
class Xb {
  /**
   * @param {number} [maxEntries] Max entries.
   */
  constructor(t) {
    this.rbush_ = new tA(t), this.items_ = {};
  }
  /**
   * Insert a value into the RBush.
   * @param {import("../extent.js").Extent} extent Extent.
   * @param {T} value Value.
   */
  insert(t, i) {
    const a = {
      minX: t[0],
      minY: t[1],
      maxX: t[2],
      maxY: t[3],
      value: i
    };
    this.rbush_.insert(a), this.items_[ue(i)] = a;
  }
  /**
   * Bulk-insert values into the RBush.
   * @param {Array<import("../extent.js").Extent>} extents Extents.
   * @param {Array<T>} values Values.
   */
  load(t, i) {
    const a = new Array(i.length);
    for (let u = 0, h = i.length; u < h; u++) {
      const g = t[u], y = i[u], _ = {
        minX: g[0],
        minY: g[1],
        maxX: g[2],
        maxY: g[3],
        value: y
      };
      a[u] = _, this.items_[ue(y)] = _;
    }
    this.rbush_.load(a);
  }
  /**
   * Remove a value from the RBush.
   * @param {T} value Value.
   * @return {boolean} Removed.
   */
  remove(t) {
    const i = ue(t), a = this.items_[i];
    return delete this.items_[i], this.rbush_.remove(a) !== null;
  }
  /**
   * Update the extent of a value in the RBush.
   * @param {import("../extent.js").Extent} extent Extent.
   * @param {T} value Value.
   */
  update(t, i) {
    const a = this.items_[ue(i)], u = [a.minX, a.minY, a.maxX, a.maxY];
    d_(u, t) || (this.remove(i), this.insert(t, i));
  }
  /**
   * Return all values in the RBush.
   * @return {Array<T>} All.
   */
  getAll() {
    return this.rbush_.all().map(function(i) {
      return i.value;
    });
  }
  /**
   * Return all values in the given extent.
   * @param {import("../extent.js").Extent} extent Extent.
   * @return {Array<T>} All in extent.
   */
  getInExtent(t) {
    const i = {
      minX: t[0],
      minY: t[1],
      maxX: t[2],
      maxY: t[3]
    };
    return this.rbush_.search(i).map(function(u) {
      return u.value;
    });
  }
  /**
   * Calls a callback function with each value in the tree.
   * If the callback returns a truthy value, this value is returned without
   * checking the rest of the tree.
   * @param {function(T): R} callback Callback.
   * @return {R|undefined} Callback return value.
   * @template R
   */
  forEach(t) {
    return this.forEach_(this.getAll(), t);
  }
  /**
   * Calls a callback function with each value in the provided extent.
   * @param {import("../extent.js").Extent} extent Extent.
   * @param {function(T): R} callback Callback.
   * @return {R|undefined} Callback return value.
   * @template R
   */
  forEachInExtent(t, i) {
    return this.forEach_(this.getInExtent(t), i);
  }
  /**
   * @param {Array<T>} values Values.
   * @param {function(T): R} callback Callback.
   * @return {R|undefined} Callback return value.
   * @template R
   * @private
   */
  forEach_(t, i) {
    let a;
    for (let u = 0, h = t.length; u < h; u++)
      if (a = i(t[u]), a)
        return a;
    return a;
  }
  /**
   * @return {boolean} Is empty.
   */
  isEmpty() {
    return Zm(this.items_);
  }
  /**
   * Remove all values from the RBush.
   */
  clear() {
    this.rbush_.clear(), this.items_ = {};
  }
  /**
   * @param {import("../extent.js").Extent} [extent] Extent.
   * @return {import("../extent.js").Extent} Extent.
   */
  getExtent(t) {
    const i = this.rbush_.toJSON();
    return $o(i.minX, i.minY, i.maxX, i.maxY, t);
  }
  /**
   * @param {RBush<T>} rbush R-Tree.
   */
  concat(t) {
    this.rbush_.load(t.rbush_.all());
    for (const i in t.items_)
      this.items_[i] = t.items_[i];
  }
}
class eA extends tr {
  /**
   * @param {Options} options Source options.
   */
  constructor(t) {
    super(), this.projection = Zi(t.projection), this.attributions_ = OR(t.attributions), this.attributionsCollapsible_ = t.attributionsCollapsible ?? !0, this.loading = !1, this.state_ = t.state !== void 0 ? t.state : "ready", this.wrapX_ = t.wrapX !== void 0 ? t.wrapX : !1, this.interpolate_ = !!t.interpolate, this.viewResolver = null, this.viewRejector = null;
    const i = this;
    this.viewPromise_ = new Promise(function(a, u) {
      i.viewResolver = a, i.viewRejector = u;
    });
  }
  /**
   * Get the attribution function for the source.
   * @return {?Attribution} Attribution function.
   * @api
   */
  getAttributions() {
    return this.attributions_;
  }
  /**
   * @return {boolean} Attributions are collapsible.
   * @api
   */
  getAttributionsCollapsible() {
    return this.attributionsCollapsible_;
  }
  /**
   * Get the projection of the source.
   * @return {import("../proj/Projection.js").default|null} Projection.
   * @api
   */
  getProjection() {
    return this.projection;
  }
  /**
   * @param {import("../proj/Projection").default} [projection] Projection.
   * @return {Array<number>|null} Resolutions.
   */
  getResolutions(t) {
    return null;
  }
  /**
   * @return {Promise<import("../View.js").ViewOptions>} A promise for view-related properties.
   */
  getView() {
    return this.viewPromise_;
  }
  /**
   * Get the state of the source, see {@link import("./Source.js").State} for possible states.
   * @return {import("./Source.js").State} State.
   * @api
   */
  getState() {
    return this.state_;
  }
  /**
   * @return {boolean|undefined} Wrap X.
   */
  getWrapX() {
    return this.wrapX_;
  }
  /**
   * @return {boolean} Use linear interpolation when resampling.
   */
  getInterpolate() {
    return this.interpolate_;
  }
  /**
   * Refreshes the source. The source will be cleared, and data from the server will be reloaded.
   * @api
   */
  refresh() {
    this.changed();
  }
  /**
   * Set the attributions of the source.
   * @param {AttributionLike|undefined} attributions Attributions.
   *     Can be passed as `string`, `Array<string>`, {@link module:ol/source/Source~Attribution},
   *     or `undefined`.
   * @api
   */
  setAttributions(t) {
    this.attributions_ = OR(t), this.changed();
  }
  /**
   * Set the state of the source.
   * @param {import("./Source.js").State} state State.
   */
  setState(t) {
    this.state_ = t, this.changed();
  }
}
function OR(r) {
  return r ? typeof r == "function" ? r : (Array.isArray(r) || (r = [r]), (t) => r) : null;
}
const Il = {
  /**
   * Triggered when a feature is added to the source.
   * @event module:ol/source/Vector.VectorSourceEvent#addfeature
   * @api
   */
  ADDFEATURE: "addfeature",
  /**
   * Triggered when a feature is updated.
   * @event module:ol/source/Vector.VectorSourceEvent#changefeature
   * @api
   */
  CHANGEFEATURE: "changefeature",
  /**
   * Triggered when the clear method is called on the source.
   * @event module:ol/source/Vector.VectorSourceEvent#clear
   * @api
   */
  CLEAR: "clear",
  /**
   * Triggered when a feature is removed from the source.
   * See {@link module:ol/source/Vector~VectorSource#clear source.clear()} for exceptions.
   * @event module:ol/source/Vector.VectorSourceEvent#removefeature
   * @api
   */
  REMOVEFEATURE: "removefeature",
  /**
   * Triggered when features starts loading.
   * @event module:ol/source/Vector.VectorSourceEvent#featuresloadstart
   * @api
   */
  FEATURESLOADSTART: "featuresloadstart",
  /**
   * Triggered when features finishes loading.
   * @event module:ol/source/Vector.VectorSourceEvent#featuresloadend
   * @api
   */
  FEATURESLOADEND: "featuresloadend",
  /**
   * Triggered if feature loading results in an error.
   * @event module:ol/source/Vector.VectorSourceEvent#featuresloaderror
   * @api
   */
  FEATURESLOADERROR: "featuresloaderror"
};
class Hc extends ys {
  /**
   * @param {string} type Type.
   * @param {FeatureType} [feature] Feature.
   * @param {Array<FeatureType>} [features] Features.
   */
  constructor(t, i, a) {
    super(t), this.feature = i, this.features = a;
  }
}
class MT extends eA {
  /**
   * @param {Options<FeatureType>} [options] Vector source options.
   */
  constructor(t) {
    t = t || {}, super({
      attributions: t.attributions,
      interpolate: !0,
      projection: void 0,
      state: "ready",
      wrapX: t.wrapX !== void 0 ? t.wrapX : !0
    }), this.on, this.once, this.un, this.loader_ = h_, this.format_ = t.format || null, this.overlaps_ = t.overlaps === void 0 ? !0 : t.overlaps, this.url_ = t.url, t.loader !== void 0 ? this.loader_ = t.loader : this.url_ !== void 0 && (je(this.format_, "`format` must be set when `url` is set"), this.loader_ = MR(this.url_, this.format_)), this.strategy_ = t.strategy !== void 0 ? t.strategy : _O;
    const i = t.useSpatialIndex !== void 0 ? t.useSpatialIndex : !0;
    this.featuresRtree_ = i ? new Xb() : null, this.loadedExtentsRtree_ = new Xb(), this.loadingExtentsCount_ = 0, this.nullGeometryFeatures_ = {}, this.idIndex_ = {}, this.uidIndex_ = {}, this.featureChangeKeys_ = {}, this.featuresCollection_ = null;
    let a, u;
    Array.isArray(t.features) ? u = t.features : t.features && (a = t.features, u = a.getArray()), !i && a === void 0 && (a = new Js(u)), u !== void 0 && this.addFeaturesInternal(u), a !== void 0 && this.bindFeaturesCollection_(a);
  }
  /**
   * Add a single feature to the source.  If you want to add a batch of features
   * at once, call {@link module:ol/source/Vector~VectorSource#addFeatures #addFeatures()}
   * instead. A feature will not be added to the source if feature with
   * the same id is already there. The reason for this behavior is to avoid
   * feature duplication when using bbox or tile loading strategies.
   * Note: this also applies if a {@link module:ol/Collection~Collection} is used for features,
   * meaning that if a feature with a duplicate id is added in the collection, it will
   * be removed from it right away.
   * @param {FeatureType} feature Feature to add.
   * @api
   */
  addFeature(t) {
    this.addFeatureInternal(t), this.changed();
  }
  /**
   * Add a feature without firing a `change` event.
   * @param {FeatureType} feature Feature.
   * @protected
   */
  addFeatureInternal(t) {
    const i = ue(t);
    if (!this.addToIndex_(i, t)) {
      this.featuresCollection_ && this.featuresCollection_.remove(t);
      return;
    }
    this.setupChangeEvents_(i, t);
    const a = t.getGeometry();
    if (a) {
      const u = a.getExtent();
      this.featuresRtree_ && this.featuresRtree_.insert(u, t);
    } else
      this.nullGeometryFeatures_[i] = t;
    this.dispatchEvent(
      new Hc(Il.ADDFEATURE, t)
    );
  }
  /**
   * @param {string} featureKey Unique identifier for the feature.
   * @param {FeatureType} feature The feature.
   * @private
   */
  setupChangeEvents_(t, i) {
    i instanceof Qs || (this.featureChangeKeys_[t] = [
      ge(i, $t.CHANGE, this.handleFeatureChange_, this),
      ge(
        i,
        qm.PROPERTYCHANGE,
        this.handleFeatureChange_,
        this
      )
    ]);
  }
  /**
   * @param {string} featureKey Unique identifier for the feature.
   * @param {FeatureType} feature The feature.
   * @return {boolean} The feature is "valid", in the sense that it is also a
   *     candidate for insertion into the Rtree.
   * @private
   */
  addToIndex_(t, i) {
    let a = !0;
    if (i.getId() !== void 0) {
      const u = String(i.getId());
      if (!(u in this.idIndex_))
        this.idIndex_[u] = i;
      else if (i instanceof Qs) {
        const h = this.idIndex_[u];
        h instanceof Qs ? Array.isArray(h) ? h.push(i) : this.idIndex_[u] = [h, i] : a = !1;
      } else
        a = !1;
    }
    return a && (je(
      !(t in this.uidIndex_),
      "The passed `feature` was already added to the source"
    ), this.uidIndex_[t] = i), a;
  }
  /**
   * Add a batch of features to the source.
   * @param {Array<FeatureType>} features Features to add.
   * @api
   */
  addFeatures(t) {
    this.addFeaturesInternal(t), this.changed();
  }
  /**
   * Add features without firing a `change` event.
   * @param {Array<FeatureType>} features Features.
   * @protected
   */
  addFeaturesInternal(t) {
    const i = [], a = [], u = [];
    for (let h = 0, g = t.length; h < g; h++) {
      const y = t[h], _ = ue(y);
      this.addToIndex_(_, y) && a.push(y);
    }
    for (let h = 0, g = a.length; h < g; h++) {
      const y = a[h], _ = ue(y);
      this.setupChangeEvents_(_, y);
      const b = y.getGeometry();
      if (b) {
        const E = b.getExtent();
        i.push(E), u.push(y);
      } else
        this.nullGeometryFeatures_[_] = y;
    }
    if (this.featuresRtree_ && this.featuresRtree_.load(i, u), this.hasListener(Il.ADDFEATURE))
      for (let h = 0, g = a.length; h < g; h++)
        this.dispatchEvent(
          new Hc(Il.ADDFEATURE, a[h])
        );
  }
  /**
   * @param {!Collection<FeatureType>} collection Collection.
   * @private
   */
  bindFeaturesCollection_(t) {
    let i = !1;
    this.addEventListener(
      Il.ADDFEATURE,
      /**
       * @param {VectorSourceEvent<FeatureType>} evt The vector source event
       */
      function(a) {
        i || (i = !0, t.push(a.feature), i = !1);
      }
    ), this.addEventListener(
      Il.REMOVEFEATURE,
      /**
       * @param {VectorSourceEvent<FeatureType>} evt The vector source event
       */
      function(a) {
        i || (i = !0, t.remove(a.feature), i = !1);
      }
    ), t.addEventListener(
      jn.ADD,
      /**
       * @param {import("../Collection.js").CollectionEvent<FeatureType>} evt The collection event
       */
      (a) => {
        i || (i = !0, this.addFeature(a.element), i = !1);
      }
    ), t.addEventListener(
      jn.REMOVE,
      /**
       * @param {import("../Collection.js").CollectionEvent<FeatureType>} evt The collection event
       */
      (a) => {
        i || (i = !0, this.removeFeature(a.element), i = !1);
      }
    ), this.featuresCollection_ = t;
  }
  /**
   * Remove all features from the source.
   * @param {boolean} [fast] Skip dispatching of {@link module:ol/source/Vector.VectorSourceEvent#event:removefeature} events.
   * @api
   */
  clear(t) {
    if (t) {
      for (const a in this.featureChangeKeys_)
        this.featureChangeKeys_[a].forEach(qe);
      this.featuresCollection_ || (this.featureChangeKeys_ = {}, this.idIndex_ = {}, this.uidIndex_ = {});
    } else if (this.featuresRtree_) {
      this.featuresRtree_.forEach((a) => {
        this.removeFeatureInternal(a);
      });
      for (const a in this.nullGeometryFeatures_)
        this.removeFeatureInternal(this.nullGeometryFeatures_[a]);
    }
    this.featuresCollection_ && this.featuresCollection_.clear(), this.featuresRtree_ && this.featuresRtree_.clear(), this.nullGeometryFeatures_ = {};
    const i = new Hc(Il.CLEAR);
    this.dispatchEvent(i), this.changed();
  }
  /**
   * Iterate through all features on the source, calling the provided callback
   * with each one.  If the callback returns any "truthy" value, iteration will
   * stop and the function will return the same value.
   * Note: this function only iterate through the feature that have a defined geometry.
   *
   * @param {function(FeatureType): T} callback Called with each feature
   *     on the source.  Return a truthy value to stop iteration.
   * @return {T|undefined} The return value from the last call to the callback.
   * @template T
   * @api
   */
  forEachFeature(t) {
    if (this.featuresRtree_)
      return this.featuresRtree_.forEach(t);
    this.featuresCollection_ && this.featuresCollection_.forEach(t);
  }
  /**
   * Iterate through all features whose geometries contain the provided
   * coordinate, calling the callback with each feature.  If the callback returns
   * a "truthy" value, iteration will stop and the function will return the same
   * value.
   *
   * For {@link module:ol/render/Feature~RenderFeature} features, the callback will be
   * called for all features.
   *
   * @param {import("../coordinate.js").Coordinate} coordinate Coordinate.
   * @param {function(FeatureType): T} callback Called with each feature
   *     whose goemetry contains the provided coordinate.
   * @return {T|undefined} The return value from the last call to the callback.
   * @template T
   */
  forEachFeatureAtCoordinateDirect(t, i) {
    const a = [t[0], t[1], t[0], t[1]];
    return this.forEachFeatureInExtent(a, function(u) {
      const h = u.getGeometry();
      if (h instanceof Qs || h.intersectsCoordinate(t))
        return i(u);
    });
  }
  /**
   * Iterate through all features whose bounding box intersects the provided
   * extent (note that the feature's geometry may not intersect the extent),
   * calling the callback with each feature.  If the callback returns a "truthy"
   * value, iteration will stop and the function will return the same value.
   *
   * If you are interested in features whose geometry intersects an extent, call
   * the {@link module:ol/source/Vector~VectorSource#forEachFeatureIntersectingExtent #forEachFeatureIntersectingExtent()} method instead.
   *
   * When `useSpatialIndex` is set to false, this method will loop through all
   * features, equivalent to {@link module:ol/source/Vector~VectorSource#forEachFeature #forEachFeature()}.
   *
   * @param {import("../extent.js").Extent} extent Extent.
   * @param {function(FeatureType): T} callback Called with each feature
   *     whose bounding box intersects the provided extent.
   * @return {T|undefined} The return value from the last call to the callback.
   * @template T
   * @api
   */
  forEachFeatureInExtent(t, i) {
    if (this.featuresRtree_)
      return this.featuresRtree_.forEachInExtent(t, i);
    this.featuresCollection_ && this.featuresCollection_.forEach(i);
  }
  /**
   * Iterate through all features whose geometry intersects the provided extent,
   * calling the callback with each feature.  If the callback returns a "truthy"
   * value, iteration will stop and the function will return the same value.
   *
   * If you only want to test for bounding box intersection, call the
   * {@link module:ol/source/Vector~VectorSource#forEachFeatureInExtent #forEachFeatureInExtent()} method instead.
   *
   * @param {import("../extent.js").Extent} extent Extent.
   * @param {function(FeatureType): T} callback Called with each feature
   *     whose geometry intersects the provided extent.
   * @return {T|undefined} The return value from the last call to the callback.
   * @template T
   * @api
   */
  forEachFeatureIntersectingExtent(t, i) {
    return this.forEachFeatureInExtent(
      t,
      /**
       * @param {FeatureType} feature Feature.
       * @return {T|undefined} The return value from the last call to the callback.
       */
      function(a) {
        const u = a.getGeometry();
        if (u instanceof Qs || u.intersectsExtent(t)) {
          const h = i(a);
          if (h)
            return h;
        }
      }
    );
  }
  /**
   * Get the features collection associated with this source. Will be `null`
   * unless the source was configured with `useSpatialIndex` set to `false`, or
   * with a {@link module:ol/Collection~Collection} as `features`.
   * @return {Collection<FeatureType>|null} The collection of features.
   * @api
   */
  getFeaturesCollection() {
    return this.featuresCollection_;
  }
  /**
   * Get a snapshot of the features currently on the source in random order. The returned array
   * is a copy, the features are references to the features in the source.
   * @return {Array<FeatureType>} Features.
   * @api
   */
  getFeatures() {
    let t;
    return this.featuresCollection_ ? t = this.featuresCollection_.getArray().slice(0) : this.featuresRtree_ && (t = this.featuresRtree_.getAll(), Zm(this.nullGeometryFeatures_) || ds(t, Object.values(this.nullGeometryFeatures_))), t;
  }
  /**
   * Get all features whose geometry intersects the provided coordinate.
   * @param {import("../coordinate.js").Coordinate} coordinate Coordinate.
   * @return {Array<FeatureType>} Features.
   * @api
   */
  getFeaturesAtCoordinate(t) {
    const i = [];
    return this.forEachFeatureAtCoordinateDirect(t, function(a) {
      i.push(a);
    }), i;
  }
  /**
   * Get all features whose bounding box intersects the provided extent.  Note that this returns an array of
   * all features intersecting the given extent in random order (so it may include
   * features whose geometries do not intersect the extent).
   *
   * When `useSpatialIndex` is set to false, this method will return all
   * features.
   *
   * @param {import("../extent.js").Extent} extent Extent.
   * @param {import("../proj/Projection.js").default} [projection] Include features
   * where `extent` exceeds the x-axis bounds of `projection` and wraps around the world.
   * @return {Array<FeatureType>} Features.
   * @api
   */
  getFeaturesInExtent(t, i) {
    if (this.featuresRtree_) {
      if (!(i && i.canWrapX() && this.getWrapX()))
        return this.featuresRtree_.getInExtent(t);
      const u = v2(t, i);
      return [].concat(
        ...u.map((h) => this.featuresRtree_.getInExtent(h))
      );
    }
    return this.featuresCollection_ ? this.featuresCollection_.getArray().slice(0) : [];
  }
  /**
   * Get the closest feature to the provided coordinate.
   *
   * This method is not available when the source is configured with
   * `useSpatialIndex` set to `false` and the features in this source are of type
   * {@link module:ol/Feature~Feature}.
   * @param {import("../coordinate.js").Coordinate} coordinate Coordinate.
   * @param {function(FeatureType):boolean} [filter] Feature filter function.
   *     The filter function will receive one argument, the {@link module:ol/Feature~Feature feature}
   *     and it should return a boolean value. By default, no filtering is made.
   * @return {FeatureType|null} Closest feature (or `null` if none found).
   * @api
   */
  getClosestFeatureToCoordinate(t, i) {
    const a = t[0], u = t[1];
    let h = null;
    const g = [NaN, NaN];
    let y = 1 / 0;
    const _ = [-1 / 0, -1 / 0, 1 / 0, 1 / 0];
    return i = i || _u, this.featuresRtree_.forEachInExtent(
      _,
      /**
       * @param {FeatureType} feature Feature.
       */
      function(b) {
        if (i(b)) {
          const E = b.getGeometry(), x = y;
          if (y = E instanceof Qs ? 0 : E.closestPointXY(a, u, g, y), y < x) {
            h = b;
            const R = Math.sqrt(y);
            _[0] = a - R, _[1] = u - R, _[2] = a + R, _[3] = u + R;
          }
        }
      }
    ), h;
  }
  /**
   * Get the extent of the features currently in the source.
   *
   * This method is not available when the source is configured with
   * `useSpatialIndex` set to `false`.
   * @param {import("../extent.js").Extent} [extent] Destination extent. If provided, no new extent
   *     will be created. Instead, that extent's coordinates will be overwritten.
   * @return {import("../extent.js").Extent} Extent.
   * @api
   */
  getExtent(t) {
    return this.featuresRtree_.getExtent(t);
  }
  /**
   * Get a feature by its identifier (the value returned by feature.getId()). When `RenderFeature`s
   * are used, `getFeatureById()` can return an array of `RenderFeature`s. This allows for handling
   * of `GeometryCollection` geometries, where format readers create one `RenderFeature` per
   * `GeometryCollection` member.
   * Note that the index treats string and numeric identifiers as the same.  So
   * `source.getFeatureById(2)` will return a feature with id `'2'` or `2`.
   *
   * @param {string|number} id Feature identifier.
   * @return {FeatureClassOrArrayOfRenderFeatures<FeatureType>|null} The feature (or `null` if not found).
   * @api
   */
  getFeatureById(t) {
    const i = this.idIndex_[t.toString()];
    return i !== void 0 ? (
      /** @type {FeatureClassOrArrayOfRenderFeatures<FeatureType>} */
      i
    ) : null;
  }
  /**
   * Get a feature by its internal unique identifier (using `getUid`).
   *
   * @param {string} uid Feature identifier.
   * @return {FeatureType|null} The feature (or `null` if not found).
   */
  getFeatureByUid(t) {
    const i = this.uidIndex_[t];
    return i !== void 0 ? i : null;
  }
  /**
   * Get the format associated with this source.
   *
   * @return {import("../format/Feature.js").default<FeatureType>|null}} The feature format.
   * @api
   */
  getFormat() {
    return this.format_;
  }
  /**
   * @return {boolean} The source can have overlapping geometries.
   */
  getOverlaps() {
    return this.overlaps_;
  }
  /**
   * Get the url associated with this source.
   *
   * @return {string|import("../featureloader.js").FeatureUrlFunction|undefined} The url.
   * @api
   */
  getUrl() {
    return this.url_;
  }
  /**
   * @param {Event} event Event.
   * @private
   */
  handleFeatureChange_(t) {
    const i = (
      /** @type {FeatureType} */
      t.target
    ), a = ue(i), u = i.getGeometry();
    if (!u)
      a in this.nullGeometryFeatures_ || (this.featuresRtree_ && this.featuresRtree_.remove(i), this.nullGeometryFeatures_[a] = i);
    else {
      const g = u.getExtent();
      a in this.nullGeometryFeatures_ ? (delete this.nullGeometryFeatures_[a], this.featuresRtree_ && this.featuresRtree_.insert(g, i)) : this.featuresRtree_ && this.featuresRtree_.update(g, i);
    }
    const h = i.getId();
    if (h !== void 0) {
      const g = h.toString();
      this.idIndex_[g] !== i && (this.removeFromIdIndex_(i), this.idIndex_[g] = i);
    } else
      this.removeFromIdIndex_(i), this.uidIndex_[a] = i;
    this.changed(), this.dispatchEvent(
      new Hc(Il.CHANGEFEATURE, i)
    );
  }
  /**
   * Returns true if the feature is contained within the source.
   * @param {FeatureType} feature Feature.
   * @return {boolean} Has feature.
   * @api
   */
  hasFeature(t) {
    const i = t.getId();
    return i !== void 0 ? i in this.idIndex_ : ue(t) in this.uidIndex_;
  }
  /**
   * @return {boolean} Is empty.
   */
  isEmpty() {
    return this.featuresRtree_ ? this.featuresRtree_.isEmpty() && Zm(this.nullGeometryFeatures_) : this.featuresCollection_ ? this.featuresCollection_.getLength() === 0 : !0;
  }
  /**
   * @param {import("../extent.js").Extent} extent Extent.
   * @param {number} resolution Resolution.
   * @param {import("../proj/Projection.js").default} projection Projection.
   */
  loadFeatures(t, i, a) {
    const u = this.loadedExtentsRtree_, h = this.strategy_(t, i, a);
    for (let g = 0, y = h.length; g < y; ++g) {
      const _ = h[g];
      u.forEachInExtent(
        _,
        /**
         * @param {{extent: import("../extent.js").Extent}} object Object.
         * @return {boolean} Contains.
         */
        function(E) {
          return e_(E.extent, _);
        }
      ) || (++this.loadingExtentsCount_, this.dispatchEvent(
        new Hc(Il.FEATURESLOADSTART)
      ), this.loader_.call(
        this,
        _,
        i,
        a,
        /**
         * @param {Array<FeatureType>} features Loaded features
         */
        (E) => {
          --this.loadingExtentsCount_, this.dispatchEvent(
            new Hc(
              Il.FEATURESLOADEND,
              void 0,
              E
            )
          );
        },
        () => {
          --this.loadingExtentsCount_, this.dispatchEvent(
            new Hc(Il.FEATURESLOADERROR)
          );
        }
      ), u.insert(_, { extent: _.slice() }));
    }
    this.loading = this.loader_.length < 4 ? !1 : this.loadingExtentsCount_ > 0;
  }
  /**
   * @override
   */
  refresh() {
    this.clear(!0), this.loadedExtentsRtree_.clear(), super.refresh();
  }
  /**
   * Remove an extent from the list of loaded extents.
   * @param {import("../extent.js").Extent} extent Extent.
   * @api
   */
  removeLoadedExtent(t) {
    const i = this.loadedExtentsRtree_, a = i.forEachInExtent(t, function(u) {
      if (d_(u.extent, t))
        return u;
    });
    a && i.remove(a);
  }
  /**
   * Batch remove features from the source.  If you want to remove all features
   * at once, use the {@link module:ol/source/Vector~VectorSource#clear #clear()} method
   * instead.
   * @param {Array<FeatureType>} features Features to remove.
   * @api
   */
  removeFeatures(t) {
    let i = !1;
    for (let a = 0, u = t.length; a < u; ++a)
      i = this.removeFeatureInternal(t[a]) || i;
    i && this.changed();
  }
  /**
   * Remove a single feature from the source. If you want to batch remove
   * features, use the {@link module:ol/source/Vector~VectorSource#removeFeatures #removeFeatures()} method
   * instead.
   * @param {FeatureType} feature Feature to remove.
   * @api
   */
  removeFeature(t) {
    if (!t)
      return;
    this.removeFeatureInternal(t) && this.changed();
  }
  /**
   * Remove feature without firing a `change` event.
   * @param {FeatureType} feature Feature.
   * @return {boolean} True if the feature was removed, false if it was not found.
   * @protected
   */
  removeFeatureInternal(t) {
    const i = ue(t);
    if (!(i in this.uidIndex_))
      return !1;
    i in this.nullGeometryFeatures_ ? delete this.nullGeometryFeatures_[i] : this.featuresRtree_ && this.featuresRtree_.remove(t);
    const a = this.featureChangeKeys_[i];
    a == null || a.forEach(qe), delete this.featureChangeKeys_[i];
    const u = t.getId();
    if (u !== void 0) {
      const h = u.toString(), g = this.idIndex_[h];
      g === t ? delete this.idIndex_[h] : Array.isArray(g) && (g.splice(g.indexOf(t), 1), g.length === 1 && (this.idIndex_[h] = g[0]));
    }
    return delete this.uidIndex_[i], this.hasListener(Il.REMOVEFEATURE) && this.dispatchEvent(
      new Hc(Il.REMOVEFEATURE, t)
    ), !0;
  }
  /**
   * Remove a feature from the id index.  Called internally when the feature id
   * may have changed.
   * @param {FeatureType} feature The feature.
   * @private
   */
  removeFromIdIndex_(t) {
    for (const i in this.idIndex_)
      if (this.idIndex_[i] === t) {
        delete this.idIndex_[i];
        break;
      }
  }
  /**
   * Set the new loader of the source. The next render cycle will use the
   * new loader.
   * @param {import("../featureloader.js").FeatureLoader} loader The loader to set.
   * @api
   */
  setLoader(t) {
    this.loader_ = t;
  }
  /**
   * Points the source to a new url. The next render cycle will use the new url.
   * @param {string|import("../featureloader.js").FeatureUrlFunction} url Url.
   * @api
   */
  setUrl(t) {
    je(this.format_, "`format` must be set when `url` is set"), this.url_ = t, this.setLoader(MR(t, this.format_));
  }
  /**
   * @param {boolean} overlaps The source can have overlapping geometries.
   */
  setOverlaps(t) {
    this.overlaps_ = t, this.changed();
  }
}
class Qc {
  /**
   * @param {Options} [options] Options.
   */
  constructor(t) {
    t = t || {}, this.patternImage_ = null, this.color_ = null, t.color !== void 0 && this.setColor(t.color);
  }
  /**
   * Clones the style. The color is not cloned if it is a {@link module:ol/colorlike~ColorLike}.
   * @return {Fill} The cloned style.
   * @api
   */
  clone() {
    const t = this.getColor();
    return new Qc({
      color: Array.isArray(t) ? t.slice() : t || void 0
    });
  }
  /**
   * Get the fill color.
   * @return {import("../color.js").Color|import("../colorlike.js").ColorLike|import('../colorlike.js').PatternDescriptor|null} Color.
   * @api
   */
  getColor() {
    return this.color_;
  }
  /**
   * Set the color.
   *
   * @param {import("../color.js").Color|import("../colorlike.js").ColorLike|import('../colorlike.js').PatternDescriptor|null} color Color.
   * @api
   */
  setColor(t) {
    if (t !== null && typeof t == "object" && "src" in t) {
      const i = RT(
        null,
        t.src,
        "anonymous",
        void 0,
        t.offset ? null : t.color ? t.color : null,
        !(t.offset && t.size)
      );
      i.ready().then(() => {
        this.patternImage_ = null;
      }), i.getImageState() === re.IDLE && i.load(), i.getImageState() === re.LOADING && (this.patternImage_ = i);
    }
    this.color_ = t;
  }
  /**
   * @return {string} Key of the fill for cache lookup.
   */
  getKey() {
    const t = this.getColor();
    return t ? t instanceof CanvasPattern || t instanceof CanvasGradient ? ue(t) : typeof t == "object" && "src" in t ? t.src + ":" + t.offset : Pm(t).toString() : "";
  }
  /**
   * @return {boolean} The fill style is loading an image pattern.
   */
  loading() {
    return !!this.patternImage_;
  }
  /**
   * @return {Promise<void>} `false` or a promise that resolves when the style is ready to use.
   */
  ready() {
    return this.patternImage_ ? this.patternImage_.ready() : Promise.resolve();
  }
}
class pu {
  /**
   * @param {Options} [options] Options.
   */
  constructor(t) {
    t = t || {}, this.color_ = t.color !== void 0 ? t.color : null, this.lineCap_ = t.lineCap, this.lineDash_ = t.lineDash !== void 0 ? t.lineDash : null, this.lineDashOffset_ = t.lineDashOffset, this.lineJoin_ = t.lineJoin, this.miterLimit_ = t.miterLimit, this.width_ = t.width;
  }
  /**
   * Clones the style.
   * @return {Stroke} The cloned style.
   * @api
   */
  clone() {
    const t = this.getColor();
    return new pu({
      color: Array.isArray(t) ? t.slice() : t || void 0,
      lineCap: this.getLineCap(),
      lineDash: this.getLineDash() ? this.getLineDash().slice() : void 0,
      lineDashOffset: this.getLineDashOffset(),
      lineJoin: this.getLineJoin(),
      miterLimit: this.getMiterLimit(),
      width: this.getWidth()
    });
  }
  /**
   * Get the stroke color.
   * @return {import("../color.js").Color|import("../colorlike.js").ColorLike} Color.
   * @api
   */
  getColor() {
    return this.color_;
  }
  /**
   * Get the line cap type for the stroke.
   * @return {CanvasLineCap|undefined} Line cap.
   * @api
   */
  getLineCap() {
    return this.lineCap_;
  }
  /**
   * Get the line dash style for the stroke.
   * @return {Array<number>|null} Line dash.
   * @api
   */
  getLineDash() {
    return this.lineDash_;
  }
  /**
   * Get the line dash offset for the stroke.
   * @return {number|undefined} Line dash offset.
   * @api
   */
  getLineDashOffset() {
    return this.lineDashOffset_;
  }
  /**
   * Get the line join type for the stroke.
   * @return {CanvasLineJoin|undefined} Line join.
   * @api
   */
  getLineJoin() {
    return this.lineJoin_;
  }
  /**
   * Get the miter limit for the stroke.
   * @return {number|undefined} Miter limit.
   * @api
   */
  getMiterLimit() {
    return this.miterLimit_;
  }
  /**
   * Get the stroke width.
   * @return {number|undefined} Width.
   * @api
   */
  getWidth() {
    return this.width_;
  }
  /**
   * Set the color.
   *
   * @param {import("../color.js").Color|import("../colorlike.js").ColorLike} color Color.
   * @api
   */
  setColor(t) {
    this.color_ = t;
  }
  /**
   * Set the line cap.
   *
   * @param {CanvasLineCap|undefined} lineCap Line cap.
   * @api
   */
  setLineCap(t) {
    this.lineCap_ = t;
  }
  /**
   * Set the line dash.
   *
   * @param {Array<number>|null} lineDash Line dash.
   * @api
   */
  setLineDash(t) {
    this.lineDash_ = t;
  }
  /**
   * Set the line dash offset.
   *
   * @param {number|undefined} lineDashOffset Line dash offset.
   * @api
   */
  setLineDashOffset(t) {
    this.lineDashOffset_ = t;
  }
  /**
   * Set the line join.
   *
   * @param {CanvasLineJoin|undefined} lineJoin Line join.
   * @api
   */
  setLineJoin(t) {
    this.lineJoin_ = t;
  }
  /**
   * Set the miter limit.
   *
   * @param {number|undefined} miterLimit Miter limit.
   * @api
   */
  setMiterLimit(t) {
    this.miterLimit_ = t;
  }
  /**
   * Set the width.
   *
   * @param {number|undefined} width Width.
   * @api
   */
  setWidth(t) {
    this.width_ = t;
  }
}
function LR(r) {
  return r[0] > 0 && r[1] > 0;
}
function CO(r, t, i) {
  return i === void 0 && (i = [0, 0]), i[0] = r[0] * t + 0.5 | 0, i[1] = r[1] * t + 0.5 | 0, i;
}
function Zl(r, t) {
  return Array.isArray(r) ? r : (t === void 0 ? t = [r, r] : (t[0] = r, t[1] = r), t);
}
class bS {
  /**
   * @param {Options} options Options.
   */
  constructor(t) {
    this.opacity_ = t.opacity, this.rotateWithView_ = t.rotateWithView, this.rotation_ = t.rotation, this.scale_ = t.scale, this.scaleArray_ = Zl(t.scale), this.displacement_ = t.displacement, this.declutterMode_ = t.declutterMode;
  }
  /**
   * Clones the style.
   * @return {ImageStyle} The cloned style.
   * @api
   */
  clone() {
    const t = this.getScale();
    return new bS({
      opacity: this.getOpacity(),
      scale: Array.isArray(t) ? t.slice() : t,
      rotation: this.getRotation(),
      rotateWithView: this.getRotateWithView(),
      displacement: this.getDisplacement().slice(),
      declutterMode: this.getDeclutterMode()
    });
  }
  /**
   * Get the symbolizer opacity.
   * @return {number} Opacity.
   * @api
   */
  getOpacity() {
    return this.opacity_;
  }
  /**
   * Determine whether the symbolizer rotates with the map.
   * @return {boolean} Rotate with map.
   * @api
   */
  getRotateWithView() {
    return this.rotateWithView_;
  }
  /**
   * Get the symoblizer rotation.
   * @return {number} Rotation.
   * @api
   */
  getRotation() {
    return this.rotation_;
  }
  /**
   * Get the symbolizer scale.
   * @return {number|import("../size.js").Size} Scale.
   * @api
   */
  getScale() {
    return this.scale_;
  }
  /**
   * Get the symbolizer scale array.
   * @return {import("../size.js").Size} Scale array.
   */
  getScaleArray() {
    return this.scaleArray_;
  }
  /**
   * Get the displacement of the shape
   * @return {Array<number>} Shape's center displacement
   * @api
   */
  getDisplacement() {
    return this.displacement_;
  }
  /**
   * Get the declutter mode of the shape
   * @return {import("./Style.js").DeclutterMode} Shape's declutter mode
   * @api
   */
  getDeclutterMode() {
    return this.declutterMode_;
  }
  /**
   * Get the anchor point in pixels. The anchor determines the center point for the
   * symbolizer.
   * @abstract
   * @return {Array<number>} Anchor.
   */
  getAnchor() {
    return we();
  }
  /**
   * Get the image element for the symbolizer.
   * @abstract
   * @param {number} pixelRatio Pixel ratio.
   * @return {import('../DataTile.js').ImageLike} Image element.
   */
  getImage(t) {
    return we();
  }
  /**
   * @abstract
   * @return {import('../DataTile.js').ImageLike} Image element.
   */
  getHitDetectionImage() {
    return we();
  }
  /**
   * Get the image pixel ratio.
   * @param {number} pixelRatio Pixel ratio.
   * @return {number} Pixel ratio.
   */
  getPixelRatio(t) {
    return 1;
  }
  /**
   * @abstract
   * @return {import("../ImageState.js").default} Image state.
   */
  getImageState() {
    return we();
  }
  /**
   * @abstract
   * @return {import("../size.js").Size} Image size.
   */
  getImageSize() {
    return we();
  }
  /**
   * Get the origin of the symbolizer.
   * @abstract
   * @return {Array<number>} Origin.
   */
  getOrigin() {
    return we();
  }
  /**
   * Get the size of the symbolizer (in pixels).
   * @abstract
   * @return {import("../size.js").Size} Size.
   */
  getSize() {
    return we();
  }
  /**
   * Set the displacement.
   *
   * @param {Array<number>} displacement Displacement.
   * @api
   */
  setDisplacement(t) {
    this.displacement_ = t;
  }
  /**
   * Set the opacity.
   *
   * @param {number} opacity Opacity.
   * @api
   */
  setOpacity(t) {
    this.opacity_ = t;
  }
  /**
   * Set whether to rotate the style with the view.
   *
   * @param {boolean} rotateWithView Rotate with map.
   * @api
   */
  setRotateWithView(t) {
    this.rotateWithView_ = t;
  }
  /**
   * Set the rotation.
   *
   * @param {number} rotation Rotation.
   * @api
   */
  setRotation(t) {
    this.rotation_ = t;
  }
  /**
   * Set the scale.
   *
   * @param {number|import("../size.js").Size} scale Scale.
   * @api
   */
  setScale(t) {
    this.scale_ = t, this.scaleArray_ = Zl(t);
  }
  /**
   * @abstract
   * @param {function(import("../events/Event.js").default): void} listener Listener function.
   */
  listenImageChange(t) {
    we();
  }
  /**
   * Load not yet loaded URI.
   * @abstract
   */
  load() {
    we();
  }
  /**
   * @abstract
   * @param {function(import("../events/Event.js").default): void} listener Listener function.
   */
  unlistenImageChange(t) {
    we();
  }
  /**
   * @return {Promise<void>} `false` or Promise that resolves when the style is ready to use.
   */
  ready() {
    return Promise.resolve();
  }
}
class TS extends bS {
  /**
   * @param {Options} options Options.
   */
  constructor(t) {
    super({
      opacity: 1,
      rotateWithView: t.rotateWithView !== void 0 ? t.rotateWithView : !1,
      rotation: t.rotation !== void 0 ? t.rotation : 0,
      scale: t.scale !== void 0 ? t.scale : 1,
      displacement: t.displacement !== void 0 ? t.displacement : [0, 0],
      declutterMode: t.declutterMode
    }), this.hitDetectionCanvas_ = null, this.fill_ = t.fill !== void 0 ? t.fill : null, this.origin_ = [0, 0], this.points_ = t.points, this.radius = t.radius, this.radius2_ = t.radius2, this.angle_ = t.angle !== void 0 ? t.angle : 0, this.stroke_ = t.stroke !== void 0 ? t.stroke : null, this.size_, this.renderOptions_, this.imageState_ = this.fill_ && this.fill_.loading() ? re.LOADING : re.LOADED, this.imageState_ === re.LOADING && this.ready().then(() => this.imageState_ = re.LOADED), this.render();
  }
  /**
   * Clones the style.
   * @return {RegularShape} The cloned style.
   * @api
   * @override
   */
  clone() {
    const t = this.getScale(), i = new TS({
      fill: this.getFill() ? this.getFill().clone() : void 0,
      points: this.getPoints(),
      radius: this.getRadius(),
      radius2: this.getRadius2(),
      angle: this.getAngle(),
      stroke: this.getStroke() ? this.getStroke().clone() : void 0,
      rotation: this.getRotation(),
      rotateWithView: this.getRotateWithView(),
      scale: Array.isArray(t) ? t.slice() : t,
      displacement: this.getDisplacement().slice(),
      declutterMode: this.getDeclutterMode()
    });
    return i.setOpacity(this.getOpacity()), i;
  }
  /**
   * Get the anchor point in pixels. The anchor determines the center point for the
   * symbolizer.
   * @return {Array<number>} Anchor.
   * @api
   * @override
   */
  getAnchor() {
    const t = this.size_, i = this.getDisplacement(), a = this.getScaleArray();
    return [
      t[0] / 2 - i[0] / a[0],
      t[1] / 2 + i[1] / a[1]
    ];
  }
  /**
   * Get the angle used in generating the shape.
   * @return {number} Shape's rotation in radians.
   * @api
   */
  getAngle() {
    return this.angle_;
  }
  /**
   * Get the fill style for the shape.
   * @return {import("./Fill.js").default|null} Fill style.
   * @api
   */
  getFill() {
    return this.fill_;
  }
  /**
   * Set the fill style.
   * @param {import("./Fill.js").default|null} fill Fill style.
   * @api
   */
  setFill(t) {
    this.fill_ = t, this.render();
  }
  /**
   * @return {HTMLCanvasElement} Image element.
   * @override
   */
  getHitDetectionImage() {
    return this.hitDetectionCanvas_ || (this.hitDetectionCanvas_ = this.createHitDetectionCanvas_(
      this.renderOptions_
    )), this.hitDetectionCanvas_;
  }
  /**
   * Get the image icon.
   * @param {number} pixelRatio Pixel ratio.
   * @return {HTMLCanvasElement} Image or Canvas element.
   * @api
   * @override
   */
  getImage(t) {
    var h, g;
    const i = (h = this.fill_) == null ? void 0 : h.getKey(), a = `${t},${this.angle_},${this.radius},${this.radius2_},${this.points_},${i}` + Object.values(this.renderOptions_).join(",");
    let u = (
      /** @type {HTMLCanvasElement} */
      (g = hs.get(a, null, null)) == null ? void 0 : g.getImage(1)
    );
    if (!u) {
      const y = this.renderOptions_, _ = Math.ceil(y.size * t), b = ei(_, _);
      this.draw_(y, b, t), u = b.canvas, hs.set(
        a,
        null,
        null,
        new V2(u, void 0, null, re.LOADED, null)
      );
    }
    return u;
  }
  /**
   * Get the image pixel ratio.
   * @param {number} pixelRatio Pixel ratio.
   * @return {number} Pixel ratio.
   * @override
   */
  getPixelRatio(t) {
    return t;
  }
  /**
   * @return {import("../size.js").Size} Image size.
   * @override
   */
  getImageSize() {
    return this.size_;
  }
  /**
   * @return {import("../ImageState.js").default} Image state.
   * @override
   */
  getImageState() {
    return this.imageState_;
  }
  /**
   * Get the origin of the symbolizer.
   * @return {Array<number>} Origin.
   * @api
   * @override
   */
  getOrigin() {
    return this.origin_;
  }
  /**
   * Get the number of points for generating the shape.
   * @return {number} Number of points for stars and regular polygons.
   * @api
   */
  getPoints() {
    return this.points_;
  }
  /**
   * Get the (primary) radius for the shape.
   * @return {number} Radius.
   * @api
   */
  getRadius() {
    return this.radius;
  }
  /**
   * Get the secondary radius for the shape.
   * @return {number|undefined} Radius2.
   * @api
   */
  getRadius2() {
    return this.radius2_;
  }
  /**
   * Get the size of the symbolizer (in pixels).
   * @return {import("../size.js").Size} Size.
   * @api
   * @override
   */
  getSize() {
    return this.size_;
  }
  /**
   * Get the stroke style for the shape.
   * @return {import("./Stroke.js").default|null} Stroke style.
   * @api
   */
  getStroke() {
    return this.stroke_;
  }
  /**
   * Set the stroke style.
   * @param {import("./Stroke.js").default|null} stroke Stroke style.
   * @api
   */
  setStroke(t) {
    this.stroke_ = t, this.render();
  }
  /**
   * @param {function(import("../events/Event.js").default): void} listener Listener function.
   * @override
   */
  listenImageChange(t) {
  }
  /**
   * Load not yet loaded URI.
   * @override
   */
  load() {
  }
  /**
   * @param {function(import("../events/Event.js").default): void} listener Listener function.
   * @override
   */
  unlistenImageChange(t) {
  }
  /**
   * Calculate additional canvas size needed for the miter.
   * @param {string} lineJoin Line join
   * @param {number} strokeWidth Stroke width
   * @param {number} miterLimit Miter limit
   * @return {number} Additional canvas size needed
   * @private
   */
  calculateLineJoinSize_(t, i, a) {
    if (i === 0 || this.points_ === 1 / 0 || t !== "bevel" && t !== "miter")
      return i;
    let u = this.radius, h = this.radius2_ === void 0 ? u : this.radius2_;
    if (u < h) {
      const dt = u;
      u = h, h = dt;
    }
    const g = this.radius2_ === void 0 ? this.points_ : this.points_ * 2, y = 2 * Math.PI / g, _ = h * Math.sin(y), b = Math.sqrt(h * h - _ * _), E = u - b, x = Math.sqrt(_ * _ + E * E), R = x / _;
    if (t === "miter" && R <= a)
      return R * i;
    const w = i / 2 / R, L = i / 2 * (E / x), N = Math.sqrt((u + w) * (u + w) + L * L) - u;
    if (this.radius2_ === void 0 || t === "bevel")
      return N * 2;
    const O = u * Math.sin(y), U = Math.sqrt(u * u - O * O), I = h - U, Z = Math.sqrt(O * O + I * I) / O;
    if (Z <= a) {
      const dt = Z * i / 2 - h - u;
      return 2 * Math.max(N, dt);
    }
    return N * 2;
  }
  /**
   * @return {RenderOptions}  The render options
   * @protected
   */
  createRenderOptions() {
    let t = Qm, i = Wm, a = 0, u = null, h = 0, g, y = 0;
    this.stroke_ && (g = Jo(this.stroke_.getColor() ?? S_), y = this.stroke_.getWidth() ?? b_, u = this.stroke_.getLineDash(), h = this.stroke_.getLineDashOffset() ?? 0, i = this.stroke_.getLineJoin() ?? Wm, t = this.stroke_.getLineCap() ?? Qm, a = this.stroke_.getMiterLimit() ?? v_);
    const _ = this.calculateLineJoinSize_(i, y, a), b = Math.max(this.radius, this.radius2_ || 0), E = Math.ceil(2 * b + _);
    return {
      strokeStyle: g,
      strokeWidth: y,
      size: E,
      lineCap: t,
      lineDash: u,
      lineDashOffset: h,
      lineJoin: i,
      miterLimit: a
    };
  }
  /**
   * @protected
   */
  render() {
    this.renderOptions_ = this.createRenderOptions();
    const t = this.renderOptions_.size;
    this.hitDetectionCanvas_ = null, this.size_ = [t, t];
  }
  /**
   * @private
   * @param {RenderOptions} renderOptions Render options.
   * @param {CanvasRenderingContext2D} context The rendering context.
   * @param {number} pixelRatio The pixel ratio.
   */
  draw_(t, i, a) {
    if (i.scale(a, a), i.translate(t.size / 2, t.size / 2), this.createPath_(i), this.fill_) {
      let u = this.fill_.getColor();
      u === null && (u = Xl), i.fillStyle = Jo(u), i.fill();
    }
    t.strokeStyle && (i.strokeStyle = t.strokeStyle, i.lineWidth = t.strokeWidth, t.lineDash && (i.setLineDash(t.lineDash), i.lineDashOffset = t.lineDashOffset), i.lineCap = t.lineCap, i.lineJoin = t.lineJoin, i.miterLimit = t.miterLimit, i.stroke());
  }
  /**
   * @private
   * @param {RenderOptions} renderOptions Render options.
   * @return {HTMLCanvasElement} Canvas containing the icon
   */
  createHitDetectionCanvas_(t) {
    let i;
    if (this.fill_) {
      let a = this.fill_.getColor(), u = 0;
      typeof a == "string" && (a = Pm(a)), a === null ? u = 1 : Array.isArray(a) && (u = a.length === 4 ? a[3] : 1), u === 0 && (i = ei(t.size, t.size), this.drawHitDetectionCanvas_(t, i));
    }
    return i ? i.canvas : this.getImage(1);
  }
  /**
   * @private
   * @param {CanvasRenderingContext2D} context The context to draw in.
   */
  createPath_(t) {
    let i = this.points_;
    const a = this.radius;
    if (i === 1 / 0)
      t.arc(0, 0, a, 0, 2 * Math.PI);
    else {
      const u = this.radius2_ === void 0 ? a : this.radius2_;
      this.radius2_ !== void 0 && (i *= 2);
      const h = this.angle_ - Math.PI / 2, g = 2 * Math.PI / i;
      for (let y = 0; y < i; y++) {
        const _ = h + y * g, b = y % 2 === 0 ? a : u;
        t.lineTo(b * Math.cos(_), b * Math.sin(_));
      }
      t.closePath();
    }
  }
  /**
   * @private
   * @param {RenderOptions} renderOptions Render options.
   * @param {CanvasRenderingContext2D} context The context.
   */
  drawHitDetectionCanvas_(t, i) {
    i.translate(t.size / 2, t.size / 2), this.createPath_(i), i.fillStyle = Xl, i.fill(), t.strokeStyle && (i.strokeStyle = t.strokeStyle, i.lineWidth = t.strokeWidth, t.lineDash && (i.setLineDash(t.lineDash), i.lineDashOffset = t.lineDashOffset), i.lineJoin = t.lineJoin, i.miterLimit = t.miterLimit, i.stroke());
  }
  /**
   * @override
   */
  ready() {
    return this.fill_ ? this.fill_.ready() : Promise.resolve();
  }
}
class L_ extends TS {
  /**
   * @param {Options} [options] Options.
   */
  constructor(t) {
    t = t || { radius: 5 }, super({
      points: 1 / 0,
      fill: t.fill,
      radius: t.radius,
      stroke: t.stroke,
      scale: t.scale !== void 0 ? t.scale : 1,
      rotation: t.rotation !== void 0 ? t.rotation : 0,
      rotateWithView: t.rotateWithView !== void 0 ? t.rotateWithView : !1,
      displacement: t.displacement !== void 0 ? t.displacement : [0, 0],
      declutterMode: t.declutterMode
    });
  }
  /**
   * Clones the style.
   * @return {CircleStyle} The cloned style.
   * @api
   * @override
   */
  clone() {
    const t = this.getScale(), i = new L_({
      fill: this.getFill() ? this.getFill().clone() : void 0,
      stroke: this.getStroke() ? this.getStroke().clone() : void 0,
      radius: this.getRadius(),
      scale: Array.isArray(t) ? t.slice() : t,
      rotation: this.getRotation(),
      rotateWithView: this.getRotateWithView(),
      displacement: this.getDisplacement().slice(),
      declutterMode: this.getDeclutterMode()
    });
    return i.setOpacity(this.getOpacity()), i;
  }
  /**
   * Set the circle radius.
   *
   * @param {number} radius Circle radius.
   * @api
   */
  setRadius(t) {
    this.radius = t, this.render();
  }
}
class pa {
  /**
   * @param {Options} [options] Style options.
   */
  constructor(t) {
    t = t || {}, this.geometry_ = null, this.geometryFunction_ = zR, t.geometry !== void 0 && this.setGeometry(t.geometry), this.fill_ = t.fill !== void 0 ? t.fill : null, this.image_ = t.image !== void 0 ? t.image : null, this.renderer_ = t.renderer !== void 0 ? t.renderer : null, this.hitDetectionRenderer_ = t.hitDetectionRenderer !== void 0 ? t.hitDetectionRenderer : null, this.stroke_ = t.stroke !== void 0 ? t.stroke : null, this.text_ = t.text !== void 0 ? t.text : null, this.zIndex_ = t.zIndex;
  }
  /**
   * Clones the style.
   * @return {Style} The cloned style.
   * @api
   */
  clone() {
    let t = this.getGeometry();
    return t && typeof t == "object" && (t = /** @type {import("../geom/Geometry.js").default} */
    t.clone()), new pa({
      geometry: t ?? void 0,
      fill: this.getFill() ? this.getFill().clone() : void 0,
      image: this.getImage() ? this.getImage().clone() : void 0,
      renderer: this.getRenderer() ?? void 0,
      stroke: this.getStroke() ? this.getStroke().clone() : void 0,
      text: this.getText() ? this.getText().clone() : void 0,
      zIndex: this.getZIndex()
    });
  }
  /**
   * Get the custom renderer function that was configured with
   * {@link #setRenderer} or the `renderer` constructor option.
   * @return {RenderFunction|null} Custom renderer function.
   * @api
   */
  getRenderer() {
    return this.renderer_;
  }
  /**
   * Sets a custom renderer function for this style. When set, `fill`, `stroke`
   * and `image` options of the style will be ignored.
   * @param {RenderFunction|null} renderer Custom renderer function.
   * @api
   */
  setRenderer(t) {
    this.renderer_ = t;
  }
  /**
   * Sets a custom renderer function for this style used
   * in hit detection.
   * @param {RenderFunction|null} renderer Custom renderer function.
   * @api
   */
  setHitDetectionRenderer(t) {
    this.hitDetectionRenderer_ = t;
  }
  /**
   * Get the custom renderer function that was configured with
   * {@link #setHitDetectionRenderer} or the `hitDetectionRenderer` constructor option.
   * @return {RenderFunction|null} Custom renderer function.
   * @api
   */
  getHitDetectionRenderer() {
    return this.hitDetectionRenderer_;
  }
  /**
   * Get the geometry to be rendered.
   * @return {string|import("../geom/Geometry.js").default|GeometryFunction|null}
   * Feature property or geometry or function that returns the geometry that will
   * be rendered with this style.
   * @api
   */
  getGeometry() {
    return this.geometry_;
  }
  /**
   * Get the function used to generate a geometry for rendering.
   * @return {!GeometryFunction} Function that is called with a feature
   * and returns the geometry to render instead of the feature's geometry.
   * @api
   */
  getGeometryFunction() {
    return this.geometryFunction_;
  }
  /**
   * Get the fill style.
   * @return {import("./Fill.js").default|null} Fill style.
   * @api
   */
  getFill() {
    return this.fill_;
  }
  /**
   * Set the fill style.
   * @param {import("./Fill.js").default|null} fill Fill style.
   * @api
   */
  setFill(t) {
    this.fill_ = t;
  }
  /**
   * Get the image style.
   * @return {import("./Image.js").default|null} Image style.
   * @api
   */
  getImage() {
    return this.image_;
  }
  /**
   * Set the image style.
   * @param {import("./Image.js").default} image Image style.
   * @api
   */
  setImage(t) {
    this.image_ = t;
  }
  /**
   * Get the stroke style.
   * @return {import("./Stroke.js").default|null} Stroke style.
   * @api
   */
  getStroke() {
    return this.stroke_;
  }
  /**
   * Set the stroke style.
   * @param {import("./Stroke.js").default|null} stroke Stroke style.
   * @api
   */
  setStroke(t) {
    this.stroke_ = t;
  }
  /**
   * Get the text style.
   * @return {import("./Text.js").default|null} Text style.
   * @api
   */
  getText() {
    return this.text_;
  }
  /**
   * Set the text style.
   * @param {import("./Text.js").default} text Text style.
   * @api
   */
  setText(t) {
    this.text_ = t;
  }
  /**
   * Get the z-index for the style.
   * @return {number|undefined} ZIndex.
   * @api
   */
  getZIndex() {
    return this.zIndex_;
  }
  /**
   * Set a geometry that is rendered instead of the feature's geometry.
   *
   * @param {string|import("../geom/Geometry.js").default|GeometryFunction|null} geometry
   *     Feature property or geometry or function returning a geometry to render
   *     for this style.
   * @api
   */
  setGeometry(t) {
    typeof t == "function" ? this.geometryFunction_ = t : typeof t == "string" ? this.geometryFunction_ = function(i) {
      return (
        /** @type {import("../geom/Geometry.js").default} */
        i.get(t)
      );
    } : t ? t !== void 0 && (this.geometryFunction_ = function() {
      return (
        /** @type {import("../geom/Geometry.js").default} */
        t
      );
    }) : this.geometryFunction_ = zR, this.geometry_ = t;
  }
  /**
   * Set the z-index.
   *
   * @param {number|undefined} zIndex ZIndex.
   * @api
   */
  setZIndex(t) {
    this.zIndex_ = t;
  }
}
function RO(r) {
  let t;
  if (typeof r == "function")
    t = r;
  else {
    let i;
    Array.isArray(r) ? i = r : (je(
      typeof /** @type {?} */
      r.getZIndex == "function",
      "Expected an `Style` or an array of `Style`"
    ), i = [
      /** @type {Style} */
      r
    ]), t = function() {
      return i;
    };
  }
  return t;
}
let ub = null;
function nA(r, t) {
  if (!ub) {
    const i = new Qc({
      color: "rgba(255,255,255,0.4)"
    }), a = new pu({
      color: "#3399CC",
      width: 1.25
    });
    ub = [
      new pa({
        image: new L_({
          fill: i,
          stroke: a,
          radius: 5
        }),
        fill: i,
        stroke: a
      })
    ];
  }
  return ub;
}
function iA() {
  const r = {}, t = [255, 255, 255, 1], i = [0, 153, 255, 1], a = 3;
  return r.Polygon = [
    new pa({
      fill: new Qc({
        color: [255, 255, 255, 0.5]
      })
    })
  ], r.MultiPolygon = r.Polygon, r.LineString = [
    new pa({
      stroke: new pu({
        color: t,
        width: a + 2
      })
    }),
    new pa({
      stroke: new pu({
        color: i,
        width: a
      })
    })
  ], r.MultiLineString = r.LineString, r.Circle = r.Polygon.concat(r.LineString), r.Point = [
    new pa({
      image: new L_({
        radius: a * 2,
        fill: new Qc({
          color: i
        }),
        stroke: new pu({
          color: t,
          width: a / 2
        })
      }),
      zIndex: 1 / 0
    })
  ], r.MultiPoint = r.Point, r.GeometryCollection = r.Polygon.concat(
    r.LineString,
    r.Point
  ), r;
}
function zR(r) {
  return r.getGeometry();
}
const AO = "#333";
class wT {
  /**
   * @param {Options} [options] Options.
   */
  constructor(t) {
    t = t || {}, this.font_ = t.font, this.rotation_ = t.rotation, this.rotateWithView_ = t.rotateWithView, this.keepUpright_ = t.keepUpright, this.scale_ = t.scale, this.scaleArray_ = Zl(t.scale !== void 0 ? t.scale : 1), this.text_ = t.text, this.textAlign_ = t.textAlign, this.justify_ = t.justify, this.repeat_ = t.repeat, this.textBaseline_ = t.textBaseline, this.fill_ = t.fill !== void 0 ? t.fill : new Qc({ color: AO }), this.maxAngle_ = t.maxAngle !== void 0 ? t.maxAngle : Math.PI / 4, this.placement_ = t.placement !== void 0 ? t.placement : "point", this.overflow_ = !!t.overflow, this.stroke_ = t.stroke !== void 0 ? t.stroke : null, this.offsetX_ = t.offsetX !== void 0 ? t.offsetX : 0, this.offsetY_ = t.offsetY !== void 0 ? t.offsetY : 0, this.backgroundFill_ = t.backgroundFill ? t.backgroundFill : null, this.backgroundStroke_ = t.backgroundStroke ? t.backgroundStroke : null, this.padding_ = t.padding === void 0 ? null : t.padding, this.declutterMode_ = t.declutterMode;
  }
  /**
   * Clones the style.
   * @return {Text} The cloned style.
   * @api
   */
  clone() {
    const t = this.getScale();
    return new wT({
      font: this.getFont(),
      placement: this.getPlacement(),
      repeat: this.getRepeat(),
      maxAngle: this.getMaxAngle(),
      overflow: this.getOverflow(),
      rotation: this.getRotation(),
      rotateWithView: this.getRotateWithView(),
      keepUpright: this.getKeepUpright(),
      scale: Array.isArray(t) ? t.slice() : t,
      text: this.getText(),
      textAlign: this.getTextAlign(),
      justify: this.getJustify(),
      textBaseline: this.getTextBaseline(),
      fill: this.getFill() ? this.getFill().clone() : void 0,
      stroke: this.getStroke() ? this.getStroke().clone() : void 0,
      offsetX: this.getOffsetX(),
      offsetY: this.getOffsetY(),
      backgroundFill: this.getBackgroundFill() ? this.getBackgroundFill().clone() : void 0,
      backgroundStroke: this.getBackgroundStroke() ? this.getBackgroundStroke().clone() : void 0,
      padding: this.getPadding() || void 0,
      declutterMode: this.getDeclutterMode()
    });
  }
  /**
   * Get the `overflow` configuration.
   * @return {boolean} Let text overflow the length of the path they follow.
   * @api
   */
  getOverflow() {
    return this.overflow_;
  }
  /**
   * Get the font name.
   * @return {string|undefined} Font.
   * @api
   */
  getFont() {
    return this.font_;
  }
  /**
   * Get the maximum angle between adjacent characters.
   * @return {number} Angle in radians.
   * @api
   */
  getMaxAngle() {
    return this.maxAngle_;
  }
  /**
   * Get the label placement.
   * @return {TextPlacement} Text placement.
   * @api
   */
  getPlacement() {
    return this.placement_;
  }
  /**
   * Get the repeat interval of the text.
   * @return {number|undefined} Repeat interval in pixels.
   * @api
   */
  getRepeat() {
    return this.repeat_;
  }
  /**
   * Get the x-offset for the text.
   * @return {number} Horizontal text offset.
   * @api
   */
  getOffsetX() {
    return this.offsetX_;
  }
  /**
   * Get the y-offset for the text.
   * @return {number} Vertical text offset.
   * @api
   */
  getOffsetY() {
    return this.offsetY_;
  }
  /**
   * Get the fill style for the text.
   * @return {import("./Fill.js").default|null} Fill style.
   * @api
   */
  getFill() {
    return this.fill_;
  }
  /**
   * Determine whether the text rotates with the map.
   * @return {boolean|undefined} Rotate with map.
   * @api
   */
  getRotateWithView() {
    return this.rotateWithView_;
  }
  /**
   * Determine whether the text can be rendered upside down.
   * @return {boolean|undefined} Keep text upright.
   * @api
   */
  getKeepUpright() {
    return this.keepUpright_;
  }
  /**
   * Get the text rotation.
   * @return {number|undefined} Rotation.
   * @api
   */
  getRotation() {
    return this.rotation_;
  }
  /**
   * Get the text scale.
   * @return {number|import("../size.js").Size|undefined} Scale.
   * @api
   */
  getScale() {
    return this.scale_;
  }
  /**
   * Get the symbolizer scale array.
   * @return {import("../size.js").Size} Scale array.
   */
  getScaleArray() {
    return this.scaleArray_;
  }
  /**
   * Get the stroke style for the text.
   * @return {import("./Stroke.js").default|null} Stroke style.
   * @api
   */
  getStroke() {
    return this.stroke_;
  }
  /**
   * Get the text to be rendered.
   * @return {string|Array<string>|undefined} Text.
   * @api
   */
  getText() {
    return this.text_;
  }
  /**
   * Get the text alignment.
   * @return {CanvasTextAlign|undefined} Text align.
   * @api
   */
  getTextAlign() {
    return this.textAlign_;
  }
  /**
   * Get the justification.
   * @return {TextJustify|undefined} Justification.
   * @api
   */
  getJustify() {
    return this.justify_;
  }
  /**
   * Get the text baseline.
   * @return {CanvasTextBaseline|undefined} Text baseline.
   * @api
   */
  getTextBaseline() {
    return this.textBaseline_;
  }
  /**
   * Get the background fill style for the text.
   * @return {import("./Fill.js").default|null} Fill style.
   * @api
   */
  getBackgroundFill() {
    return this.backgroundFill_;
  }
  /**
   * Get the background stroke style for the text.
   * @return {import("./Stroke.js").default|null} Stroke style.
   * @api
   */
  getBackgroundStroke() {
    return this.backgroundStroke_;
  }
  /**
   * Get the padding for the text.
   * @return {Array<number>|null} Padding.
   * @api
   */
  getPadding() {
    return this.padding_;
  }
  /**
   * Get the declutter mode of the shape
   * @return {import("./Style.js").DeclutterMode} Shape's declutter mode
   * @api
   */
  getDeclutterMode() {
    return this.declutterMode_;
  }
  /**
   * Set the `overflow` property.
   *
   * @param {boolean} overflow Let text overflow the path that it follows.
   * @api
   */
  setOverflow(t) {
    this.overflow_ = t;
  }
  /**
   * Set the font.
   *
   * @param {string|undefined} font Font.
   * @api
   */
  setFont(t) {
    this.font_ = t;
  }
  /**
   * Set the maximum angle between adjacent characters.
   *
   * @param {number} maxAngle Angle in radians.
   * @api
   */
  setMaxAngle(t) {
    this.maxAngle_ = t;
  }
  /**
   * Set the x offset.
   *
   * @param {number} offsetX Horizontal text offset.
   * @api
   */
  setOffsetX(t) {
    this.offsetX_ = t;
  }
  /**
   * Set the y offset.
   *
   * @param {number} offsetY Vertical text offset.
   * @api
   */
  setOffsetY(t) {
    this.offsetY_ = t;
  }
  /**
   * Set the text placement.
   *
   * @param {TextPlacement} placement Placement.
   * @api
   */
  setPlacement(t) {
    this.placement_ = t;
  }
  /**
   * Set the repeat interval of the text.
   * @param {number|undefined} [repeat] Repeat interval in pixels.
   * @api
   */
  setRepeat(t) {
    this.repeat_ = t;
  }
  /**
   * Set whether to rotate the text with the view.
   *
   * @param {boolean} rotateWithView Rotate with map.
   * @api
   */
  setRotateWithView(t) {
    this.rotateWithView_ = t;
  }
  /**
   * Set whether the text can be rendered upside down.
   *
   * @param {boolean} keepUpright Keep text upright.
   * @api
   */
  setKeepUpright(t) {
    this.keepUpright_ = t;
  }
  /**
   * Set the fill.
   *
   * @param {import("./Fill.js").default|null} fill Fill style.
   * @api
   */
  setFill(t) {
    this.fill_ = t;
  }
  /**
   * Set the rotation.
   *
   * @param {number|undefined} rotation Rotation.
   * @api
   */
  setRotation(t) {
    this.rotation_ = t;
  }
  /**
   * Set the scale.
   *
   * @param {number|import("../size.js").Size|undefined} scale Scale.
   * @api
   */
  setScale(t) {
    this.scale_ = t, this.scaleArray_ = Zl(t !== void 0 ? t : 1);
  }
  /**
   * Set the stroke.
   *
   * @param {import("./Stroke.js").default|null} stroke Stroke style.
   * @api
   */
  setStroke(t) {
    this.stroke_ = t;
  }
  /**
   * Set the text.
   *
   * @param {string|Array<string>|undefined} text Text.
   * @api
   */
  setText(t) {
    this.text_ = t;
  }
  /**
   * Set the text alignment.
   *
   * @param {CanvasTextAlign|undefined} textAlign Text align.
   * @api
   */
  setTextAlign(t) {
    this.textAlign_ = t;
  }
  /**
   * Set the justification.
   *
   * @param {TextJustify|undefined} justify Justification.
   * @api
   */
  setJustify(t) {
    this.justify_ = t;
  }
  /**
   * Set the text baseline.
   *
   * @param {CanvasTextBaseline|undefined} textBaseline Text baseline.
   * @api
   */
  setTextBaseline(t) {
    this.textBaseline_ = t;
  }
  /**
   * Set the background fill.
   *
   * @param {import("./Fill.js").default|null} fill Fill style.
   * @api
   */
  setBackgroundFill(t) {
    this.backgroundFill_ = t;
  }
  /**
   * Set the background stroke.
   *
   * @param {import("./Stroke.js").default|null} stroke Stroke style.
   * @api
   */
  setBackgroundStroke(t) {
    this.backgroundStroke_ = t;
  }
  /**
   * Set the padding (`[top, right, bottom, left]`).
   *
   * @param {Array<number>|null} padding Padding.
   * @api
   */
  setPadding(t) {
    this.padding_ = t;
  }
}
const Vi = {
  ANIMATING: 0,
  INTERACTING: 1
}, Yt = {
  BEGIN_GEOMETRY: 0,
  BEGIN_PATH: 1,
  CIRCLE: 2,
  CLOSE_PATH: 3,
  CUSTOM: 4,
  DRAW_CHARS: 5,
  DRAW_IMAGE: 6,
  END_GEOMETRY: 7,
  FILL: 8,
  MOVE_TO_LINE_TO: 9,
  SET_FILL_STYLE: 10,
  SET_STROKE_STYLE: 11,
  STROKE: 12
}, z1 = [Yt.FILL], Zc = [Yt.STROKE], Kh = [Yt.BEGIN_PATH], NR = [Yt.CLOSE_PATH];
class z_ extends q2 {
  /**
   * @param {number} tolerance Tolerance.
   * @param {import("../../extent.js").Extent} maxExtent Maximum extent.
   * @param {number} resolution Resolution.
   * @param {number} pixelRatio Pixel ratio.
   */
  constructor(t, i, a, u) {
    super(), this.tolerance = t, this.maxExtent = i, this.pixelRatio = u, this.maxLineWidth = 0, this.resolution = a, this.beginGeometryInstruction1_ = null, this.beginGeometryInstruction2_ = null, this.bufferedMaxExtent_ = null, this.instructions = [], this.coordinates = [], this.tmpCoordinate_ = [], this.hitDetectionInstructions = [], this.state = /** @type {import("../canvas.js").FillStrokeState} */
    {};
  }
  /**
   * @protected
   * @param {Array<number>} dashArray Dash array.
   * @return {Array<number>} Dash array with pixel ratio applied
   */
  applyPixelRatio(t) {
    const i = this.pixelRatio;
    return i == 1 ? t : t.map(function(a) {
      return a * i;
    });
  }
  /**
   * @param {Array<number>} flatCoordinates Flat coordinates.
   * @param {number} stride Stride.
   * @protected
   * @return {number} My end
   */
  appendFlatPointCoordinates(t, i) {
    const a = this.getBufferedMaxExtent(), u = this.tmpCoordinate_, h = this.coordinates;
    let g = h.length;
    for (let y = 0, _ = t.length; y < _; y += i)
      u[0] = t[y], u[1] = t[y + 1], Fm(a, u) && (h[g++] = u[0], h[g++] = u[1]);
    return g;
  }
  /**
   * @param {Array<number>} flatCoordinates Flat coordinates.
   * @param {number} offset Offset.
   * @param {number} end End.
   * @param {number} stride Stride.
   * @param {boolean} closed Last input coordinate equals first.
   * @param {boolean} skipFirst Skip first coordinate.
   * @protected
   * @return {number} My end.
   */
  appendFlatLineCoordinates(t, i, a, u, h, g) {
    const y = this.coordinates;
    let _ = y.length;
    const b = this.getBufferedMaxExtent();
    g && (i += u);
    let E = t[i], x = t[i + 1];
    const R = this.tmpCoordinate_;
    let w = !0, L, H, N;
    for (L = i + u; L < a; L += u)
      R[0] = t[L], R[1] = t[L + 1], N = wb(b, R), N !== H ? (w && (y[_++] = E, y[_++] = x, w = !1), y[_++] = R[0], y[_++] = R[1]) : N === fi.INTERSECTING ? (y[_++] = R[0], y[_++] = R[1], w = !1) : w = !0, E = R[0], x = R[1], H = N;
    return (h && w || L === i + u) && (y[_++] = E, y[_++] = x), _;
  }
  /**
   * @param {Array<number>} flatCoordinates Flat coordinates.
   * @param {number} offset Offset.
   * @param {Array<number>} ends Ends.
   * @param {number} stride Stride.
   * @param {Array<number>} builderEnds Builder ends.
   * @return {number} Offset.
   */
  drawCustomCoordinates_(t, i, a, u, h) {
    for (let g = 0, y = a.length; g < y; ++g) {
      const _ = a[g], b = this.appendFlatLineCoordinates(
        t,
        i,
        _,
        u,
        !1,
        !1
      );
      h.push(b), i = _;
    }
    return i;
  }
  /**
   * @param {import("../../geom/SimpleGeometry.js").default} geometry Geometry.
   * @param {import("../../Feature.js").FeatureLike} feature Feature.
   * @param {Function} renderer Renderer.
   * @param {Function} hitDetectionRenderer Renderer.
   * @param {number} [index] Render order index.
   * @override
   */
  drawCustom(t, i, a, u, h) {
    this.beginGeometry(t, i, h);
    const g = t.getType(), y = t.getStride(), _ = this.coordinates.length;
    let b, E, x, R, w;
    switch (g) {
      case "MultiPolygon":
        b = /** @type {import("../../geom/MultiPolygon.js").default} */
        t.getOrientedFlatCoordinates(), R = [];
        const L = (
          /** @type {import("../../geom/MultiPolygon.js").default} */
          t.getEndss()
        );
        w = 0;
        for (let H = 0, N = L.length; H < N; ++H) {
          const O = [];
          w = this.drawCustomCoordinates_(
            b,
            w,
            L[H],
            y,
            O
          ), R.push(O);
        }
        this.instructions.push([
          Yt.CUSTOM,
          _,
          R,
          t,
          a,
          Gb,
          h
        ]), this.hitDetectionInstructions.push([
          Yt.CUSTOM,
          _,
          R,
          t,
          u || a,
          Gb,
          h
        ]);
        break;
      case "Polygon":
      case "MultiLineString":
        x = [], b = g == "Polygon" ? (
          /** @type {import("../../geom/Polygon.js").default} */
          t.getOrientedFlatCoordinates()
        ) : t.getFlatCoordinates(), w = this.drawCustomCoordinates_(
          b,
          0,
          /** @type {import("../../geom/Polygon.js").default|import("../../geom/MultiLineString.js").default} */
          t.getEnds(),
          y,
          x
        ), this.instructions.push([
          Yt.CUSTOM,
          _,
          x,
          t,
          a,
          p_,
          h
        ]), this.hitDetectionInstructions.push([
          Yt.CUSTOM,
          _,
          x,
          t,
          u || a,
          p_,
          h
        ]);
        break;
      case "LineString":
      case "Circle":
        b = t.getFlatCoordinates(), E = this.appendFlatLineCoordinates(
          b,
          0,
          b.length,
          y,
          !1,
          !1
        ), this.instructions.push([
          Yt.CUSTOM,
          _,
          E,
          t,
          a,
          qc,
          h
        ]), this.hitDetectionInstructions.push([
          Yt.CUSTOM,
          _,
          E,
          t,
          u || a,
          qc,
          h
        ]);
        break;
      case "MultiPoint":
        b = t.getFlatCoordinates(), E = this.appendFlatPointCoordinates(b, y), E > _ && (this.instructions.push([
          Yt.CUSTOM,
          _,
          E,
          t,
          a,
          qc,
          h
        ]), this.hitDetectionInstructions.push([
          Yt.CUSTOM,
          _,
          E,
          t,
          u || a,
          qc,
          h
        ]));
        break;
      case "Point":
        b = t.getFlatCoordinates(), this.coordinates.push(b[0], b[1]), E = this.coordinates.length, this.instructions.push([
          Yt.CUSTOM,
          _,
          E,
          t,
          a,
          void 0,
          h
        ]), this.hitDetectionInstructions.push([
          Yt.CUSTOM,
          _,
          E,
          t,
          u || a,
          void 0,
          h
        ]);
        break;
    }
    this.endGeometry(i);
  }
  /**
   * @protected
   * @param {import("../../geom/Geometry").default|import("../Feature.js").default} geometry The geometry.
   * @param {import("../../Feature.js").FeatureLike} feature Feature.
   * @param {number} index Render order index
   */
  beginGeometry(t, i, a) {
    this.beginGeometryInstruction1_ = [
      Yt.BEGIN_GEOMETRY,
      i,
      0,
      t,
      a
    ], this.instructions.push(this.beginGeometryInstruction1_), this.beginGeometryInstruction2_ = [
      Yt.BEGIN_GEOMETRY,
      i,
      0,
      t,
      a
    ], this.hitDetectionInstructions.push(this.beginGeometryInstruction2_);
  }
  /**
   * @return {import("../canvas.js").SerializableInstructions} the serializable instructions.
   */
  finish() {
    return {
      instructions: this.instructions,
      hitDetectionInstructions: this.hitDetectionInstructions,
      coordinates: this.coordinates
    };
  }
  /**
   * Reverse the hit detection instructions.
   */
  reverseHitDetectionInstructions() {
    const t = this.hitDetectionInstructions;
    t.reverse();
    let i;
    const a = t.length;
    let u, h, g = -1;
    for (i = 0; i < a; ++i)
      u = t[i], h = /** @type {import("./Instruction.js").default} */
      u[0], h == Yt.END_GEOMETRY ? g = i : h == Yt.BEGIN_GEOMETRY && (u[2] = i, ow(this.hitDetectionInstructions, g, i), g = -1);
  }
  /**
   * @param {import("../../style/Fill.js").default} fillStyle Fill style.
   * @param {import('../canvas.js').FillStrokeState} [state] State.
   * @return {import('../canvas.js').FillStrokeState} State.
   */
  fillStyleToState(t, i = (
    /** @type {import('../canvas.js').FillStrokeState} */
    {}
  )) {
    if (t) {
      const a = t.getColor();
      i.fillPatternScale = a && typeof a == "object" && "src" in a ? this.pixelRatio : 1, i.fillStyle = Jo(
        a || Xl
      );
    } else
      i.fillStyle = void 0;
    return i;
  }
  /**
   * @param {import("../../style/Stroke.js").default} strokeStyle Stroke style.
   * @param {import("../canvas.js").FillStrokeState} state State.
   * @return {import("../canvas.js").FillStrokeState} State.
   */
  strokeStyleToState(t, i = (
    /** @type {import('../canvas.js').FillStrokeState} */
    {}
  )) {
    if (t) {
      const a = t.getColor();
      i.strokeStyle = Jo(
        a || S_
      );
      const u = t.getLineCap();
      i.lineCap = u !== void 0 ? u : Qm;
      const h = t.getLineDash();
      i.lineDash = h ? h.slice() : mu;
      const g = t.getLineDashOffset();
      i.lineDashOffset = g || yu;
      const y = t.getLineJoin();
      i.lineJoin = y !== void 0 ? y : Wm;
      const _ = t.getWidth();
      i.lineWidth = _ !== void 0 ? _ : b_;
      const b = t.getMiterLimit();
      i.miterLimit = b !== void 0 ? b : v_, i.lineWidth > this.maxLineWidth && (this.maxLineWidth = i.lineWidth, this.bufferedMaxExtent_ = null);
    } else
      i.strokeStyle = void 0, i.lineCap = void 0, i.lineDash = null, i.lineDashOffset = void 0, i.lineJoin = void 0, i.lineWidth = void 0, i.miterLimit = void 0;
    return i;
  }
  /**
   * @param {import("../../style/Fill.js").default} fillStyle Fill style.
   * @param {import("../../style/Stroke.js").default} strokeStyle Stroke style.
   * @override
   */
  setFillStrokeStyle(t, i) {
    const a = this.state;
    this.fillStyleToState(t, a), this.strokeStyleToState(i, a);
  }
  /**
   * @param {import("../canvas.js").FillStrokeState} state State.
   * @return {Array<*>} Fill instruction.
   */
  createFill(t) {
    const i = t.fillStyle, a = [Yt.SET_FILL_STYLE, i];
    return typeof i != "string" && a.push(t.fillPatternScale), a;
  }
  /**
   * @param {import("../canvas.js").FillStrokeState} state State.
   */
  applyStroke(t) {
    this.instructions.push(this.createStroke(t));
  }
  /**
   * @param {import("../canvas.js").FillStrokeState} state State.
   * @return {Array<*>} Stroke instruction.
   */
  createStroke(t) {
    return [
      Yt.SET_STROKE_STYLE,
      t.strokeStyle,
      t.lineWidth * this.pixelRatio,
      t.lineCap,
      t.lineJoin,
      t.miterLimit,
      t.lineDash ? this.applyPixelRatio(t.lineDash) : null,
      t.lineDashOffset * this.pixelRatio
    ];
  }
  /**
   * @param {import("../canvas.js").FillStrokeState} state State.
   * @param {function(this:CanvasBuilder, import("../canvas.js").FillStrokeState):Array<*>} createFill Create fill.
   */
  updateFillStyle(t, i) {
    const a = t.fillStyle;
    (typeof a != "string" || t.currentFillStyle != a) && (this.instructions.push(i.call(this, t)), t.currentFillStyle = a);
  }
  /**
   * @param {import("../canvas.js").FillStrokeState} state State.
   * @param {function(this:CanvasBuilder, import("../canvas.js").FillStrokeState): void} applyStroke Apply stroke.
   */
  updateStrokeStyle(t, i) {
    const a = t.strokeStyle, u = t.lineCap, h = t.lineDash, g = t.lineDashOffset, y = t.lineJoin, _ = t.lineWidth, b = t.miterLimit;
    (t.currentStrokeStyle != a || t.currentLineCap != u || h != t.currentLineDash && !Wc(t.currentLineDash, h) || t.currentLineDashOffset != g || t.currentLineJoin != y || t.currentLineWidth != _ || t.currentMiterLimit != b) && (i.call(this, t), t.currentStrokeStyle = a, t.currentLineCap = u, t.currentLineDash = h, t.currentLineDashOffset = g, t.currentLineJoin = y, t.currentLineWidth = _, t.currentMiterLimit = b);
  }
  /**
   * @param {import("../../Feature.js").FeatureLike} feature Feature.
   */
  endGeometry(t) {
    this.beginGeometryInstruction1_[2] = this.instructions.length, this.beginGeometryInstruction1_ = null, this.beginGeometryInstruction2_[2] = this.hitDetectionInstructions.length, this.beginGeometryInstruction2_ = null;
    const i = [Yt.END_GEOMETRY, t];
    this.instructions.push(i), this.hitDetectionInstructions.push(i);
  }
  /**
   * Get the buffered rendering extent.  Rendering will be clipped to the extent
   * provided to the constructor.  To account for symbolizers that may intersect
   * this extent, we calculate a buffered extent (e.g. based on stroke width).
   * @return {import("../../extent.js").Extent} The buffered rendering extent.
   * @protected
   */
  getBufferedMaxExtent() {
    if (!this.bufferedMaxExtent_ && (this.bufferedMaxExtent_ = d2(this.maxExtent), this.maxLineWidth > 0)) {
      const t = this.resolution * (this.maxLineWidth + 1) / 2;
      rS(this.bufferedMaxExtent_, t, this.bufferedMaxExtent_);
    }
    return this.bufferedMaxExtent_;
  }
}
class MO extends z_ {
  /**
   * @param {number} tolerance Tolerance.
   * @param {import("../../extent.js").Extent} maxExtent Maximum extent.
   * @param {number} resolution Resolution.
   * @param {number} pixelRatio Pixel ratio.
   */
  constructor(t, i, a, u) {
    super(t, i, a, u), this.hitDetectionImage_ = null, this.image_ = null, this.imagePixelRatio_ = void 0, this.anchorX_ = void 0, this.anchorY_ = void 0, this.height_ = void 0, this.opacity_ = void 0, this.originX_ = void 0, this.originY_ = void 0, this.rotateWithView_ = void 0, this.rotation_ = void 0, this.scale_ = void 0, this.width_ = void 0, this.declutterMode_ = void 0, this.declutterImageWithText_ = void 0;
  }
  /**
   * @param {import("../../geom/Point.js").default|import("../Feature.js").default} pointGeometry Point geometry.
   * @param {import("../../Feature.js").FeatureLike} feature Feature.
   * @param {number} [index] Render order index.
   * @override
   */
  drawPoint(t, i, a) {
    if (!this.image_ || this.maxExtent && !Fm(this.maxExtent, t.getFlatCoordinates()))
      return;
    this.beginGeometry(t, i, a);
    const u = t.getFlatCoordinates(), h = t.getStride(), g = this.coordinates.length, y = this.appendFlatPointCoordinates(u, h);
    this.instructions.push([
      Yt.DRAW_IMAGE,
      g,
      y,
      this.image_,
      // Remaining arguments to DRAW_IMAGE are in alphabetical order
      this.anchorX_ * this.imagePixelRatio_,
      this.anchorY_ * this.imagePixelRatio_,
      Math.ceil(this.height_ * this.imagePixelRatio_),
      this.opacity_,
      this.originX_ * this.imagePixelRatio_,
      this.originY_ * this.imagePixelRatio_,
      this.rotateWithView_,
      this.rotation_,
      [
        this.scale_[0] * this.pixelRatio / this.imagePixelRatio_,
        this.scale_[1] * this.pixelRatio / this.imagePixelRatio_
      ],
      Math.ceil(this.width_ * this.imagePixelRatio_),
      this.declutterMode_,
      this.declutterImageWithText_
    ]), this.hitDetectionInstructions.push([
      Yt.DRAW_IMAGE,
      g,
      y,
      this.hitDetectionImage_,
      // Remaining arguments to DRAW_IMAGE are in alphabetical order
      this.anchorX_,
      this.anchorY_,
      this.height_,
      1,
      this.originX_,
      this.originY_,
      this.rotateWithView_,
      this.rotation_,
      this.scale_,
      this.width_,
      this.declutterMode_,
      this.declutterImageWithText_
    ]), this.endGeometry(i);
  }
  /**
   * @param {import("../../geom/MultiPoint.js").default|import("../Feature.js").default} multiPointGeometry MultiPoint geometry.
   * @param {import("../../Feature.js").FeatureLike} feature Feature.
   * @param {number} [index] Render order index.
   * @override
   */
  drawMultiPoint(t, i, a) {
    if (!this.image_)
      return;
    this.beginGeometry(t, i, a);
    const u = t.getFlatCoordinates(), h = [];
    for (let _ = 0, b = u.length; _ < b; _ += t.getStride())
      (!this.maxExtent || Fm(this.maxExtent, u.slice(_, _ + 2))) && h.push(
        u[_],
        u[_ + 1]
      );
    const g = this.coordinates.length, y = this.appendFlatPointCoordinates(h, 2);
    this.instructions.push([
      Yt.DRAW_IMAGE,
      g,
      y,
      this.image_,
      // Remaining arguments to DRAW_IMAGE are in alphabetical order
      this.anchorX_ * this.imagePixelRatio_,
      this.anchorY_ * this.imagePixelRatio_,
      Math.ceil(this.height_ * this.imagePixelRatio_),
      this.opacity_,
      this.originX_ * this.imagePixelRatio_,
      this.originY_ * this.imagePixelRatio_,
      this.rotateWithView_,
      this.rotation_,
      [
        this.scale_[0] * this.pixelRatio / this.imagePixelRatio_,
        this.scale_[1] * this.pixelRatio / this.imagePixelRatio_
      ],
      Math.ceil(this.width_ * this.imagePixelRatio_),
      this.declutterMode_,
      this.declutterImageWithText_
    ]), this.hitDetectionInstructions.push([
      Yt.DRAW_IMAGE,
      g,
      y,
      this.hitDetectionImage_,
      // Remaining arguments to DRAW_IMAGE are in alphabetical order
      this.anchorX_,
      this.anchorY_,
      this.height_,
      1,
      this.originX_,
      this.originY_,
      this.rotateWithView_,
      this.rotation_,
      this.scale_,
      this.width_,
      this.declutterMode_,
      this.declutterImageWithText_
    ]), this.endGeometry(i);
  }
  /**
   * @return {import("../canvas.js").SerializableInstructions} the serializable instructions.
   * @override
   */
  finish() {
    return this.reverseHitDetectionInstructions(), this.anchorX_ = void 0, this.anchorY_ = void 0, this.hitDetectionImage_ = null, this.image_ = null, this.imagePixelRatio_ = void 0, this.height_ = void 0, this.scale_ = void 0, this.opacity_ = void 0, this.originX_ = void 0, this.originY_ = void 0, this.rotateWithView_ = void 0, this.rotation_ = void 0, this.width_ = void 0, super.finish();
  }
  /**
   * @param {import("../../style/Image.js").default} imageStyle Image style.
   * @param {Object} [sharedData] Shared data.
   * @override
   */
  setImageStyle(t, i) {
    const a = t.getAnchor(), u = t.getSize(), h = t.getOrigin();
    this.imagePixelRatio_ = t.getPixelRatio(this.pixelRatio), this.anchorX_ = a[0], this.anchorY_ = a[1], this.hitDetectionImage_ = t.getHitDetectionImage(), this.image_ = t.getImage(this.pixelRatio), this.height_ = u[1], this.opacity_ = t.getOpacity(), this.originX_ = h[0], this.originY_ = h[1], this.rotateWithView_ = t.getRotateWithView(), this.rotation_ = t.getRotation(), this.scale_ = t.getScaleArray(), this.width_ = u[0], this.declutterMode_ = t.getDeclutterMode(), this.declutterImageWithText_ = i;
  }
}
class wO extends z_ {
  /**
   * @param {number} tolerance Tolerance.
   * @param {import("../../extent.js").Extent} maxExtent Maximum extent.
   * @param {number} resolution Resolution.
   * @param {number} pixelRatio Pixel ratio.
   */
  constructor(t, i, a, u) {
    super(t, i, a, u);
  }
  /**
   * @param {Array<number>} flatCoordinates Flat coordinates.
   * @param {number} offset Offset.
   * @param {number} end End.
   * @param {number} stride Stride.
   * @private
   * @return {number} end.
   */
  drawFlatCoordinates_(t, i, a, u) {
    const h = this.coordinates.length, g = this.appendFlatLineCoordinates(
      t,
      i,
      a,
      u,
      !1,
      !1
    ), y = [
      Yt.MOVE_TO_LINE_TO,
      h,
      g
    ];
    return this.instructions.push(y), this.hitDetectionInstructions.push(y), a;
  }
  /**
   * @param {import("../../geom/LineString.js").default|import("../Feature.js").default} lineStringGeometry Line string geometry.
   * @param {import("../../Feature.js").FeatureLike} feature Feature.
   * @param {number} [index] Render order index.
   * @override
   */
  drawLineString(t, i, a) {
    const u = this.state, h = u.strokeStyle, g = u.lineWidth;
    if (h === void 0 || g === void 0)
      return;
    this.updateStrokeStyle(u, this.applyStroke), this.beginGeometry(t, i, a), this.hitDetectionInstructions.push(
      [
        Yt.SET_STROKE_STYLE,
        u.strokeStyle,
        u.lineWidth,
        u.lineCap,
        u.lineJoin,
        u.miterLimit,
        mu,
        yu
      ],
      Kh
    );
    const y = t.getFlatCoordinates(), _ = t.getStride();
    this.drawFlatCoordinates_(
      y,
      0,
      y.length,
      _
    ), this.hitDetectionInstructions.push(Zc), this.endGeometry(i);
  }
  /**
   * @param {import("../../geom/MultiLineString.js").default|import("../Feature.js").default} multiLineStringGeometry MultiLineString geometry.
   * @param {import("../../Feature.js").FeatureLike} feature Feature.
   * @param {number} [index] Render order index.
   * @override
   */
  drawMultiLineString(t, i, a) {
    const u = this.state, h = u.strokeStyle, g = u.lineWidth;
    if (h === void 0 || g === void 0)
      return;
    this.updateStrokeStyle(u, this.applyStroke), this.beginGeometry(t, i, a), this.hitDetectionInstructions.push(
      [
        Yt.SET_STROKE_STYLE,
        u.strokeStyle,
        u.lineWidth,
        u.lineCap,
        u.lineJoin,
        u.miterLimit,
        mu,
        yu
      ],
      Kh
    );
    const y = t.getEnds(), _ = t.getFlatCoordinates(), b = t.getStride();
    let E = 0;
    for (let x = 0, R = y.length; x < R; ++x)
      E = this.drawFlatCoordinates_(
        _,
        E,
        /** @type {number} */
        y[x],
        b
      );
    this.hitDetectionInstructions.push(Zc), this.endGeometry(i);
  }
  /**
   * @return {import("../canvas.js").SerializableInstructions} the serializable instructions.
   * @override
   */
  finish() {
    const t = this.state;
    return t.lastStroke != null && t.lastStroke != this.coordinates.length && this.instructions.push(Zc), this.reverseHitDetectionInstructions(), this.state = null, super.finish();
  }
  /**
   * @param {import("../canvas.js").FillStrokeState} state State.
   * @override
   */
  applyStroke(t) {
    t.lastStroke != null && t.lastStroke != this.coordinates.length && (this.instructions.push(Zc), t.lastStroke = this.coordinates.length), t.lastStroke = 0, super.applyStroke(t), this.instructions.push(Kh);
  }
}
class UR extends z_ {
  /**
   * @param {number} tolerance Tolerance.
   * @param {import("../../extent.js").Extent} maxExtent Maximum extent.
   * @param {number} resolution Resolution.
   * @param {number} pixelRatio Pixel ratio.
   */
  constructor(t, i, a, u) {
    super(t, i, a, u);
  }
  /**
   * @param {Array<number>} flatCoordinates Flat coordinates.
   * @param {number} offset Offset.
   * @param {Array<number>} ends Ends.
   * @param {number} stride Stride.
   * @private
   * @return {number} End.
   */
  drawFlatCoordinatess_(t, i, a, u) {
    const h = this.state, g = h.fillStyle !== void 0, y = h.strokeStyle !== void 0, _ = a.length;
    this.instructions.push(Kh), this.hitDetectionInstructions.push(Kh);
    for (let b = 0; b < _; ++b) {
      const E = a[b], x = this.coordinates.length, R = this.appendFlatLineCoordinates(
        t,
        i,
        E,
        u,
        !0,
        !y
      ), w = [
        Yt.MOVE_TO_LINE_TO,
        x,
        R
      ];
      this.instructions.push(w), this.hitDetectionInstructions.push(w), y && (this.instructions.push(NR), this.hitDetectionInstructions.push(NR)), i = E;
    }
    return g && (this.instructions.push(z1), this.hitDetectionInstructions.push(z1)), y && (this.instructions.push(Zc), this.hitDetectionInstructions.push(Zc)), i;
  }
  /**
   * @param {import("../../geom/Circle.js").default} circleGeometry Circle geometry.
   * @param {import("../../Feature.js").default} feature Feature.
   * @param {number} [index] Render order index.
   * @override
   */
  drawCircle(t, i, a) {
    const u = this.state, h = u.fillStyle, g = u.strokeStyle;
    if (h === void 0 && g === void 0)
      return;
    this.setFillStrokeStyles_(), this.beginGeometry(t, i, a), u.fillStyle !== void 0 && this.hitDetectionInstructions.push([
      Yt.SET_FILL_STYLE,
      Xl
    ]), u.strokeStyle !== void 0 && this.hitDetectionInstructions.push([
      Yt.SET_STROKE_STYLE,
      u.strokeStyle,
      u.lineWidth,
      u.lineCap,
      u.lineJoin,
      u.miterLimit,
      mu,
      yu
    ]);
    const y = t.getFlatCoordinates(), _ = t.getStride(), b = this.coordinates.length;
    this.appendFlatLineCoordinates(
      y,
      0,
      y.length,
      _,
      !1,
      !1
    );
    const E = [Yt.CIRCLE, b];
    this.instructions.push(Kh, E), this.hitDetectionInstructions.push(Kh, E), u.fillStyle !== void 0 && (this.instructions.push(z1), this.hitDetectionInstructions.push(z1)), u.strokeStyle !== void 0 && (this.instructions.push(Zc), this.hitDetectionInstructions.push(Zc)), this.endGeometry(i);
  }
  /**
   * @param {import("../../geom/Polygon.js").default|import("../Feature.js").default} polygonGeometry Polygon geometry.
   * @param {import("../../Feature.js").FeatureLike} feature Feature.
   * @param {number} [index] Render order index.
   * @override
   */
  drawPolygon(t, i, a) {
    const u = this.state, h = u.fillStyle, g = u.strokeStyle;
    if (h === void 0 && g === void 0)
      return;
    this.setFillStrokeStyles_(), this.beginGeometry(t, i, a), u.fillStyle !== void 0 && this.hitDetectionInstructions.push([
      Yt.SET_FILL_STYLE,
      Xl
    ]), u.strokeStyle !== void 0 && this.hitDetectionInstructions.push([
      Yt.SET_STROKE_STYLE,
      u.strokeStyle,
      u.lineWidth,
      u.lineCap,
      u.lineJoin,
      u.miterLimit,
      mu,
      yu
    ]);
    const y = t.getEnds(), _ = t.getOrientedFlatCoordinates(), b = t.getStride();
    this.drawFlatCoordinatess_(
      _,
      0,
      /** @type {Array<number>} */
      y,
      b
    ), this.endGeometry(i);
  }
  /**
   * @param {import("../../geom/MultiPolygon.js").default} multiPolygonGeometry MultiPolygon geometry.
   * @param {import("../../Feature.js").FeatureLike} feature Feature.
   * @param {number} [index] Render order index.
   * @override
   */
  drawMultiPolygon(t, i, a) {
    const u = this.state, h = u.fillStyle, g = u.strokeStyle;
    if (h === void 0 && g === void 0)
      return;
    this.setFillStrokeStyles_(), this.beginGeometry(t, i, a), u.fillStyle !== void 0 && this.hitDetectionInstructions.push([
      Yt.SET_FILL_STYLE,
      Xl
    ]), u.strokeStyle !== void 0 && this.hitDetectionInstructions.push([
      Yt.SET_STROKE_STYLE,
      u.strokeStyle,
      u.lineWidth,
      u.lineCap,
      u.lineJoin,
      u.miterLimit,
      mu,
      yu
    ]);
    const y = t.getEndss(), _ = t.getOrientedFlatCoordinates(), b = t.getStride();
    let E = 0;
    for (let x = 0, R = y.length; x < R; ++x)
      E = this.drawFlatCoordinatess_(
        _,
        E,
        y[x],
        b
      );
    this.endGeometry(i);
  }
  /**
   * @return {import("../canvas.js").SerializableInstructions} the serializable instructions.
   * @override
   */
  finish() {
    this.reverseHitDetectionInstructions(), this.state = null;
    const t = this.tolerance;
    if (t !== 0) {
      const i = this.coordinates;
      for (let a = 0, u = i.length; a < u; ++a)
        i[a] = Xh(i[a], t);
    }
    return super.finish();
  }
  /**
   * @private
   */
  setFillStrokeStyles_() {
    const t = this.state;
    this.updateFillStyle(t, this.createFill), this.updateStrokeStyle(t, this.applyStroke);
  }
}
function DO(r, t, i, a, u) {
  const h = [];
  let g = i, y = 0, _ = t.slice(i, 2);
  for (; y < r && g + u < a; ) {
    const [b, E] = _.slice(-2), x = t[g + u], R = t[g + u + 1], w = Math.sqrt(
      (x - b) * (x - b) + (R - E) * (R - E)
    );
    if (y += w, y >= r) {
      const L = (r - y + w) / w, H = ya(b, x, L), N = ya(E, R, L);
      _.push(H, N), h.push(_), _ = [H, N], y == r && (g += u), y = 0;
    } else if (y < r)
      _.push(
        t[g + u],
        t[g + u + 1]
      ), g += u;
    else {
      const L = w - y, H = ya(b, x, L / w), N = ya(E, R, L / w);
      _.push(H, N), h.push(_), _ = [H, N], y = 0, g += u;
    }
  }
  return y > 0 && h.push(_), h;
}
function OO(r, t, i, a, u) {
  let h = i, g = i, y = 0, _ = 0, b = i, E, x, R, w, L, H, N, O, U, I;
  for (x = i; x < a; x += u) {
    const X = t[x], Z = t[x + 1];
    L !== void 0 && (U = X - L, I = Z - H, w = Math.sqrt(U * U + I * I), N !== void 0 && (_ += R, E = Math.acos((N * U + O * I) / (R * w)), E > r && (_ > y && (y = _, h = b, g = x), _ = 0, b = x - u)), R = w, N = U, O = I), L = X, H = Z;
  }
  return _ += w, _ > y ? [b, x] : [h, g];
}
const $1 = {
  left: 0,
  center: 0.5,
  right: 1,
  top: 0,
  middle: 0.5,
  hanging: 0.2,
  alphabetic: 0.8,
  ideographic: 0.8,
  bottom: 1
};
class LO extends z_ {
  /**
   * @param {number} tolerance Tolerance.
   * @param {import("../../extent.js").Extent} maxExtent Maximum extent.
   * @param {number} resolution Resolution.
   * @param {number} pixelRatio Pixel ratio.
   */
  constructor(t, i, a, u) {
    super(t, i, a, u), this.labels_ = null, this.text_ = "", this.textOffsetX_ = 0, this.textOffsetY_ = 0, this.textRotateWithView_ = void 0, this.textKeepUpright_ = void 0, this.textRotation_ = 0, this.textFillState_ = null, this.fillStates = {}, this.fillStates[Xl] = { fillStyle: Xl }, this.textStrokeState_ = null, this.strokeStates = {}, this.textState_ = /** @type {import("../canvas.js").TextState} */
    {}, this.textStates = {}, this.textKey_ = "", this.fillKey_ = "", this.strokeKey_ = "", this.declutterMode_ = void 0, this.declutterImageWithText_ = void 0;
  }
  /**
   * @return {import("../canvas.js").SerializableInstructions} the serializable instructions.
   * @override
   */
  finish() {
    const t = super.finish();
    return t.textStates = this.textStates, t.fillStates = this.fillStates, t.strokeStates = this.strokeStates, t;
  }
  /**
   * @param {import("../../geom/SimpleGeometry.js").default|import("../Feature.js").default} geometry Geometry.
   * @param {import("../../Feature.js").FeatureLike} feature Feature.
   * @param {number} [index] Render order index.
   * @override
   */
  drawText(t, i, a) {
    const u = this.textFillState_, h = this.textStrokeState_, g = this.textState_;
    if (this.text_ === "" || !g || !u && !h)
      return;
    const y = this.coordinates;
    let _ = y.length;
    const b = t.getType();
    let E = null, x = t.getStride();
    if (g.placement === "line" && (b == "LineString" || b == "MultiLineString" || b == "Polygon" || b == "MultiPolygon")) {
      if (!qi(this.maxExtent, t.getExtent()))
        return;
      let R;
      if (E = t.getFlatCoordinates(), b == "LineString")
        R = [E.length];
      else if (b == "MultiLineString")
        R = /** @type {import("../../geom/MultiLineString.js").default} */
        t.getEnds();
      else if (b == "Polygon")
        R = /** @type {import("../../geom/Polygon.js").default} */
        t.getEnds().slice(0, 1);
      else if (b == "MultiPolygon") {
        const N = (
          /** @type {import("../../geom/MultiPolygon.js").default} */
          t.getEndss()
        );
        R = [];
        for (let O = 0, U = N.length; O < U; ++O)
          R.push(N[O][0]);
      }
      this.beginGeometry(t, i, a);
      const w = g.repeat, L = w ? void 0 : g.textAlign;
      let H = 0;
      for (let N = 0, O = R.length; N < O; ++N) {
        let U;
        w ? U = DO(
          w * this.resolution,
          E,
          H,
          R[N],
          x
        ) : U = [E.slice(H, R[N])];
        for (let I = 0, X = U.length; I < X; ++I) {
          const Z = U[I];
          let dt = 0, ft = Z.length;
          if (L == null) {
            const it = OO(
              g.maxAngle,
              Z,
              0,
              Z.length,
              2
            );
            dt = it[0], ft = it[1];
          }
          for (let it = dt; it < ft; it += x)
            y.push(Z[it], Z[it + 1]);
          const yt = y.length;
          H = R[N], this.drawChars_(_, yt), _ = yt;
        }
      }
      this.endGeometry(i);
    } else {
      let R = g.overflow ? null : [];
      switch (b) {
        case "Point":
        case "MultiPoint":
          E = /** @type {import("../../geom/MultiPoint.js").default} */
          t.getFlatCoordinates();
          break;
        case "LineString":
          E = /** @type {import("../../geom/LineString.js").default} */
          t.getFlatMidpoint();
          break;
        case "Circle":
          E = /** @type {import("../../geom/Circle.js").default} */
          t.getCenter();
          break;
        case "MultiLineString":
          E = /** @type {import("../../geom/MultiLineString.js").default} */
          t.getFlatMidpoints(), x = 2;
          break;
        case "Polygon":
          E = /** @type {import("../../geom/Polygon.js").default} */
          t.getFlatInteriorPoint(), g.overflow || R.push(E[2] / this.resolution), x = 3;
          break;
        case "MultiPolygon":
          const X = (
            /** @type {import("../../geom/MultiPolygon.js").default} */
            t.getFlatInteriorPoints()
          );
          E = [];
          for (let Z = 0, dt = X.length; Z < dt; Z += 3)
            g.overflow || R.push(X[Z + 2] / this.resolution), E.push(X[Z], X[Z + 1]);
          if (E.length === 0)
            return;
          x = 2;
          break;
      }
      const w = this.appendFlatPointCoordinates(E, x);
      if (w === _)
        return;
      if (R && (w - _) / 2 !== E.length / x) {
        let X = _ / 2;
        R = R.filter((Z, dt) => {
          const ft = y[(X + dt) * 2] === E[dt * x] && y[(X + dt) * 2 + 1] === E[dt * x + 1];
          return ft || --X, ft;
        });
      }
      this.saveTextStates_();
      const L = g.backgroundFill ? this.createFill(this.fillStyleToState(g.backgroundFill)) : null, H = g.backgroundStroke ? this.createStroke(this.strokeStyleToState(g.backgroundStroke)) : null;
      this.beginGeometry(t, i, a);
      let N = g.padding;
      if (N != Fh && (g.scale[0] < 0 || g.scale[1] < 0)) {
        let X = g.padding[0], Z = g.padding[1], dt = g.padding[2], ft = g.padding[3];
        g.scale[0] < 0 && (Z = -Z, ft = -ft), g.scale[1] < 0 && (X = -X, dt = -dt), N = [X, Z, dt, ft];
      }
      const O = this.pixelRatio;
      this.instructions.push([
        Yt.DRAW_IMAGE,
        _,
        w,
        null,
        NaN,
        NaN,
        NaN,
        1,
        0,
        0,
        this.textRotateWithView_,
        this.textRotation_,
        [1, 1],
        NaN,
        this.declutterMode_,
        this.declutterImageWithText_,
        N == Fh ? Fh : N.map(function(X) {
          return X * O;
        }),
        L,
        H,
        this.text_,
        this.textKey_,
        this.strokeKey_,
        this.fillKey_,
        this.textOffsetX_,
        this.textOffsetY_,
        R
      ]);
      const U = 1 / O, I = L ? L.slice(0) : null;
      I && (I[1] = Xl), this.hitDetectionInstructions.push([
        Yt.DRAW_IMAGE,
        _,
        w,
        null,
        NaN,
        NaN,
        NaN,
        1,
        0,
        0,
        this.textRotateWithView_,
        this.textRotation_,
        [U, U],
        NaN,
        this.declutterMode_,
        this.declutterImageWithText_,
        N,
        I,
        H,
        this.text_,
        this.textKey_,
        this.strokeKey_,
        this.fillKey_ ? Xl : this.fillKey_,
        this.textOffsetX_,
        this.textOffsetY_,
        R
      ]), this.endGeometry(i);
    }
  }
  /**
   * @private
   */
  saveTextStates_() {
    const t = this.textStrokeState_, i = this.textState_, a = this.textFillState_, u = this.strokeKey_;
    t && (u in this.strokeStates || (this.strokeStates[u] = {
      strokeStyle: t.strokeStyle,
      lineCap: t.lineCap,
      lineDashOffset: t.lineDashOffset,
      lineWidth: t.lineWidth,
      lineJoin: t.lineJoin,
      miterLimit: t.miterLimit,
      lineDash: t.lineDash
    }));
    const h = this.textKey_;
    h in this.textStates || (this.textStates[h] = {
      font: i.font,
      textAlign: i.textAlign || E_,
      justify: i.justify,
      textBaseline: i.textBaseline || W1,
      scale: i.scale
    });
    const g = this.fillKey_;
    a && (g in this.fillStates || (this.fillStates[g] = {
      fillStyle: a.fillStyle
    }));
  }
  /**
   * @private
   * @param {number} begin Begin.
   * @param {number} end End.
   */
  drawChars_(t, i) {
    const a = this.textStrokeState_, u = this.textState_, h = this.strokeKey_, g = this.textKey_, y = this.fillKey_;
    this.saveTextStates_();
    const _ = this.pixelRatio, b = $1[u.textBaseline], E = this.textOffsetY_ * _, x = this.text_, R = a ? a.lineWidth * Math.abs(u.scale[0]) / 2 : 0;
    this.instructions.push([
      Yt.DRAW_CHARS,
      t,
      i,
      b,
      u.overflow,
      y,
      u.maxAngle,
      _,
      E,
      h,
      R * _,
      x,
      g,
      1,
      this.declutterMode_,
      this.textKeepUpright_
    ]), this.hitDetectionInstructions.push([
      Yt.DRAW_CHARS,
      t,
      i,
      b,
      u.overflow,
      y && Xl,
      u.maxAngle,
      _,
      E,
      h,
      R * _,
      x,
      g,
      1 / _,
      this.declutterMode_,
      this.textKeepUpright_
    ]);
  }
  /**
   * @param {import("../../style/Text.js").default} textStyle Text style.
   * @param {Object} [sharedData] Shared data.
   * @override
   */
  setTextStyle(t, i) {
    let a, u, h;
    if (!t)
      this.text_ = "";
    else {
      const g = t.getFill();
      g ? (u = this.textFillState_, u || (u = /** @type {import("../canvas.js").FillState} */
      {}, this.textFillState_ = u), u.fillStyle = Jo(
        g.getColor() || Xl
      )) : (u = null, this.textFillState_ = u);
      const y = t.getStroke();
      if (!y)
        h = null, this.textStrokeState_ = h;
      else {
        h = this.textStrokeState_, h || (h = /** @type {import("../canvas.js").StrokeState} */
        {}, this.textStrokeState_ = h);
        const H = y.getLineDash(), N = y.getLineDashOffset(), O = y.getWidth(), U = y.getMiterLimit();
        h.lineCap = y.getLineCap() || Qm, h.lineDash = H ? H.slice() : mu, h.lineDashOffset = N === void 0 ? yu : N, h.lineJoin = y.getLineJoin() || Wm, h.lineWidth = O === void 0 ? b_ : O, h.miterLimit = U === void 0 ? v_ : U, h.strokeStyle = Jo(
          y.getColor() || S_
        );
      }
      a = this.textState_;
      const _ = t.getFont() || Z2;
      $D(_);
      const b = t.getScaleArray();
      a.overflow = t.getOverflow(), a.font = _, a.maxAngle = t.getMaxAngle(), a.placement = t.getPlacement(), a.textAlign = t.getTextAlign(), a.repeat = t.getRepeat(), a.justify = t.getJustify(), a.textBaseline = t.getTextBaseline() || W1, a.backgroundFill = t.getBackgroundFill(), a.backgroundStroke = t.getBackgroundStroke(), a.padding = t.getPadding() || Fh, a.scale = b === void 0 ? [1, 1] : b;
      const E = t.getOffsetX(), x = t.getOffsetY(), R = t.getRotateWithView(), w = t.getKeepUpright(), L = t.getRotation();
      this.text_ = t.getText() || "", this.textOffsetX_ = E === void 0 ? 0 : E, this.textOffsetY_ = x === void 0 ? 0 : x, this.textRotateWithView_ = R === void 0 ? !1 : R, this.textKeepUpright_ = w === void 0 ? !0 : w, this.textRotation_ = L === void 0 ? 0 : L, this.strokeKey_ = h ? (typeof h.strokeStyle == "string" ? h.strokeStyle : ue(h.strokeStyle)) + h.lineCap + h.lineDashOffset + "|" + h.lineWidth + h.lineJoin + h.miterLimit + "[" + h.lineDash.join() + "]" : "", this.textKey_ = a.font + a.scale + (a.textAlign || "?") + (a.repeat || "?") + (a.justify || "?") + (a.textBaseline || "?"), this.fillKey_ = u && u.fillStyle ? typeof u.fillStyle == "string" ? u.fillStyle : "|" + ue(u.fillStyle) : "";
    }
    this.declutterMode_ = t.getDeclutterMode(), this.declutterImageWithText_ = i;
  }
}
const zO = {
  Circle: UR,
  Default: z_,
  Image: MO,
  LineString: wO,
  Polygon: UR,
  Text: LO
};
class NO {
  /**
   * @param {number} tolerance Tolerance.
   * @param {import("../../extent.js").Extent} maxExtent Max extent.
   * @param {number} resolution Resolution.
   * @param {number} pixelRatio Pixel ratio.
   */
  constructor(t, i, a, u) {
    this.tolerance_ = t, this.maxExtent_ = i, this.pixelRatio_ = u, this.resolution_ = a, this.buildersByZIndex_ = {};
  }
  /**
   * @return {!Object<string, !Object<import("../canvas.js").BuilderType, import("./Builder.js").SerializableInstructions>>} The serializable instructions
   */
  finish() {
    const t = {};
    for (const i in this.buildersByZIndex_) {
      t[i] = t[i] || {};
      const a = this.buildersByZIndex_[i];
      for (const u in a) {
        const h = a[u].finish();
        t[i][u] = h;
      }
    }
    return t;
  }
  /**
   * @param {number|undefined} zIndex Z index.
   * @param {import("../canvas.js").BuilderType} builderType Replay type.
   * @return {import("../VectorContext.js").default} Replay.
   */
  getBuilder(t, i) {
    const a = t !== void 0 ? t.toString() : "0";
    let u = this.buildersByZIndex_[a];
    u === void 0 && (u = {}, this.buildersByZIndex_[a] = u);
    let h = u[i];
    if (h === void 0) {
      const g = zO[i];
      h = new g(
        this.tolerance_,
        this.maxExtent_,
        this.resolution_,
        this.pixelRatio_
      ), u[i] = h;
    }
    return h;
  }
}
function UO(r, t, i, a, u, h, g, y, _, b, E, x, R = !0) {
  let w = r[t], L = r[t + 1], H = 0, N = 0, O = 0, U = 0;
  function I() {
    H = w, N = L, t += a, w = r[t], L = r[t + 1], U += O, O = Math.sqrt((w - H) * (w - H) + (L - N) * (L - N));
  }
  do
    I();
  while (t < i - a && U + O < h);
  let X = O === 0 ? 0 : (h - U) / O;
  const Z = ya(H, w, X), dt = ya(N, L, X), ft = t - a, yt = U, it = h + y * _(b, u, E);
  for (; t < i - a && U + O < it; )
    I();
  X = O === 0 ? 0 : (it - U) / O;
  const Et = ya(H, w, X), xt = ya(N, L, X);
  let pt = !1;
  if (R)
    if (x) {
      const K = [Z, dt, Et, xt];
      fT(K, 0, 4, 2, x, K, K), pt = K[0] > K[2];
    } else
      pt = Z > Et;
  const Tt = Math.PI, Nt = [], Rt = ft + a === t;
  t = ft, O = 0, U = yt, w = r[t], L = r[t + 1];
  let Y;
  if (Rt) {
    I(), Y = Math.atan2(L - N, w - H), pt && (Y += Y > 0 ? -Tt : Tt);
    const K = (Et + Z) / 2, nt = (xt + dt) / 2;
    return Nt[0] = [K, nt, (it - h) / 2, Y, u], Nt;
  }
  u = u.replace(/\n/g, " ");
  for (let K = 0, nt = u.length; K < nt; ) {
    I();
    let vt = Math.atan2(L - N, w - H);
    if (pt && (vt += vt > 0 ? -Tt : Tt), Y !== void 0) {
      let kt = vt - Y;
      if (kt += kt > Tt ? -2 * Tt : kt < -Tt ? 2 * Tt : 0, Math.abs(kt) > g)
        return null;
    }
    Y = vt;
    const F = K;
    let St = 0;
    for (; K < nt; ++K) {
      const kt = pt ? nt - K - 1 : K, Xt = y * _(b, u[kt], E);
      if (t + a < i && U + O < h + St + Xt / 2)
        break;
      St += Xt;
    }
    if (K === F)
      continue;
    const Q = pt ? u.substring(nt - F, nt - K) : u.substring(F, K);
    X = O === 0 ? 0 : (h + St / 2 - U) / O;
    const At = ya(H, w, X), Ut = ya(N, L, X);
    Nt.push([At, Ut, St / 2, vt, Q]), h += St;
  }
  return Nt;
}
class lA {
  constructor() {
    /**
     * @private
     * @param {...*} args Args.
     * @return {ZIndexContext} This.
     */
    qC(this, "pushMethodArgs_", (...t) => (this.push_(t), this));
    this.instructions_ = [], this.zIndex = 0, this.offset_ = 0, this.context_ = /** @type {ZIndexContextProxy} */
    new Proxy(Q1(), {
      get: (t, i) => {
        if (typeof /** @type {*} */
        Q1()[i] == "function")
          return this.push_(i), this.pushMethodArgs_;
      },
      set: (t, i, a) => (this.push_(i, a), !0)
    });
  }
  /**
   * @param {...*} args Arguments to push to the instructions array.
   * @private
   */
  push_(...t) {
    const i = this.instructions_, a = this.zIndex + this.offset_;
    i[a] || (i[a] = []), i[a].push(...t);
  }
  /**
   * Push a function that renders to the context directly.
   * @param {function(CanvasRenderingContext2D): void} render Function.
   */
  pushFunction(t) {
    this.push_(t);
  }
  /**
   * Get a proxy for CanvasRenderingContext2D which does not support getting state
   * (e.g. `context.globalAlpha`, which will return `undefined`). To set state, if it relies on a
   * previous state (e.g. `context.globalAlpha = context.globalAlpha / 2`), set a function,
   * e.g. `context.globalAlpha = (context) => context.globalAlpha / 2`.
   * @return {ZIndexContextProxy} Context.
   */
  getContext() {
    return this.context_;
  }
  /**
   * @param {CanvasRenderingContext2D} context Context.
   */
  draw(t) {
    this.instructions_.forEach((i) => {
      for (let a = 0, u = i.length; a < u; ++a) {
        const h = i[a];
        if (typeof h == "function") {
          h(t);
          continue;
        }
        const g = i[++a];
        if (typeof /** @type {*} */
        t[h] == "function")
          t[h](...g);
        else {
          if (typeof g == "function") {
            t[h] = g(t);
            continue;
          }
          t[h] = g;
        }
      }
    });
  }
  clear() {
    this.instructions_.length = 0, this.zIndex = 0, this.offset_ = 0;
  }
  /**
   * Offsets the zIndex by the highest current zIndex. Useful for rendering multiple worlds or tiles, to
   * avoid conflicting context.clip() or context.save()/restore() calls.
   */
  offset() {
    this.offset_ = this.instructions_.length, this.zIndex = 0;
  }
}
const Mm = _a(), Yc = [], su = [], ou = [], jc = [];
function GR(r) {
  return r[3].declutterBox;
}
const HR = new RegExp(
  /* eslint-disable prettier/prettier */
  "[֑-ࣿיִ-﷿ﹰ-ﻼࠀ-࿿-]"
  /* eslint-enable prettier/prettier */
);
function cb(r, t) {
  return t === "start" ? t = HR.test(r) ? "right" : "left" : t === "end" && (t = HR.test(r) ? "left" : "right"), $1[t];
}
function GO(r, t, i) {
  return i > 0 && r.push(`
`, ""), r.push(t, ""), r;
}
function HO(r, t, i) {
  return i % 2 === 0 && (r += t), r;
}
class YO {
  /**
   * @param {number} resolution Resolution.
   * @param {number} pixelRatio Pixel ratio.
   * @param {boolean} overlaps The replay can have overlapping geometries.
   * @param {import("../canvas.js").SerializableInstructions} instructions The serializable instructions.
   * @param {boolean} [deferredRendering] Enable deferred rendering.
   */
  constructor(t, i, a, u, h) {
    this.overlaps = a, this.pixelRatio = i, this.resolution = t, this.alignAndScaleFill_, this.instructions = u.instructions, this.coordinates = u.coordinates, this.coordinateCache_ = {}, this.renderedTransform_ = $s(), this.hitDetectionInstructions = u.hitDetectionInstructions, this.pixelCoordinates_ = null, this.viewRotation_ = 0, this.fillStates = u.fillStates || {}, this.strokeStates = u.strokeStates || {}, this.textStates = u.textStates || {}, this.widths_ = {}, this.labels_ = {}, this.zIndexContext_ = h ? new lA() : null;
  }
  /**
   * @return {ZIndexContext} ZIndex context.
   */
  getZIndexContext() {
    return this.zIndexContext_;
  }
  /**
   * @param {string|Array<string>} text Text.
   * @param {string} textKey Text style key.
   * @param {string} fillKey Fill style key.
   * @param {string} strokeKey Stroke style key.
   * @return {import("../canvas.js").Label} Label.
   */
  createLabel(t, i, a, u) {
    const h = t + i + a + u;
    if (this.labels_[h])
      return this.labels_[h];
    const g = u ? this.strokeStates[u] : null, y = a ? this.fillStates[a] : null, _ = this.textStates[i], b = this.pixelRatio, E = [
      _.scale[0] * b,
      _.scale[1] * b
    ], x = _.justify ? $1[_.justify] : cb(
      Array.isArray(t) ? t[0] : t,
      _.textAlign || E_
    ), R = u && g.lineWidth ? g.lineWidth : 0, w = Array.isArray(t) ? t : String(t).split(`
`).reduce(GO, []), { width: L, height: H, widths: N, heights: O, lineWidths: U } = eO(
      _,
      w
    ), I = L + R, X = [], Z = (I + 2) * E[0], dt = (H + R) * E[1], ft = {
      width: Z < 0 ? Math.floor(Z) : Math.ceil(Z),
      height: dt < 0 ? Math.floor(dt) : Math.ceil(dt),
      contextInstructions: X
    };
    (E[0] != 1 || E[1] != 1) && X.push("scale", E), u && (X.push("strokeStyle", g.strokeStyle), X.push("lineWidth", R), X.push("lineCap", g.lineCap), X.push("lineJoin", g.lineJoin), X.push("miterLimit", g.miterLimit), X.push("setLineDash", [g.lineDash]), X.push("lineDashOffset", g.lineDashOffset)), a && X.push("fillStyle", y.fillStyle), X.push("textBaseline", "middle"), X.push("textAlign", "center");
    const yt = 0.5 - x;
    let it = x * I + yt * R;
    const Et = [], xt = [];
    let pt = 0, Tt = 0, Nt = 0, Rt = 0, Y;
    for (let K = 0, nt = w.length; K < nt; K += 2) {
      const vt = w[K];
      if (vt === `
`) {
        Tt += pt, pt = 0, it = x * I + yt * R, ++Rt;
        continue;
      }
      const F = w[K + 1] || _.font;
      F !== Y && (u && Et.push("font", F), a && xt.push("font", F), Y = F), pt = Math.max(pt, O[Nt]);
      const St = [
        vt,
        it + yt * N[Nt] + x * (N[Nt] - U[Rt]),
        0.5 * (R + pt) + Tt
      ];
      it += N[Nt], u && Et.push("strokeText", St), a && xt.push("fillText", St), ++Nt;
    }
    return Array.prototype.push.apply(X, Et), Array.prototype.push.apply(X, xt), this.labels_[h] = ft, ft;
  }
  /**
   * @param {CanvasRenderingContext2D} context Context.
   * @param {import("../../coordinate.js").Coordinate} p1 1st point of the background box.
   * @param {import("../../coordinate.js").Coordinate} p2 2nd point of the background box.
   * @param {import("../../coordinate.js").Coordinate} p3 3rd point of the background box.
   * @param {import("../../coordinate.js").Coordinate} p4 4th point of the background box.
   * @param {Array<*>} fillInstruction Fill instruction.
   * @param {Array<*>} strokeInstruction Stroke instruction.
   */
  replayTextBackground_(t, i, a, u, h, g, y) {
    t.beginPath(), t.moveTo.apply(t, i), t.lineTo.apply(t, a), t.lineTo.apply(t, u), t.lineTo.apply(t, h), t.lineTo.apply(t, i), g && (this.alignAndScaleFill_ = /** @type {number} */
    g[2], t.fillStyle = /** @type {string} */
    g[1], this.fill_(t)), y && (this.setStrokeStyle_(
      t,
      /** @type {Array<*>} */
      y
    ), t.stroke());
  }
  /**
   * @private
   * @param {number} sheetWidth Width of the sprite sheet.
   * @param {number} sheetHeight Height of the sprite sheet.
   * @param {number} centerX X.
   * @param {number} centerY Y.
   * @param {number} width Width.
   * @param {number} height Height.
   * @param {number} anchorX Anchor X.
   * @param {number} anchorY Anchor Y.
   * @param {number} originX Origin X.
   * @param {number} originY Origin Y.
   * @param {number} rotation Rotation.
   * @param {import("../../size.js").Size} scale Scale.
   * @param {boolean} snapToPixel Snap to pixel.
   * @param {Array<number>} padding Padding.
   * @param {boolean} fillStroke Background fill or stroke.
   * @param {import("../../Feature.js").FeatureLike} feature Feature.
   * @return {ImageOrLabelDimensions} Dimensions for positioning and decluttering the image or label.
   */
  calculateImageOrLabelDimensions_(t, i, a, u, h, g, y, _, b, E, x, R, w, L, H, N) {
    y *= R[0], _ *= R[1];
    let O = a - y, U = u - _;
    const I = h + b > t ? t - b : h, X = g + E > i ? i - E : g, Z = L[3] + I * R[0] + L[1], dt = L[0] + X * R[1] + L[2], ft = O - L[3], yt = U - L[0];
    (H || x !== 0) && (Yc[0] = ft, jc[0] = ft, Yc[1] = yt, su[1] = yt, su[0] = ft + Z, ou[0] = su[0], ou[1] = yt + dt, jc[1] = ou[1]);
    let it;
    return x !== 0 ? (it = vu(
      $s(),
      a,
      u,
      1,
      1,
      x,
      -a,
      -u
    ), hi(it, Yc), hi(it, su), hi(it, ou), hi(it, jc), $o(
      Math.min(Yc[0], su[0], ou[0], jc[0]),
      Math.min(Yc[1], su[1], ou[1], jc[1]),
      Math.max(Yc[0], su[0], ou[0], jc[0]),
      Math.max(Yc[1], su[1], ou[1], jc[1]),
      Mm
    )) : $o(
      Math.min(ft, ft + Z),
      Math.min(yt, yt + dt),
      Math.max(ft, ft + Z),
      Math.max(yt, yt + dt),
      Mm
    ), w && (O = Math.round(O), U = Math.round(U)), {
      drawImageX: O,
      drawImageY: U,
      drawImageW: I,
      drawImageH: X,
      originX: b,
      originY: E,
      declutterBox: {
        minX: Mm[0],
        minY: Mm[1],
        maxX: Mm[2],
        maxY: Mm[3],
        value: N
      },
      canvasTransform: it,
      scale: R
    };
  }
  /**
   * @private
   * @param {CanvasRenderingContext2D} context Context.
   * @param {import('../../size.js').Size} scaledCanvasSize Scaled canvas size.
   * @param {import("../canvas.js").Label|HTMLImageElement|HTMLCanvasElement|HTMLVideoElement} imageOrLabel Image.
   * @param {ImageOrLabelDimensions} dimensions Dimensions.
   * @param {number} opacity Opacity.
   * @param {Array<*>} fillInstruction Fill instruction.
   * @param {Array<*>} strokeInstruction Stroke instruction.
   * @return {boolean} The image or label was rendered.
   */
  replayImageOrLabel_(t, i, a, u, h, g, y) {
    const _ = !!(g || y), b = u.declutterBox, E = y ? y[2] * u.scale[0] / 2 : 0;
    return b.minX - E <= i[0] && b.maxX + E >= 0 && b.minY - E <= i[1] && b.maxY + E >= 0 && (_ && this.replayTextBackground_(
      t,
      Yc,
      su,
      ou,
      jc,
      /** @type {Array<*>} */
      g,
      /** @type {Array<*>} */
      y
    ), nO(
      t,
      u.canvasTransform,
      h,
      a,
      u.originX,
      u.originY,
      u.drawImageW,
      u.drawImageH,
      u.drawImageX,
      u.drawImageY,
      u.scale
    )), !0;
  }
  /**
   * @private
   * @param {CanvasRenderingContext2D} context Context.
   */
  fill_(t) {
    const i = this.alignAndScaleFill_;
    if (i) {
      const a = hi(this.renderedTransform_, [0, 0]), u = 512 * this.pixelRatio;
      t.save(), t.translate(a[0] % u, a[1] % u), i !== 1 && t.scale(i, i), t.rotate(this.viewRotation_);
    }
    t.fill(), i && t.restore();
  }
  /**
   * @private
   * @param {CanvasRenderingContext2D} context Context.
   * @param {Array<*>} instruction Instruction.
   */
  setStrokeStyle_(t, i) {
    t.strokeStyle = /** @type {import("../../colorlike.js").ColorLike} */
    i[1], i[1] && (t.lineWidth = /** @type {number} */
    i[2], t.lineCap = /** @type {CanvasLineCap} */
    i[3], t.lineJoin = /** @type {CanvasLineJoin} */
    i[4], t.miterLimit = /** @type {number} */
    i[5], t.lineDashOffset = /** @type {number} */
    i[7], t.setLineDash(
      /** @type {Array<number>} */
      i[6]
    ));
  }
  /**
   * @private
   * @param {string|Array<string>} text The text to draw.
   * @param {string} textKey The key of the text state.
   * @param {string} strokeKey The key for the stroke state.
   * @param {string} fillKey The key for the fill state.
   * @return {{label: import("../canvas.js").Label, anchorX: number, anchorY: number}} The text image and its anchor.
   */
  drawLabelWithPointPlacement_(t, i, a, u) {
    const h = this.textStates[i], g = this.createLabel(t, i, u, a), y = this.strokeStates[a], _ = this.pixelRatio, b = cb(
      Array.isArray(t) ? t[0] : t,
      h.textAlign || E_
    ), E = $1[h.textBaseline || W1], x = y && y.lineWidth ? y.lineWidth : 0, R = g.width / _ - 2 * h.scale[0], w = b * R + 2 * (0.5 - b) * x, L = E * g.height / _ + 2 * (0.5 - E) * x;
    return {
      label: g,
      anchorX: w,
      anchorY: L
    };
  }
  /**
   * @private
   * @param {CanvasRenderingContext2D} context Context.
   * @param {import('../../size.js').Size} scaledCanvasSize Scaled canvas size
   * @param {import("../../transform.js").Transform} transform Transform.
   * @param {Array<*>} instructions Instructions array.
   * @param {boolean} snapToPixel Snap point symbols and text to integer pixels.
   * @param {FeatureCallback<T>} [featureCallback] Feature callback.
   * @param {import("../../extent.js").Extent} [hitExtent] Only check
   *     features that intersect this extent.
   * @param {import("rbush").default<DeclutterEntry>} [declutterTree] Declutter tree.
   * @return {T|undefined} Callback result.
   * @template T
   */
  execute_(t, i, a, u, h, g, y, _) {
    const b = this.zIndexContext_;
    let E;
    this.pixelCoordinates_ && Wc(a, this.renderedTransform_) ? E = this.pixelCoordinates_ : (this.pixelCoordinates_ || (this.pixelCoordinates_ = []), E = Kc(
      this.coordinates,
      0,
      this.coordinates.length,
      2,
      a,
      this.pixelCoordinates_
    ), aD(this.renderedTransform_, a));
    let x = 0;
    const R = u.length;
    let w = 0, L, H, N, O, U, I, X, Z, dt, ft, yt, it, Et, xt = 0, pt = 0;
    const Tt = this.coordinateCache_, Nt = this.viewRotation_, Rt = Math.round(Math.atan2(-a[1], a[0]) * 1e12) / 1e12, Y = (
      /** @type {import("../../render.js").State} */
      {
        context: t,
        pixelRatio: this.pixelRatio,
        resolution: this.resolution,
        rotation: Nt
      }
    ), K = this.instructions != u || this.overlaps ? 0 : 200;
    let nt, vt, F, St;
    for (; x < R; ) {
      const Q = u[x];
      switch (
        /** @type {import("./Instruction.js").default} */
        Q[0]
      ) {
        case Yt.BEGIN_GEOMETRY:
          nt = /** @type {import("../../Feature.js").FeatureLike} */
          Q[1], St = Q[3], nt.getGeometry() ? y !== void 0 && !qi(y, St.getExtent()) ? x = /** @type {number} */
          Q[2] + 1 : ++x : x = /** @type {number} */
          Q[2], b && (b.zIndex = Q[4]);
          break;
        case Yt.BEGIN_PATH:
          xt > K && (this.fill_(t), xt = 0), pt > K && (t.stroke(), pt = 0), !xt && !pt && (t.beginPath(), U = NaN, I = NaN), ++x;
          break;
        case Yt.CIRCLE:
          w = /** @type {number} */
          Q[1];
          const Ut = E[w], kt = E[w + 1], Xt = E[w + 2], Ze = E[w + 3], _e = Xt - Ut, De = Ze - kt, ke = Math.sqrt(_e * _e + De * De);
          t.moveTo(Ut + ke, kt), t.arc(Ut, kt, ke, 0, 2 * Math.PI, !0), ++x;
          break;
        case Yt.CLOSE_PATH:
          t.closePath(), ++x;
          break;
        case Yt.CUSTOM:
          w = /** @type {number} */
          Q[1], L = Q[2];
          const yl = (
            /** @type {import("../../geom/SimpleGeometry.js").default} */
            Q[3]
          ), no = Q[4], Fi = Q[5];
          Y.geometry = yl, Y.feature = nt, x in Tt || (Tt[x] = []);
          const Kl = Tt[x];
          Fi ? Fi(E, w, L, 2, Kl) : (Kl[0] = E[w], Kl[1] = E[w + 1], Kl.length = 2), b && (b.zIndex = Q[6]), no(Kl, Y), ++x;
          break;
        case Yt.DRAW_IMAGE:
          w = /** @type {number} */
          Q[1], L = /** @type {number} */
          Q[2], dt = /** @type {HTMLCanvasElement|HTMLVideoElement|HTMLImageElement} */
          Q[3], H = /** @type {number} */
          Q[4], N = /** @type {number} */
          Q[5];
          let xi = (
            /** @type {number} */
            Q[6]
          );
          const Dn = (
            /** @type {number} */
            Q[7]
          ), yn = (
            /** @type {number} */
            Q[8]
          ), pl = (
            /** @type {number} */
            Q[9]
          ), io = (
            /** @type {boolean} */
            Q[10]
          );
          let Pl = (
            /** @type {number} */
            Q[11]
          );
          const _l = (
            /** @type {import("../../size.js").Size} */
            Q[12]
          );
          let Sa = (
            /** @type {number} */
            Q[13]
          );
          O = Q[14] || "declutter";
          const Fe = (
            /** @type {{args: import("../canvas.js").DeclutterImageWithText, declutterMode: import('../../style/Style.js').DeclutterMode}} */
            Q[15]
          );
          if (!dt && Q.length >= 20) {
            ft = /** @type {string} */
            Q[19], yt = /** @type {string} */
            Q[20], it = /** @type {string} */
            Q[21], Et = /** @type {string} */
            Q[22];
            const On = this.drawLabelWithPointPlacement_(
              ft,
              yt,
              it,
              Et
            );
            dt = On.label, Q[3] = dt;
            const bn = (
              /** @type {number} */
              Q[23]
            );
            H = (On.anchorX - bn) * this.pixelRatio, Q[4] = H;
            const In = (
              /** @type {number} */
              Q[24]
            );
            N = (On.anchorY - In) * this.pixelRatio, Q[5] = N, xi = dt.height, Q[6] = xi, Sa = dt.width, Q[13] = Sa;
          }
          let tf;
          Q.length > 25 && (tf = /** @type {number} */
          Q[25]);
          let Eu, Ql, pn;
          Q.length > 17 ? (Eu = /** @type {Array<number>} */
          Q[16], Ql = /** @type {Array<*>} */
          Q[17], pn = /** @type {Array<*>} */
          Q[18]) : (Eu = Fh, Ql = null, pn = null), io && Rt ? Pl += Nt : !io && !Rt && (Pl -= Nt);
          let Wl = 0;
          for (; w < L; w += 2) {
            if (tf && tf[Wl++] < Sa / this.pixelRatio)
              continue;
            const On = this.calculateImageOrLabelDimensions_(
              dt.width,
              dt.height,
              E[w],
              E[w + 1],
              Sa,
              xi,
              H,
              N,
              yn,
              pl,
              Pl,
              _l,
              h,
              Eu,
              !!Ql || !!pn,
              nt
            ), bn = [
              t,
              i,
              dt,
              On,
              Dn,
              Ql,
              pn
            ];
            if (_) {
              let In, Wi, Ri;
              if (Fe) {
                const se = L - w;
                if (!Fe[se]) {
                  Fe[se] = { args: bn, declutterMode: O };
                  continue;
                }
                const an = Fe[se];
                In = an.args, Wi = an.declutterMode, delete Fe[se], Ri = GR(In);
              }
              let Oe, Ue;
              if (In && (Wi !== "declutter" || !_.collides(Ri)) && (Oe = !0), (O !== "declutter" || !_.collides(On.declutterBox)) && (Ue = !0), Wi === "declutter" && O === "declutter") {
                const se = Oe && Ue;
                Oe = se, Ue = se;
              }
              Oe && (Wi !== "none" && _.insert(Ri), this.replayImageOrLabel_.apply(this, In)), Ue && (O !== "none" && _.insert(On.declutterBox), this.replayImageOrLabel_.apply(this, bn));
            } else
              this.replayImageOrLabel_.apply(this, bn);
          }
          ++x;
          break;
        case Yt.DRAW_CHARS:
          const Bn = (
            /** @type {number} */
            Q[1]
          ), bu = (
            /** @type {number} */
            Q[2]
          ), Tu = (
            /** @type {number} */
            Q[3]
          ), iy = (
            /** @type {number} */
            Q[4]
          );
          Et = /** @type {string} */
          Q[5];
          const lo = (
            /** @type {number} */
            Q[6]
          ), vl = (
            /** @type {number} */
            Q[7]
          ), ni = (
            /** @type {number} */
            Q[8]
          );
          it = /** @type {string} */
          Q[9];
          const Sl = (
            /** @type {number} */
            Q[10]
          );
          ft = /** @type {string|Array<string>} */
          Q[11], Array.isArray(ft) && (ft = ft.reduce(HO, "")), yt = /** @type {string} */
          Q[12];
          const Ki = [
            /** @type {number} */
            Q[13],
            /** @type {number} */
            Q[13]
          ];
          O = Q[14] || "declutter";
          const _n = (
            /** @type {boolean} */
            Q[15]
          ), El = this.textStates[yt], Pi = El.font, Ci = [
            El.scale[0] * vl,
            El.scale[1] * vl
          ];
          let Qi;
          Pi in this.widths_ ? Qi = this.widths_[Pi] : (Qi = {}, this.widths_[Pi] = Qi);
          const ef = ET(E, Bn, bu, 2), ao = Math.abs(Ci[0]) * CR(Pi, ft, Qi);
          if (iy || ao <= ef) {
            const On = this.textStates[yt].textAlign, bn = (ef - ao) * cb(ft, On), In = UO(
              E,
              Bn,
              bu,
              2,
              ft,
              bn,
              lo,
              Math.abs(Ci[0]),
              CR,
              Pi,
              Qi,
              Rt ? 0 : this.viewRotation_,
              _n
            );
            t: if (In) {
              const Wi = [];
              let Ri, Oe, Ue, se, an;
              if (it)
                for (Ri = 0, Oe = In.length; Ri < Oe; ++Ri) {
                  an = In[Ri], Ue = /** @type {string} */
                  an[4], se = this.createLabel(Ue, yt, "", it), H = /** @type {number} */
                  an[2] + (Ci[0] < 0 ? -Sl : Sl), N = Tu * se.height + (0.5 - Tu) * 2 * Sl * Ci[1] / Ci[0] - ni;
                  const Ai = this.calculateImageOrLabelDimensions_(
                    se.width,
                    se.height,
                    an[0],
                    an[1],
                    se.width,
                    se.height,
                    H,
                    N,
                    0,
                    0,
                    an[3],
                    Ki,
                    !1,
                    Fh,
                    !1,
                    nt
                  );
                  if (_ && O === "declutter" && _.collides(Ai.declutterBox))
                    break t;
                  Wi.push([
                    t,
                    i,
                    se,
                    Ai,
                    1,
                    null,
                    null
                  ]);
                }
              if (Et)
                for (Ri = 0, Oe = In.length; Ri < Oe; ++Ri) {
                  an = In[Ri], Ue = /** @type {string} */
                  an[4], se = this.createLabel(Ue, yt, Et, ""), H = /** @type {number} */
                  an[2], N = Tu * se.height - ni;
                  const Ai = this.calculateImageOrLabelDimensions_(
                    se.width,
                    se.height,
                    an[0],
                    an[1],
                    se.width,
                    se.height,
                    H,
                    N,
                    0,
                    0,
                    an[3],
                    Ki,
                    !1,
                    Fh,
                    !1,
                    nt
                  );
                  if (_ && O === "declutter" && _.collides(Ai.declutterBox))
                    break t;
                  Wi.push([
                    t,
                    i,
                    se,
                    Ai,
                    1,
                    null,
                    null
                  ]);
                }
              _ && O !== "none" && _.load(Wi.map(GR));
              for (let Ai = 0, ad = Wi.length; Ai < ad; ++Ai)
                this.replayImageOrLabel_.apply(this, Wi[Ai]);
            }
          }
          ++x;
          break;
        case Yt.END_GEOMETRY:
          if (g !== void 0) {
            nt = /** @type {import("../../Feature.js").FeatureLike} */
            Q[1];
            const On = g(
              nt,
              St,
              O
            );
            if (On)
              return On;
          }
          ++x;
          break;
        case Yt.FILL:
          K ? xt++ : this.fill_(t), ++x;
          break;
        case Yt.MOVE_TO_LINE_TO:
          for (w = /** @type {number} */
          Q[1], L = /** @type {number} */
          Q[2], vt = E[w], F = E[w + 1], t.moveTo(vt, F), U = vt + 0.5 | 0, I = F + 0.5 | 0, w += 2; w < L; w += 2)
            vt = E[w], F = E[w + 1], X = vt + 0.5 | 0, Z = F + 0.5 | 0, (w == L - 2 || X !== U || Z !== I) && (t.lineTo(vt, F), U = X, I = Z);
          ++x;
          break;
        case Yt.SET_FILL_STYLE:
          this.alignAndScaleFill_ = Q[2], xt && (this.fill_(t), xt = 0, pt && (t.stroke(), pt = 0)), t.fillStyle = Q[1], ++x;
          break;
        case Yt.SET_STROKE_STYLE:
          pt && (t.stroke(), pt = 0), this.setStrokeStyle_(
            t,
            /** @type {Array<*>} */
            Q
          ), ++x;
          break;
        case Yt.STROKE:
          K ? pt++ : t.stroke(), ++x;
          break;
        default:
          ++x;
          break;
      }
    }
    xt && this.fill_(t), pt && t.stroke();
  }
  /**
   * @param {CanvasRenderingContext2D} context Context.
   * @param {import('../../size.js').Size} scaledCanvasSize Scaled canvas size.
   * @param {import("../../transform.js").Transform} transform Transform.
   * @param {number} viewRotation View rotation.
   * @param {boolean} snapToPixel Snap point symbols and text to integer pixels.
   * @param {import("rbush").default<DeclutterEntry>} [declutterTree] Declutter tree.
   */
  execute(t, i, a, u, h, g) {
    this.viewRotation_ = u, this.execute_(
      t,
      i,
      a,
      this.instructions,
      h,
      void 0,
      void 0,
      g
    );
  }
  /**
   * @param {CanvasRenderingContext2D} context Context.
   * @param {import("../../transform.js").Transform} transform Transform.
   * @param {number} viewRotation View rotation.
   * @param {FeatureCallback<T>} [featureCallback] Feature callback.
   * @param {import("../../extent.js").Extent} [hitExtent] Only check
   *     features that intersect this extent.
   * @return {T|undefined} Callback result.
   * @template T
   */
  executeHitDetection(t, i, a, u, h) {
    return this.viewRotation_ = a, this.execute_(
      t,
      [t.canvas.width, t.canvas.height],
      i,
      this.hitDetectionInstructions,
      !0,
      u,
      h
    );
  }
}
const kh = [
  "Polygon",
  "Circle",
  "LineString",
  "Image",
  "Text",
  "Default"
], aA = ["Image", "Text"], jO = kh.filter(
  (r) => !aA.includes(r)
);
class BO {
  /**
   * @param {import("../../extent.js").Extent} maxExtent Max extent for clipping. When a
   * `maxExtent` was set on the Builder for this executor group, the same `maxExtent`
   * should be set here, unless the target context does not exceed that extent (which
   * can be the case when rendering to tiles).
   * @param {number} resolution Resolution.
   * @param {number} pixelRatio Pixel ratio.
   * @param {boolean} overlaps The executor group can have overlapping geometries.
   * @param {!Object<string, !Object<import("../canvas.js").BuilderType, import("../canvas.js").SerializableInstructions>>} allInstructions
   * The serializable instructions.
   * @param {number} [renderBuffer] Optional rendering buffer.
   * @param {boolean} [deferredRendering] Enable deferred rendering with renderDeferred().
   */
  constructor(t, i, a, u, h, g, y) {
    this.maxExtent_ = t, this.overlaps_ = u, this.pixelRatio_ = a, this.resolution_ = i, this.renderBuffer_ = g, this.executorsByZIndex_ = {}, this.hitDetectionContext_ = null, this.hitDetectionTransform_ = $s(), this.renderedContext_ = null, this.deferredZIndexContexts_ = {}, this.createExecutors_(h, y);
  }
  /**
   * @param {CanvasRenderingContext2D} context Context.
   * @param {import("../../transform.js").Transform} transform Transform.
   */
  clip(t, i) {
    const a = this.getClipCoords(i);
    t.beginPath(), t.moveTo(a[0], a[1]), t.lineTo(a[2], a[3]), t.lineTo(a[4], a[5]), t.lineTo(a[6], a[7]), t.clip();
  }
  /**
   * Create executors and populate them using the provided instructions.
   * @private
   * @param {!Object<string, !Object<string, import("../canvas.js").SerializableInstructions>>} allInstructions The serializable instructions
   * @param {boolean} deferredRendering Enable deferred rendering.
   */
  createExecutors_(t, i) {
    for (const a in t) {
      let u = this.executorsByZIndex_[a];
      u === void 0 && (u = {}, this.executorsByZIndex_[a] = u);
      const h = t[a];
      for (const g in h) {
        const y = h[g];
        u[g] = new YO(
          this.resolution_,
          this.pixelRatio_,
          this.overlaps_,
          y,
          i
        );
      }
    }
  }
  /**
   * @param {Array<import("../canvas.js").BuilderType>} executors Executors.
   * @return {boolean} Has executors of the provided types.
   */
  hasExecutors(t) {
    for (const i in this.executorsByZIndex_) {
      const a = this.executorsByZIndex_[i];
      for (let u = 0, h = t.length; u < h; ++u)
        if (t[u] in a)
          return !0;
    }
    return !1;
  }
  /**
   * @param {import("../../coordinate.js").Coordinate} coordinate Coordinate.
   * @param {number} resolution Resolution.
   * @param {number} rotation Rotation.
   * @param {number} hitTolerance Hit tolerance in pixels.
   * @param {function(import("../../Feature.js").FeatureLike, import("../../geom/SimpleGeometry.js").default, number): T} callback Feature callback.
   * @param {Array<import("../../Feature.js").FeatureLike>} declutteredFeatures Decluttered features.
   * @return {T|undefined} Callback result.
   * @template T
   */
  forEachFeatureAtCoordinate(t, i, a, u, h, g) {
    u = Math.round(u);
    const y = u * 2 + 1, _ = vu(
      this.hitDetectionTransform_,
      u + 0.5,
      u + 0.5,
      1 / i,
      -1 / i,
      -a,
      -t[0],
      -t[1]
    ), b = !this.hitDetectionContext_;
    b && (this.hitDetectionContext_ = ei(
      y,
      y
    ));
    const E = this.hitDetectionContext_;
    E.canvas.width !== y || E.canvas.height !== y ? (E.canvas.width = y, E.canvas.height = y) : b || E.clearRect(0, 0, y, y);
    let x;
    this.renderBuffer_ !== void 0 && (x = _a(), r_(x, t), rS(
      x,
      i * (this.renderBuffer_ + u),
      x
    ));
    const R = IO(u);
    let w;
    function L(Z, dt, ft) {
      const yt = E.getImageData(
        0,
        0,
        y,
        y
      ).data;
      for (let it = 0, Et = R.length; it < Et; it++)
        if (yt[R[it]] > 0) {
          if (!g || ft === "none" || w !== "Image" && w !== "Text" || g.includes(Z)) {
            const xt = (R[it] - 3) / 4, pt = u - xt % y, Tt = u - (xt / y | 0), Nt = h(Z, dt, pt * pt + Tt * Tt);
            if (Nt)
              return Nt;
          }
          E.clearRect(0, 0, y, y);
          break;
        }
    }
    const H = Object.keys(this.executorsByZIndex_).map(Number);
    H.sort(hu);
    let N, O, U, I, X;
    for (N = H.length - 1; N >= 0; --N) {
      const Z = H[N].toString();
      for (U = this.executorsByZIndex_[Z], O = kh.length - 1; O >= 0; --O)
        if (w = kh[O], I = U[w], I !== void 0 && (X = I.executeHitDetection(
          E,
          _,
          a,
          L,
          x
        ), X))
          return X;
    }
  }
  /**
   * @param {import("../../transform.js").Transform} transform Transform.
   * @return {Array<number>|null} Clip coordinates.
   */
  getClipCoords(t) {
    const i = this.maxExtent_;
    if (!i)
      return null;
    const a = i[0], u = i[1], h = i[2], g = i[3], y = [a, u, a, g, h, g, h, u];
    return Kc(y, 0, 8, 2, t, y), y;
  }
  /**
   * @return {boolean} Is empty.
   */
  isEmpty() {
    return Zm(this.executorsByZIndex_);
  }
  /**
   * @param {CanvasRenderingContext2D} targetContext Context.
   * @param {import('../../size.js').Size} scaledCanvasSize Scale of the context.
   * @param {import("../../transform.js").Transform} transform Transform.
   * @param {number} viewRotation View rotation.
   * @param {boolean} snapToPixel Snap point symbols and test to integer pixel.
   * @param {Array<import("../canvas.js").BuilderType>} [builderTypes] Ordered replay types to replay.
   *     Default is {@link module:ol/render/replay~ALL}
   * @param {import("rbush").default<import('./Executor.js').DeclutterEntry>|null} [declutterTree] Declutter tree.
   *     When set to null, no decluttering is done, even when the executor group has a `ZIndexContext`.
   */
  execute(t, i, a, u, h, g, y) {
    const _ = Object.keys(this.executorsByZIndex_).map(Number);
    _.sort(y ? sw : hu), g = g || kh;
    const b = kh.length;
    for (let E = 0, x = _.length; E < x; ++E) {
      const R = _[E].toString(), w = this.executorsByZIndex_[R];
      for (let L = 0, H = g.length; L < H; ++L) {
        const N = g[L], O = w[N];
        if (O !== void 0) {
          const U = y === null ? void 0 : O.getZIndexContext(), I = U ? U.getContext() : t, X = this.maxExtent_ && N !== "Image" && N !== "Text";
          if (X && (I.save(), this.clip(I, a)), !U || N === "Text" || N === "Image" ? O.execute(
            I,
            i,
            a,
            u,
            h,
            y
          ) : U.pushFunction(
            (Z) => O.execute(
              Z,
              i,
              a,
              u,
              h,
              y
            )
          ), X && I.restore(), U) {
            U.offset();
            const Z = _[E] * b + kh.indexOf(N);
            this.deferredZIndexContexts_[Z] || (this.deferredZIndexContexts_[Z] = []), this.deferredZIndexContexts_[Z].push(U);
          }
        }
      }
    }
    this.renderedContext_ = t;
  }
  getDeferredZIndexContexts() {
    return this.deferredZIndexContexts_;
  }
  getRenderedContext() {
    return this.renderedContext_;
  }
  renderDeferred() {
    const t = this.deferredZIndexContexts_, i = Object.keys(t).map(Number).sort(hu);
    for (let a = 0, u = i.length; a < u; ++a)
      t[i[a]].forEach((h) => {
        h.draw(this.renderedContext_), h.clear();
      }), t[i[a]].length = 0;
  }
}
const fb = {};
function IO(r) {
  if (fb[r] !== void 0)
    return fb[r];
  const t = r * 2 + 1, i = r * r, a = new Array(i + 1);
  for (let h = 0; h <= r; ++h)
    for (let g = 0; g <= r; ++g) {
      const y = h * h + g * g;
      if (y > i)
        break;
      let _ = a[y];
      _ || (_ = [], a[y] = _), _.push(((r + h) * t + (r + g)) * 4 + 3), h > 0 && _.push(((r - h) * t + (r + g)) * 4 + 3), g > 0 && (_.push(((r + h) * t + (r - g)) * 4 + 3), h > 0 && _.push(((r - h) * t + (r - g)) * 4 + 3));
    }
  const u = [];
  for (let h = 0, g = a.length; h < g; ++h)
    a[h] && u.push(...a[h]);
  return fb[r] = u, u;
}
function YR(r, t, i, a) {
  return i !== void 0 && a !== void 0 ? [i / r, a / t] : i !== void 0 ? i / r : a !== void 0 ? a / t : 1;
}
class xS extends bS {
  /**
   * @param {Options} [options] Options.
   */
  constructor(t) {
    t = t || {};
    const i = t.opacity !== void 0 ? t.opacity : 1, a = t.rotation !== void 0 ? t.rotation : 0, u = t.scale !== void 0 ? t.scale : 1, h = t.rotateWithView !== void 0 ? t.rotateWithView : !1;
    super({
      opacity: i,
      rotation: a,
      scale: u,
      displacement: t.displacement !== void 0 ? t.displacement : [0, 0],
      rotateWithView: h,
      declutterMode: t.declutterMode
    }), this.anchor_ = t.anchor !== void 0 ? t.anchor : [0.5, 0.5], this.normalizedAnchor_ = null, this.anchorOrigin_ = t.anchorOrigin !== void 0 ? t.anchorOrigin : "top-left", this.anchorXUnits_ = t.anchorXUnits !== void 0 ? t.anchorXUnits : "fraction", this.anchorYUnits_ = t.anchorYUnits !== void 0 ? t.anchorYUnits : "fraction", this.crossOrigin_ = t.crossOrigin !== void 0 ? t.crossOrigin : null;
    const g = t.img !== void 0 ? t.img : null;
    let y = t.src;
    je(
      !(y !== void 0 && g),
      "`image` and `src` cannot be provided at the same time"
    ), (y === void 0 || y.length === 0) && g && (y = /** @type {HTMLImageElement} */
    g.src || ue(g)), je(
      y !== void 0 && y.length > 0,
      "A defined and non-empty `src` or `image` must be provided"
    ), je(
      !((t.width !== void 0 || t.height !== void 0) && t.scale !== void 0),
      "`width` or `height` cannot be provided together with `scale`"
    );
    let _;
    if (t.src !== void 0 ? _ = re.IDLE : g !== void 0 && ("complete" in g ? g.complete ? _ = g.src ? re.LOADED : re.IDLE : _ = re.LOADING : _ = re.LOADED), this.color_ = t.color !== void 0 ? Pm(t.color) : null, this.iconImage_ = RT(
      g,
      /** @type {string} */
      y,
      this.crossOrigin_,
      _,
      this.color_
    ), this.offset_ = t.offset !== void 0 ? t.offset : [0, 0], this.offsetOrigin_ = t.offsetOrigin !== void 0 ? t.offsetOrigin : "top-left", this.origin_ = null, this.size_ = t.size !== void 0 ? t.size : null, this.initialOptions_, t.width !== void 0 || t.height !== void 0) {
      let b, E;
      if (t.size)
        [b, E] = t.size;
      else {
        const x = this.getImage(1);
        if (x.width && x.height)
          b = x.width, E = x.height;
        else if (x instanceof HTMLImageElement) {
          this.initialOptions_ = t;
          const R = () => {
            if (this.unlistenImageChange(R), !this.initialOptions_)
              return;
            const w = this.iconImage_.getSize();
            this.setScale(
              YR(
                w[0],
                w[1],
                t.width,
                t.height
              )
            );
          };
          this.listenImageChange(R);
          return;
        }
      }
      b !== void 0 && this.setScale(
        YR(b, E, t.width, t.height)
      );
    }
  }
  /**
   * Clones the style. The underlying Image/HTMLCanvasElement is not cloned.
   * @return {Icon} The cloned style.
   * @api
   * @override
   */
  clone() {
    let t, i, a;
    return this.initialOptions_ ? (i = this.initialOptions_.width, a = this.initialOptions_.height) : (t = this.getScale(), t = Array.isArray(t) ? t.slice() : t), new xS({
      anchor: this.anchor_.slice(),
      anchorOrigin: this.anchorOrigin_,
      anchorXUnits: this.anchorXUnits_,
      anchorYUnits: this.anchorYUnits_,
      color: this.color_ && this.color_.slice ? this.color_.slice() : this.color_ || void 0,
      crossOrigin: this.crossOrigin_,
      offset: this.offset_.slice(),
      offsetOrigin: this.offsetOrigin_,
      opacity: this.getOpacity(),
      rotateWithView: this.getRotateWithView(),
      rotation: this.getRotation(),
      scale: t,
      width: i,
      height: a,
      size: this.size_ !== null ? this.size_.slice() : void 0,
      src: this.getSrc(),
      displacement: this.getDisplacement().slice(),
      declutterMode: this.getDeclutterMode()
    });
  }
  /**
   * Get the anchor point in pixels. The anchor determines the center point for the
   * symbolizer.
   * @return {Array<number>} Anchor.
   * @api
   * @override
   */
  getAnchor() {
    let t = this.normalizedAnchor_;
    if (!t) {
      t = this.anchor_;
      const u = this.getSize();
      if (this.anchorXUnits_ == "fraction" || this.anchorYUnits_ == "fraction") {
        if (!u)
          return null;
        t = this.anchor_.slice(), this.anchorXUnits_ == "fraction" && (t[0] *= u[0]), this.anchorYUnits_ == "fraction" && (t[1] *= u[1]);
      }
      if (this.anchorOrigin_ != "top-left") {
        if (!u)
          return null;
        t === this.anchor_ && (t = this.anchor_.slice()), (this.anchorOrigin_ == "top-right" || this.anchorOrigin_ == "bottom-right") && (t[0] = -t[0] + u[0]), (this.anchorOrigin_ == "bottom-left" || this.anchorOrigin_ == "bottom-right") && (t[1] = -t[1] + u[1]);
      }
      this.normalizedAnchor_ = t;
    }
    const i = this.getDisplacement(), a = this.getScaleArray();
    return [
      t[0] - i[0] / a[0],
      t[1] + i[1] / a[1]
    ];
  }
  /**
   * Set the anchor point. The anchor determines the center point for the
   * symbolizer.
   *
   * @param {Array<number>} anchor Anchor.
   * @api
   */
  setAnchor(t) {
    this.anchor_ = t, this.normalizedAnchor_ = null;
  }
  /**
   * Get the icon color.
   * @return {import("../color.js").Color} Color.
   * @api
   */
  getColor() {
    return this.color_;
  }
  /**
   * Get the image icon.
   * @param {number} pixelRatio Pixel ratio.
   * @return {HTMLImageElement|HTMLCanvasElement|ImageBitmap} Image or Canvas element. If the Icon
   * style was configured with `src` or with a not let loaded `img`, an `ImageBitmap` will be returned.
   * @api
   * @override
   */
  getImage(t) {
    return this.iconImage_.getImage(t);
  }
  /**
   * Get the pixel ratio.
   * @param {number} pixelRatio Pixel ratio.
   * @return {number} The pixel ratio of the image.
   * @api
   * @override
   */
  getPixelRatio(t) {
    return this.iconImage_.getPixelRatio(t);
  }
  /**
   * @return {import("../size.js").Size} Image size.
   * @override
   */
  getImageSize() {
    return this.iconImage_.getSize();
  }
  /**
   * @return {import("../ImageState.js").default} Image state.
   * @override
   */
  getImageState() {
    return this.iconImage_.getImageState();
  }
  /**
   * @return {HTMLImageElement|HTMLCanvasElement|ImageBitmap} Image element.
   * @override
   */
  getHitDetectionImage() {
    return this.iconImage_.getHitDetectionImage();
  }
  /**
   * Get the origin of the symbolizer.
   * @return {Array<number>} Origin.
   * @api
   * @override
   */
  getOrigin() {
    if (this.origin_)
      return this.origin_;
    let t = this.offset_;
    if (this.offsetOrigin_ != "top-left") {
      const i = this.getSize(), a = this.iconImage_.getSize();
      if (!i || !a)
        return null;
      t = t.slice(), (this.offsetOrigin_ == "top-right" || this.offsetOrigin_ == "bottom-right") && (t[0] = a[0] - i[0] - t[0]), (this.offsetOrigin_ == "bottom-left" || this.offsetOrigin_ == "bottom-right") && (t[1] = a[1] - i[1] - t[1]);
    }
    return this.origin_ = t, this.origin_;
  }
  /**
   * Get the image URL.
   * @return {string|undefined} Image src.
   * @api
   */
  getSrc() {
    return this.iconImage_.getSrc();
  }
  /**
   * Get the size of the icon (in pixels).
   * @return {import("../size.js").Size} Image size.
   * @api
   * @override
   */
  getSize() {
    return this.size_ ? this.size_ : this.iconImage_.getSize();
  }
  /**
   * Get the width of the icon (in pixels). Will return undefined when the icon image is not yet loaded.
   * @return {number} Icon width (in pixels).
   * @api
   */
  getWidth() {
    const t = this.getScaleArray();
    if (this.size_)
      return this.size_[0] * t[0];
    if (this.iconImage_.getImageState() == re.LOADED)
      return this.iconImage_.getSize()[0] * t[0];
  }
  /**
   * Get the height of the icon (in pixels). Will return undefined when the icon image is not yet loaded.
   * @return {number} Icon height (in pixels).
   * @api
   */
  getHeight() {
    const t = this.getScaleArray();
    if (this.size_)
      return this.size_[1] * t[1];
    if (this.iconImage_.getImageState() == re.LOADED)
      return this.iconImage_.getSize()[1] * t[1];
  }
  /**
   * Set the scale.
   *
   * @param {number|import("../size.js").Size} scale Scale.
   * @api
   * @override
   */
  setScale(t) {
    delete this.initialOptions_, super.setScale(t);
  }
  /**
   * @param {function(import("../events/Event.js").default): void} listener Listener function.
   * @override
   */
  listenImageChange(t) {
    this.iconImage_.addEventListener($t.CHANGE, t);
  }
  /**
   * Load not yet loaded URI.
   * When rendering a feature with an icon style, the vector renderer will
   * automatically call this method. However, you might want to call this
   * method yourself for preloading or other purposes.
   * @api
   * @override
   */
  load() {
    this.iconImage_.load();
  }
  /**
   * @param {function(import("../events/Event.js").default): void} listener Listener function.
   * @override
   */
  unlistenImageChange(t) {
    this.iconImage_.removeEventListener($t.CHANGE, t);
  }
  /**
   * @override
   */
  ready() {
    return this.iconImage_.ready();
  }
}
const Qo = 0.5;
function XO(r, t, i, a, u, h, g, y, _) {
  const b = _ ? y_(u, _) : u, E = r[0] * Qo, x = r[1] * Qo, R = ei(E, x);
  R.imageSmoothingEnabled = !1;
  const w = R.canvas, L = new lO(
    R,
    Qo,
    u,
    null,
    g,
    y,
    _ ? O_(Wh(), _) : null
  ), H = i.length, N = Math.floor((256 * 256 * 256 - 1) / H), O = {};
  for (let I = 1; I <= H; ++I) {
    const X = i[I - 1], Z = X.getStyleFunction() || a;
    if (!Z)
      continue;
    let dt = Z(X, h);
    if (!dt)
      continue;
    Array.isArray(dt) || (dt = [dt]);
    const yt = (I * N).toString(16).padStart(7, "#00000");
    for (let it = 0, Et = dt.length; it < Et; ++it) {
      const xt = dt[it], pt = xt.getGeometryFunction()(X);
      if (!pt || !qi(b, pt.getExtent()))
        continue;
      const Tt = xt.clone(), Nt = Tt.getFill();
      Nt && Nt.setColor(yt);
      const Rt = Tt.getStroke();
      Rt && (Rt.setColor(yt), Rt.setLineDash(null)), Tt.setText(void 0);
      const Y = xt.getImage();
      if (Y) {
        const F = Y.getImageSize();
        if (!F)
          continue;
        const St = ei(
          F[0],
          F[1],
          void 0,
          { alpha: !1 }
        ), Q = St.canvas;
        St.fillStyle = yt, St.fillRect(0, 0, Q.width, Q.height), Tt.setImage(
          new xS({
            img: Q,
            anchor: Y.getAnchor(),
            anchorXUnits: "pixels",
            anchorYUnits: "pixels",
            offset: Y.getOrigin(),
            opacity: 1,
            size: Y.getSize(),
            scale: Y.getScale(),
            rotation: Y.getRotation(),
            rotateWithView: Y.getRotateWithView()
          })
        );
      }
      const K = Tt.getZIndex() || 0;
      let nt = O[K];
      nt || (nt = {}, O[K] = nt, nt.Polygon = [], nt.Circle = [], nt.LineString = [], nt.Point = []);
      const vt = pt.getType();
      if (vt === "GeometryCollection") {
        const F = (
          /** @type {import("../../geom/GeometryCollection.js").default} */
          pt.getGeometriesArrayRecursive()
        );
        for (let St = 0, Q = F.length; St < Q; ++St) {
          const At = F[St];
          nt[At.getType().replace("Multi", "")].push(
            At,
            Tt
          );
        }
      } else
        nt[vt.replace("Multi", "")].push(pt, Tt);
    }
  }
  const U = Object.keys(O).map(Number).sort(hu);
  for (let I = 0, X = U.length; I < X; ++I) {
    const Z = O[U[I]];
    for (const dt in Z) {
      const ft = Z[dt];
      for (let yt = 0, it = ft.length; yt < it; yt += 2) {
        L.setStyle(ft[yt + 1]);
        for (let Et = 0, xt = t.length; Et < xt; ++Et)
          L.setTransform(t[Et]), L.drawGeometry(ft[yt]);
      }
    }
  }
  return R.getImageData(0, 0, w.width, w.height);
}
function kO(r, t, i) {
  const a = [];
  if (i) {
    const u = Math.floor(Math.round(r[0]) * Qo), h = Math.floor(Math.round(r[1]) * Qo), g = (gn(u, 0, i.width - 1) + gn(h, 0, i.height - 1) * i.width) * 4, y = i.data[g], _ = i.data[g + 1], E = i.data[g + 2] + 256 * (_ + 256 * y), x = Math.floor((256 * 256 * 256 - 1) / t.length);
    E && E % x === 0 && a.push(t[E / x - 1]);
  }
  return a;
}
class sA extends ys {
  /**
   * @param {import("./EventType.js").default} type Type.
   * @param {import("../transform.js").Transform} [inversePixelTransform] Transform for
   *     CSS pixels to rendered pixels.
   * @param {import("../Map.js").FrameState} [frameState] Frame state.
   * @param {?(CanvasRenderingContext2D|WebGLRenderingContext)} [context] Context.
   */
  constructor(t, i, a, u) {
    super(t), this.inversePixelTransform = i, this.frameState = a, this.context = u;
  }
}
const VO = 5;
class qO extends A_ {
  /**
   * @param {LayerType} layer Layer.
   */
  constructor(t) {
    super(), this.ready = !0, this.boundHandleImageChange_ = this.handleImageChange_.bind(this), this.layer_ = t, this.staleKeys_ = new Array(), this.maxStaleKeys = VO;
  }
  /**
   * @return {Array<string>} Get the list of stale keys.
   */
  getStaleKeys() {
    return this.staleKeys_;
  }
  /**
   * @param {string} key The new stale key.
   */
  prependStaleKey(t) {
    this.staleKeys_.unshift(t), this.staleKeys_.length > this.maxStaleKeys && (this.staleKeys_.length = this.maxStaleKeys);
  }
  /**
   * Asynchronous layer level hit detection.
   * @param {import("../pixel.js").Pixel} pixel Pixel.
   * @return {Promise<Array<import("../Feature").FeatureLike>>} Promise that resolves with
   * an array of features.
   */
  getFeatures(t) {
    return we();
  }
  /**
   * @param {import("../pixel.js").Pixel} pixel Pixel.
   * @return {Uint8ClampedArray|Uint8Array|Float32Array|DataView|null} Pixel data.
   */
  getData(t) {
    return null;
  }
  /**
   * Determine whether render should be called.
   * @abstract
   * @param {import("../Map.js").FrameState} frameState Frame state.
   * @return {boolean} Layer is ready to be rendered.
   */
  prepareFrame(t) {
    return we();
  }
  /**
   * Render the layer.
   * @abstract
   * @param {import("../Map.js").FrameState} frameState Frame state.
   * @param {HTMLElement|null} target Target that may be used to render content to.
   * @return {HTMLElement} The rendered element.
   */
  renderFrame(t, i) {
    return we();
  }
  /**
   * @abstract
   * @param {import("../coordinate.js").Coordinate} coordinate Coordinate.
   * @param {import("../Map.js").FrameState} frameState Frame state.
   * @param {number} hitTolerance Hit tolerance in pixels.
   * @param {import("./vector.js").FeatureCallback<T>} callback Feature callback.
   * @param {Array<import("./Map.js").HitMatch<T>>} matches The hit detected matches with tolerance.
   * @return {T|undefined} Callback result.
   * @template T
   */
  forEachFeatureAtCoordinate(t, i, a, u, h) {
  }
  /**
   * @return {LayerType} Layer.
   */
  getLayer() {
    return this.layer_;
  }
  /**
   * Perform action necessary to get the layer rendered after new fonts have loaded
   * @abstract
   */
  handleFontsChanged() {
  }
  /**
   * Handle changes in image state.
   * @param {import("../events/Event.js").default} event Image change event.
   * @private
   */
  handleImageChange_(t) {
    const i = (
      /** @type {import("../Image.js").default} */
      t.target
    );
    (i.getState() === re.LOADED || i.getState() === re.ERROR) && this.renderIfReadyAndVisible();
  }
  /**
   * Load the image if not already loaded, and register the image change
   * listener if needed.
   * @param {import("../Image.js").default} image Image.
   * @return {boolean} `true` if the image is already loaded, `false` otherwise.
   * @protected
   */
  loadImage(t) {
    let i = t.getState();
    return i != re.LOADED && i != re.ERROR && t.addEventListener($t.CHANGE, this.boundHandleImageChange_), i == re.IDLE && (t.load(), i = t.getState()), i == re.LOADED;
  }
  /**
   * @protected
   */
  renderIfReadyAndVisible() {
    const t = this.getLayer();
    t && t.getVisible() && t.getSourceState() === "ready" && t.changed();
  }
  /**
   * @param {import("../Map.js").FrameState} frameState Frame state.
   */
  renderDeferred(t) {
  }
  /**
   * Clean up.
   * @override
   */
  disposeInternal() {
    delete this.layer_, super.disposeInternal();
  }
}
const jR = [];
let Gm = null;
function ZO() {
  Gm = ei(1, 1, void 0, {
    willReadFrequently: !0
  });
}
class oA extends qO {
  /**
   * @param {LayerType} layer Layer.
   */
  constructor(t) {
    super(t), this.container = null, this.renderedResolution, this.tempTransform = $s(), this.pixelTransform = $s(), this.inversePixelTransform = $s(), this.context = null, this.deferredContext_ = null, this.containerReused = !1, this.frameState = null;
  }
  /**
   * @param {import('../../DataTile.js').ImageLike} image Image.
   * @param {number} col The column index.
   * @param {number} row The row index.
   * @return {Uint8ClampedArray|null} The image data.
   */
  getImageData(t, i, a) {
    Gm || ZO(), Gm.clearRect(0, 0, 1, 1);
    let u;
    try {
      Gm.drawImage(t, i, a, 1, 1, 0, 0, 1, 1), u = Gm.getImageData(0, 0, 1, 1).data;
    } catch {
      return Gm = null, null;
    }
    return u;
  }
  /**
   * @param {import('../../Map.js').FrameState} frameState Frame state.
   * @return {string} Background color.
   */
  getBackground(t) {
    let a = this.getLayer().getBackground();
    return typeof a == "function" && (a = a(t.viewState.resolution)), a || void 0;
  }
  /**
   * Get a rendering container from an existing target, if compatible.
   * @param {HTMLElement} target Potential render target.
   * @param {string} transform CSS transform matrix.
   * @param {string} [backgroundColor] Background color.
   */
  useContainer(t, i, a) {
    const u = this.getLayer().getClassName();
    let h, g;
    if (t && t.className === u && (!a || t && t.style.backgroundColor && Wc(
      Pm(t.style.backgroundColor),
      Pm(a)
    ))) {
      const y = t.firstElementChild;
      y instanceof HTMLCanvasElement && (g = y.getContext("2d"));
    }
    if (g && uD(g.canvas.style.transform, i) ? (this.container = t, this.context = g, this.containerReused = !0) : this.containerReused ? (this.container = null, this.context = null, this.containerReused = !1) : this.container && (this.container.style.backgroundColor = null), !this.container) {
      h = document.createElement("div"), h.className = u;
      let y = h.style;
      y.position = "absolute", y.width = "100%", y.height = "100%", g = ei();
      const _ = g.canvas;
      h.appendChild(_), y = _.style, y.position = "absolute", y.left = "0", y.transformOrigin = "top left", this.container = h, this.context = g;
    }
    !this.containerReused && a && !this.container.style.backgroundColor && (this.container.style.backgroundColor = a);
  }
  /**
   * @param {CanvasRenderingContext2D} context Context.
   * @param {import("../../Map.js").FrameState} frameState Frame state.
   * @param {import("../../extent.js").Extent} extent Clip extent.
   * @protected
   */
  clipUnrotated(t, i, a) {
    const u = nd(a), h = fS(a), g = cS(a), y = uS(a);
    hi(i.coordinateToPixelTransform, u), hi(i.coordinateToPixelTransform, h), hi(i.coordinateToPixelTransform, g), hi(i.coordinateToPixelTransform, y);
    const _ = this.inversePixelTransform;
    hi(_, u), hi(_, h), hi(_, g), hi(_, y), t.save(), t.beginPath(), t.moveTo(Math.round(u[0]), Math.round(u[1])), t.lineTo(Math.round(h[0]), Math.round(h[1])), t.lineTo(Math.round(g[0]), Math.round(g[1])), t.lineTo(Math.round(y[0]), Math.round(y[1])), t.clip();
  }
  /**
   * @param {import("../../Map.js").FrameState} frameState Frame state.
   * @param {HTMLElement} target Target that may be used to render content to.
   * @protected
   */
  prepareContainer(t, i) {
    const a = t.extent, u = t.viewState.resolution, h = t.viewState.rotation, g = t.pixelRatio, y = Math.round(Xe(a) / u * g), _ = Math.round(ml(a) / u * g);
    vu(
      this.pixelTransform,
      t.size[0] / 2,
      t.size[1] / 2,
      1 / g,
      1 / g,
      h,
      -y / 2,
      -_ / 2
    ), w2(this.inversePixelTransform, this.pixelTransform);
    const b = rD(this.pixelTransform);
    if (this.useContainer(i, b, this.getBackground(t)), !this.containerReused) {
      const E = this.context.canvas;
      E.width != y || E.height != _ ? (E.width = y, E.height = _) : this.context.clearRect(0, 0, y, _), b !== E.style.transform && (E.style.transform = b);
    }
  }
  /**
   * @param {import("../../render/EventType.js").default} type Event type.
   * @param {CanvasRenderingContext2D} context Context.
   * @param {import("../../Map.js").FrameState} frameState Frame state.
   * @private
   */
  dispatchRenderEvent_(t, i, a) {
    const u = this.getLayer();
    if (u.hasListener(t)) {
      const h = new sA(
        t,
        this.inversePixelTransform,
        a,
        i
      );
      u.dispatchEvent(h);
    }
  }
  /**
   * @param {CanvasRenderingContext2D} context Context.
   * @param {import("../../Map.js").FrameState} frameState Frame state.
   * @protected
   */
  preRender(t, i) {
    this.frameState = i, !i.declutter && this.dispatchRenderEvent_(fs.PRERENDER, t, i);
  }
  /**
   * @param {CanvasRenderingContext2D} context Context.
   * @param {import("../../Map.js").FrameState} frameState Frame state.
   * @protected
   */
  postRender(t, i) {
    i.declutter || this.dispatchRenderEvent_(fs.POSTRENDER, t, i);
  }
  /**
   * @param {import("../../Map.js").FrameState} frameState Frame state.
   */
  renderDeferredInternal(t) {
  }
  /**
   * @param {import("../../Map.js").FrameState} frameState Frame state.
   * @return {import('../../render/canvas/ZIndexContext.js').ZIndexContextProxy} Context.
   */
  getRenderContext(t) {
    return t.declutter && !this.deferredContext_ && (this.deferredContext_ = new lA()), t.declutter ? this.deferredContext_.getContext() : this.context;
  }
  /**
   * @param {import("../../Map.js").FrameState} frameState Frame state.
   * @override
   */
  renderDeferred(t) {
    t.declutter && (this.dispatchRenderEvent_(
      fs.PRERENDER,
      this.context,
      t
    ), t.declutter && this.deferredContext_ && (this.deferredContext_.draw(this.context), this.deferredContext_.clear()), this.renderDeferredInternal(t), this.dispatchRenderEvent_(
      fs.POSTRENDER,
      this.context,
      t
    ));
  }
  /**
   * Creates a transform for rendering to an element that will be rotated after rendering.
   * @param {import("../../coordinate.js").Coordinate} center Center.
   * @param {number} resolution Resolution.
   * @param {number} rotation Rotation.
   * @param {number} pixelRatio Pixel ratio.
   * @param {number} width Width of the rendered element (in pixels).
   * @param {number} height Height of the rendered element (in pixels).
   * @param {number} offsetX Offset on the x-axis in view coordinates.
   * @protected
   * @return {!import("../../transform.js").Transform} Transform.
   */
  getRenderTransform(t, i, a, u, h, g, y) {
    const _ = h / 2, b = g / 2, E = u / i, x = -E, R = -t[0] + y, w = -t[1];
    return vu(
      this.tempTransform,
      _,
      b,
      E,
      x,
      -a,
      R,
      w
    );
  }
  /**
   * Clean up.
   * @override
   */
  disposeInternal() {
    delete this.frameState, super.disposeInternal();
  }
}
class FO extends oA {
  /**
   * @param {import("../../layer/BaseVector.js").default} vectorLayer Vector layer.
   */
  constructor(t) {
    super(t), this.boundHandleStyleImageChange_ = this.handleStyleImageChange_.bind(this), this.animatingOrInteracting_, this.hitDetectionImageData_ = null, this.clipped_ = !1, this.renderedFeatures_ = null, this.renderedRevision_ = -1, this.renderedResolution_ = NaN, this.renderedExtent_ = _a(), this.wrappedRenderedExtent_ = _a(), this.renderedRotation_, this.renderedCenter_ = null, this.renderedProjection_ = null, this.renderedPixelRatio_ = 1, this.renderedRenderOrder_ = null, this.renderedFrameDeclutter_, this.replayGroup_ = null, this.replayGroupChanged = !0, this.clipping = !0, this.targetContext_ = null, this.opacity_ = 1;
  }
  /**
   * @param {ExecutorGroup} executorGroup Executor group.
   * @param {import("../../Map.js").FrameState} frameState Frame state.
   * @param {boolean} [declutterable] `true` to only render declutterable items,
   *     `false` to only render non-declutterable items, `undefined` to render all.
   */
  renderWorlds(t, i, a) {
    const u = i.extent, h = i.viewState, g = h.center, y = h.resolution, _ = h.projection, b = h.rotation, E = _.getExtent(), x = this.getLayer().getSource(), R = this.getLayer().getDeclutter(), w = i.pixelRatio, L = i.viewHints, H = !(L[Vi.ANIMATING] || L[Vi.INTERACTING]), N = this.context, O = Math.round(Xe(u) / y * w), U = Math.round(ml(u) / y * w), I = x.getWrapX() && _.canWrapX(), X = I ? Xe(E) : null, Z = I ? Math.ceil((u[2] - E[2]) / X) + 1 : 1;
    let dt = I ? Math.floor((u[0] - E[0]) / X) : 0;
    do {
      let ft = this.getRenderTransform(
        g,
        y,
        0,
        w,
        O,
        U,
        dt * X
      );
      i.declutter && (ft = ft.slice(0)), t.execute(
        N,
        [N.canvas.width, N.canvas.height],
        ft,
        b,
        H,
        a === void 0 ? kh : a ? aA : jO,
        a ? R && i.declutter[R] : void 0
      );
    } while (++dt < Z);
  }
  /**
   * @private
   */
  setDrawContext_() {
    this.opacity_ !== 1 && (this.targetContext_ = this.context, this.context = ei(
      this.context.canvas.width,
      this.context.canvas.height,
      jR
    ));
  }
  /**
   * @private
   */
  resetDrawContext_() {
    if (this.opacity_ !== 1 && this.targetContext_) {
      const t = this.targetContext_.globalAlpha;
      this.targetContext_.globalAlpha = this.opacity_, this.targetContext_.drawImage(this.context.canvas, 0, 0), this.targetContext_.globalAlpha = t, _S(this.context), jR.push(this.context.canvas), this.context = this.targetContext_, this.targetContext_ = null;
    }
  }
  /**
   * Render declutter items for this layer
   * @param {import("../../Map.js").FrameState} frameState Frame state.
   */
  renderDeclutter(t) {
    !this.replayGroup_ || !this.getLayer().getDeclutter() || this.renderWorlds(this.replayGroup_, t, !0);
  }
  /**
   * Render deferred instructions.
   * @param {import("../../Map.js").FrameState} frameState Frame state.
   * @override
   */
  renderDeferredInternal(t) {
    this.replayGroup_ && (this.replayGroup_.renderDeferred(), this.clipped_ && this.context.restore(), this.resetDrawContext_());
  }
  /**
   * Render the layer.
   * @param {import("../../Map.js").FrameState} frameState Frame state.
   * @param {HTMLElement|null} target Target that may be used to render content to.
   * @return {HTMLElement} The rendered element.
   * @override
   */
  renderFrame(t, i) {
    const a = t.layerStatesArray[t.layerIndex];
    this.opacity_ = a.opacity;
    const u = t.viewState;
    this.prepareContainer(t, i);
    const h = this.context, g = this.replayGroup_;
    let y = g && !g.isEmpty();
    if (!y && !(this.getLayer().hasListener(fs.PRERENDER) || this.getLayer().hasListener(fs.POSTRENDER)))
      return this.container;
    this.setDrawContext_(), this.preRender(h, t);
    const _ = u.projection;
    if (this.clipped_ = !1, y && a.extent && this.clipping) {
      const b = Vc(a.extent, _);
      y = qi(b, t.extent), this.clipped_ = y && !e_(b, t.extent), this.clipped_ && this.clipUnrotated(h, t, b);
    }
    return y && this.renderWorlds(
      g,
      t,
      this.getLayer().getDeclutter() ? !1 : void 0
    ), !t.declutter && this.clipped_ && h.restore(), this.postRender(h, t), this.renderedRotation_ !== u.rotation && (this.renderedRotation_ = u.rotation, this.hitDetectionImageData_ = null), t.declutter || this.resetDrawContext_(), this.container;
  }
  /**
   * Asynchronous layer level hit detection.
   * @param {import("../../pixel.js").Pixel} pixel Pixel.
   * @return {Promise<Array<import("../../Feature").default>>} Promise
   * that resolves with an array of features.
   * @override
   */
  getFeatures(t) {
    return new Promise((i) => {
      if (this.frameState && !this.hitDetectionImageData_ && !this.animatingOrInteracting_) {
        const a = this.frameState.size.slice(), u = this.renderedCenter_, h = this.renderedResolution_, g = this.renderedRotation_, y = this.renderedProjection_, _ = this.wrappedRenderedExtent_, b = this.getLayer(), E = [], x = a[0] * Qo, R = a[1] * Qo;
        E.push(
          this.getRenderTransform(
            u,
            h,
            g,
            Qo,
            x,
            R,
            0
          ).slice()
        );
        const w = b.getSource(), L = y.getExtent();
        if (w.getWrapX() && y.canWrapX() && !e_(L, _)) {
          let N = _[0];
          const O = Xe(L);
          let U = 0, I;
          for (; N < L[0]; )
            --U, I = O * U, E.push(
              this.getRenderTransform(
                u,
                h,
                g,
                Qo,
                x,
                R,
                I
              ).slice()
            ), N += O;
          for (U = 0, N = _[2]; N > L[2]; )
            ++U, I = O * U, E.push(
              this.getRenderTransform(
                u,
                h,
                g,
                Qo,
                x,
                R,
                I
              ).slice()
            ), N -= O;
        }
        const H = Wh();
        this.hitDetectionImageData_ = XO(
          a,
          E,
          this.renderedFeatures_,
          b.getStyleFunction(),
          _,
          h,
          g,
          RR(h, this.renderedPixelRatio_),
          H ? y : null
        );
      }
      i(
        kO(t, this.renderedFeatures_, this.hitDetectionImageData_)
      );
    });
  }
  /**
   * @param {import("../../coordinate.js").Coordinate} coordinate Coordinate.
   * @param {import("../../Map.js").FrameState} frameState Frame state.
   * @param {number} hitTolerance Hit tolerance in pixels.
   * @param {import("../vector.js").FeatureCallback<T>} callback Feature callback.
   * @param {Array<import("../Map.js").HitMatch<T>>} matches The hit detected matches with tolerance.
   * @return {T|undefined} Callback result.
   * @template T
   * @override
   */
  forEachFeatureAtCoordinate(t, i, a, u, h) {
    var R, w;
    if (!this.replayGroup_)
      return;
    const g = i.viewState.resolution, y = i.viewState.rotation, _ = this.getLayer(), b = {}, E = function(L, H, N) {
      const O = ue(L), U = b[O];
      if (U) {
        if (U !== !0 && N < U.distanceSq) {
          if (N === 0)
            return b[O] = !0, h.splice(h.lastIndexOf(U), 1), u(L, _, H);
          U.geometry = H, U.distanceSq = N;
        }
      } else {
        if (N === 0)
          return b[O] = !0, u(L, _, H);
        h.push(
          b[O] = {
            feature: L,
            layer: _,
            geometry: H,
            distanceSq: N,
            callback: u
          }
        );
      }
    }, x = this.getLayer().getDeclutter();
    return this.replayGroup_.forEachFeatureAtCoordinate(
      t,
      g,
      y,
      a,
      E,
      x ? (w = (R = i.declutter) == null ? void 0 : R[x]) == null ? void 0 : w.all().map((L) => L.value) : null
    );
  }
  /**
   * Perform action necessary to get the layer rendered after new fonts have loaded
   * @override
   */
  handleFontsChanged() {
    const t = this.getLayer();
    t.getVisible() && this.replayGroup_ && t.changed();
  }
  /**
   * Handle changes in image style state.
   * @param {import("../../events/Event.js").default} event Image style change event.
   * @private
   */
  handleStyleImageChange_(t) {
    this.renderIfReadyAndVisible();
  }
  /**
   * Determine whether render should be called.
   * @param {import("../../Map.js").FrameState} frameState Frame state.
   * @return {boolean} Layer is ready to be rendered.
   * @override
   */
  prepareFrame(t) {
    const i = this.getLayer(), a = i.getSource();
    if (!a)
      return !1;
    const u = t.viewHints[Vi.ANIMATING], h = t.viewHints[Vi.INTERACTING], g = i.getUpdateWhileAnimating(), y = i.getUpdateWhileInteracting();
    if (this.ready && !g && u || !y && h)
      return this.animatingOrInteracting_ = !0, !0;
    this.animatingOrInteracting_ = !1;
    const _ = t.extent, b = t.viewState, E = b.projection, x = b.resolution, R = t.pixelRatio, w = i.getRevision(), L = i.getRenderBuffer();
    let H = i.getRenderOrder();
    H === void 0 && (H = sO);
    const N = b.center.slice(), O = rS(
      _,
      L * x
    ), U = O.slice(), I = [O.slice()], X = E.getExtent();
    if (a.getWrapX() && E.canWrapX() && !e_(X, t.extent)) {
      const Rt = Xe(X), Y = Math.max(Xe(O) / 2, Rt);
      O[0] = X[0] - Y, O[2] = X[2] + Y, b2(N, E);
      const K = _2(I[0], E);
      K[0] < X[0] && K[2] < X[2] ? I.push([
        K[0] + Rt,
        K[1],
        K[2] + Rt,
        K[3]
      ]) : K[0] > X[0] && K[2] > X[2] && I.push([
        K[0] - Rt,
        K[1],
        K[2] - Rt,
        K[3]
      ]);
    }
    if (this.ready && this.renderedResolution_ == x && this.renderedRevision_ == w && this.renderedRenderOrder_ == H && this.renderedFrameDeclutter_ === !!t.declutter && e_(this.wrappedRenderedExtent_, O))
      return Wc(this.renderedExtent_, U) || (this.hitDetectionImageData_ = null, this.renderedExtent_ = U), this.renderedCenter_ = N, this.replayGroupChanged = !1, !0;
    this.replayGroup_ = null;
    const Z = new NO(
      Q2(x, R),
      O,
      x,
      R
    ), dt = Wh();
    let ft;
    if (dt) {
      for (let Rt = 0, Y = I.length; Rt < Y; ++Rt) {
        const K = I[Rt], nt = y_(K, E);
        a.loadFeatures(
          nt,
          iD(x, E),
          dt
        );
      }
      ft = O_(dt, E);
    } else
      for (let Rt = 0, Y = I.length; Rt < Y; ++Rt)
        a.loadFeatures(I[Rt], x, E);
    const yt = RR(x, R);
    let it = !0;
    const Et = (
      /**
       * @param {import("../../Feature.js").default} feature Feature.
       * @param {number} index Index.
       */
      (Rt, Y) => {
        let K;
        const nt = Rt.getStyleFunction() || i.getStyleFunction();
        if (nt && (K = nt(Rt, x)), K) {
          const vt = this.renderFeature(
            Rt,
            yt,
            K,
            Z,
            ft,
            this.getLayer().getDeclutter(),
            Y
          );
          it = it && !vt;
        }
      }
    ), xt = y_(O, E), pt = a.getFeaturesInExtent(xt);
    H && pt.sort(H);
    for (let Rt = 0, Y = pt.length; Rt < Y; ++Rt)
      Et(pt[Rt], Rt);
    this.renderedFeatures_ = pt, this.ready = it;
    const Tt = Z.finish(), Nt = new BO(
      O,
      x,
      R,
      a.getOverlaps(),
      Tt,
      i.getRenderBuffer(),
      !!t.declutter
    );
    return this.renderedResolution_ = x, this.renderedRevision_ = w, this.renderedRenderOrder_ = H, this.renderedFrameDeclutter_ = !!t.declutter, this.renderedExtent_ = U, this.wrappedRenderedExtent_ = O, this.renderedCenter_ = N, this.renderedProjection_ = E, this.renderedPixelRatio_ = R, this.replayGroup_ = Nt, this.hitDetectionImageData_ = null, this.replayGroupChanged = !0, !0;
  }
  /**
   * @param {import("../../Feature.js").default} feature Feature.
   * @param {number} squaredTolerance Squared render tolerance.
   * @param {import("../../style/Style.js").default|Array<import("../../style/Style.js").default>} styles The style or array of styles.
   * @param {import("../../render/canvas/BuilderGroup.js").default} builderGroup Builder group.
   * @param {import("../../proj.js").TransformFunction} [transform] Transform from user to view projection.
   * @param {boolean} [declutter] Enable decluttering.
   * @param {number} [index] Render order index.
   * @return {boolean} `true` if an image is loading.
   */
  renderFeature(t, i, a, u, h, g, y) {
    if (!a)
      return !1;
    let _ = !1;
    if (Array.isArray(a))
      for (let b = 0, E = a.length; b < E; ++b)
        _ = AR(
          u,
          t,
          a[b],
          i,
          this.boundHandleStyleImageChange_,
          h,
          g,
          y
        ) || _;
    else
      _ = AR(
        u,
        t,
        a,
        i,
        this.boundHandleStyleImageChange_,
        h,
        g,
        y
      );
    return _;
  }
}
let id = 0;
const gl = 1 << id++, Se = 1 << id++, kl = 1 << id++, Ws = 1 << id++, $h = 1 << id++, l_ = 1 << id++, N1 = Math.pow(2, id) - 1, DT = {
  [gl]: "boolean",
  [Se]: "number",
  [kl]: "string",
  [Ws]: "color",
  [$h]: "number[]",
  [l_]: "size"
}, KO = Object.keys(DT).map(Number).sort(hu);
function PO(r) {
  return r in DT;
}
function a_(r) {
  const t = [];
  for (const i of KO)
    s_(r, i) && t.push(DT[i]);
  return t.length === 0 ? "untyped" : t.length < 3 ? t.join(" or ") : t.slice(0, -1).join(", ") + ", or " + t[t.length - 1];
}
function s_(r, t) {
  return (r & t) === t;
}
function Bc(r, t) {
  return r === t;
}
class ti {
  /**
   * @param {number} type The value type.
   * @param {LiteralValue} value The literal value.
   */
  constructor(t, i) {
    if (!PO(t))
      throw new Error(
        `literal expressions must have a specific type, got ${a_(t)}`
      );
    this.type = t, this.value = i;
  }
}
class QO {
  /**
   * @param {number} type The return type.
   * @param {string} operator The operator.
   * @param {...Expression} args The arguments.
   */
  constructor(t, i, ...a) {
    this.type = t, this.operator = i, this.args = a;
  }
}
function rA() {
  return {
    variables: /* @__PURE__ */ new Set(),
    properties: /* @__PURE__ */ new Set(),
    featureId: !1,
    geometryType: !1,
    mapState: !1
  };
}
function Ti(r, t, i) {
  switch (typeof r) {
    case "boolean": {
      if (Bc(t, kl))
        return new ti(kl, r ? "true" : "false");
      if (!s_(t, gl))
        throw new Error(
          `got a boolean, but expected ${a_(t)}`
        );
      return new ti(gl, r);
    }
    case "number": {
      if (Bc(t, l_))
        return new ti(l_, Zl(r));
      if (Bc(t, gl))
        return new ti(gl, !!r);
      if (Bc(t, kl))
        return new ti(kl, r.toString());
      if (!s_(t, Se))
        throw new Error(`got a number, but expected ${a_(t)}`);
      return new ti(Se, r);
    }
    case "string": {
      if (Bc(t, Ws))
        return new ti(Ws, xT(r));
      if (Bc(t, gl))
        return new ti(gl, !!r);
      if (!s_(t, kl))
        throw new Error(`got a string, but expected ${a_(t)}`);
      return new ti(kl, r);
    }
  }
  if (!Array.isArray(r))
    throw new Error("expression must be an array or a primitive value");
  if (r.length === 0)
    throw new Error("empty expression");
  if (typeof r[0] == "string")
    return o3(r, t, i);
  for (const a of r)
    if (typeof a != "number")
      throw new Error("expected an array of numbers");
  if (Bc(t, l_)) {
    if (r.length !== 2)
      throw new Error(
        `expected an array of two values for a size, got ${r.length}`
      );
    return new ti(l_, r);
  }
  if (Bc(t, Ws)) {
    if (r.length === 3)
      return new ti(Ws, [...r, 1]);
    if (r.length === 4)
      return new ti(Ws, r);
    throw new Error(
      `expected an array of 3 or 4 values for a color, got ${r.length}`
    );
  }
  if (!s_(t, $h))
    throw new Error(
      `got an array of numbers, but expected ${a_(t)}`
    );
  return new ti($h, r);
}
const mt = {
  Get: "get",
  Var: "var",
  Concat: "concat",
  GeometryType: "geometry-type",
  LineMetric: "line-metric",
  Any: "any",
  All: "all",
  Not: "!",
  Resolution: "resolution",
  Zoom: "zoom",
  Time: "time",
  Equal: "==",
  NotEqual: "!=",
  GreaterThan: ">",
  GreaterThanOrEqualTo: ">=",
  LessThan: "<",
  LessThanOrEqualTo: "<=",
  Multiply: "*",
  Divide: "/",
  Add: "+",
  Subtract: "-",
  Clamp: "clamp",
  Mod: "%",
  Pow: "^",
  Abs: "abs",
  Floor: "floor",
  Ceil: "ceil",
  Round: "round",
  Sin: "sin",
  Cos: "cos",
  Atan: "atan",
  Sqrt: "sqrt",
  Match: "match",
  Between: "between",
  Interpolate: "interpolate",
  Coalesce: "coalesce",
  Case: "case",
  In: "in",
  Number: "number",
  String: "string",
  Array: "array",
  Color: "color",
  Id: "id",
  Band: "band",
  Palette: "palette",
  ToString: "to-string",
  Has: "has"
}, WO = {
  [mt.Get]: Zt(ae(1, 1 / 0), BR),
  [mt.Var]: Zt(ae(1, 1), JO),
  [mt.Has]: Zt(ae(1, 1 / 0), BR),
  [mt.Id]: Zt($O, wm),
  [mt.Concat]: Zt(
    ae(2, 1 / 0),
    Ne(kl)
  ),
  [mt.GeometryType]: Zt(t3, wm),
  [mt.LineMetric]: Zt(wm),
  [mt.Resolution]: Zt(hb, wm),
  [mt.Zoom]: Zt(hb, wm),
  [mt.Time]: Zt(hb, wm),
  [mt.Any]: Zt(
    ae(2, 1 / 0),
    Ne(gl)
  ),
  [mt.All]: Zt(
    ae(2, 1 / 0),
    Ne(gl)
  ),
  [mt.Not]: Zt(
    ae(1, 1),
    Ne(gl)
  ),
  [mt.Equal]: Zt(
    ae(2, 2),
    Ne(N1)
  ),
  [mt.NotEqual]: Zt(
    ae(2, 2),
    Ne(N1)
  ),
  [mt.GreaterThan]: Zt(
    ae(2, 2),
    Ne(Se)
  ),
  [mt.GreaterThanOrEqualTo]: Zt(
    ae(2, 2),
    Ne(Se)
  ),
  [mt.LessThan]: Zt(
    ae(2, 2),
    Ne(Se)
  ),
  [mt.LessThanOrEqualTo]: Zt(
    ae(2, 2),
    Ne(Se)
  ),
  [mt.Multiply]: Zt(
    ae(2, 1 / 0),
    IR
  ),
  [mt.Coalesce]: Zt(
    ae(2, 1 / 0),
    IR
  ),
  [mt.Divide]: Zt(
    ae(2, 2),
    Ne(Se)
  ),
  [mt.Add]: Zt(
    ae(2, 1 / 0),
    Ne(Se)
  ),
  [mt.Subtract]: Zt(
    ae(2, 2),
    Ne(Se)
  ),
  [mt.Clamp]: Zt(
    ae(3, 3),
    Ne(Se)
  ),
  [mt.Mod]: Zt(
    ae(2, 2),
    Ne(Se)
  ),
  [mt.Pow]: Zt(
    ae(2, 2),
    Ne(Se)
  ),
  [mt.Abs]: Zt(
    ae(1, 1),
    Ne(Se)
  ),
  [mt.Floor]: Zt(
    ae(1, 1),
    Ne(Se)
  ),
  [mt.Ceil]: Zt(
    ae(1, 1),
    Ne(Se)
  ),
  [mt.Round]: Zt(
    ae(1, 1),
    Ne(Se)
  ),
  [mt.Sin]: Zt(
    ae(1, 1),
    Ne(Se)
  ),
  [mt.Cos]: Zt(
    ae(1, 1),
    Ne(Se)
  ),
  [mt.Atan]: Zt(
    ae(1, 2),
    Ne(Se)
  ),
  [mt.Sqrt]: Zt(
    ae(1, 1),
    Ne(Se)
  ),
  [mt.Match]: Zt(
    ae(4, 1 / 0),
    XR,
    n3
  ),
  [mt.Between]: Zt(
    ae(3, 3),
    Ne(Se)
  ),
  [mt.Interpolate]: Zt(
    ae(6, 1 / 0),
    XR,
    i3
  ),
  [mt.Case]: Zt(
    ae(3, 1 / 0),
    e3,
    l3
  ),
  [mt.In]: Zt(ae(2, 2), a3),
  [mt.Number]: Zt(
    ae(1, 1 / 0),
    Ne(N1)
  ),
  [mt.String]: Zt(
    ae(1, 1 / 0),
    Ne(N1)
  ),
  [mt.Array]: Zt(
    ae(1, 1 / 0),
    Ne(Se)
  ),
  [mt.Color]: Zt(
    ae(1, 4),
    Ne(Se)
  ),
  [mt.Band]: Zt(
    ae(1, 3),
    Ne(Se)
  ),
  [mt.Palette]: Zt(
    ae(2, 2),
    s3
  ),
  [mt.ToString]: Zt(
    ae(1, 1),
    Ne(gl | Se | kl | Ws)
  )
};
function BR(r, t, i) {
  const a = r.length - 1, u = new Array(a);
  for (let h = 0; h < a; ++h) {
    const g = r[h + 1];
    switch (typeof g) {
      case "number": {
        u[h] = new ti(Se, g);
        break;
      }
      case "string": {
        u[h] = new ti(kl, g);
        break;
      }
      default:
        throw new Error(
          `expected a string key or numeric array index for a get operation, got ${g}`
        );
    }
    h === 0 && i.properties.add(String(g));
  }
  return u;
}
function JO(r, t, i) {
  const a = r[1];
  if (typeof a != "string")
    throw new Error("expected a string argument for var operation");
  return i.variables.add(a), [new ti(kl, a)];
}
function $O(r, t, i) {
  i.featureId = !0;
}
function t3(r, t, i) {
  i.geometryType = !0;
}
function hb(r, t, i) {
  i.mapState = !0;
}
function wm(r, t, i) {
  const a = r[0];
  if (r.length !== 1)
    throw new Error(`expected no arguments for ${a} operation`);
  return [];
}
function ae(r, t) {
  return function(i, a, u) {
    const h = i[0], g = i.length - 1;
    if (r === t) {
      if (g !== r) {
        const y = r === 1 ? "" : "s";
        throw new Error(
          `expected ${r} argument${y} for ${h}, got ${g}`
        );
      }
    } else if (g < r || g > t) {
      const y = t === 1 / 0 ? `${r} or more` : `${r} to ${t}`;
      throw new Error(
        `expected ${y} arguments for ${h}, got ${g}`
      );
    }
  };
}
function IR(r, t, i) {
  const a = r.length - 1, u = new Array(a);
  for (let h = 0; h < a; ++h) {
    const g = Ti(r[h + 1], t, i);
    u[h] = g;
  }
  return u;
}
function Ne(r) {
  return function(t, i, a) {
    const u = t.length - 1, h = new Array(u);
    for (let g = 0; g < u; ++g) {
      const y = Ti(t[g + 1], r, a);
      h[g] = y;
    }
    return h;
  };
}
function e3(r, t, i) {
  const a = r[0], u = r.length - 1;
  if (u % 2 === 0)
    throw new Error(
      `expected an odd number of arguments for ${a}, got ${u} instead`
    );
}
function XR(r, t, i) {
  const a = r[0], u = r.length - 1;
  if (u % 2 === 1)
    throw new Error(
      `expected an even number of arguments for operation ${a}, got ${u} instead`
    );
}
function n3(r, t, i) {
  const a = r.length - 1, u = kl | Se | gl, h = Ti(r[1], u, i), g = Ti(r[r.length - 1], t, i), y = new Array(a - 2);
  for (let _ = 0; _ < a - 2; _ += 2) {
    try {
      const b = Ti(r[_ + 2], h.type, i);
      y[_] = b;
    } catch (b) {
      throw new Error(
        `failed to parse argument ${_ + 1} of match expression: ${b.message}`
      );
    }
    try {
      const b = Ti(r[_ + 3], g.type, i);
      y[_ + 1] = b;
    } catch (b) {
      throw new Error(
        `failed to parse argument ${_ + 2} of match expression: ${b.message}`
      );
    }
  }
  return [h, ...y, g];
}
function i3(r, t, i) {
  const a = r[1];
  let u;
  switch (a[0]) {
    case "linear":
      u = 1;
      break;
    case "exponential":
      const _ = a[1];
      if (typeof _ != "number" || _ <= 0)
        throw new Error(
          `expected a number base for exponential interpolation, got ${JSON.stringify(_)} instead`
        );
      u = _;
      break;
    default:
      throw new Error(
        `invalid interpolation type: ${JSON.stringify(a)}`
      );
  }
  const h = new ti(Se, u);
  let g;
  try {
    g = Ti(r[2], Se, i);
  } catch (_) {
    throw new Error(
      `failed to parse argument 1 in interpolate expression: ${_.message}`
    );
  }
  const y = new Array(r.length - 3);
  for (let _ = 0; _ < y.length; _ += 2) {
    try {
      const b = Ti(r[_ + 3], Se, i);
      y[_] = b;
    } catch (b) {
      throw new Error(
        `failed to parse argument ${_ + 2} for interpolate expression: ${b.message}`
      );
    }
    try {
      const b = Ti(r[_ + 4], t, i);
      y[_ + 1] = b;
    } catch (b) {
      throw new Error(
        `failed to parse argument ${_ + 3} for interpolate expression: ${b.message}`
      );
    }
  }
  return [h, g, ...y];
}
function l3(r, t, i) {
  const a = Ti(r[r.length - 1], t, i), u = new Array(r.length - 1);
  for (let h = 0; h < u.length - 1; h += 2) {
    try {
      const g = Ti(r[h + 1], gl, i);
      u[h] = g;
    } catch (g) {
      throw new Error(
        `failed to parse argument ${h} of case expression: ${g.message}`
      );
    }
    try {
      const g = Ti(r[h + 2], a.type, i);
      u[h + 1] = g;
    } catch (g) {
      throw new Error(
        `failed to parse argument ${h + 1} of case expression: ${g.message}`
      );
    }
  }
  return u[u.length - 1] = a, u;
}
function a3(r, t, i) {
  let a = r[2];
  if (!Array.isArray(a))
    throw new Error(
      'the second argument for the "in" operator must be an array'
    );
  let u;
  if (typeof a[0] == "string") {
    if (a[0] !== "literal")
      throw new Error(
        'for the "in" operator, a string array should be wrapped in a "literal" operator to disambiguate from expressions'
      );
    if (!Array.isArray(a[1]))
      throw new Error(
        'failed to parse "in" expression: the literal operator must be followed by an array'
      );
    a = a[1], u = kl;
  } else
    u = Se;
  const h = new Array(a.length);
  for (let y = 0; y < h.length; y++)
    try {
      const _ = Ti(a[y], u, i);
      h[y] = _;
    } catch (_) {
      throw new Error(
        `failed to parse haystack item ${y} for "in" expression: ${_.message}`
      );
    }
  return [Ti(r[1], u, i), ...h];
}
function s3(r, t, i) {
  let a;
  try {
    a = Ti(r[1], Se, i);
  } catch (g) {
    throw new Error(
      `failed to parse first argument in palette expression: ${g.message}`
    );
  }
  const u = r[2];
  if (!Array.isArray(u))
    throw new Error("the second argument of palette must be an array");
  const h = new Array(u.length);
  for (let g = 0; g < h.length; g++) {
    let y;
    try {
      y = Ti(u[g], Ws, i);
    } catch (_) {
      throw new Error(
        `failed to parse color at index ${g} in palette expression: ${_.message}`
      );
    }
    if (!(y instanceof ti))
      throw new Error(
        `the palette color at index ${g} must be a literal value`
      );
    h[g] = y;
  }
  return [a, ...h];
}
function Zt(...r) {
  return function(t, i, a) {
    const u = t[0];
    let h;
    for (let g = 0; g < r.length; g++) {
      const y = r[g](t, i, a);
      if (g == r.length - 1) {
        if (!y)
          throw new Error(
            "expected last argument validator to return the parsed args"
          );
        h = y;
      }
    }
    return new QO(i, u, ...h);
  };
}
function o3(r, t, i) {
  const a = r[0], u = WO[a];
  if (!u)
    throw new Error(`unknown operator: ${a}`);
  return u(r, t, i);
}
function uA(r) {
  if (!r)
    return "";
  const t = r.getType();
  switch (t) {
    case "Point":
    case "LineString":
    case "Polygon":
      return t;
    case "MultiPoint":
    case "MultiLineString":
    case "MultiPolygon":
      return (
        /** @type {'Point'|'LineString'|'Polygon'} */
        t.substring(5)
      );
    case "Circle":
      return "Polygon";
    case "GeometryCollection":
      return uA(
        /** @type {import("../geom/GeometryCollection.js").default} */
        r.getGeometries()[0]
      );
    default:
      return "";
  }
}
function cA() {
  return {
    variables: {},
    properties: {},
    resolution: NaN,
    featureId: null,
    geometryType: ""
  };
}
function Su(r, t, i) {
  const a = Ti(r, t, i);
  return eo(a);
}
function eo(r, t) {
  if (r instanceof ti) {
    if (r.type === Ws && typeof r.value == "string") {
      const a = xT(r.value);
      return function() {
        return a;
      };
    }
    return function() {
      return r.value;
    };
  }
  const i = r.operator;
  switch (i) {
    case mt.Number:
    case mt.String:
    case mt.Coalesce:
      return r3(r);
    case mt.Get:
    case mt.Var:
    case mt.Has:
      return u3(r);
    case mt.Id:
      return (a) => a.featureId;
    case mt.GeometryType:
      return (a) => a.geometryType;
    case mt.Concat: {
      const a = r.args.map((u) => eo(u));
      return (u) => "".concat(...a.map((h) => h(u).toString()));
    }
    case mt.Resolution:
      return (a) => a.resolution;
    case mt.Any:
    case mt.All:
    case mt.Between:
    case mt.In:
    case mt.Not:
      return f3(r);
    case mt.Equal:
    case mt.NotEqual:
    case mt.LessThan:
    case mt.LessThanOrEqualTo:
    case mt.GreaterThan:
    case mt.GreaterThanOrEqualTo:
      return c3(r);
    case mt.Multiply:
    case mt.Divide:
    case mt.Add:
    case mt.Subtract:
    case mt.Clamp:
    case mt.Mod:
    case mt.Pow:
    case mt.Abs:
    case mt.Floor:
    case mt.Ceil:
    case mt.Round:
    case mt.Sin:
    case mt.Cos:
    case mt.Atan:
    case mt.Sqrt:
      return h3(r);
    case mt.Case:
      return d3(r);
    case mt.Match:
      return g3(r);
    case mt.Interpolate:
      return m3(r);
    case mt.ToString:
      return y3(r);
    default:
      throw new Error(`Unsupported operator ${i}`);
  }
}
function r3(r, t) {
  const i = r.operator, a = r.args.length, u = new Array(a);
  for (let h = 0; h < a; ++h)
    u[h] = eo(r.args[h]);
  switch (i) {
    case mt.Coalesce:
      return (h) => {
        for (let g = 0; g < a; ++g) {
          const y = u[g](h);
          if (typeof y < "u" && y !== null)
            return y;
        }
        throw new Error("Expected one of the values to be non-null");
      };
    case mt.Number:
    case mt.String:
      return (h) => {
        for (let g = 0; g < a; ++g) {
          const y = u[g](h);
          if (typeof y === i)
            return y;
        }
        throw new Error(`Expected one of the values to be a ${i}`);
      };
    default:
      throw new Error(`Unsupported assertion operator ${i}`);
  }
}
function u3(r, t) {
  const a = (
    /** @type {string} */
    /** @type {LiteralExpression} */
    r.args[0].value
  );
  switch (r.operator) {
    case mt.Get:
      return (u) => {
        const h = r.args;
        let g = u.properties[a];
        for (let y = 1, _ = h.length; y < _; ++y) {
          const E = (
            /** @type {string|number} */
            /** @type {LiteralExpression} */
            h[y].value
          );
          g = g[E];
        }
        return g;
      };
    case mt.Var:
      return (u) => u.variables[a];
    case mt.Has:
      return (u) => {
        const h = r.args;
        if (!(a in u.properties))
          return !1;
        let g = u.properties[a];
        for (let y = 1, _ = h.length; y < _; ++y) {
          const E = (
            /** @type {string|number} */
            /** @type {LiteralExpression} */
            h[y].value
          );
          if (!g || !Object.hasOwn(g, E))
            return !1;
          g = g[E];
        }
        return !0;
      };
    default:
      throw new Error(`Unsupported accessor operator ${r.operator}`);
  }
}
function c3(r, t) {
  const i = r.operator, a = eo(r.args[0]), u = eo(r.args[1]);
  switch (i) {
    case mt.Equal:
      return (h) => a(h) === u(h);
    case mt.NotEqual:
      return (h) => a(h) !== u(h);
    case mt.LessThan:
      return (h) => a(h) < u(h);
    case mt.LessThanOrEqualTo:
      return (h) => a(h) <= u(h);
    case mt.GreaterThan:
      return (h) => a(h) > u(h);
    case mt.GreaterThanOrEqualTo:
      return (h) => a(h) >= u(h);
    default:
      throw new Error(`Unsupported comparison operator ${i}`);
  }
}
function f3(r, t) {
  const i = r.operator, a = r.args.length, u = new Array(a);
  for (let h = 0; h < a; ++h)
    u[h] = eo(r.args[h]);
  switch (i) {
    case mt.Any:
      return (h) => {
        for (let g = 0; g < a; ++g)
          if (u[g](h))
            return !0;
        return !1;
      };
    case mt.All:
      return (h) => {
        for (let g = 0; g < a; ++g)
          if (!u[g](h))
            return !1;
        return !0;
      };
    case mt.Between:
      return (h) => {
        const g = u[0](h), y = u[1](h), _ = u[2](h);
        return g >= y && g <= _;
      };
    case mt.In:
      return (h) => {
        const g = u[0](h);
        for (let y = 1; y < a; ++y)
          if (g === u[y](h))
            return !0;
        return !1;
      };
    case mt.Not:
      return (h) => !u[0](h);
    default:
      throw new Error(`Unsupported logical operator ${i}`);
  }
}
function h3(r, t) {
  const i = r.operator, a = r.args.length, u = new Array(a);
  for (let h = 0; h < a; ++h)
    u[h] = eo(r.args[h]);
  switch (i) {
    case mt.Multiply:
      return (h) => {
        let g = 1;
        for (let y = 0; y < a; ++y)
          g *= u[y](h);
        return g;
      };
    case mt.Divide:
      return (h) => u[0](h) / u[1](h);
    case mt.Add:
      return (h) => {
        let g = 0;
        for (let y = 0; y < a; ++y)
          g += u[y](h);
        return g;
      };
    case mt.Subtract:
      return (h) => u[0](h) - u[1](h);
    case mt.Clamp:
      return (h) => {
        const g = u[0](h), y = u[1](h);
        if (g < y)
          return y;
        const _ = u[2](h);
        return g > _ ? _ : g;
      };
    case mt.Mod:
      return (h) => u[0](h) % u[1](h);
    case mt.Pow:
      return (h) => Math.pow(u[0](h), u[1](h));
    case mt.Abs:
      return (h) => Math.abs(u[0](h));
    case mt.Floor:
      return (h) => Math.floor(u[0](h));
    case mt.Ceil:
      return (h) => Math.ceil(u[0](h));
    case mt.Round:
      return (h) => Math.round(u[0](h));
    case mt.Sin:
      return (h) => Math.sin(u[0](h));
    case mt.Cos:
      return (h) => Math.cos(u[0](h));
    case mt.Atan:
      return a === 2 ? (h) => Math.atan2(u[0](h), u[1](h)) : (h) => Math.atan(u[0](h));
    case mt.Sqrt:
      return (h) => Math.sqrt(u[0](h));
    default:
      throw new Error(`Unsupported numeric operator ${i}`);
  }
}
function d3(r, t) {
  const i = r.args.length, a = new Array(i);
  for (let u = 0; u < i; ++u)
    a[u] = eo(r.args[u]);
  return (u) => {
    for (let h = 0; h < i - 1; h += 2)
      if (a[h](u))
        return a[h + 1](u);
    return a[i - 1](u);
  };
}
function g3(r, t) {
  const i = r.args.length, a = new Array(i);
  for (let u = 0; u < i; ++u)
    a[u] = eo(r.args[u]);
  return (u) => {
    const h = a[0](u);
    for (let g = 1; g < i - 1; g += 2)
      if (h === a[g](u))
        return a[g + 1](u);
    return a[i - 1](u);
  };
}
function m3(r, t) {
  const i = r.args.length, a = new Array(i);
  for (let u = 0; u < i; ++u)
    a[u] = eo(r.args[u]);
  return (u) => {
    const h = a[0](u), g = a[1](u);
    let y, _;
    for (let b = 2; b < i; b += 2) {
      const E = a[b](u);
      let x = a[b + 1](u);
      const R = Array.isArray(x);
      if (R && (x = kD(x)), E >= g)
        return b === 2 ? x : R ? p3(
          h,
          g,
          y,
          _,
          E,
          x
        ) : o_(
          h,
          g,
          y,
          _,
          E,
          x
        );
      y = E, _ = x;
    }
    return _;
  };
}
function y3(r, t) {
  const i = r.operator, a = r.args.length, u = new Array(a);
  for (let h = 0; h < a; ++h)
    u[h] = eo(r.args[h]);
  switch (i) {
    case mt.ToString:
      return (h) => {
        const g = u[0](h);
        return r.args[0].type === Ws ? CT(g) : g.toString();
      };
    default:
      throw new Error(`Unsupported convert operator ${i}`);
  }
}
function o_(r, t, i, a, u, h) {
  const g = u - i;
  if (g === 0)
    return a;
  const y = t - i, _ = r === 1 ? y / g : (Math.pow(r, y) - 1) / (Math.pow(r, g) - 1);
  return a + _ * (h - a);
}
function p3(r, t, i, a, u, h) {
  if (u - i === 0)
    return a;
  const y = ER(a), _ = ER(h);
  let b = _[2] - y[2];
  b > 180 ? b -= 360 : b < -180 && (b += 360);
  const E = [
    o_(r, t, i, y[0], u, _[0]),
    o_(r, t, i, y[1], u, _[1]),
    y[2] + o_(r, t, i, 0, u, b),
    o_(r, t, i, a[3], u, h[3])
  ];
  return VD(E);
}
function _3(r) {
  return !0;
}
function v3(r) {
  const t = rA(), i = S3(r, t), a = cA();
  return function(u, h) {
    if (a.properties = u.getPropertiesInternal(), a.resolution = h, t.featureId) {
      const g = u.getId();
      g !== void 0 ? a.featureId = g : a.featureId = null;
    }
    return t.geometryType && (a.geometryType = uA(
      u.getGeometry()
    )), i(a);
  };
}
function kR(r) {
  const t = rA(), i = r.length, a = new Array(i);
  for (let g = 0; g < i; ++g)
    a[g] = kb(r[g], t);
  const u = cA(), h = new Array(i);
  return function(g, y) {
    if (u.properties = g.getPropertiesInternal(), u.resolution = y, t.featureId) {
      const b = g.getId();
      b !== void 0 ? u.featureId = b : u.featureId = null;
    }
    let _ = 0;
    for (let b = 0; b < i; ++b) {
      const E = a[b](u);
      E && (h[_] = E, _ += 1);
    }
    return h.length = _, h;
  };
}
function S3(r, t) {
  const i = r.length, a = new Array(i);
  for (let u = 0; u < i; ++u) {
    const h = r[u], g = "filter" in h ? Su(h.filter, gl, t) : _3;
    let y;
    if (Array.isArray(h.style)) {
      const _ = h.style.length;
      y = new Array(_);
      for (let b = 0; b < _; ++b)
        y[b] = kb(h.style[b], t);
    } else
      y = [kb(h.style, t)];
    a[u] = { filter: g, styles: y };
  }
  return function(u) {
    const h = [];
    let g = !1;
    for (let y = 0; y < i; ++y) {
      const _ = a[y].filter;
      if (_(u) && !(r[y].else && g)) {
        g = !0;
        for (const b of a[y].styles) {
          const E = b(u);
          E && h.push(E);
        }
      }
    }
    return h;
  };
}
function kb(r, t) {
  const i = C_(r, "", t), a = R_(r, "", t), u = E3(r, t), h = b3(r, t), g = Vl(r, "z-index", t);
  if (!i && !a && !u && !h && !Zm(r))
    throw new Error(
      "No fill, stroke, point, or text symbolizer properties in style: " + JSON.stringify(r)
    );
  const y = new pa();
  return function(_) {
    let b = !0;
    if (i) {
      const E = i(_);
      E && (b = !1), y.setFill(E);
    }
    if (a) {
      const E = a(_);
      E && (b = !1), y.setStroke(E);
    }
    if (u) {
      const E = u(_);
      E && (b = !1), y.setText(E);
    }
    if (h) {
      const E = h(_);
      E && (b = !1), y.setImage(E);
    }
    return g && y.setZIndex(g(_)), b ? null : y;
  };
}
function C_(r, t, i) {
  let a;
  if (t + "fill-pattern-src" in r)
    a = R3(r, t + "fill-", i);
  else {
    if (r[t + "fill-color"] === "none")
      return (h) => null;
    a = OT(
      r,
      t + "fill-color",
      i
    );
  }
  if (!a)
    return null;
  const u = new Qc();
  return function(h) {
    const g = a(h);
    return g === TT ? null : (u.setColor(g), u);
  };
}
function R_(r, t, i) {
  const a = Vl(
    r,
    t + "stroke-width",
    i
  ), u = OT(
    r,
    t + "stroke-color",
    i
  );
  if (!a && !u)
    return null;
  const h = cu(
    r,
    t + "stroke-line-cap",
    i
  ), g = cu(
    r,
    t + "stroke-line-join",
    i
  ), y = fA(
    r,
    t + "stroke-line-dash",
    i
  ), _ = Vl(
    r,
    t + "stroke-line-dash-offset",
    i
  ), b = Vl(
    r,
    t + "stroke-miter-limit",
    i
  ), E = new pu();
  return function(x) {
    if (u) {
      const R = u(x);
      if (R === TT)
        return null;
      E.setColor(R);
    }
    if (a && E.setWidth(a(x)), h) {
      const R = h(x);
      if (R !== "butt" && R !== "round" && R !== "square")
        throw new Error("Expected butt, round, or square line cap");
      E.setLineCap(R);
    }
    if (g) {
      const R = g(x);
      if (R !== "bevel" && R !== "round" && R !== "miter")
        throw new Error("Expected bevel, round, or miter line join");
      E.setLineJoin(R);
    }
    return y && E.setLineDash(y(x)), _ && E.setLineDashOffset(_(x)), b && E.setMiterLimit(b(x)), E;
  };
}
function E3(r, t) {
  const i = "text-", a = cu(r, i + "value", t);
  if (!a)
    return null;
  const u = C_(r, i, t), h = C_(
    r,
    i + "background-",
    t
  ), g = R_(r, i, t), y = R_(
    r,
    i + "background-",
    t
  ), _ = cu(r, i + "font", t), b = Vl(
    r,
    i + "max-angle",
    t
  ), E = Vl(
    r,
    i + "offset-x",
    t
  ), x = Vl(
    r,
    i + "offset-y",
    t
  ), R = Xm(
    r,
    i + "overflow",
    t
  ), w = cu(
    r,
    i + "placement",
    t
  ), L = Vl(r, i + "repeat", t), H = CS(r, i + "scale", t), N = Xm(
    r,
    i + "rotate-with-view",
    t
  ), O = Vl(
    r,
    i + "rotation",
    t
  ), U = cu(r, i + "align", t), I = cu(
    r,
    i + "justify",
    t
  ), X = cu(
    r,
    i + "baseline",
    t
  ), Z = Xm(
    r,
    i + "keep-upright",
    t
  ), dt = fA(
    r,
    i + "padding",
    t
  ), ft = RS(
    r,
    i + "declutter-mode"
  ), yt = new wT({ declutterMode: ft });
  return function(it) {
    if (yt.setText(a(it)), u && yt.setFill(u(it)), h && yt.setBackgroundFill(h(it)), g && yt.setStroke(g(it)), y && yt.setBackgroundStroke(y(it)), _ && yt.setFont(_(it)), b && yt.setMaxAngle(b(it)), E && yt.setOffsetX(E(it)), x && yt.setOffsetY(x(it)), R && yt.setOverflow(R(it)), w) {
      const Et = w(it);
      if (Et !== "point" && Et !== "line")
        throw new Error("Expected point or line for text-placement");
      yt.setPlacement(Et);
    }
    if (L && yt.setRepeat(L(it)), H && yt.setScale(H(it)), N && yt.setRotateWithView(N(it)), O && yt.setRotation(O(it)), U) {
      const Et = U(it);
      if (Et !== "left" && Et !== "center" && Et !== "right" && Et !== "end" && Et !== "start")
        throw new Error(
          "Expected left, right, center, start, or end for text-align"
        );
      yt.setTextAlign(Et);
    }
    if (I) {
      const Et = I(it);
      if (Et !== "left" && Et !== "right" && Et !== "center")
        throw new Error("Expected left, right, or center for text-justify");
      yt.setJustify(Et);
    }
    if (X) {
      const Et = X(it);
      if (Et !== "bottom" && Et !== "top" && Et !== "middle" && Et !== "alphabetic" && Et !== "hanging")
        throw new Error(
          "Expected bottom, top, middle, alphabetic, or hanging for text-baseline"
        );
      yt.setTextBaseline(Et);
    }
    return dt && yt.setPadding(dt(it)), Z && yt.setKeepUpright(Z(it)), yt;
  };
}
function b3(r, t) {
  return "icon-src" in r ? T3(r, t) : "shape-points" in r ? x3(r, t) : "circle-radius" in r ? C3(r, t) : null;
}
function T3(r, t) {
  const i = "icon-", a = i + "src", u = hA(r[a], a), h = tS(
    r,
    i + "anchor",
    t
  ), g = CS(r, i + "scale", t), y = Vl(
    r,
    i + "opacity",
    t
  ), _ = tS(
    r,
    i + "displacement",
    t
  ), b = Vl(
    r,
    i + "rotation",
    t
  ), E = Xm(
    r,
    i + "rotate-with-view",
    t
  ), x = qR(r, i + "anchor-origin"), R = ZR(
    r,
    i + "anchor-x-units"
  ), w = ZR(
    r,
    i + "anchor-y-units"
  ), L = D3(r, i + "color"), H = M3(r, i + "cross-origin"), N = w3(r, i + "offset"), O = qR(r, i + "offset-origin"), U = eS(r, i + "width"), I = eS(r, i + "height"), X = A3(r, i + "size"), Z = RS(
    r,
    i + "declutter-mode"
  ), dt = new xS({
    src: u,
    anchorOrigin: x,
    anchorXUnits: R,
    anchorYUnits: w,
    color: L,
    crossOrigin: H,
    offset: N,
    offsetOrigin: O,
    height: I,
    width: U,
    size: X,
    declutterMode: Z
  });
  return function(ft) {
    return y && dt.setOpacity(y(ft)), _ && dt.setDisplacement(_(ft)), b && dt.setRotation(b(ft)), E && dt.setRotateWithView(E(ft)), g && dt.setScale(g(ft)), h && dt.setAnchor(h(ft)), dt;
  };
}
function x3(r, t) {
  const i = "shape-", a = i + "points", u = i + "radius", h = Vb(r[a], a), g = Vb(r[u], u), y = C_(r, i, t), _ = R_(r, i, t), b = CS(r, i + "scale", t), E = tS(
    r,
    i + "displacement",
    t
  ), x = Vl(
    r,
    i + "rotation",
    t
  ), R = Xm(
    r,
    i + "rotate-with-view",
    t
  ), w = eS(r, i + "radius2"), L = eS(r, i + "angle"), H = RS(
    r,
    i + "declutter-mode"
  ), N = new TS({
    points: h,
    radius: g,
    radius2: w,
    angle: L,
    declutterMode: H
  });
  return function(O) {
    return y && N.setFill(y(O)), _ && N.setStroke(_(O)), E && N.setDisplacement(E(O)), x && N.setRotation(x(O)), R && N.setRotateWithView(R(O)), b && N.setScale(b(O)), N;
  };
}
function C3(r, t) {
  const i = "circle-", a = C_(r, i, t), u = R_(r, i, t), h = Vl(r, i + "radius", t), g = CS(r, i + "scale", t), y = tS(
    r,
    i + "displacement",
    t
  ), _ = Vl(
    r,
    i + "rotation",
    t
  ), b = Xm(
    r,
    i + "rotate-with-view",
    t
  ), E = RS(
    r,
    i + "declutter-mode"
  ), x = new L_({
    radius: 5,
    // this is arbitrary, but required - the evaluated radius is used below
    declutterMode: E
  });
  return function(R) {
    return h && x.setRadius(h(R)), a && x.setFill(a(R)), u && x.setStroke(u(R)), y && x.setDisplacement(y(R)), _ && x.setRotation(_(R)), b && x.setRotateWithView(b(R)), g && x.setScale(g(R)), x;
  };
}
function Vl(r, t, i) {
  if (!(t in r))
    return;
  const a = Su(r[t], Se, i);
  return function(u) {
    return Vb(a(u), t);
  };
}
function cu(r, t, i) {
  if (!(t in r))
    return null;
  const a = Su(r[t], kl, i);
  return function(u) {
    return hA(a(u), t);
  };
}
function R3(r, t, i) {
  const a = cu(
    r,
    t + "pattern-src",
    i
  ), u = VR(
    r,
    t + "pattern-offset",
    i
  ), h = VR(
    r,
    t + "pattern-size",
    i
  ), g = OT(
    r,
    t + "color",
    i
  );
  return function(y) {
    return {
      src: a(y),
      offset: u && u(y),
      size: h && h(y),
      color: g && g(y)
    };
  };
}
function Xm(r, t, i) {
  if (!(t in r))
    return null;
  const a = Su(r[t], gl, i);
  return function(u) {
    const h = a(u);
    if (typeof h != "boolean")
      throw new Error(`Expected a boolean for ${t}`);
    return h;
  };
}
function OT(r, t, i) {
  if (!(t in r))
    return null;
  const a = Su(r[t], Ws, i);
  return function(u) {
    return dA(a(u), t);
  };
}
function fA(r, t, i) {
  if (!(t in r))
    return null;
  const a = Su(r[t], $h, i);
  return function(u) {
    return N_(a(u), t);
  };
}
function tS(r, t, i) {
  if (!(t in r))
    return null;
  const a = Su(r[t], $h, i);
  return function(u) {
    const h = N_(a(u), t);
    if (h.length !== 2)
      throw new Error(`Expected two numbers for ${t}`);
    return h;
  };
}
function VR(r, t, i) {
  if (!(t in r))
    return null;
  const a = Su(r[t], $h, i);
  return function(u) {
    return gA(a(u), t);
  };
}
function CS(r, t, i) {
  if (!(t in r))
    return null;
  const a = Su(
    r[t],
    $h | Se,
    i
  );
  return function(u) {
    return O3(a(u), t);
  };
}
function eS(r, t) {
  const i = r[t];
  if (i !== void 0) {
    if (typeof i != "number")
      throw new Error(`Expected a number for ${t}`);
    return i;
  }
}
function A3(r, t) {
  const i = r[t];
  if (i !== void 0) {
    if (typeof i == "number")
      return Zl(i);
    if (!Array.isArray(i))
      throw new Error(`Expected a number or size array for ${t}`);
    if (i.length !== 2 || typeof i[0] != "number" || typeof i[1] != "number")
      throw new Error(`Expected a number or size array for ${t}`);
    return i;
  }
}
function M3(r, t) {
  const i = r[t];
  if (i !== void 0) {
    if (typeof i != "string")
      throw new Error(`Expected a string for ${t}`);
    return i;
  }
}
function qR(r, t) {
  const i = r[t];
  if (i !== void 0) {
    if (i !== "bottom-left" && i !== "bottom-right" && i !== "top-left" && i !== "top-right")
      throw new Error(
        `Expected bottom-left, bottom-right, top-left, or top-right for ${t}`
      );
    return i;
  }
}
function ZR(r, t) {
  const i = r[t];
  if (i !== void 0) {
    if (i !== "pixels" && i !== "fraction")
      throw new Error(`Expected pixels or fraction for ${t}`);
    return i;
  }
}
function w3(r, t) {
  const i = r[t];
  if (i !== void 0)
    return N_(i, t);
}
function RS(r, t) {
  const i = r[t];
  if (i !== void 0) {
    if (typeof i != "string")
      throw new Error(`Expected a string for ${t}`);
    if (i !== "declutter" && i !== "obstacle" && i !== "none")
      throw new Error(`Expected declutter, obstacle, or none for ${t}`);
    return i;
  }
}
function D3(r, t) {
  const i = r[t];
  if (i !== void 0)
    return dA(i, t);
}
function N_(r, t) {
  if (!Array.isArray(r))
    throw new Error(`Expected an array for ${t}`);
  const i = r.length;
  for (let a = 0; a < i; ++a)
    if (typeof r[a] != "number")
      throw new Error(`Expected an array of numbers for ${t}`);
  return r;
}
function hA(r, t) {
  if (typeof r != "string")
    throw new Error(`Expected a string for ${t}`);
  return r;
}
function Vb(r, t) {
  if (typeof r != "number")
    throw new Error(`Expected a number for ${t}`);
  return r;
}
function dA(r, t) {
  if (typeof r == "string")
    return r;
  const i = N_(r, t), a = i.length;
  if (a < 3 || a > 4)
    throw new Error(`Expected a color with 3 or 4 values for ${t}`);
  return i;
}
function gA(r, t) {
  const i = N_(r, t);
  if (i.length !== 2)
    throw new Error(`Expected an array of two numbers for ${t}`);
  return i;
}
function O3(r, t) {
  return typeof r == "number" ? r : gA(r, t);
}
const Ps = {
  CENTER: "center",
  RESOLUTION: "resolution",
  ROTATION: "rotation"
};
function FR(r, t, i) {
  return (
    /**
     * @param {import("./coordinate.js").Coordinate|undefined} center Center.
     * @param {number|undefined} resolution Resolution.
     * @param {import("./size.js").Size} size Viewport size; unused if `onlyCenter` was specified.
     * @param {boolean} [isMoving] True if an interaction or animation is in progress.
     * @param {Array<number>} [centerShift] Shift between map center and viewport center.
     * @return {import("./coordinate.js").Coordinate|undefined} Center.
     */
    function(a, u, h, g, y) {
      if (!a)
        return;
      if (!u && !t)
        return a;
      const _ = t ? 0 : h[0] * u, b = t ? 0 : h[1] * u, E = y ? y[0] : 0, x = y ? y[1] : 0;
      let R = r[0] + _ / 2 + E, w = r[2] - _ / 2 + E, L = r[1] + b / 2 + x, H = r[3] - b / 2 + x;
      R > w && (R = (w + R) / 2, w = R), L > H && (L = (H + L) / 2, H = L);
      let N = gn(a[0], R, w), O = gn(a[1], L, H);
      if (g && i && u) {
        const U = 30 * u;
        N += -U * Math.log(1 + Math.max(0, R - a[0]) / U) + U * Math.log(1 + Math.max(0, a[0] - w) / U), O += -U * Math.log(1 + Math.max(0, L - a[1]) / U) + U * Math.log(1 + Math.max(0, a[1] - H) / U);
      }
      return [N, O];
    }
  );
}
function L3(r) {
  return r;
}
function mA(r) {
  return Math.pow(r, 3);
}
function ty(r) {
  return 1 - mA(1 - r);
}
function z3(r) {
  return 3 * r * r - 2 * r * r * r;
}
function N3(r) {
  return r;
}
function LT(r, t, i, a) {
  const u = Xe(t) / i[0], h = ml(t) / i[1];
  return a ? Math.min(r, Math.max(u, h)) : Math.min(r, Math.min(u, h));
}
function zT(r, t, i) {
  let a = Math.min(r, t);
  const u = 50;
  return a *= Math.log(1 + u * Math.max(0, r / t - 1)) / u + 1, i && (a = Math.max(a, i), a /= Math.log(1 + u * Math.max(0, i / r - 1)) / u + 1), gn(a, i / 2, t * 2);
}
function U3(r, t, i, a) {
  return t = t !== void 0 ? t : !0, /**
   * @param {number|undefined} resolution Resolution.
   * @param {number} direction Direction.
   * @param {import("./size.js").Size} size Viewport size.
   * @param {boolean} [isMoving] True if an interaction or animation is in progress.
   * @return {number|undefined} Resolution.
   */
  function(u, h, g, y) {
    if (u !== void 0) {
      const _ = r[0], b = r[r.length - 1], E = i ? LT(
        _,
        i,
        g,
        a
      ) : _;
      if (y)
        return t ? zT(
          u,
          E,
          b
        ) : gn(u, b, E);
      const x = Math.min(E, u), R = Math.floor(tT(r, x, h));
      return r[R] > E && R < r.length - 1 ? r[R + 1] : r[R];
    }
  };
}
function G3(r, t, i, a, u, h) {
  return a = a !== void 0 ? a : !0, i = i !== void 0 ? i : 0, /**
   * @param {number|undefined} resolution Resolution.
   * @param {number} direction Direction.
   * @param {import("./size.js").Size} size Viewport size.
   * @param {boolean} [isMoving] True if an interaction or animation is in progress.
   * @return {number|undefined} Resolution.
   */
  function(g, y, _, b) {
    if (g !== void 0) {
      const E = u ? LT(
        t,
        u,
        _,
        h
      ) : t;
      if (b)
        return a ? zT(
          g,
          E,
          i
        ) : gn(g, i, E);
      const x = 1e-9, R = Math.ceil(
        Math.log(t / E) / Math.log(r) - x
      ), w = -y * (0.5 - x) + 0.5, L = Math.min(E, g), H = Math.floor(
        Math.log(t / L) / Math.log(r) + w
      ), N = Math.max(R, H), O = t / Math.pow(r, N);
      return gn(O, i, E);
    }
  };
}
function KR(r, t, i, a, u) {
  return i = i !== void 0 ? i : !0, /**
   * @param {number|undefined} resolution Resolution.
   * @param {number} direction Direction.
   * @param {import("./size.js").Size} size Viewport size.
   * @param {boolean} [isMoving] True if an interaction or animation is in progress.
   * @return {number|undefined} Resolution.
   */
  function(h, g, y, _) {
    if (h !== void 0) {
      const b = a ? LT(
        r,
        a,
        y,
        u
      ) : r;
      return !i || !_ ? gn(h, t, b) : zT(
        h,
        b,
        t
      );
    }
  };
}
function NT(r) {
  if (r !== void 0)
    return 0;
}
function PR(r) {
  if (r !== void 0)
    return r;
}
function H3(r) {
  const t = 2 * Math.PI / r;
  return (
    /**
     * @param {number|undefined} rotation Rotation.
     * @param {boolean} [isMoving] True if an interaction or animation is in progress.
     * @return {number|undefined} Rotation.
     */
    function(i, a) {
      if (a)
        return i;
      if (i !== void 0)
        return i = Math.floor(i / t + 0.5) * t, i;
    }
  );
}
function Y3(r) {
  const t = Fc(5);
  return (
    /**
     * @param {number|undefined} rotation Rotation.
     * @param {boolean} [isMoving] True if an interaction or animation is in progress.
     * @return {number|undefined} Rotation.
     */
    function(i, a) {
      return a || i === void 0 ? i : Math.abs(i) <= t ? 0 : i;
    }
  );
}
const j3 = 42, UT = 256, db = 0;
class Wo extends tr {
  /**
   * @param {ViewOptions} [options] View options.
   */
  constructor(t) {
    super(), this.on, this.once, this.un, t = Object.assign({}, t), this.hints_ = [0, 0], this.animations_ = [], this.updateAnimationKey_, this.projection_ = cT(t.projection, "EPSG:3857"), this.viewportSize_ = [100, 100], this.targetCenter_ = null, this.targetResolution_, this.targetRotation_, this.nextCenter_ = null, this.nextResolution_, this.nextRotation_, this.cancelAnchor_ = void 0, t.projection && Ww(), t.center && (t.center = dl(t.center, this.projection_)), t.extent && (t.extent = Vc(t.extent, this.projection_)), this.applyOptions_(t);
  }
  /**
   * Set up the view with the given options.
   * @param {ViewOptions} options View options.
   */
  applyOptions_(t) {
    const i = Object.assign({}, t);
    for (const y in Ps)
      delete i[y];
    this.setProperties(i, !0);
    const a = I3(t);
    this.maxResolution_ = a.maxResolution, this.minResolution_ = a.minResolution, this.zoomFactor_ = a.zoomFactor, this.resolutions_ = t.resolutions, this.padding_ = t.padding, this.minZoom_ = a.minZoom;
    const u = B3(t), h = a.constraint, g = X3(t);
    this.constraints_ = {
      center: u,
      resolution: h,
      rotation: g
    }, this.setRotation(t.rotation !== void 0 ? t.rotation : 0), this.setCenterInternal(
      t.center !== void 0 ? t.center : null
    ), t.resolution !== void 0 ? this.setResolution(t.resolution) : t.zoom !== void 0 && this.setZoom(t.zoom);
  }
  /**
   * Padding (in css pixels).
   * If the map viewport is partially covered with other content (overlays) along
   * its edges, this setting allows to shift the center of the viewport away from that
   * content. The order of the values in the array is top, right, bottom, left.
   * The default is no padding, which is equivalent to `[0, 0, 0, 0]`.
   * @type {Array<number>|undefined}
   * @api
   */
  get padding() {
    return this.padding_;
  }
  set padding(t) {
    let i = this.padding_;
    this.padding_ = t;
    const a = this.getCenterInternal();
    if (a) {
      const u = t || [0, 0, 0, 0];
      i = i || [0, 0, 0, 0];
      const h = this.getResolution(), g = h / 2 * (u[3] - i[3] + i[1] - u[1]), y = h / 2 * (u[0] - i[0] + i[2] - u[2]);
      this.setCenterInternal([a[0] + g, a[1] - y]);
    }
  }
  /**
   * Get an updated version of the view options used to construct the view.  The
   * current resolution (or zoom), center, and rotation are applied to any stored
   * options.  The provided options can be used to apply new min/max zoom or
   * resolution limits.
   * @param {ViewOptions} newOptions New options to be applied.
   * @return {ViewOptions} New options updated with the current view state.
   */
  getUpdatedOptions_(t) {
    const i = this.getProperties();
    return i.resolution !== void 0 ? i.resolution = this.getResolution() : i.zoom = this.getZoom(), i.center = this.getCenterInternal(), i.rotation = this.getRotation(), Object.assign({}, i, t);
  }
  /**
   * Animate the view.  The view's center, zoom (or resolution), and rotation
   * can be animated for smooth transitions between view states.  For example,
   * to animate the view to a new zoom level:
   *
   *     view.animate({zoom: view.getZoom() + 1});
   *
   * By default, the animation lasts one second and uses in-and-out easing.  You
   * can customize this behavior by including `duration` (in milliseconds) and
   * `easing` options (see {@link module:ol/easing}).
   *
   * To chain together multiple animations, call the method with multiple
   * animation objects.  For example, to first zoom and then pan:
   *
   *     view.animate({zoom: 10}, {center: [0, 0]});
   *
   * If you provide a function as the last argument to the animate method, it
   * will get called at the end of an animation series.  The callback will be
   * called with `true` if the animation series completed on its own or `false`
   * if it was cancelled.
   *
   * Animations are cancelled by user interactions (e.g. dragging the map) or by
   * calling `view.setCenter()`, `view.setResolution()`, or `view.setRotation()`
   * (or another method that calls one of these).
   *
   * @param {...(AnimationOptions|function(boolean): void)} var_args Animation
   *     options.  Multiple animations can be run in series by passing multiple
   *     options objects.  To run multiple animations in parallel, call the method
   *     multiple times.  An optional callback can be provided as a final
   *     argument.  The callback will be called with a boolean indicating whether
   *     the animation completed without being cancelled.
   * @api
   */
  animate(t) {
    this.isDef() && !this.getAnimating() && this.resolveConstraints(0);
    const i = new Array(arguments.length);
    for (let a = 0; a < i.length; ++a) {
      let u = arguments[a];
      u.center && (u = Object.assign({}, u), u.center = dl(
        u.center,
        this.getProjection()
      )), u.anchor && (u = Object.assign({}, u), u.anchor = dl(
        u.anchor,
        this.getProjection()
      )), i[a] = u;
    }
    this.animateInternal.apply(this, i);
  }
  /**
   * @param {...(AnimationOptions|function(boolean): void)} var_args Animation options.
   */
  animateInternal(t) {
    let i = arguments.length, a;
    i > 1 && typeof arguments[i - 1] == "function" && (a = arguments[i - 1], --i);
    let u = 0;
    for (; u < i && !this.isDef(); ++u) {
      const E = arguments[u];
      E.center && this.setCenterInternal(E.center), E.zoom !== void 0 ? this.setZoom(E.zoom) : E.resolution && this.setResolution(E.resolution), E.rotation !== void 0 && this.setRotation(E.rotation);
    }
    if (u === i) {
      a && U1(a, !0);
      return;
    }
    let h = Date.now(), g = this.targetCenter_.slice(), y = this.targetResolution_, _ = this.targetRotation_;
    const b = [];
    for (; u < i; ++u) {
      const E = (
        /** @type {AnimationOptions} */
        arguments[u]
      ), x = {
        start: h,
        complete: !1,
        anchor: E.anchor,
        duration: E.duration !== void 0 ? E.duration : 1e3,
        easing: E.easing || z3,
        callback: a
      };
      if (E.center && (x.sourceCenter = g, x.targetCenter = E.center.slice(), g = x.targetCenter), E.zoom !== void 0 ? (x.sourceResolution = y, x.targetResolution = this.getResolutionForZoom(E.zoom), y = x.targetResolution) : E.resolution && (x.sourceResolution = y, x.targetResolution = E.resolution, y = x.targetResolution), E.rotation !== void 0) {
        x.sourceRotation = _;
        const R = Qh(E.rotation - _ + Math.PI, 2 * Math.PI) - Math.PI;
        x.targetRotation = _ + R, _ = x.targetRotation;
      }
      k3(x) ? x.complete = !0 : h += x.duration, b.push(x);
    }
    this.animations_.push(b), this.setHint(Vi.ANIMATING, 1), this.updateAnimations_();
  }
  /**
   * Determine if the view is being animated.
   * @return {boolean} The view is being animated.
   * @api
   */
  getAnimating() {
    return this.hints_[Vi.ANIMATING] > 0;
  }
  /**
   * Determine if the user is interacting with the view, such as panning or zooming.
   * @return {boolean} The view is being interacted with.
   * @api
   */
  getInteracting() {
    return this.hints_[Vi.INTERACTING] > 0;
  }
  /**
   * Cancel any ongoing animations.
   * @api
   */
  cancelAnimations() {
    this.setHint(Vi.ANIMATING, -this.hints_[Vi.ANIMATING]);
    let t;
    for (let i = 0, a = this.animations_.length; i < a; ++i) {
      const u = this.animations_[i];
      if (u[0].callback && U1(u[0].callback, !1), !t)
        for (let h = 0, g = u.length; h < g; ++h) {
          const y = u[h];
          if (!y.complete) {
            t = y.anchor;
            break;
          }
        }
    }
    this.animations_.length = 0, this.cancelAnchor_ = t, this.nextCenter_ = null, this.nextResolution_ = NaN, this.nextRotation_ = NaN;
  }
  /**
   * Update all animations.
   */
  updateAnimations_() {
    if (this.updateAnimationKey_ !== void 0 && (cancelAnimationFrame(this.updateAnimationKey_), this.updateAnimationKey_ = void 0), !this.getAnimating())
      return;
    const t = Date.now();
    let i = !1;
    for (let a = this.animations_.length - 1; a >= 0; --a) {
      const u = this.animations_[a];
      let h = !0;
      for (let g = 0, y = u.length; g < y; ++g) {
        const _ = u[g];
        if (_.complete)
          continue;
        const b = t - _.start;
        let E = _.duration > 0 ? b / _.duration : 1;
        E >= 1 ? (_.complete = !0, E = 1) : h = !1;
        const x = _.easing(E);
        if (_.sourceCenter) {
          const R = _.sourceCenter[0], w = _.sourceCenter[1], L = _.targetCenter[0], H = _.targetCenter[1];
          this.nextCenter_ = _.targetCenter;
          const N = R + x * (L - R), O = w + x * (H - w);
          this.targetCenter_ = [N, O];
        }
        if (_.sourceResolution && _.targetResolution) {
          const R = x === 1 ? _.targetResolution : _.sourceResolution + x * (_.targetResolution - _.sourceResolution);
          if (_.anchor) {
            const w = this.getViewportSize_(this.getRotation()), L = this.constraints_.resolution(
              R,
              0,
              w,
              !0
            );
            this.targetCenter_ = this.calculateCenterZoom(
              L,
              _.anchor
            );
          }
          this.nextResolution_ = _.targetResolution, this.targetResolution_ = R, this.applyTargetState_(!0);
        }
        if (_.sourceRotation !== void 0 && _.targetRotation !== void 0) {
          const R = x === 1 ? Qh(_.targetRotation + Math.PI, 2 * Math.PI) - Math.PI : _.sourceRotation + x * (_.targetRotation - _.sourceRotation);
          if (_.anchor) {
            const w = this.constraints_.rotation(
              R,
              !0
            );
            this.targetCenter_ = this.calculateCenterRotate(
              w,
              _.anchor
            );
          }
          this.nextRotation_ = _.targetRotation, this.targetRotation_ = R;
        }
        if (this.applyTargetState_(!0), i = !0, !_.complete)
          break;
      }
      if (h) {
        this.animations_[a] = null, this.setHint(Vi.ANIMATING, -1), this.nextCenter_ = null, this.nextResolution_ = NaN, this.nextRotation_ = NaN;
        const g = u[0].callback;
        g && U1(g, !0);
      }
    }
    this.animations_ = this.animations_.filter(Boolean), i && this.updateAnimationKey_ === void 0 && (this.updateAnimationKey_ = requestAnimationFrame(
      this.updateAnimations_.bind(this)
    ));
  }
  /**
   * @param {number} rotation Target rotation.
   * @param {import("./coordinate.js").Coordinate} anchor Rotation anchor.
   * @return {import("./coordinate.js").Coordinate|undefined} Center for rotation and anchor.
   */
  calculateCenterRotate(t, i) {
    let a;
    const u = this.getCenterInternal();
    return u !== void 0 && (a = [u[0] - i[0], u[1] - i[1]], lT(a, t - this.getRotation()), bw(a, i)), a;
  }
  /**
   * @param {number} resolution Target resolution.
   * @param {import("./coordinate.js").Coordinate} anchor Zoom anchor.
   * @return {import("./coordinate.js").Coordinate|undefined} Center for resolution and anchor.
   */
  calculateCenterZoom(t, i) {
    let a;
    const u = this.getCenterInternal(), h = this.getResolution();
    if (u !== void 0 && h !== void 0) {
      const g = i[0] - t * (i[0] - u[0]) / h, y = i[1] - t * (i[1] - u[1]) / h;
      a = [g, y];
    }
    return a;
  }
  /**
   * Returns the current viewport size.
   * @private
   * @param {number} [rotation] Take into account the rotation of the viewport when giving the size
   * @return {import("./size.js").Size} Viewport size or `[100, 100]` when no viewport is found.
   */
  getViewportSize_(t) {
    const i = this.viewportSize_;
    if (t) {
      const a = i[0], u = i[1];
      return [
        Math.abs(a * Math.cos(t)) + Math.abs(u * Math.sin(t)),
        Math.abs(a * Math.sin(t)) + Math.abs(u * Math.cos(t))
      ];
    }
    return i;
  }
  /**
   * Stores the viewport size on the view. The viewport size is not read every time from the DOM
   * to avoid performance hit and layout reflow.
   * This should be done on map size change.
   * Note: the constraints are not resolved during an animation to avoid stopping it
   * @param {import("./size.js").Size} [size] Viewport size; if undefined, [100, 100] is assumed
   */
  setViewportSize(t) {
    this.viewportSize_ = Array.isArray(t) ? t.slice() : [100, 100], this.getAnimating() || this.resolveConstraints(0);
  }
  /**
   * Get the view center.
   * @return {import("./coordinate.js").Coordinate|undefined} The center of the view.
   * @observable
   * @api
   */
  getCenter() {
    const t = this.getCenterInternal();
    return t && F1(t, this.getProjection());
  }
  /**
   * Get the view center without transforming to user projection.
   * @return {import("./coordinate.js").Coordinate|undefined} The center of the view.
   */
  getCenterInternal() {
    return (
      /** @type {import("./coordinate.js").Coordinate|undefined} */
      this.get(Ps.CENTER)
    );
  }
  /**
   * @return {Constraints} Constraints.
   */
  getConstraints() {
    return this.constraints_;
  }
  /**
   * @return {boolean} Resolution constraint is set
   */
  getConstrainResolution() {
    return this.get("constrainResolution");
  }
  /**
   * @param {Array<number>} [hints] Destination array.
   * @return {Array<number>} Hint.
   */
  getHints(t) {
    return t !== void 0 ? (t[0] = this.hints_[0], t[1] = this.hints_[1], t) : this.hints_.slice();
  }
  /**
   * Calculate the extent for the current view state and the passed box size.
   * @param {import("./size.js").Size} [size] The pixel dimensions of the box
   * into which the calculated extent should fit. Defaults to the size of the
   * map the view is associated with.
   * If no map or multiple maps are connected to the view, provide the desired
   * box size (e.g. `map.getSize()`).
   * @return {import("./extent.js").Extent} Extent.
   * @api
   */
  calculateExtent(t) {
    const i = this.calculateExtentInternal(t);
    return y_(i, this.getProjection());
  }
  /**
   * @param {import("./size.js").Size} [size] Box pixel size. If not provided,
   * the map's last known viewport size will be used.
   * @return {import("./extent.js").Extent} Extent.
   */
  calculateExtentInternal(t) {
    t = t || this.getViewportSizeMinusPadding_();
    const i = (
      /** @type {!import("./coordinate.js").Coordinate} */
      this.getCenterInternal()
    );
    je(i, "The view center is not defined");
    const a = (
      /** @type {!number} */
      this.getResolution()
    );
    je(a !== void 0, "The view resolution is not defined");
    const u = (
      /** @type {!number} */
      this.getRotation()
    );
    return je(u !== void 0, "The view rotation is not defined"), Ob(i, a, u, t);
  }
  /**
   * Get the maximum resolution of the view.
   * @return {number} The maximum resolution of the view.
   * @api
   */
  getMaxResolution() {
    return this.maxResolution_;
  }
  /**
   * Get the minimum resolution of the view.
   * @return {number} The minimum resolution of the view.
   * @api
   */
  getMinResolution() {
    return this.minResolution_;
  }
  /**
   * Get the maximum zoom level for the view.
   * @return {number} The maximum zoom level.
   * @api
   */
  getMaxZoom() {
    return (
      /** @type {number} */
      this.getZoomForResolution(this.minResolution_)
    );
  }
  /**
   * Set a new maximum zoom level for the view.
   * @param {number} zoom The maximum zoom level.
   * @api
   */
  setMaxZoom(t) {
    this.applyOptions_(this.getUpdatedOptions_({ maxZoom: t }));
  }
  /**
   * Get the minimum zoom level for the view.
   * @return {number} The minimum zoom level.
   * @api
   */
  getMinZoom() {
    return (
      /** @type {number} */
      this.getZoomForResolution(this.maxResolution_)
    );
  }
  /**
   * Set a new minimum zoom level for the view.
   * @param {number} zoom The minimum zoom level.
   * @api
   */
  setMinZoom(t) {
    this.applyOptions_(this.getUpdatedOptions_({ minZoom: t }));
  }
  /**
   * Set whether the view should allow intermediary zoom levels.
   * @param {boolean} enabled Whether the resolution is constrained.
   * @api
   */
  setConstrainResolution(t) {
    this.applyOptions_(this.getUpdatedOptions_({ constrainResolution: t }));
  }
  /**
   * Get the view projection.
   * @return {import("./proj/Projection.js").default} The projection of the view.
   * @api
   */
  getProjection() {
    return this.projection_;
  }
  /**
   * Get the view resolution.
   * @return {number|undefined} The resolution of the view.
   * @observable
   * @api
   */
  getResolution() {
    return (
      /** @type {number|undefined} */
      this.get(Ps.RESOLUTION)
    );
  }
  /**
   * Get the resolutions for the view. This returns the array of resolutions
   * passed to the constructor of the View, or undefined if none were given.
   * @return {Array<number>|undefined} The resolutions of the view.
   * @api
   */
  getResolutions() {
    return this.resolutions_;
  }
  /**
   * Get the resolution for a provided extent (in map units) and size (in pixels).
   * @param {import("./extent.js").Extent} extent Extent.
   * @param {import("./size.js").Size} [size] Box pixel size.
   * @return {number} The resolution at which the provided extent will render at
   *     the given size.
   * @api
   */
  getResolutionForExtent(t, i) {
    return this.getResolutionForExtentInternal(
      Vc(t, this.getProjection()),
      i
    );
  }
  /**
   * Get the resolution for a provided extent (in map units) and size (in pixels).
   * @param {import("./extent.js").Extent} extent Extent.
   * @param {import("./size.js").Size} [size] Box pixel size.
   * @return {number} The resolution at which the provided extent will render at
   *     the given size.
   */
  getResolutionForExtentInternal(t, i) {
    i = i || this.getViewportSizeMinusPadding_();
    const a = Xe(t) / i[0], u = ml(t) / i[1];
    return Math.max(a, u);
  }
  /**
   * Return a function that returns a value between 0 and 1 for a
   * resolution. Exponential scaling is assumed.
   * @param {number} [power] Power.
   * @return {function(number): number} Resolution for value function.
   */
  getResolutionForValueFunction(t) {
    t = t || 2;
    const i = this.getConstrainedResolution(this.maxResolution_), a = this.minResolution_, u = Math.log(i / a) / Math.log(t);
    return (
      /**
       * @param {number} value Value.
       * @return {number} Resolution.
       */
      function(h) {
        return i / Math.pow(t, h * u);
      }
    );
  }
  /**
   * Get the view rotation.
   * @return {number} The rotation of the view in radians.
   * @observable
   * @api
   */
  getRotation() {
    return (
      /** @type {number} */
      this.get(Ps.ROTATION)
    );
  }
  /**
   * Return a function that returns a resolution for a value between
   * 0 and 1. Exponential scaling is assumed.
   * @param {number} [power] Power.
   * @return {function(number): number} Value for resolution function.
   */
  getValueForResolutionFunction(t) {
    const i = Math.log(t || 2), a = this.getConstrainedResolution(this.maxResolution_), u = this.minResolution_, h = Math.log(a / u) / i;
    return (
      /**
       * @param {number} resolution Resolution.
       * @return {number} Value.
       */
      function(g) {
        return Math.log(a / g) / i / h;
      }
    );
  }
  /**
   * Returns the size of the viewport minus padding.
   * @private
   * @param {number} [rotation] Take into account the rotation of the viewport when giving the size
   * @return {import("./size.js").Size} Viewport size reduced by the padding.
   */
  getViewportSizeMinusPadding_(t) {
    let i = this.getViewportSize_(t);
    const a = this.padding_;
    return a && (i = [
      i[0] - a[1] - a[3],
      i[1] - a[0] - a[2]
    ]), i;
  }
  /**
   * @return {State} View state.
   */
  getState() {
    const t = this.getProjection(), i = this.getResolution(), a = this.getRotation();
    let u = (
      /** @type {import("./coordinate.js").Coordinate} */
      this.getCenterInternal()
    );
    const h = this.padding_;
    if (h) {
      const g = this.getViewportSizeMinusPadding_();
      u = gb(
        u,
        this.getViewportSize_(),
        [g[0] / 2 + h[3], g[1] / 2 + h[0]],
        i,
        a
      );
    }
    return {
      center: u.slice(0),
      projection: t !== void 0 ? t : null,
      resolution: i,
      nextCenter: this.nextCenter_,
      nextResolution: this.nextResolution_,
      nextRotation: this.nextRotation_,
      rotation: a,
      zoom: this.getZoom()
    };
  }
  /**
   * @return {ViewStateLayerStateExtent} Like `FrameState`, but just `viewState` and `extent`.
   */
  getViewStateAndExtent() {
    return {
      viewState: this.getState(),
      extent: this.calculateExtent()
    };
  }
  /**
   * Get the current zoom level. This method may return non-integer zoom levels
   * if the view does not constrain the resolution, or if an interaction or
   * animation is underway.
   * @return {number|undefined} Zoom.
   * @api
   */
  getZoom() {
    let t;
    const i = this.getResolution();
    return i !== void 0 && (t = this.getZoomForResolution(i)), t;
  }
  /**
   * Get the zoom level for a resolution.
   * @param {number} resolution The resolution.
   * @return {number|undefined} The zoom level for the provided resolution.
   * @api
   */
  getZoomForResolution(t) {
    let i = this.minZoom_ || 0, a, u;
    if (this.resolutions_) {
      const h = tT(this.resolutions_, t, 1);
      i = h, a = this.resolutions_[h], h == this.resolutions_.length - 1 ? u = 2 : u = a / this.resolutions_[h + 1];
    } else
      a = this.maxResolution_, u = this.zoomFactor_;
    return i + Math.log(a / t) / Math.log(u);
  }
  /**
   * Get the resolution for a zoom level.
   * @param {number} zoom Zoom level.
   * @return {number} The view resolution for the provided zoom level.
   * @api
   */
  getResolutionForZoom(t) {
    var i;
    if ((i = this.resolutions_) != null && i.length) {
      if (this.resolutions_.length === 1)
        return this.resolutions_[0];
      const a = gn(
        Math.floor(t),
        0,
        this.resolutions_.length - 2
      ), u = this.resolutions_[a] / this.resolutions_[a + 1];
      return this.resolutions_[a] / Math.pow(u, gn(t - a, 0, 1));
    }
    return this.maxResolution_ / Math.pow(this.zoomFactor_, t - this.minZoom_);
  }
  /**
   * Fit the given geometry or extent based on the given map size and border.
   * The size is pixel dimensions of the box to fit the extent into.
   * In most cases you will want to use the map size, that is `map.getSize()`.
   * Takes care of the map angle.
   * @param {import("./geom/SimpleGeometry.js").default|import("./extent.js").Extent} geometryOrExtent The geometry or
   *     extent to fit the view to.
   * @param {FitOptions} [options] Options.
   * @api
   */
  fit(t, i) {
    let a;
    if (je(
      Array.isArray(t) || typeof /** @type {?} */
      t.getSimplifiedGeometry == "function",
      "Invalid extent or geometry provided as `geometry`"
    ), Array.isArray(t)) {
      je(
        !M_(t),
        "Cannot fit empty extent provided as `geometry`"
      );
      const u = Vc(t, this.getProjection());
      a = vR(u);
    } else if (t.getType() === "Circle") {
      const u = Vc(
        t.getExtent(),
        this.getProjection()
      );
      a = vR(u), a.rotate(this.getRotation(), Pc(u));
    } else {
      const u = Wh();
      u ? a = /** @type {import("./geom/SimpleGeometry.js").default} */
      t.clone().transform(u, this.getProjection()) : a = t;
    }
    this.fitInternal(a, i);
  }
  /**
   * Calculate rotated extent
   * @param {import("./geom/SimpleGeometry.js").default} geometry The geometry.
   * @return {import("./extent").Extent} The rotated extent for the geometry.
   */
  rotatedExtentForGeometry(t) {
    const i = this.getRotation(), a = Math.cos(i), u = Math.sin(-i), h = t.getFlatCoordinates(), g = t.getStride();
    let y = 1 / 0, _ = 1 / 0, b = -1 / 0, E = -1 / 0;
    for (let x = 0, R = h.length; x < R; x += g) {
      const w = h[x] * a - h[x + 1] * u, L = h[x] * u + h[x + 1] * a;
      y = Math.min(y, w), _ = Math.min(_, L), b = Math.max(b, w), E = Math.max(E, L);
    }
    return [y, _, b, E];
  }
  /**
   * @param {import("./geom/SimpleGeometry.js").default} geometry The geometry.
   * @param {FitOptions} [options] Options.
   */
  fitInternal(t, i) {
    i = i || {};
    let a = i.size;
    a || (a = this.getViewportSizeMinusPadding_());
    const u = i.padding !== void 0 ? i.padding : [0, 0, 0, 0], h = i.nearest !== void 0 ? i.nearest : !1;
    let g;
    i.minResolution !== void 0 ? g = i.minResolution : i.maxZoom !== void 0 ? g = this.getResolutionForZoom(i.maxZoom) : g = 0;
    const y = this.rotatedExtentForGeometry(t);
    let _ = this.getResolutionForExtentInternal(y, [
      a[0] - u[1] - u[3],
      a[1] - u[0] - u[2]
    ]);
    _ = isNaN(_) ? g : Math.max(_, g), _ = this.getConstrainedResolution(_, h ? 0 : 1);
    const b = this.getRotation(), E = Math.sin(b), x = Math.cos(b), R = Pc(y);
    R[0] += (u[1] - u[3]) / 2 * _, R[1] += (u[0] - u[2]) / 2 * _;
    const w = R[0] * x - R[1] * E, L = R[1] * x + R[0] * E, H = this.getConstrainedCenter([w, L], _), N = i.callback ? i.callback : h_;
    i.duration !== void 0 ? this.animateInternal(
      {
        resolution: _,
        center: H,
        duration: i.duration,
        easing: i.easing
      },
      N
    ) : (this.targetResolution_ = _, this.targetCenter_ = H, this.applyTargetState_(!1, !0), U1(N, !0));
  }
  /**
   * Center on coordinate and view position.
   * @param {import("./coordinate.js").Coordinate} coordinate Coordinate.
   * @param {import("./size.js").Size} size Box pixel size.
   * @param {import("./pixel.js").Pixel} position Position on the view to center on.
   * @api
   */
  centerOn(t, i, a) {
    this.centerOnInternal(
      dl(t, this.getProjection()),
      i,
      a
    );
  }
  /**
   * @param {import("./coordinate.js").Coordinate} coordinate Coordinate.
   * @param {import("./size.js").Size} size Box pixel size.
   * @param {import("./pixel.js").Pixel} position Position on the view to center on.
   */
  centerOnInternal(t, i, a) {
    this.setCenterInternal(
      gb(
        t,
        i,
        a,
        this.getResolution(),
        this.getRotation()
      )
    );
  }
  /**
   * Calculates the shift between map and viewport center.
   * @param {import("./coordinate.js").Coordinate} center Center.
   * @param {number} resolution Resolution.
   * @param {number} rotation Rotation.
   * @param {import("./size.js").Size} size Size.
   * @return {Array<number>|undefined} Center shift.
   */
  calculateCenterShift(t, i, a, u) {
    let h;
    const g = this.padding_;
    if (g && t) {
      const y = this.getViewportSizeMinusPadding_(-a), _ = gb(
        t,
        u,
        [y[0] / 2 + g[3], y[1] / 2 + g[0]],
        i,
        a
      );
      h = [
        t[0] - _[0],
        t[1] - _[1]
      ];
    }
    return h;
  }
  /**
   * @return {boolean} Is defined.
   */
  isDef() {
    return !!this.getCenterInternal() && this.getResolution() !== void 0;
  }
  /**
   * Adds relative coordinates to the center of the view. Any extent constraint will apply.
   * @param {import("./coordinate.js").Coordinate} deltaCoordinates Relative value to add.
   * @api
   */
  adjustCenter(t) {
    const i = F1(this.targetCenter_, this.getProjection());
    this.setCenter([
      i[0] + t[0],
      i[1] + t[1]
    ]);
  }
  /**
   * Adds relative coordinates to the center of the view. Any extent constraint will apply.
   * @param {import("./coordinate.js").Coordinate} deltaCoordinates Relative value to add.
   */
  adjustCenterInternal(t) {
    const i = this.targetCenter_;
    this.setCenterInternal([
      i[0] + t[0],
      i[1] + t[1]
    ]);
  }
  /**
   * Multiply the view resolution by a ratio, optionally using an anchor. Any resolution
   * constraint will apply.
   * @param {number} ratio The ratio to apply on the view resolution.
   * @param {import("./coordinate.js").Coordinate} [anchor] The origin of the transformation.
   * @api
   */
  adjustResolution(t, i) {
    i = i && dl(i, this.getProjection()), this.adjustResolutionInternal(t, i);
  }
  /**
   * Multiply the view resolution by a ratio, optionally using an anchor. Any resolution
   * constraint will apply.
   * @param {number} ratio The ratio to apply on the view resolution.
   * @param {import("./coordinate.js").Coordinate} [anchor] The origin of the transformation.
   */
  adjustResolutionInternal(t, i) {
    const a = this.getAnimating() || this.getInteracting(), u = this.getViewportSize_(this.getRotation()), h = this.constraints_.resolution(
      this.targetResolution_ * t,
      0,
      u,
      a
    );
    i && (this.targetCenter_ = this.calculateCenterZoom(h, i)), this.targetResolution_ *= t, this.applyTargetState_();
  }
  /**
   * Adds a value to the view zoom level, optionally using an anchor. Any resolution
   * constraint will apply.
   * @param {number} delta Relative value to add to the zoom level.
   * @param {import("./coordinate.js").Coordinate} [anchor] The origin of the transformation.
   * @api
   */
  adjustZoom(t, i) {
    this.adjustResolution(Math.pow(this.zoomFactor_, -t), i);
  }
  /**
   * Adds a value to the view rotation, optionally using an anchor. Any rotation
   * constraint will apply.
   * @param {number} delta Relative value to add to the zoom rotation, in radians.
   * @param {import("./coordinate.js").Coordinate} [anchor] The rotation center.
   * @api
   */
  adjustRotation(t, i) {
    i && (i = dl(i, this.getProjection())), this.adjustRotationInternal(t, i);
  }
  /**
   * @param {number} delta Relative value to add to the zoom rotation, in radians.
   * @param {import("./coordinate.js").Coordinate} [anchor] The rotation center.
   */
  adjustRotationInternal(t, i) {
    const a = this.getAnimating() || this.getInteracting(), u = this.constraints_.rotation(
      this.targetRotation_ + t,
      a
    );
    i && (this.targetCenter_ = this.calculateCenterRotate(u, i)), this.targetRotation_ += t, this.applyTargetState_();
  }
  /**
   * Set the center of the current view. Any extent constraint will apply.
   * @param {import("./coordinate.js").Coordinate|undefined} center The center of the view.
   * @observable
   * @api
   */
  setCenter(t) {
    this.setCenterInternal(
      t && dl(t, this.getProjection())
    );
  }
  /**
   * Set the center using the view projection (not the user projection).
   * @param {import("./coordinate.js").Coordinate|undefined} center The center of the view.
   */
  setCenterInternal(t) {
    this.targetCenter_ = t, this.applyTargetState_();
  }
  /**
   * @param {import("./ViewHint.js").default} hint Hint.
   * @param {number} delta Delta.
   * @return {number} New value.
   */
  setHint(t, i) {
    return this.hints_[t] += i, this.changed(), this.hints_[t];
  }
  /**
   * Set the resolution for this view. Any resolution constraint will apply.
   * @param {number|undefined} resolution The resolution of the view.
   * @observable
   * @api
   */
  setResolution(t) {
    this.targetResolution_ = t, this.applyTargetState_();
  }
  /**
   * Set the rotation for this view. Any rotation constraint will apply.
   * @param {number} rotation The rotation of the view in radians.
   * @observable
   * @api
   */
  setRotation(t) {
    this.targetRotation_ = t, this.applyTargetState_();
  }
  /**
   * Zoom to a specific zoom level. Any resolution constrain will apply.
   * @param {number} zoom Zoom level.
   * @api
   */
  setZoom(t) {
    this.setResolution(this.getResolutionForZoom(t));
  }
  /**
   * Recompute rotation/resolution/center based on target values.
   * Note: we have to compute rotation first, then resolution and center considering that
   * parameters can influence one another in case a view extent constraint is present.
   * @param {boolean} [doNotCancelAnims] Do not cancel animations.
   * @param {boolean} [forceMoving] Apply constraints as if the view is moving.
   * @private
   */
  applyTargetState_(t, i) {
    const a = this.getAnimating() || this.getInteracting() || i, u = this.constraints_.rotation(
      this.targetRotation_,
      a
    ), h = this.getViewportSize_(u), g = this.constraints_.resolution(
      this.targetResolution_,
      0,
      h,
      a
    ), y = this.constraints_.center(
      this.targetCenter_,
      g,
      h,
      a,
      this.calculateCenterShift(
        this.targetCenter_,
        g,
        u,
        h
      )
    );
    this.get(Ps.ROTATION) !== u && this.set(Ps.ROTATION, u), this.get(Ps.RESOLUTION) !== g && (this.set(Ps.RESOLUTION, g), this.set("zoom", this.getZoom(), !0)), (!y || !this.get(Ps.CENTER) || !V1(this.get(Ps.CENTER), y)) && this.set(Ps.CENTER, y), this.getAnimating() && !t && this.cancelAnimations(), this.cancelAnchor_ = void 0;
  }
  /**
   * If any constraints need to be applied, an animation will be triggered.
   * This is typically done on interaction end.
   * Note: calling this with a duration of 0 will apply the constrained values straight away,
   * without animation.
   * @param {number} [duration] The animation duration in ms.
   * @param {number} [resolutionDirection] Which direction to zoom.
   * @param {import("./coordinate.js").Coordinate} [anchor] The origin of the transformation.
   */
  resolveConstraints(t, i, a) {
    t = t !== void 0 ? t : 200;
    const u = i || 0, h = this.constraints_.rotation(this.targetRotation_), g = this.getViewportSize_(h), y = this.constraints_.resolution(
      this.targetResolution_,
      u,
      g
    ), _ = this.constraints_.center(
      this.targetCenter_,
      y,
      g,
      !1,
      this.calculateCenterShift(
        this.targetCenter_,
        y,
        h,
        g
      )
    );
    if (t === 0 && !this.cancelAnchor_) {
      this.targetResolution_ = y, this.targetRotation_ = h, this.targetCenter_ = _, this.applyTargetState_();
      return;
    }
    a = a || (t === 0 ? this.cancelAnchor_ : void 0), this.cancelAnchor_ = void 0, (this.getResolution() !== y || this.getRotation() !== h || !this.getCenterInternal() || !V1(this.getCenterInternal(), _)) && (this.getAnimating() && this.cancelAnimations(), this.animateInternal({
      rotation: h,
      center: _,
      resolution: y,
      duration: t,
      easing: ty,
      anchor: a
    }));
  }
  /**
   * Notify the View that an interaction has started.
   * The view state will be resolved to a stable one if needed
   * (depending on its constraints).
   * @api
   */
  beginInteraction() {
    this.resolveConstraints(0), this.setHint(Vi.INTERACTING, 1);
  }
  /**
   * Notify the View that an interaction has ended. The view state will be resolved
   * to a stable one if needed (depending on its constraints).
   * @param {number} [duration] Animation duration in ms.
   * @param {number} [resolutionDirection] Which direction to zoom.
   * @param {import("./coordinate.js").Coordinate} [anchor] The origin of the transformation.
   * @api
   */
  endInteraction(t, i, a) {
    a = a && dl(a, this.getProjection()), this.endInteractionInternal(t, i, a);
  }
  /**
   * Notify the View that an interaction has ended. The view state will be resolved
   * to a stable one if needed (depending on its constraints).
   * @param {number} [duration] Animation duration in ms.
   * @param {number} [resolutionDirection] Which direction to zoom.
   * @param {import("./coordinate.js").Coordinate} [anchor] The origin of the transformation.
   */
  endInteractionInternal(t, i, a) {
    this.getInteracting() && (this.setHint(Vi.INTERACTING, -1), this.resolveConstraints(t, i, a));
  }
  /**
   * Get a valid position for the view center according to the current constraints.
   * @param {import("./coordinate.js").Coordinate|undefined} targetCenter Target center position.
   * @param {number} [targetResolution] Target resolution. If not supplied, the current one will be used.
   * This is useful to guess a valid center position at a different zoom level.
   * @return {import("./coordinate.js").Coordinate|undefined} Valid center position.
   */
  getConstrainedCenter(t, i) {
    const a = this.getViewportSize_(this.getRotation());
    return this.constraints_.center(
      t,
      i || this.getResolution(),
      a
    );
  }
  /**
   * Get a valid zoom level according to the current view constraints.
   * @param {number|undefined} targetZoom Target zoom.
   * @param {number} [direction] Indicate which resolution should be used
   * by a renderer if the view resolution does not match any resolution of the tile source.
   * If 0, the nearest resolution will be used. If 1, the nearest lower resolution
   * will be used. If -1, the nearest higher resolution will be used.
   * @return {number|undefined} Valid zoom level.
   */
  getConstrainedZoom(t, i) {
    const a = this.getResolutionForZoom(t);
    return this.getZoomForResolution(
      this.getConstrainedResolution(a, i)
    );
  }
  /**
   * Get a valid resolution according to the current view constraints.
   * @param {number|undefined} targetResolution Target resolution.
   * @param {number} [direction] Indicate which resolution should be used
   * by a renderer if the view resolution does not match any resolution of the tile source.
   * If 0, the nearest resolution will be used. If 1, the nearest lower resolution
   * will be used. If -1, the nearest higher resolution will be used.
   * @return {number|undefined} Valid resolution.
   */
  getConstrainedResolution(t, i) {
    i = i || 0;
    const a = this.getViewportSize_(this.getRotation());
    return this.constraints_.resolution(t, i, a);
  }
}
function U1(r, t) {
  setTimeout(function() {
    r(t);
  }, 0);
}
function B3(r) {
  if (r.extent !== void 0) {
    const i = r.smoothExtentConstraint !== void 0 ? r.smoothExtentConstraint : !0;
    return FR(r.extent, r.constrainOnlyCenter, i);
  }
  const t = cT(r.projection, "EPSG:3857");
  if (r.multiWorld !== !0 && t.isGlobal()) {
    const i = t.getExtent().slice();
    return i[0] = -1 / 0, i[2] = 1 / 0, FR(i, !1, !1);
  }
  return L3;
}
function I3(r) {
  let t, i, a, g = r.minZoom !== void 0 ? r.minZoom : db, y = r.maxZoom !== void 0 ? r.maxZoom : 28;
  const _ = r.zoomFactor !== void 0 ? r.zoomFactor : 2, b = r.multiWorld !== void 0 ? r.multiWorld : !1, E = r.smoothResolutionConstraint !== void 0 ? r.smoothResolutionConstraint : !0, x = r.showFullExtent !== void 0 ? r.showFullExtent : !1, R = cT(r.projection, "EPSG:3857"), w = R.getExtent();
  let L = r.constrainOnlyCenter, H = r.extent;
  if (!b && !H && R.isGlobal() && (L = !1, H = w), r.resolutions !== void 0) {
    const N = r.resolutions;
    i = N[g], a = N[y] !== void 0 ? N[y] : N[N.length - 1], r.constrainResolution ? t = U3(
      N,
      E,
      !L && H,
      x
    ) : t = KR(
      i,
      a,
      E,
      !L && H,
      x
    );
  } else {
    const O = (w ? Math.max(Xe(w), ml(w)) : (
      // use an extent that can fit the whole world if need be
      360 * aT.degrees / R.getMetersPerUnit()
    )) / UT / Math.pow(2, db), U = O / Math.pow(2, 28 - db);
    i = r.maxResolution, i !== void 0 ? g = 0 : i = O / Math.pow(_, g), a = r.minResolution, a === void 0 && (r.maxZoom !== void 0 ? r.maxResolution !== void 0 ? a = i / Math.pow(_, y) : a = O / Math.pow(_, y) : a = U), y = g + Math.floor(
      Math.log(i / a) / Math.log(_)
    ), a = i / Math.pow(_, y - g), r.constrainResolution ? t = G3(
      _,
      i,
      a,
      E,
      !L && H,
      x
    ) : t = KR(
      i,
      a,
      E,
      !L && H,
      x
    );
  }
  return {
    constraint: t,
    maxResolution: i,
    minResolution: a,
    minZoom: g,
    zoomFactor: _
  };
}
function X3(r) {
  if (r.enableRotation !== void 0 ? r.enableRotation : !0) {
    const i = r.constrainRotation;
    return i === void 0 || i === !0 ? Y3() : i === !1 ? PR : typeof i == "number" ? H3(i) : PR;
  }
  return NT;
}
function k3(r) {
  return !(r.sourceCenter && r.targetCenter && !V1(r.sourceCenter, r.targetCenter) || r.sourceResolution !== r.targetResolution || r.sourceRotation !== r.targetRotation);
}
function gb(r, t, i, a, u) {
  const h = Math.cos(-u);
  let g = Math.sin(-u), y = r[0] * h - r[1] * g, _ = r[1] * h + r[0] * g;
  y += (t[0] / 2 - i[0]) * a, _ += (i[1] - t[1] / 2) * a, g = -g;
  const b = y * h - _ * g, E = _ * h + y * g;
  return [b, E];
}
const Ie = {
  OPACITY: "opacity",
  VISIBLE: "visible",
  EXTENT: "extent",
  Z_INDEX: "zIndex",
  MAX_RESOLUTION: "maxResolution",
  MIN_RESOLUTION: "minResolution",
  MAX_ZOOM: "maxZoom",
  MIN_ZOOM: "minZoom",
  SOURCE: "source",
  MAP: "map"
};
class yA extends tr {
  /**
   * @param {Options} options Layer options.
   */
  constructor(t) {
    super(), this.on, this.once, this.un, this.background_ = t.background;
    const i = Object.assign({}, t);
    typeof t.properties == "object" && (delete i.properties, Object.assign(i, t.properties)), i[Ie.OPACITY] = t.opacity !== void 0 ? t.opacity : 1, je(
      typeof i[Ie.OPACITY] == "number",
      "Layer opacity must be a number"
    ), i[Ie.VISIBLE] = t.visible !== void 0 ? t.visible : !0, i[Ie.Z_INDEX] = t.zIndex, i[Ie.MAX_RESOLUTION] = t.maxResolution !== void 0 ? t.maxResolution : 1 / 0, i[Ie.MIN_RESOLUTION] = t.minResolution !== void 0 ? t.minResolution : 0, i[Ie.MIN_ZOOM] = t.minZoom !== void 0 ? t.minZoom : -1 / 0, i[Ie.MAX_ZOOM] = t.maxZoom !== void 0 ? t.maxZoom : 1 / 0, this.className_ = i.className !== void 0 ? i.className : "ol-layer", delete i.className, this.setProperties(i), this.state_ = null;
  }
  /**
   * Get the background for this layer.
   * @return {BackgroundColor|false} Layer background.
   */
  getBackground() {
    return this.background_;
  }
  /**
   * @return {string} CSS class name.
   */
  getClassName() {
    return this.className_;
  }
  /**
   * This method is not meant to be called by layers or layer renderers because the state
   * is incorrect if the layer is included in a layer group.
   *
   * @param {boolean} [managed] Layer is managed.
   * @return {import("./Layer.js").State} Layer state.
   */
  getLayerState(t) {
    const i = this.state_ || /** @type {?} */
    {
      layer: this,
      managed: t === void 0 ? !0 : t
    }, a = this.getZIndex();
    return i.opacity = gn(Math.round(this.getOpacity() * 100) / 100, 0, 1), i.visible = this.getVisible(), i.extent = this.getExtent(), i.zIndex = a === void 0 && !i.managed ? 1 / 0 : a, i.maxResolution = this.getMaxResolution(), i.minResolution = Math.max(this.getMinResolution(), 0), i.minZoom = this.getMinZoom(), i.maxZoom = this.getMaxZoom(), this.state_ = i, i;
  }
  /**
   * @abstract
   * @param {Array<import("./Layer.js").default>} [array] Array of layers (to be
   *     modified in place).
   * @return {Array<import("./Layer.js").default>} Array of layers.
   */
  getLayersArray(t) {
    return we();
  }
  /**
   * @abstract
   * @param {Array<import("./Layer.js").State>} [states] Optional list of layer
   *     states (to be modified in place).
   * @return {Array<import("./Layer.js").State>} List of layer states.
   */
  getLayerStatesArray(t) {
    return we();
  }
  /**
   * Return the {@link module:ol/extent~Extent extent} of the layer or `undefined` if it
   * will be visible regardless of extent.
   * @return {import("../extent.js").Extent|undefined} The layer extent.
   * @observable
   * @api
   */
  getExtent() {
    return (
      /** @type {import("../extent.js").Extent|undefined} */
      this.get(Ie.EXTENT)
    );
  }
  /**
   * Return the maximum resolution of the layer. Returns Infinity if
   * the layer has no maximum resolution set.
   * @return {number} The maximum resolution of the layer.
   * @observable
   * @api
   */
  getMaxResolution() {
    return (
      /** @type {number} */
      this.get(Ie.MAX_RESOLUTION)
    );
  }
  /**
   * Return the minimum resolution of the layer. Returns 0 if
   * the layer has no minimum resolution set.
   * @return {number} The minimum resolution of the layer.
   * @observable
   * @api
   */
  getMinResolution() {
    return (
      /** @type {number} */
      this.get(Ie.MIN_RESOLUTION)
    );
  }
  /**
   * Return the minimum zoom level of the layer. Returns -Infinity if
   * the layer has no minimum zoom set.
   * @return {number} The minimum zoom level of the layer.
   * @observable
   * @api
   */
  getMinZoom() {
    return (
      /** @type {number} */
      this.get(Ie.MIN_ZOOM)
    );
  }
  /**
   * Return the maximum zoom level of the layer. Returns Infinity if
   * the layer has no maximum zoom set.
   * @return {number} The maximum zoom level of the layer.
   * @observable
   * @api
   */
  getMaxZoom() {
    return (
      /** @type {number} */
      this.get(Ie.MAX_ZOOM)
    );
  }
  /**
   * Return the opacity of the layer (between 0 and 1).
   * @return {number} The opacity of the layer.
   * @observable
   * @api
   */
  getOpacity() {
    return (
      /** @type {number} */
      this.get(Ie.OPACITY)
    );
  }
  /**
   * @abstract
   * @return {import("../source/Source.js").State} Source state.
   */
  getSourceState() {
    return we();
  }
  /**
   * Return the value of this layer's `visible` property. To find out whether the layer
   * is visible on a map, use `isVisible()` instead.
   * @return {boolean} The value of the `visible` property of the layer.
   * @observable
   * @api
   */
  getVisible() {
    return (
      /** @type {boolean} */
      this.get(Ie.VISIBLE)
    );
  }
  /**
   * Return the Z-index of the layer, which is used to order layers before
   * rendering. Returns undefined if the layer is unmanaged.
   * @return {number|undefined} The Z-index of the layer.
   * @observable
   * @api
   */
  getZIndex() {
    return (
      /** @type {number|undefined} */
      this.get(Ie.Z_INDEX)
    );
  }
  /**
   * Sets the background color.
   * @param {BackgroundColor} [background] Background color.
   */
  setBackground(t) {
    this.background_ = t, this.changed();
  }
  /**
   * Set the extent at which the layer is visible.  If `undefined`, the layer
   * will be visible at all extents.
   * @param {import("../extent.js").Extent|undefined} extent The extent of the layer.
   * @observable
   * @api
   */
  setExtent(t) {
    this.set(Ie.EXTENT, t);
  }
  /**
   * Set the maximum resolution at which the layer is visible.
   * @param {number} maxResolution The maximum resolution of the layer.
   * @observable
   * @api
   */
  setMaxResolution(t) {
    this.set(Ie.MAX_RESOLUTION, t);
  }
  /**
   * Set the minimum resolution at which the layer is visible.
   * @param {number} minResolution The minimum resolution of the layer.
   * @observable
   * @api
   */
  setMinResolution(t) {
    this.set(Ie.MIN_RESOLUTION, t);
  }
  /**
   * Set the maximum zoom (exclusive) at which the layer is visible.
   * Note that the zoom levels for layer visibility are based on the
   * view zoom level, which may be different from a tile source zoom level.
   * @param {number} maxZoom The maximum zoom of the layer.
   * @observable
   * @api
   */
  setMaxZoom(t) {
    this.set(Ie.MAX_ZOOM, t);
  }
  /**
   * Set the minimum zoom (inclusive) at which the layer is visible.
   * Note that the zoom levels for layer visibility are based on the
   * view zoom level, which may be different from a tile source zoom level.
   * @param {number} minZoom The minimum zoom of the layer.
   * @observable
   * @api
   */
  setMinZoom(t) {
    this.set(Ie.MIN_ZOOM, t);
  }
  /**
   * Set the opacity of the layer, allowed values range from 0 to 1.
   * @param {number} opacity The opacity of the layer.
   * @observable
   * @api
   */
  setOpacity(t) {
    je(typeof t == "number", "Layer opacity must be a number"), this.set(Ie.OPACITY, t);
  }
  /**
   * Set the visibility of the layer (`true` or `false`).
   * @param {boolean} visible The visibility of the layer.
   * @observable
   * @api
   */
  setVisible(t) {
    this.set(Ie.VISIBLE, t);
  }
  /**
   * Set Z-index of the layer, which is used to order layers before rendering.
   * The default Z-index is 0.
   * @param {number} zindex The z-index of the layer.
   * @observable
   * @api
   */
  setZIndex(t) {
    this.set(Ie.Z_INDEX, t);
  }
  /**
   * Clean up.
   * @override
   */
  disposeInternal() {
    this.state_ && (this.state_.layer = null, this.state_ = null), super.disposeInternal();
  }
}
class AS extends yA {
  /**
   * @param {Options<SourceType>} options Layer options.
   */
  constructor(t) {
    const i = Object.assign({}, t);
    delete i.source, super(i), this.on, this.once, this.un, this.mapPrecomposeKey_ = null, this.mapRenderKey_ = null, this.sourceChangeKey_ = null, this.renderer_ = null, this.sourceReady_ = !1, this.rendered = !1, t.render && (this.render = t.render), t.map && this.setMap(t.map), this.addChangeListener(
      Ie.SOURCE,
      this.handleSourcePropertyChange_
    );
    const a = t.source ? (
      /** @type {SourceType} */
      t.source
    ) : null;
    this.setSource(a);
  }
  /**
   * @param {Array<import("./Layer.js").default>} [array] Array of layers (to be modified in place).
   * @return {Array<import("./Layer.js").default>} Array of layers.
   * @override
   */
  getLayersArray(t) {
    return t = t || [], t.push(this), t;
  }
  /**
   * @param {Array<import("./Layer.js").State>} [states] Optional list of layer states (to be modified in place).
   * @return {Array<import("./Layer.js").State>} List of layer states.
   * @override
   */
  getLayerStatesArray(t) {
    return t = t || [], t.push(this.getLayerState()), t;
  }
  /**
   * Get the layer source.
   * @return {SourceType|null} The layer source (or `null` if not yet set).
   * @observable
   * @api
   */
  getSource() {
    return (
      /** @type {SourceType} */
      this.get(Ie.SOURCE) || null
    );
  }
  /**
   * @return {SourceType|null} The source being rendered.
   */
  getRenderSource() {
    return this.getSource();
  }
  /**
   * @return {import("../source/Source.js").State} Source state.
   * @override
   */
  getSourceState() {
    const t = this.getSource();
    return t ? t.getState() : "undefined";
  }
  /**
   * @private
   */
  handleSourceChange_() {
    this.changed(), !(this.sourceReady_ || this.getSource().getState() !== "ready") && (this.sourceReady_ = !0, this.dispatchEvent("sourceready"));
  }
  /**
   * @private
   */
  handleSourcePropertyChange_() {
    this.sourceChangeKey_ && (qe(this.sourceChangeKey_), this.sourceChangeKey_ = null), this.sourceReady_ = !1;
    const t = this.getSource();
    t && (this.sourceChangeKey_ = ge(
      t,
      $t.CHANGE,
      this.handleSourceChange_,
      this
    ), t.getState() === "ready" && (this.sourceReady_ = !0, setTimeout(() => {
      this.dispatchEvent("sourceready");
    }, 0))), this.changed();
  }
  /**
   * @param {import("../pixel").Pixel} pixel Pixel.
   * @return {Promise<Array<import("../Feature").FeatureLike>>} Promise that resolves with
   * an array of features.
   */
  getFeatures(t) {
    return this.renderer_ ? this.renderer_.getFeatures(t) : Promise.resolve([]);
  }
  /**
   * @param {import("../pixel").Pixel} pixel Pixel.
   * @return {Uint8ClampedArray|Uint8Array|Float32Array|DataView|null} Pixel data.
   */
  getData(t) {
    return !this.renderer_ || !this.rendered ? null : this.renderer_.getData(t);
  }
  /**
   * The layer is visible on the map view, i.e. within its min/max resolution or zoom and
   * extent, not set to `visible: false`, and not inside a layer group that is set
   * to `visible: false`.
   * @param {View|import("../View.js").ViewStateLayerStateExtent} [view] View or {@link import("../Map.js").FrameState}.
   * Only required when the layer is not added to a map.
   * @return {boolean} The layer is visible in the map view.
   * @api
   */
  isVisible(t) {
    let i;
    const a = this.getMapInternal();
    !t && a && (t = a.getView()), t instanceof Wo ? i = {
      viewState: t.getState(),
      extent: t.calculateExtent()
    } : i = t, !i.layerStatesArray && a && (i.layerStatesArray = a.getLayerGroup().getLayerStatesArray());
    let u;
    if (i.layerStatesArray) {
      if (u = i.layerStatesArray.find(
        (g) => g.layer === this
      ), !u)
        return !1;
    } else
      u = this.getLayerState();
    const h = this.getExtent();
    return GT(u, i.viewState) && (!h || qi(h, i.extent));
  }
  /**
   * Get the attributions of the source of this layer for the given view.
   * @param {View|import("../View.js").ViewStateLayerStateExtent} [view] View or {@link import("../Map.js").FrameState}.
   * Only required when the layer is not added to a map.
   * @return {Array<string>} Attributions for this layer at the given view.
   * @api
   */
  getAttributions(t) {
    var h;
    if (!this.isVisible(t))
      return [];
    const i = (h = this.getSource()) == null ? void 0 : h.getAttributions();
    if (!i)
      return [];
    const a = t instanceof Wo ? t.getViewStateAndExtent() : t;
    let u = i(a);
    return Array.isArray(u) || (u = [u]), u;
  }
  /**
   * In charge to manage the rendering of the layer. One layer type is
   * bounded with one layer renderer.
   * @param {?import("../Map.js").FrameState} frameState Frame state.
   * @param {HTMLElement} target Target which the renderer may (but need not) use
   * for rendering its content.
   * @return {HTMLElement|null} The rendered element.
   */
  render(t, i) {
    const a = this.getRenderer();
    return a.prepareFrame(t) ? (this.rendered = !0, a.renderFrame(t, i)) : null;
  }
  /**
   * Called when a layer is not visible during a map render.
   */
  unrender() {
    this.rendered = !1;
  }
  /** @return {string} Declutter */
  getDeclutter() {
  }
  /**
   * @param {import("../Map.js").FrameState} frameState Frame state.
   * @param {import("../layer/Layer.js").State} layerState Layer state.
   */
  renderDeclutter(t, i) {
  }
  /**
   * When the renderer follows a layout -> render approach, do the final rendering here.
   * @param {import('../Map.js').FrameState} frameState Frame state
   */
  renderDeferred(t) {
    const i = this.getRenderer();
    i && i.renderDeferred(t);
  }
  /**
   * For use inside the library only.
   * @param {import("../Map.js").default|null} map Map.
   */
  setMapInternal(t) {
    t || this.unrender(), this.set(Ie.MAP, t);
  }
  /**
   * For use inside the library only.
   * @return {import("../Map.js").default|null} Map.
   */
  getMapInternal() {
    return this.get(Ie.MAP);
  }
  /**
   * Sets the layer to be rendered on top of other layers on a map. The map will
   * not manage this layer in its layers collection. This
   * is useful for temporary layers. To remove an unmanaged layer from the map,
   * use `#setMap(null)`.
   *
   * To add the layer to a map and have it managed by the map, use
   * {@link module:ol/Map~Map#addLayer} instead.
   * @param {import("../Map.js").default|null} map Map.
   * @api
   */
  setMap(t) {
    this.mapPrecomposeKey_ && (qe(this.mapPrecomposeKey_), this.mapPrecomposeKey_ = null), t || this.changed(), this.mapRenderKey_ && (qe(this.mapRenderKey_), this.mapRenderKey_ = null), t && (this.mapPrecomposeKey_ = ge(
      t,
      fs.PRECOMPOSE,
      this.handlePrecompose_,
      this
    ), this.mapRenderKey_ = ge(this, $t.CHANGE, t.render, t), this.changed());
  }
  /**
   * @param {import("../events/Event.js").default} renderEvent Render event
   * @private
   */
  handlePrecompose_(t) {
    const i = (
      /** @type {import("../render/Event.js").default} */
      t.frameState.layerStatesArray
    ), a = this.getLayerState(!1);
    je(
      !i.some(
        (u) => u.layer === a.layer
      ),
      "A layer can only be added to the map once. Use either `layer.setMap()` or `map.addLayer()`, not both."
    ), i.push(a);
  }
  /**
   * Set the layer source.
   * @param {SourceType|null} source The layer source.
   * @observable
   * @api
   */
  setSource(t) {
    this.set(Ie.SOURCE, t);
  }
  /**
   * Get the renderer for this layer.
   * @return {RendererType|null} The layer renderer.
   */
  getRenderer() {
    return this.renderer_ || (this.renderer_ = this.createRenderer()), this.renderer_;
  }
  /**
   * @return {boolean} The layer has a renderer.
   */
  hasRenderer() {
    return !!this.renderer_;
  }
  /**
   * Create a renderer for this layer.
   * @return {RendererType} A layer renderer.
   * @protected
   */
  createRenderer() {
    return null;
  }
  /**
   * This will clear the renderer so that a new one can be created next time it is needed
   */
  clearRenderer() {
    this.renderer_ && (this.renderer_.dispose(), delete this.renderer_);
  }
  /**
   * Clean up.
   * @override
   */
  disposeInternal() {
    this.clearRenderer(), this.setSource(null), super.disposeInternal();
  }
}
function GT(r, t) {
  if (!r.visible)
    return !1;
  const i = t.resolution;
  if (i < r.minResolution || i >= r.maxResolution)
    return !1;
  const a = t.zoom;
  return a > r.minZoom && a <= r.maxZoom;
}
const QR = {
  RENDER_ORDER: "renderOrder"
};
class pA extends AS {
  /**
   * @param {Options<FeatureType, VectorSourceType>} [options] Options.
   */
  constructor(t) {
    t = t || {};
    const i = Object.assign({}, t);
    delete i.style, delete i.renderBuffer, delete i.updateWhileAnimating, delete i.updateWhileInteracting, super(i), this.declutter_ = t.declutter ? String(t.declutter) : void 0, this.renderBuffer_ = t.renderBuffer !== void 0 ? t.renderBuffer : 100, this.style_ = null, this.styleFunction_ = void 0, this.setStyle(t.style), this.updateWhileAnimating_ = t.updateWhileAnimating !== void 0 ? t.updateWhileAnimating : !1, this.updateWhileInteracting_ = t.updateWhileInteracting !== void 0 ? t.updateWhileInteracting : !1;
  }
  /**
   * @return {string} Declutter group.
   * @override
   */
  getDeclutter() {
    return this.declutter_;
  }
  /**
   * Get the topmost feature that intersects the given pixel on the viewport. Returns a promise
   * that resolves with an array of features. The array will either contain the topmost feature
   * when a hit was detected, or it will be empty.
   *
   * The hit detection algorithm used for this method is optimized for performance, but is less
   * accurate than the one used in [map.getFeaturesAtPixel()]{@link import("../Map.js").default#getFeaturesAtPixel}.
   * Text is not considered, and icons are only represented by their bounding box instead of the exact
   * image.
   *
   * @param {import("../pixel.js").Pixel} pixel Pixel.
   * @return {Promise<Array<import("../Feature").FeatureLike>>} Promise that resolves with an array of features.
   * @api
   * @override
   */
  getFeatures(t) {
    return super.getFeatures(t);
  }
  /**
   * @return {number|undefined} Render buffer.
   */
  getRenderBuffer() {
    return this.renderBuffer_;
  }
  /**
   * @return {import("../render.js").OrderFunction|null|undefined} Render order.
   */
  getRenderOrder() {
    return (
      /** @type {import("../render.js").OrderFunction|null|undefined} */
      this.get(QR.RENDER_ORDER)
    );
  }
  /**
   * Get the style for features.  This returns whatever was passed to the `style`
   * option at construction or to the `setStyle` method.
   * @return {import("../style/Style.js").StyleLike|import("../style/flat.js").FlatStyleLike|null|undefined} Layer style.
   * @api
   */
  getStyle() {
    return this.style_;
  }
  /**
   * Get the style function.
   * @return {import("../style/Style.js").StyleFunction|undefined} Layer style function.
   * @api
   */
  getStyleFunction() {
    return this.styleFunction_;
  }
  /**
   * @return {boolean} Whether the rendered layer should be updated while
   *     animating.
   */
  getUpdateWhileAnimating() {
    return this.updateWhileAnimating_;
  }
  /**
   * @return {boolean} Whether the rendered layer should be updated while
   *     interacting.
   */
  getUpdateWhileInteracting() {
    return this.updateWhileInteracting_;
  }
  /**
   * Render declutter items for this layer
   * @param {import("../Map.js").FrameState} frameState Frame state.
   * @param {import("../layer/Layer.js").State} layerState Layer state.
   * @override
   */
  renderDeclutter(t, i) {
    const a = this.getDeclutter();
    a in t.declutter || (t.declutter[a] = new tA(9)), this.getRenderer().renderDeclutter(t, i);
  }
  /**
   * @param {import("../render.js").OrderFunction|null|undefined} renderOrder
   *     Render order.
   */
  setRenderOrder(t) {
    this.set(QR.RENDER_ORDER, t);
  }
  /**
   * Set the style for features.  This can be a single style object, an array
   * of styles, or a function that takes a feature and resolution and returns
   * an array of styles. If set to `null`, the layer has no style (a `null` style),
   * so only features that have their own styles will be rendered in the layer. Call
   * `setStyle()` without arguments to reset to the default style. See
   * [the ol/style/Style module]{@link module:ol/style/Style~Style} for information on the default style.
   *
   * If your layer has a static style, you can use [flat style]{@link module:ol/style/flat~FlatStyle} object
   * literals instead of using the `Style` and symbolizer constructors (`Fill`, `Stroke`, etc.):
   * ```js
   * vectorLayer.setStyle({
   *   "fill-color": "yellow",
   *   "stroke-color": "black",
   *   "stroke-width": 4
   * })
   * ```
   *
   * @param {import("../style/Style.js").StyleLike|import("../style/flat.js").FlatStyleLike|null} [style] Layer style.
   * @api
   */
  setStyle(t) {
    this.style_ = t === void 0 ? nA : t;
    const i = V3(t);
    this.styleFunction_ = t === null ? void 0 : RO(i), this.changed();
  }
  /**
   * @param {boolean|string|number} declutter Declutter images and text.
   * @api
   */
  setDeclutter(t) {
    this.declutter_ = t ? String(t) : void 0, this.changed();
  }
}
function V3(r) {
  if (r === void 0)
    return nA;
  if (!r)
    return null;
  if (typeof r == "function" || r instanceof pa)
    return r;
  if (!Array.isArray(r))
    return kR([r]);
  if (r.length === 0)
    return [];
  const t = r.length, i = r[0];
  if (i instanceof pa) {
    const u = new Array(t);
    for (let h = 0; h < t; ++h) {
      const g = r[h];
      if (!(g instanceof pa))
        throw new Error("Expected a list of style instances");
      u[h] = g;
    }
    return u;
  }
  if ("style" in i) {
    const u = new Array(t);
    for (let h = 0; h < t; ++h) {
      const g = r[h];
      if (!("style" in g))
        throw new Error("Expected a list of rules with a style property");
      u[h] = g;
    }
    return v3(u);
  }
  return kR(
    /** @type {Array<import("../style/flat.js").FlatStyle>} */
    r
  );
}
class MS extends pA {
  /**
   * @param {Options<VectorSourceType, FeatureType>} [options] Options.
   */
  constructor(t) {
    super(t);
  }
  /**
   * @override
   */
  createRenderer() {
    return new FO(this);
  }
}
const It = {
  IDLE: 0,
  LOADING: 1,
  LOADED: 2,
  /**
   * Indicates that tile loading failed
   * @type {number}
   */
  ERROR: 3,
  EMPTY: 4
};
class HT extends oS {
  /**
   * @param {import("./tilecoord.js").TileCoord} tileCoord Tile coordinate.
   * @param {import("./TileState.js").default} state State.
   * @param {Options} [options] Tile options.
   */
  constructor(t, i, a) {
    super(), a = a || {}, this.tileCoord = t, this.state = i, this.key = "", this.transition_ = a.transition === void 0 ? 250 : a.transition, this.transitionStarts_ = {}, this.interpolate = !!a.interpolate;
  }
  /**
   * @protected
   */
  changed() {
    this.dispatchEvent($t.CHANGE);
  }
  /**
   * Called by the tile cache when the tile is removed from the cache due to expiry
   */
  release() {
    this.setState(It.EMPTY);
  }
  /**
   * @return {string} Key.
   */
  getKey() {
    return this.key + "/" + this.tileCoord;
  }
  /**
   * Get the tile coordinate for this tile.
   * @return {import("./tilecoord.js").TileCoord} The tile coordinate.
   * @api
   */
  getTileCoord() {
    return this.tileCoord;
  }
  /**
   * @return {import("./TileState.js").default} State.
   */
  getState() {
    return this.state;
  }
  /**
   * Sets the state of this tile. If you write your own {@link module:ol/Tile~LoadFunction tileLoadFunction} ,
   * it is important to set the state correctly to {@link module:ol/TileState~ERROR}
   * when the tile cannot be loaded. Otherwise the tile cannot be removed from
   * the tile queue and will block other requests.
   * @param {import("./TileState.js").default} state State.
   * @api
   */
  setState(t) {
    if (this.state !== It.EMPTY) {
      if (this.state !== It.ERROR && this.state > t)
        throw new Error("Tile load sequence violation");
      this.state = t, this.changed();
    }
  }
  /**
   * Load the image or retry if loading previously failed.
   * Loading is taken care of by the tile queue, and calling this method is
   * only needed for preloading or for reloading in case of an error.
   * @abstract
   * @api
   */
  load() {
    we();
  }
  /**
   * Get the alpha value for rendering.
   * @param {string} id An id for the renderer.
   * @param {number} time The render frame time.
   * @return {number} A number between 0 and 1.
   */
  getAlpha(t, i) {
    if (!this.transition_)
      return 1;
    let a = this.transitionStarts_[t];
    if (!a)
      a = i, this.transitionStarts_[t] = a;
    else if (a === -1)
      return 1;
    const u = i - a + 1e3 / 60;
    return u >= this.transition_ ? 1 : mA(u / this.transition_);
  }
  /**
   * Determine if a tile is in an alpha transition.  A tile is considered in
   * transition if tile.getAlpha() has not yet been called or has been called
   * and returned 1.
   * @param {string} id An id for the renderer.
   * @return {boolean} The tile is in transition.
   */
  inTransition(t) {
    return this.transition_ ? this.transitionStarts_[t] !== -1 : !1;
  }
  /**
   * Mark a transition as complete.
   * @param {string} id An id for the renderer.
   */
  endTransition(t) {
    this.transition_ && (this.transitionStarts_[t] = -1);
  }
  /**
   * @override
   */
  disposeInternal() {
    this.release(), super.disposeInternal();
  }
}
class _A extends HT {
  /**
   * @param {import("./tilecoord.js").TileCoord} tileCoord Tile coordinate.
   * @param {import("./TileState.js").default} state State.
   * @param {string} src Image source URI.
   * @param {?string} crossOrigin Cross origin.
   * @param {import("./Tile.js").LoadFunction} tileLoadFunction Tile load function.
   * @param {import("./Tile.js").Options} [options] Tile options.
   */
  constructor(t, i, a, u, h, g) {
    super(t, i, g), this.crossOrigin_ = u, this.src_ = a, this.key = a, this.image_ = new Image(), u !== null && (this.image_.crossOrigin = u), this.unlisten_ = null, this.tileLoadFunction_ = h;
  }
  /**
   * Get the HTML image element for this tile (may be a Canvas, Image, or Video).
   * @return {HTMLCanvasElement|HTMLImageElement|HTMLVideoElement} Image.
   * @api
   */
  getImage() {
    return this.image_;
  }
  /**
   * Sets an HTML image element for this tile (may be a Canvas or preloaded Image).
   * @param {HTMLCanvasElement|HTMLImageElement} element Element.
   */
  setImage(t) {
    this.image_ = t, this.state = It.LOADED, this.unlistenImage_(), this.changed();
  }
  /**
   * Tracks loading or read errors.
   *
   * @private
   */
  handleImageError_() {
    this.state = It.ERROR, this.unlistenImage_(), this.image_ = q3(), this.changed();
  }
  /**
   * Tracks successful image load.
   *
   * @private
   */
  handleImageLoad_() {
    const t = (
      /** @type {HTMLImageElement} */
      this.image_
    );
    t.naturalWidth && t.naturalHeight ? this.state = It.LOADED : this.state = It.EMPTY, this.unlistenImage_(), this.changed();
  }
  /**
   * Load the image or retry if loading previously failed.
   * Loading is taken care of by the tile queue, and calling this method is
   * only needed for preloading or for reloading in case of an error.
   *
   * To retry loading tiles on failed requests, use a custom `tileLoadFunction`
   * that checks for error status codes and reloads only when the status code is
   * 408, 429, 500, 502, 503 and 504, and only when not too many retries have been
   * made already:
   *
   * ```js
   * const retryCodes = [408, 429, 500, 502, 503, 504];
   * const retries = {};
   * source.setTileLoadFunction((tile, src) => {
   *   const image = tile.getImage();
   *   fetch(src)
   *     .then((response) => {
   *       if (retryCodes.includes(response.status)) {
   *         retries[src] = (retries[src] || 0) + 1;
   *         if (retries[src] <= 3) {
   *           setTimeout(() => tile.load(), retries[src] * 1000);
   *         }
   *         return Promise.reject();
   *       }
   *       return response.blob();
   *     })
   *     .then((blob) => {
   *       const imageUrl = URL.createObjectURL(blob);
   *       image.src = imageUrl;
   *       setTimeout(() => URL.revokeObjectURL(imageUrl), 5000);
   *     })
   *     .catch(() => tile.setState(3)); // error
   * });
   * ```
   * @api
   * @override
   */
  load() {
    this.state == It.ERROR && (this.state = It.IDLE, this.image_ = new Image(), this.crossOrigin_ !== null && (this.image_.crossOrigin = this.crossOrigin_)), this.state == It.IDLE && (this.state = It.LOADING, this.changed(), this.tileLoadFunction_(this, this.src_), this.unlisten_ = qD(
      this.image_,
      this.handleImageLoad_.bind(this),
      this.handleImageError_.bind(this)
    ));
  }
  /**
   * Discards event handlers which listen for load completion or errors.
   *
   * @private
   */
  unlistenImage_() {
    this.unlisten_ && (this.unlisten_(), this.unlisten_ = null);
  }
  /**
   * @override
   */
  disposeInternal() {
    this.unlistenImage_(), this.image_ = null, super.disposeInternal();
  }
}
function q3() {
  const r = ei(1, 1);
  return r.fillStyle = "rgba(0,0,0,0)", r.fillRect(0, 0, 1, 1), r.canvas;
}
class Z3 {
  /**
   * @param {number} decay Rate of decay (must be negative).
   * @param {number} minVelocity Minimum velocity (pixels/millisecond).
   * @param {number} delay Delay to consider to calculate the kinetic
   *     initial values (milliseconds).
   */
  constructor(t, i, a) {
    this.decay_ = t, this.minVelocity_ = i, this.delay_ = a, this.points_ = [], this.angle_ = 0, this.initialVelocity_ = 0;
  }
  /**
   * FIXME empty description for jsdoc
   */
  begin() {
    this.points_.length = 0, this.angle_ = 0, this.initialVelocity_ = 0;
  }
  /**
   * @param {number} x X.
   * @param {number} y Y.
   */
  update(t, i) {
    this.points_.push(t, i, Date.now());
  }
  /**
   * @return {boolean} Whether we should do kinetic animation.
   */
  end() {
    if (this.points_.length < 6)
      return !1;
    const t = Date.now() - this.delay_, i = this.points_.length - 3;
    if (this.points_[i + 2] < t)
      return !1;
    let a = i - 3;
    for (; a > 0 && this.points_[a + 2] > t; )
      a -= 3;
    const u = this.points_[i + 2] - this.points_[a + 2];
    if (u < 1e3 / 60)
      return !1;
    const h = this.points_[i] - this.points_[a], g = this.points_[i + 1] - this.points_[a + 1];
    return this.angle_ = Math.atan2(g, h), this.initialVelocity_ = Math.sqrt(h * h + g * g) / u, this.initialVelocity_ > this.minVelocity_;
  }
  /**
   * @return {number} Total distance travelled (pixels).
   */
  getDistance() {
    return (this.minVelocity_ - this.initialVelocity_) / this.decay_;
  }
  /**
   * @return {number} Angle of the kinetic panning animation (radians).
   */
  getAngle() {
    return this.angle_;
  }
}
class Hm extends ys {
  /**
   * @param {string} type Event type.
   * @param {import("./Map.js").default} map Map.
   * @param {?import("./Map.js").FrameState} [frameState] Frame state.
   */
  constructor(t, i, a) {
    super(t), this.map = i, this.frameState = a !== void 0 ? a : null;
  }
}
class uu extends Hm {
  /**
   * @param {string} type Event type.
   * @param {import("./Map.js").default} map Map.
   * @param {EVENT} originalEvent Original event.
   * @param {boolean} [dragging] Is the map currently being dragged?
   * @param {import("./Map.js").FrameState} [frameState] Frame state.
   * @param {Array<PointerEvent>} [activePointers] Active pointers.
   */
  constructor(t, i, a, u, h, g) {
    super(t, i, h), this.originalEvent = a, this.pixel_ = null, this.coordinate_ = null, this.dragging = u !== void 0 ? u : !1, this.activePointers = g;
  }
  /**
   * The map pixel relative to the viewport corresponding to the original event.
   * @type {import("./pixel.js").Pixel}
   * @api
   */
  get pixel() {
    return this.pixel_ || (this.pixel_ = this.map.getEventPixel(this.originalEvent)), this.pixel_;
  }
  set pixel(t) {
    this.pixel_ = t;
  }
  /**
   * The coordinate corresponding to the original browser event.  This will be in the user
   * projection if one is set.  Otherwise it will be in the view projection.
   * @type {import("./coordinate.js").Coordinate}
   * @api
   */
  get coordinate() {
    return this.coordinate_ || (this.coordinate_ = this.map.getCoordinateFromPixel(this.pixel)), this.coordinate_;
  }
  set coordinate(t) {
    this.coordinate_ = t;
  }
  /**
   * Prevents the default browser action.
   * See https://developer.mozilla.org/en-US/docs/Web/API/event.preventDefault.
   * @api
   * @override
   */
  preventDefault() {
    super.preventDefault(), "preventDefault" in this.originalEvent && this.originalEvent.preventDefault();
  }
  /**
   * Prevents further propagation of the current event.
   * See https://developer.mozilla.org/en-US/docs/Web/API/event.stopPropagation.
   * @api
   * @override
   */
  stopPropagation() {
    super.stopPropagation(), "stopPropagation" in this.originalEvent && this.originalEvent.stopPropagation();
  }
}
const Me = {
  /**
   * A true single click with no dragging and no double click. Note that this
   * event is delayed by 250 ms to ensure that it is not a double click.
   * @event module:ol/MapBrowserEvent~MapBrowserEvent#singleclick
   * @api
   */
  SINGLECLICK: "singleclick",
  /**
   * A click with no dragging. A double click will fire two of this.
   * @event module:ol/MapBrowserEvent~MapBrowserEvent#click
   * @api
   */
  CLICK: $t.CLICK,
  /**
   * A true double click, with no dragging.
   * @event module:ol/MapBrowserEvent~MapBrowserEvent#dblclick
   * @api
   */
  DBLCLICK: $t.DBLCLICK,
  /**
   * Triggered when a pointer is dragged.
   * @event module:ol/MapBrowserEvent~MapBrowserEvent#pointerdrag
   * @api
   */
  POINTERDRAG: "pointerdrag",
  /**
   * Triggered when a pointer is moved. Note that on touch devices this is
   * triggered when the map is panned, so is not the same as mousemove.
   * @event module:ol/MapBrowserEvent~MapBrowserEvent#pointermove
   * @api
   */
  POINTERMOVE: "pointermove",
  POINTERDOWN: "pointerdown",
  POINTERUP: "pointerup",
  POINTEROVER: "pointerover",
  POINTEROUT: "pointerout",
  POINTERENTER: "pointerenter",
  POINTERLEAVE: "pointerleave",
  POINTERCANCEL: "pointercancel"
}, qb = {
  POINTERMOVE: "pointermove",
  POINTERDOWN: "pointerdown"
};
class F3 extends oS {
  /**
   * @param {import("./Map.js").default} map The map with the viewport to listen to events on.
   * @param {number} [moveTolerance] The minimal distance the pointer must travel to trigger a move.
   */
  constructor(t, i) {
    super(t), this.map_ = t, this.clickTimeoutId_, this.emulateClicks_ = !1, this.dragging_ = !1, this.dragListenerKeys_ = [], this.moveTolerance_ = i === void 0 ? 1 : i, this.down_ = null;
    const a = this.map_.getViewport();
    this.activePointers_ = [], this.trackedTouches_ = {}, this.element_ = a, this.pointerdownListenerKey_ = ge(
      a,
      qb.POINTERDOWN,
      this.handlePointerDown_,
      this
    ), this.originalPointerMoveEvent_, this.relayedListenerKey_ = ge(
      a,
      qb.POINTERMOVE,
      this.relayMoveEvent_,
      this
    ), this.boundHandleTouchMove_ = this.handleTouchMove_.bind(this), this.element_.addEventListener(
      $t.TOUCHMOVE,
      this.boundHandleTouchMove_,
      X2 ? { passive: !1 } : !1
    );
  }
  /**
   * @param {PointerEvent} pointerEvent Pointer
   * event.
   * @private
   */
  emulateClick_(t) {
    let i = new uu(
      Me.CLICK,
      this.map_,
      t
    );
    this.dispatchEvent(i), this.clickTimeoutId_ !== void 0 ? (clearTimeout(this.clickTimeoutId_), this.clickTimeoutId_ = void 0, i = new uu(
      Me.DBLCLICK,
      this.map_,
      t
    ), this.dispatchEvent(i)) : this.clickTimeoutId_ = setTimeout(() => {
      this.clickTimeoutId_ = void 0;
      const a = new uu(
        Me.SINGLECLICK,
        this.map_,
        t
      );
      this.dispatchEvent(a);
    }, 250);
  }
  /**
   * Keeps track on how many pointers are currently active.
   *
   * @param {PointerEvent} pointerEvent Pointer
   * event.
   * @private
   */
  updateActivePointers_(t) {
    const i = t, a = i.pointerId;
    if (i.type == Me.POINTERUP || i.type == Me.POINTERCANCEL) {
      delete this.trackedTouches_[a];
      for (const u in this.trackedTouches_)
        if (this.trackedTouches_[u].target !== i.target) {
          delete this.trackedTouches_[u];
          break;
        }
    } else (i.type == Me.POINTERDOWN || i.type == Me.POINTERMOVE) && (this.trackedTouches_[a] = i);
    this.activePointers_ = Object.values(this.trackedTouches_);
  }
  /**
   * @param {PointerEvent} pointerEvent Pointer
   * event.
   * @private
   */
  handlePointerUp_(t) {
    this.updateActivePointers_(t);
    const i = new uu(
      Me.POINTERUP,
      this.map_,
      t,
      void 0,
      void 0,
      this.activePointers_
    );
    this.dispatchEvent(i), this.emulateClicks_ && !i.defaultPrevented && !this.dragging_ && this.isMouseActionButton_(t) && this.emulateClick_(this.down_), this.activePointers_.length === 0 && (this.dragListenerKeys_.forEach(qe), this.dragListenerKeys_.length = 0, this.dragging_ = !1, this.down_ = null);
  }
  /**
   * @param {PointerEvent} pointerEvent Pointer
   * event.
   * @return {boolean} If the left mouse button was pressed.
   * @private
   */
  isMouseActionButton_(t) {
    return t.button === 0;
  }
  /**
   * @param {PointerEvent} pointerEvent Pointer
   * event.
   * @private
   */
  handlePointerDown_(t) {
    this.emulateClicks_ = this.activePointers_.length === 0, this.updateActivePointers_(t);
    const i = new uu(
      Me.POINTERDOWN,
      this.map_,
      t,
      void 0,
      void 0,
      this.activePointers_
    );
    if (this.dispatchEvent(i), this.down_ = new PointerEvent(t.type, t), Object.defineProperty(this.down_, "target", {
      writable: !1,
      value: t.target
    }), this.dragListenerKeys_.length === 0) {
      const a = this.map_.getOwnerDocument();
      this.dragListenerKeys_.push(
        ge(
          a,
          Me.POINTERMOVE,
          this.handlePointerMove_,
          this
        ),
        ge(a, Me.POINTERUP, this.handlePointerUp_, this),
        /* Note that the listener for `pointercancel is set up on
         * `pointerEventHandler_` and not `documentPointerEventHandler_` like
         * the `pointerup` and `pointermove` listeners.
         *
         * The reason for this is the following: `TouchSource.vacuumTouches_()`
         * issues `pointercancel` events, when there was no `touchend` for a
         * `touchstart`. Now, let's say a first `touchstart` is registered on
         * `pointerEventHandler_`. The `documentPointerEventHandler_` is set up.
         * But `documentPointerEventHandler_` doesn't know about the first
         * `touchstart`. If there is no `touchend` for the `touchstart`, we can
         * only receive a `touchcancel` from `pointerEventHandler_`, because it is
         * only registered there.
         */
        ge(
          this.element_,
          Me.POINTERCANCEL,
          this.handlePointerUp_,
          this
        )
      ), this.element_.getRootNode && this.element_.getRootNode() !== a && this.dragListenerKeys_.push(
        ge(
          this.element_.getRootNode(),
          Me.POINTERUP,
          this.handlePointerUp_,
          this
        )
      );
    }
  }
  /**
   * @param {PointerEvent} pointerEvent Pointer
   * event.
   * @private
   */
  handlePointerMove_(t) {
    if (this.isMoving_(t)) {
      this.updateActivePointers_(t), this.dragging_ = !0;
      const i = new uu(
        Me.POINTERDRAG,
        this.map_,
        t,
        this.dragging_,
        void 0,
        this.activePointers_
      );
      this.dispatchEvent(i);
    }
  }
  /**
   * Wrap and relay a pointermove event.
   * @param {PointerEvent} pointerEvent Pointer
   * event.
   * @private
   */
  relayMoveEvent_(t) {
    this.originalPointerMoveEvent_ = t;
    const i = !!(this.down_ && this.isMoving_(t));
    this.dispatchEvent(
      new uu(
        Me.POINTERMOVE,
        this.map_,
        t,
        i
      )
    );
  }
  /**
   * Flexible handling of a `touch-action: none` css equivalent: because calling
   * `preventDefault()` on a `pointermove` event does not stop native page scrolling
   * and zooming, we also listen for `touchmove` and call `preventDefault()` on it
   * when an interaction (currently `DragPan` handles the event.
   * @param {TouchEvent} event Event.
   * @private
   */
  handleTouchMove_(t) {
    const i = this.originalPointerMoveEvent_;
    (!i || i.defaultPrevented) && (typeof t.cancelable != "boolean" || t.cancelable === !0) && t.preventDefault();
  }
  /**
   * @param {PointerEvent} pointerEvent Pointer
   * event.
   * @return {boolean} Is moving.
   * @private
   */
  isMoving_(t) {
    return this.dragging_ || Math.abs(t.clientX - this.down_.clientX) > this.moveTolerance_ || Math.abs(t.clientY - this.down_.clientY) > this.moveTolerance_;
  }
  /**
   * Clean up.
   * @override
   */
  disposeInternal() {
    this.relayedListenerKey_ && (qe(this.relayedListenerKey_), this.relayedListenerKey_ = null), this.element_.removeEventListener(
      $t.TOUCHMOVE,
      this.boundHandleTouchMove_
    ), this.pointerdownListenerKey_ && (qe(this.pointerdownListenerKey_), this.pointerdownListenerKey_ = null), this.dragListenerKeys_.forEach(qe), this.dragListenerKeys_.length = 0, this.element_ = null, super.disposeInternal();
  }
}
const Xc = {
  /**
   * Triggered after a map frame is rendered.
   * @event module:ol/MapEvent~MapEvent#postrender
   * @api
   */
  POSTRENDER: "postrender",
  /**
   * Triggered when the map starts moving.
   * @event module:ol/MapEvent~MapEvent#movestart
   * @api
   */
  MOVESTART: "movestart",
  /**
   * Triggered after the map is moved.
   * @event module:ol/MapEvent~MapEvent#moveend
   * @api
   */
  MOVEEND: "moveend",
  /**
   * Triggered when loading of additional map data (tiles, images, features) starts.
   * @event module:ol/MapEvent~MapEvent#loadstart
   * @api
   */
  LOADSTART: "loadstart",
  /**
   * Triggered when loading of additional map data has completed.
   * @event module:ol/MapEvent~MapEvent#loadend
   * @api
   */
  LOADEND: "loadend"
}, ki = {
  LAYERGROUP: "layergroup",
  SIZE: "size",
  TARGET: "target",
  VIEW: "view"
}, nS = 1 / 0;
class K3 {
  /**
   * @param {function(T): number} priorityFunction Priority function.
   * @param {function(T): string} keyFunction Key function.
   */
  constructor(t, i) {
    this.priorityFunction_ = t, this.keyFunction_ = i, this.elements_ = [], this.priorities_ = [], this.queuedElements_ = {};
  }
  /**
   * FIXME empty description for jsdoc
   */
  clear() {
    this.elements_.length = 0, this.priorities_.length = 0, Jm(this.queuedElements_);
  }
  /**
   * Remove and return the highest-priority element. O(log N).
   * @return {T} Element.
   */
  dequeue() {
    const t = this.elements_, i = this.priorities_, a = t[0];
    t.length == 1 ? (t.length = 0, i.length = 0) : (t[0] = /** @type {T} */
    t.pop(), i[0] = /** @type {number} */
    i.pop(), this.siftUp_(0));
    const u = this.keyFunction_(a);
    return delete this.queuedElements_[u], a;
  }
  /**
   * Enqueue an element. O(log N).
   * @param {T} element Element.
   * @return {boolean} The element was added to the queue.
   */
  enqueue(t) {
    je(
      !(this.keyFunction_(t) in this.queuedElements_),
      "Tried to enqueue an `element` that was already added to the queue"
    );
    const i = this.priorityFunction_(t);
    return i != nS ? (this.elements_.push(t), this.priorities_.push(i), this.queuedElements_[this.keyFunction_(t)] = !0, this.siftDown_(0, this.elements_.length - 1), !0) : !1;
  }
  /**
   * @return {number} Count.
   */
  getCount() {
    return this.elements_.length;
  }
  /**
   * Gets the index of the left child of the node at the given index.
   * @param {number} index The index of the node to get the left child for.
   * @return {number} The index of the left child.
   * @private
   */
  getLeftChildIndex_(t) {
    return t * 2 + 1;
  }
  /**
   * Gets the index of the right child of the node at the given index.
   * @param {number} index The index of the node to get the right child for.
   * @return {number} The index of the right child.
   * @private
   */
  getRightChildIndex_(t) {
    return t * 2 + 2;
  }
  /**
   * Gets the index of the parent of the node at the given index.
   * @param {number} index The index of the node to get the parent for.
   * @return {number} The index of the parent.
   * @private
   */
  getParentIndex_(t) {
    return t - 1 >> 1;
  }
  /**
   * Make this a heap. O(N).
   * @private
   */
  heapify_() {
    let t;
    for (t = (this.elements_.length >> 1) - 1; t >= 0; t--)
      this.siftUp_(t);
  }
  /**
   * @return {boolean} Is empty.
   */
  isEmpty() {
    return this.elements_.length === 0;
  }
  /**
   * @param {string} key Key.
   * @return {boolean} Is key queued.
   */
  isKeyQueued(t) {
    return t in this.queuedElements_;
  }
  /**
   * @param {T} element Element.
   * @return {boolean} Is queued.
   */
  isQueued(t) {
    return this.isKeyQueued(this.keyFunction_(t));
  }
  /**
   * @param {number} index The index of the node to move down.
   * @private
   */
  siftUp_(t) {
    const i = this.elements_, a = this.priorities_, u = i.length, h = i[t], g = a[t], y = t;
    for (; t < u >> 1; ) {
      const _ = this.getLeftChildIndex_(t), b = this.getRightChildIndex_(t), E = b < u && a[b] < a[_] ? b : _;
      i[t] = i[E], a[t] = a[E], t = E;
    }
    i[t] = h, a[t] = g, this.siftDown_(y, t);
  }
  /**
   * @param {number} startIndex The index of the root.
   * @param {number} index The index of the node to move up.
   * @private
   */
  siftDown_(t, i) {
    const a = this.elements_, u = this.priorities_, h = a[i], g = u[i];
    for (; i > t; ) {
      const y = this.getParentIndex_(i);
      if (u[y] > g)
        a[i] = a[y], u[i] = u[y], i = y;
      else
        break;
    }
    a[i] = h, u[i] = g;
  }
  /**
   * FIXME empty description for jsdoc
   */
  reprioritize() {
    const t = this.priorityFunction_, i = this.elements_, a = this.priorities_;
    let u = 0;
    const h = i.length;
    let g, y, _;
    for (y = 0; y < h; ++y)
      g = i[y], _ = t(g), _ == nS ? delete this.queuedElements_[this.keyFunction_(g)] : (a[u] = _, i[u++] = g);
    i.length = u, a.length = u, this.heapify_();
  }
}
class P3 extends K3 {
  /**
   * @param {PriorityFunction} tilePriorityFunction Tile priority function.
   * @param {function(): ?} tileChangeCallback Function called on each tile change event.
   */
  constructor(t, i) {
    super(
      (a) => t.apply(null, a),
      (a) => a[0].getKey()
    ), this.boundHandleTileChange_ = this.handleTileChange.bind(this), this.tileChangeCallback_ = i, this.tilesLoading_ = 0, this.tilesLoadingKeys_ = {};
  }
  /**
   * @param {TileQueueElement} element Element.
   * @return {boolean} The element was added to the queue.
   * @override
   */
  enqueue(t) {
    const i = super.enqueue(t);
    return i && t[0].addEventListener($t.CHANGE, this.boundHandleTileChange_), i;
  }
  /**
   * @return {number} Number of tiles loading.
   */
  getTilesLoading() {
    return this.tilesLoading_;
  }
  /**
   * @param {import("./events/Event.js").default} event Event.
   * @protected
   */
  handleTileChange(t) {
    const i = (
      /** @type {import("./Tile.js").default} */
      t.target
    ), a = i.getState();
    if (a === It.LOADED || a === It.ERROR || a === It.EMPTY) {
      a !== It.ERROR && i.removeEventListener($t.CHANGE, this.boundHandleTileChange_);
      const u = i.getKey();
      u in this.tilesLoadingKeys_ && (delete this.tilesLoadingKeys_[u], --this.tilesLoading_), this.tileChangeCallback_();
    }
  }
  /**
   * @param {number} maxTotalLoading Maximum number tiles to load simultaneously.
   * @param {number} maxNewLoads Maximum number of new tiles to load.
   */
  loadMoreTiles(t, i) {
    let a = 0;
    for (; this.tilesLoading_ < t && a < i && this.getCount() > 0; ) {
      const u = this.dequeue()[0], h = u.getKey();
      u.getState() === It.IDLE && !(h in this.tilesLoadingKeys_) && (this.tilesLoadingKeys_[h] = !0, ++this.tilesLoading_, ++a, u.load());
    }
  }
}
function Q3(r, t, i, a, u) {
  if (!r || !(i in r.wantedTiles) || !r.wantedTiles[i][t.getKey()])
    return nS;
  const h = r.viewState.center, g = a[0] - h[0], y = a[1] - h[1];
  return 65536 * Math.log(u) + Math.sqrt(g * g + y * y) / u;
}
class YT extends tr {
  /**
   * @param {Options} options Control options.
   */
  constructor(t) {
    super();
    const i = t.element;
    i && !t.target && !i.style.pointerEvents && (i.style.pointerEvents = "auto"), this.element = i || null, this.target_ = null, this.map_ = null, this.listenerKeys = [], t.render && (this.render = t.render), t.target && this.setTarget(t.target);
  }
  /**
   * Clean up.
   * @override
   */
  disposeInternal() {
    var t;
    (t = this.element) == null || t.remove(), super.disposeInternal();
  }
  /**
   * Get the map associated with this control.
   * @return {import("../Map.js").default|null} Map.
   * @api
   */
  getMap() {
    return this.map_;
  }
  /**
   * Remove the control from its current map and attach it to the new map.
   * Pass `null` to just remove the control from the current map.
   * Subclasses may set up event handlers to get notified about changes to
   * the map here.
   * @param {import("../Map.js").default|null} map Map.
   * @api
   */
  setMap(t) {
    var i;
    this.map_ && ((i = this.element) == null || i.remove());
    for (let a = 0, u = this.listenerKeys.length; a < u; ++a)
      qe(this.listenerKeys[a]);
    if (this.listenerKeys.length = 0, this.map_ = t, t) {
      const a = this.target_ ?? t.getOverlayContainerStopEvent();
      this.element && a.appendChild(this.element), this.render !== h_ && this.listenerKeys.push(
        ge(t, Xc.POSTRENDER, this.render, this)
      ), t.render();
    }
  }
  /**
   * Renders the control.
   * @param {import("../MapEvent.js").default} mapEvent Map event.
   * @api
   */
  render(t) {
  }
  /**
   * This function is used to set a target element for the control. It has no
   * effect if it is called after the control has been added to the map (i.e.
   * after `setMap` is called on the control). If no `target` is set in the
   * options passed to the control constructor and if `setTarget` is not called
   * then the control is added to the map's overlay container.
   * @param {HTMLElement|string} target Target.
   * @api
   */
  setTarget(t) {
    this.target_ = typeof t == "string" ? document.getElementById(t) : t;
  }
}
class W3 extends YT {
  /**
   * @param {Options} [options] Attribution options.
   */
  constructor(t) {
    t = t || {}, super({
      element: document.createElement("div"),
      render: t.render,
      target: t.target
    }), this.ulElement_ = document.createElement("ul"), this.collapsed_ = t.collapsed !== void 0 ? t.collapsed : !0, this.userCollapsed_ = this.collapsed_, this.overrideCollapsible_ = t.collapsible !== void 0, this.collapsible_ = t.collapsible !== void 0 ? t.collapsible : !0, this.collapsible_ || (this.collapsed_ = !1), this.attributions_ = t.attributions;
    const i = t.className !== void 0 ? t.className : "ol-attribution", a = t.tipLabel !== void 0 ? t.tipLabel : "Attributions", u = t.expandClassName !== void 0 ? t.expandClassName : i + "-expand", h = t.collapseLabel !== void 0 ? t.collapseLabel : "›", g = t.collapseClassName !== void 0 ? t.collapseClassName : i + "-collapse";
    typeof h == "string" ? (this.collapseLabel_ = document.createElement("span"), this.collapseLabel_.textContent = h, this.collapseLabel_.className = g) : this.collapseLabel_ = h;
    const y = t.label !== void 0 ? t.label : "i";
    typeof y == "string" ? (this.label_ = document.createElement("span"), this.label_.textContent = y, this.label_.className = u) : this.label_ = y;
    const _ = this.collapsible_ && !this.collapsed_ ? this.collapseLabel_ : this.label_;
    this.toggleButton_ = document.createElement("button"), this.toggleButton_.setAttribute("type", "button"), this.toggleButton_.setAttribute("aria-expanded", String(!this.collapsed_)), this.toggleButton_.title = a, this.toggleButton_.appendChild(_), this.toggleButton_.addEventListener(
      $t.CLICK,
      this.handleClick_.bind(this),
      !1
    );
    const b = i + " " + vS + " " + AT + (this.collapsed_ && this.collapsible_ ? " " + bR : "") + (this.collapsible_ ? "" : " ol-uncollapsible"), E = this.element;
    E.className = b, E.appendChild(this.toggleButton_), E.appendChild(this.ulElement_), this.renderedAttributions_ = [], this.renderedVisible_ = !0;
  }
  /**
   * Collect a list of visible attributions and set the collapsible state.
   * @param {import("../Map.js").FrameState} frameState Frame state.
   * @return {Array<string>} Attributions.
   * @private
   */
  collectSourceAttributions_(t) {
    const i = this.getMap().getAllLayers(), a = new Set(
      i.flatMap((u) => u.getAttributions(t))
    );
    if (this.attributions_ !== void 0 && (Array.isArray(this.attributions_) ? this.attributions_.forEach((u) => a.add(u)) : a.add(this.attributions_)), !this.overrideCollapsible_) {
      const u = !i.some(
        (h) => {
          var g;
          return ((g = h.getSource()) == null ? void 0 : g.getAttributionsCollapsible()) === !1;
        }
      );
      this.setCollapsible(u);
    }
    return Array.from(a);
  }
  /**
   * @private
   * @param {?import("../Map.js").FrameState} frameState Frame state.
   */
  async updateElement_(t) {
    if (!t) {
      this.renderedVisible_ && (this.element.style.display = "none", this.renderedVisible_ = !1);
      return;
    }
    const i = await Promise.all(
      this.collectSourceAttributions_(t).map(
        (u) => uw(() => u)
      )
    ), a = i.length > 0;
    if (this.renderedVisible_ != a && (this.element.style.display = a ? "" : "none", this.renderedVisible_ = a), !Wc(i, this.renderedAttributions_)) {
      ND(this.ulElement_);
      for (let u = 0, h = i.length; u < h; ++u) {
        const g = document.createElement("li");
        g.innerHTML = i[u], this.ulElement_.appendChild(g);
      }
      this.renderedAttributions_ = i;
    }
  }
  /**
   * @param {MouseEvent} event The event to handle
   * @private
   */
  handleClick_(t) {
    t.preventDefault(), this.handleToggle_(), this.userCollapsed_ = this.collapsed_;
  }
  /**
   * @private
   */
  handleToggle_() {
    this.element.classList.toggle(bR), this.collapsed_ ? SR(this.collapseLabel_, this.label_) : SR(this.label_, this.collapseLabel_), this.collapsed_ = !this.collapsed_, this.toggleButton_.setAttribute("aria-expanded", String(!this.collapsed_));
  }
  /**
   * Return `true` if the attribution is collapsible, `false` otherwise.
   * @return {boolean} True if the widget is collapsible.
   * @api
   */
  getCollapsible() {
    return this.collapsible_;
  }
  /**
   * Set whether the attribution should be collapsible.
   * @param {boolean} collapsible True if the widget is collapsible.
   * @api
   */
  setCollapsible(t) {
    this.collapsible_ !== t && (this.collapsible_ = t, this.element.classList.toggle("ol-uncollapsible"), this.userCollapsed_ && this.handleToggle_());
  }
  /**
   * Collapse or expand the attribution according to the passed parameter. Will
   * not do anything if the attribution isn't collapsible or if the current
   * collapsed state is already the one requested.
   * @param {boolean} collapsed True if the widget is collapsed.
   * @api
   */
  setCollapsed(t) {
    this.userCollapsed_ = t, !(!this.collapsible_ || this.collapsed_ === t) && this.handleToggle_();
  }
  /**
   * Return `true` when the attribution is currently collapsed or `false`
   * otherwise.
   * @return {boolean} True if the widget is collapsed.
   * @api
   */
  getCollapsed() {
    return this.collapsed_;
  }
  /**
   * Update the attribution element.
   * @param {import("../MapEvent.js").default} mapEvent Map event.
   * @override
   */
  render(t) {
    this.updateElement_(t.frameState);
  }
}
class J3 extends YT {
  /**
   * @param {Options} [options] Rotate options.
   */
  constructor(t) {
    t = t || {}, super({
      element: document.createElement("div"),
      render: t.render,
      target: t.target
    });
    const i = t.className !== void 0 ? t.className : "ol-rotate", a = t.label !== void 0 ? t.label : "⇧", u = t.compassClassName !== void 0 ? t.compassClassName : "ol-compass";
    this.label_ = null, typeof a == "string" ? (this.label_ = document.createElement("span"), this.label_.className = u, this.label_.textContent = a) : (this.label_ = a, this.label_.classList.add(u));
    const h = t.tipLabel ? t.tipLabel : "Reset rotation", g = document.createElement("button");
    g.className = i + "-reset", g.setAttribute("type", "button"), g.title = h, g.appendChild(this.label_), g.addEventListener(
      $t.CLICK,
      this.handleClick_.bind(this),
      !1
    );
    const y = i + " " + vS + " " + AT, _ = this.element;
    _.className = y, _.appendChild(g), this.callResetNorth_ = t.resetNorth ? t.resetNorth : void 0, this.duration_ = t.duration !== void 0 ? t.duration : 250, this.autoHide_ = t.autoHide !== void 0 ? t.autoHide : !0, this.rotation_ = void 0, this.autoHide_ && this.element.classList.add(D1);
  }
  /**
   * @param {MouseEvent} event The event to handle
   * @private
   */
  handleClick_(t) {
    t.preventDefault(), this.callResetNorth_ !== void 0 ? this.callResetNorth_() : this.resetNorth_();
  }
  /**
   * @private
   */
  resetNorth_() {
    const i = this.getMap().getView();
    if (!i)
      return;
    const a = i.getRotation();
    a !== void 0 && (this.duration_ > 0 && a % (2 * Math.PI) !== 0 ? i.animate({
      rotation: 0,
      duration: this.duration_,
      easing: ty
    }) : i.setRotation(0));
  }
  /**
   * Update the rotate control element.
   * @param {import("../MapEvent.js").default} mapEvent Map event.
   * @override
   */
  render(t) {
    const i = t.frameState;
    if (!i)
      return;
    const a = i.viewState.rotation;
    if (a != this.rotation_) {
      const u = "rotate(" + a + "rad)";
      if (this.autoHide_) {
        const h = this.element.classList.contains(D1);
        !h && a === 0 ? this.element.classList.add(D1) : h && a !== 0 && this.element.classList.remove(D1);
      }
      this.label_.style.transform = u;
    }
    this.rotation_ = a;
  }
}
class $3 extends YT {
  /**
   * @param {Options} [options] Zoom options.
   */
  constructor(t) {
    t = t || {}, super({
      element: document.createElement("div"),
      target: t.target
    });
    const i = t.className !== void 0 ? t.className : "ol-zoom", a = t.delta !== void 0 ? t.delta : 1, u = t.zoomInClassName !== void 0 ? t.zoomInClassName : i + "-in", h = t.zoomOutClassName !== void 0 ? t.zoomOutClassName : i + "-out", g = t.zoomInLabel !== void 0 ? t.zoomInLabel : "+", y = t.zoomOutLabel !== void 0 ? t.zoomOutLabel : "–", _ = t.zoomInTipLabel !== void 0 ? t.zoomInTipLabel : "Zoom in", b = t.zoomOutTipLabel !== void 0 ? t.zoomOutTipLabel : "Zoom out", E = document.createElement("button");
    E.className = u, E.setAttribute("type", "button"), E.title = _, E.appendChild(
      typeof g == "string" ? document.createTextNode(g) : g
    ), E.addEventListener(
      $t.CLICK,
      this.handleClick_.bind(this, a),
      !1
    );
    const x = document.createElement("button");
    x.className = h, x.setAttribute("type", "button"), x.title = b, x.appendChild(
      typeof y == "string" ? document.createTextNode(y) : y
    ), x.addEventListener(
      $t.CLICK,
      this.handleClick_.bind(this, -a),
      !1
    );
    const R = i + " " + vS + " " + AT, w = this.element;
    w.className = R, w.appendChild(E), w.appendChild(x), this.duration_ = t.duration !== void 0 ? t.duration : 250;
  }
  /**
   * @param {number} delta Zoom delta.
   * @param {MouseEvent} event The event to handle
   * @private
   */
  handleClick_(t, i) {
    i.preventDefault(), this.zoomByDelta_(t);
  }
  /**
   * @param {number} delta Zoom delta.
   * @private
   */
  zoomByDelta_(t) {
    const a = this.getMap().getView();
    if (!a)
      return;
    const u = a.getZoom();
    if (u !== void 0) {
      const h = a.getConstrainedZoom(u + t);
      this.duration_ > 0 ? (a.getAnimating() && a.cancelAnimations(), a.animate({
        zoom: h,
        duration: this.duration_,
        easing: ty
      })) : a.setZoom(h);
    }
  }
}
function tL(r) {
  r = r || {};
  const t = new Js();
  return (r.zoom !== void 0 ? r.zoom : !0) && t.push(new $3(r.zoomOptions)), (r.rotate !== void 0 ? r.rotate : !0) && t.push(new J3(r.rotateOptions)), (r.attribution !== void 0 ? r.attribution : !0) && t.push(new W3(r.attributionOptions)), t;
}
const Zb = {
  ACTIVE: "active"
};
class ey extends tr {
  /**
   * @param {InteractionOptions} [options] Options.
   */
  constructor(t) {
    super(), this.on, this.once, this.un, t && t.handleEvent && (this.handleEvent = t.handleEvent), this.map_ = null, this.setActive(!0);
  }
  /**
   * Return whether the interaction is currently active.
   * @return {boolean} `true` if the interaction is active, `false` otherwise.
   * @observable
   * @api
   */
  getActive() {
    return (
      /** @type {boolean} */
      this.get(Zb.ACTIVE)
    );
  }
  /**
   * Get the map associated with this interaction.
   * @return {import("../Map.js").default|null} Map.
   * @api
   */
  getMap() {
    return this.map_;
  }
  /**
   * Handles the {@link module:ol/MapBrowserEvent~MapBrowserEvent map browser event}.
   * @param {import("../MapBrowserEvent.js").default} mapBrowserEvent Map browser event.
   * @return {boolean} `false` to stop event propagation.
   * @api
   */
  handleEvent(t) {
    return !0;
  }
  /**
   * Activate or deactivate the interaction.
   * @param {boolean} active Active.
   * @observable
   * @api
   */
  setActive(t) {
    this.set(Zb.ACTIVE, t);
  }
  /**
   * Remove the interaction from its current map and attach it to the new map.
   * Subclasses may set up event handlers to get notified about changes to
   * the map here.
   * @param {import("../Map.js").default|null} map Map.
   */
  setMap(t) {
    this.map_ = t;
  }
}
function eL(r, t, i) {
  const a = r.getCenterInternal();
  if (a) {
    const u = [a[0] + t[0], a[1] + t[1]];
    r.animateInternal({
      duration: i !== void 0 ? i : 250,
      easing: N3,
      center: r.getConstrainedCenter(u)
    });
  }
}
function jT(r, t, i, a) {
  const u = r.getZoom();
  if (u === void 0)
    return;
  const h = r.getConstrainedZoom(u + t), g = r.getResolutionForZoom(h);
  r.getAnimating() && r.cancelAnimations(), r.animate({
    resolution: g,
    anchor: i,
    duration: a !== void 0 ? a : 250,
    easing: ty
  });
}
class nL extends ey {
  /**
   * @param {Options} [options] Options.
   */
  constructor(t) {
    super(), t = t || {}, this.delta_ = t.delta ? t.delta : 1, this.duration_ = t.duration !== void 0 ? t.duration : 250;
  }
  /**
   * Handles the {@link module:ol/MapBrowserEvent~MapBrowserEvent map browser event} (if it was a
   * doubleclick) and eventually zooms the map.
   * @param {import("../MapBrowserEvent.js").default} mapBrowserEvent Map browser event.
   * @return {boolean} `false` to stop event propagation.
   * @override
   */
  handleEvent(t) {
    let i = !1;
    if (t.type == Me.DBLCLICK) {
      const a = (
        /** @type {MouseEvent} */
        t.originalEvent
      ), u = t.map, h = t.coordinate, g = a.shiftKey ? -this.delta_ : this.delta_, y = u.getView();
      jT(y, g, h, this.duration_), a.preventDefault(), i = !0;
    }
    return !i;
  }
}
function Fb(r) {
  const t = arguments;
  return function(i) {
    let a = !0;
    for (let u = 0, h = t.length; u < h && (a = a && t[u](i), !!a); ++u)
      ;
    return a;
  };
}
const iL = function(r) {
  const t = r.originalEvent;
  return t.altKey && !(t.metaKey || t.ctrlKey) && t.shiftKey;
}, lL = function(r) {
  const t = r.map.getTargetElement(), i = t.getRootNode(), a = r.map.getOwnerDocument().activeElement;
  return i instanceof ShadowRoot ? i.host.contains(a) : t.contains(a);
}, vA = function(r) {
  const t = r.map.getTargetElement(), i = t.getRootNode();
  return (i instanceof ShadowRoot ? i.host : t).hasAttribute("tabindex") ? lL(r) : !0;
}, Kb = _u, aL = function(r) {
  return r.type == Me.CLICK;
}, SA = function(r) {
  const t = r.originalEvent;
  return "pointerId" in t && t.button == 0 && !(LD && B2 && t.ctrlKey);
}, Pb = td, sL = function(r) {
  return r.type == Me.SINGLECLICK;
}, BT = function(r) {
  const t = (
    /** @type {KeyboardEvent|MouseEvent|TouchEvent} */
    r.originalEvent
  );
  return !t.altKey && !(t.metaKey || t.ctrlKey) && !t.shiftKey;
}, oL = function(r) {
  const t = r.originalEvent;
  return B2 ? t.metaKey : t.ctrlKey;
}, IT = function(r) {
  const t = r.originalEvent;
  return !t.altKey && !(t.metaKey || t.ctrlKey) && t.shiftKey;
}, EA = function(r) {
  const t = r.originalEvent, i = (
    /** @type {Element} */
    t.target.tagName
  );
  return i !== "INPUT" && i !== "SELECT" && i !== "TEXTAREA" && // `isContentEditable` is only available on `HTMLElement`, but it may also be a
  // different type like `SVGElement`.
  // @ts-ignore
  !t.target.isContentEditable;
}, mb = function(r) {
  const t = r.originalEvent;
  return "pointerId" in t && t.pointerType == "mouse";
}, rL = function(r) {
  const t = r.originalEvent;
  return "pointerId" in t && t.isPrimary && t.button === 0;
};
class ld extends ey {
  /**
   * @param {Options} [options] Options.
   */
  constructor(t) {
    t = t || {}, super(
      /** @type {import("./Interaction.js").InteractionOptions} */
      t
    ), t.handleDownEvent && (this.handleDownEvent = t.handleDownEvent), t.handleDragEvent && (this.handleDragEvent = t.handleDragEvent), t.handleMoveEvent && (this.handleMoveEvent = t.handleMoveEvent), t.handleUpEvent && (this.handleUpEvent = t.handleUpEvent), t.stopDown && (this.stopDown = t.stopDown), this.handlingDownUpSequence = !1, this.targetPointers = [];
  }
  /**
   * Returns the current number of pointers involved in the interaction,
   * e.g. `2` when two fingers are used.
   * @return {number} The number of pointers.
   * @api
   */
  getPointerCount() {
    return this.targetPointers.length;
  }
  /**
   * Handle pointer down events.
   * @param {import("../MapBrowserEvent.js").default} mapBrowserEvent Event.
   * @return {boolean} If the event was consumed.
   * @protected
   */
  handleDownEvent(t) {
    return !1;
  }
  /**
   * Handle pointer drag events.
   * @param {import("../MapBrowserEvent.js").default} mapBrowserEvent Event.
   * @protected
   */
  handleDragEvent(t) {
  }
  /**
   * Handles the {@link module:ol/MapBrowserEvent~MapBrowserEvent map browser event} and may call into
   * other functions, if event sequences like e.g. 'drag' or 'down-up' etc. are
   * detected.
   * @param {import("../MapBrowserEvent.js").default} mapBrowserEvent Map browser event.
   * @return {boolean} `false` to stop event propagation.
   * @api
   * @override
   */
  handleEvent(t) {
    if (!t.originalEvent)
      return !0;
    let i = !1;
    if (this.updateTrackedPointers_(t), this.handlingDownUpSequence) {
      if (t.type == Me.POINTERDRAG)
        this.handleDragEvent(t), t.originalEvent.preventDefault();
      else if (t.type == Me.POINTERUP) {
        const a = this.handleUpEvent(t);
        this.handlingDownUpSequence = a && this.targetPointers.length > 0;
      }
    } else if (t.type == Me.POINTERDOWN) {
      const a = this.handleDownEvent(t);
      this.handlingDownUpSequence = a, i = this.stopDown(a);
    } else t.type == Me.POINTERMOVE && this.handleMoveEvent(t);
    return !i;
  }
  /**
   * Handle pointer move events.
   * @param {import("../MapBrowserEvent.js").default} mapBrowserEvent Event.
   * @protected
   */
  handleMoveEvent(t) {
  }
  /**
   * Handle pointer up events.
   * @param {import("../MapBrowserEvent.js").default} mapBrowserEvent Event.
   * @return {boolean} If the event was consumed.
   * @protected
   */
  handleUpEvent(t) {
    return !1;
  }
  /**
   * This function is used to determine if "down" events should be propagated
   * to other interactions or should be stopped.
   * @param {boolean} handled Was the event handled by the interaction?
   * @return {boolean} Should the `down` event be stopped?
   */
  stopDown(t) {
    return t;
  }
  /**
   * @param {import("../MapBrowserEvent.js").default} mapBrowserEvent Event.
   * @private
   */
  updateTrackedPointers_(t) {
    t.activePointers && (this.targetPointers = t.activePointers);
  }
}
function XT(r) {
  const t = r.length;
  let i = 0, a = 0;
  for (let u = 0; u < t; u++)
    i += r[u].clientX, a += r[u].clientY;
  return { clientX: i / t, clientY: a / t };
}
class uL extends ld {
  /**
   * @param {Options} [options] Options.
   */
  constructor(t) {
    super({
      stopDown: td
    }), t = t || {}, this.kinetic_ = t.kinetic, this.lastCentroid = null, this.lastPointersCount_, this.panning_ = !1;
    const i = t.condition ? t.condition : Fb(BT, rL);
    this.condition_ = t.onFocusOnly ? Fb(vA, i) : i, this.noKinetic_ = !1;
  }
  /**
   * Handle pointer drag events.
   * @param {import("../MapBrowserEvent.js").default} mapBrowserEvent Event.
   * @override
   */
  handleDragEvent(t) {
    const i = t.map;
    this.panning_ || (this.panning_ = !0, i.getView().beginInteraction());
    const a = this.targetPointers, u = i.getEventPixel(XT(a));
    if (a.length == this.lastPointersCount_) {
      if (this.kinetic_ && this.kinetic_.update(u[0], u[1]), this.lastCentroid) {
        const h = [
          this.lastCentroid[0] - u[0],
          u[1] - this.lastCentroid[1]
        ], y = t.map.getView();
        Cw(h, y.getResolution()), lT(h, y.getRotation()), y.adjustCenterInternal(h);
      }
    } else this.kinetic_ && this.kinetic_.begin();
    this.lastCentroid = u, this.lastPointersCount_ = a.length, t.originalEvent.preventDefault();
  }
  /**
   * Handle pointer up events.
   * @param {import("../MapBrowserEvent.js").default} mapBrowserEvent Event.
   * @return {boolean} If the event was consumed.
   * @override
   */
  handleUpEvent(t) {
    const i = t.map, a = i.getView();
    if (this.targetPointers.length === 0) {
      if (!this.noKinetic_ && this.kinetic_ && this.kinetic_.end()) {
        const u = this.kinetic_.getDistance(), h = this.kinetic_.getAngle(), g = a.getCenterInternal(), y = i.getPixelFromCoordinateInternal(g), _ = i.getCoordinateFromPixelInternal([
          y[0] - u * Math.cos(h),
          y[1] - u * Math.sin(h)
        ]);
        a.animateInternal({
          center: a.getConstrainedCenter(_),
          duration: 500,
          easing: ty
        });
      }
      return this.panning_ && (this.panning_ = !1, a.endInteraction()), !1;
    }
    return this.kinetic_ && this.kinetic_.begin(), this.lastCentroid = null, !0;
  }
  /**
   * Handle pointer down events.
   * @param {import("../MapBrowserEvent.js").default} mapBrowserEvent Event.
   * @return {boolean} If the event was consumed.
   * @override
   */
  handleDownEvent(t) {
    if (this.targetPointers.length > 0 && this.condition_(t)) {
      const a = t.map.getView();
      return this.lastCentroid = null, a.getAnimating() && a.cancelAnimations(), this.kinetic_ && this.kinetic_.begin(), this.noKinetic_ = this.targetPointers.length > 1, !0;
    }
    return !1;
  }
}
class cL extends ld {
  /**
   * @param {Options} [options] Options.
   */
  constructor(t) {
    t = t || {}, super({
      stopDown: td
    }), this.condition_ = t.condition ? t.condition : iL, this.lastAngle_ = void 0, this.duration_ = t.duration !== void 0 ? t.duration : 250;
  }
  /**
   * Handle pointer drag events.
   * @param {import("../MapBrowserEvent.js").default} mapBrowserEvent Event.
   * @override
   */
  handleDragEvent(t) {
    if (!mb(t))
      return;
    const i = t.map, a = i.getView();
    if (a.getConstraints().rotation === NT)
      return;
    const u = i.getSize(), h = t.pixel, g = Math.atan2(u[1] / 2 - h[1], h[0] - u[0] / 2);
    if (this.lastAngle_ !== void 0) {
      const y = g - this.lastAngle_;
      a.adjustRotationInternal(-y);
    }
    this.lastAngle_ = g;
  }
  /**
   * Handle pointer up events.
   * @param {import("../MapBrowserEvent.js").default} mapBrowserEvent Event.
   * @return {boolean} If the event was consumed.
   * @override
   */
  handleUpEvent(t) {
    return mb(t) ? (t.map.getView().endInteraction(this.duration_), !1) : !0;
  }
  /**
   * Handle pointer down events.
   * @param {import("../MapBrowserEvent.js").default} mapBrowserEvent Event.
   * @return {boolean} If the event was consumed.
   * @override
   */
  handleDownEvent(t) {
    return mb(t) && SA(t) && this.condition_(t) ? (t.map.getView().beginInteraction(), this.lastAngle_ = void 0, !0) : !1;
  }
}
class fL extends sS {
  /**
   * @param {string} className CSS class name.
   */
  constructor(t) {
    super(), this.geometry_ = null, this.element_ = document.createElement("div"), this.element_.style.position = "absolute", this.element_.style.pointerEvents = "auto", this.element_.className = "ol-box " + t, this.map_ = null, this.startPixel_ = null, this.endPixel_ = null;
  }
  /**
   * Clean up.
   * @override
   */
  disposeInternal() {
    this.setMap(null);
  }
  /**
   * @private
   */
  render_() {
    const t = this.startPixel_, i = this.endPixel_, a = "px", u = this.element_.style;
    u.left = Math.min(t[0], i[0]) + a, u.top = Math.min(t[1], i[1]) + a, u.width = Math.abs(i[0] - t[0]) + a, u.height = Math.abs(i[1] - t[1]) + a;
  }
  /**
   * @param {import("../Map.js").default|null} map Map.
   */
  setMap(t) {
    if (this.map_) {
      this.map_.getOverlayContainer().removeChild(this.element_);
      const i = this.element_.style;
      i.left = "inherit", i.top = "inherit", i.width = "inherit", i.height = "inherit";
    }
    this.map_ = t, this.map_ && this.map_.getOverlayContainer().appendChild(this.element_);
  }
  /**
   * @param {import("../pixel.js").Pixel} startPixel Start pixel.
   * @param {import("../pixel.js").Pixel} endPixel End pixel.
   */
  setPixels(t, i) {
    this.startPixel_ = t, this.endPixel_ = i, this.createOrUpdateGeometry(), this.render_();
  }
  /**
   * Creates or updates the cached geometry.
   */
  createOrUpdateGeometry() {
    if (!this.map_)
      return;
    const t = this.startPixel_, i = this.endPixel_, u = [
      t,
      [t[0], i[1]],
      i,
      [i[0], t[1]]
    ].map(
      this.map_.getCoordinateFromPixelInternal,
      this.map_
    );
    u[4] = u[0].slice(), this.geometry_ ? this.geometry_.setCoordinates([u]) : this.geometry_ = new va([u]);
  }
  /**
   * @return {import("../geom/Polygon.js").default} Geometry.
   */
  getGeometry() {
    return this.geometry_;
  }
}
const Dm = {
  /**
   * Triggered upon drag box start.
   * @event DragBoxEvent#boxstart
   * @api
   */
  BOXSTART: "boxstart",
  /**
   * Triggered on drag when box is active.
   * @event DragBoxEvent#boxdrag
   * @api
   */
  BOXDRAG: "boxdrag",
  /**
   * Triggered upon drag box end.
   * @event DragBoxEvent#boxend
   * @api
   */
  BOXEND: "boxend",
  /**
   * Triggered upon drag box canceled.
   * @event DragBoxEvent#boxcancel
   * @api
   */
  BOXCANCEL: "boxcancel"
};
class t_ extends ys {
  /**
   * @param {string} type The event type.
   * @param {import("../coordinate.js").Coordinate} coordinate The event coordinate.
   * @param {import("../MapBrowserEvent.js").default} mapBrowserEvent Originating event.
   */
  constructor(t, i, a) {
    super(t), this.coordinate = i, this.mapBrowserEvent = a;
  }
}
class hL extends ld {
  /**
   * @param {Options} [options] Options.
   */
  constructor(t) {
    super(), this.on, this.once, this.un, t = t ?? {}, this.box_ = new fL(t.className || "ol-dragbox"), this.minArea_ = t.minArea ?? 64, t.onBoxEnd && (this.onBoxEnd = t.onBoxEnd), this.startPixel_ = null, this.condition_ = t.condition ?? SA, this.boxEndCondition_ = t.boxEndCondition ?? this.defaultBoxEndCondition;
  }
  /**
   * The default condition for determining whether the boxend event
   * should fire.
   * @param {import("../MapBrowserEvent.js").default} mapBrowserEvent The originating MapBrowserEvent
   *     leading to the box end.
   * @param {import("../pixel.js").Pixel} startPixel The starting pixel of the box.
   * @param {import("../pixel.js").Pixel} endPixel The end pixel of the box.
   * @return {boolean} Whether or not the boxend condition should be fired.
   */
  defaultBoxEndCondition(t, i, a) {
    const u = a[0] - i[0], h = a[1] - i[1];
    return u * u + h * h >= this.minArea_;
  }
  /**
   * Returns geometry of last drawn box.
   * @return {import("../geom/Polygon.js").default} Geometry.
   * @api
   */
  getGeometry() {
    return this.box_.getGeometry();
  }
  /**
   * Handle pointer drag events.
   * @param {import("../MapBrowserEvent.js").default} mapBrowserEvent Event.
   * @override
   */
  handleDragEvent(t) {
    this.startPixel_ && (this.box_.setPixels(this.startPixel_, t.pixel), this.dispatchEvent(
      new t_(
        Dm.BOXDRAG,
        t.coordinate,
        t
      )
    ));
  }
  /**
   * Handle pointer up events.
   * @param {import("../MapBrowserEvent.js").default} mapBrowserEvent Event.
   * @return {boolean} If the event was consumed.
   * @override
   */
  handleUpEvent(t) {
    if (!this.startPixel_)
      return !1;
    const i = this.boxEndCondition_(
      t,
      this.startPixel_,
      t.pixel
    );
    return i && this.onBoxEnd(t), this.dispatchEvent(
      new t_(
        i ? Dm.BOXEND : Dm.BOXCANCEL,
        t.coordinate,
        t
      )
    ), this.box_.setMap(null), this.startPixel_ = null, !1;
  }
  /**
   * Handle pointer down events.
   * @param {import("../MapBrowserEvent.js").default} mapBrowserEvent Event.
   * @return {boolean} If the event was consumed.
   * @override
   */
  handleDownEvent(t) {
    return this.condition_(t) ? (this.startPixel_ = t.pixel, this.box_.setMap(t.map), this.box_.setPixels(this.startPixel_, this.startPixel_), this.dispatchEvent(
      new t_(
        Dm.BOXSTART,
        t.coordinate,
        t
      )
    ), !0) : !1;
  }
  /**
   * Function to execute just before `onboxend` is fired
   * @param {import("../MapBrowserEvent.js").default} event Event.
   */
  onBoxEnd(t) {
  }
  /**
   * Activate or deactivate the interaction.
   * @param {boolean} active Active.
   * @observable
   * @api
   * @override
   */
  setActive(t) {
    t || (this.box_.setMap(null), this.startPixel_ && (this.dispatchEvent(
      new t_(Dm.BOXCANCEL, this.startPixel_, null)
    ), this.startPixel_ = null)), super.setActive(t);
  }
  /**
   * @param {import("../Map.js").default|null} map Map.
   * @override
   */
  setMap(t) {
    this.getMap() && (this.box_.setMap(null), this.startPixel_ && (this.dispatchEvent(
      new t_(Dm.BOXCANCEL, this.startPixel_, null)
    ), this.startPixel_ = null)), super.setMap(t);
  }
}
class dL extends hL {
  /**
   * @param {Options} [options] Options.
   */
  constructor(t) {
    t = t || {};
    const i = t.condition ? t.condition : IT;
    super({
      condition: i,
      className: t.className || "ol-dragzoom",
      minArea: t.minArea
    }), this.duration_ = t.duration !== void 0 ? t.duration : 200, this.out_ = t.out !== void 0 ? t.out : !1;
  }
  /**
   * Function to execute just before `onboxend` is fired
   * @param {import("../MapBrowserEvent.js").default} event Event.
   * @override
   */
  onBoxEnd(t) {
    const a = (
      /** @type {!import("../View.js").default} */
      this.getMap().getView()
    );
    let u = this.getGeometry();
    if (this.out_) {
      const h = a.rotatedExtentForGeometry(u), g = a.getResolutionForExtentInternal(h), y = a.getResolution() / g;
      u = u.clone(), u.scale(y * y);
    }
    a.fitInternal(u, {
      duration: this.duration_,
      easing: ty
    });
  }
}
const Ih = {
  LEFT: "ArrowLeft",
  UP: "ArrowUp",
  RIGHT: "ArrowRight",
  DOWN: "ArrowDown"
};
class gL extends ey {
  /**
   * @param {Options} [options] Options.
   */
  constructor(t) {
    super(), t = t || {}, this.defaultCondition_ = function(i) {
      return BT(i) && EA(i);
    }, this.condition_ = t.condition !== void 0 ? t.condition : this.defaultCondition_, this.duration_ = t.duration !== void 0 ? t.duration : 100, this.pixelDelta_ = t.pixelDelta !== void 0 ? t.pixelDelta : 128;
  }
  /**
   * Handles the {@link module:ol/MapBrowserEvent~MapBrowserEvent map browser event} if it was a
   * `KeyEvent`, and decides the direction to pan to (if an arrow key was
   * pressed).
   * @param {import("../MapBrowserEvent.js").default} mapBrowserEvent Map browser event.
   * @return {boolean} `false` to stop event propagation.
   * @override
   */
  handleEvent(t) {
    let i = !1;
    if (t.type == $t.KEYDOWN) {
      const a = (
        /** @type {KeyboardEvent} */
        t.originalEvent
      ), u = a.key;
      if (this.condition_(t) && (u == Ih.DOWN || u == Ih.LEFT || u == Ih.RIGHT || u == Ih.UP)) {
        const g = t.map.getView(), y = g.getResolution() * this.pixelDelta_;
        let _ = 0, b = 0;
        u == Ih.DOWN ? b = -y : u == Ih.LEFT ? _ = -y : u == Ih.RIGHT ? _ = y : b = y;
        const E = [_, b];
        lT(E, g.getRotation()), eL(g, E, this.duration_), a.preventDefault(), i = !0;
      }
    }
    return !i;
  }
}
class mL extends ey {
  /**
   * @param {Options} [options] Options.
   */
  constructor(t) {
    super(), t = t || {}, this.condition_ = t.condition ? t.condition : function(i) {
      return !oL(i) && EA(i);
    }, this.delta_ = t.delta ? t.delta : 1, this.duration_ = t.duration !== void 0 ? t.duration : 100;
  }
  /**
   * Handles the {@link module:ol/MapBrowserEvent~MapBrowserEvent map browser event} if it was a
   * `KeyEvent`, and decides whether to zoom in or out (depending on whether the
   * key pressed was '+' or '-').
   * @param {import("../MapBrowserEvent.js").default} mapBrowserEvent Map browser event.
   * @return {boolean} `false` to stop event propagation.
   * @override
   */
  handleEvent(t) {
    let i = !1;
    if (t.type == $t.KEYDOWN || t.type == $t.KEYPRESS) {
      const a = (
        /** @type {KeyboardEvent} */
        t.originalEvent
      ), u = a.key;
      if (this.condition_(t) && (u === "+" || u === "-")) {
        const h = t.map, g = u === "+" ? this.delta_ : -this.delta_, y = h.getView();
        jT(y, g, void 0, this.duration_), a.preventDefault(), i = !0;
      }
    }
    return !i;
  }
}
const yL = 40, pL = 300;
class _L extends ey {
  /**
   * @param {Options} [options] Options.
   */
  constructor(t) {
    t = t || {}, super(
      /** @type {import("./Interaction.js").InteractionOptions} */
      t
    ), this.totalDelta_ = 0, this.lastDelta_ = 0, this.maxDelta_ = t.maxDelta !== void 0 ? t.maxDelta : 1, this.duration_ = t.duration !== void 0 ? t.duration : 250, this.timeout_ = t.timeout !== void 0 ? t.timeout : 80, this.useAnchor_ = t.useAnchor !== void 0 ? t.useAnchor : !0, this.constrainResolution_ = t.constrainResolution !== void 0 ? t.constrainResolution : !1;
    const i = t.condition ? t.condition : Kb;
    this.condition_ = t.onFocusOnly ? Fb(vA, i) : i, this.lastAnchor_ = null, this.startTime_ = void 0, this.timeoutId_, this.mode_ = void 0, this.trackpadEventGap_ = 400, this.trackpadTimeoutId_, this.deltaPerZoom_ = 300;
  }
  /**
   * @private
   */
  endInteraction_() {
    this.trackpadTimeoutId_ = void 0;
    const t = this.getMap();
    if (!t)
      return;
    t.getView().endInteraction(
      void 0,
      this.lastDelta_ ? this.lastDelta_ > 0 ? 1 : -1 : 0,
      this.lastAnchor_ ? t.getCoordinateFromPixel(this.lastAnchor_) : null
    );
  }
  /**
   * Handles the {@link module:ol/MapBrowserEvent~MapBrowserEvent map browser event} (if it was a mousewheel-event) and eventually
   * zooms the map.
   * @param {import("../MapBrowserEvent.js").default} mapBrowserEvent Map browser event.
   * @return {boolean} `false` to stop event propagation.
   * @override
   */
  handleEvent(t) {
    if (!this.condition_(t) || t.type !== $t.WHEEL)
      return !0;
    const a = t.map, u = (
      /** @type {WheelEvent} */
      t.originalEvent
    );
    u.preventDefault(), this.useAnchor_ && (this.lastAnchor_ = t.pixel);
    let h = u.deltaY;
    switch (u.deltaMode) {
      case WheelEvent.DOM_DELTA_LINE:
        h *= yL;
        break;
      case WheelEvent.DOM_DELTA_PAGE:
        h *= pL;
        break;
    }
    if (h === 0)
      return !1;
    this.lastDelta_ = h;
    const g = Date.now();
    this.startTime_ === void 0 && (this.startTime_ = g), (!this.mode_ || g - this.startTime_ > this.trackpadEventGap_) && (this.mode_ = Math.abs(h) < 4 ? "trackpad" : "wheel");
    const y = a.getView();
    if (this.mode_ === "trackpad" && !(y.getConstrainResolution() || this.constrainResolution_))
      return this.trackpadTimeoutId_ ? clearTimeout(this.trackpadTimeoutId_) : (y.getAnimating() && y.cancelAnimations(), y.beginInteraction()), this.trackpadTimeoutId_ = setTimeout(
        this.endInteraction_.bind(this),
        this.timeout_
      ), y.adjustZoom(
        -h / this.deltaPerZoom_,
        this.lastAnchor_ ? a.getCoordinateFromPixel(this.lastAnchor_) : null
      ), this.startTime_ = g, !1;
    this.totalDelta_ += h;
    const _ = Math.max(this.timeout_ - (g - this.startTime_), 0);
    return clearTimeout(this.timeoutId_), this.timeoutId_ = setTimeout(
      this.handleWheelZoom_.bind(this, a),
      _
    ), !1;
  }
  /**
   * @private
   * @param {import("../Map.js").default} map Map.
   */
  handleWheelZoom_(t) {
    const i = t.getView();
    i.getAnimating() && i.cancelAnimations();
    let a = -gn(
      this.totalDelta_,
      -this.maxDelta_ * this.deltaPerZoom_,
      this.maxDelta_ * this.deltaPerZoom_
    ) / this.deltaPerZoom_;
    (i.getConstrainResolution() || this.constrainResolution_) && (a = a ? a > 0 ? 1 : -1 : 0), jT(
      i,
      a,
      this.lastAnchor_ ? t.getCoordinateFromPixel(this.lastAnchor_) : null,
      this.duration_
    ), this.mode_ = void 0, this.totalDelta_ = 0, this.lastAnchor_ = null, this.startTime_ = void 0, this.timeoutId_ = void 0;
  }
  /**
   * Enable or disable using the mouse's location as an anchor when zooming
   * @param {boolean} useAnchor true to zoom to the mouse's location, false
   * to zoom to the center of the map
   * @api
   */
  setMouseAnchor(t) {
    this.useAnchor_ = t, t || (this.lastAnchor_ = null);
  }
}
class vL extends ld {
  /**
   * @param {Options} [options] Options.
   */
  constructor(t) {
    t = t || {};
    const i = (
      /** @type {import("./Pointer.js").Options} */
      t
    );
    i.stopDown || (i.stopDown = td), super(i), this.anchor_ = null, this.lastAngle_ = void 0, this.rotating_ = !1, this.rotationDelta_ = 0, this.threshold_ = t.threshold !== void 0 ? t.threshold : 0.3, this.duration_ = t.duration !== void 0 ? t.duration : 250;
  }
  /**
   * Handle pointer drag events.
   * @param {import("../MapBrowserEvent.js").default} mapBrowserEvent Event.
   * @override
   */
  handleDragEvent(t) {
    let i = 0;
    const a = this.targetPointers[0], u = this.targetPointers[1], h = Math.atan2(
      u.clientY - a.clientY,
      u.clientX - a.clientX
    );
    if (this.lastAngle_ !== void 0) {
      const _ = h - this.lastAngle_;
      this.rotationDelta_ += _, !this.rotating_ && Math.abs(this.rotationDelta_) > this.threshold_ && (this.rotating_ = !0), i = _;
    }
    this.lastAngle_ = h;
    const g = t.map, y = g.getView();
    y.getConstraints().rotation !== NT && (this.anchor_ = g.getCoordinateFromPixelInternal(
      g.getEventPixel(XT(this.targetPointers))
    ), this.rotating_ && (g.render(), y.adjustRotationInternal(i, this.anchor_)));
  }
  /**
   * Handle pointer up events.
   * @param {import("../MapBrowserEvent.js").default} mapBrowserEvent Event.
   * @return {boolean} If the event was consumed.
   * @override
   */
  handleUpEvent(t) {
    return this.targetPointers.length < 2 ? (t.map.getView().endInteraction(this.duration_), !1) : !0;
  }
  /**
   * Handle pointer down events.
   * @param {import("../MapBrowserEvent.js").default} mapBrowserEvent Event.
   * @return {boolean} If the event was consumed.
   * @override
   */
  handleDownEvent(t) {
    if (this.targetPointers.length >= 2) {
      const i = t.map;
      return this.anchor_ = null, this.lastAngle_ = void 0, this.rotating_ = !1, this.rotationDelta_ = 0, this.handlingDownUpSequence || i.getView().beginInteraction(), !0;
    }
    return !1;
  }
}
class SL extends ld {
  /**
   * @param {Options} [options] Options.
   */
  constructor(t) {
    t = t || {};
    const i = (
      /** @type {import("./Pointer.js").Options} */
      t
    );
    i.stopDown || (i.stopDown = td), super(i), this.anchor_ = null, this.duration_ = t.duration !== void 0 ? t.duration : 400, this.lastDistance_ = void 0, this.lastScaleDelta_ = 1;
  }
  /**
   * Handle pointer drag events.
   * @param {import("../MapBrowserEvent.js").default} mapBrowserEvent Event.
   * @override
   */
  handleDragEvent(t) {
    let i = 1;
    const a = this.targetPointers[0], u = this.targetPointers[1], h = a.clientX - u.clientX, g = a.clientY - u.clientY, y = Math.sqrt(h * h + g * g);
    this.lastDistance_ !== void 0 && (i = this.lastDistance_ / y), this.lastDistance_ = y;
    const _ = t.map, b = _.getView();
    i != 1 && (this.lastScaleDelta_ = i), this.anchor_ = _.getCoordinateFromPixelInternal(
      _.getEventPixel(XT(this.targetPointers))
    ), _.render(), b.adjustResolutionInternal(i, this.anchor_);
  }
  /**
   * Handle pointer up events.
   * @param {import("../MapBrowserEvent.js").default} mapBrowserEvent Event.
   * @return {boolean} If the event was consumed.
   * @override
   */
  handleUpEvent(t) {
    if (this.targetPointers.length < 2) {
      const a = t.map.getView(), u = this.lastScaleDelta_ > 1 ? 1 : -1;
      return a.endInteraction(this.duration_, u), !1;
    }
    return !0;
  }
  /**
   * Handle pointer down events.
   * @param {import("../MapBrowserEvent.js").default} mapBrowserEvent Event.
   * @return {boolean} If the event was consumed.
   * @override
   */
  handleDownEvent(t) {
    if (this.targetPointers.length >= 2) {
      const i = t.map;
      return this.anchor_ = null, this.lastDistance_ = void 0, this.lastScaleDelta_ = 1, this.handlingDownUpSequence || i.getView().beginInteraction(), !0;
    }
    return !1;
  }
}
function EL(r) {
  r = r || {};
  const t = new Js(), i = new Z3(-5e-3, 0.05, 100);
  return (r.altShiftDragRotate !== void 0 ? r.altShiftDragRotate : !0) && t.push(new cL()), (r.doubleClickZoom !== void 0 ? r.doubleClickZoom : !0) && t.push(
    new nL({
      delta: r.zoomDelta,
      duration: r.zoomDuration
    })
  ), (r.dragPan !== void 0 ? r.dragPan : !0) && t.push(
    new uL({
      onFocusOnly: r.onFocusOnly,
      kinetic: i
    })
  ), (r.pinchRotate !== void 0 ? r.pinchRotate : !0) && t.push(new vL()), (r.pinchZoom !== void 0 ? r.pinchZoom : !0) && t.push(
    new SL({
      duration: r.zoomDuration
    })
  ), (r.keyboard !== void 0 ? r.keyboard : !0) && (t.push(new gL()), t.push(
    new mL({
      delta: r.zoomDelta,
      duration: r.zoomDuration
    })
  )), (r.mouseWheelZoom !== void 0 ? r.mouseWheelZoom : !0) && t.push(
    new _L({
      onFocusOnly: r.onFocusOnly,
      duration: r.zoomDuration
    })
  ), (r.shiftDragZoom !== void 0 ? r.shiftDragZoom : !0) && t.push(
    new dL({
      duration: r.zoomDuration
    })
  ), t;
}
class kc extends ys {
  /**
   * @param {GroupEventType} type The event type.
   * @param {BaseLayer} layer The layer.
   */
  constructor(t, i) {
    super(t), this.layer = i;
  }
}
const yb = {
  LAYERS: "layers"
};
class ny extends yA {
  /**
   * @param {Options} [options] Layer options.
   */
  constructor(t) {
    t = t || {};
    const i = (
      /** @type {Options} */
      Object.assign({}, t)
    );
    delete i.layers;
    let a = t.layers;
    super(i), this.on, this.once, this.un, this.layersListenerKeys_ = [], this.listenerKeys_ = {}, this.addChangeListener(yb.LAYERS, this.handleLayersChanged_), a ? Array.isArray(a) ? a = new Js(a.slice(), { unique: !0 }) : je(
      typeof /** @type {?} */
      a.getArray == "function",
      "Expected `layers` to be an array or a `Collection`"
    ) : a = new Js(void 0, { unique: !0 }), this.setLayers(a);
  }
  /**
   * @private
   */
  handleLayerChange_() {
    this.changed();
  }
  /**
   * @private
   */
  handleLayersChanged_() {
    this.layersListenerKeys_.forEach(qe), this.layersListenerKeys_.length = 0;
    const t = this.getLayers();
    this.layersListenerKeys_.push(
      ge(t, jn.ADD, this.handleLayersAdd_, this),
      ge(
        t,
        jn.REMOVE,
        this.handleLayersRemove_,
        this
      )
    );
    for (const a in this.listenerKeys_)
      this.listenerKeys_[a].forEach(qe);
    Jm(this.listenerKeys_);
    const i = t.getArray();
    for (let a = 0, u = i.length; a < u; a++) {
      const h = i[a];
      this.registerLayerListeners_(h), this.dispatchEvent(new kc("addlayer", h));
    }
    this.changed();
  }
  /**
   * @param {BaseLayer} layer The layer.
   */
  registerLayerListeners_(t) {
    const i = [
      ge(
        t,
        qm.PROPERTYCHANGE,
        this.handleLayerChange_,
        this
      ),
      ge(t, $t.CHANGE, this.handleLayerChange_, this)
    ];
    t instanceof ny && i.push(
      ge(t, "addlayer", this.handleLayerGroupAdd_, this),
      ge(t, "removelayer", this.handleLayerGroupRemove_, this)
    ), this.listenerKeys_[ue(t)] = i;
  }
  /**
   * @param {GroupEvent} event The layer group event.
   */
  handleLayerGroupAdd_(t) {
    this.dispatchEvent(new kc("addlayer", t.layer));
  }
  /**
   * @param {GroupEvent} event The layer group event.
   */
  handleLayerGroupRemove_(t) {
    this.dispatchEvent(new kc("removelayer", t.layer));
  }
  /**
   * @param {import("../Collection.js").CollectionEvent<import("./Base.js").default>} collectionEvent CollectionEvent.
   * @private
   */
  handleLayersAdd_(t) {
    const i = t.element;
    this.registerLayerListeners_(i), this.dispatchEvent(new kc("addlayer", i)), this.changed();
  }
  /**
   * @param {import("../Collection.js").CollectionEvent<import("./Base.js").default>} collectionEvent CollectionEvent.
   * @private
   */
  handleLayersRemove_(t) {
    const i = t.element, a = ue(i);
    this.listenerKeys_[a].forEach(qe), delete this.listenerKeys_[a], this.dispatchEvent(new kc("removelayer", i)), this.changed();
  }
  /**
   * Returns the {@link module:ol/Collection~Collection collection} of {@link module:ol/layer/Layer~Layer layers}
   * in this group.
   * @return {!Collection<import("./Base.js").default>} Collection of
   *   {@link module:ol/layer/Base~BaseLayer layers} that are part of this group.
   * @observable
   * @api
   */
  getLayers() {
    return (
      /** @type {!Collection<import("./Base.js").default>} */
      this.get(yb.LAYERS)
    );
  }
  /**
   * Set the {@link module:ol/Collection~Collection collection} of {@link module:ol/layer/Layer~Layer layers}
   * in this group.
   * @param {!Collection<import("./Base.js").default>} layers Collection of
   *   {@link module:ol/layer/Base~BaseLayer layers} that are part of this group.
   * @observable
   * @api
   */
  setLayers(t) {
    const i = this.getLayers();
    if (i) {
      const a = i.getArray();
      for (let u = 0, h = a.length; u < h; ++u)
        this.dispatchEvent(new kc("removelayer", a[u]));
    }
    this.set(yb.LAYERS, t);
  }
  /**
   * @param {Array<import("./Layer.js").default>} [array] Array of layers (to be modified in place).
   * @return {Array<import("./Layer.js").default>} Array of layers.
   * @override
   */
  getLayersArray(t) {
    return t = t !== void 0 ? t : [], this.getLayers().forEach(function(i) {
      i.getLayersArray(t);
    }), t;
  }
  /**
   * Get the layer states list and use this groups z-index as the default
   * for all layers in this and nested groups, if it is unset at this point.
   * If dest is not provided and this group's z-index is undefined
   * 0 is used a the default z-index.
   * @param {Array<import("./Layer.js").State>} [dest] Optional list
   * of layer states (to be modified in place).
   * @return {Array<import("./Layer.js").State>} List of layer states.
   * @override
   */
  getLayerStatesArray(t) {
    const i = t !== void 0 ? t : [], a = i.length;
    this.getLayers().forEach(function(g) {
      g.getLayerStatesArray(i);
    });
    const u = this.getLayerState();
    let h = u.zIndex;
    !t && u.zIndex === void 0 && (h = 0);
    for (let g = a, y = i.length; g < y; g++) {
      const _ = i[g];
      _.opacity *= u.opacity, _.visible = _.visible && u.visible, _.maxResolution = Math.min(
        _.maxResolution,
        u.maxResolution
      ), _.minResolution = Math.max(
        _.minResolution,
        u.minResolution
      ), _.minZoom = Math.max(_.minZoom, u.minZoom), _.maxZoom = Math.min(_.maxZoom, u.maxZoom), u.extent !== void 0 && (_.extent !== void 0 ? _.extent = Ph(
        _.extent,
        u.extent
      ) : _.extent = u.extent), _.zIndex === void 0 && (_.zIndex = h);
    }
    return i;
  }
  /**
   * @return {import("../source/Source.js").State} Source state.
   * @override
   */
  getSourceState() {
    return "ready";
  }
}
class bL extends sS {
  /**
   * @param {import("../Map.js").default} map Map.
   */
  constructor(t) {
    super(), this.map_ = t;
  }
  /**
   * @abstract
   * @param {import("../render/EventType.js").default} type Event type.
   * @param {import("../Map.js").FrameState} frameState Frame state.
   */
  dispatchRenderEvent(t, i) {
    we();
  }
  /**
   * @param {import("../Map.js").FrameState} frameState FrameState.
   * @protected
   */
  calculateMatrices2D(t) {
    const i = t.viewState, a = t.coordinateToPixelTransform, u = t.pixelToCoordinateTransform;
    vu(
      a,
      t.size[0] / 2,
      t.size[1] / 2,
      1 / i.resolution,
      -1 / i.resolution,
      -i.rotation,
      -i.center[0],
      -i.center[1]
    ), w2(u, a);
  }
  /**
   * @param {import("../coordinate.js").Coordinate} coordinate Coordinate.
   * @param {import("../Map.js").FrameState} frameState FrameState.
   * @param {number} hitTolerance Hit tolerance in pixels.
   * @param {boolean} checkWrapped Check for wrapped geometries.
   * @param {import("./vector.js").FeatureCallback<T>} callback Feature callback.
   * @param {S} thisArg Value to use as `this` when executing `callback`.
   * @param {function(this: U, import("../layer/Layer.js").default): boolean} layerFilter Layer filter
   *     function, only layers which are visible and for which this function
   *     returns `true` will be tested for features.  By default, all visible
   *     layers will be tested.
   * @param {U} thisArg2 Value to use as `this` when executing `layerFilter`.
   * @return {T|undefined} Callback result.
   * @template S,T,U
   */
  forEachFeatureAtCoordinate(t, i, a, u, h, g, y, _) {
    let b;
    const E = i.viewState;
    function x(X, Z, dt, ft) {
      return h.call(g, Z, X ? dt : null, ft);
    }
    const R = E.projection, w = b2(t.slice(), R), L = [[0, 0]];
    if (R.canWrapX() && u) {
      const X = R.getExtent(), Z = Xe(X);
      L.push([-Z, 0], [Z, 0]);
    }
    const H = i.layerStatesArray, N = H.length, O = (
      /** @type {Array<HitMatch<T>>} */
      []
    ), U = [];
    for (let X = 0; X < L.length; X++)
      for (let Z = N - 1; Z >= 0; --Z) {
        const dt = H[Z], ft = dt.layer;
        if (ft.hasRenderer() && GT(dt, E) && y.call(_, ft)) {
          const yt = ft.getRenderer(), it = ft.getSource();
          if (yt && it) {
            const Et = it.getWrapX() ? w : t, xt = x.bind(
              null,
              dt.managed
            );
            U[0] = Et[0] + L[X][0], U[1] = Et[1] + L[X][1], b = yt.forEachFeatureAtCoordinate(
              U,
              i,
              a,
              xt,
              O
            );
          }
          if (b)
            return b;
        }
      }
    if (O.length === 0)
      return;
    const I = 1 / O.length;
    return O.forEach((X, Z) => X.distanceSq += Z * I), O.sort((X, Z) => X.distanceSq - Z.distanceSq), O.some((X) => b = X.callback(X.feature, X.layer, X.geometry)), b;
  }
  /**
   * @param {import("../coordinate.js").Coordinate} coordinate Coordinate.
   * @param {import("../Map.js").FrameState} frameState FrameState.
   * @param {number} hitTolerance Hit tolerance in pixels.
   * @param {boolean} checkWrapped Check for wrapped geometries.
   * @param {function(this: U, import("../layer/Layer.js").default): boolean} layerFilter Layer filter
   *     function, only layers which are visible and for which this function
   *     returns `true` will be tested for features.  By default, all visible
   *     layers will be tested.
   * @param {U} thisArg Value to use as `this` when executing `layerFilter`.
   * @return {boolean} Is there a feature at the given coordinate?
   * @template U
   */
  hasFeatureAtCoordinate(t, i, a, u, h, g) {
    return this.forEachFeatureAtCoordinate(
      t,
      i,
      a,
      u,
      _u,
      this,
      h,
      g
    ) !== void 0;
  }
  /**
   * @return {import("../Map.js").default} Map.
   */
  getMap() {
    return this.map_;
  }
  /**
   * Render.
   * @abstract
   * @param {?import("../Map.js").FrameState} frameState Frame state.
   */
  renderFrame(t) {
    we();
  }
  /**
   * @param {import("../Map.js").FrameState} frameState Frame state.
   * @protected
   */
  scheduleExpireIconCache(t) {
    hs.canExpireCache() && t.postRenderFunctions.push(TL);
  }
}
function TL(r, t) {
  hs.expire();
}
class xL extends bL {
  /**
   * @param {import("../Map.js").default} map Map.
   */
  constructor(t) {
    super(t), this.fontChangeListenerKey_ = ge(
      Nm,
      qm.PROPERTYCHANGE,
      t.redrawText,
      t
    ), this.element_ = document.createElement("div");
    const i = this.element_.style;
    i.position = "absolute", i.width = "100%", i.height = "100%", i.zIndex = "0", this.element_.className = vS + " ol-layers";
    const a = t.getViewport();
    a.insertBefore(this.element_, a.firstChild || null), this.children_ = [], this.renderedVisible_ = !0;
  }
  /**
   * @param {import("../render/EventType.js").default} type Event type.
   * @param {import("../Map.js").FrameState} frameState Frame state.
   * @override
   */
  dispatchRenderEvent(t, i) {
    const a = this.getMap();
    if (a.hasListener(t)) {
      const u = new sA(t, void 0, i);
      a.dispatchEvent(u);
    }
  }
  /**
   * @override
   */
  disposeInternal() {
    qe(this.fontChangeListenerKey_), this.element_.remove(), super.disposeInternal();
  }
  /**
   * Render.
   * @param {?import("../Map.js").FrameState} frameState Frame state.
   * @override
   */
  renderFrame(t) {
    if (!t) {
      this.renderedVisible_ && (this.element_.style.display = "none", this.renderedVisible_ = !1);
      return;
    }
    this.calculateMatrices2D(t), this.dispatchRenderEvent(fs.PRECOMPOSE, t);
    const i = t.layerStatesArray.sort(
      (y, _) => y.zIndex - _.zIndex
    );
    i.some(
      (y) => y.layer instanceof pA && y.layer.getDeclutter()
    ) && (t.declutter = {});
    const u = t.viewState;
    this.children_.length = 0;
    const h = [];
    let g = null;
    for (let y = 0, _ = i.length; y < _; ++y) {
      const b = i[y];
      t.layerIndex = y;
      const E = b.layer, x = E.getSourceState();
      if (!GT(b, u) || x != "ready" && x != "undefined") {
        E.unrender();
        continue;
      }
      const R = E.render(t, g);
      R && (R !== g && (this.children_.push(R), g = R), h.push(b));
    }
    this.declutter(t, h), UD(this.element_, this.children_), this.dispatchRenderEvent(fs.POSTCOMPOSE, t), this.renderedVisible_ || (this.element_.style.display = "", this.renderedVisible_ = !0), this.scheduleExpireIconCache(t);
  }
  /**
   * @param {import("../Map.js").FrameState} frameState Frame state.
   * @param {Array<import('../layer/Layer.js').State>} layerStates Layers.
   */
  declutter(t, i) {
    if (t.declutter) {
      for (let a = i.length - 1; a >= 0; --a) {
        const u = i[a], h = u.layer;
        h.getDeclutter() && h.renderDeclutter(t, u);
      }
      i.forEach(
        (a) => a.layer.renderDeferred(t)
      );
    }
  }
}
function bA(r) {
  if (r instanceof AS) {
    r.setMapInternal(null);
    return;
  }
  r instanceof ny && r.getLayers().forEach(bA);
}
function TA(r, t) {
  if (r instanceof AS) {
    r.setMapInternal(t);
    return;
  }
  if (r instanceof ny) {
    const i = r.getLayers().getArray();
    for (let a = 0, u = i.length; a < u; ++a)
      TA(i[a], t);
  }
}
let xA = class extends tr {
  /**
   * @param {MapOptions} [options] Map options.
   */
  constructor(t) {
    super(), t = t || {}, this.on, this.once, this.un;
    const i = CL(t);
    this.renderComplete_ = !1, this.loaded_ = !0, this.boundHandleBrowserEvent_ = this.handleBrowserEvent.bind(this), this.maxTilesLoading_ = t.maxTilesLoading !== void 0 ? t.maxTilesLoading : 16, this.pixelRatio_ = t.pixelRatio !== void 0 ? t.pixelRatio : zD, this.postRenderTimeoutHandle_, this.animationDelayKey_, this.animationDelay_ = this.animationDelay_.bind(this), this.coordinateToPixelTransform_ = $s(), this.pixelToCoordinateTransform_ = $s(), this.frameIndex_ = 0, this.frameState_ = null, this.previousExtent_ = null, this.viewPropertyListenerKey_ = null, this.viewChangeListenerKey_ = null, this.layerGroupPropertyListenerKeys_ = null, this.viewport_ = document.createElement("div"), this.viewport_.className = "ol-viewport" + ("ontouchstart" in window ? " ol-touch" : ""), this.viewport_.style.position = "relative", this.viewport_.style.overflow = "hidden", this.viewport_.style.width = "100%", this.viewport_.style.height = "100%", this.overlayContainer_ = document.createElement("div"), this.overlayContainer_.style.position = "absolute", this.overlayContainer_.style.zIndex = "0", this.overlayContainer_.style.width = "100%", this.overlayContainer_.style.height = "100%", this.overlayContainer_.style.pointerEvents = "none", this.overlayContainer_.className = "ol-overlaycontainer", this.viewport_.appendChild(this.overlayContainer_), this.overlayContainerStopEvent_ = document.createElement("div"), this.overlayContainerStopEvent_.style.position = "absolute", this.overlayContainerStopEvent_.style.zIndex = "0", this.overlayContainerStopEvent_.style.width = "100%", this.overlayContainerStopEvent_.style.height = "100%", this.overlayContainerStopEvent_.style.pointerEvents = "none", this.overlayContainerStopEvent_.className = "ol-overlaycontainer-stopevent", this.viewport_.appendChild(this.overlayContainerStopEvent_), this.mapBrowserEventHandler_ = null, this.moveTolerance_ = t.moveTolerance, this.keyboardEventTarget_ = i.keyboardEventTarget, this.targetChangeHandlerKeys_ = null, this.targetElement_ = null, this.resizeObserver_ = new ResizeObserver(() => this.updateSize()), this.controls = i.controls || tL(), this.interactions = i.interactions || EL({
      onFocusOnly: !0
    }), this.overlays_ = i.overlays, this.overlayIdIndex_ = {}, this.renderer_ = null, this.postRenderFunctions_ = [], this.tileQueue_ = new P3(
      this.getTilePriority.bind(this),
      this.handleTileChange_.bind(this)
    ), this.addChangeListener(
      ki.LAYERGROUP,
      this.handleLayerGroupChanged_
    ), this.addChangeListener(ki.VIEW, this.handleViewChanged_), this.addChangeListener(ki.SIZE, this.handleSizeChanged_), this.addChangeListener(ki.TARGET, this.handleTargetChanged_), this.setProperties(i.values);
    const a = this;
    t.view && !(t.view instanceof Wo) && t.view.then(function(u) {
      a.setView(new Wo(u));
    }), this.controls.addEventListener(
      jn.ADD,
      /**
       * @param {import("./Collection.js").CollectionEvent<import("./control/Control.js").default>} event CollectionEvent
       */
      (u) => {
        u.element.setMap(this);
      }
    ), this.controls.addEventListener(
      jn.REMOVE,
      /**
       * @param {import("./Collection.js").CollectionEvent<import("./control/Control.js").default>} event CollectionEvent.
       */
      (u) => {
        u.element.setMap(null);
      }
    ), this.interactions.addEventListener(
      jn.ADD,
      /**
       * @param {import("./Collection.js").CollectionEvent<import("./interaction/Interaction.js").default>} event CollectionEvent.
       */
      (u) => {
        u.element.setMap(this);
      }
    ), this.interactions.addEventListener(
      jn.REMOVE,
      /**
       * @param {import("./Collection.js").CollectionEvent<import("./interaction/Interaction.js").default>} event CollectionEvent.
       */
      (u) => {
        u.element.setMap(null);
      }
    ), this.overlays_.addEventListener(
      jn.ADD,
      /**
       * @param {import("./Collection.js").CollectionEvent<import("./Overlay.js").default>} event CollectionEvent.
       */
      (u) => {
        this.addOverlayInternal_(u.element);
      }
    ), this.overlays_.addEventListener(
      jn.REMOVE,
      /**
       * @param {import("./Collection.js").CollectionEvent<import("./Overlay.js").default>} event CollectionEvent.
       */
      (u) => {
        const h = u.element.getId();
        h !== void 0 && delete this.overlayIdIndex_[h.toString()], u.element.setMap(null);
      }
    ), this.controls.forEach(
      /**
       * @param {import("./control/Control.js").default} control Control.
       */
      (u) => {
        u.setMap(this);
      }
    ), this.interactions.forEach(
      /**
       * @param {import("./interaction/Interaction.js").default} interaction Interaction.
       */
      (u) => {
        u.setMap(this);
      }
    ), this.overlays_.forEach(this.addOverlayInternal_.bind(this));
  }
  /**
   * Add the given control to the map.
   * @param {import("./control/Control.js").default} control Control.
   * @api
   */
  addControl(t) {
    this.getControls().push(t);
  }
  /**
   * Add the given interaction to the map. If you want to add an interaction
   * at another point of the collection use `getInteractions()` and the methods
   * available on {@link module:ol/Collection~Collection}. This can be used to
   * stop the event propagation from the handleEvent function. The interactions
   * get to handle the events in the reverse order of this collection.
   * @param {import("./interaction/Interaction.js").default} interaction Interaction to add.
   * @api
   */
  addInteraction(t) {
    this.getInteractions().push(t);
  }
  /**
   * Adds the given layer to the top of this map. If you want to add a layer
   * elsewhere in the stack, use `getLayers()` and the methods available on
   * {@link module:ol/Collection~Collection}.
   * @param {import("./layer/Base.js").default} layer Layer.
   * @api
   */
  addLayer(t) {
    this.getLayerGroup().getLayers().push(t);
  }
  /**
   * @param {import("./layer/Group.js").GroupEvent} event The layer add event.
   * @private
   */
  handleLayerAdd_(t) {
    TA(t.layer, this);
  }
  /**
   * Add the given overlay to the map.
   * @param {import("./Overlay.js").default} overlay Overlay.
   * @api
   */
  addOverlay(t) {
    this.getOverlays().push(t);
  }
  /**
   * This deals with map's overlay collection changes.
   * @param {import("./Overlay.js").default} overlay Overlay.
   * @private
   */
  addOverlayInternal_(t) {
    const i = t.getId();
    i !== void 0 && (this.overlayIdIndex_[i.toString()] = t), t.setMap(this);
  }
  /**
   *
   * Clean up.
   * @override
   */
  disposeInternal() {
    this.controls.clear(), this.interactions.clear(), this.overlays_.clear(), this.resizeObserver_.disconnect(), this.setTarget(null), super.disposeInternal();
  }
  /**
   * Detect features that intersect a pixel on the viewport, and execute a
   * callback with each intersecting feature. Layers included in the detection can
   * be configured through the `layerFilter` option in `options`.
   * For polygons without a fill, only the stroke will be used for hit detection.
   * Polygons must have a fill style applied to ensure that pixels inside a polygon are detected.
   * The fill can be transparent.
   * @param {import("./pixel.js").Pixel} pixel Pixel.
   * @param {function(import("./Feature.js").FeatureLike, import("./layer/Layer.js").default<import("./source/Source").default>, import("./geom/SimpleGeometry.js").default): T} callback Feature callback. The callback will be
   *     called with two arguments. The first argument is one
   *     {@link module:ol/Feature~Feature feature} or
   *     {@link module:ol/render/Feature~RenderFeature render feature} at the pixel, the second is
   *     the {@link module:ol/layer/Layer~Layer layer} of the feature and will be null for
   *     unmanaged layers. To stop detection, callback functions can return a
   *     truthy value.
   * @param {AtPixelOptions} [options] Optional options.
   * @return {T|undefined} Callback result, i.e. the return value of last
   * callback execution, or the first truthy callback return value.
   * @template T
   * @api
   */
  forEachFeatureAtPixel(t, i, a) {
    if (!this.frameState_ || !this.renderer_)
      return;
    const u = this.getCoordinateFromPixelInternal(t);
    a = a !== void 0 ? a : {};
    const h = a.hitTolerance !== void 0 ? a.hitTolerance : 0, g = a.layerFilter !== void 0 ? a.layerFilter : _u, y = a.checkWrapped !== !1;
    return this.renderer_.forEachFeatureAtCoordinate(
      u,
      this.frameState_,
      h,
      y,
      i,
      null,
      g,
      null
    );
  }
  /**
   * Get all features that intersect a pixel on the viewport.
   * For polygons without a fill, only the stroke will be used for hit detection.
   * Polygons must have a fill style applied to ensure that pixels inside a polygon are detected.
   * The fill can be transparent.
   * @param {import("./pixel.js").Pixel} pixel Pixel.
   * @param {AtPixelOptions} [options] Optional options.
   * @return {Array<import("./Feature.js").FeatureLike>} The detected features or
   * an empty array if none were found.
   * @api
   */
  getFeaturesAtPixel(t, i) {
    const a = [];
    return this.forEachFeatureAtPixel(
      t,
      function(u) {
        a.push(u);
      },
      i
    ), a;
  }
  /**
   * Get all layers from all layer groups.
   * @return {Array<import("./layer/Layer.js").default>} Layers.
   * @api
   */
  getAllLayers() {
    const t = [];
    function i(a) {
      a.forEach(function(u) {
        u instanceof ny ? i(u.getLayers()) : t.push(u);
      });
    }
    return i(this.getLayers()), t;
  }
  /**
   * Detect if features intersect a pixel on the viewport. Layers included in the
   * detection can be configured through the `layerFilter` option.
   * For polygons without a fill, only the stroke will be used for hit detection.
   * Polygons must have a fill style applied to ensure that pixels inside a polygon are detected.
   * The fill can be transparent.
   * @param {import("./pixel.js").Pixel} pixel Pixel.
   * @param {AtPixelOptions} [options] Optional options.
   * @return {boolean} Is there a feature at the given pixel?
   * @api
   */
  hasFeatureAtPixel(t, i) {
    if (!this.frameState_ || !this.renderer_)
      return !1;
    const a = this.getCoordinateFromPixelInternal(t);
    i = i !== void 0 ? i : {};
    const u = i.layerFilter !== void 0 ? i.layerFilter : _u, h = i.hitTolerance !== void 0 ? i.hitTolerance : 0, g = i.checkWrapped !== !1;
    return this.renderer_.hasFeatureAtCoordinate(
      a,
      this.frameState_,
      h,
      g,
      u,
      null
    );
  }
  /**
   * Returns the coordinate in user projection for a browser event.
   * @param {MouseEvent} event Event.
   * @return {import("./coordinate.js").Coordinate} Coordinate.
   * @api
   */
  getEventCoordinate(t) {
    return this.getCoordinateFromPixel(this.getEventPixel(t));
  }
  /**
   * Returns the coordinate in view projection for a browser event.
   * @param {MouseEvent} event Event.
   * @return {import("./coordinate.js").Coordinate} Coordinate.
   */
  getEventCoordinateInternal(t) {
    return this.getCoordinateFromPixelInternal(this.getEventPixel(t));
  }
  /**
   * Returns the map pixel position for a browser event relative to the viewport.
   * @param {UIEvent|{clientX: number, clientY: number}} event Event.
   * @return {import("./pixel.js").Pixel} Pixel.
   * @api
   */
  getEventPixel(t) {
    const a = this.viewport_.getBoundingClientRect(), u = this.getSize(), h = a.width / u[0], g = a.height / u[1], y = (
      //FIXME Are we really calling this with a TouchEvent anywhere?
      "changedTouches" in t ? (
        /** @type {TouchEvent} */
        t.changedTouches[0]
      ) : (
        /** @type {MouseEvent} */
        t
      )
    );
    return [
      (y.clientX - a.left) / h,
      (y.clientY - a.top) / g
    ];
  }
  /**
   * Get the target in which this map is rendered.
   * Note that this returns what is entered as an option or in setTarget:
   * if that was an element, it returns an element; if a string, it returns that.
   * @return {HTMLElement|string|undefined} The Element or id of the Element that the
   *     map is rendered in.
   * @observable
   * @api
   */
  getTarget() {
    return (
      /** @type {HTMLElement|string|undefined} */
      this.get(ki.TARGET)
    );
  }
  /**
   * Get the DOM element into which this map is rendered. In contrast to
   * `getTarget` this method always return an `Element`, or `null` if the
   * map has no target.
   * @return {HTMLElement} The element that the map is rendered in.
   * @api
   */
  getTargetElement() {
    return this.targetElement_;
  }
  /**
   * Get the coordinate for a given pixel.  This returns a coordinate in the
   * user projection.
   * @param {import("./pixel.js").Pixel} pixel Pixel position in the map viewport.
   * @return {import("./coordinate.js").Coordinate} The coordinate for the pixel position.
   * @api
   */
  getCoordinateFromPixel(t) {
    return F1(
      this.getCoordinateFromPixelInternal(t),
      this.getView().getProjection()
    );
  }
  /**
   * Get the coordinate for a given pixel.  This returns a coordinate in the
   * map view projection.
   * @param {import("./pixel.js").Pixel} pixel Pixel position in the map viewport.
   * @return {import("./coordinate.js").Coordinate} The coordinate for the pixel position.
   */
  getCoordinateFromPixelInternal(t) {
    const i = this.frameState_;
    return i ? hi(i.pixelToCoordinateTransform, t.slice()) : null;
  }
  /**
   * Get the map controls. Modifying this collection changes the controls
   * associated with the map.
   * @return {Collection<import("./control/Control.js").default>} Controls.
   * @api
   */
  getControls() {
    return this.controls;
  }
  /**
   * Get the map overlays. Modifying this collection changes the overlays
   * associated with the map.
   * @return {Collection<import("./Overlay.js").default>} Overlays.
   * @api
   */
  getOverlays() {
    return this.overlays_;
  }
  /**
   * Get an overlay by its identifier (the value returned by overlay.getId()).
   * Note that the index treats string and numeric identifiers as the same. So
   * `map.getOverlayById(2)` will return an overlay with id `'2'` or `2`.
   * @param {string|number} id Overlay identifier.
   * @return {import("./Overlay.js").default|null} Overlay.
   * @api
   */
  getOverlayById(t) {
    const i = this.overlayIdIndex_[t.toString()];
    return i !== void 0 ? i : null;
  }
  /**
   * Get the map interactions. Modifying this collection changes the interactions
   * associated with the map.
   *
   * Interactions are used for e.g. pan, zoom and rotate.
   * @return {Collection<import("./interaction/Interaction.js").default>} Interactions.
   * @api
   */
  getInteractions() {
    return this.interactions;
  }
  /**
   * Get the layergroup associated with this map.
   * @return {LayerGroup} A layer group containing the layers in this map.
   * @observable
   * @api
   */
  getLayerGroup() {
    return (
      /** @type {LayerGroup} */
      this.get(ki.LAYERGROUP)
    );
  }
  /**
   * Clear any existing layers and add layers to the map.
   * @param {Array<import("./layer/Base.js").default>|Collection<import("./layer/Base.js").default>} layers The layers to be added to the map.
   * @api
   */
  setLayers(t) {
    const i = this.getLayerGroup();
    if (t instanceof Js) {
      i.setLayers(t);
      return;
    }
    const a = i.getLayers();
    a.clear(), a.extend(t);
  }
  /**
   * Get the collection of layers associated with this map.
   * @return {!Collection<import("./layer/Base.js").default>} Layers.
   * @api
   */
  getLayers() {
    return this.getLayerGroup().getLayers();
  }
  /**
   * @return {boolean} Layers have sources that are still loading.
   */
  getLoadingOrNotReady() {
    const t = this.getLayerGroup().getLayerStatesArray();
    for (let i = 0, a = t.length; i < a; ++i) {
      const u = t[i];
      if (!u.visible)
        continue;
      const h = u.layer.getRenderer();
      if (h && !h.ready)
        return !0;
      const g = u.layer.getSource();
      if (g && g.loading)
        return !0;
    }
    return !1;
  }
  /**
   * Get the pixel for a coordinate.  This takes a coordinate in the user
   * projection and returns the corresponding pixel.
   * @param {import("./coordinate.js").Coordinate} coordinate A map coordinate.
   * @return {import("./pixel.js").Pixel} A pixel position in the map viewport.
   * @api
   */
  getPixelFromCoordinate(t) {
    const i = dl(
      t,
      this.getView().getProjection()
    );
    return this.getPixelFromCoordinateInternal(i);
  }
  /**
   * Get the pixel for a coordinate.  This takes a coordinate in the map view
   * projection and returns the corresponding pixel.
   * @param {import("./coordinate.js").Coordinate} coordinate A map coordinate.
   * @return {import("./pixel.js").Pixel} A pixel position in the map viewport.
   */
  getPixelFromCoordinateInternal(t) {
    const i = this.frameState_;
    return i ? hi(
      i.coordinateToPixelTransform,
      t.slice(0, 2)
    ) : null;
  }
  /**
   * Get the map renderer.
   * @return {import("./renderer/Map.js").default|null} Renderer
   */
  getRenderer() {
    return this.renderer_;
  }
  /**
   * Get the size of this map.
   * @return {import("./size.js").Size|undefined} The size in pixels of the map in the DOM.
   * @observable
   * @api
   */
  getSize() {
    return (
      /** @type {import("./size.js").Size|undefined} */
      this.get(ki.SIZE)
    );
  }
  /**
   * Get the view associated with this map. A view manages properties such as
   * center and resolution.
   * @return {View} The view that controls this map.
   * @observable
   * @api
   */
  getView() {
    return (
      /** @type {View} */
      this.get(ki.VIEW)
    );
  }
  /**
   * Get the element that serves as the map viewport.
   * @return {HTMLElement} Viewport.
   * @api
   */
  getViewport() {
    return this.viewport_;
  }
  /**
   * Get the element that serves as the container for overlays.  Elements added to
   * this container will let mousedown and touchstart events through to the map,
   * so clicks and gestures on an overlay will trigger {@link module:ol/MapBrowserEvent~MapBrowserEvent}
   * events.
   * @return {!HTMLElement} The map's overlay container.
   */
  getOverlayContainer() {
    return this.overlayContainer_;
  }
  /**
   * Get the element that serves as a container for overlays that don't allow
   * event propagation. Elements added to this container won't let mousedown and
   * touchstart events through to the map, so clicks and gestures on an overlay
   * don't trigger any {@link module:ol/MapBrowserEvent~MapBrowserEvent}.
   * @return {!HTMLElement} The map's overlay container that stops events.
   */
  getOverlayContainerStopEvent() {
    return this.overlayContainerStopEvent_;
  }
  /**
   * @return {!Document} The document where the map is displayed.
   */
  getOwnerDocument() {
    const t = this.getTargetElement();
    return t ? t.ownerDocument : document;
  }
  /**
   * @param {import("./Tile.js").default} tile Tile.
   * @param {string} tileSourceKey Tile source key.
   * @param {import("./coordinate.js").Coordinate} tileCenter Tile center.
   * @param {number} tileResolution Tile resolution.
   * @return {number} Tile priority.
   */
  getTilePriority(t, i, a, u) {
    return Q3(
      this.frameState_,
      t,
      i,
      a,
      u
    );
  }
  /**
   * @param {PointerEvent|KeyboardEvent|WheelEvent} browserEvent Browser event.
   * @param {string} [type] Type.
   */
  handleBrowserEvent(t, i) {
    i = i || t.type;
    const a = new uu(i, this, t);
    this.handleMapBrowserEvent(a);
  }
  /**
   * @param {MapBrowserEvent} mapBrowserEvent The event to handle.
   */
  handleMapBrowserEvent(t) {
    if (!this.frameState_)
      return;
    const i = t.originalEvent, a = i.type;
    if (a === qb.POINTERDOWN || a === $t.WHEEL || a === $t.KEYDOWN) {
      const u = this.getOwnerDocument(), h = this.viewport_.getRootNode ? this.viewport_.getRootNode() : u, g = (
        /** @type {Node} */
        i.target
      ), y = h instanceof ShadowRoot ? h.host === g ? h.host.ownerDocument : h : h === u ? u.documentElement : h;
      if (
        // Abort if the target is a child of the container for elements whose events are not meant
        // to be handled by map interactions.
        this.overlayContainerStopEvent_.contains(g) || // Abort if the event target is a child of the container that is no longer in the page.
        // It's possible for the target to no longer be in the page if it has been removed in an
        // event listener, this might happen in a Control that recreates it's content based on
        // user interaction either manually or via a render in something like https://reactjs.org/
        !y.contains(g)
      )
        return;
    }
    if (t.frameState = this.frameState_, this.dispatchEvent(t) !== !1) {
      const u = this.getInteractions().getArray().slice();
      for (let h = u.length - 1; h >= 0; h--) {
        const g = u[h];
        if (g.getMap() !== this || !g.getActive() || !this.getTargetElement())
          continue;
        if (!g.handleEvent(t) || t.propagationStopped)
          break;
      }
    }
  }
  /**
   * @protected
   */
  handlePostRender() {
    const t = this.frameState_, i = this.tileQueue_;
    if (!i.isEmpty()) {
      let u = this.maxTilesLoading_, h = u;
      if (t) {
        const g = t.viewHints;
        if (g[Vi.ANIMATING] || g[Vi.INTERACTING]) {
          const y = Date.now() - t.time > 8;
          u = y ? 0 : 8, h = y ? 0 : 2;
        }
      }
      i.getTilesLoading() < u && (i.reprioritize(), i.loadMoreTiles(u, h));
    }
    t && this.renderer_ && !t.animate && (this.renderComplete_ ? (this.hasListener(fs.RENDERCOMPLETE) && this.renderer_.dispatchRenderEvent(
      fs.RENDERCOMPLETE,
      t
    ), this.loaded_ === !1 && (this.loaded_ = !0, this.dispatchEvent(
      new Hm(Xc.LOADEND, this, t)
    ))) : this.loaded_ === !0 && (this.loaded_ = !1, this.dispatchEvent(
      new Hm(Xc.LOADSTART, this, t)
    )));
    const a = this.postRenderFunctions_;
    if (t)
      for (let u = 0, h = a.length; u < h; ++u)
        a[u](this, t);
    a.length = 0;
  }
  /**
   * @private
   */
  handleSizeChanged_() {
    this.getView() && !this.getView().getAnimating() && this.getView().resolveConstraints(0), this.render();
  }
  /**
   * @private
   */
  handleTargetChanged_() {
    if (this.mapBrowserEventHandler_) {
      for (let a = 0, u = this.targetChangeHandlerKeys_.length; a < u; ++a)
        qe(this.targetChangeHandlerKeys_[a]);
      this.targetChangeHandlerKeys_ = null, this.viewport_.removeEventListener(
        $t.CONTEXTMENU,
        this.boundHandleBrowserEvent_
      ), this.viewport_.removeEventListener(
        $t.WHEEL,
        this.boundHandleBrowserEvent_
      ), this.mapBrowserEventHandler_.dispose(), this.mapBrowserEventHandler_ = null, this.viewport_.remove();
    }
    if (this.targetElement_) {
      this.resizeObserver_.unobserve(this.targetElement_);
      const a = this.targetElement_.getRootNode();
      a instanceof ShadowRoot && this.resizeObserver_.unobserve(a.host), this.setSize(void 0);
    }
    const t = this.getTarget(), i = typeof t == "string" ? document.getElementById(t) : t;
    if (this.targetElement_ = i, !i)
      this.renderer_ && (clearTimeout(this.postRenderTimeoutHandle_), this.postRenderTimeoutHandle_ = void 0, this.postRenderFunctions_.length = 0, this.renderer_.dispose(), this.renderer_ = null), this.animationDelayKey_ && (cancelAnimationFrame(this.animationDelayKey_), this.animationDelayKey_ = void 0);
    else {
      i.appendChild(this.viewport_), this.renderer_ || (this.renderer_ = new xL(this)), this.mapBrowserEventHandler_ = new F3(
        this,
        this.moveTolerance_
      );
      for (const h in Me)
        this.mapBrowserEventHandler_.addEventListener(
          Me[h],
          this.handleMapBrowserEvent.bind(this)
        );
      this.viewport_.addEventListener(
        $t.CONTEXTMENU,
        this.boundHandleBrowserEvent_,
        !1
      ), this.viewport_.addEventListener(
        $t.WHEEL,
        this.boundHandleBrowserEvent_,
        X2 ? { passive: !1 } : !1
      );
      let a;
      if (this.keyboardEventTarget_)
        a = this.keyboardEventTarget_;
      else {
        const h = i.getRootNode();
        a = h instanceof ShadowRoot ? h.host : i;
      }
      this.targetChangeHandlerKeys_ = [
        ge(
          a,
          $t.KEYDOWN,
          this.handleBrowserEvent,
          this
        ),
        ge(
          a,
          $t.KEYPRESS,
          this.handleBrowserEvent,
          this
        )
      ];
      const u = i.getRootNode();
      u instanceof ShadowRoot && this.resizeObserver_.observe(u.host), this.resizeObserver_.observe(i);
    }
    this.updateSize();
  }
  /**
   * @private
   */
  handleTileChange_() {
    this.render();
  }
  /**
   * @private
   */
  handleViewPropertyChanged_() {
    this.render();
  }
  /**
   * @private
   */
  handleViewChanged_() {
    this.viewPropertyListenerKey_ && (qe(this.viewPropertyListenerKey_), this.viewPropertyListenerKey_ = null), this.viewChangeListenerKey_ && (qe(this.viewChangeListenerKey_), this.viewChangeListenerKey_ = null);
    const t = this.getView();
    t && (this.updateViewportSize_(this.getSize()), this.viewPropertyListenerKey_ = ge(
      t,
      qm.PROPERTYCHANGE,
      this.handleViewPropertyChanged_,
      this
    ), this.viewChangeListenerKey_ = ge(
      t,
      $t.CHANGE,
      this.handleViewPropertyChanged_,
      this
    ), t.resolveConstraints(0)), this.render();
  }
  /**
   * @private
   */
  handleLayerGroupChanged_() {
    this.layerGroupPropertyListenerKeys_ && (this.layerGroupPropertyListenerKeys_.forEach(qe), this.layerGroupPropertyListenerKeys_ = null);
    const t = this.getLayerGroup();
    t && (this.handleLayerAdd_(new kc("addlayer", t)), this.layerGroupPropertyListenerKeys_ = [
      ge(t, qm.PROPERTYCHANGE, this.render, this),
      ge(t, $t.CHANGE, this.render, this),
      ge(t, "addlayer", this.handleLayerAdd_, this),
      ge(t, "removelayer", this.handleLayerRemove_, this)
    ]), this.render();
  }
  /**
   * @return {boolean} Is rendered.
   */
  isRendered() {
    return !!this.frameState_;
  }
  /**
   * @private
   */
  animationDelay_() {
    this.animationDelayKey_ = void 0, this.renderFrame_(Date.now());
  }
  /**
   * Requests an immediate render in a synchronous manner.
   * @api
   */
  renderSync() {
    this.animationDelayKey_ && cancelAnimationFrame(this.animationDelayKey_), this.animationDelay_();
  }
  /**
   * Redraws all text after new fonts have loaded
   */
  redrawText() {
    if (!this.frameState_)
      return;
    const t = this.frameState_.layerStatesArray;
    for (let i = 0, a = t.length; i < a; ++i) {
      const u = t[i].layer;
      u.hasRenderer() && u.getRenderer().handleFontsChanged();
    }
  }
  /**
   * Request a map rendering (at the next animation frame).
   * @api
   */
  render() {
    this.renderer_ && this.animationDelayKey_ === void 0 && (this.animationDelayKey_ = requestAnimationFrame(this.animationDelay_));
  }
  /**
   * Remove the given control from the map.
   * @param {import("./control/Control.js").default} control Control.
   * @return {import("./control/Control.js").default|undefined} The removed control (or undefined
   *     if the control was not found).
   * @api
   */
  removeControl(t) {
    return this.getControls().remove(t);
  }
  /**
   * Remove the given interaction from the map.
   * @param {import("./interaction/Interaction.js").default} interaction Interaction to remove.
   * @return {import("./interaction/Interaction.js").default|undefined} The removed interaction (or
   *     undefined if the interaction was not found).
   * @api
   */
  removeInteraction(t) {
    return this.getInteractions().remove(t);
  }
  /**
   * Removes the given layer from the map.
   * @param {import("./layer/Base.js").default} layer Layer.
   * @return {import("./layer/Base.js").default|undefined} The removed layer (or undefined if the
   *     layer was not found).
   * @api
   */
  removeLayer(t) {
    return this.getLayerGroup().getLayers().remove(t);
  }
  /**
   * @param {import("./layer/Group.js").GroupEvent} event The layer remove event.
   * @private
   */
  handleLayerRemove_(t) {
    bA(t.layer);
  }
  /**
   * Remove the given overlay from the map.
   * @param {import("./Overlay.js").default} overlay Overlay.
   * @return {import("./Overlay.js").default|undefined} The removed overlay (or undefined
   *     if the overlay was not found).
   * @api
   */
  removeOverlay(t) {
    return this.getOverlays().remove(t);
  }
  /**
   * @param {number} time Time.
   * @private
   */
  renderFrame_(t) {
    const i = this.getSize(), a = this.getView(), u = this.frameState_;
    let h = null;
    if (i !== void 0 && LR(i) && a && a.isDef()) {
      const g = a.getHints(
        this.frameState_ ? this.frameState_.viewHints : void 0
      ), y = a.getState();
      if (h = {
        animate: !1,
        coordinateToPixelTransform: this.coordinateToPixelTransform_,
        declutter: null,
        extent: Ob(
          y.center,
          y.resolution,
          y.rotation,
          i
        ),
        index: this.frameIndex_++,
        layerIndex: 0,
        layerStatesArray: this.getLayerGroup().getLayerStatesArray(),
        pixelRatio: this.pixelRatio_,
        pixelToCoordinateTransform: this.pixelToCoordinateTransform_,
        postRenderFunctions: [],
        size: i,
        tileQueue: this.tileQueue_,
        time: t,
        usedTiles: {},
        viewState: y,
        viewHints: g,
        wantedTiles: {},
        mapId: ue(this),
        renderTargets: {}
      }, y.nextCenter && y.nextResolution) {
        const _ = isNaN(y.nextRotation) ? y.rotation : y.nextRotation;
        h.nextExtent = Ob(
          y.nextCenter,
          y.nextResolution,
          _,
          i
        );
      }
    }
    this.frameState_ = h, this.renderer_.renderFrame(h), h && (h.animate && this.render(), Array.prototype.push.apply(
      this.postRenderFunctions_,
      h.postRenderFunctions
    ), u && (!this.previousExtent_ || !M_(this.previousExtent_) && !d_(h.extent, this.previousExtent_)) && (this.dispatchEvent(
      new Hm(Xc.MOVESTART, this, u)
    ), this.previousExtent_ = $m(this.previousExtent_)), this.previousExtent_ && !h.viewHints[Vi.ANIMATING] && !h.viewHints[Vi.INTERACTING] && !d_(h.extent, this.previousExtent_) && (this.dispatchEvent(
      new Hm(Xc.MOVEEND, this, h)
    ), d2(h.extent, this.previousExtent_))), this.dispatchEvent(new Hm(Xc.POSTRENDER, this, h)), this.renderComplete_ = (this.hasListener(Xc.LOADSTART) || this.hasListener(Xc.LOADEND) || this.hasListener(fs.RENDERCOMPLETE)) && !this.tileQueue_.getTilesLoading() && !this.tileQueue_.getCount() && !this.getLoadingOrNotReady(), this.postRenderTimeoutHandle_ || (this.postRenderTimeoutHandle_ = setTimeout(() => {
      this.postRenderTimeoutHandle_ = void 0, this.handlePostRender();
    }, 0));
  }
  /**
   * Sets the layergroup of this map.
   * @param {LayerGroup} layerGroup A layer group containing the layers in this map.
   * @observable
   * @api
   */
  setLayerGroup(t) {
    const i = this.getLayerGroup();
    i && this.handleLayerRemove_(new kc("removelayer", i)), this.set(ki.LAYERGROUP, t);
  }
  /**
   * Set the size of this map.
   * @param {import("./size.js").Size|undefined} size The size in pixels of the map in the DOM.
   * @observable
   * @api
   */
  setSize(t) {
    this.set(ki.SIZE, t);
  }
  /**
   * Set the target element to render this map into.
   * For accessibility (focus and keyboard events for map navigation), the `target` element must have a
   *  properly configured `tabindex` attribute. If the `target` element is inside a Shadow DOM, the
   *  `tabindex` atribute must be set on the custom element's host element.
   * @param {HTMLElement|string} [target] The Element or id of the Element
   *     that the map is rendered in.
   * @observable
   * @api
   */
  setTarget(t) {
    this.set(ki.TARGET, t);
  }
  /**
   * Set the view for this map.
   * @param {View|Promise<import("./View.js").ViewOptions>|null} view The view that controls this map.
   * It is also possible to pass a promise that resolves to options for constructing a view.  This
   * alternative allows view properties to be resolved by sources or other components that load
   * view-related metadata.
   * @observable
   * @api
   */
  setView(t) {
    if (!t || t instanceof Wo) {
      this.set(ki.VIEW, t);
      return;
    }
    this.set(ki.VIEW, new Wo());
    const i = this;
    t.then(function(a) {
      i.setView(new Wo(a));
    });
  }
  /**
   * Force a recalculation of the map viewport size.  This should be called when
   * third-party code changes the size of the map viewport.
   * @api
   */
  updateSize() {
    const t = this.getTargetElement();
    let i;
    if (t) {
      const u = getComputedStyle(t), h = t.offsetWidth - parseFloat(u.borderLeftWidth) - parseFloat(u.paddingLeft) - parseFloat(u.paddingRight) - parseFloat(u.borderRightWidth), g = t.offsetHeight - parseFloat(u.borderTopWidth) - parseFloat(u.paddingTop) - parseFloat(u.paddingBottom) - parseFloat(u.borderBottomWidth);
      !isNaN(h) && !isNaN(g) && (i = [Math.max(0, h), Math.max(0, g)], !LR(i) && (t.offsetWidth || t.offsetHeight || t.getClientRects().length) && S2(
        "No map visible because the map container's width or height are 0."
      ));
    }
    const a = this.getSize();
    i && (!a || !Wc(i, a)) && (this.setSize(i), this.updateViewportSize_(i));
  }
  /**
   * Recomputes the viewport size and save it on the view object (if any)
   * @param {import("./size.js").Size|undefined} size The size.
   * @private
   */
  updateViewportSize_(t) {
    const i = this.getView();
    i && i.setViewportSize(t);
  }
};
function CL(r) {
  let t = null;
  r.keyboardEventTarget !== void 0 && (t = typeof r.keyboardEventTarget == "string" ? document.getElementById(r.keyboardEventTarget) : r.keyboardEventTarget);
  const i = {}, a = r.layers && typeof /** @type {?} */
  r.layers.getLayers == "function" ? (
    /** @type {LayerGroup} */
    r.layers
  ) : new ny({
    layers: (
      /** @type {Collection<import("./layer/Base.js").default>|Array<import("./layer/Base.js").default>} */
      r.layers
    )
  });
  i[ki.LAYERGROUP] = a, i[ki.TARGET] = r.target, i[ki.VIEW] = r.view instanceof Wo ? r.view : new Wo();
  let u;
  r.controls !== void 0 && (Array.isArray(r.controls) ? u = new Js(r.controls.slice()) : (je(
    typeof /** @type {?} */
    r.controls.getArray == "function",
    "Expected `controls` to be an array or an `ol/Collection.js`"
  ), u = r.controls));
  let h;
  r.interactions !== void 0 && (Array.isArray(r.interactions) ? h = new Js(r.interactions.slice()) : (je(
    typeof /** @type {?} */
    r.interactions.getArray == "function",
    "Expected `interactions` to be an array or an `ol/Collection.js`"
  ), h = r.interactions));
  let g;
  return r.overlays !== void 0 ? Array.isArray(r.overlays) ? g = new Js(r.overlays.slice()) : (je(
    typeof /** @type {?} */
    r.overlays.getArray == "function",
    "Expected `overlays` to be an array or an `ol/Collection.js`"
  ), g = r.overlays) : g = new Js(), {
    controls: u,
    interactions: h,
    keyboardEventTarget: t,
    overlays: g,
    values: i
  };
}
class kT {
  /**
   * @param {number} minX Minimum X.
   * @param {number} maxX Maximum X.
   * @param {number} minY Minimum Y.
   * @param {number} maxY Maximum Y.
   */
  constructor(t, i, a, u) {
    this.minX = t, this.maxX = i, this.minY = a, this.maxY = u;
  }
  /**
   * @param {import("./tilecoord.js").TileCoord} tileCoord Tile coordinate.
   * @return {boolean} Contains tile coordinate.
   */
  contains(t) {
    return this.containsXY(t[1], t[2]);
  }
  /**
   * @param {TileRange} tileRange Tile range.
   * @return {boolean} Contains.
   */
  containsTileRange(t) {
    return this.minX <= t.minX && t.maxX <= this.maxX && this.minY <= t.minY && t.maxY <= this.maxY;
  }
  /**
   * @param {number} x Tile coordinate x.
   * @param {number} y Tile coordinate y.
   * @return {boolean} Contains coordinate.
   */
  containsXY(t, i) {
    return this.minX <= t && t <= this.maxX && this.minY <= i && i <= this.maxY;
  }
  /**
   * @param {TileRange} tileRange Tile range.
   * @return {boolean} Equals.
   */
  equals(t) {
    return this.minX == t.minX && this.minY == t.minY && this.maxX == t.maxX && this.maxY == t.maxY;
  }
  /**
   * @param {TileRange} tileRange Tile range.
   */
  extend(t) {
    t.minX < this.minX && (this.minX = t.minX), t.maxX > this.maxX && (this.maxX = t.maxX), t.minY < this.minY && (this.minY = t.minY), t.maxY > this.maxY && (this.maxY = t.maxY);
  }
  /**
   * @return {number} Height.
   */
  getHeight() {
    return this.maxY - this.minY + 1;
  }
  /**
   * @return {import("./size.js").Size} Size.
   */
  getSize() {
    return [this.getWidth(), this.getHeight()];
  }
  /**
   * @return {number} Width.
   */
  getWidth() {
    return this.maxX - this.minX + 1;
  }
  /**
   * @param {TileRange} tileRange Tile range.
   * @return {boolean} Intersects.
   */
  intersects(t) {
    return this.minX <= t.maxX && this.maxX >= t.minX && this.minY <= t.maxY && this.maxY >= t.minY;
  }
}
function Om(r, t, i, a, u) {
  return u !== void 0 ? (u.minX = r, u.maxX = t, u.minY = i, u.maxY = a, u) : new kT(r, t, i, a);
}
const G1 = {
  /**
   * Triggered upon feature draw start
   * @event DrawEvent#drawstart
   * @api
   */
  DRAWSTART: "drawstart",
  /**
   * Triggered upon feature draw end
   * @event DrawEvent#drawend
   * @api
   */
  DRAWEND: "drawend",
  /**
   * Triggered upon feature draw abortion
   * @event DrawEvent#drawabort
   * @api
   */
  DRAWABORT: "drawabort"
};
class H1 extends ys {
  /**
   * @param {DrawEventType} type Type.
   * @param {Feature} feature The feature drawn.
   */
  constructor(t, i) {
    super(t), this.feature = i;
  }
}
function RL(r, t) {
  const i = [];
  for (let a = 0; a < t.length; ++a) {
    const h = t[a].getGeometry();
    CA(r, h, i);
  }
  return i;
}
function Y1(r, t) {
  return du(r[0], r[1], t[0], t[1]);
}
function jm(r, t) {
  const i = r.length;
  return t < 0 ? r[t + i] : t >= i ? r[t - i] : r[t];
}
function j1(r, t, i) {
  let a, u;
  t < i ? (a = t, u = i) : (a = i, u = t);
  const h = Math.ceil(a), g = Math.floor(u);
  if (h > g) {
    const _ = Bm(r, a), b = Bm(r, u);
    return Y1(_, b);
  }
  let y = 0;
  if (a < h) {
    const _ = Bm(r, a), b = jm(r, h);
    y += Y1(_, b);
  }
  if (g < u) {
    const _ = jm(r, g), b = Bm(r, u);
    y += Y1(_, b);
  }
  for (let _ = h; _ < g - 1; ++_) {
    const b = jm(r, _), E = jm(r, _ + 1);
    y += Y1(b, E);
  }
  return y;
}
function CA(r, t, i) {
  if (t instanceof to) {
    B1(r, t.getCoordinates(), !1, i);
    return;
  }
  if (t instanceof T_) {
    const a = t.getCoordinates();
    for (let u = 0, h = a.length; u < h; ++u)
      B1(r, a[u], !1, i);
    return;
  }
  if (t instanceof va) {
    const a = t.getCoordinates();
    for (let u = 0, h = a.length; u < h; ++u)
      B1(r, a[u], !0, i);
    return;
  }
  if (t instanceof x_) {
    const a = t.getCoordinates();
    for (let u = 0, h = a.length; u < h; ++u) {
      const g = a[u];
      for (let y = 0, _ = g.length; y < _; ++y)
        B1(r, g[y], !0, i);
    }
    return;
  }
  if (t instanceof J1) {
    const a = t.getGeometries();
    for (let u = 0; u < a.length; ++u)
      CA(r, a[u], i);
    return;
  }
}
const pb = { index: -1, endIndex: NaN };
function AL(r, t, i, a) {
  const u = r[0], h = r[1];
  let g = 1 / 0, y = -1, _ = NaN;
  for (let x = 0; x < t.targets.length; ++x) {
    const R = t.targets[x], w = R.coordinates;
    let L = 1 / 0, H;
    for (let N = 0; N < w.length - 1; ++N) {
      const O = w[N], U = w[N + 1], I = RA(u, h, O, U);
      I.squaredDistance < L && (L = I.squaredDistance, H = N + I.along);
    }
    L < g && (g = L, R.ring && t.targetIndex === x && (R.endIndex > R.startIndex ? H < R.startIndex && (H += w.length) : R.endIndex < R.startIndex && H > R.startIndex && (H -= w.length)), _ = H, y = x);
  }
  const b = t.targets[y];
  let E = b.ring;
  if (t.targetIndex === y && E) {
    const x = Bm(
      b.coordinates,
      _
    ), R = i.getPixelFromCoordinate(x);
    E2(R, t.startPx) > a && (E = !1);
  }
  if (E) {
    const x = b.coordinates, R = x.length, w = b.startIndex, L = _;
    if (w < L) {
      const H = j1(
        x,
        w,
        L
      );
      j1(
        x,
        w,
        L - R
      ) < H && (_ -= R);
    } else {
      const H = j1(
        x,
        w,
        L
      );
      j1(
        x,
        w,
        L + R
      ) < H && (_ += R);
    }
  }
  return pb.index = y, pb.endIndex = _, pb;
}
function B1(r, t, i, a) {
  const u = r[0], h = r[1];
  for (let g = 0, y = t.length - 1; g < y; ++g) {
    const _ = t[g], b = t[g + 1], E = RA(u, h, _, b);
    if (E.squaredDistance === 0) {
      const x = g + E.along;
      a.push({
        coordinates: t,
        ring: i,
        startIndex: x,
        endIndex: x
      });
      return;
    }
  }
}
const _b = { along: 0, squaredDistance: 0 };
function RA(r, t, i, a) {
  const u = i[0], h = i[1], g = a[0], y = a[1], _ = g - u, b = y - h;
  let E = 0, x = u, R = h;
  return (_ !== 0 || b !== 0) && (E = gn(((r - u) * _ + (t - h) * b) / (_ * _ + b * b), 0, 1), x += _ * E, R += b * E), _b.along = E, _b.squaredDistance = w_(du(r, t, x, R), 10), _b;
}
function Bm(r, t) {
  const i = r.length;
  let a = Math.floor(t);
  const u = t - a;
  a >= i ? a -= i : a < 0 && (a += i);
  let h = a + 1;
  h >= i && (h -= i);
  const g = r[a], y = g[0], _ = g[1], b = r[h], E = b[0] - y, x = b[1] - _;
  return [y + E * u, _ + x * u];
}
class ML extends ld {
  /**
   * @param {Options} options Options.
   */
  constructor(t) {
    const i = (
      /** @type {import("./Pointer.js").Options} */
      t
    );
    i.stopDown || (i.stopDown = td), super(i), this.on, this.once, this.un, this.shouldHandle_ = !1, this.downPx_ = null, this.downTimeout_, this.lastDragTime_, this.pointerType_, this.freehand_ = !1, this.source_ = t.source ? t.source : null, this.features_ = t.features ? t.features : null, this.snapTolerance_ = t.snapTolerance ? t.snapTolerance : 12, this.type_ = /** @type {import("../geom/Geometry.js").Type} */
    t.type, this.mode_ = DL(this.type_), this.stopClick_ = !!t.stopClick, this.minPoints_ = t.minPoints ? t.minPoints : this.mode_ === "Polygon" ? 3 : 2, this.maxPoints_ = this.mode_ === "Circle" ? 2 : t.maxPoints ? t.maxPoints : 1 / 0, this.finishCondition_ = t.finishCondition ? t.finishCondition : _u, this.geometryLayout_ = t.geometryLayout ? t.geometryLayout : "XY";
    let a = t.geometryFunction;
    if (!a) {
      const u = this.mode_;
      if (u === "Circle")
        a = (h, g, y) => {
          const _ = g || new SS([NaN, NaN]), b = dl(h[0], y), E = u_(
            b,
            dl(h[h.length - 1], y)
          );
          _.setCenterAndRadius(
            b,
            Math.sqrt(E),
            this.geometryLayout_
          );
          const x = Wh();
          return x && _.transform(y, x), _;
        };
      else {
        let h;
        u === "Point" ? h = gu : u === "LineString" ? h = to : u === "Polygon" && (h = va), a = (g, y, _) => (y ? u === "Polygon" ? g[0].length ? y.setCoordinates(
          [g[0].concat([g[0][0]])],
          this.geometryLayout_
        ) : y.setCoordinates([], this.geometryLayout_) : y.setCoordinates(g, this.geometryLayout_) : y = new h(g, this.geometryLayout_), y);
      }
    }
    this.geometryFunction_ = a, this.dragVertexDelay_ = t.dragVertexDelay !== void 0 ? t.dragVertexDelay : 500, this.finishCoordinate_ = null, this.sketchFeature_ = null, this.sketchPoint_ = null, this.sketchCoords_ = null, this.sketchLine_ = null, this.sketchLineCoords_ = null, this.squaredClickTolerance_ = t.clickTolerance ? t.clickTolerance * t.clickTolerance : 36, this.overlay_ = new MS({
      source: new MT({
        useSpatialIndex: !1,
        wrapX: t.wrapX ? t.wrapX : !1
      }),
      style: t.style ? t.style : wL(),
      updateWhileInteracting: !0
    }), this.geometryName_ = t.geometryName, this.condition_ = t.condition ? t.condition : BT, this.freehandCondition_, t.freehand ? this.freehandCondition_ = Kb : this.freehandCondition_ = t.freehandCondition ? t.freehandCondition : IT, this.traceCondition_, this.setTrace(t.trace || !1), this.traceState_ = { active: !1 }, this.traceSource_ = t.traceSource || t.source || null, this.addChangeListener(Zb.ACTIVE, this.updateState_);
  }
  /**
   * Toggle tracing mode or set a tracing condition.
   *
   * @param {boolean|import("../events/condition.js").Condition} trace A boolean to toggle tracing mode or an event
   *     condition that will be checked when a feature is clicked to determine if tracing should be active.
   */
  setTrace(t) {
    let i;
    t ? t === !0 ? i = Kb : i = t : i = Pb, this.traceCondition_ = i;
  }
  /**
   * Remove the interaction from its current map and attach it to the new map.
   * Subclasses may set up event handlers to get notified about changes to
   * the map here.
   * @param {import("../Map.js").default} map Map.
   * @override
   */
  setMap(t) {
    super.setMap(t), this.updateState_();
  }
  /**
   * Get the overlay layer that this interaction renders sketch features to.
   * @return {VectorLayer} Overlay layer.
   * @api
   */
  getOverlay() {
    return this.overlay_;
  }
  /**
   * Handles the {@link module:ol/MapBrowserEvent~MapBrowserEvent map browser event} and may actually draw or finish the drawing.
   * @param {import("../MapBrowserEvent.js").default<PointerEvent>} event Map browser event.
   * @return {boolean} `false` to stop event propagation.
   * @api
   * @override
   */
  handleEvent(t) {
    t.originalEvent.type === $t.CONTEXTMENU && t.originalEvent.preventDefault(), this.freehand_ = this.mode_ !== "Point" && this.freehandCondition_(t);
    let i = t.type === Me.POINTERMOVE, a = !0;
    return !this.freehand_ && this.lastDragTime_ && t.type === Me.POINTERDRAG && (Date.now() - this.lastDragTime_ >= this.dragVertexDelay_ ? (this.downPx_ = t.pixel, this.shouldHandle_ = !this.freehand_, i = !0) : this.lastDragTime_ = void 0, this.shouldHandle_ && this.downTimeout_ !== void 0 && (clearTimeout(this.downTimeout_), this.downTimeout_ = void 0)), this.freehand_ && t.type === Me.POINTERDRAG && this.sketchFeature_ !== null ? (this.addToDrawing_(t.coordinate), a = !1) : this.freehand_ && t.type === Me.POINTERDOWN ? a = !1 : i && this.getPointerCount() < 2 ? (a = t.type === Me.POINTERMOVE, a && this.freehand_ ? (this.handlePointerMove_(t), this.shouldHandle_ && t.originalEvent.preventDefault()) : (t.originalEvent.pointerType === "mouse" || t.type === Me.POINTERDRAG && this.downTimeout_ === void 0) && this.handlePointerMove_(t)) : t.type === Me.DBLCLICK && (a = !1), super.handleEvent(t) && a;
  }
  /**
   * Handle pointer down events.
   * @param {import("../MapBrowserEvent.js").default<PointerEvent>} event Event.
   * @return {boolean} If the event was consumed.
   * @override
   */
  handleDownEvent(t) {
    return this.shouldHandle_ = !this.freehand_, this.freehand_ ? (this.downPx_ = t.pixel, this.finishCoordinate_ || this.startDrawing_(t.coordinate), !0) : this.condition_(t) ? (this.lastDragTime_ = Date.now(), this.downTimeout_ = setTimeout(() => {
      this.handlePointerMove_(
        new uu(
          Me.POINTERMOVE,
          t.map,
          t.originalEvent,
          !1,
          t.frameState
        )
      );
    }, this.dragVertexDelay_), this.downPx_ = t.pixel, !0) : (this.lastDragTime_ = void 0, !1);
  }
  /**
   * @private
   */
  deactivateTrace_() {
    this.traceState_ = { active: !1 };
  }
  /**
   * Activate or deactivate trace state based on a browser event.
   * @param {import("../MapBrowserEvent.js").default} event Event.
   * @private
   */
  toggleTraceState_(t) {
    if (!this.traceSource_ || !this.traceCondition_(t))
      return;
    if (this.traceState_.active) {
      this.deactivateTrace_();
      return;
    }
    const i = this.getMap(), a = i.getCoordinateFromPixel([
      t.pixel[0] - this.snapTolerance_,
      t.pixel[1] + this.snapTolerance_
    ]), u = i.getCoordinateFromPixel([
      t.pixel[0] + this.snapTolerance_,
      t.pixel[1] - this.snapTolerance_
    ]), h = Vh([a, u]), g = this.traceSource_.getFeaturesInExtent(h);
    if (g.length === 0)
      return;
    const y = RL(t.coordinate, g);
    y.length && (this.traceState_ = {
      active: !0,
      startPx: t.pixel.slice(),
      targets: y,
      targetIndex: -1
    });
  }
  /**
   * @param {TraceTarget} target The trace target.
   * @param {number} endIndex The new end index of the trace.
   * @private
   */
  addOrRemoveTracedCoordinates_(t, i) {
    const a = t.startIndex <= t.endIndex, u = t.startIndex <= i;
    a === u ? a && i > t.endIndex || !a && i < t.endIndex ? this.addTracedCoordinates_(t, t.endIndex, i) : (a && i < t.endIndex || !a && i > t.endIndex) && this.removeTracedCoordinates_(i, t.endIndex) : (this.removeTracedCoordinates_(t.startIndex, t.endIndex), this.addTracedCoordinates_(t, t.startIndex, i));
  }
  /**
   * @param {number} fromIndex The start index.
   * @param {number} toIndex The end index.
   * @private
   */
  removeTracedCoordinates_(t, i) {
    if (t === i)
      return;
    let a = 0;
    if (t < i) {
      const u = Math.ceil(t);
      let h = Math.floor(i);
      h === i && (h -= 1), a = h - u + 1;
    } else {
      const u = Math.floor(t);
      let h = Math.ceil(i);
      h === i && (h += 1), a = u - h + 1;
    }
    a > 0 && this.removeLastPoints_(a);
  }
  /**
   * @param {TraceTarget} target The trace target.
   * @param {number} fromIndex The start index.
   * @param {number} toIndex The end index.
   * @private
   */
  addTracedCoordinates_(t, i, a) {
    if (i === a)
      return;
    const u = [];
    if (i < a) {
      const h = Math.ceil(i);
      let g = Math.floor(a);
      g === a && (g -= 1);
      for (let y = h; y <= g; ++y)
        u.push(jm(t.coordinates, y));
    } else {
      const h = Math.floor(i);
      let g = Math.ceil(a);
      g === a && (g += 1);
      for (let y = h; y >= g; --y)
        u.push(jm(t.coordinates, y));
    }
    u.length && this.appendCoordinates(u);
  }
  /**
   * Update the trace.
   * @param {import("../MapBrowserEvent.js").default} event Event.
   * @private
   */
  updateTrace_(t) {
    const i = this.traceState_;
    if (!i.active || i.targetIndex === -1 && E2(i.startPx, t.pixel) < this.snapTolerance_)
      return;
    const a = AL(
      t.coordinate,
      i,
      this.getMap(),
      this.snapTolerance_
    );
    if (i.targetIndex !== a.index) {
      if (i.targetIndex !== -1) {
        const _ = i.targets[i.targetIndex];
        this.removeTracedCoordinates_(_.startIndex, _.endIndex);
      }
      const y = i.targets[a.index];
      this.addTracedCoordinates_(
        y,
        y.startIndex,
        a.endIndex
      );
    } else {
      const y = i.targets[i.targetIndex];
      this.addOrRemoveTracedCoordinates_(y, a.endIndex);
    }
    i.targetIndex = a.index;
    const u = i.targets[i.targetIndex];
    u.endIndex = a.endIndex;
    const h = Bm(
      u.coordinates,
      u.endIndex
    ), g = this.getMap().getPixelFromCoordinate(h);
    t.coordinate = h, t.pixel = [Math.round(g[0]), Math.round(g[1])];
  }
  /**
   * Handle pointer up events.
   * @param {import("../MapBrowserEvent.js").default<PointerEvent>} event Event.
   * @return {boolean} If the event was consumed.
   * @override
   */
  handleUpEvent(t) {
    let i = !0;
    if (this.getPointerCount() === 0) {
      this.downTimeout_ && (clearTimeout(this.downTimeout_), this.downTimeout_ = void 0), this.handlePointerMove_(t);
      const a = this.traceState_.active;
      if (this.toggleTraceState_(t), this.shouldHandle_) {
        const u = !this.finishCoordinate_;
        u && this.startDrawing_(t.coordinate), !u && this.freehand_ ? this.finishDrawing() : !this.freehand_ && (!u || this.mode_ === "Point") && (this.atFinish_(t.pixel, a) ? this.finishCondition_(t) && this.finishDrawing() : this.addToDrawing_(t.coordinate)), i = !1;
      } else this.freehand_ && this.abortDrawing();
    }
    return !i && this.stopClick_ && t.preventDefault(), i;
  }
  /**
   * Handle move events.
   * @param {import("../MapBrowserEvent.js").default<PointerEvent>} event A move event.
   * @private
   */
  handlePointerMove_(t) {
    if (this.pointerType_ = t.originalEvent.pointerType, this.downPx_ && (!this.freehand_ && this.shouldHandle_ || this.freehand_ && !this.shouldHandle_)) {
      const i = this.downPx_, a = t.pixel, u = i[0] - a[0], h = i[1] - a[1], g = u * u + h * h;
      if (this.shouldHandle_ = this.freehand_ ? g > this.squaredClickTolerance_ : g <= this.squaredClickTolerance_, !this.shouldHandle_)
        return;
    }
    if (!this.finishCoordinate_) {
      this.createOrUpdateSketchPoint_(t.coordinate.slice());
      return;
    }
    this.updateTrace_(t), this.modifyDrawing_(t.coordinate);
  }
  /**
   * Determine if an event is within the snapping tolerance of the start coord.
   * @param {import("../pixel.js").Pixel} pixel Pixel.
   * @param {boolean} [tracing] Drawing in trace mode (only stop if at the starting point).
   * @return {boolean} The event is within the snapping tolerance of the start.
   * @private
   */
  atFinish_(t, i) {
    let a = !1;
    if (this.sketchFeature_) {
      let u = !1, h = [this.finishCoordinate_];
      const g = this.mode_;
      if (g === "Point")
        a = !0;
      else if (g === "Circle")
        a = this.sketchCoords_.length === 2;
      else if (g === "LineString")
        u = !i && this.sketchCoords_.length > this.minPoints_;
      else if (g === "Polygon") {
        const y = (
          /** @type {PolyCoordType} */
          this.sketchCoords_
        );
        u = y[0].length > this.minPoints_, h = [
          y[0][0],
          y[0][y[0].length - 2]
        ], i ? h = [y[0][0]] : h = [
          y[0][0],
          y[0][y[0].length - 2]
        ];
      }
      if (u) {
        const y = this.getMap();
        for (let _ = 0, b = h.length; _ < b; _++) {
          const E = h[_], x = y.getPixelFromCoordinate(E), R = t[0] - x[0], w = t[1] - x[1], L = this.freehand_ ? 1 : this.snapTolerance_;
          if (a = Math.sqrt(R * R + w * w) <= L, a) {
            this.finishCoordinate_ = E;
            break;
          }
        }
      }
    }
    return a;
  }
  /**
   * @param {import("../coordinate").Coordinate} coordinates Coordinate.
   * @private
   */
  createOrUpdateSketchPoint_(t) {
    this.sketchPoint_ ? this.sketchPoint_.getGeometry().setCoordinates(t) : (this.sketchPoint_ = new cs(new gu(t)), this.updateSketchFeatures_());
  }
  /**
   * @param {import("../geom/Polygon.js").default} geometry Polygon geometry.
   * @private
   */
  createOrUpdateCustomSketchLine_(t) {
    this.sketchLine_ || (this.sketchLine_ = new cs());
    const i = t.getLinearRing(0);
    let a = this.sketchLine_.getGeometry();
    a ? (a.setFlatCoordinates(
      i.getLayout(),
      i.getFlatCoordinates()
    ), a.changed()) : (a = new to(
      i.getFlatCoordinates(),
      i.getLayout()
    ), this.sketchLine_.setGeometry(a));
  }
  /**
   * Start the drawing.
   * @param {import("../coordinate.js").Coordinate} start Start coordinate.
   * @private
   */
  startDrawing_(t) {
    const i = this.getMap().getView().getProjection(), a = K1(this.geometryLayout_);
    for (; t.length < a; )
      t.push(0);
    this.finishCoordinate_ = t, this.mode_ === "Point" ? this.sketchCoords_ = t.slice() : this.mode_ === "Polygon" ? (this.sketchCoords_ = [[t.slice(), t.slice()]], this.sketchLineCoords_ = this.sketchCoords_[0]) : this.sketchCoords_ = [t.slice(), t.slice()], this.sketchLineCoords_ && (this.sketchLine_ = new cs(new to(this.sketchLineCoords_)));
    const u = this.geometryFunction_(
      this.sketchCoords_,
      void 0,
      i
    );
    this.sketchFeature_ = new cs(), this.geometryName_ && this.sketchFeature_.setGeometryName(this.geometryName_), this.sketchFeature_.setGeometry(u), this.updateSketchFeatures_(), this.dispatchEvent(
      new H1(G1.DRAWSTART, this.sketchFeature_)
    );
  }
  /**
   * Modify the drawing.
   * @param {import("../coordinate.js").Coordinate} coordinate Coordinate.
   * @private
   */
  modifyDrawing_(t) {
    const i = this.getMap(), a = this.sketchFeature_.getGeometry(), u = i.getView().getProjection(), h = K1(this.geometryLayout_);
    let g, y;
    for (; t.length < h; )
      t.push(0);
    this.mode_ === "Point" ? y = this.sketchCoords_ : this.mode_ === "Polygon" ? (g = /** @type {PolyCoordType} */
    this.sketchCoords_[0], y = g[g.length - 1], this.atFinish_(i.getPixelFromCoordinate(t)) && (t = this.finishCoordinate_.slice())) : (g = this.sketchCoords_, y = g[g.length - 1]), y[0] = t[0], y[1] = t[1], this.geometryFunction_(
      /** @type {!LineCoordType} */
      this.sketchCoords_,
      a,
      u
    ), this.sketchPoint_ && this.sketchPoint_.getGeometry().setCoordinates(t), a.getType() === "Polygon" && this.mode_ !== "Polygon" ? this.createOrUpdateCustomSketchLine_(
      /** @type {Polygon} */
      a
    ) : this.sketchLineCoords_ && this.sketchLine_.getGeometry().setCoordinates(this.sketchLineCoords_), this.updateSketchFeatures_();
  }
  /**
   * Add a new coordinate to the drawing.
   * @param {!PointCoordType} coordinate Coordinate
   * @return {Feature<import("../geom/SimpleGeometry.js").default>} The sketch feature.
   * @private
   */
  addToDrawing_(t) {
    const i = this.sketchFeature_.getGeometry(), a = this.getMap().getView().getProjection();
    let u, h;
    const g = this.mode_;
    return g === "LineString" || g === "Circle" ? (this.finishCoordinate_ = t.slice(), h = /** @type {LineCoordType} */
    this.sketchCoords_, h.length >= this.maxPoints_ && (this.freehand_ ? h.pop() : u = !0), h.push(t.slice()), this.geometryFunction_(h, i, a)) : g === "Polygon" && (h = /** @type {PolyCoordType} */
    this.sketchCoords_[0], h.length >= this.maxPoints_ && (this.freehand_ ? h.pop() : u = !0), h.push(t.slice()), u && (this.finishCoordinate_ = h[0]), this.geometryFunction_(this.sketchCoords_, i, a)), this.createOrUpdateSketchPoint_(t.slice()), this.updateSketchFeatures_(), u ? this.finishDrawing() : this.sketchFeature_;
  }
  /**
   * @param {number} n The number of points to remove.
   */
  removeLastPoints_(t) {
    if (!this.sketchFeature_)
      return;
    const i = this.sketchFeature_.getGeometry(), a = this.getMap().getView().getProjection(), u = this.mode_;
    for (let h = 0; h < t; ++h) {
      let g;
      if (u === "LineString" || u === "Circle") {
        if (g = /** @type {LineCoordType} */
        this.sketchCoords_, g.splice(-2, 1), g.length >= 2) {
          this.finishCoordinate_ = g[g.length - 2].slice();
          const y = this.finishCoordinate_.slice();
          g[g.length - 1] = y, this.createOrUpdateSketchPoint_(y);
        }
        this.geometryFunction_(g, i, a), i.getType() === "Polygon" && this.sketchLine_ && this.createOrUpdateCustomSketchLine_(
          /** @type {Polygon} */
          i
        );
      } else if (u === "Polygon") {
        g = /** @type {PolyCoordType} */
        this.sketchCoords_[0], g.splice(-2, 1);
        const y = this.sketchLine_.getGeometry();
        if (g.length >= 2) {
          const _ = g[g.length - 2].slice();
          g[g.length - 1] = _, this.createOrUpdateSketchPoint_(_);
        }
        y.setCoordinates(g), this.geometryFunction_(this.sketchCoords_, i, a);
      }
      if (g.length === 1) {
        this.abortDrawing();
        break;
      }
    }
    this.updateSketchFeatures_();
  }
  /**
   * Remove last point of the feature currently being drawn. Does not do anything when
   * drawing POINT or MULTI_POINT geometries.
   * @api
   */
  removeLastPoint() {
    this.removeLastPoints_(1);
  }
  /**
   * Stop drawing and add the sketch feature to the target layer.
   * The {@link module:ol/interaction/Draw~DrawEventType.DRAWEND} event is
   * dispatched before inserting the feature.
   * @return {Feature<import("../geom/SimpleGeometry.js").default>|null} The drawn feature.
   * @api
   */
  finishDrawing() {
    const t = this.abortDrawing_();
    if (!t)
      return null;
    let i = this.sketchCoords_;
    const a = t.getGeometry(), u = this.getMap().getView().getProjection();
    return this.mode_ === "LineString" ? (i.pop(), this.geometryFunction_(i, a, u)) : this.mode_ === "Polygon" && (i[0].pop(), this.geometryFunction_(i, a, u), i = a.getCoordinates()), this.type_ === "MultiPoint" ? t.setGeometry(
      new ES([
        /** @type {PointCoordType} */
        i
      ])
    ) : this.type_ === "MultiLineString" ? t.setGeometry(
      new T_([
        /** @type {LineCoordType} */
        i
      ])
    ) : this.type_ === "MultiPolygon" && t.setGeometry(
      new x_([
        /** @type {PolyCoordType} */
        i
      ])
    ), this.dispatchEvent(new H1(G1.DRAWEND, t)), this.features_ && this.features_.push(t), this.source_ && this.source_.addFeature(t), t;
  }
  /**
   * Stop drawing without adding the sketch feature to the target layer.
   * @return {Feature<import("../geom/SimpleGeometry.js").default>|null} The sketch feature (or null if none).
   * @private
   */
  abortDrawing_() {
    this.finishCoordinate_ = null;
    const t = this.sketchFeature_;
    return this.sketchFeature_ = null, this.sketchPoint_ = null, this.sketchLine_ = null, this.overlay_.getSource().clear(!0), this.deactivateTrace_(), t;
  }
  /**
   * Stop drawing without adding the sketch feature to the target layer.
   * @api
   */
  abortDrawing() {
    const t = this.abortDrawing_();
    t && this.dispatchEvent(new H1(G1.DRAWABORT, t));
  }
  /**
   * Append coordinates to the end of the geometry that is currently being drawn.
   * This can be used when drawing LineStrings or Polygons. Coordinates will
   * either be appended to the current LineString or the outer ring of the current
   * Polygon. If no geometry is being drawn, a new one will be created.
   * @param {!LineCoordType} coordinates Linear coordinates to be appended to
   * the coordinate array.
   * @api
   */
  appendCoordinates(t) {
    const i = this.mode_, a = !this.sketchFeature_;
    a && this.startDrawing_(t[0]);
    let u;
    if (i === "LineString" || i === "Circle")
      u = /** @type {LineCoordType} */
      this.sketchCoords_;
    else if (i === "Polygon")
      u = this.sketchCoords_ && this.sketchCoords_.length ? (
        /** @type {PolyCoordType} */
        this.sketchCoords_[0]
      ) : [];
    else
      return;
    a && u.shift(), u.pop();
    for (let g = 0; g < t.length; g++)
      this.addToDrawing_(t[g]);
    const h = t[t.length - 1];
    this.sketchFeature_ = this.addToDrawing_(h), this.modifyDrawing_(h);
  }
  /**
   * Initiate draw mode by starting from an existing geometry which will
   * receive new additional points. This only works on features with
   * `LineString` geometries, where the interaction will extend lines by adding
   * points to the end of the coordinates array.
   * This will change the original feature, instead of drawing a copy.
   *
   * The function will dispatch a `drawstart` event.
   *
   * @param {!Feature<LineString>} feature Feature to be extended.
   * @api
   */
  extend(t) {
    const a = t.getGeometry();
    this.sketchFeature_ = t, this.sketchCoords_ = a.getCoordinates();
    const u = this.sketchCoords_[this.sketchCoords_.length - 1];
    this.finishCoordinate_ = u.slice(), this.sketchCoords_.push(u.slice()), this.sketchPoint_ = new cs(new gu(u)), this.updateSketchFeatures_(), this.dispatchEvent(
      new H1(G1.DRAWSTART, this.sketchFeature_)
    );
  }
  /**
   * Redraw the sketch features.
   * @private
   */
  updateSketchFeatures_() {
    const t = [];
    this.sketchFeature_ && t.push(this.sketchFeature_), this.sketchLine_ && t.push(this.sketchLine_), this.sketchPoint_ && t.push(this.sketchPoint_);
    const i = this.overlay_.getSource();
    i.clear(!0), i.addFeatures(t);
  }
  /**
   * @private
   */
  updateState_() {
    const t = this.getMap(), i = this.getActive();
    (!t || !i) && this.abortDrawing(), this.overlay_.setMap(i ? t : null);
  }
}
function wL() {
  const r = iA();
  return function(t, i) {
    return r[t.getGeometry().getType()];
  };
}
function DL(r) {
  switch (r) {
    case "Point":
    case "MultiPoint":
      return "Point";
    case "LineString":
    case "MultiLineString":
      return "LineString";
    case "Polygon":
    case "MultiPolygon":
      return "Polygon";
    case "Circle":
      return "Circle";
    default:
      throw new Error("Invalid type: " + r);
  }
}
const OL = {
  /**
   * Triggered when feature(s) has been (de)selected.
   * @event SelectEvent#select
   * @api
   */
  SELECT: "select"
};
class LL extends ys {
  /**
   * @param {SelectEventType} type The event type.
   * @param {Array<import("../Feature.js").default>} selected Selected features.
   * @param {Array<import("../Feature.js").default>} deselected Deselected features.
   * @param {import("../MapBrowserEvent.js").default} mapBrowserEvent Associated
   *     {@link module:ol/MapBrowserEvent~MapBrowserEvent}.
   */
  constructor(t, i, a, u) {
    super(t), this.selected = i, this.deselected = a, this.mapBrowserEvent = u;
  }
}
const I1 = {};
class VT extends ey {
  /**
   * @param {Options} [options] Options.
   */
  constructor(t) {
    super(), this.on, this.once, this.un, t = t || {}, this.boundAddFeature_ = this.addFeature_.bind(this), this.boundRemoveFeature_ = this.removeFeature_.bind(this), this.condition_ = t.condition ? t.condition : sL, this.addCondition_ = t.addCondition ? t.addCondition : Pb, this.removeCondition_ = t.removeCondition ? t.removeCondition : Pb, this.toggleCondition_ = t.toggleCondition ? t.toggleCondition : IT, this.multi_ = t.multi ? t.multi : !1, this.filter_ = t.filter ? t.filter : _u, this.hitTolerance_ = t.hitTolerance ? t.hitTolerance : 0, this.style_ = t.style !== void 0 ? t.style : zL(), this.features_ = t.features || new Js();
    let i;
    if (t.layers)
      if (typeof t.layers == "function")
        i = t.layers;
      else {
        const a = t.layers;
        i = function(u) {
          return a.includes(u);
        };
      }
    else
      i = _u;
    this.layerFilter_ = i, this.featureLayerAssociation_ = {};
  }
  /**
   * @param {import("../Feature.js").default} feature Feature.
   * @param {import("../layer/Layer.js").default} layer Layer.
   * @private
   */
  addFeatureLayerAssociation_(t, i) {
    this.featureLayerAssociation_[ue(t)] = i;
  }
  /**
   * Get the selected features.
   * @return {Collection<Feature>} Features collection.
   * @api
   */
  getFeatures() {
    return this.features_;
  }
  /**
   * Returns the Hit-detection tolerance.
   * @return {number} Hit tolerance in pixels.
   * @api
   */
  getHitTolerance() {
    return this.hitTolerance_;
  }
  /**
   * Returns the associated {@link module:ol/layer/Vector~VectorLayer vector layer} of
   * a selected feature.
   * @param {import("../Feature.js").default} feature Feature
   * @return {import('../layer/Vector.js').default} Layer.
   * @api
   */
  getLayer(t) {
    return (
      /** @type {import('../layer/Vector.js').default} */
      this.featureLayerAssociation_[ue(t)]
    );
  }
  /**
   * Hit-detection tolerance. Pixels inside the radius around the given position
   * will be checked for features.
   * @param {number} hitTolerance Hit tolerance in pixels.
   * @api
   */
  setHitTolerance(t) {
    this.hitTolerance_ = t;
  }
  /**
   * Remove the interaction from its current map, if any,  and attach it to a new
   * map, if any. Pass `null` to just remove the interaction from the current map.
   * @param {import("../Map.js").default|null} map Map.
   * @api
   * @override
   */
  setMap(t) {
    this.getMap() && this.style_ && this.features_.forEach(this.restorePreviousStyle_.bind(this)), super.setMap(t), t ? (this.features_.addEventListener(
      jn.ADD,
      this.boundAddFeature_
    ), this.features_.addEventListener(
      jn.REMOVE,
      this.boundRemoveFeature_
    ), this.style_ && this.features_.forEach(this.applySelectedStyle_.bind(this))) : (this.features_.removeEventListener(
      jn.ADD,
      this.boundAddFeature_
    ), this.features_.removeEventListener(
      jn.REMOVE,
      this.boundRemoveFeature_
    ));
  }
  /**
   * @param {import("../Collection.js").CollectionEvent<Feature>} evt Event.
   * @private
   */
  addFeature_(t) {
    const i = t.element;
    if (this.style_ && this.applySelectedStyle_(i), !this.getLayer(i)) {
      const a = (
        /** @type {VectorLayer} */
        this.getMap().getAllLayers().find(function(u) {
          if (u instanceof MS && u.getSource() && u.getSource().hasFeature(i))
            return u;
        })
      );
      a && this.addFeatureLayerAssociation_(i, a);
    }
  }
  /**
   * @param {import("../Collection.js").CollectionEvent<Feature>} evt Event.
   * @private
   */
  removeFeature_(t) {
    this.style_ && this.restorePreviousStyle_(t.element);
  }
  /**
   * @return {import("../style/Style.js").StyleLike|null} Select style.
   */
  getStyle() {
    return this.style_;
  }
  /**
   * @param {Feature} feature Feature
   * @private
   */
  applySelectedStyle_(t) {
    const i = ue(t);
    i in I1 || (I1[i] = t.getStyle()), t.setStyle(this.style_);
  }
  /**
   * @param {Feature} feature Feature
   * @private
   */
  restorePreviousStyle_(t) {
    const i = this.getMap().getInteractions().getArray();
    for (let u = i.length - 1; u >= 0; --u) {
      const h = i[u];
      if (h !== this && h instanceof VT && h.getStyle() && h.getFeatures().getArray().lastIndexOf(t) !== -1) {
        t.setStyle(h.getStyle());
        return;
      }
    }
    const a = ue(t);
    t.setStyle(I1[a]), delete I1[a];
  }
  /**
   * @param {Feature} feature Feature.
   * @private
   */
  removeFeatureLayerAssociation_(t) {
    delete this.featureLayerAssociation_[ue(t)];
  }
  /**
   * Handles the {@link module:ol/MapBrowserEvent~MapBrowserEvent map browser event} and may change the
   * selected state of features.
   * @param {import("../MapBrowserEvent.js").default} mapBrowserEvent Map browser event.
   * @return {boolean} `false` to stop event propagation.
   * @override
   */
  handleEvent(t) {
    if (!this.condition_(t))
      return !0;
    const i = this.addCondition_(t), a = this.removeCondition_(t), u = this.toggleCondition_(t), h = !i && !a && !u, g = t.map, y = this.getFeatures(), _ = [], b = [];
    if (h) {
      Jm(this.featureLayerAssociation_), g.forEachFeatureAtPixel(
        t.pixel,
        /**
         * @param {import("../Feature.js").FeatureLike} feature Feature.
         * @param {import("../layer/Layer.js").default} layer Layer.
         * @return {boolean|undefined} Continue to iterate over the features.
         */
        (E, x) => {
          if (!(!(E instanceof cs) || !this.filter_(E, x)))
            return this.addFeatureLayerAssociation_(E, x), b.push(E), !this.multi_;
        },
        {
          layerFilter: this.layerFilter_,
          hitTolerance: this.hitTolerance_
        }
      );
      for (let E = y.getLength() - 1; E >= 0; --E) {
        const x = y.item(E), R = b.indexOf(x);
        R > -1 ? b.splice(R, 1) : (y.remove(x), _.push(x));
      }
      b.length !== 0 && y.extend(b);
    } else {
      g.forEachFeatureAtPixel(
        t.pixel,
        /**
         * @param {import("../Feature.js").FeatureLike} feature Feature.
         * @param {import("../layer/Layer.js").default} layer Layer.
         * @return {boolean|undefined} Continue to iterate over the features.
         */
        (E, x) => {
          if (!(!(E instanceof cs) || !this.filter_(E, x)))
            return (i || u) && !y.getArray().includes(E) ? (this.addFeatureLayerAssociation_(E, x), b.push(E)) : (a || u) && y.getArray().includes(E) && (_.push(E), this.removeFeatureLayerAssociation_(E)), !this.multi_;
        },
        {
          layerFilter: this.layerFilter_,
          hitTolerance: this.hitTolerance_
        }
      );
      for (let E = _.length - 1; E >= 0; --E)
        y.remove(_[E]);
      y.extend(b);
    }
    return (b.length > 0 || _.length > 0) && this.dispatchEvent(
      new LL(
        OL.SELECT,
        b,
        _,
        t
      )
    ), !0;
  }
}
function zL() {
  const r = iA();
  return ds(r.Polygon, r.LineString), ds(r.GeometryCollection, r.LineString), function(t) {
    return t.getGeometry() ? r[t.getGeometry().getType()] : null;
  };
}
const vb = {
  /**
   * Triggered upon snapping to vertex or edge
   * @event SnapEvent#snap
   * @api
   */
  SNAP: "snap",
  UNSNAP: "unsnap"
};
class Sb extends ys {
  /**
   * @param {SnapEventType} type Type.
   * @param {Object} options Options.
   * @param {import("../coordinate.js").Coordinate} options.vertex The snapped vertex.
   * @param {import("../coordinate.js").Coordinate} options.vertexPixel The pixel of the snapped vertex.
   * @param {import("../Feature.js").default} options.feature The feature being snapped.
   * @param {Array<import("../coordinate.js").Coordinate>|null} options.segment Segment, or `null` if snapped to a vertex.
   */
  constructor(t, i) {
    super(t), this.vertex = i.vertex, this.vertexPixel = i.vertexPixel, this.feature = i.feature, this.segment = i.segment;
  }
}
const Qb = {
  /**
   * @param {import("../geom/Circle.js").default} geometry Geometry.
   * @param {import("../proj/Projection.js").default} projection Projection.
   * @return {Array<Segment>} Segments
   */
  Circle(r, t) {
    let i = r;
    const a = Wh();
    a && (i = i.clone().transform(a, t));
    const u = MD(i);
    return a && u.transform(t, a), Qb.Polygon(u);
  },
  /**
   * @param {import("../geom/GeometryCollection.js").default} geometry Geometry.
   * @param {import("../proj/Projection.js").default} projection Projection.
   * @return {Array<Segment>} Segments
   */
  GeometryCollection(r, t) {
    const i = [], a = r.getGeometriesArray();
    for (let u = 0; u < a.length; ++u) {
      const h = Qb[a[u].getType()];
      h && i.push(h(a[u], t));
    }
    return i.flat();
  },
  /**
   * @param {import("../geom/LineString.js").default} geometry Geometry.
   * @return {Array<Segment>} Segments
   */
  LineString(r) {
    const t = [], i = r.getFlatCoordinates(), a = r.getStride();
    for (let u = 0, h = i.length - a; u < h; u += a)
      t.push([
        i.slice(u, u + 2),
        i.slice(u + a, u + a + 2)
      ]);
    return t;
  },
  /**
   * @param {import("../geom/MultiLineString.js").default} geometry Geometry.
   * @return {Array<Segment>} Segments
   */
  MultiLineString(r) {
    const t = [], i = r.getFlatCoordinates(), a = r.getStride(), u = r.getEnds();
    let h = 0;
    for (let g = 0, y = u.length; g < y; ++g) {
      const _ = u[g];
      for (let b = h, E = _ - a; b < E; b += a)
        t.push([
          i.slice(b, b + 2),
          i.slice(b + a, b + a + 2)
        ]);
      h = _;
    }
    return t;
  },
  /**
   * @param {import("../geom/MultiPoint.js").default} geometry Geometry.
   * @return {Array<Segment>} Segments
   */
  MultiPoint(r) {
    const t = [], i = r.getFlatCoordinates(), a = r.getStride();
    for (let u = 0, h = i.length; u < h; u += a)
      t.push([i.slice(u, u + 2)]);
    return t;
  },
  /**
   * @param {import("../geom/MultiPolygon.js").default} geometry Geometry.
   * @return {Array<Segment>} Segments
   */
  MultiPolygon(r) {
    const t = [], i = r.getFlatCoordinates(), a = r.getStride(), u = r.getEndss();
    let h = 0;
    for (let g = 0, y = u.length; g < y; ++g) {
      const _ = u[g];
      for (let b = 0, E = _.length; b < E; ++b) {
        const x = _[b];
        for (let R = h, w = x - a; R < w; R += a)
          t.push([
            i.slice(R, R + 2),
            i.slice(R + a, R + a + 2)
          ]);
        h = x;
      }
    }
    return t;
  },
  /**
   * @param {import("../geom/Point.js").default} geometry Geometry.
   * @return {Array<Segment>} Segments
   */
  Point(r) {
    return [[r.getFlatCoordinates().slice(0, 2)]];
  },
  /**
   * @param {import("../geom/Polygon.js").default} geometry Geometry.
   * @return {Array<Segment>} Segments
   */
  Polygon(r) {
    const t = [], i = r.getFlatCoordinates(), a = r.getStride(), u = r.getEnds();
    let h = 0;
    for (let g = 0, y = u.length; g < y; ++g) {
      const _ = u[g];
      for (let b = h, E = _ - a; b < E; b += a)
        t.push([
          i.slice(b, b + 2),
          i.slice(b + a, b + a + 2)
        ]);
      h = _;
    }
    return t;
  }
};
function WR(r) {
  return (
    /** @type {import("../source/Vector.js").VectorSourceEvent} */
    r.feature ? (
      /** @type {import("../source/Vector.js").VectorSourceEvent} */
      r.feature
    ) : (
      /** @type {import("../Collection.js").CollectionEvent<import("../Feature.js").default>} */
      r.element ? (
        /** @type {import("../Collection.js").CollectionEvent<import("../Feature.js").default>} */
        r.element
      ) : null
    )
  );
}
const Eb = [], ru = [], Lm = [];
class NL extends ld {
  /**
   * @param {Options} [options] Options.
   */
  constructor(t) {
    t = t || {};
    const i = (
      /** @type {import("./Pointer.js").Options} */
      t
    );
    i.handleDownEvent || (i.handleDownEvent = _u), i.stopDown || (i.stopDown = td), super(i), this.on, this.once, this.un, this.source_ = t.source ? t.source : null, this.vertex_ = t.vertex !== void 0 ? t.vertex : !0, this.edge_ = t.edge !== void 0 ? t.edge : !0, this.intersection_ = t.intersection !== void 0 ? t.intersection : !1, this.features_ = t.features ? t.features : null, this.featuresListenerKeys_ = [], this.featureChangeListenerKeys_ = {}, this.indexedFeaturesExtents_ = {}, this.pendingFeatures_ = {}, this.pixelTolerance_ = t.pixelTolerance !== void 0 ? t.pixelTolerance : 10, this.rBush_ = new Xb(), this.snapped_ = null, this.segmenters_ = Object.assign(
      {},
      Qb,
      t.segmenters
    );
  }
  /**
   * Add a feature to the collection of features that we may snap to.
   * @param {import("../Feature.js").default} feature Feature.
   * @param {boolean} [register] Whether to listen to the feature change or not
   *     Defaults to `true`.
   * @api
   */
  addFeature(t, i) {
    i = i !== void 0 ? i : !0;
    const a = ue(t), u = t.getGeometry();
    if (u) {
      const h = this.segmenters_[u.getType()];
      if (h) {
        this.indexedFeaturesExtents_[a] = u.getExtent(_a());
        const g = h(
          u,
          this.getMap().getView().getProjection()
        );
        let y = g.length;
        for (let _ = 0; _ < y; ++_) {
          const b = g[_];
          ru[_] = Vh(b), Lm[_] = {
            feature: t,
            segment: b
          };
        }
        if (ru.length = y, Lm.length = y, this.intersection_)
          for (let _ = 0, b = g.length; _ < b; ++_) {
            const E = g[_];
            if (E.length === 1)
              continue;
            const x = ru[_];
            for (let w = 0, L = g.length; w < L; ++w) {
              if (_ === w || _ - 1 === w || _ + 1 === w)
                continue;
              const H = g[w];
              if (!qi(x, ru[w]))
                continue;
              const N = pR(E, H);
              if (!N)
                continue;
              const O = [N];
              ru[y] = Vh(O), Lm[y++] = {
                feature: t,
                segment: O,
                isIntersection: !0
              };
            }
            const R = this.rBush_.getInExtent(ru[_]);
            for (const { segment: w } of R) {
              if (w.length === 1)
                continue;
              const L = pR(E, w);
              if (!L)
                continue;
              const H = [L];
              ru[y] = Vh(H), Lm[y++] = {
                feature: t,
                segment: H,
                isIntersection: !0
              };
            }
          }
        y === 1 ? this.rBush_.insert(ru[0], Lm[0]) : this.rBush_.load(ru, Lm);
      }
    }
    i && (this.featureChangeListenerKeys_[a] = ge(
      t,
      $t.CHANGE,
      this.handleFeatureChange_,
      this
    ));
  }
  /**
   * @return {import("../Collection.js").default<import("../Feature.js").default>|Array<import("../Feature.js").default>} Features.
   * @private
   */
  getFeatures_() {
    let t;
    return this.features_ ? t = this.features_ : this.source_ && (t = this.source_.getFeatures()), t;
  }
  /**
   * Checks if two snap data sets are equal.
   * Compares the segment and the feature.
   *
   * @param {SnappedInfo} data1 The first snap data set.
   * @param {SnappedInfo} data2 The second snap data set.
   * @return {boolean} `true` if the data sets are equal, otherwise `false`.
   * @private
   */
  areSnapDataEqual_(t, i) {
    return t.segment === i.segment && t.feature === i.feature;
  }
  /**
   * @param {import("../MapBrowserEvent.js").default} evt Map browser event.
   * @return {boolean} `false` to stop event propagation.
   * @api
   * @override
   */
  handleEvent(t) {
    const i = this.snapTo(t.pixel, t.coordinate, t.map);
    return i ? (t.coordinate = i.vertex.slice(0, 2), t.pixel = i.vertexPixel, this.snapped_ && !this.areSnapDataEqual_(this.snapped_, i) && this.dispatchEvent(new Sb(vb.UNSNAP, this.snapped_)), this.snapped_ = {
      vertex: t.coordinate,
      vertexPixel: t.pixel,
      feature: i.feature,
      segment: i.segment
    }, this.dispatchEvent(new Sb(vb.SNAP, this.snapped_))) : this.snapped_ && (this.dispatchEvent(new Sb(vb.UNSNAP, this.snapped_)), this.snapped_ = null), super.handleEvent(t);
  }
  /**
   * @param {import("../source/Vector.js").VectorSourceEvent|import("../Collection.js").CollectionEvent<import("../Feature.js").default>} evt Event.
   * @private
   */
  handleFeatureAdd_(t) {
    const i = WR(t);
    i && this.addFeature(i);
  }
  /**
   * @param {import("../source/Vector.js").VectorSourceEvent|import("../Collection.js").CollectionEvent<import("../Feature.js").default>} evt Event.
   * @private
   */
  handleFeatureRemove_(t) {
    const i = WR(t);
    i && this.removeFeature(i);
  }
  /**
   * @param {import("../events/Event.js").default} evt Event.
   * @private
   */
  handleFeatureChange_(t) {
    const i = (
      /** @type {import("../Feature.js").default} */
      t.target
    );
    if (this.handlingDownUpSequence) {
      const a = ue(i);
      a in this.pendingFeatures_ || (this.pendingFeatures_[a] = i);
    } else
      this.updateFeature_(i);
  }
  /**
   * Handle pointer up events.
   * @param {import("../MapBrowserEvent.js").default} evt Event.
   * @return {boolean} If the event was consumed.
   * @override
   */
  handleUpEvent(t) {
    const i = Object.values(this.pendingFeatures_);
    if (i.length)
      for (const a of i)
        this.updateFeature_(a);
    return !1;
  }
  /**
   * Remove a feature from the collection of features that we may snap to.
   * @param {import("../Feature.js").default} feature Feature
   * @param {boolean} [unlisten] Whether to unlisten to the feature change
   *     or not. Defaults to `true`.
   * @api
   */
  removeFeature(t, i) {
    const a = i !== void 0 ? i : !0, u = ue(t), h = this.indexedFeaturesExtents_[u];
    if (h) {
      const g = this.rBush_, y = [];
      g.forEachInExtent(h, function(_) {
        t === _.feature && y.push(_);
      });
      for (let _ = y.length - 1; _ >= 0; --_)
        g.remove(y[_]);
    }
    a && (qe(this.featureChangeListenerKeys_[u]), delete this.featureChangeListenerKeys_[u]);
  }
  /**
   * Remove the interaction from its current map and attach it to the new map.
   * Subclasses may set up event handlers to get notified about changes to
   * the map here.
   * @param {import("../Map.js").default} map Map.
   * @override
   */
  setMap(t) {
    const i = this.getMap(), a = this.featuresListenerKeys_;
    let u = this.getFeatures_();
    if (Array.isArray(u) || (u = u.getArray()), i && (a.forEach(qe), a.length = 0, this.rBush_.clear(), Object.values(this.featureChangeListenerKeys_).forEach(qe), this.featureChangeListenerKeys_ = {}), super.setMap(t), t) {
      this.features_ ? a.push(
        ge(
          this.features_,
          jn.ADD,
          this.handleFeatureAdd_,
          this
        ),
        ge(
          this.features_,
          jn.REMOVE,
          this.handleFeatureRemove_,
          this
        )
      ) : this.source_ && a.push(
        ge(
          this.source_,
          Il.ADDFEATURE,
          this.handleFeatureAdd_,
          this
        ),
        ge(
          this.source_,
          Il.REMOVEFEATURE,
          this.handleFeatureRemove_,
          this
        )
      );
      for (const h of u)
        this.addFeature(h);
    }
  }
  /**
   * @param {import("../pixel.js").Pixel} pixel Pixel
   * @param {import("../coordinate.js").Coordinate} pixelCoordinate Coordinate
   * @param {import("../Map.js").default} map Map.
   * @return {SnappedInfo|null} Snap result
   */
  snapTo(t, i, a) {
    const u = a.getView().getProjection(), h = dl(i, u), g = y_(
      rS(
        Vh([h]),
        a.getView().getResolution() * this.pixelTolerance_
      ),
      u
    ), y = this.rBush_.getInExtent(g), _ = y.length;
    if (_ === 0)
      return null;
    let b, E = 1 / 0, x, R = null, w;
    const L = this.pixelTolerance_ * this.pixelTolerance_, H = () => {
      if (b) {
        const N = a.getPixelFromCoordinate(b);
        if (u_(t, N) <= L && (w && this.intersection_ || !w && (this.vertex_ || this.edge_)))
          return {
            vertex: b,
            vertexPixel: [
              Math.round(N[0]),
              Math.round(N[1])
            ],
            feature: x,
            segment: R
          };
      }
      return null;
    };
    if (this.vertex_ || this.intersection_) {
      for (let O = 0; O < _; ++O) {
        const U = y[O];
        if (U.feature.getGeometry().getType() !== "Circle")
          for (const I of U.segment) {
            const X = dl(I, u), Z = u_(h, X);
            Z < E && (b = I, E = Z, x = U.feature, w = U.isIntersection);
          }
      }
      const N = H();
      if (N)
        return N;
    }
    if (this.edge_) {
      for (let O = 0; O < _; ++O) {
        let U = null;
        const I = y[O];
        if (I.feature.getGeometry().getType() === "Circle") {
          let X = I.feature.getGeometry();
          const Z = Wh();
          Z && (X = X.clone().transform(Z, u)), U = Tw(
            h,
            /** @type {import("../geom/Circle.js").default} */
            X
          );
        } else {
          const [X, Z] = I.segment;
          Z && (Eb[0] = dl(X, u), Eb[1] = dl(Z, u), U = xw(h, Eb));
        }
        if (U) {
          const X = u_(h, U);
          X < E && (b = F1(U, u), R = I.feature.getGeometry().getType() === "Circle" ? null : I.segment, E = X, x = I.feature);
        }
      }
      const N = H();
      if (N)
        return N;
    }
    return null;
  }
  /**
   * @param {import("../Feature.js").default} feature Feature
   * @private
   */
  updateFeature_(t) {
    this.removeFeature(t, !1), this.addFeature(t, !1);
  }
}
class UL {
  /**
   * @param {number} [highWaterMark] High water mark.
   */
  constructor(t) {
    this.highWaterMark = t !== void 0 ? t : 2048, this.count_ = 0, this.entries_ = {}, this.oldest_ = null, this.newest_ = null;
  }
  deleteOldest() {
    const t = this.pop();
    t instanceof sS && t.dispose();
  }
  /**
   * @return {boolean} Can expire cache.
   */
  canExpireCache() {
    return this.highWaterMark > 0 && this.getCount() > this.highWaterMark;
  }
  /**
   * Expire the cache. When the cache entry is a {@link module:ol/Disposable~Disposable},
   * the entry will be disposed.
   * @param {!Object<string, boolean>} [keep] Keys to keep. To be implemented by subclasses.
   */
  expireCache(t) {
    for (; this.canExpireCache(); )
      this.deleteOldest();
  }
  /**
   * FIXME empty description for jsdoc
   */
  clear() {
    for (; this.oldest_; )
      this.deleteOldest();
  }
  /**
   * @param {string} key Key.
   * @return {boolean} Contains key.
   */
  containsKey(t) {
    return this.entries_.hasOwnProperty(t);
  }
  /**
   * @param {function(T, string, LRUCache<T>): ?} f The function
   *     to call for every entry from the oldest to the newer. This function takes
   *     3 arguments (the entry value, the entry key and the LRUCache object).
   *     The return value is ignored.
   */
  forEach(t) {
    let i = this.oldest_;
    for (; i; )
      t(i.value_, i.key_, this), i = i.newer;
  }
  /**
   * @param {string} key Key.
   * @param {*} [options] Options (reserved for subclasses).
   * @return {T} Value.
   */
  get(t, i) {
    const a = this.entries_[t];
    return je(
      a !== void 0,
      "Tried to get a value for a key that does not exist in the cache"
    ), a === this.newest_ || (a === this.oldest_ ? (this.oldest_ = /** @type {Entry} */
    this.oldest_.newer, this.oldest_.older = null) : (a.newer.older = a.older, a.older.newer = a.newer), a.newer = null, a.older = this.newest_, this.newest_.newer = a, this.newest_ = a), a.value_;
  }
  /**
   * Remove an entry from the cache.
   * @param {string} key The entry key.
   * @return {T} The removed entry.
   */
  remove(t) {
    const i = this.entries_[t];
    return je(
      i !== void 0,
      "Tried to get a value for a key that does not exist in the cache"
    ), i === this.newest_ ? (this.newest_ = /** @type {Entry} */
    i.older, this.newest_ && (this.newest_.newer = null)) : i === this.oldest_ ? (this.oldest_ = /** @type {Entry} */
    i.newer, this.oldest_ && (this.oldest_.older = null)) : (i.newer.older = i.older, i.older.newer = i.newer), delete this.entries_[t], --this.count_, i.value_;
  }
  /**
   * @return {number} Count.
   */
  getCount() {
    return this.count_;
  }
  /**
   * @return {Array<string>} Keys.
   */
  getKeys() {
    const t = new Array(this.count_);
    let i = 0, a;
    for (a = this.newest_; a; a = a.older)
      t[i++] = a.key_;
    return t;
  }
  /**
   * @return {Array<T>} Values.
   */
  getValues() {
    const t = new Array(this.count_);
    let i = 0, a;
    for (a = this.newest_; a; a = a.older)
      t[i++] = a.value_;
    return t;
  }
  /**
   * @return {T} Last value.
   */
  peekLast() {
    return this.oldest_.value_;
  }
  /**
   * @return {string} Last key.
   */
  peekLastKey() {
    return this.oldest_.key_;
  }
  /**
   * Get the key of the newest item in the cache.  Throws if the cache is empty.
   * @return {string} The newest key.
   */
  peekFirstKey() {
    return this.newest_.key_;
  }
  /**
   * Return an entry without updating least recently used time.
   * @param {string} key Key.
   * @return {T|undefined} Value.
   */
  peek(t) {
    var i;
    return (i = this.entries_[t]) == null ? void 0 : i.value_;
  }
  /**
   * @return {T} value Value.
   */
  pop() {
    const t = this.oldest_;
    return delete this.entries_[t.key_], t.newer && (t.newer.older = null), this.oldest_ = /** @type {Entry} */
    t.newer, this.oldest_ || (this.newest_ = null), --this.count_, t.value_;
  }
  /**
   * @param {string} key Key.
   * @param {T} value Value.
   */
  replace(t, i) {
    this.get(t), this.entries_[t].value_ = i;
  }
  /**
   * @param {string} key Key.
   * @param {T} value Value.
   */
  set(t, i) {
    je(
      !(t in this.entries_),
      "Tried to set a value for a key that is used already"
    );
    const a = {
      key_: t,
      newer: null,
      older: this.newest_,
      value_: i
    };
    this.newest_ ? this.newest_.newer = a : this.oldest_ = a, this.newest_ = a, this.entries_[t] = a, ++this.count_;
  }
  /**
   * Set a maximum number of entries for the cache.
   * @param {number} size Cache size.
   * @api
   */
  setSize(t) {
    this.highWaterMark = t;
  }
}
function iS(r, t, i, a) {
  return a !== void 0 ? (a[0] = r, a[1] = t, a[2] = i, a) : [r, t, i];
}
function GL(r, t, i) {
  return r + "/" + t + "/" + i;
}
function HL(r) {
  return YL(r[0], r[1], r[2]);
}
function YL(r, t, i) {
  return (t << r) + i;
}
function jL(r, t) {
  const i = r[0], a = r[1], u = r[2];
  if (t.getMinZoom() > i || i > t.getMaxZoom())
    return !1;
  const h = t.getFullTileRange(i);
  return h ? h.containsXY(a, u) : !0;
}
const zm = [0, 0, 0], Ic = 5;
class AA {
  /**
   * @param {Options} options Tile grid options.
   */
  constructor(t) {
    this.minZoom = t.minZoom !== void 0 ? t.minZoom : 0, this.resolutions_ = t.resolutions, je(
      rw(
        this.resolutions_,
        /**
         * @param {number} a First resolution
         * @param {number} b Second resolution
         * @return {number} Comparison result
         */
        (u, h) => h - u
      ),
      "`resolutions` must be sorted in descending order"
    );
    let i;
    if (!t.origins) {
      for (let u = 0, h = this.resolutions_.length - 1; u < h; ++u)
        if (!i)
          i = this.resolutions_[u] / this.resolutions_[u + 1];
        else if (this.resolutions_[u] / this.resolutions_[u + 1] !== i) {
          i = void 0;
          break;
        }
    }
    this.zoomFactor_ = i, this.maxZoom = this.resolutions_.length - 1, this.origin_ = t.origin !== void 0 ? t.origin : null, this.origins_ = null, t.origins !== void 0 && (this.origins_ = t.origins, je(
      this.origins_.length == this.resolutions_.length,
      "Number of `origins` and `resolutions` must be equal"
    ));
    const a = t.extent;
    a !== void 0 && !this.origin_ && !this.origins_ && (this.origin_ = nd(a)), je(
      !this.origin_ && this.origins_ || this.origin_ && !this.origins_,
      "Either `origin` or `origins` must be configured, never both"
    ), this.tileSizes_ = null, t.tileSizes !== void 0 && (this.tileSizes_ = t.tileSizes, je(
      this.tileSizes_.length == this.resolutions_.length,
      "Number of `tileSizes` and `resolutions` must be equal"
    )), this.tileSize_ = t.tileSize !== void 0 ? t.tileSize : this.tileSizes_ ? null : UT, je(
      !this.tileSize_ && this.tileSizes_ || this.tileSize_ && !this.tileSizes_,
      "Either `tileSize` or `tileSizes` must be configured, never both"
    ), this.extent_ = a !== void 0 ? a : null, this.fullTileRanges_ = null, this.tmpSize_ = [0, 0], this.tmpExtent_ = [0, 0, 0, 0], t.sizes !== void 0 ? this.fullTileRanges_ = t.sizes.map((u, h) => {
      const g = new kT(
        Math.min(0, u[0]),
        Math.max(u[0] - 1, -1),
        Math.min(0, u[1]),
        Math.max(u[1] - 1, -1)
      );
      if (a) {
        const y = this.getTileRangeForExtentAndZ(a, h);
        g.minX = Math.max(y.minX, g.minX), g.maxX = Math.min(y.maxX, g.maxX), g.minY = Math.max(y.minY, g.minY), g.maxY = Math.min(y.maxY, g.maxY);
      }
      return g;
    }) : a && this.calculateTileRanges_(a);
  }
  /**
   * Call a function with each tile coordinate for a given extent and zoom level.
   *
   * @param {import("../extent.js").Extent} extent Extent.
   * @param {number} zoom Integer zoom level.
   * @param {function(import("../tilecoord.js").TileCoord): void} callback Function called with each tile coordinate.
   * @api
   */
  forEachTileCoord(t, i, a) {
    const u = this.getTileRangeForExtentAndZ(t, i);
    for (let h = u.minX, g = u.maxX; h <= g; ++h)
      for (let y = u.minY, _ = u.maxY; y <= _; ++y)
        a([i, h, y]);
  }
  /**
   * @param {import("../tilecoord.js").TileCoord} tileCoord Tile coordinate.
   * @param {function(number, import("../TileRange.js").default): boolean} callback Callback.
   * @param {import("../TileRange.js").default} [tempTileRange] Temporary import("../TileRange.js").default object.
   * @param {import("../extent.js").Extent} [tempExtent] Temporary import("../extent.js").Extent object.
   * @return {boolean} Callback succeeded.
   */
  forEachTileCoordParentTileRange(t, i, a, u) {
    let h, g, y, _ = null, b = t[0] - 1;
    for (this.zoomFactor_ === 2 ? (g = t[1], y = t[2]) : _ = this.getTileCoordExtent(t, u); b >= this.minZoom; ) {
      if (g !== void 0 && y !== void 0 ? (g = Math.floor(g / 2), y = Math.floor(y / 2), h = Om(g, g, y, y, a)) : h = this.getTileRangeForExtentAndZ(
        _,
        b,
        a
      ), i(b, h))
        return !0;
      --b;
    }
    return !1;
  }
  /**
   * Get the extent for this tile grid, if it was configured.
   * @return {import("../extent.js").Extent} Extent.
   * @api
   */
  getExtent() {
    return this.extent_;
  }
  /**
   * Get the maximum zoom level for the grid.
   * @return {number} Max zoom.
   * @api
   */
  getMaxZoom() {
    return this.maxZoom;
  }
  /**
   * Get the minimum zoom level for the grid.
   * @return {number} Min zoom.
   * @api
   */
  getMinZoom() {
    return this.minZoom;
  }
  /**
   * Get the origin for the grid at the given zoom level.
   * @param {number} z Integer zoom level.
   * @return {import("../coordinate.js").Coordinate} Origin.
   * @api
   */
  getOrigin(t) {
    return this.origin_ ? this.origin_ : this.origins_[t];
  }
  /**
   * Get the resolution for the given zoom level.
   * @param {number} z Integer zoom level.
   * @return {number} Resolution.
   * @api
   */
  getResolution(t) {
    return this.resolutions_[t];
  }
  /**
   * Get the list of resolutions for the tile grid.
   * @return {Array<number>} Resolutions.
   * @api
   */
  getResolutions() {
    return this.resolutions_;
  }
  /**
   * @param {import("../tilecoord.js").TileCoord} tileCoord Tile coordinate.
   * @param {import("../TileRange.js").default} [tempTileRange] Temporary import("../TileRange.js").default object.
   * @param {import("../extent.js").Extent} [tempExtent] Temporary import("../extent.js").Extent object.
   * @return {import("../TileRange.js").default|null} Tile range.
   */
  getTileCoordChildTileRange(t, i, a) {
    if (t[0] < this.maxZoom) {
      if (this.zoomFactor_ === 2) {
        const h = t[1] * 2, g = t[2] * 2;
        return Om(
          h,
          h + 1,
          g,
          g + 1,
          i
        );
      }
      const u = this.getTileCoordExtent(
        t,
        a || this.tmpExtent_
      );
      return this.getTileRangeForExtentAndZ(
        u,
        t[0] + 1,
        i
      );
    }
    return null;
  }
  /**
   * @param {import("../tilecoord.js").TileCoord} tileCoord Tile coordinate.
   * @param {number} z Integer zoom level.
   * @param {import("../TileRange.js").default} [tempTileRange] Temporary import("../TileRange.js").default object.
   * @return {import("../TileRange.js").default|null} Tile range.
   */
  getTileRangeForTileCoordAndZ(t, i, a) {
    if (i > this.maxZoom || i < this.minZoom)
      return null;
    const u = t[0], h = t[1], g = t[2];
    if (i === u)
      return Om(
        h,
        g,
        h,
        g,
        a
      );
    if (this.zoomFactor_) {
      const _ = Math.pow(this.zoomFactor_, i - u), b = Math.floor(h * _), E = Math.floor(g * _);
      if (i < u)
        return Om(b, b, E, E, a);
      const x = Math.floor(_ * (h + 1)) - 1, R = Math.floor(_ * (g + 1)) - 1;
      return Om(b, x, E, R, a);
    }
    const y = this.getTileCoordExtent(t, this.tmpExtent_);
    return this.getTileRangeForExtentAndZ(y, i, a);
  }
  /**
   * Get a tile range for the given extent and integer zoom level.
   * @param {import("../extent.js").Extent} extent Extent.
   * @param {number} z Integer zoom level.
   * @param {import("../TileRange.js").default} [tempTileRange] Temporary tile range object.
   * @return {import("../TileRange.js").default} Tile range.
   */
  getTileRangeForExtentAndZ(t, i, a) {
    this.getTileCoordForXYAndZ_(t[0], t[3], i, !1, zm);
    const u = zm[1], h = zm[2];
    this.getTileCoordForXYAndZ_(t[2], t[1], i, !0, zm);
    const g = zm[1], y = zm[2];
    return Om(u, g, h, y, a);
  }
  /**
   * @param {import("../tilecoord.js").TileCoord} tileCoord Tile coordinate.
   * @return {import("../coordinate.js").Coordinate} Tile center.
   */
  getTileCoordCenter(t) {
    const i = this.getOrigin(t[0]), a = this.getResolution(t[0]), u = Zl(this.getTileSize(t[0]), this.tmpSize_);
    return [
      i[0] + (t[1] + 0.5) * u[0] * a,
      i[1] - (t[2] + 0.5) * u[1] * a
    ];
  }
  /**
   * Get the extent of a tile coordinate.
   *
   * @param {import("../tilecoord.js").TileCoord} tileCoord Tile coordinate.
   * @param {import("../extent.js").Extent} [tempExtent] Temporary extent object.
   * @return {import("../extent.js").Extent} Extent.
   * @api
   */
  getTileCoordExtent(t, i) {
    const a = this.getOrigin(t[0]), u = this.getResolution(t[0]), h = Zl(this.getTileSize(t[0]), this.tmpSize_), g = a[0] + t[1] * h[0] * u, y = a[1] - (t[2] + 1) * h[1] * u, _ = g + h[0] * u, b = y + h[1] * u;
    return $o(g, y, _, b, i);
  }
  /**
   * Get the tile coordinate for the given map coordinate and resolution.  This
   * method considers that coordinates that intersect tile boundaries should be
   * assigned the higher tile coordinate.
   *
   * @param {import("../coordinate.js").Coordinate} coordinate Coordinate.
   * @param {number} resolution Resolution.
   * @param {import("../tilecoord.js").TileCoord} [opt_tileCoord] Destination import("../tilecoord.js").TileCoord object.
   * @return {import("../tilecoord.js").TileCoord} Tile coordinate.
   * @api
   */
  getTileCoordForCoordAndResolution(t, i, a) {
    return this.getTileCoordForXYAndResolution_(
      t[0],
      t[1],
      i,
      !1,
      a
    );
  }
  /**
   * Note that this method should not be called for resolutions that correspond
   * to an integer zoom level.  Instead call the `getTileCoordForXYAndZ_` method.
   * @param {number} x X.
   * @param {number} y Y.
   * @param {number} resolution Resolution (for a non-integer zoom level).
   * @param {boolean} reverseIntersectionPolicy Instead of letting edge
   *     intersections go to the higher tile coordinate, let edge intersections
   *     go to the lower tile coordinate.
   * @param {import("../tilecoord.js").TileCoord} [opt_tileCoord] Temporary import("../tilecoord.js").TileCoord object.
   * @return {import("../tilecoord.js").TileCoord} Tile coordinate.
   * @private
   */
  getTileCoordForXYAndResolution_(t, i, a, u, h) {
    const g = this.getZForResolution(a), y = a / this.getResolution(g), _ = this.getOrigin(g), b = Zl(this.getTileSize(g), this.tmpSize_);
    let E = y * (t - _[0]) / a / b[0], x = y * (_[1] - i) / a / b[1];
    return u ? (E = A1(E, Ic) - 1, x = A1(x, Ic) - 1) : (E = R1(E, Ic), x = R1(x, Ic)), iS(g, E, x, h);
  }
  /**
   * Although there is repetition between this method and `getTileCoordForXYAndResolution_`,
   * they should have separate implementations.  This method is for integer zoom
   * levels.  The other method should only be called for resolutions corresponding
   * to non-integer zoom levels.
   * @param {number} x Map x coordinate.
   * @param {number} y Map y coordinate.
   * @param {number} z Integer zoom level.
   * @param {boolean} reverseIntersectionPolicy Instead of letting edge
   *     intersections go to the higher tile coordinate, let edge intersections
   *     go to the lower tile coordinate.
   * @param {import("../tilecoord.js").TileCoord} [opt_tileCoord] Temporary import("../tilecoord.js").TileCoord object.
   * @return {import("../tilecoord.js").TileCoord} Tile coordinate.
   * @private
   */
  getTileCoordForXYAndZ_(t, i, a, u, h) {
    const g = this.getOrigin(a), y = this.getResolution(a), _ = Zl(this.getTileSize(a), this.tmpSize_);
    let b = (t - g[0]) / y / _[0], E = (g[1] - i) / y / _[1];
    return u ? (b = A1(b, Ic) - 1, E = A1(E, Ic) - 1) : (b = R1(b, Ic), E = R1(E, Ic)), iS(a, b, E, h);
  }
  /**
   * Get a tile coordinate given a map coordinate and zoom level.
   * @param {import("../coordinate.js").Coordinate} coordinate Coordinate.
   * @param {number} z Integer zoom level, e.g. the result of a `getZForResolution()` method call
   * @param {import("../tilecoord.js").TileCoord} [opt_tileCoord] Destination import("../tilecoord.js").TileCoord object.
   * @return {import("../tilecoord.js").TileCoord} Tile coordinate.
   * @api
   */
  getTileCoordForCoordAndZ(t, i, a) {
    return this.getTileCoordForXYAndZ_(
      t[0],
      t[1],
      i,
      !1,
      a
    );
  }
  /**
   * @param {import("../tilecoord.js").TileCoord} tileCoord Tile coordinate.
   * @return {number} Tile resolution.
   */
  getTileCoordResolution(t) {
    return this.resolutions_[t[0]];
  }
  /**
   * Get the tile size for a zoom level. The type of the return value matches the
   * `tileSize` or `tileSizes` that the tile grid was configured with. To always
   * get an {@link import("../size.js").Size}, run the result through {@link module:ol/size.toSize}.
   * @param {number} z Z.
   * @return {number|import("../size.js").Size} Tile size.
   * @api
   */
  getTileSize(t) {
    return this.tileSize_ ? this.tileSize_ : this.tileSizes_[t];
  }
  /**
   * @param {number} z Zoom level.
   * @return {import("../TileRange.js").default|null} Extent tile range for the specified zoom level.
   */
  getFullTileRange(t) {
    return this.fullTileRanges_ ? this.fullTileRanges_[t] : this.extent_ ? this.getTileRangeForExtentAndZ(this.extent_, t) : null;
  }
  /**
   * @param {number} resolution Resolution.
   * @param {number|import("../array.js").NearestDirectionFunction} [opt_direction]
   *     If 0, the nearest resolution will be used.
   *     If 1, the nearest higher resolution (lower Z) will be used. If -1, the
   *     nearest lower resolution (higher Z) will be used. Default is 0.
   *     Use a {@link module:ol/array~NearestDirectionFunction} for more precise control.
   *
   * For example to change tile Z at the midpoint of zoom levels
   * ```js
   * function(value, high, low) {
   *   return value - low * Math.sqrt(high / low);
   * }
   * ```
   * @return {number} Z.
   * @api
   */
  getZForResolution(t, i) {
    const a = tT(
      this.resolutions_,
      t,
      i || 0
    );
    return gn(a, this.minZoom, this.maxZoom);
  }
  /**
   * The tile with the provided tile coordinate intersects the given viewport.
   * @param {import('../tilecoord.js').TileCoord} tileCoord Tile coordinate.
   * @param {Array<number>} viewport Viewport as returned from {@link module:ol/extent.getRotatedViewport}.
   * @return {boolean} The tile with the provided tile coordinate intersects the given viewport.
   */
  tileCoordIntersectsViewport(t, i) {
    return H2(
      i,
      0,
      i.length,
      2,
      this.getTileCoordExtent(t)
    );
  }
  /**
   * @param {!import("../extent.js").Extent} extent Extent for this tile grid.
   * @private
   */
  calculateTileRanges_(t) {
    const i = this.resolutions_.length, a = new Array(i);
    for (let u = this.minZoom; u < i; ++u)
      a[u] = this.getTileRangeForExtentAndZ(t, u);
    this.fullTileRanges_ = a;
  }
}
function MA(r) {
  let t = r.getDefaultTileGrid();
  return t || (t = kL(r), r.setDefaultTileGrid(t)), t;
}
function BL(r, t, i) {
  const a = t[0], u = r.getTileCoordCenter(t), h = qT(i);
  if (!Fm(h, u)) {
    const g = Xe(h), y = Math.ceil(
      (h[0] - u[0]) / g
    );
    return u[0] += g * y, r.getTileCoordForCoordAndZ(u, a);
  }
  return t;
}
function IL(r, t, i, a) {
  a = a !== void 0 ? a : "top-left";
  const u = wA(r, t, i);
  return new AA({
    extent: r,
    origin: mw(r, a),
    resolutions: u,
    tileSize: i
  });
}
function XL(r) {
  const t = r || {}, i = t.extent || Zi("EPSG:3857").getExtent(), a = {
    extent: i,
    minZoom: t.minZoom,
    tileSize: t.tileSize,
    resolutions: wA(
      i,
      t.maxZoom,
      t.tileSize,
      t.maxResolution
    )
  };
  return new AA(a);
}
function wA(r, t, i, a) {
  t = t !== void 0 ? t : j3, i = Zl(i !== void 0 ? i : UT);
  const u = ml(r), h = Xe(r);
  a = a > 0 ? a : Math.max(h / i[0], u / i[1]);
  const g = t + 1, y = new Array(g);
  for (let _ = 0; _ < g; ++_)
    y[_] = a / Math.pow(2, _);
  return y;
}
function kL(r, t, i, a) {
  const u = qT(r);
  return IL(u, t, i, a);
}
function qT(r) {
  r = Zi(r);
  let t = r.getExtent();
  if (!t) {
    const i = 180 * aT.degrees / r.getMetersPerUnit();
    t = $o(-i, -i, i, i);
  }
  return t;
}
const VL = /\{z\}/g, qL = /\{x\}/g, ZL = /\{y\}/g, FL = /\{-y\}/g;
function KL(r, t, i, a, u) {
  return r.replace(VL, t.toString()).replace(qL, i.toString()).replace(ZL, a.toString()).replace(FL, function() {
    if (u === void 0)
      throw new Error(
        "If the URL template has a {-y} placeholder, the grid extent must be known"
      );
    return (u - a).toString();
  });
}
function PL(r) {
  const t = [];
  let i = /\{([a-z])-([a-z])\}/.exec(r);
  if (i) {
    const a = i[1].charCodeAt(0), u = i[2].charCodeAt(0);
    let h;
    for (h = a; h <= u; ++h)
      t.push(r.replace(i[0], String.fromCharCode(h)));
    return t;
  }
  if (i = /\{(\d+)-(\d+)\}/.exec(r), i) {
    const a = parseInt(i[2], 10);
    for (let u = parseInt(i[1], 10); u <= a; u++)
      t.push(r.replace(i[0], u.toString()));
    return t;
  }
  return t.push(r), t;
}
function QL(r, t) {
  return (
    /**
     * @param {import("./tilecoord.js").TileCoord} tileCoord Tile Coordinate.
     * @param {number} pixelRatio Pixel ratio.
     * @param {import("./proj/Projection.js").default} projection Projection.
     * @return {string|undefined} Tile URL.
     */
    function(i, a, u) {
      if (!i)
        return;
      let h;
      const g = i[0];
      if (t) {
        const y = t.getFullTileRange(g);
        y && (h = y.getHeight() - 1);
      }
      return KL(r, g, i[1], i[2], h);
    }
  );
}
function WL(r, t) {
  const i = r.length, a = new Array(i);
  for (let u = 0; u < i; ++u)
    a[u] = QL(r[u], t);
  return JL(a);
}
function JL(r) {
  return r.length === 1 ? r[0] : (
    /**
     * @param {import("./tilecoord.js").TileCoord} tileCoord Tile Coordinate.
     * @param {number} pixelRatio Pixel ratio.
     * @param {import("./proj/Projection.js").default} projection Projection.
     * @return {string|undefined} Tile URL.
     */
    function(t, i, a) {
      if (!t)
        return;
      const u = HL(t), h = Qh(u, r.length);
      return r[h](t, i, a);
    }
  );
}
let bb;
const km = [];
function JR(r, t, i, a, u) {
  r.beginPath(), r.moveTo(0, 0), r.lineTo(t, i), r.lineTo(a, u), r.closePath(), r.save(), r.clip(), r.fillRect(0, 0, Math.max(t, a) + 1, Math.max(i, u)), r.restore();
}
function Tb(r, t) {
  return Math.abs(r[t * 4] - 210) > 2 || Math.abs(r[t * 4 + 3] - 0.75 * 255) > 2;
}
function $L() {
  if (bb === void 0) {
    const r = ei(6, 6, km);
    r.globalCompositeOperation = "lighter", r.fillStyle = "rgba(210, 0, 0, 0.75)", JR(r, 4, 5, 4, 0), JR(r, 4, 5, 0, 5);
    const t = r.getImageData(0, 0, 3, 3).data;
    bb = Tb(t, 0) || Tb(t, 4) || Tb(t, 8), _S(r), km.push(r.canvas);
  }
  return bb;
}
function $R(r, t, i, a) {
  const u = gS(i, t, r);
  let h = hR(
    t,
    a,
    i
  );
  const g = t.getMetersPerUnit();
  g !== void 0 && (h *= g);
  const y = r.getMetersPerUnit();
  y !== void 0 && (h /= y);
  const _ = r.getExtent();
  if (!_ || Fm(_, u)) {
    const b = hR(r, h, u) / h;
    isFinite(b) && b > 0 && (h /= b);
  }
  return h;
}
function tz(r, t, i, a) {
  const u = Pc(i);
  let h = $R(
    r,
    t,
    u,
    a
  );
  return (!isFinite(h) || h <= 0) && iT(i, function(g) {
    return h = $R(
      r,
      t,
      g,
      a
    ), isFinite(h) && h > 0;
  }), h;
}
function ez(r, t, i, a, u, h, g, y, _, b, E, x, R, w) {
  const L = ei(
    Math.round(i * r),
    Math.round(i * t),
    km
  );
  if (x || (L.imageSmoothingEnabled = !1), _.length === 0)
    return L.canvas;
  L.scale(i, i);
  function H(Z) {
    return Math.round(Z * i) / i;
  }
  L.globalCompositeOperation = "lighter";
  const N = _a();
  _.forEach(function(Z, dt, ft) {
    m2(N, Z.extent);
  });
  let O;
  const U = i / a, I = (x ? 1 : 1 + Math.pow(2, -24)) / U;
  O = ei(
    Math.round(Xe(N) * U),
    Math.round(ml(N) * U),
    km
  ), x || (O.imageSmoothingEnabled = !1), _.forEach(function(Z, dt, ft) {
    if (Z.image.width > 0 && Z.image.height > 0) {
      if (Z.clipExtent) {
        O.save();
        const pt = (Z.clipExtent[0] - N[0]) * U, Tt = -(Z.clipExtent[3] - N[3]) * U, Nt = Xe(Z.clipExtent) * U, Rt = ml(Z.clipExtent) * U;
        O.rect(
          x ? pt : Math.round(pt),
          x ? Tt : Math.round(Tt),
          x ? Nt : Math.round(pt + Nt) - Math.round(pt),
          x ? Rt : Math.round(Tt + Rt) - Math.round(Tt)
        ), O.clip();
      }
      const yt = (Z.extent[0] - N[0]) * U, it = -(Z.extent[3] - N[3]) * U, Et = Xe(Z.extent) * U, xt = ml(Z.extent) * U;
      O.drawImage(
        Z.image,
        b,
        b,
        Z.image.width - 2 * b,
        Z.image.height - 2 * b,
        x ? yt : Math.round(yt),
        x ? it : Math.round(it),
        x ? Et : Math.round(yt + Et) - Math.round(yt),
        x ? xt : Math.round(it + xt) - Math.round(it)
      ), Z.clipExtent && O.restore();
    }
  });
  const X = nd(g);
  return y.getTriangles().forEach(function(Z, dt, ft) {
    const yt = Z.source, it = Z.target;
    let Et = yt[0][0], xt = yt[0][1], pt = yt[1][0], Tt = yt[1][1], Nt = yt[2][0], Rt = yt[2][1];
    const Y = H((it[0][0] - X[0]) / h), K = H(
      -(it[0][1] - X[1]) / h
    ), nt = H((it[1][0] - X[0]) / h), vt = H(
      -(it[1][1] - X[1]) / h
    ), F = H((it[2][0] - X[0]) / h), St = H(
      -(it[2][1] - X[1]) / h
    ), Q = Et, At = xt;
    Et = 0, xt = 0, pt -= Q, Tt -= At, Nt -= Q, Rt -= At;
    const Ut = [
      [pt, Tt, 0, 0, nt - Y],
      [Nt, Rt, 0, 0, F - Y],
      [0, 0, pt, Tt, vt - K],
      [0, 0, Nt, Rt, St - K]
    ], kt = Sw(Ut);
    if (!kt)
      return;
    if (L.save(), L.beginPath(), $L() || !x) {
      L.moveTo(nt, vt);
      const Ze = 4, _e = Y - nt, De = K - vt;
      for (let ke = 0; ke < Ze; ke++)
        L.lineTo(
          nt + H((ke + 1) * _e / Ze),
          vt + H(ke * De / (Ze - 1))
        ), ke != Ze - 1 && L.lineTo(
          nt + H((ke + 1) * _e / Ze),
          vt + H((ke + 1) * De / (Ze - 1))
        );
      L.lineTo(F, St);
    } else
      L.moveTo(nt, vt), L.lineTo(Y, K), L.lineTo(F, St);
    L.clip(), L.transform(
      kt[0],
      kt[2],
      kt[1],
      kt[3],
      Y,
      K
    ), L.translate(
      N[0] - Q,
      N[3] - At
    );
    let Xt;
    if (O)
      Xt = O.canvas, L.scale(I, -I);
    else {
      const Ze = _[0], _e = Ze.extent;
      Xt = Ze.image, L.scale(
        Xe(_e) / Xt.width,
        -ml(_e) / Xt.height
      );
    }
    L.drawImage(Xt, 0, 0), L.restore();
  }), O && (_S(O), km.push(O.canvas)), E && (L.save(), L.globalCompositeOperation = "source-over", L.strokeStyle = "black", L.lineWidth = 1, y.getTriangles().forEach(function(Z, dt, ft) {
    const yt = Z.target, it = (yt[0][0] - X[0]) / h, Et = -(yt[0][1] - X[1]) / h, xt = (yt[1][0] - X[0]) / h, pt = -(yt[1][1] - X[1]) / h, Tt = (yt[2][0] - X[0]) / h, Nt = -(yt[2][1] - X[1]) / h;
    L.beginPath(), L.moveTo(xt, pt), L.lineTo(it, Et), L.lineTo(Tt, Nt), L.closePath(), L.stroke();
  }), L.restore()), L.canvas;
}
const nz = 10, t2 = 0.25;
class iz {
  /**
   * @param {import("../proj/Projection.js").default} sourceProj Source projection.
   * @param {import("../proj/Projection.js").default} targetProj Target projection.
   * @param {import("../extent.js").Extent} targetExtent Target extent to triangulate.
   * @param {import("../extent.js").Extent} maxSourceExtent Maximal source extent that can be used.
   * @param {number} errorThreshold Acceptable error (in source units).
   * @param {?number} destinationResolution The (optional) resolution of the destination.
   * @param {import("../transform.js").Transform} [sourceMatrix] Source transform matrix.
   */
  constructor(t, i, a, u, h, g, y) {
    this.sourceProj_ = t, this.targetProj_ = i;
    let _ = {};
    const b = y ? tD(
      (I) => hi(
        y,
        gS(I, this.targetProj_, this.sourceProj_)
      )
    ) : m_(this.targetProj_, this.sourceProj_);
    this.transformInv_ = function(I) {
      const X = I[0] + "/" + I[1];
      return _[X] || (_[X] = b(I)), _[X];
    }, this.maxSourceExtent_ = u, this.errorThresholdSquared_ = h * h, this.triangles_ = [], this.wrapsXInSource_ = !1, this.canWrapXInSource_ = this.sourceProj_.canWrapX() && !!u && !!this.sourceProj_.getExtent() && Xe(u) >= Xe(this.sourceProj_.getExtent()), this.sourceWorldWidth_ = this.sourceProj_.getExtent() ? Xe(this.sourceProj_.getExtent()) : null, this.targetWorldWidth_ = this.targetProj_.getExtent() ? Xe(this.targetProj_.getExtent()) : null;
    const E = nd(a), x = fS(a), R = cS(a), w = uS(a), L = this.transformInv_(E), H = this.transformInv_(x), N = this.transformInv_(R), O = this.transformInv_(w), U = nz + (g ? Math.max(
      0,
      Math.ceil(
        Math.log2(
          Db(a) / (g * g * 256 * 256)
        )
      )
    ) : 0);
    if (this.addQuad_(
      E,
      x,
      R,
      w,
      L,
      H,
      N,
      O,
      U
    ), this.wrapsXInSource_) {
      let I = 1 / 0;
      this.triangles_.forEach(function(X, Z, dt) {
        I = Math.min(
          I,
          X.source[0][0],
          X.source[1][0],
          X.source[2][0]
        );
      }), this.triangles_.forEach((X) => {
        if (Math.max(
          X.source[0][0],
          X.source[1][0],
          X.source[2][0]
        ) - I > this.sourceWorldWidth_ / 2) {
          const Z = [
            [X.source[0][0], X.source[0][1]],
            [X.source[1][0], X.source[1][1]],
            [X.source[2][0], X.source[2][1]]
          ];
          Z[0][0] - I > this.sourceWorldWidth_ / 2 && (Z[0][0] -= this.sourceWorldWidth_), Z[1][0] - I > this.sourceWorldWidth_ / 2 && (Z[1][0] -= this.sourceWorldWidth_), Z[2][0] - I > this.sourceWorldWidth_ / 2 && (Z[2][0] -= this.sourceWorldWidth_);
          const dt = Math.min(
            Z[0][0],
            Z[1][0],
            Z[2][0]
          );
          Math.max(
            Z[0][0],
            Z[1][0],
            Z[2][0]
          ) - dt < this.sourceWorldWidth_ / 2 && (X.source = Z);
        }
      });
    }
    _ = {};
  }
  /**
   * Adds triangle to the triangulation.
   * @param {import("../coordinate.js").Coordinate} a The target a coordinate.
   * @param {import("../coordinate.js").Coordinate} b The target b coordinate.
   * @param {import("../coordinate.js").Coordinate} c The target c coordinate.
   * @param {import("../coordinate.js").Coordinate} aSrc The source a coordinate.
   * @param {import("../coordinate.js").Coordinate} bSrc The source b coordinate.
   * @param {import("../coordinate.js").Coordinate} cSrc The source c coordinate.
   * @private
   */
  addTriangle_(t, i, a, u, h, g) {
    this.triangles_.push({
      source: [u, h, g],
      target: [t, i, a]
    });
  }
  /**
   * Adds quad (points in clock-wise order) to the triangulation
   * (and reprojects the vertices) if valid.
   * Performs quad subdivision if needed to increase precision.
   *
   * @param {import("../coordinate.js").Coordinate} a The target a coordinate.
   * @param {import("../coordinate.js").Coordinate} b The target b coordinate.
   * @param {import("../coordinate.js").Coordinate} c The target c coordinate.
   * @param {import("../coordinate.js").Coordinate} d The target d coordinate.
   * @param {import("../coordinate.js").Coordinate} aSrc The source a coordinate.
   * @param {import("../coordinate.js").Coordinate} bSrc The source b coordinate.
   * @param {import("../coordinate.js").Coordinate} cSrc The source c coordinate.
   * @param {import("../coordinate.js").Coordinate} dSrc The source d coordinate.
   * @param {number} maxSubdivision Maximal allowed subdivision of the quad.
   * @private
   */
  addQuad_(t, i, a, u, h, g, y, _, b) {
    const E = Vh([h, g, y, _]), x = this.sourceWorldWidth_ ? Xe(E) / this.sourceWorldWidth_ : null, R = (
      /** @type {number} */
      this.sourceWorldWidth_
    ), w = this.sourceProj_.canWrapX() && x > 0.5 && x < 1;
    let L = !1;
    if (b > 0) {
      if (this.targetProj_.isGlobal() && this.targetWorldWidth_) {
        const N = Vh([t, i, a, u]);
        L = Xe(N) / this.targetWorldWidth_ > t2 || L;
      }
      !w && this.sourceProj_.isGlobal() && x && (L = x > t2 || L);
    }
    if (!L && this.maxSourceExtent_ && isFinite(E[0]) && isFinite(E[1]) && isFinite(E[2]) && isFinite(E[3]) && !qi(E, this.maxSourceExtent_))
      return;
    let H = 0;
    if (!L && (!isFinite(h[0]) || !isFinite(h[1]) || !isFinite(g[0]) || !isFinite(g[1]) || !isFinite(y[0]) || !isFinite(y[1]) || !isFinite(_[0]) || !isFinite(_[1]))) {
      if (b > 0)
        L = !0;
      else if (H = (!isFinite(h[0]) || !isFinite(h[1]) ? 8 : 0) + (!isFinite(g[0]) || !isFinite(g[1]) ? 4 : 0) + (!isFinite(y[0]) || !isFinite(y[1]) ? 2 : 0) + (!isFinite(_[0]) || !isFinite(_[1]) ? 1 : 0), H != 1 && H != 2 && H != 4 && H != 8)
        return;
    }
    if (b > 0) {
      if (!L) {
        const N = [(t[0] + a[0]) / 2, (t[1] + a[1]) / 2], O = this.transformInv_(N);
        let U;
        w ? U = (Qh(h[0], R) + Qh(y[0], R)) / 2 - Qh(O[0], R) : U = (h[0] + y[0]) / 2 - O[0];
        const I = (h[1] + y[1]) / 2 - O[1];
        L = U * U + I * I > this.errorThresholdSquared_;
      }
      if (L) {
        if (Math.abs(t[0] - a[0]) <= Math.abs(t[1] - a[1])) {
          const N = [(i[0] + a[0]) / 2, (i[1] + a[1]) / 2], O = this.transformInv_(N), U = [(u[0] + t[0]) / 2, (u[1] + t[1]) / 2], I = this.transformInv_(U);
          this.addQuad_(
            t,
            i,
            N,
            U,
            h,
            g,
            O,
            I,
            b - 1
          ), this.addQuad_(
            U,
            N,
            a,
            u,
            I,
            O,
            y,
            _,
            b - 1
          );
        } else {
          const N = [(t[0] + i[0]) / 2, (t[1] + i[1]) / 2], O = this.transformInv_(N), U = [(a[0] + u[0]) / 2, (a[1] + u[1]) / 2], I = this.transformInv_(U);
          this.addQuad_(
            t,
            N,
            U,
            u,
            h,
            O,
            I,
            _,
            b - 1
          ), this.addQuad_(
            N,
            i,
            a,
            U,
            O,
            g,
            y,
            I,
            b - 1
          );
        }
        return;
      }
    }
    if (w) {
      if (!this.canWrapXInSource_)
        return;
      this.wrapsXInSource_ = !0;
    }
    H & 11 || this.addTriangle_(t, a, u, h, y, _), H & 14 || this.addTriangle_(t, a, i, h, y, g), H && (H & 13 || this.addTriangle_(i, u, t, g, _, h), H & 7 || this.addTriangle_(i, u, a, g, _, y));
  }
  /**
   * Calculates extent of the `source` coordinates from all the triangles.
   *
   * @return {import("../extent.js").Extent} Calculated extent.
   */
  calculateSourceExtent() {
    const t = _a();
    return this.triangles_.forEach(function(i, a, u) {
      const h = i.source;
      r_(t, h[0]), r_(t, h[1]), r_(t, h[2]);
    }), t;
  }
  /**
   * @return {Array<Triangle>} Array of the calculated triangles.
   */
  getTriangles() {
    return this.triangles_;
  }
}
const lz = 0.5;
class DA extends HT {
  /**
   * @param {import("../proj/Projection.js").default} sourceProj Source projection.
   * @param {import("../tilegrid/TileGrid.js").default} sourceTileGrid Source tile grid.
   * @param {import("../proj/Projection.js").default} targetProj Target projection.
   * @param {import("../tilegrid/TileGrid.js").default} targetTileGrid Target tile grid.
   * @param {import("../tilecoord.js").TileCoord} tileCoord Coordinate of the tile.
   * @param {import("../tilecoord.js").TileCoord} wrappedTileCoord Coordinate of the tile wrapped in X.
   * @param {number} pixelRatio Pixel ratio.
   * @param {number} gutter Gutter of the source tiles.
   * @param {FunctionType} getTileFunction
   *     Function returning source tiles (z, x, y, pixelRatio).
   * @param {number} [errorThreshold] Acceptable reprojection error (in px).
   * @param {boolean} [renderEdges] Render reprojection edges.
   * @param {import("../Tile.js").Options} [options] Tile options.
   */
  constructor(t, i, a, u, h, g, y, _, b, E, x, R) {
    super(h, It.IDLE, R), this.renderEdges_ = x !== void 0 ? x : !1, this.pixelRatio_ = y, this.gutter_ = _, this.canvas_ = null, this.sourceTileGrid_ = i, this.targetTileGrid_ = u, this.wrappedTileCoord_ = g || h, this.sourceTiles_ = [], this.sourcesListenerKeys_ = null, this.sourceZ_ = 0, this.clipExtent_ = t.canWrapX() ? t.getExtent() : void 0;
    const w = u.getTileCoordExtent(
      this.wrappedTileCoord_
    ), L = this.targetTileGrid_.getExtent();
    let H = this.sourceTileGrid_.getExtent();
    const N = L ? Ph(w, L) : w;
    if (Db(N) === 0) {
      this.state = It.EMPTY;
      return;
    }
    const O = t.getExtent();
    O && (H ? H = Ph(H, O) : H = O);
    const U = u.getResolution(
      this.wrappedTileCoord_[0]
    ), I = tz(
      t,
      a,
      N,
      U
    );
    if (!isFinite(I) || I <= 0) {
      this.state = It.EMPTY;
      return;
    }
    const X = E !== void 0 ? E : lz;
    if (this.triangulation_ = new iz(
      t,
      a,
      N,
      H,
      I * X,
      U
    ), this.triangulation_.getTriangles().length === 0) {
      this.state = It.EMPTY;
      return;
    }
    this.sourceZ_ = i.getZForResolution(I);
    let Z = this.triangulation_.calculateSourceExtent();
    if (H && (t.canWrapX() ? (Z[1] = gn(
      Z[1],
      H[1],
      H[3]
    ), Z[3] = gn(
      Z[3],
      H[1],
      H[3]
    )) : Z = Ph(Z, H)), !Db(Z))
      this.state = It.EMPTY;
    else {
      let dt = 0, ft = 0;
      t.canWrapX() && (dt = Xe(O), ft = Math.floor(
        (Z[0] - O[0]) / dt
      )), v2(
        Z.slice(),
        t,
        !0
      ).forEach((it) => {
        const Et = i.getTileRangeForExtentAndZ(
          it,
          this.sourceZ_
        );
        for (let xt = Et.minX; xt <= Et.maxX; xt++)
          for (let pt = Et.minY; pt <= Et.maxY; pt++) {
            const Tt = b(this.sourceZ_, xt, pt, y);
            if (Tt) {
              const Nt = ft * dt;
              this.sourceTiles_.push({ tile: Tt, offset: Nt });
            }
          }
        ++ft;
      }), this.sourceTiles_.length === 0 && (this.state = It.EMPTY);
    }
  }
  /**
   * Get the HTML Canvas element for this tile.
   * @return {HTMLCanvasElement} Canvas.
   */
  getImage() {
    return this.canvas_;
  }
  /**
   * @private
   */
  reproject_() {
    const t = [];
    if (this.sourceTiles_.forEach((i) => {
      var u;
      const a = i.tile;
      if (a && a.getState() == It.LOADED) {
        const h = this.sourceTileGrid_.getTileCoordExtent(a.tileCoord);
        h[0] += i.offset, h[2] += i.offset;
        const g = (u = this.clipExtent_) == null ? void 0 : u.slice();
        g && (g[0] += i.offset, g[2] += i.offset), t.push({
          extent: h,
          clipExtent: g,
          image: a.getImage()
        });
      }
    }), this.sourceTiles_.length = 0, t.length === 0)
      this.state = It.ERROR;
    else {
      const i = this.wrappedTileCoord_[0], a = this.targetTileGrid_.getTileSize(i), u = typeof a == "number" ? a : a[0], h = typeof a == "number" ? a : a[1], g = this.targetTileGrid_.getResolution(i), y = this.sourceTileGrid_.getResolution(
        this.sourceZ_
      ), _ = this.targetTileGrid_.getTileCoordExtent(
        this.wrappedTileCoord_
      );
      this.canvas_ = ez(
        u,
        h,
        this.pixelRatio_,
        y,
        this.sourceTileGrid_.getExtent(),
        g,
        _,
        this.triangulation_,
        t,
        this.gutter_,
        this.renderEdges_,
        this.interpolate
      ), this.state = It.LOADED;
    }
    this.changed();
  }
  /**
   * Load not yet loaded URI.
   * @override
   */
  load() {
    if (this.state == It.IDLE) {
      this.state = It.LOADING, this.changed();
      let t = 0;
      this.sourcesListenerKeys_ = [], this.sourceTiles_.forEach(({ tile: i }) => {
        const a = i.getState();
        if (a == It.IDLE || a == It.LOADING) {
          t++;
          const u = ge(i, $t.CHANGE, (h) => {
            const g = i.getState();
            (g == It.LOADED || g == It.ERROR || g == It.EMPTY) && (qe(u), t--, t === 0 && (this.unlistenSources_(), this.reproject_()));
          });
          this.sourcesListenerKeys_.push(u);
        }
      }), t === 0 ? setTimeout(this.reproject_.bind(this), 0) : this.sourceTiles_.forEach(function({ tile: i }, a, u) {
        i.getState() == It.IDLE && i.load();
      });
    }
  }
  /**
   * @private
   */
  unlistenSources_() {
    this.sourcesListenerKeys_.forEach(qe), this.sourcesListenerKeys_ = null;
  }
  /**
   * Remove from the cache due to expiry
   * @override
   */
  release() {
    this.canvas_ && (_S(this.canvas_.getContext("2d")), km.push(this.canvas_), this.canvas_ = null), super.release();
  }
}
class az extends eA {
  /**
   * @param {Options} options SourceTile source options.
   */
  constructor(t) {
    super({
      attributions: t.attributions,
      attributionsCollapsible: t.attributionsCollapsible,
      projection: t.projection,
      state: t.state,
      wrapX: t.wrapX,
      interpolate: t.interpolate
    }), this.on, this.once, this.un, this.tilePixelRatio_ = t.tilePixelRatio !== void 0 ? t.tilePixelRatio : 1, this.tileGrid = t.tileGrid !== void 0 ? t.tileGrid : null;
    const i = [256, 256];
    this.tileGrid && Zl(this.tileGrid.getTileSize(this.tileGrid.getMinZoom()), i), this.tmpSize = [0, 0], this.key_ = t.key || ue(this), this.tileOptions = {
      transition: t.transition,
      interpolate: t.interpolate
    }, this.zDirection = t.zDirection ? t.zDirection : 0;
  }
  /**
   * @param {import("../proj/Projection.js").default} projection Projection.
   * @return {number} Gutter.
   */
  getGutterForProjection(t) {
    return 0;
  }
  /**
   * Return the key to be used for all tiles in the source.
   * @return {string} The key for all tiles.
   */
  getKey() {
    return this.key_;
  }
  /**
   * Set the value to be used as the key for all tiles in the source.
   * @param {string} key The key for tiles.
   * @protected
   */
  setKey(t) {
    this.key_ !== t && (this.key_ = t, this.changed());
  }
  /**
   * @param {import("../proj/Projection").default} [projection] Projection.
   * @return {Array<number>|null} Resolutions.
   * @override
   */
  getResolutions(t) {
    const i = t ? this.getTileGridForProjection(t) : this.tileGrid;
    return i ? i.getResolutions() : null;
  }
  /**
   * @abstract
   * @param {number} z Tile coordinate z.
   * @param {number} x Tile coordinate x.
   * @param {number} y Tile coordinate y.
   * @param {number} pixelRatio Pixel ratio.
   * @param {import("../proj/Projection.js").default} projection Projection.
   * @return {TileType|null} Tile.
   */
  getTile(t, i, a, u, h) {
    return we();
  }
  /**
   * Return the tile grid of the tile source.
   * @return {import("../tilegrid/TileGrid.js").default|null} Tile grid.
   * @api
   */
  getTileGrid() {
    return this.tileGrid;
  }
  /**
   * @param {import("../proj/Projection.js").default} projection Projection.
   * @return {!import("../tilegrid/TileGrid.js").default} Tile grid.
   */
  getTileGridForProjection(t) {
    return this.tileGrid ? this.tileGrid : MA(t);
  }
  /**
   * Get the tile pixel ratio for this source. Subclasses may override this
   * method, which is meant to return a supported pixel ratio that matches the
   * provided `pixelRatio` as close as possible.
   * @param {number} pixelRatio Pixel ratio.
   * @return {number} Tile pixel ratio.
   */
  getTilePixelRatio(t) {
    return this.tilePixelRatio_;
  }
  /**
   * @param {number} z Z.
   * @param {number} pixelRatio Pixel ratio.
   * @param {import("../proj/Projection.js").default} projection Projection.
   * @return {import("../size.js").Size} Tile size.
   */
  getTilePixelSize(t, i, a) {
    const u = this.getTileGridForProjection(a), h = this.getTilePixelRatio(i), g = Zl(u.getTileSize(t), this.tmpSize);
    return h == 1 ? g : CO(g, h, this.tmpSize);
  }
  /**
   * Returns a tile coordinate wrapped around the x-axis. When the tile coordinate
   * is outside the resolution and extent range of the tile grid, `null` will be
   * returned.
   * @param {import("../tilecoord.js").TileCoord} tileCoord Tile coordinate.
   * @param {import("../proj/Projection.js").default} [projection] Projection.
   * @return {import("../tilecoord.js").TileCoord} Tile coordinate to be passed to the tileUrlFunction or
   *     null if no tile URL should be created for the passed `tileCoord`.
   */
  getTileCoordForTileUrlFunction(t, i) {
    const a = i !== void 0 ? i : this.getProjection(), u = i !== void 0 ? this.getTileGridForProjection(a) : this.tileGrid || this.getTileGridForProjection(a);
    return this.getWrapX() && a.isGlobal() && (t = BL(u, t, a)), jL(t, u) ? t : null;
  }
  /**
   * Remove all cached reprojected tiles from the source. The next render cycle will create new tiles.
   * @api
   */
  clear() {
  }
  /**
   * @override
   */
  refresh() {
    this.clear(), super.refresh();
  }
}
class sz extends ys {
  /**
   * @param {string} type Type.
   * @param {import("../Tile.js").default} tile The tile.
   */
  constructor(t, i) {
    super(t), this.tile = i;
  }
}
const xb = {
  /**
   * Triggered when a tile starts loading.
   * @event module:ol/source/Tile.TileSourceEvent#tileloadstart
   * @api
   */
  TILELOADSTART: "tileloadstart",
  /**
   * Triggered when a tile finishes loading, either when its data is loaded,
   * or when loading was aborted because the tile is no longer needed.
   * @event module:ol/source/Tile.TileSourceEvent#tileloadend
   * @api
   */
  TILELOADEND: "tileloadend",
  /**
   * Triggered if tile loading results in an error. Note that this is not the
   * right place to re-fetch tiles. See {@link module:ol/ImageTile~ImageTile#load}
   * for details.
   * @event module:ol/source/Tile.TileSourceEvent#tileloaderror
   * @api
   */
  TILELOADERROR: "tileloaderror"
};
class ZT extends az {
  /**
   * @param {Options} options Image tile options.
   */
  constructor(t) {
    super({
      attributions: t.attributions,
      cacheSize: t.cacheSize,
      projection: t.projection,
      state: t.state,
      tileGrid: t.tileGrid,
      tilePixelRatio: t.tilePixelRatio,
      wrapX: t.wrapX,
      transition: t.transition,
      interpolate: t.interpolate,
      key: t.key,
      attributionsCollapsible: t.attributionsCollapsible,
      zDirection: t.zDirection
    }), this.generateTileUrlFunction_ = this.tileUrlFunction === ZT.prototype.tileUrlFunction, this.tileLoadFunction = t.tileLoadFunction, t.tileUrlFunction && (this.tileUrlFunction = t.tileUrlFunction), this.urls = null, t.urls ? this.setUrls(t.urls) : t.url && this.setUrl(t.url), this.tileLoadingKeys_ = {};
  }
  /**
   * Deprecated.  Use an ImageTile source instead.
   * Return the tile load function of the source.
   * @return {import("../Tile.js").LoadFunction} TileLoadFunction
   * @api
   */
  getTileLoadFunction() {
    return this.tileLoadFunction;
  }
  /**
   * Deprecated.  Use an ImageTile source instead.
   * Return the tile URL function of the source.
   * @return {import("../Tile.js").UrlFunction} TileUrlFunction
   * @api
   */
  getTileUrlFunction() {
    return Object.getPrototypeOf(this).tileUrlFunction === this.tileUrlFunction ? this.tileUrlFunction.bind(this) : this.tileUrlFunction;
  }
  /**
   * Deprecated.  Use an ImageTile source instead.
   * Return the URLs used for this source.
   * When a tileUrlFunction is used instead of url or urls,
   * null will be returned.
   * @return {!Array<string>|null} URLs.
   * @api
   */
  getUrls() {
    return this.urls;
  }
  /**
   * Handle tile change events.
   * @param {import("../events/Event.js").default} event Event.
   * @protected
   */
  handleTileChange(t) {
    const i = (
      /** @type {import("../Tile.js").default} */
      t.target
    ), a = ue(i), u = i.getState();
    let h;
    u == It.LOADING ? (this.tileLoadingKeys_[a] = !0, h = xb.TILELOADSTART) : a in this.tileLoadingKeys_ && (delete this.tileLoadingKeys_[a], h = u == It.ERROR ? xb.TILELOADERROR : u == It.LOADED ? xb.TILELOADEND : void 0), h != null && this.dispatchEvent(new sz(h, i));
  }
  /**
   * Deprecated.  Use an ImageTile source instead.
   * Set the tile load function of the source.
   * @param {import("../Tile.js").LoadFunction} tileLoadFunction Tile load function.
   * @api
   */
  setTileLoadFunction(t) {
    this.tileLoadFunction = t, this.changed();
  }
  /**
   * Deprecated.  Use an ImageTile source instead.
   * Set the tile URL function of the source.
   * @param {import("../Tile.js").UrlFunction} tileUrlFunction Tile URL function.
   * @param {string} [key] Optional new tile key for the source.
   * @api
   */
  setTileUrlFunction(t, i) {
    this.tileUrlFunction = t, typeof i < "u" ? this.setKey(i) : this.changed();
  }
  /**
   * Set the URL to use for requests.
   * @param {string} url URL.
   * @api
   */
  setUrl(t) {
    const i = PL(t);
    this.urls = i, this.setUrls(i);
  }
  /**
   * Deprecated.  Use an ImageTile source instead.
   * Set the URLs to use for requests.
   * @param {Array<string>} urls URLs.
   * @api
   */
  setUrls(t) {
    this.urls = t;
    const i = t.join(`
`);
    this.generateTileUrlFunction_ ? this.setTileUrlFunction(WL(t, this.tileGrid), i) : this.setKey(i);
  }
  /**
   * @param {import("../tilecoord.js").TileCoord} tileCoord Tile coordinate.
   * @param {number} pixelRatio Pixel ratio.
   * @param {import("../proj/Projection.js").default} projection Projection.
   * @return {string|undefined} Tile URL.
   */
  tileUrlFunction(t, i, a) {
  }
}
class oz extends ZT {
  /**
   * @param {!Options} options Image tile options.
   */
  constructor(t) {
    super({
      attributions: t.attributions,
      cacheSize: t.cacheSize,
      projection: t.projection,
      state: t.state,
      tileGrid: t.tileGrid,
      tileLoadFunction: t.tileLoadFunction ? t.tileLoadFunction : rz,
      tilePixelRatio: t.tilePixelRatio,
      tileUrlFunction: t.tileUrlFunction,
      url: t.url,
      urls: t.urls,
      wrapX: t.wrapX,
      transition: t.transition,
      interpolate: t.interpolate !== void 0 ? t.interpolate : !0,
      key: t.key,
      attributionsCollapsible: t.attributionsCollapsible,
      zDirection: t.zDirection
    }), this.crossOrigin = t.crossOrigin !== void 0 ? t.crossOrigin : null, this.tileClass = t.tileClass !== void 0 ? t.tileClass : _A, this.tileGridForProjection = {}, this.reprojectionErrorThreshold_ = t.reprojectionErrorThreshold, this.renderReprojectionEdges_ = !1;
  }
  /**
   * @param {import("../proj/Projection.js").default} projection Projection.
   * @return {number} Gutter.
   * @override
   */
  getGutterForProjection(t) {
    return this.getProjection() && t && !QE(this.getProjection(), t) ? 0 : this.getGutter();
  }
  /**
   * @return {number} Gutter.
   */
  getGutter() {
    return 0;
  }
  /**
   * Return the key to be used for all tiles in the source.
   * @return {string} The key for all tiles.
   * @override
   */
  getKey() {
    let t = super.getKey();
    return this.getInterpolate() || (t += ":disable-interpolation"), t;
  }
  /**
   * @param {import("../proj/Projection.js").default} projection Projection.
   * @return {!import("../tilegrid/TileGrid.js").default} Tile grid.
   * @override
   */
  getTileGridForProjection(t) {
    const i = this.getProjection();
    if (this.tileGrid && (!i || QE(i, t)))
      return this.tileGrid;
    const a = ue(t);
    return a in this.tileGridForProjection || (this.tileGridForProjection[a] = MA(t)), this.tileGridForProjection[a];
  }
  /**
   * @param {number} z Tile coordinate z.
   * @param {number} x Tile coordinate x.
   * @param {number} y Tile coordinate y.
   * @param {number} pixelRatio Pixel ratio.
   * @param {import("../proj/Projection.js").default} projection Projection.
   * @param {string} key The key set on the tile.
   * @return {!ImageTile} Tile.
   * @private
   */
  createTile_(t, i, a, u, h, g) {
    const y = [t, i, a], _ = this.getTileCoordForTileUrlFunction(
      y,
      h
    ), b = _ ? this.tileUrlFunction(_, u, h) : void 0, E = new this.tileClass(
      y,
      b !== void 0 ? It.IDLE : It.EMPTY,
      b !== void 0 ? b : "",
      this.crossOrigin,
      this.tileLoadFunction,
      this.tileOptions
    );
    return E.key = g, E.addEventListener($t.CHANGE, this.handleTileChange.bind(this)), E;
  }
  /**
   * @param {number} z Tile coordinate z.
   * @param {number} x Tile coordinate x.
   * @param {number} y Tile coordinate y.
   * @param {number} pixelRatio Pixel ratio.
   * @param {import("../proj/Projection.js").default} projection Projection.
   * @return {!(ImageTile|ReprojTile)} Tile.
   * @override
   */
  getTile(t, i, a, u, h) {
    const g = this.getProjection();
    if (!g || !h || QE(g, h))
      return this.getTileInternal(
        t,
        i,
        a,
        u,
        g || h
      );
    const y = [t, i, a], _ = this.getKey(), b = this.getTileGridForProjection(g), E = this.getTileGridForProjection(h), x = this.getTileCoordForTileUrlFunction(
      y,
      h
    ), R = new DA(
      g,
      b,
      h,
      E,
      y,
      x,
      this.getTilePixelRatio(u),
      this.getGutter(),
      (w, L, H, N) => this.getTileInternal(w, L, H, N, g),
      this.reprojectionErrorThreshold_,
      this.renderReprojectionEdges_,
      this.tileOptions
    );
    return R.key = _, R;
  }
  /**
   * @param {number} z Tile coordinate z.
   * @param {number} x Tile coordinate x.
   * @param {number} y Tile coordinate y.
   * @param {number} pixelRatio Pixel ratio.
   * @param {!import("../proj/Projection.js").default} projection Projection.
   * @return {!ImageTile} Tile.
   * @protected
   */
  getTileInternal(t, i, a, u, h) {
    const g = this.getKey();
    return this.createTile_(t, i, a, u, h, g);
  }
  /**
   * Sets whether to render reprojection edges or not (usually for debugging).
   * @param {boolean} render Render the edges.
   * @api
   */
  setRenderReprojectionEdges(t) {
    this.renderReprojectionEdges_ != t && (this.renderReprojectionEdges_ = t, this.changed());
  }
  /**
   * Sets the tile grid to use when reprojecting the tiles to the given
   * projection instead of the default tile grid for the projection.
   *
   * This can be useful when the default tile grid cannot be created
   * (e.g. projection has no extent defined) or
   * for optimization reasons (custom tile size, resolutions, ...).
   *
   * @param {import("../proj.js").ProjectionLike} projection Projection.
   * @param {import("../tilegrid/TileGrid.js").default} tilegrid Tile grid to use for the projection.
   * @api
   */
  setTileGridForProjection(t, i) {
    const a = Zi(t);
    if (a) {
      const u = ue(a);
      u in this.tileGridForProjection || (this.tileGridForProjection[u] = i);
    }
  }
}
function rz(r, t) {
  r.getImage().src = t;
}
class uz extends oz {
  /**
   * @param {Options} [options] XYZ options.
   */
  constructor(t) {
    t = t || {};
    const i = t.projection !== void 0 ? t.projection : "EPSG:3857", a = t.tileGrid !== void 0 ? t.tileGrid : XL({
      extent: qT(i),
      maxResolution: t.maxResolution,
      maxZoom: t.maxZoom,
      minZoom: t.minZoom,
      tileSize: t.tileSize
    });
    super({
      attributions: t.attributions,
      cacheSize: t.cacheSize,
      crossOrigin: t.crossOrigin,
      interpolate: t.interpolate,
      projection: i,
      reprojectionErrorThreshold: t.reprojectionErrorThreshold,
      tileGrid: a,
      tileLoadFunction: t.tileLoadFunction,
      tilePixelRatio: t.tilePixelRatio,
      tileUrlFunction: t.tileUrlFunction,
      url: t.url,
      urls: t.urls,
      wrapX: t.wrapX !== void 0 ? t.wrapX : !0,
      transition: t.transition,
      attributionsCollapsible: t.attributionsCollapsible,
      zDirection: t.zDirection
    }), this.gutter_ = t.gutter !== void 0 ? t.gutter : 0;
  }
  /**
   * @return {number} Gutter.
   * @override
   */
  getGutter() {
    return this.gutter_;
  }
}
function Wb(r) {
  return r instanceof Image || r instanceof HTMLCanvasElement || r instanceof HTMLVideoElement || r instanceof ImageBitmap ? r : null;
}
const cz = new Error("disposed"), fz = [256, 256];
class e2 extends HT {
  /**
   * @param {Options} options Tile options.
   */
  constructor(t) {
    const i = It.IDLE;
    super(t.tileCoord, i, {
      transition: t.transition,
      interpolate: t.interpolate
    }), this.loader_ = t.loader, this.data_ = null, this.error_ = null, this.size_ = t.size || null, this.controller_ = t.controller || null;
  }
  /**
   * Get the tile size.
   * @return {import('./size.js').Size} Tile size.
   */
  getSize() {
    if (this.size_)
      return this.size_;
    const t = Wb(this.data_);
    return t ? [t.width, t.height] : fz;
  }
  /**
   * Get the data for the tile.
   * @return {Data} Tile data.
   * @api
   */
  getData() {
    return this.data_;
  }
  /**
   * Get any loading error.
   * @return {Error} Loading error.
   * @api
   */
  getError() {
    return this.error_;
  }
  /**
   * Load the tile data.
   * @api
   * @override
   */
  load() {
    if (this.state !== It.IDLE && this.state !== It.ERROR)
      return;
    this.state = It.LOADING, this.changed();
    const t = this;
    this.loader_().then(function(i) {
      t.data_ = i, t.state = It.LOADED, t.changed();
    }).catch(function(i) {
      t.error_ = i, t.state = It.ERROR, t.changed();
    });
  }
  /**
   * Clean up.
   * @override
   */
  disposeInternal() {
    this.controller_ && (this.controller_.abort(cz), this.controller_ = null), super.disposeInternal();
  }
}
const hz = '&#169; <a href="https://www.openstreetmap.org/copyright" target="_blank">OpenStreetMap</a> contributors.';
class dz extends uz {
  /**
   * @param {Options} [options] Open Street Map options.
   */
  constructor(t) {
    t = t || {};
    let i;
    t.attributions !== void 0 ? i = t.attributions : i = [hz];
    const a = t.crossOrigin !== void 0 ? t.crossOrigin : "anonymous", u = t.url !== void 0 ? t.url : "https://tile.openstreetmap.org/{z}/{x}/{y}.png";
    super({
      attributions: i,
      attributionsCollapsible: !1,
      cacheSize: t.cacheSize,
      crossOrigin: a,
      interpolate: t.interpolate,
      maxZoom: t.maxZoom !== void 0 ? t.maxZoom : 19,
      reprojectionErrorThreshold: t.reprojectionErrorThreshold,
      tileLoadFunction: t.tileLoadFunction,
      transition: t.transition,
      url: u,
      wrapX: t.wrapX,
      zDirection: t.zDirection
    });
  }
}
function Cb(r, t, i, a, u) {
  return `${ue(r)},${t},${GL(i, a, u)}`;
}
function Rb(r, t, i) {
  if (!(i in r))
    return r[i] = /* @__PURE__ */ new Set([t]), !0;
  const a = r[i], u = a.has(t);
  return u || a.add(t), !u;
}
function gz(r, t, i) {
  const a = r[i];
  return a ? a.delete(t) : !1;
}
function n2(r, t) {
  const i = r.layerStatesArray[r.layerIndex];
  i.extent && (t = Ph(
    t,
    Vc(i.extent, r.viewState.projection)
  ));
  const a = (
    /** @type {import("../../source/Tile.js").default} */
    i.layer.getRenderSource()
  );
  if (!a.getWrapX()) {
    const u = a.getTileGridForProjection(r.viewState.projection).getExtent();
    u && (t = Ph(t, u));
  }
  return t;
}
class mz extends oA {
  /**
   * @param {LayerType} tileLayer Tile layer.
   * @param {Options} [options] Options.
   */
  constructor(t, i) {
    super(t), i = i || {}, this.extentChanged = !0, this.renderComplete = !1, this.renderedExtent_ = null, this.renderedPixelRatio, this.renderedProjection = null, this.renderedTiles = [], this.renderedSourceKey_, this.renderedSourceRevision_, this.tempExtent = _a(), this.tempTileRange_ = new kT(0, 0, 0, 0), this.tempTileCoord_ = iS(0, 0, 0);
    const a = i.cacheSize !== void 0 ? i.cacheSize : 512;
    this.tileCache_ = new UL(a), this.maxStaleKeys = a * 0.5;
  }
  /**
   * @return {LRUCache} Tile cache.
   */
  getTileCache() {
    return this.tileCache_;
  }
  /**
   * Get a tile from the cache or create one if needed.
   *
   * @param {number} z Tile coordinate z.
   * @param {number} x Tile coordinate x.
   * @param {number} y Tile coordinate y.
   * @param {import("../../Map.js").FrameState} frameState Frame state.
   * @return {import("../../Tile.js").default|null} Tile (or null if outside source extent).
   * @protected
   */
  getOrCreateTile(t, i, a, u) {
    const h = this.tileCache_, y = this.getLayer().getSource(), _ = Cb(y, y.getKey(), t, i, a);
    let b;
    if (h.containsKey(_))
      b = h.get(_);
    else {
      if (b = y.getTile(
        t,
        i,
        a,
        u.pixelRatio,
        u.viewState.projection
      ), !b)
        return null;
      h.set(_, b);
    }
    return b;
  }
  /**
   * @param {number} z Tile coordinate z.
   * @param {number} x Tile coordinate x.
   * @param {number} y Tile coordinate y.
   * @param {import("../../Map.js").FrameState} frameState Frame state.
   * @return {import("../../Tile.js").default|null} Tile (or null if outside source extent).
   * @protected
   */
  getTile(t, i, a, u) {
    const h = this.getOrCreateTile(t, i, a, u);
    return h || null;
  }
  /**
   * @param {import("../../pixel.js").Pixel} pixel Pixel.
   * @return {Uint8ClampedArray} Data at the pixel location.
   * @override
   */
  getData(t) {
    const i = this.frameState;
    if (!i)
      return null;
    const a = this.getLayer(), u = hi(
      i.pixelToCoordinateTransform,
      t.slice()
    ), h = a.getExtent();
    if (h && !Fm(h, u))
      return null;
    const g = i.viewState, y = a.getRenderSource(), _ = y.getTileGridForProjection(g.projection), b = y.getTilePixelRatio(i.pixelRatio);
    for (let E = _.getZForResolution(g.resolution); E >= _.getMinZoom(); --E) {
      const x = _.getTileCoordForCoordAndZ(u, E), R = this.getTile(E, x[1], x[2], i);
      if (!R || R.getState() !== It.LOADED)
        continue;
      const w = _.getOrigin(E), L = Zl(_.getTileSize(E)), H = _.getResolution(E);
      let N;
      if (R instanceof _A || R instanceof DA)
        N = R.getImage();
      else if (R instanceof e2) {
        if (N = Wb(R.getData()), !N)
          continue;
      } else
        continue;
      const O = Math.floor(
        b * ((u[0] - w[0]) / H - x[1] * L[0])
      ), U = Math.floor(
        b * ((w[1] - u[1]) / H - x[2] * L[1])
      ), I = Math.round(
        b * y.getGutterForProjection(g.projection)
      );
      return this.getImageData(N, O + I, U + I);
    }
    return null;
  }
  /**
   * Determine whether render should be called.
   * @param {import("../../Map.js").FrameState} frameState Frame state.
   * @return {boolean} Layer is ready to be rendered.
   * @override
   */
  prepareFrame(t) {
    this.renderedProjection ? t.viewState.projection !== this.renderedProjection && (this.tileCache_.clear(), this.renderedProjection = t.viewState.projection) : this.renderedProjection = t.viewState.projection;
    const i = this.getLayer().getSource();
    if (!i)
      return !1;
    const a = i.getRevision();
    return this.renderedSourceRevision_ ? this.renderedSourceRevision_ !== a && (this.renderedSourceRevision_ = a, this.renderedSourceKey_ === i.getKey() && this.tileCache_.clear()) : this.renderedSourceRevision_ = a, !0;
  }
  /**
   * @param {import("../../Map.js").FrameState} frameState Frame state.
   * @param {import("../../extent.js").Extent} extent The extent to be rendered.
   * @param {number} initialZ The zoom level.
   * @param {TileLookup} tilesByZ Lookup of tiles by zoom level.
   * @param {number} preload Number of additional levels to load.
   */
  enqueueTiles(t, i, a, u, h) {
    const g = t.viewState, y = this.getLayer(), _ = y.getRenderSource(), b = _.getTileGridForProjection(g.projection), E = ue(_);
    E in t.wantedTiles || (t.wantedTiles[E] = {});
    const x = t.wantedTiles[E], R = y.getMapInternal(), w = Math.max(
      a - h,
      b.getMinZoom(),
      b.getZForResolution(
        Math.min(
          y.getMaxResolution(),
          R ? R.getView().getResolutionForZoom(Math.max(y.getMinZoom(), 0)) : b.getResolution(0)
        ),
        _.zDirection
      )
    ), L = g.rotation, H = L ? p2(
      g.center,
      g.resolution,
      L,
      t.size
    ) : void 0;
    for (let N = a; N >= w; --N) {
      const O = b.getTileRangeForExtentAndZ(
        i,
        N,
        this.tempTileRange_
      ), U = b.getResolution(N);
      for (let I = O.minX; I <= O.maxX; ++I)
        for (let X = O.minY; X <= O.maxY; ++X) {
          if (L && !b.tileCoordIntersectsViewport([N, I, X], H))
            continue;
          const Z = this.getTile(N, I, X, t);
          if (!Z || !Rb(u, Z, N))
            continue;
          const ft = Z.getKey();
          if (x[ft] = !0, Z.getState() === It.IDLE && !t.tileQueue.isKeyQueued(ft)) {
            const yt = iS(N, I, X, this.tempTileCoord_);
            t.tileQueue.enqueue([
              Z,
              E,
              b.getTileCoordCenter(yt),
              U
            ]);
          }
        }
    }
  }
  /**
   * Look for tiles covering the provided tile coordinate at an alternate
   * zoom level.  Loaded tiles will be added to the provided tile texture lookup.
   * @param {import("../../tilecoord.js").TileCoord} tileCoord The target tile coordinate.
   * @param {TileLookup} tilesByZ Lookup of tiles by zoom level.
   * @return {boolean} The tile coordinate is covered by loaded tiles at the alternate zoom level.
   * @private
   */
  findStaleTile_(t, i) {
    const a = this.tileCache_, u = t[0], h = t[1], g = t[2], y = this.getStaleKeys();
    for (let _ = 0; _ < y.length; ++_) {
      const b = Cb(
        this.getLayer().getSource(),
        y[_],
        u,
        h,
        g
      );
      if (a.containsKey(b)) {
        const E = a.peek(b);
        if (E.getState() === It.LOADED)
          return E.endTransition(ue(this)), Rb(i, E, u), !0;
      }
    }
    return !1;
  }
  /**
   * Look for tiles covering the provided tile coordinate at an alternate
   * zoom level.  Loaded tiles will be added to the provided tile texture lookup.
   * @param {import("../../tilegrid/TileGrid.js").default} tileGrid The tile grid.
   * @param {import("../../tilecoord.js").TileCoord} tileCoord The target tile coordinate.
   * @param {number} altZ The alternate zoom level.
   * @param {TileLookup} tilesByZ Lookup of tiles by zoom level.
   * @return {boolean} The tile coordinate is covered by loaded tiles at the alternate zoom level.
   * @private
   */
  findAltTiles_(t, i, a, u) {
    const h = t.getTileRangeForTileCoordAndZ(
      i,
      a,
      this.tempTileRange_
    );
    if (!h)
      return !1;
    let g = !0;
    const y = this.tileCache_, _ = this.getLayer().getRenderSource(), b = _.getKey();
    for (let E = h.minX; E <= h.maxX; ++E)
      for (let x = h.minY; x <= h.maxY; ++x) {
        const R = Cb(_, b, a, E, x);
        let w = !1;
        if (y.containsKey(R)) {
          const L = y.peek(R);
          L.getState() === It.LOADED && (Rb(u, L, a), w = !0);
        }
        w || (g = !1);
      }
    return g;
  }
  /**
   * Render the layer.
   *
   * The frame rendering logic has three parts:
   *
   *  1. Enqueue tiles
   *  2. Find alt tiles for those that are not yet loaded
   *  3. Render loaded tiles
   *
   * @param {import("../../Map.js").FrameState} frameState Frame state.
   * @param {HTMLElement} target Target that may be used to render content to.
   * @return {HTMLElement} The rendered element.
   * @override
   */
  renderFrame(t, i) {
    this.renderComplete = !0;
    const a = t.layerStatesArray[t.layerIndex], u = t.viewState, h = u.projection, g = u.resolution, y = u.center, _ = t.pixelRatio, b = this.getLayer(), E = b.getSource(), x = E.getTileGridForProjection(h), R = x.getZForResolution(g, E.zDirection), w = x.getResolution(R), L = E.getKey();
    this.renderedSourceKey_ ? this.renderedSourceKey_ !== L && (this.prependStaleKey(this.renderedSourceKey_), this.renderedSourceKey_ = L) : this.renderedSourceKey_ = L;
    let H = t.extent;
    const N = E.getTilePixelRatio(_);
    this.prepareContainer(t, i);
    const O = this.context.canvas.width, U = this.context.canvas.height, I = a.extent && Vc(a.extent, h);
    I && (H = Ph(
      H,
      Vc(a.extent, h)
    ));
    const X = w * O / 2 / N, Z = w * U / 2 / N, dt = [
      y[0] - X,
      y[1] - Z,
      y[0] + X,
      y[1] + Z
    ], ft = {};
    this.renderedTiles.length = 0;
    const yt = b.getPreload();
    if (t.nextExtent) {
      const nt = x.getZForResolution(
        u.nextResolution,
        E.zDirection
      ), vt = n2(t, t.nextExtent);
      this.enqueueTiles(t, vt, nt, ft, yt);
    }
    const it = n2(t, H);
    if (this.enqueueTiles(t, it, R, ft, 0), yt > 0 && setTimeout(() => {
      this.enqueueTiles(
        t,
        it,
        R - 1,
        ft,
        yt - 1
      );
    }, 0), !(R in ft))
      return this.container;
    const Et = ue(this), xt = t.time;
    for (const nt of ft[R]) {
      const vt = nt.getState();
      if (vt === It.EMPTY)
        continue;
      const F = nt.tileCoord;
      if (vt === It.LOADED && nt.getAlpha(Et, xt) === 1) {
        nt.endTransition(Et);
        continue;
      }
      if (vt !== It.ERROR && (this.renderComplete = !1), this.findStaleTile_(F, ft)) {
        gz(ft, nt, R), t.animate = !0;
        continue;
      }
      if (this.findAltTiles_(
        x,
        F,
        R + 1,
        ft
      ))
        continue;
      const At = x.getMinZoom();
      for (let Ut = R - 1; Ut >= At && !this.findAltTiles_(
        x,
        F,
        Ut,
        ft
      ); --Ut)
        ;
    }
    const pt = w / g * _ / N, Tt = this.getRenderContext(t);
    vu(
      this.tempTransform,
      O / 2,
      U / 2,
      pt,
      pt,
      0,
      -O / 2,
      -U / 2
    ), a.extent && this.clipUnrotated(Tt, t, I), E.getInterpolate() || (Tt.imageSmoothingEnabled = !1), this.preRender(Tt, t);
    const Nt = Object.keys(ft).map(Number);
    Nt.sort(hu);
    let Rt;
    const Y = [], K = [];
    for (let nt = Nt.length - 1; nt >= 0; --nt) {
      const vt = Nt[nt], F = E.getTilePixelSize(
        vt,
        _,
        h
      ), Q = x.getResolution(vt) / w, At = F[0] * Q * pt, Ut = F[1] * Q * pt, kt = x.getTileCoordForCoordAndZ(
        nd(dt),
        vt
      ), Xt = x.getTileCoordExtent(kt), Ze = hi(this.tempTransform, [
        N * (Xt[0] - dt[0]) / w,
        N * (dt[3] - Xt[3]) / w
      ]), _e = N * E.getGutterForProjection(h);
      for (const De of ft[vt]) {
        if (De.getState() !== It.LOADED)
          continue;
        const ke = De.tileCoord, yl = kt[1] - ke[1], no = Math.round(Ze[0] - (yl - 1) * At), Fi = kt[2] - ke[2], Kl = Math.round(Ze[1] - (Fi - 1) * Ut), xi = Math.round(Ze[0] - yl * At), Dn = Math.round(Ze[1] - Fi * Ut), yn = no - xi, pl = Kl - Dn, io = Nt.length === 1;
        let Pl = !1;
        Rt = [xi, Dn, xi + yn, Dn, xi + yn, Dn + pl, xi, Dn + pl];
        for (let _l = 0, Sa = Y.length; _l < Sa; ++_l)
          if (!io && vt < K[_l]) {
            const Fe = Y[_l];
            qi(
              [xi, Dn, xi + yn, Dn + pl],
              [Fe[0], Fe[3], Fe[4], Fe[7]]
            ) && (Pl || (Tt.save(), Pl = !0), Tt.beginPath(), Tt.moveTo(Rt[0], Rt[1]), Tt.lineTo(Rt[2], Rt[3]), Tt.lineTo(Rt[4], Rt[5]), Tt.lineTo(Rt[6], Rt[7]), Tt.moveTo(Fe[6], Fe[7]), Tt.lineTo(Fe[4], Fe[5]), Tt.lineTo(Fe[2], Fe[3]), Tt.lineTo(Fe[0], Fe[1]), Tt.clip());
          }
        Y.push(Rt), K.push(vt), this.drawTile(De, t, xi, Dn, yn, pl, _e, io), Pl && Tt.restore(), this.renderedTiles.unshift(De), this.updateUsedTiles(t.usedTiles, E, De);
      }
    }
    if (this.renderedResolution = w, this.extentChanged = !this.renderedExtent_ || !d_(this.renderedExtent_, dt), this.renderedExtent_ = dt, this.renderedPixelRatio = _, this.postRender(this.context, t), a.extent && Tt.restore(), Tt.imageSmoothingEnabled = !0, this.renderComplete) {
      const nt = (vt, F) => {
        const St = ue(E), Q = F.wantedTiles[St], At = Q ? Object.keys(Q).length : 0;
        this.updateCacheSize(At), this.tileCache_.expireCache();
      };
      t.postRenderFunctions.push(nt);
    }
    return this.container;
  }
  /**
   * Increases the cache size if needed
   * @param {number} tileCount Minimum number of tiles needed.
   */
  updateCacheSize(t) {
    this.tileCache_.highWaterMark = Math.max(
      this.tileCache_.highWaterMark,
      t * 2
    );
  }
  /**
   * @param {import("../../Tile.js").default} tile Tile.
   * @param {import("../../Map.js").FrameState} frameState Frame state.
   * @param {number} x Left of the tile.
   * @param {number} y Top of the tile.
   * @param {number} w Width of the tile.
   * @param {number} h Height of the tile.
   * @param {number} gutter Tile gutter.
   * @param {boolean} transition Apply an alpha transition.
   * @protected
   */
  drawTile(t, i, a, u, h, g, y, _) {
    let b;
    if (t instanceof e2) {
      if (b = Wb(t.getData()), !b)
        throw new Error("Rendering array data is not yet supported");
    } else
      b = this.getTileImage(
        /** @type {import("../../ImageTile.js").default} */
        t
      );
    if (!b)
      return;
    const E = this.getRenderContext(i), x = ue(this), R = i.layerStatesArray[i.layerIndex], w = R.opacity * (_ ? t.getAlpha(x, i.time) : 1), L = w !== E.globalAlpha;
    L && (E.save(), E.globalAlpha = w), E.drawImage(
      b,
      y,
      y,
      b.width - 2 * y,
      b.height - 2 * y,
      a,
      u,
      h,
      g
    ), L && E.restore(), w !== R.opacity ? i.animate = !0 : _ && t.endTransition(x);
  }
  /**
   * @return {HTMLCanvasElement} Image
   */
  getImage() {
    const t = this.context;
    return t ? t.canvas : null;
  }
  /**
   * Get the image from a tile.
   * @param {import("../../ImageTile.js").default} tile Tile.
   * @return {HTMLCanvasElement|HTMLImageElement|HTMLVideoElement} Image.
   * @protected
   */
  getTileImage(t) {
    return t.getImage();
  }
  /**
   * @param {!Object<string, !Object<string, boolean>>} usedTiles Used tiles.
   * @param {import("../../source/Tile.js").default} tileSource Tile source.
   * @param {import('../../Tile.js').default} tile Tile.
   * @protected
   */
  updateUsedTiles(t, i, a) {
    const u = ue(i);
    u in t || (t[u] = {}), t[u][a.getKey()] = !0;
  }
}
const X1 = {
  PRELOAD: "preload",
  USE_INTERIM_TILES_ON_ERROR: "useInterimTilesOnError"
};
class yz extends AS {
  /**
   * @param {Options<TileSourceType>} [options] Tile layer options.
   */
  constructor(t) {
    t = t || {};
    const i = Object.assign({}, t), a = t.cacheSize;
    delete t.cacheSize, delete i.preload, delete i.useInterimTilesOnError, super(i), this.on, this.once, this.un, this.cacheSize_ = a, this.setPreload(t.preload !== void 0 ? t.preload : 0), this.setUseInterimTilesOnError(
      t.useInterimTilesOnError !== void 0 ? t.useInterimTilesOnError : !0
    );
  }
  /**
   * @return {number|undefined} The suggested cache size
   * @protected
   */
  getCacheSize() {
    return this.cacheSize_;
  }
  /**
   * Return the level as number to which we will preload tiles up to.
   * @return {number} The level to preload tiles up to.
   * @observable
   * @api
   */
  getPreload() {
    return (
      /** @type {number} */
      this.get(X1.PRELOAD)
    );
  }
  /**
   * Set the level as number to which we will preload tiles up to.
   * @param {number} preload The level to preload tiles up to.
   * @observable
   * @api
   */
  setPreload(t) {
    this.set(X1.PRELOAD, t);
  }
  /**
   * Deprecated.  Whether we use interim tiles on error.
   * @return {boolean} Use interim tiles on error.
   * @observable
   * @api
   */
  getUseInterimTilesOnError() {
    return (
      /** @type {boolean} */
      this.get(X1.USE_INTERIM_TILES_ON_ERROR)
    );
  }
  /**
   * Deprecated.  Set whether we use interim tiles on error.
   * @param {boolean} useInterimTilesOnError Use interim tiles on error.
   * @observable
   * @api
   */
  setUseInterimTilesOnError(t) {
    this.set(X1.USE_INTERIM_TILES_ON_ERROR, t);
  }
  /**
   * Get data for a pixel location.  The return type depends on the source data.  For image tiles,
   * a four element RGBA array will be returned.  For data tiles, the array length will match the
   * number of bands in the dataset.  For requests outside the layer extent, `null` will be returned.
   * Data for a image tiles can only be retrieved if the source's `crossOrigin` property is set.
   *
   * ```js
   * // display layer data on every pointer move
   * map.on('pointermove', (event) => {
   *   console.log(layer.getData(event.pixel));
   * });
   * ```
   * @param {import("../pixel").Pixel} pixel Pixel.
   * @return {Uint8ClampedArray|Uint8Array|Float32Array|DataView|null} Pixel data.
   * @api
   * @override
   */
  getData(t) {
    return super.getData(t);
  }
}
class pz extends yz {
  /**
   * @param {import("./BaseTile.js").Options<TileSourceType>} [options] Tile layer options.
   */
  constructor(t) {
    super(t);
  }
  /**
   * @override
   */
  createRenderer() {
    return new mz(this, {
      cacheSize: this.getCacheSize()
    });
  }
}
let Vm = null, f_ = null, fu = null;
function _z(r) {
  nD();
  const t = new xA({
    layers: [new pz({
      source: new dz(),
      zIndex: 0
    })],
    target: r,
    view: new Wo({
      center: [13.068307772123394, 43.139407493133405],
      zoom: 14
    }),
    controls: []
  });
  return t.addLayer(new MS({
    source: new MT(),
    style: (i) => {
      const a = i.getGeometry();
      i.get("name");
      const u = i.get("highlighted") === !0;
      if (a instanceof va)
        return new pa({
          stroke: new pu({ color: "red", width: 2 }),
          fill: new Qc({
            color: u ? "rgba(255, 255, 0, 0.4)" : "rgba(0, 0, 0, 0.1)"
          })
          // text: new Text({
          //     text: name,
          //     font: '8px sans-serif',
          //     fill: new Fill({ color: '#000' }),
          //     stroke: new Stroke({ color: '#fff', width: 2 }),
          //     textAlign: 'center',
          //     textBaseline: 'middle',
          //     overflow: true,
          // }),
        });
      if (a instanceof to) {
        const h = [];
        return h.push(new pa({
          stroke: new pu({
            color: u ? "rgb(255, 255, 0)" : "rgba(255, 0, 0, 0.2)",
            width: 3
          })
          // text: new Text({
          //     text: name,
          //     font: '8px sans-serif',
          //     fill: new Fill({ color: '#000' }),
          //     stroke: new Stroke({ color: '#fff', width: 2 }),
          //     textAlign: 'center',
          //     textBaseline: 'middle',
          //     overflow: true,
          // }),
        })), h;
      }
    }
  })), t;
}
function $c(r) {
  return r.getLayers().getArray().find(
    (i) => i instanceof MS && i.getSource() instanceof MT
  ) || null;
}
function Jb(r, t) {
  var a;
  const i = (a = $c(r)) == null ? void 0 : a.getSource();
  if (i)
    return i.getFeatures().find((u) => u.get("name") === t);
}
function i2(r, t) {
  var a;
  const i = (a = $c(r)) == null ? void 0 : a.getSource();
  if (i) {
    const u = Jb(r, t);
    u && i.removeFeature(u);
  }
}
function l2(r) {
  var i;
  const t = (i = $c(r)) == null ? void 0 : i.getSource();
  t && t.clear();
}
function OA(r, t) {
  var i, a;
  (a = (i = $c(r)) == null ? void 0 : i.getSource()) == null || a.getFeatures().forEach((u) => {
    u.get("name") === t && u.set("highlighted", !0);
  });
}
function LA(r, t) {
  var i, a;
  (a = (i = $c(r)) == null ? void 0 : i.getSource()) == null || a.getFeatures().forEach((u) => {
    u.get("name") === t && u.set("highlighted", !1);
  });
}
function vz(r) {
  const t = $c(r).getSource();
  Vm = new ML({
    source: t,
    type: "Polygon"
  }), f_ = new NL({
    source: t
  }), r.addInteraction(Vm), r.addInteraction(f_), Vm.on("drawend", (i) => {
    const a = "Place_" + Math.random().toString(36).substring(2, 15);
    i.feature.set("name", a);
    const h = {
      id: a,
      name: a,
      coordinates: i.target.sketchLineCoords_,
      attributes: []
    };
    zt.getState().addPlace(h);
  });
}
function Sz(r, t, i) {
  const a = $c(r).getSource(), u = new cs({
    geometry: new va([[...i]]),
    name: t
  });
  a.addFeature(u);
}
function Ez(r) {
  const t = [];
  fu = new VT({
    condition: aL,
    filter: (i) => i.getGeometry() instanceof va
  }), r.addInteraction(fu), fu.on("select", (i) => {
    const a = i.selected[0];
    if (a && !t.includes(a) && t.push(a), t.length === 2) {
      const [u, h] = t, g = u.getProperties().name, y = h.getProperties().name;
      if (!g || !y)
        console.warn("Missing IDs on features");
      else {
        const _ = "Edge_" + Math.random().toString(36).substring(2, 15), b = {
          id: _,
          name: _,
          source: g,
          target: y,
          attributes: []
        };
        zt.getState().edges.some((E) => E.source === b.source && E.target === b.target) ? alert("Edge with same source and target already exists.") : (zt.getState().addEdge(b), zA(r, u, h, _));
      }
      t.length = 0, fu == null || fu.getFeatures().clear();
    }
  });
}
function zA(r, t, i, a) {
  var _, b, E;
  const u = (_ = t.getGeometry()) == null ? void 0 : _.getInteriorPoint().getCoordinates(), h = (b = i.getGeometry()) == null ? void 0 : b.getInteriorPoint().getCoordinates();
  if (!u || !h) return;
  const g = new cs({
    geometry: new to([u, h])
  });
  g.set("name", a);
  const y = new cs({
    geometry: new gu(h)
  });
  (E = $c(r).getSource()) == null || E.addFeatures([g, y]);
}
function bz(r) {
  Vm && (r.removeInteraction(Vm), Vm = null), f_ && (r.removeInteraction(f_), f_ = null), fu && (r.removeInteraction(fu), fu = null);
}
const zt = lw((r, t) => ({
  mapInstance: new xA(),
  setMapInstance: (i) => r({ mapInstance: i }),
  isEditable: !0,
  setEditable: (i) => r({ isEditable: i }),
  activeTool: "hand",
  setActiveTool: (i) => {
    bz(t().mapInstance), i === "place" ? vz(t().mapInstance) : i === "edge" && Ez(t().mapInstance), r({ activeTool: i });
  },
  physicalPlaces: [],
  logicalPlaces: [],
  edges: [],
  views: [],
  addPlace: (i) => r((a) => ({
    physicalPlaces: [...a.physicalPlaces, i]
  })),
  updatePlace: (i, a) => r((u) => ({
    physicalPlaces: u.physicalPlaces.map(
      (h) => h.id === i ? { ...h, ...a } : h
    )
  })),
  removePlace: (i) => {
    i2(t().mapInstance, i), r((a) => ({
      physicalPlaces: a.physicalPlaces.filter((u) => u.id !== i)
    })), t().edges.forEach((a) => {
      (a.source === i || a.target === i) && t().removeEdge(a.id);
    });
  },
  addEdge: (i) => r((a) => ({
    edges: [...a.edges, i]
  })),
  updateEdge: (i, a) => r((u) => ({
    edges: u.edges.map(
      (h) => h.id === i ? { ...h, ...a } : h
    )
  })),
  removeEdge: (i) => {
    i2(t().mapInstance, i), r((a) => ({
      edges: a.edges.filter((u) => u.id !== i)
    }));
  },
  addLogicalPlace: (i) => r((a) => ({
    logicalPlaces: [...a.logicalPlaces, i]
  })),
  updateLogicalPlace: (i, a) => r((u) => ({
    logicalPlaces: u.logicalPlaces.map(
      (h) => h.id === i ? { ...h, ...a } : h
    )
  })),
  removeLogicalPlace: (i) => r((a) => ({
    logicalPlaces: a.logicalPlaces.filter((u) => u.id !== i)
  })),
  addView: (i) => r((a) => ({
    views: [...a.views, i]
  })),
  updateView: (i, a) => r((u) => ({
    views: u.views.map(
      (h) => h.id === i ? { ...h, ...a } : h
    )
  })),
  removeView: (i) => r((a) => ({
    views: a.views.filter((u) => u.id !== i)
  })),
  setModel: (i) => {
    l2(t().mapInstance), r(() => ({
      physicalPlaces: i.physicalPlaces,
      edges: i.edges,
      logicalPlaces: i.logicalPlaces,
      views: i.views
    })), i.physicalPlaces.forEach((a) => {
      Sz(t().mapInstance, a.id, a.coordinates);
    }), i.edges.forEach((a) => {
      const u = Jb(t().mapInstance, a.source), h = Jb(t().mapInstance, a.target);
      u && h && zA(t().mapInstance, u, h, a.id);
    });
  },
  clearModel: () => {
    l2(t().mapInstance), r(() => ({
      physicalPlaces: [],
      logicalPlaces: [],
      edges: [],
      views: []
    }));
  }
})), Tz = () => {
  const r = zt((u) => u.clearModel), t = zt((u) => u.setModel), i = () => {
    const u = document.createElement("input");
    u.type = "file", u.accept = ".json", u.onchange = (h) => {
      var y;
      const g = (y = h.target.files) == null ? void 0 : y[0];
      if (g) {
        const _ = new FileReader();
        _.onload = (b) => {
          var E;
          try {
            const x = JSON.parse((E = b.target) == null ? void 0 : E.result);
            t(x);
          } catch (x) {
            console.log("Error parsing JSON:", x);
          }
        }, _.readAsText(g);
      }
    }, u.click();
  }, a = () => {
    const u = {
      physicalPlaces: zt.getState().physicalPlaces,
      edges: zt.getState().edges.map((_) => {
        var b, E;
        return _.name = ((b = zt.getState().physicalPlaces.find((x) => x.id === _.source)) == null ? void 0 : b.name) + ">" + ((E = zt.getState().physicalPlaces.find((x) => x.id === _.target)) == null ? void 0 : E.name), _;
      }),
      logicalPlaces: zt.getState().logicalPlaces,
      views: zt.getState().views
    }, h = new Blob([JSON.stringify(u, null, 2)], { type: "application/json" }), g = URL.createObjectURL(h), y = document.createElement("a");
    y.href = g, y.download = "model.json", y.click(), URL.revokeObjectURL(g);
  };
  return /* @__PURE__ */ ot.jsxs("nav", { className: "navbar navbar-dark bg-dark px-3 d-flex justify-content-between align-items-center", children: [
    /* @__PURE__ */ ot.jsx("span", { className: "navbar-brand mb-0 h1", children: "🌎 BPEnv Modeler" }),
    /* @__PURE__ */ ot.jsxs("div", { className: "btn-group", children: [
      /* @__PURE__ */ ot.jsx("button", { className: "btn btn-outline-light btn-sm", onClick: i, children: "⬆️" }),
      /* @__PURE__ */ ot.jsx("button", { className: "btn btn-outline-light btn-sm", onClick: a, children: "⬇️" }),
      /* @__PURE__ */ ot.jsx("button", { className: "btn btn-outline-light btn-sm", onClick: () => {
        confirm("Are you sure you want to clear the model?") && r();
      }, children: "🗑️" })
    ] })
  ] });
};
var NA = {
  color: void 0,
  size: void 0,
  className: void 0,
  style: void 0,
  attr: void 0
}, a2 = ql.createContext && /* @__PURE__ */ ql.createContext(NA), xz = ["attr", "size", "title"];
function Cz(r, t) {
  if (r == null) return {};
  var i = Rz(r, t), a, u;
  if (Object.getOwnPropertySymbols) {
    var h = Object.getOwnPropertySymbols(r);
    for (u = 0; u < h.length; u++)
      a = h[u], !(t.indexOf(a) >= 0) && Object.prototype.propertyIsEnumerable.call(r, a) && (i[a] = r[a]);
  }
  return i;
}
function Rz(r, t) {
  if (r == null) return {};
  var i = {};
  for (var a in r)
    if (Object.prototype.hasOwnProperty.call(r, a)) {
      if (t.indexOf(a) >= 0) continue;
      i[a] = r[a];
    }
  return i;
}
function lS() {
  return lS = Object.assign ? Object.assign.bind() : function(r) {
    for (var t = 1; t < arguments.length; t++) {
      var i = arguments[t];
      for (var a in i)
        Object.prototype.hasOwnProperty.call(i, a) && (r[a] = i[a]);
    }
    return r;
  }, lS.apply(this, arguments);
}
function s2(r, t) {
  var i = Object.keys(r);
  if (Object.getOwnPropertySymbols) {
    var a = Object.getOwnPropertySymbols(r);
    t && (a = a.filter(function(u) {
      return Object.getOwnPropertyDescriptor(r, u).enumerable;
    })), i.push.apply(i, a);
  }
  return i;
}
function aS(r) {
  for (var t = 1; t < arguments.length; t++) {
    var i = arguments[t] != null ? arguments[t] : {};
    t % 2 ? s2(Object(i), !0).forEach(function(a) {
      Az(r, a, i[a]);
    }) : Object.getOwnPropertyDescriptors ? Object.defineProperties(r, Object.getOwnPropertyDescriptors(i)) : s2(Object(i)).forEach(function(a) {
      Object.defineProperty(r, a, Object.getOwnPropertyDescriptor(i, a));
    });
  }
  return r;
}
function Az(r, t, i) {
  return t = Mz(t), t in r ? Object.defineProperty(r, t, { value: i, enumerable: !0, configurable: !0, writable: !0 }) : r[t] = i, r;
}
function Mz(r) {
  var t = wz(r, "string");
  return typeof t == "symbol" ? t : t + "";
}
function wz(r, t) {
  if (typeof r != "object" || !r) return r;
  var i = r[Symbol.toPrimitive];
  if (i !== void 0) {
    var a = i.call(r, t);
    if (typeof a != "object") return a;
    throw new TypeError("@@toPrimitive must return a primitive value.");
  }
  return (t === "string" ? String : Number)(r);
}
function UA(r) {
  return r && r.map((t, i) => /* @__PURE__ */ ql.createElement(t.tag, aS({
    key: i
  }, t.attr), UA(t.child)));
}
function wS(r) {
  return (t) => /* @__PURE__ */ ql.createElement(Dz, lS({
    attr: aS({}, r.attr)
  }, t), UA(r.child));
}
function Dz(r) {
  var t = (i) => {
    var {
      attr: a,
      size: u,
      title: h
    } = r, g = Cz(r, xz), y = u || i.size || "1em", _;
    return i.className && (_ = i.className), r.className && (_ = (_ ? _ + " " : "") + r.className), /* @__PURE__ */ ql.createElement("svg", lS({
      stroke: "currentColor",
      fill: "currentColor",
      strokeWidth: "0"
    }, i.attr, a, g, {
      className: _,
      style: aS(aS({
        color: r.color || i.color
      }, i.style), r.style),
      height: y,
      width: y,
      xmlns: "http://www.w3.org/2000/svg"
    }), h && /* @__PURE__ */ ql.createElement("title", null, h), r.children);
  };
  return a2 !== void 0 ? /* @__PURE__ */ ql.createElement(a2.Consumer, null, (i) => t(i)) : t(NA);
}
function Oz(r) {
  return wS({ attr: { viewBox: "0 0 448 512" }, child: [{ tag: "path", attr: { d: "M190.5 66.9l22.2-22.2c9.4-9.4 24.6-9.4 33.9 0L441 239c9.4 9.4 9.4 24.6 0 33.9L246.6 467.3c-9.4 9.4-24.6 9.4-33.9 0l-22.2-22.2c-9.5-9.5-9.3-25 .4-34.3L311.4 296H24c-13.3 0-24-10.7-24-24v-32c0-13.3 10.7-24 24-24h287.4L190.9 101.2c-9.8-9.3-10-24.8-.4-34.3z" }, child: [] }] })(r);
}
function Lz(r) {
  return wS({ attr: { viewBox: "0 0 448 512" }, child: [{ tag: "path", attr: { d: "M408.781 128.007C386.356 127.578 368 146.36 368 168.79V256h-8V79.79c0-22.43-18.356-41.212-40.781-40.783C297.488 39.423 280 57.169 280 79v177h-8V40.79C272 18.36 253.644-.422 231.219.007 209.488.423 192 18.169 192 40v216h-8V80.79c0-22.43-18.356-41.212-40.781-40.783C121.488 40.423 104 58.169 104 80v235.992l-31.648-43.519c-12.993-17.866-38.009-21.817-55.877-8.823-17.865 12.994-21.815 38.01-8.822 55.877l125.601 172.705A48 48 0 0 0 172.073 512h197.59c22.274 0 41.622-15.324 46.724-37.006l26.508-112.66a192.011 192.011 0 0 0 5.104-43.975V168c.001-21.831-17.487-39.577-39.218-39.993z" }, child: [] }] })(r);
}
function zz(r) {
  return wS({ attr: { viewBox: "0 0 512 512" }, child: [{ tag: "path", attr: { d: "M256 8C119 8 8 119 8 256s111 248 248 248 248-111 248-248S393 8 256 8zm0 448c-110.5 0-200-89.5-200-200S145.5 56 256 56s200 89.5 200 200-89.5 200-200 200z" }, child: [] }] })(r);
}
const Nz = () => {
  const r = zt((g) => g.activeTool), t = zt((g) => g.mapInstance), i = zt((g) => g.setActiveTool), a = () => {
    t && i("hand");
  }, u = () => {
    t && i("place");
  }, h = () => {
    t && i("edge");
  };
  return /* @__PURE__ */ ot.jsxs("div", { className: "toolbar draggable", children: [
    /* @__PURE__ */ ot.jsx("button", { className: r === "hand" ? "toolbar-btn" : "toolbar-btn-selected", onClick: a, children: /* @__PURE__ */ ot.jsx(Lz, {}) }),
    /* @__PURE__ */ ot.jsx("button", { className: r === "place" ? "toolbar-btn" : "toolbar-btn-selected", onClick: u, children: /* @__PURE__ */ ot.jsx(zz, {}) }),
    /* @__PURE__ */ ot.jsx("button", { className: r === "edge" ? "toolbar-btn" : "toolbar-btn-selected", onClick: h, children: /* @__PURE__ */ ot.jsx(Oz, {}) })
  ] });
}, GA = ({ title: r, onClose: t, children: i, footer: a }) => /* @__PURE__ */ ot.jsx("div", { className: "modal d-block", tabIndex: -1, role: "dialog", children: /* @__PURE__ */ ot.jsx("div", { className: "modal-dialog", role: "document", children: /* @__PURE__ */ ot.jsxs("div", { className: "modal-content bg-dark text-white", children: [
  /* @__PURE__ */ ot.jsxs("div", { className: "modal-header", children: [
    /* @__PURE__ */ ot.jsx("h5", { className: "modal-title", children: r }),
    /* @__PURE__ */ ot.jsx(
      "button",
      {
        type: "button",
        className: "btn-close btn-close-white",
        onClick: t,
        "aria-label": "Close"
      }
    )
  ] }),
  /* @__PURE__ */ ot.jsx("div", { className: "modal-body", children: i }),
  a && /* @__PURE__ */ ot.jsx("div", { className: "modal-footer", children: a })
] }) }) }), HA = ({ initialPlace: r, onSave: t, showAsModal: i = !1, onClose: a }) => {
  const [u, h] = Fl((r == null ? void 0 : r.name) ?? ""), [g, y] = Fl(
    (r == null ? void 0 : r.conditions) ?? [{ attribute: "", operator: "==", value: "" }]
  ), _ = zt((N) => N.logicalPlaces), b = zt((N) => N.addLogicalPlace), E = zt((N) => N.updateLogicalPlace), x = [
    ...new Set(zt.getState().physicalPlaces.flatMap((N) => Object.keys(N.attributes)))
  ], R = () => {
    const N = (r == null ? void 0 : r.id) ?? "Lp_" + Math.random().toString(36).substring(2, 8), O = {
      id: N,
      name: u,
      conditions: g,
      operator: "AND",
      attributes: {}
    };
    r ? E(N, O) : b(O), h(""), y([{ attribute: "", operator: "==", value: "" }]), t == null || t(), a == null || a();
  }, w = () => y([...g, { attribute: "", operator: "==", value: "" }]), L = (N) => y(g.filter((O, U) => U !== N)), H = /* @__PURE__ */ ot.jsxs(ot.Fragment, { children: [
    /* @__PURE__ */ ot.jsx("div", { className: "mb-2", children: /* @__PURE__ */ ot.jsx(
      "input",
      {
        className: "form-control",
        placeholder: "Logical Place Name",
        value: u,
        onChange: (N) => h(N.target.value)
      }
    ) }),
    /* @__PURE__ */ ot.jsxs("div", { className: "mb-2", children: [
      /* @__PURE__ */ ot.jsx("label", { className: "form-label", children: "Conditions" }),
      g.map((N, O) => /* @__PURE__ */ ot.jsxs("div", { className: "d-flex align-items-center gap-2 mb-2", children: [
        /* @__PURE__ */ ot.jsxs(
          "select",
          {
            className: "form-select",
            style: { width: "35%" },
            value: N.attribute,
            onChange: (U) => {
              const I = [...g];
              I[O] = { ...I[O], attribute: U.target.value }, y(I);
            },
            children: [
              /* @__PURE__ */ ot.jsx("option", { value: "", children: "Select attribute" }),
              x.map((U) => /* @__PURE__ */ ot.jsx("option", { value: U, children: U }, U))
            ]
          }
        ),
        /* @__PURE__ */ ot.jsxs(
          "select",
          {
            className: "form-select",
            style: { width: "20%" },
            value: N.operator,
            onChange: (U) => {
              const I = [...g];
              I[O] = { ...I[O], operator: U.target.value }, y(I);
            },
            children: [
              /* @__PURE__ */ ot.jsx("option", { value: "==", children: "==" }),
              /* @__PURE__ */ ot.jsx("option", { value: "!=", children: "!=" }),
              /* @__PURE__ */ ot.jsx("option", { value: "<", children: "<" }),
              /* @__PURE__ */ ot.jsx("option", { value: ">", children: ">" })
            ]
          }
        ),
        /* @__PURE__ */ ot.jsx(
          "input",
          {
            className: "form-control",
            style: { width: "35%" },
            placeholder: "Value",
            value: N.value,
            onChange: (U) => {
              const I = [...g];
              I[O] = { ...I[O], value: U.target.value }, y(I);
            }
          }
        ),
        /* @__PURE__ */ ot.jsx("button", { className: "btn btn-outline-danger btn-sm", onClick: () => L(O), children: "-" })
      ] }, O)),
      /* @__PURE__ */ ot.jsx("button", { className: "btn btn-sm btn-outline-light w-100 mt-2", onClick: w, children: "Add Condition" })
    ] }),
    !i && /* @__PURE__ */ ot.jsx(
      "button",
      {
        className: "btn btn-sm btn-success w-100 mt-2",
        onClick: R,
        disabled: !u.trim() || _.some((N) => N.name === u && N.id !== (r == null ? void 0 : r.id)) || g.some((N) => !N.attribute || !N.value),
        children: r ? "Update Logical Place" : "Save Logical Place"
      }
    )
  ] });
  return i ? /* @__PURE__ */ ot.jsx(
    GA,
    {
      title: r ? "Edit Logical Place" : "Create Logical Place",
      onClose: a ?? (() => {
      }),
      footer: /* @__PURE__ */ ot.jsx(
        "button",
        {
          className: "btn btn-success w-100",
          onClick: R,
          disabled: !u.trim() || _.some((N) => N.name === u && N.id !== (r == null ? void 0 : r.id)) || g.some((N) => !N.attribute || !N.value),
          children: r ? "Update Logical Place" : "Save Logical Place"
        }
      ),
      children: H
    }
  ) : /* @__PURE__ */ ot.jsx("div", { children: H });
}, Uz = ({ item: r }) => {
  const [t, i] = Fl(!1), a = zt((_) => _.physicalPlaces), u = zt((_) => _.removeLogicalPlace), h = zt((_) => _.mapInstance), g = zt((_) => _.isEditable);
  $b(null);
  const y = (_, b) => b.filter(
    (E) => _.conditions.every((x) => {
      const R = E.attributes[x.attribute];
      switch (x.operator) {
        case "==":
          return R == x.value;
        case "!=":
          return R != x.value;
        case "<":
          return parseFloat(R) < parseFloat(x.value);
        case ">":
          return parseFloat(R) > parseFloat(x.value);
        default:
          return !1;
      }
    })
  ).map((E) => E.id);
  return /* @__PURE__ */ ot.jsxs(ot.Fragment, { children: [
    /* @__PURE__ */ ot.jsxs(
      "li",
      {
        className: "list-group-item bg-dark text-white d-flex justify-content-between align-items-center",
        onMouseEnter: () => {
          y(r, a).forEach((b) => OA(h, b));
        },
        onMouseLeave: () => {
          y(r, a).forEach((b) => LA(h, b));
        },
        children: [
          /* @__PURE__ */ ot.jsx("span", { children: r.name }),
          /* @__PURE__ */ ot.jsxs("div", { className: "btn-group btn-group-sm", children: [
            /* @__PURE__ */ ot.jsx(
              "button",
              {
                className: "btn btn-outline-light p-1 me-1",
                onClick: () => i(!0),
                title: "Edit Logical Place",
                hidden: !g,
                children: "✎"
              }
            ),
            /* @__PURE__ */ ot.jsx(
              "button",
              {
                className: "btn btn-outline-danger p-1",
                onClick: () => u(r.id),
                title: "Delete Logical Place",
                hidden: !g,
                children: "x"
              }
            )
          ] })
        ]
      }
    ),
    t && /* @__PURE__ */ ot.jsx("div", { className: "modal d-block", tabIndex: -1, role: "dialog", children: /* @__PURE__ */ ot.jsx("div", { className: "modal-dialog", role: "document", children: /* @__PURE__ */ ot.jsxs("div", { className: "modal-content bg-dark text-white", children: [
      /* @__PURE__ */ ot.jsxs("div", { className: "modal-header", children: [
        /* @__PURE__ */ ot.jsx("h5", { className: "modal-title", children: "Edit Logical Place" }),
        /* @__PURE__ */ ot.jsx(
          "button",
          {
            type: "button",
            className: "btn-close btn-close-white",
            "aria-label": "Close",
            onClick: () => i(!1)
          }
        )
      ] }),
      /* @__PURE__ */ ot.jsx("div", { className: "modal-body", children: /* @__PURE__ */ ot.jsx(
        HA,
        {
          initialPlace: r,
          onSave: () => i(!1)
        }
      ) })
    ] }) }) })
  ] });
}, YA = ({
  onClose: r,
  initialView: t
}) => {
  const [i, a] = Fl((t == null ? void 0 : t.name) ?? ""), [u, h] = Fl(
    t ? Object.entries(t.aggregations).map(([O, U]) => ({ attribute: O, operation: U })) : [{ attribute: "", operation: "MIN" }]
  ), [g, y] = Fl(
    (t == null ? void 0 : t.logicalPlaces) ?? []
  ), _ = zt((O) => O.addView), b = zt((O) => O.logicalPlaces), E = zt((O) => O.views), x = [
    ...new Set(zt.getState().physicalPlaces.flatMap((O) => Object.keys(O.attributes)))
  ], R = () => h([...u, { attribute: "", operation: "MIN" }]), w = (O) => h(u.filter((U, I) => I !== O)), L = (O, U) => h(
    u.map((I, X) => X === O ? { ...I, ...U } : I)
  ), H = (O) => {
    y(
      (U) => U.includes(O) ? U.filter((I) => I !== O) : [...U, O]
    );
  }, N = () => {
    const O = {
      id: (t == null ? void 0 : t.id) ?? "View_" + Math.random().toString(36).substring(2, 8),
      name: i,
      logicalPlaces: g,
      aggregations: Object.fromEntries(
        u.filter((U) => U.attribute && U.operation).map((U) => [U.attribute, U.operation])
      )
    };
    if (t) {
      const U = E.map((I) => I.id === O.id ? O : I);
      zt.setState({ views: U });
    } else
      _(O);
    zt.setState((U) => ({
      logicalPlaces: U.logicalPlaces.map((I) => g.includes(I.id) ? {
        ...I,
        attributes: {
          ...u.filter((X) => X.attribute && X.operation).reduce((X, Z) => (X[Z.attribute] = Z.operation, X), {})
        }
      } : I)
    })), r();
  };
  return /* @__PURE__ */ ot.jsxs(
    GA,
    {
      title: t ? "Edit View" : "Create New View",
      onClose: r,
      footer: /* @__PURE__ */ ot.jsxs(ot.Fragment, { children: [
        /* @__PURE__ */ ot.jsx("button", { type: "button", className: "btn btn-secondary", onClick: r, children: "Close" }),
        /* @__PURE__ */ ot.jsx(
          "button",
          {
            type: "button",
            className: "btn btn-success",
            onClick: N,
            disabled: !i.trim() || g.length === 0 || E.some(
              (O) => O.name === i && O.id !== (t == null ? void 0 : t.id)
            ),
            children: "Save View"
          }
        )
      ] }),
      children: [
        /* @__PURE__ */ ot.jsx("div", { className: "mb-3", children: /* @__PURE__ */ ot.jsx(
          "input",
          {
            className: "form-control",
            placeholder: "View Name",
            value: i,
            onChange: (O) => a(O.target.value)
          }
        ) }),
        /* @__PURE__ */ ot.jsx("div", { children: /* @__PURE__ */ ot.jsxs("div", { className: "mb-3", children: [
          /* @__PURE__ */ ot.jsx("label", { className: "form-label", children: "Aggregations" }),
          u.map((O, U) => /* @__PURE__ */ ot.jsxs("div", { className: "d-flex align-items-center gap-2 mb-2", children: [
            /* @__PURE__ */ ot.jsxs(
              "select",
              {
                className: "form-select",
                style: { width: "50%" },
                value: O.attribute,
                onChange: (I) => L(U, { attribute: I.target.value }),
                children: [
                  /* @__PURE__ */ ot.jsx("option", { value: "", children: "Select attribute" }),
                  x.map((I) => /* @__PURE__ */ ot.jsx("option", { value: I, children: I }, I))
                ]
              }
            ),
            /* @__PURE__ */ ot.jsxs(
              "select",
              {
                className: "form-select",
                style: { width: "30%" },
                value: O.operation,
                onChange: (I) => L(U, { operation: I.target.value }),
                children: [
                  /* @__PURE__ */ ot.jsx("option", { value: "MIN", children: "MIN" }),
                  /* @__PURE__ */ ot.jsx("option", { value: "MAX", children: "MAX" }),
                  /* @__PURE__ */ ot.jsx("option", { value: "AVG", children: "AVG" }),
                  /* @__PURE__ */ ot.jsx("option", { value: "SUM", children: "SUM" }),
                  /* @__PURE__ */ ot.jsx("option", { value: "OR", children: "OR" }),
                  /* @__PURE__ */ ot.jsx("option", { value: "AND", children: "AND" })
                ]
              }
            ),
            /* @__PURE__ */ ot.jsx(
              "button",
              {
                className: "btn btn-outline-danger btn-sm",
                onClick: () => w(U),
                children: "-"
              }
            )
          ] }, U)),
          /* @__PURE__ */ ot.jsx("button", { className: "btn btn-sm btn-outline-light w-100 mt-2", onClick: R, children: "Add Aggregation" })
        ] }) }),
        /* @__PURE__ */ ot.jsxs("div", { className: "mb-3", children: [
          /* @__PURE__ */ ot.jsx("label", { className: "form-label", children: "Select Logical Places" }),
          b.map((O) => /* @__PURE__ */ ot.jsxs("div", { className: "form-check", children: [
            /* @__PURE__ */ ot.jsx(
              "input",
              {
                className: "form-check-input",
                type: "checkbox",
                checked: g.includes(O.id),
                onChange: () => H(O.id),
                id: `lp-${O.id}`
              }
            ),
            /* @__PURE__ */ ot.jsx("label", { className: "form-check-label", htmlFor: `lp-${O.id}`, children: O.name })
          ] }, O.id))
        ] }),
        /* @__PURE__ */ ot.jsxs("div", { className: "mb-3", children: [
          /* @__PURE__ */ ot.jsx("label", { className: "form-label", children: "Create New Logical Place" }),
          /* @__PURE__ */ ot.jsx(HA, {})
        ] })
      ]
    }
  );
}, Gz = ({ view: r }) => {
  const [t, i] = Fl(!1), [a, u] = Fl(!1), h = zt((x) => x.views), g = zt((x) => x.removeView), y = zt((x) => x.logicalPlaces), _ = zt((x) => x.removeLogicalPlace), b = zt((x) => x.isEditable), E = y.filter(
    (x) => r.logicalPlaces.includes(x.id)
  );
  return /* @__PURE__ */ ot.jsxs("div", { className: "mb-2", children: [
    /* @__PURE__ */ ot.jsxs(
      "div",
      {
        className: "btn btn-outline-light w-100 text-start d-flex justify-content-between align-items-center",
        onClick: () => i(!t),
        style: { cursor: "pointer" },
        children: [
          /* @__PURE__ */ ot.jsxs("span", { children: [
            t ? "▼" : "▶",
            " ",
            r.name
          ] }),
          /* @__PURE__ */ ot.jsxs("div", { className: "btn-group btn-group-sm", children: [
            /* @__PURE__ */ ot.jsx(
              "button",
              {
                className: "btn btn-sm btn-outline-light p-1 me-1",
                onClick: (x) => {
                  x.stopPropagation(), u(!0);
                },
                title: "Edit view",
                hidden: !b,
                children: "✎"
              }
            ),
            /* @__PURE__ */ ot.jsx(
              "button",
              {
                className: "btn btn-outline-danger p-1",
                onClick: (x) => {
                  x.stopPropagation(), g(r.id), r.logicalPlaces.forEach((R) => {
                    h.some(
                      (L) => L.id !== r.id && L.logicalPlaces.includes(R)
                    ) || _(R);
                  });
                },
                title: "Delete View",
                hidden: !b,
                children: "x"
              }
            )
          ] })
        ]
      }
    ),
    t && /* @__PURE__ */ ot.jsx("ul", { className: "list-group list-group-flush", children: E.map((x) => /* @__PURE__ */ ot.jsx(Uz, { item: x }, x.id)) }),
    a && /* @__PURE__ */ ot.jsx(
      YA,
      {
        initialView: r,
        onClose: () => u(!1)
      }
    )
  ] });
}, Hz = () => {
  const [r, t] = Fl(!1), i = zt((u) => u.isEditable), a = zt((u) => u.views);
  return /* @__PURE__ */ ot.jsxs("div", { className: "mb-3", children: [
    /* @__PURE__ */ ot.jsx("h5", { className: "text-white mb-3", children: "Logical Layer" }),
    a.length === 0 ? /* @__PURE__ */ ot.jsx("h6", { className: "text-secondary mb-3", children: "Nothing to see here..." }) : a.map((u) => /* @__PURE__ */ ot.jsx(Gz, { view: u }, u.id)),
    i && /* @__PURE__ */ ot.jsx(
      "button",
      {
        className: "btn btn-outline-light w-100 mt-3",
        onClick: () => t(!0),
        children: "+ Add View"
      }
    ),
    r && /* @__PURE__ */ ot.jsx(YA, { onClose: () => t(!1) })
  ] });
};
function Yz(r) {
  return wS({ attr: { fill: "currentColor", viewBox: "0 0 16 16" }, child: [{ tag: "path", attr: { d: "M5.5 5.5A.5.5 0 0 1 6 6v6a.5.5 0 0 1-1 0V6a.5.5 0 0 1 .5-.5m2.5 0a.5.5 0 0 1 .5.5v6a.5.5 0 0 1-1 0V6a.5.5 0 0 1 .5-.5m3 .5a.5.5 0 0 0-1 0v6a.5.5 0 0 0 1 0z" }, child: [] }, { tag: "path", attr: { d: "M14.5 3a1 1 0 0 1-1 1H13v9a2 2 0 0 1-2 2H5a2 2 0 0 1-2-2V4h-.5a1 1 0 0 1-1-1V2a1 1 0 0 1 1-1H6a1 1 0 0 1 1-1h2a1 1 0 0 1 1 1h3.5a1 1 0 0 1 1 1zM4.118 4 4 4.059V13a1 1 0 0 0 1 1h6a1 1 0 0 0 1-1V4.059L11.882 4zM2.5 3h11V2h-11z" }, child: [] }] })(r);
}
const jz = ({ elementId: r, type: t, initialAttributes: i }) => {
  const [a, u] = Fl(i), [h, g] = Fl({}), y = zt((O) => O.updatePlace), _ = zt((O) => O.updateEdge), b = zt((O) => O.isEditable);
  r2(() => {
    u(i), g(Object.fromEntries(Object.keys(i).map((O) => [O, O])));
  }, [i]);
  const E = (O) => {
    u(O), t === "edge" ? _ == null || _(r, { attributes: O }) : y == null || y(r, { attributes: O });
  }, x = (O, U) => O.trim() === "" || O === U || Object.prototype.hasOwnProperty.call(a, O), R = (O, U) => {
    const I = { ...a }, X = I[O];
    delete I[O], I[U] = X, E(I);
    const Z = { ...h };
    delete Z[O], Z[U] = U, g(Z);
  }, w = (O, U) => {
    if (x(U, O)) {
      g((I) => ({ ...I, [O]: O }));
      return;
    }
    R(O, U);
  }, L = (O, U) => {
    g((I) => ({ ...I, [O]: U }));
  }, H = (O, U) => {
    const I = { ...a, [O]: U };
    E(I);
  }, N = (O) => {
    const U = { ...a };
    delete U[O], E(U);
  };
  return /* @__PURE__ */ ot.jsx("div", { className: "mt-2 ms-3", children: Object.entries(a).map(([O, U], I) => /* @__PURE__ */ ot.jsxs("div", { className: "d-flex mb-1 align-items-center gap-1", children: [
    /* @__PURE__ */ ot.jsx(
      "input",
      {
        className: "form-control form-control-md border-0 bg-transparent text-white p-0 custom-placeholder no-focus-outline",
        value: h[O] ?? O,
        onChange: (X) => L(O, X.target.value),
        onBlur: (X) => w(O, X.target.value),
        placeholder: "Attribute Key",
        disabled: !b,
        spellCheck: !1
      }
    ),
    /* @__PURE__ */ ot.jsx(
      "input",
      {
        className: "form-control form-control-md border-0 bg-transparent text-white p-0 custom-placeholder no-focus-outline",
        value: U,
        onChange: (X) => H(O, X.target.value),
        onBlur: (X) => H(O, X.target.value),
        placeholder: b ? "Attribute Value" : "null",
        disabled: !b,
        spellCheck: !1
      }
    ),
    /* @__PURE__ */ ot.jsx(
      "button",
      {
        className: "btn btn-sm btn-outline-danger p-1",
        onClick: () => N(O),
        title: "Remove",
        hidden: !b,
        children: "✕"
      }
    )
  ] }, I)) });
}, o2 = ({ element: r, type: t }) => {
  const [i, a] = Fl(r.name || ""), u = zt((E) => E.mapInstance), h = zt((E) => E.isEditable);
  $b(null);
  const g = zt(t === "place" ? (E) => E.updatePlace : (E) => E.updateEdge), y = zt(t === "place" ? (E) => E.removePlace : (E) => E.removeEdge), _ = () => {
    const E = i.trim();
    if (E === "" || E === r.name) return;
    (t === "place" ? zt.getState().physicalPlaces : zt.getState().edges).some(
      (w) => w.name === E && w.id !== r.id
    ) ? a(r.name) : g == null || g(r.id, { name: E });
  }, b = () => {
    confirm(
      `Are you sure you want to delete ${r.name}?`
    ) && y(r.id);
  };
  return /* @__PURE__ */ ot.jsxs(
    "li",
    {
      className: "list-group-item bg-dark text-white",
      onMouseEnter: () => {
        OA(u, r.id);
      },
      onMouseLeave: () => {
        LA(u, r.id);
      },
      children: [
        /* @__PURE__ */ ot.jsxs("div", { className: "d-flex justify-content-between align-items-center", children: [
          /* @__PURE__ */ ot.jsx(
            "input",
            {
              className: "form-control form-control-md border-0 bg-transparent text-white p-0 custom-placeholder no-focus-outline",
              value: i,
              placeholder: t === "place" ? "Place Name" : "Edge Name",
              onChange: (E) => a(E.target.value),
              onBlur: _,
              disabled: !h,
              spellCheck: !1
            }
          ),
          /* @__PURE__ */ ot.jsxs("div", { className: "btn-group btn-group-sm", children: [
            /* @__PURE__ */ ot.jsx(
              "button",
              {
                className: "btn btn-outline-light p-1 me-1",
                onClick: () => {
                  const E = { ...r.attributes };
                  E.key = "value", g == null || g(r.id, { attributes: E });
                },
                title: "Add attribute",
                disabled: Object.keys(r.attributes).some((E) => E === "" || E === "key"),
                hidden: !h,
                children: "+"
              }
            ),
            /* @__PURE__ */ ot.jsx(
              "button",
              {
                className: "btn btn-outline-danger p-1",
                onClick: b,
                title: "Delete",
                hidden: !h,
                children: /* @__PURE__ */ ot.jsx(Yz, {})
              }
            )
          ] })
        ] }),
        /* @__PURE__ */ ot.jsx(jz, { elementId: r.id, type: t, initialAttributes: r.attributes })
      ]
    }
  );
}, Bz = () => {
  const [r, t] = Fl(!0), [i, a] = Fl(!0), u = zt((g) => g.physicalPlaces), h = zt((g) => g.edges);
  return /* @__PURE__ */ ot.jsxs("div", { className: "mb-3", children: [
    /* @__PURE__ */ ot.jsx("h5", { className: "text-white mb-3", children: "Physical Layer" }),
    /* @__PURE__ */ ot.jsxs(
      "button",
      {
        className: "btn btn-outline-light w-100 text-start mb-2",
        onClick: () => t(!r),
        children: [
          r ? "▼" : "▶",
          " Places"
        ]
      }
    ),
    r && (u.length === 0 ? /* @__PURE__ */ ot.jsx("h6", { className: "text-secondary mb-3", children: "Nothing to see here..." }) : /* @__PURE__ */ ot.jsx("ul", { className: "list-group list-group-flush mb-3", children: u.map((g) => /* @__PURE__ */ ot.jsx(o2, { element: g, type: "place" }, g.id)) })),
    /* @__PURE__ */ ot.jsxs(
      "button",
      {
        className: "btn btn-outline-light w-100 text-start mb-2",
        onClick: () => a(!i),
        children: [
          i ? "▼" : "▶",
          " Edges"
        ]
      }
    ),
    i && (h.length === 0 ? /* @__PURE__ */ ot.jsx("h6", { className: "text-secondary mb-3", children: "Nothing to see here..." }) : /* @__PURE__ */ ot.jsx("ul", { className: "list-group list-group-flush", children: h.map((g) => /* @__PURE__ */ ot.jsx(o2, { element: g, type: "edge" }, g.id)) }))
  ] });
}, Iz = () => /* @__PURE__ */ ot.jsxs("div", { className: "sidebar bg-dark text-white", children: [
  /* @__PURE__ */ ot.jsx(Hz, {}),
  /* @__PURE__ */ ot.jsx(Bz, {})
] }), Xz = () => {
  const r = $b(null), t = zt((u) => u.mapInstance), i = zt((u) => u.setMapInstance), a = zt((u) => u.isEditable);
  return r2(() => {
    if (r.current)
      return i(
        _z(r.current)
      ), () => t.setTarget(void 0);
  }, []), /* @__PURE__ */ ot.jsxs("div", { className: "map-wrapper", children: [
    /* @__PURE__ */ ot.jsx("div", { id: "map", ref: r, className: "map" }),
    a && /* @__PURE__ */ ot.jsx(Nz, {}),
    /* @__PURE__ */ ot.jsx(Iz, {})
  ] });
};
let di = null;
function kz(r, t = !1) {
  const i = document.getElementById(r);
  if (!i) throw new Error(`Container element with id "${r}" not found.`);
  tw.createRoot(i).render(
    /* @__PURE__ */ ot.jsxs("div", { style: { height: "100%" }, children: [
      !t && /* @__PURE__ */ ot.jsx(Tz, {}),
      /* @__PURE__ */ ot.jsx(Xz, {})
    ] })
  );
  function u() {
    return zt.getState().physicalPlaces;
  }
  function h() {
    return zt.getState().edges;
  }
  function g() {
    return zt.getState().logicalPlaces;
  }
  function y() {
    return zt.getState().views;
  }
  function _() {
    return {
      physicalPlaces: u(),
      edges: h(),
      logicalPlaces: g(),
      views: y()
    };
  }
  function b(R) {
    zt.getState().setModel(R);
  }
  function E() {
    return zt.getState().isEditable;
  }
  function x(R) {
    zt.getState().setEditable(R);
  }
  return di = {
    getPhysicalPlaces: u,
    getEdges: h,
    getLogicalPlaces: g,
    getViews: y,
    getModel: _,
    setModel: b,
    isEditable: E,
    setEditable: x
  }, di;
}
function Vz() {
  if (!di) throw new Error("Modeler not initialized. Call render() first.");
  return di.getPhysicalPlaces();
}
function qz() {
  if (!di) throw new Error("Modeler not initialized. Call render() first.");
  return di.getEdges();
}
function Zz() {
  if (!di) throw new Error("Modeler not initialized. Call render() first.");
  return di.getLogicalPlaces();
}
function Fz() {
  if (!di) throw new Error("Modeler not initialized. Call render() first.");
  return di.getViews();
}
function Kz() {
  if (!di) throw new Error("Modeler not initialized. Call render() first.");
  return di.getModel();
}
function Pz(r) {
  if (!di) throw new Error("Modeler not initialized. Call render() first.");
  di.setModel(r);
}
function Qz() {
  if (!di) throw new Error("Modeler not initialized. Call render() first.");
  return di.isEditable();
}
function Wz(r) {
  if (!di) throw new Error("Modeler not initialized. Call render() first.");
  di.setEditable(r);
}
const i4 = {
  render: kz,
  getPhysicalPlaces: Vz,
  getEdges: qz,
  getLogicalPlaces: Zz,
  getViews: Fz,
  getModel: Kz,
  setModel: Pz,
  isEditable: Qz,
  setEditable: Wz
};
export {
  i4 as default
};
